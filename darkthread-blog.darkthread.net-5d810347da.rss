<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
    <title><![CDATA[分析 Log 計算平均、標準差、95 百分位數並繪製圖表 - 從 C# 到 Python]]></title>
    <link>https://blog.darkthread.net/blog/python-stats-cahrt/</link>
    <itunes:title><![CDATA[分析 Log 計算平均、標準差、95 百分位數並繪製圖表 - 從 C# 到 Python]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png" medium="image"/>
    <pubDate>Sun, 25 Feb 2024 03:45:12 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 計算百分位數]]></title>
    <link>https://blog.darkthread.net/blog/calc-percentile/</link>
    <itunes:title><![CDATA[Coding4Fun - 計算百分位數]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://zh.wikipedia.org/zh-tw/%E7%99%BE%E5%88%86%E4%BD%8D%E6%95%B0">百分位數</a> (Percentile) 跟 PR (Percentile Rank 百分等級) 是統計學常用參考值，年輕同學應該很不陌生(我們那個年代聯考比較簡單，直接用分數比大小，沒這麼多名堂)，用考試分數比喻的話，百分位數是要在一百個等級中勝過幾個等級需要考幾分，或是位於某百分等級的人分數是幾分；PR 則是一個人的原始分數在團體依序被分為一百個等級的情況下，可以勝過多少等級。<a href="https://we147121.pixnet.net/blog/post/291747236">參考</a></p> <p>我最近一次看到百分位數的場合是在<a href="/blog/k6-load-testing/">壓力測試工具 - K6</a>的報告，在 avg, max 外還用 P(90)、P(95) 評估效能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638443857768971850.png"></p> <p>最近在做資料庫評測，也想自己算百分位數，就研究了一下。</p> <p>Excel 跟 Pythong NumPy 都有 Percentile 函數。Excel 的 PERCENTILE 已過時，等同 <a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">PERCENTILE.INC</a>，另外還有個 <a href="https://support.microsoft.com/zh-tw/office/percentile-exc-%E5%87%BD%E6%95%B8-bbaa7204-e9e1-4010-85bf-c31dc5dce4ba?WT.mc_id=DOP-MVP-37580">PERCENTILE.EXC</a>，二者差在 INC (Inclusive) 認為數據集的最小值和最大值分別是第 0 和第 100 百分位，然後此範圍內插值得到任何特定百分位數。EXC 則是排除型(Exclusive)，認為數據集的最小值和最大值之間的任何點都可能是相應的百分位數，但最小值和最大值本身不包括在內。實測 NumPy 的 percentile() 結果同 PERCENTILE.INC：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443856527354378.png"></p> <p>如果要自己用 C# 算，公式要怎麼寫？我決定抄 Excel 的<a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">邏輯</a>：</p> <ul><li>如果 array 是空值，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 非數值，PERCENTILE.INC 會傳回 #VALUE! 錯誤值。</li> <li>如果 K &lt; 0 或 K &gt; 1，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 不是 1/(n - 1) 的倍數，則 PERCENTILE.INC 會在陣列中內插一個中間值，以決定第 K 個百分位數的值。</li></ul> <p>寫成函式會像這樣：</p> <pre><code>var data = new List&lt;int&gt; { 2, 4, 4, 4, 6, 12, 25, 32, 48, 112 };
Console.WriteLine(GetPercentile(data, 25));
Console.WriteLine(GetPercentile(data, 50));
Console.WriteLine(GetPercentile(data, 90));
Console.WriteLine(GetPercentile(data, 95));
Console.WriteLine(GetPercentile(data, 99));
Console.WriteLine(GetPercentile(data, 100));
double GetPercentile(IEnumerable&lt;int&gt; data, float percent)
{
if (data == null || !data.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var sorted = data.OrderBy(x =&gt; x).ToArray();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
</code></pre> <p>測試結果與 Excel、NumPy 一致，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638443856529608626.png"></p> <p data-slug="calc-percentile" data-fbimg="https://cdn.pixabay.com/photo/2016/03/05/20/00/accountant-1238598_640.jpg#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://zh.wikipedia.org/zh-tw/%E7%99%BE%E5%88%86%E4%BD%8D%E6%95%B0">百分位數</a> (Percentile) 跟 PR (Percentile Rank 百分等級) 是統計學常用參考值，年輕同學應該很不陌生(我們那個年代聯考比較簡單，直接用分數比大小，沒這麼多名堂)，用考試分數比喻的話，百分位數是要在一百個等級中勝過幾個等級需要考幾分，或是位於某百分等級的人分數是幾分；PR 則是一個人的原始分數在團體依序被分為一百個等級的情況下，可以勝過多少等級。<a href="https://we147121.pixnet.net/blog/post/291747236">參考</a></p> <p>我最近一次看到百分位數的場合是在<a href="/blog/k6-load-testing/">壓力測試工具 - K6</a>的報告，在 avg, max 外還用 P(90)、P(95) 評估效能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638443857768971850.png"></p> <p>最近在做資料庫評測，也想自己算百分位數，就研究了一下。</p> <p>Excel 跟 Pythong NumPy 都有 Percentile 函數。Excel 的 PERCENTILE 已過時，等同 <a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">PERCENTILE.INC</a>，另外還有個 <a href="https://support.microsoft.com/zh-tw/office/percentile-exc-%E5%87%BD%E6%95%B8-bbaa7204-e9e1-4010-85bf-c31dc5dce4ba?WT.mc_id=DOP-MVP-37580">PERCENTILE.EXC</a>，二者差在 INC (Inclusive) 認為數據集的最小值和最大值分別是第 0 和第 100 百分位，然後此範圍內插值得到任何特定百分位數。EXC 則是排除型(Exclusive)，認為數據集的最小值和最大值之間的任何點都可能是相應的百分位數，但最小值和最大值本身不包括在內。實測 NumPy 的 percentile() 結果同 PERCENTILE.INC：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443856527354378.png"></p> <p>如果要自己用 C# 算，公式要怎麼寫？我決定抄 Excel 的<a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">邏輯</a>：</p> <ul><li>如果 array 是空值，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 非數值，PERCENTILE.INC 會傳回 #VALUE! 錯誤值。</li> <li>如果 K &lt; 0 或 K &gt; 1，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 不是 1/(n - 1) 的倍數，則 PERCENTILE.INC 會在陣列中內插一個中間值，以決定第 K 個百分位數的值。</li></ul> <p>寫成函式會像這樣：</p> <pre><code>var data = new List&lt;int&gt; { 2, 4, 4, 4, 6, 12, 25, 32, 48, 112 };
Console.WriteLine(GetPercentile(data, 25));
Console.WriteLine(GetPercentile(data, 50));
Console.WriteLine(GetPercentile(data, 90));
Console.WriteLine(GetPercentile(data, 95));
Console.WriteLine(GetPercentile(data, 99));
Console.WriteLine(GetPercentile(data, 100));
double GetPercentile(IEnumerable&lt;int&gt; data, float percent)
{
if (data == null || !data.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var sorted = data.OrderBy(x =&gt; x).ToArray();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
</code></pre> <p>測試結果與 Excel、NumPy 一致，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638443856529608626.png"></p> <p data-slug="calc-percentile" data-fbimg="https://cdn.pixabay.com/photo/2016/03/05/20/00/accountant-1238598_640.jpg#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://zh.wikipedia.org/zh-tw/%E7%99%BE%E5%88%86%E4%BD%8D%E6%95%B0">百分位數</a> (Percentile) 跟 PR (Percentile Rank 百分等級) 是統計學常用參考值，年輕同學應該很不陌生(我們那個年代聯考比較簡單，直接用分數比大小，沒這麼多名堂)，用考試分數比喻的話，百分位數是要在一百個等級中勝過幾個等級需要考幾分，或是位於某百分等級的人分數是幾分；PR 則是一個人的原始分數在團體依序被分為一百個等級的情況下，可以勝過多少等級。<a href="https://we147121.pixnet.net/blog/post/291747236">參考</a></p> <p>我最近一次看到百分位數的場合是在<a href="/blog/k6-load-testing/">壓力測試工具 - K6</a>的報告，在 avg, max 外還用 P(90)、P(95) 評估效能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638443857768971850.png"></p> <p>最近在做資料庫評測，也想自己算百分位數，就研究了一下。</p> <p>Excel 跟 Pythong NumPy 都有 Percentile 函數。Excel 的 PERCENTILE 已過時，等同 <a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">PERCENTILE.INC</a>，另外還有個 <a href="https://support.microsoft.com/zh-tw/office/percentile-exc-%E5%87%BD%E6%95%B8-bbaa7204-e9e1-4010-85bf-c31dc5dce4ba?WT.mc_id=DOP-MVP-37580">PERCENTILE.EXC</a>，二者差在 INC (Inclusive) 認為數據集的最小值和最大值分別是第 0 和第 100 百分位，然後此範圍內插值得到任何特定百分位數。EXC 則是排除型(Exclusive)，認為數據集的最小值和最大值之間的任何點都可能是相應的百分位數，但最小值和最大值本身不包括在內。實測 NumPy 的 percentile() 結果同 PERCENTILE.INC：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443856527354378.png"></p> <p>如果要自己用 C# 算，公式要怎麼寫？我決定抄 Excel 的<a href="https://support.microsoft.com/zh-tw/office/percentile-inc-%E5%87%BD%E6%95%B8-680f9539-45eb-410b-9a5e-c1355e5fe2ed?WT.mc_id=DOP-MVP-37580">邏輯</a>：</p> <ul><li>如果 array 是空值，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 非數值，PERCENTILE.INC 會傳回 #VALUE! 錯誤值。</li> <li>如果 K &lt; 0 或 K &gt; 1，PERCENTILE.INC 會傳回 #NUM! 錯誤值。</li> <li>如果 K 不是 1/(n - 1) 的倍數，則 PERCENTILE.INC 會在陣列中內插一個中間值，以決定第 K 個百分位數的值。</li></ul> <p>寫成函式會像這樣：</p> <pre><code>var data = new List&lt;int&gt; { 2, 4, 4, 4, 6, 12, 25, 32, 48, 112 };
Console.WriteLine(GetPercentile(data, 25));
Console.WriteLine(GetPercentile(data, 50));
Console.WriteLine(GetPercentile(data, 90));
Console.WriteLine(GetPercentile(data, 95));
Console.WriteLine(GetPercentile(data, 99));
Console.WriteLine(GetPercentile(data, 100));
double GetPercentile(IEnumerable&lt;int&gt; data, float percent)
{
if (data == null || !data.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var sorted = data.OrderBy(x =&gt; x).ToArray();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
</code></pre> <p>測試結果與 Excel、NumPy 一致，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638443856529608626.png"></p> <p data-slug="calc-percentile" data-fbimg="https://cdn.pixabay.com/photo/2016/03/05/20/00/accountant-1238598_640.jpg#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig3_638443857768971850.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig3_638443857768971850.png" medium="image"/>
    <pubDate>Sat, 24 Feb 2024 14:20:16 GMT</pubDate>
  </item><item>
    <title><![CDATA[【茶包射手日記】鬼問題 - YouTube 影片不停播放暫停]]></title>
    <link>https://blog.darkthread.net/blog/youtube-play-pause-loop/</link>
    <itunes:title><![CDATA[【茶包射手日記】鬼問題 - YouTube 影片不停播放暫停]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>發現我的電腦沒法正常播放 YouTube 影片，播放斷斷續續，播放操作區不斷出現消失，進一步觀察問題出在播放器不斷切換暫停跟播放狀態，永無止境：</p> <p><a href="https://www.youtube.com/watch?v=lxQN32bUIkQ">故障展示</a></p> <p>首先想到是鍵盤故障，一直按著空白格也會造成類似結果，但拔除鍵盤故障依舊，加上其他軟體使用一切正常，裁定鍵盤無保請回。</p> <p>莫非瀏覽器有問題？試了 Chrome 跟 Firefox 放 YouTube 影片，也出現不斷播放暫停的現象，所以瀏覽器是清白的。</p> <p>YouTube 網頁壞了？試了 Facebook，發現播放 Facebook 影片也出現一模一樣的現象。</p> <p>慘，難道是電腦硬體壞了？還有最近裝了什麼軟體在搞鬼？</p> <p>趕緊回想電腦最近有哪些異動，一番檢測後找到兇手：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443415937561060.jpg" alt="thumbnail"></p> <p>前陣子翻到塞在雜物堆裡的手機耳機，閒來無事串 Type-C 轉 3.5 耳機接頭當成電腦耳機用，還驚喜發現耳機的大小聲鍵跟播放暫停鍵可以用來控制電腦的媒體播放。嘿，問題就出在這裡，當插頭沒插緊時，除了可能造成一邊沒聲音，還可能錯送按鍵訊號，造成今天的鬼現象，把插頭插緊，問題消失無蹤~~</p> <p data-slug="youtube-play-pause-loop" data-fbimg="https://cdn.pixabay.com/photo/2018/10/12/10/22/white-goat-3741838_960_720.jpg#960#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>發現我的電腦沒法正常播放 YouTube 影片，播放斷斷續續，播放操作區不斷出現消失，進一步觀察問題出在播放器不斷切換暫停跟播放狀態，永無止境：</p> <p><a href="https://www.youtube.com/watch?v=lxQN32bUIkQ">故障展示</a></p> <p>首先想到是鍵盤故障，一直按著空白格也會造成類似結果，但拔除鍵盤故障依舊，加上其他軟體使用一切正常，裁定鍵盤無保請回。</p> <p>莫非瀏覽器有問題？試了 Chrome 跟 Firefox 放 YouTube 影片，也出現不斷播放暫停的現象，所以瀏覽器是清白的。</p> <p>YouTube 網頁壞了？試了 Facebook，發現播放 Facebook 影片也出現一模一樣的現象。</p> <p>慘，難道是電腦硬體壞了？還有最近裝了什麼軟體在搞鬼？</p> <p>趕緊回想電腦最近有哪些異動，一番檢測後找到兇手：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443415937561060.jpg" alt="thumbnail"></p> <p>前陣子翻到塞在雜物堆裡的手機耳機，閒來無事串 Type-C 轉 3.5 耳機接頭當成電腦耳機用，還驚喜發現耳機的大小聲鍵跟播放暫停鍵可以用來控制電腦的媒體播放。嘿，問題就出在這裡，當插頭沒插緊時，除了可能造成一邊沒聲音，還可能錯送按鍵訊號，造成今天的鬼現象，把插頭插緊，問題消失無蹤~~</p> <p data-slug="youtube-play-pause-loop" data-fbimg="https://cdn.pixabay.com/photo/2018/10/12/10/22/white-goat-3741838_960_720.jpg#960#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>發現我的電腦沒法正常播放 YouTube 影片，播放斷斷續續，播放操作區不斷出現消失，進一步觀察問題出在播放器不斷切換暫停跟播放狀態，永無止境：</p> <p><a href="https://www.youtube.com/watch?v=lxQN32bUIkQ">故障展示</a></p> <p>首先想到是鍵盤故障，一直按著空白格也會造成類似結果，但拔除鍵盤故障依舊，加上其他軟體使用一切正常，裁定鍵盤無保請回。</p> <p>莫非瀏覽器有問題？試了 Chrome 跟 Firefox 放 YouTube 影片，也出現不斷播放暫停的現象，所以瀏覽器是清白的。</p> <p>YouTube 網頁壞了？試了 Facebook，發現播放 Facebook 影片也出現一模一樣的現象。</p> <p>慘，難道是電腦硬體壞了？還有最近裝了什麼軟體在搞鬼？</p> <p>趕緊回想電腦最近有哪些異動，一番檢測後找到兇手：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638443415937561060.jpg" alt="thumbnail"></p> <p>前陣子翻到塞在雜物堆裡的手機耳機，閒來無事串 Type-C 轉 3.5 耳機接頭當成電腦耳機用，還驚喜發現耳機的大小聲鍵跟播放暫停鍵可以用來控制電腦的媒體播放。嘿，問題就出在這裡，當插頭沒插緊時，除了可能造成一邊沒聲音，還可能錯送按鍵訊號，造成今天的鬼現象，把插頭插緊，問題消失無蹤~~</p> <p data-slug="youtube-play-pause-loop" data-fbimg="https://cdn.pixabay.com/photo/2018/10/12/10/22/white-goat-3741838_960_720.jpg#960#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638443415937561060.jpg"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638443415937561060.jpg" medium="image"/>
    <pubDate>Sat, 24 Feb 2024 03:18:44 GMT</pubDate>
  </item><item>
    <title><![CDATA[【打破砂鍋】IIS 6 起就不該使用的 IISRESET，結果我用了 21 年？]]></title>
    <link>https://blog.darkthread.net/blog/stop-using-iisreset/</link>
    <itunes:title><![CDATA[【打破砂鍋】IIS 6 起就不該使用的 IISRESET，結果我用了 21 年？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>同事分享冷知識一枚，依據微軟<a href="https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/dd364308(v=ws.10)?redirectedfrom=MSDN&amp;WT.mc_id=DOP-MVP-37580#verify">官方建議</a>，我平時順手就來一發的 IISREST，是 IIS 5.0 時代的產物，IIS 6.0 後已不建議使用，登楞!!</p> <p>原文是這麼寫的：</p> <blockquote> <p>IISReset.exe is a command-line utility that was introduced in IIS 5.0 that could be used to stop, start, and restart IIS Internet services. Its use is not recommended for IIS 6.0 and is not supported in IIS 7.0 or IIS 7.5.<br> Note: Restarting or stopping IIS, or rebooting your Web server, is a severe action. When you restart the Internet service, changes to the metabase can be lost. All sessions connected to your Web server are dropped, and any data held in Web applications is lost. All Internet sites are unavailable until Internet services are restarted. For these reasons, the use of IISReset is not recommended.</p> </blockquote> <p>大意是 IISRESET 是 IIS 5.0 引進可重啟 IIS 的工具程式，不建議在 IIS 6.0 使用，且 IIS 7.0 後將不再支援。理由是重啟 IIS 或重開機是一個 Server 層級動作，可能造成 Metabase 修改遺失，網站連線會被丟棄、記憶體資料也會遺失，網站會無法使用直到服務啟動，故不建議使用。</p> <p>查了一下，IIS 6.0 始於 Windows 2003，若 IISRESET 不該用，我豈不錯用了 21 年？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638442964022201848.png"><br> IIS 版本對照表 <a href="https://learn.microsoft.com/zh-tw/lifecycle/products/internet-information-services-iis?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>不過呢，我注意到該文件的最近更新日期是 2009，適用 Windows Server 2008 R2，文章上方有警示 "We're no longer updating this content regularly."，代表文件久未更新，資訊可能已過時。我決定打破砂鍋，探索這個議題。</p> <p>文件有個 "Restarting IIS (IIS 6.)" 連結提供詳情，但超連結已失效，我找到<a href="https://www.cnblogs.com/shineqiujuan/archive/2011/03/22/1990945.html">網友留存的備份</a>，裡面提到不建議使用 IISRESET 的理由：</p> <ol><li>IIS 6.0 已新增更優雅的重啟選項：有 AppPool 可停止啟動重啟，非必要別重啟整台 IIS</li> <li>IIS 跟 FTP、NMTP、SMTP 也有關，若只是要重啟 Web，沒必要全部一起暫停服務重啟</li> <li>過往 IIS 5 必須 IISRESET 是因為 IISAdmin 跟 W3SVC 綁在一起(shared the same application space)，IIS 6 起二者為獨立 Process，IISAdmin 停止時，W3SVC 仍可執行但被通知 Metabase 來源異常中止，IIS 則會在 IISAdmin 恢復後再連上 W3SVC。</li></ol> <p>我另外查到一篇 <a href="https://www.leansentry.com/guide/reset-restart-recycle-iis/dangers-of-iisreset">IISRESET: the WORST way to restart your IIS website</a> 有更完整的闡述，給足不該用 IISREST 的理由：</p> <ol><li>過去有些 Registry 異動需重啟 IIS 套用，IIS 7+ 已能自動偵測並套用 IIS 修改，需重啟的機會已大幅減少。(註：但經驗不乏有其他設定需 IISREST 生效，例如：ODP.NET)</li> <li>IIS 6 加入 AppPool 架構，單一網站有問題重啟 AppPool 即可，沒必要整台網站重啟</li> <li>回收(Recycle) AppPool 會另起新 Process，原 Process 會等請求處理完才結束，讓客戶端無感，是更優雅的作法</li> <li>下圖是 IISRESET 的運作流程，步驟多時間長(數秒到數十秒)，中間會有一段時間使用者會看到 503：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638442964026937842.png"><br> 若外，若 IISRESET 程序失敗將可能讓 IIS 留在停機狀態。<br> 下圖是不同重啟方式的網站停機時間及網頁錯誤次數，回收 AppPool 是首選：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638442964029144971.png"><br> 若非用 IISREST 不可，以下是更嚴謹的做法： <pre><code>iisreset /stop /timeout:60
taskkill /F /FI "SERVICES eq was"
iisreset /start
</code></pre> </li> <li>IISRESET 是系統不穩、Memory Leak、網站當死、CPU 飆高等各式疑難雜症的鋸箭特效藥，但過度依賴會錯失根本解決問題的機會，埋下日後大爆炸的種子。</li></ol> <p>另外，在這篇<a href="https://techcommunity.microsoft.com/t5/iis-support-blog/iisreset-internals/ba-p/951578?WT.mc_id=DOP-MVP-37580">部落格</a>有提到 IISRESET 如遇 IISAdmin 停不掉會強制砍程序(應是導致 IIS Metadata 遺失的情境)，並中止所有使用 IISW3ADM.DLL 的程序。(用 IISRESET /NOFORCE 可改善，它只會跑 NET STOP IISADMIN /Y，停不掉就算了不會砍 Process)</p> <p>到這裡，差不多可以理解 IISRESET 的缺點：拉長服務中斷時間，降低網站可用性，但看起來並無嚴重後遺症，不到罪無可赦的程度。且十多年前 IIS 7.0+ 將不再支援 IISRESET 的計劃看來也修改了，至今 IIS 10 都還支援 IISRESET，故我認為它仍是可用選項。</p> <p>話說回來，如果目的是重啟網站，你有其他更輕巧快速的做法，例如：</p> <ul><li>PowerShell 回收特定 AppPool：<code>(Get-IISAppPool &lt;AppPoolName&gt;).Recycle()</code> <a href="https://learn.microsoft.com/en-us/dotnet/api/microsoft.web.administration.applicationpool.recycle?view=iis-dotnet&amp;WT.mc_id=DOP-MVP-37580">參考</a></li> <li>PowerShell 重啟特定 AppPool：<code>Restart-WebAppPool &lt;AppPoolName&gt;</code> 或 <code>Restart-WebAppPool (Get-Website -Name &lt;YourSiteName&gt;).ApplicationPool</code> <a href="https://learn.microsoft.com/en-us/powershell/module/webadministration/restart-webapppool?WT.mc_id=DOP-MVP-37580">參考</a></li> <li>重啟所有 AppPool：<code>NET STOP W3SVC &amp;&amp; NET START W3SVC</code> 或 PowerShell <code>Restart-Service W3SVC</code></li></ul> <p>當然，如果不在乎網站中斷，也不介意等久一點(以及發生機率不高的 Metadata 遣失)，指令短又好記是 IISRESET 的最大優點，是能少打幾個字的簡單粗暴選項。估評過缺點跟風險仍決定使用，便是思考過的取捨而非不明究理盲用，我可以接受。</p> <p>結案。</p> <p data-slug="stop-using-iisreset" data-title-en="Why is IISREST bad?" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/08/08/19/55/confused-880735_1280.jpg#1280#720">IISREST has been declared as not recommended for use after IIS6. This article explores the issues that may be caused by using IISRESET and how to properly restart IIS websites.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>同事分享冷知識一枚，依據微軟<a href="https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/dd364308(v=ws.10)?redirectedfrom=MSDN&amp;WT.mc_id=DOP-MVP-37580#verify">官方建議</a>，我平時順手就來一發的 IISREST，是 IIS 5.0 時代的產物，IIS 6.0 後已不建議使用，登楞!!</p> <p>原文是這麼寫的：</p> <blockquote> <p>IISReset.exe is a command-line utility that was introduced in IIS 5.0 that could be used to stop, start, and restart IIS Internet services. Its use is not recommended for IIS 6.0 and is not supported in IIS 7.0 or IIS 7.5.<br> Note: Restarting or stopping IIS, or rebooting your Web server, is a severe action. When you restart the Internet service, changes to the metabase can be lost. All sessions connected to your Web server are dropped, and any data held in Web applications is lost. All Internet sites are unavailable until Internet services are restarted. For these reasons, the use of IISReset is not recommended.</p> </blockquote> <p>大意是 IISRESET 是 IIS 5.0 引進可重啟 IIS 的工具程式，不建議在 IIS 6.0 使用，且 IIS 7.0 後將不再支援。理由是重啟 IIS 或重開機是一個 Server 層級動作，可能造成 Metabase 修改遺失，網站連線會被丟棄、記憶體資料也會遺失，網站會無法使用直到服務啟動，故不建議使用。</p> <p>查了一下，IIS 6.0 始於 Windows 2003，若 IISRESET 不該用，我豈不錯用了 21 年？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638442964022201848.png"><br> IIS 版本對照表 <a href="https://learn.microsoft.com/zh-tw/lifecycle/products/internet-information-services-iis?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>不過呢，我注意到該文件的最近更新日期是 2009，適用 Windows Server 2008 R2，文章上方有警示 "We're no longer updating this content regularly."，代表文件久未更新，資訊可能已過時。我決定打破砂鍋，探索這個議題。</p> <p>文件有個 "Restarting IIS (IIS 6.)" 連結提供詳情，但超連結已失效，我找到<a href="https://www.cnblogs.com/shineqiujuan/archive/2011/03/22/1990945.html">網友留存的備份</a>，裡面提到不建議使用 IISRESET 的理由：</p> <ol><li>IIS 6.0 已新增更優雅的重啟選項：有 AppPool 可停止啟動重啟，非必要別重啟整台 IIS</li> <li>IIS 跟 FTP、NMTP、SMTP 也有關，若只是要重啟 Web，沒必要全部一起暫停服務重啟</li> <li>過往 IIS 5 必須 IISRESET 是因為 IISAdmin 跟 W3SVC 綁在一起(shared the same application space)，IIS 6 起二者為獨立 Process，IISAdmin 停止時，W3SVC 仍可執行但被通知 Metabase 來源異常中止，IIS 則會在 IISAdmin 恢復後再連上 W3SVC。</li></ol> <p>我另外查到一篇 <a href="https://www.leansentry.com/guide/reset-restart-recycle-iis/dangers-of-iisreset">IISRESET: the WORST way to restart your IIS website</a> 有更完整的闡述，給足不該用 IISREST 的理由：</p> <ol><li>過去有些 Registry 異動需重啟 IIS 套用，IIS 7+ 已能自動偵測並套用 IIS 修改，需重啟的機會已大幅減少。(註：但經驗不乏有其他設定需 IISREST 生效，例如：ODP.NET)</li> <li>IIS 6 加入 AppPool 架構，單一網站有問題重啟 AppPool 即可，沒必要整台網站重啟</li> <li>回收(Recycle) AppPool 會另起新 Process，原 Process 會等請求處理完才結束，讓客戶端無感，是更優雅的作法</li> <li>下圖是 IISRESET 的運作流程，步驟多時間長(數秒到數十秒)，中間會有一段時間使用者會看到 503：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638442964026937842.png"><br> 若外，若 IISRESET 程序失敗將可能讓 IIS 留在停機狀態。<br> 下圖是不同重啟方式的網站停機時間及網頁錯誤次數，回收 AppPool 是首選：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638442964029144971.png"><br> 若非用 IISREST 不可，以下是更嚴謹的做法： <pre><code>iisreset /stop /timeout:60
taskkill /F /FI "SERVICES eq was"
iisreset /start
</code></pre> </li> <li>IISRESET 是系統不穩、Memory Leak、網站當死、CPU 飆高等各式疑難雜症的鋸箭特效藥，但過度依賴會錯失根本解決問題的機會，埋下日後大爆炸的種子。</li></ol> <p>另外，在這篇<a href="https://techcommunity.microsoft.com/t5/iis-support-blog/iisreset-internals/ba-p/951578?WT.mc_id=DOP-MVP-37580">部落格</a>有提到 IISRESET 如遇 IISAdmin 停不掉會強制砍程序(應是導致 IIS Metadata 遺失的情境)，並中止所有使用 IISW3ADM.DLL 的程序。(用 IISRESET /NOFORCE 可改善，它只會跑 NET STOP IISADMIN /Y，停不掉就算了不會砍 Process)</p> <p>到這裡，差不多可以理解 IISRESET 的缺點：拉長服務中斷時間，降低網站可用性，但看起來並無嚴重後遺症，不到罪無可赦的程度。且十多年前 IIS 7.0+ 將不再支援 IISRESET 的計劃看來也修改了，至今 IIS 10 都還支援 IISRESET，故我認為它仍是可用選項。</p> <p>話說回來，如果目的是重啟網站，你有其他更輕巧快速的做法，例如：</p> <ul><li>PowerShell 回收特定 AppPool：<code>(Get-IISAppPool &lt;AppPoolName&gt;).Recycle()</code> <a href="https://learn.microsoft.com/en-us/dotnet/api/microsoft.web.administration.applicationpool.recycle?view=iis-dotnet&amp;WT.mc_id=DOP-MVP-37580">參考</a></li> <li>PowerShell 重啟特定 AppPool：<code>Restart-WebAppPool &lt;AppPoolName&gt;</code> 或 <code>Restart-WebAppPool (Get-Website -Name &lt;YourSiteName&gt;).ApplicationPool</code> <a href="https://learn.microsoft.com/en-us/powershell/module/webadministration/restart-webapppool?WT.mc_id=DOP-MVP-37580">參考</a></li> <li>重啟所有 AppPool：<code>NET STOP W3SVC &amp;&amp; NET START W3SVC</code> 或 PowerShell <code>Restart-Service W3SVC</code></li></ul> <p>當然，如果不在乎網站中斷，也不介意等久一點(以及發生機率不高的 Metadata 遣失)，指令短又好記是 IISRESET 的最大優點，是能少打幾個字的簡單粗暴選項。估評過缺點跟風險仍決定使用，便是思考過的取捨而非不明究理盲用，我可以接受。</p> <p>結案。</p> <p data-slug="stop-using-iisreset" data-title-en="Why is IISREST bad?" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/08/08/19/55/confused-880735_1280.jpg#1280#720">IISREST has been declared as not recommended for use after IIS6. This article explores the issues that may be caused by using IISRESET and how to properly restart IIS websites.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>同事分享冷知識一枚，依據微軟<a href="https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-server-2008-R2-and-2008/dd364308(v=ws.10)?redirectedfrom=MSDN&amp;WT.mc_id=DOP-MVP-37580#verify">官方建議</a>，我平時順手就來一發的 IISREST，是 IIS 5.0 時代的產物，IIS 6.0 後已不建議使用，登楞!!</p> <p>原文是這麼寫的：</p> <blockquote> <p>IISReset.exe is a command-line utility that was introduced in IIS 5.0 that could be used to stop, start, and restart IIS Internet services. Its use is not recommended for IIS 6.0 and is not supported in IIS 7.0 or IIS 7.5.<br> Note: Restarting or stopping IIS, or rebooting your Web server, is a severe action. When you restart the Internet service, changes to the metabase can be lost. All sessions connected to your Web server are dropped, and any data held in Web applications is lost. All Internet sites are unavailable until Internet services are restarted. For these reasons, the use of IISReset is not recommended.</p> </blockquote> <p>大意是 IISRESET 是 IIS 5.0 引進可重啟 IIS 的工具程式，不建議在 IIS 6.0 使用，且 IIS 7.0 後將不再支援。理由是重啟 IIS 或重開機是一個 Server 層級動作，可能造成 Metabase 修改遺失，網站連線會被丟棄、記憶體資料也會遺失，網站會無法使用直到服務啟動，故不建議使用。</p> <p>查了一下，IIS 6.0 始於 Windows 2003，若 IISRESET 不該用，我豈不錯用了 21 年？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638442964022201848.png"><br> IIS 版本對照表 <a href="https://learn.microsoft.com/zh-tw/lifecycle/products/internet-information-services-iis?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>不過呢，我注意到該文件的最近更新日期是 2009，適用 Windows Server 2008 R2，文章上方有警示 "We're no longer updating this content regularly."，代表文件久未更新，資訊可能已過時。我決定打破砂鍋，探索這個議題。</p> <p>文件有個 "Restarting IIS (IIS 6.)" 連結提供詳情，但超連結已失效，我找到<a href="https://www.cnblogs.com/shineqiujuan/archive/2011/03/22/1990945.html">網友留存的備份</a>，裡面提到不建議使用 IISRESET 的理由：</p> <ol><li>IIS 6.0 已新增更優雅的重啟選項：有 AppPool 可停止啟動重啟，非必要別重啟整台 IIS</li> <li>IIS 跟 FTP、NMTP、SMTP 也有關，若只是要重啟 Web，沒必要全部一起暫停服務重啟</li> <li>過往 IIS 5 必須 IISRESET 是因為 IISAdmin 跟 W3SVC 綁在一起(shared the same application space)，IIS 6 起二者為獨立 Process，IISAdmin 停止時，W3SVC 仍可執行但被通知 Metabase 來源異常中止，IIS 則會在 IISAdmin 恢復後再連上 W3SVC。</li></ol> <p>我另外查到一篇 <a href="https://www.leansentry.com/guide/reset-restart-recycle-iis/dangers-of-iisreset">IISRESET: the WORST way to restart your IIS website</a> 有更完整的闡述，給足不該用 IISREST 的理由：</p> <ol><li>過去有些 Registry 異動需重啟 IIS 套用，IIS 7+ 已能自動偵測並套用 IIS 修改，需重啟的機會已大幅減少。(註：但經驗不乏有其他設定需 IISREST 生效，例如：ODP.NET)</li> <li>IIS 6 加入 AppPool 架構，單一網站有問題重啟 AppPool 即可，沒必要整台網站重啟</li> <li>回收(Recycle) AppPool 會另起新 Process，原 Process 會等請求處理完才結束，讓客戶端無感，是更優雅的作法</li> <li>下圖是 IISRESET 的運作流程，步驟多時間長(數秒到數十秒)，中間會有一段時間使用者會看到 503：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638442964026937842.png"><br> 若外，若 IISRESET 程序失敗將可能讓 IIS 留在停機狀態。<br> 下圖是不同重啟方式的網站停機時間及網頁錯誤次數，回收 AppPool 是首選：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638442964029144971.png"><br> 若非用 IISREST 不可，以下是更嚴謹的做法： <pre><code>iisreset /stop /timeout:60
taskkill /F /FI "SERVICES eq was"
iisreset /start
</code></pre> </li> <li>IISRESET 是系統不穩、Memory Leak、網站當死、CPU 飆高等各式疑難雜症的鋸箭特效藥，但過度依賴會錯失根本解決問題的機會，埋下日後大爆炸的種子。</li></ol> <p>另外，在這篇<a href="https://techcommunity.microsoft.com/t5/iis-support-blog/iisreset-internals/ba-p/951578?WT.mc_id=DOP-MVP-37580">部落格</a>有提到 IISRESET 如遇 IISAdmin 停不掉會強制砍程序(應是導致 IIS Metadata 遺失的情境)，並中止所有使用 IISW3ADM.DLL 的程序。(用 IISRESET /NOFORCE 可改善，它只會跑 NET STOP IISADMIN /Y，停不掉就算了不會砍 Process)</p> <p>到這裡，差不多可以理解 IISRESET 的缺點：拉長服務中斷時間，降低網站可用性，但看起來並無嚴重後遺症，不到罪無可赦的程度。且十多年前 IIS 7.0+ 將不再支援 IISRESET 的計劃看來也修改了，至今 IIS 10 都還支援 IISRESET，故我認為它仍是可用選項。</p> <p>話說回來，如果目的是重啟網站，你有其他更輕巧快速的做法，例如：</p> <ul><li>PowerShell 回收特定 AppPool：<code>(Get-IISAppPool &lt;AppPoolName&gt;).Recycle()</code> <a href="https://learn.microsoft.com/en-us/dotnet/api/microsoft.web.administration.applicationpool.recycle?view=iis-dotnet&amp;WT.mc_id=DOP-MVP-37580">參考</a></li> <li>PowerShell 重啟特定 AppPool：<code>Restart-WebAppPool &lt;AppPoolName&gt;</code> 或 <code>Restart-WebAppPool (Get-Website -Name &lt;YourSiteName&gt;).ApplicationPool</code> <a href="https://learn.microsoft.com/en-us/powershell/module/webadministration/restart-webapppool?WT.mc_id=DOP-MVP-37580">參考</a></li> <li>重啟所有 AppPool：<code>NET STOP W3SVC &amp;&amp; NET START W3SVC</code> 或 PowerShell <code>Restart-Service W3SVC</code></li></ul> <p>當然，如果不在乎網站中斷，也不介意等久一點(以及發生機率不高的 Metadata 遣失)，指令短又好記是 IISRESET 的最大優點，是能少打幾個字的簡單粗暴選項。估評過缺點跟風險仍決定使用，便是思考過的取捨而非不明究理盲用，我可以接受。</p> <p>結案。</p> <p data-slug="stop-using-iisreset" data-title-en="Why is IISREST bad?" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/08/08/19/55/confused-880735_1280.jpg#1280#720">IISREST has been declared as not recommended for use after IIS6. This article explores the issues that may be caused by using IISRESET and how to properly restart IIS websites.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638442964022201848.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638442964022201848.png" medium="image"/>
    <pubDate>Fri, 23 Feb 2024 14:40:09 GMT</pubDate>
  </item><item>
    <title><![CDATA[IIS AlwaysRunning 深入研究 - 幽靈排程與自動啟動行為]]></title>
    <link>https://blog.darkthread.net/blog/iis-always-running-tips/</link>
    <itunes:title><![CDATA[IIS AlwaysRunning 深入研究 - 幽靈排程與自動啟動行為]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>之前研究過<a href="/blog/hangfire-recurringjob-notes/">確保網站永遠處於執行狀態的 IIS 設定方式</a>，最近遇上網站停用但網站背景排程照跑的靈異事件，發現事情跟我想的不一樣，自己對 IIS 站台 Process 模式及 AlwaysRunning 行為有些誤解，寫篇筆記備忘。</p> <p>我們都知道 IIS 管理員站台有組控制鈕，可以重新啟動、啟動或停止站台。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400387242809.png"></p> <p>大家想像按下去的動作是什麼？重啟或切換 AppPool 狀態？並不是! 這也是這次我誤解的主要來源。</p> <p>.NET 在一個 Process 裡可建立多個記憶體空間和資源獨立的 AppDomain，達到比另建 Process 省資源又兼顧安全及穩定性隔離的效果。(參考：<a href="https://vito-note.blogspot.com/2012/03/appdomain.html">VITO の 學習筆記：什麼是應用程式定義域 ( application domain )</a>)。IIS 7+ 的做法是在第一次收到 ASP.NET 網頁要求時，由 IIS 受控引擎模組在記憶體建立 AppDomain，由 AppDomain 處理 aspx 頁面要求。(詳情可參考<a href="https://learn.microsoft.com/zh-tw/iis/manage/scripting/managing-worker-processes-and-appdomains-in-iis-with-wmi?WT.mc_id=DOP-MVP-37580#appdomains"> MS Learn 文件</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400389394892.png"><br> <a href="https://vito-note.blogspot.com/2012/03/appdomain.html">圖片來源</a></p> <p>為了實地驗證，我做了一個簡單的 ASP.NET Web Site Project 網站，並<a href="/blog/hangfire-recurringjob-notes/">設好 AlwaysRunning、Preload Enabled</a>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400391452608.png"></p> <p>Global.asax 會啟動一個 Task 跑背景排程，每一秒寫入一筆 Log，另外，在站啟動及結束時也會寫 Log。Log 訊息前方則會加上 Process ID # AppDomain ID 方便觀察： (延伸閱讀：<a href="/blog/tpl-threadpool-usage/">新時代 .NET ThreadPool 程式寫法以及為什麼你該用力 async/await</a>)</p> <pre><code>&lt;%@ Application Language="C#" %&gt;
&lt;%@ Import Namespace="System.Threading" %&gt;
&lt;%@ Import Namespace="System.Threading.Tasks" %&gt;
&lt;%@ Import Namespace="System.Diagnostics" %&gt;
&lt;script RunAt="server"&gt;
CancellationTokenSource cts;
void Log(string msg)
{
System.IO.File.AppendAllText("D:\\Logs\\trace.txt",
Process.GetCurrentProcess().Id +
"#" + AppDomain.CurrentDomain.Id +
" - " + msg + "
");
}
void Application_Start(object sender, EventArgs e)
{
Log("Application Start");
cts = new CancellationTokenSource();
Task.Run(async () =&gt;
{
while (!cts.Token.IsCancellationRequested)
{
Log(DateTime.Now.ToString("HH:mm:ss"));
await Task.Delay(1000, cts.Token);
}
});
}
void Application_End(object sender, EventArgs e)
{
cts.Cancel();
Log("Application End");
}
&lt;/script&gt;
</code></pre> <p>有了這個小實驗，我的疑惑很快獲得解答：</p> <ol><li>啟動停止中的站台時會自動啟動網站嗎？<br> 答案是不會， AlwaysRunning、Preload Enabled 自動啟動網站只適用電腦重開或 AppPool 重啟時。如下面展示，按下 Start 網站沒開始寫 Log，得等呼叫 <code>http://localhost:8000/</code> 後才開始：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400395896314.gif"><br> 如前面所提，站台需等到第一個請求進來時才啟動 AppDoamin。而由 Process ID / AppDomain ID 可證明站台重啟後仍是同一個 Process，只有換 AppDomain。<br> 同理，更新 bin、web.config 也會造成 AppDomain 停止，之後需發出 HTTP 請求才能啟動網站。(註：web.config 的 initializationPage 設定是在網站啟動後自動呼叫，無法用來觸發停止中的站台)<br> 以下展示在 bin 寫入檔案讓網站停止，停止後用 PowerShell 發送 HTTP 請求後才被喚醒另建 AppDomain 執行：(AppDomain Id 由 4 變 5)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638440400398351903.gif"></li> <li>那 IISEREST 或重啟 W3WC 服務後網站會自動啟動嗎？<br> AppPool 重啟時，不需 HTTP 請求便可啟動 AlwaysRunning + Preload 網站，實驗可觀察到重啟後 ProcessID 不同。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400400957056.gif"></li> <li>「站台已停用，網站背景程序卻還在執行」的靈異現象是怎麼發生的？<br> 如第 2 點所說，設定 AlwaysRunning 的站台會在機器重啟或 IISRESET 時自動啟動，即便站台已被停用。簡單解釋是停用站台是停掉目前執行中的 AppDoamin 並拒絕接收 HTTP 請求，但 AlwaysRunning + Preload Enabled 會在 AppPool 啟動時建立 AppDomain 執行，造成這次的靈異現象。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400403397714.gif"></li></ol> <p>歸納結論：</p> <ol><li>IIS 站台是以 AppDomain 方式執行，一個 AppPool Process 可有多個 AppDomain，IIS 管理的站台啟動、停止、重啟功能控制的是 AppDomain，bin 或 web.config 異動也會造成 AppDomain 結束</li> <li>AlwaysRunning + Preload Enabled 適用重新開機、AppPool 啟動、IISREST 後自動啟動網站，IIS 管理員重啟站台後需對網站發動請求才會建立 AppDomain</li> <li>站台停用狀態下，AlwaysRunning + Preload Enabled 網站會在 AppPool 啟動時自動啟動。若不想程式被誤執行，可將 AppPool 也停用 (註：IIS 管理員設定停止的 AppPool 及站台，重新開機後仍會維持停止狀態)</li></ol> <p data-slug="iis-always-running-tips" data-title-en="Why a stopped website is still running?" data-catg="Web Development" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Explanation of AppDomain operation design in IIS sites and how AlwaysRunning and Preload Enabled settings work, demonstrated by experiments.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>之前研究過<a href="/blog/hangfire-recurringjob-notes/">確保網站永遠處於執行狀態的 IIS 設定方式</a>，最近遇上網站停用但網站背景排程照跑的靈異事件，發現事情跟我想的不一樣，自己對 IIS 站台 Process 模式及 AlwaysRunning 行為有些誤解，寫篇筆記備忘。</p> <p>我們都知道 IIS 管理員站台有組控制鈕，可以重新啟動、啟動或停止站台。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400387242809.png"></p> <p>大家想像按下去的動作是什麼？重啟或切換 AppPool 狀態？並不是! 這也是這次我誤解的主要來源。</p> <p>.NET 在一個 Process 裡可建立多個記憶體空間和資源獨立的 AppDomain，達到比另建 Process 省資源又兼顧安全及穩定性隔離的效果。(參考：<a href="https://vito-note.blogspot.com/2012/03/appdomain.html">VITO の 學習筆記：什麼是應用程式定義域 ( application domain )</a>)。IIS 7+ 的做法是在第一次收到 ASP.NET 網頁要求時，由 IIS 受控引擎模組在記憶體建立 AppDomain，由 AppDomain 處理 aspx 頁面要求。(詳情可參考<a href="https://learn.microsoft.com/zh-tw/iis/manage/scripting/managing-worker-processes-and-appdomains-in-iis-with-wmi?WT.mc_id=DOP-MVP-37580#appdomains"> MS Learn 文件</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400389394892.png"><br> <a href="https://vito-note.blogspot.com/2012/03/appdomain.html">圖片來源</a></p> <p>為了實地驗證，我做了一個簡單的 ASP.NET Web Site Project 網站，並<a href="/blog/hangfire-recurringjob-notes/">設好 AlwaysRunning、Preload Enabled</a>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400391452608.png"></p> <p>Global.asax 會啟動一個 Task 跑背景排程，每一秒寫入一筆 Log，另外，在站啟動及結束時也會寫 Log。Log 訊息前方則會加上 Process ID # AppDomain ID 方便觀察： (延伸閱讀：<a href="/blog/tpl-threadpool-usage/">新時代 .NET ThreadPool 程式寫法以及為什麼你該用力 async/await</a>)</p> <pre><code>&lt;%@ Application Language="C#" %&gt;
&lt;%@ Import Namespace="System.Threading" %&gt;
&lt;%@ Import Namespace="System.Threading.Tasks" %&gt;
&lt;%@ Import Namespace="System.Diagnostics" %&gt;
&lt;script RunAt="server"&gt;
CancellationTokenSource cts;
void Log(string msg)
{
System.IO.File.AppendAllText("D:\\Logs\\trace.txt",
Process.GetCurrentProcess().Id +
"#" + AppDomain.CurrentDomain.Id +
" - " + msg + "
");
}
void Application_Start(object sender, EventArgs e)
{
Log("Application Start");
cts = new CancellationTokenSource();
Task.Run(async () =&gt;
{
while (!cts.Token.IsCancellationRequested)
{
Log(DateTime.Now.ToString("HH:mm:ss"));
await Task.Delay(1000, cts.Token);
}
});
}
void Application_End(object sender, EventArgs e)
{
cts.Cancel();
Log("Application End");
}
&lt;/script&gt;
</code></pre> <p>有了這個小實驗，我的疑惑很快獲得解答：</p> <ol><li>啟動停止中的站台時會自動啟動網站嗎？<br> 答案是不會， AlwaysRunning、Preload Enabled 自動啟動網站只適用電腦重開或 AppPool 重啟時。如下面展示，按下 Start 網站沒開始寫 Log，得等呼叫 <code>http://localhost:8000/</code> 後才開始：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400395896314.gif"><br> 如前面所提，站台需等到第一個請求進來時才啟動 AppDoamin。而由 Process ID / AppDomain ID 可證明站台重啟後仍是同一個 Process，只有換 AppDomain。<br> 同理，更新 bin、web.config 也會造成 AppDomain 停止，之後需發出 HTTP 請求才能啟動網站。(註：web.config 的 initializationPage 設定是在網站啟動後自動呼叫，無法用來觸發停止中的站台)<br> 以下展示在 bin 寫入檔案讓網站停止，停止後用 PowerShell 發送 HTTP 請求後才被喚醒另建 AppDomain 執行：(AppDomain Id 由 4 變 5)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638440400398351903.gif"></li> <li>那 IISEREST 或重啟 W3WC 服務後網站會自動啟動嗎？<br> AppPool 重啟時，不需 HTTP 請求便可啟動 AlwaysRunning + Preload 網站，實驗可觀察到重啟後 ProcessID 不同。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400400957056.gif"></li> <li>「站台已停用，網站背景程序卻還在執行」的靈異現象是怎麼發生的？<br> 如第 2 點所說，設定 AlwaysRunning 的站台會在機器重啟或 IISRESET 時自動啟動，即便站台已被停用。簡單解釋是停用站台是停掉目前執行中的 AppDoamin 並拒絕接收 HTTP 請求，但 AlwaysRunning + Preload Enabled 會在 AppPool 啟動時建立 AppDomain 執行，造成這次的靈異現象。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400403397714.gif"></li></ol> <p>歸納結論：</p> <ol><li>IIS 站台是以 AppDomain 方式執行，一個 AppPool Process 可有多個 AppDomain，IIS 管理的站台啟動、停止、重啟功能控制的是 AppDomain，bin 或 web.config 異動也會造成 AppDomain 結束</li> <li>AlwaysRunning + Preload Enabled 適用重新開機、AppPool 啟動、IISREST 後自動啟動網站，IIS 管理員重啟站台後需對網站發動請求才會建立 AppDomain</li> <li>站台停用狀態下，AlwaysRunning + Preload Enabled 網站會在 AppPool 啟動時自動啟動。若不想程式被誤執行，可將 AppPool 也停用 (註：IIS 管理員設定停止的 AppPool 及站台，重新開機後仍會維持停止狀態)</li></ol> <p data-slug="iis-always-running-tips" data-title-en="Why a stopped website is still running?" data-catg="Web Development" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Explanation of AppDomain operation design in IIS sites and how AlwaysRunning and Preload Enabled settings work, demonstrated by experiments.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>之前研究過<a href="/blog/hangfire-recurringjob-notes/">確保網站永遠處於執行狀態的 IIS 設定方式</a>，最近遇上網站停用但網站背景排程照跑的靈異事件，發現事情跟我想的不一樣，自己對 IIS 站台 Process 模式及 AlwaysRunning 行為有些誤解，寫篇筆記備忘。</p> <p>我們都知道 IIS 管理員站台有組控制鈕，可以重新啟動、啟動或停止站台。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400387242809.png"></p> <p>大家想像按下去的動作是什麼？重啟或切換 AppPool 狀態？並不是! 這也是這次我誤解的主要來源。</p> <p>.NET 在一個 Process 裡可建立多個記憶體空間和資源獨立的 AppDomain，達到比另建 Process 省資源又兼顧安全及穩定性隔離的效果。(參考：<a href="https://vito-note.blogspot.com/2012/03/appdomain.html">VITO の 學習筆記：什麼是應用程式定義域 ( application domain )</a>)。IIS 7+ 的做法是在第一次收到 ASP.NET 網頁要求時，由 IIS 受控引擎模組在記憶體建立 AppDomain，由 AppDomain 處理 aspx 頁面要求。(詳情可參考<a href="https://learn.microsoft.com/zh-tw/iis/manage/scripting/managing-worker-processes-and-appdomains-in-iis-with-wmi?WT.mc_id=DOP-MVP-37580#appdomains"> MS Learn 文件</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400389394892.png"><br> <a href="https://vito-note.blogspot.com/2012/03/appdomain.html">圖片來源</a></p> <p>為了實地驗證，我做了一個簡單的 ASP.NET Web Site Project 網站，並<a href="/blog/hangfire-recurringjob-notes/">設好 AlwaysRunning、Preload Enabled</a>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400391452608.png"></p> <p>Global.asax 會啟動一個 Task 跑背景排程，每一秒寫入一筆 Log，另外，在站啟動及結束時也會寫 Log。Log 訊息前方則會加上 Process ID # AppDomain ID 方便觀察： (延伸閱讀：<a href="/blog/tpl-threadpool-usage/">新時代 .NET ThreadPool 程式寫法以及為什麼你該用力 async/await</a>)</p> <pre><code>&lt;%@ Application Language="C#" %&gt;
&lt;%@ Import Namespace="System.Threading" %&gt;
&lt;%@ Import Namespace="System.Threading.Tasks" %&gt;
&lt;%@ Import Namespace="System.Diagnostics" %&gt;
&lt;script RunAt="server"&gt;
CancellationTokenSource cts;
void Log(string msg)
{
System.IO.File.AppendAllText("D:\\Logs\\trace.txt",
Process.GetCurrentProcess().Id +
"#" + AppDomain.CurrentDomain.Id +
" - " + msg + "
");
}
void Application_Start(object sender, EventArgs e)
{
Log("Application Start");
cts = new CancellationTokenSource();
Task.Run(async () =&gt;
{
while (!cts.Token.IsCancellationRequested)
{
Log(DateTime.Now.ToString("HH:mm:ss"));
await Task.Delay(1000, cts.Token);
}
});
}
void Application_End(object sender, EventArgs e)
{
cts.Cancel();
Log("Application End");
}
&lt;/script&gt;
</code></pre> <p>有了這個小實驗，我的疑惑很快獲得解答：</p> <ol><li>啟動停止中的站台時會自動啟動網站嗎？<br> 答案是不會， AlwaysRunning、Preload Enabled 自動啟動網站只適用電腦重開或 AppPool 重啟時。如下面展示，按下 Start 網站沒開始寫 Log，得等呼叫 <code>http://localhost:8000/</code> 後才開始：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638440400395896314.gif"><br> 如前面所提，站台需等到第一個請求進來時才啟動 AppDoamin。而由 Process ID / AppDomain ID 可證明站台重啟後仍是同一個 Process，只有換 AppDomain。<br> 同理，更新 bin、web.config 也會造成 AppDomain 停止，之後需發出 HTTP 請求才能啟動網站。(註：web.config 的 initializationPage 設定是在網站啟動後自動呼叫，無法用來觸發停止中的站台)<br> 以下展示在 bin 寫入檔案讓網站停止，停止後用 PowerShell 發送 HTTP 請求後才被喚醒另建 AppDomain 執行：(AppDomain Id 由 4 變 5)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638440400398351903.gif"></li> <li>那 IISEREST 或重啟 W3WC 服務後網站會自動啟動嗎？<br> AppPool 重啟時，不需 HTTP 請求便可啟動 AlwaysRunning + Preload 網站，實驗可觀察到重啟後 ProcessID 不同。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400400957056.gif"></li> <li>「站台已停用，網站背景程序卻還在執行」的靈異現象是怎麼發生的？<br> 如第 2 點所說，設定 AlwaysRunning 的站台會在機器重啟或 IISRESET 時自動啟動，即便站台已被停用。簡單解釋是停用站台是停掉目前執行中的 AppDoamin 並拒絕接收 HTTP 請求，但 AlwaysRunning + Preload Enabled 會在 AppPool 啟動時建立 AppDomain 執行，造成這次的靈異現象。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638440400403397714.gif"></li></ol> <p>歸納結論：</p> <ol><li>IIS 站台是以 AppDomain 方式執行，一個 AppPool Process 可有多個 AppDomain，IIS 管理的站台啟動、停止、重啟功能控制的是 AppDomain，bin 或 web.config 異動也會造成 AppDomain 結束</li> <li>AlwaysRunning + Preload Enabled 適用重新開機、AppPool 啟動、IISREST 後自動啟動網站，IIS 管理員重啟站台後需對網站發動請求才會建立 AppDomain</li> <li>站台停用狀態下，AlwaysRunning + Preload Enabled 網站會在 AppPool 啟動時自動啟動。若不想程式被誤執行，可將 AppPool 也停用 (註：IIS 管理員設定停止的 AppPool 及站台，重新開機後仍會維持停止狀態)</li></ol> <p data-slug="iis-always-running-tips" data-title-en="Why a stopped website is still running?" data-catg="Web Development" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Explanation of AppDomain operation design in IIS sites and how AlwaysRunning and Preload Enabled settings work, demonstrated by experiments.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400387242809.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig2_638440400387242809.png" medium="image"/>
    <pubDate>Tue, 20 Feb 2024 15:31:13 GMT</pubDate>
  </item><item>
    <title><![CDATA[前端小技巧 - 在網頁 Ctrl-V 貼圖片上傳檔案]]></title>
    <link>https://blog.darkthread.net/blog/paste-to-upload/</link>
    <itunes:title><![CDATA[前端小技巧 - 在網頁 Ctrl-V 貼圖片上傳檔案]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>在很多網站看過一種方便設計 - 從別處選取圖片複製到剪貼簿，在網頁空白處按 Ctrl-V，便會將圖片上傳到網站。</p> <p>總想著，改天我要為自己的網站也加上這個酷功能，就今天吧!</p> <p>在 IE 退役及Edge 改用 Chromium 引擎後，瀏覽器回到大一統的和平局面(令人想起當年 IE 市佔 95% 的時光)，現在寫桌機用網頁輕鬆許多，很少需要煩惱跨瀏覽器問題。加上 HTML5 標準成熟與普及，要從剪貼簿拿到圖片內容遠比想像簡單，用內建 API 短短幾行便能搞定。(最重要的是現在還多了 <a href="/blog/github-copilot-demo/">Github Copilot 魔法</a>)</p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;從剪貼簿貼上圖片&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;p&gt;按 Ctrl-V 貼上複製的圖片&lt;/p&gt;
&lt;div id="panel"&gt;&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData &amp;&amp; e.clipboardData.items;
if (!items) return;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
const reader = new FileReader();
reader.onload = function (e) {
var image = new Image();
image.src = e.target.result;
document.getElementById('panel').appendChild(image);
};
reader.readAsDataURL(items[i].getAsFile());
}
}
});
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>程式原理很簡單：傾聽 paste 事件，逐一檢查 e.clipboardData.items 的資料選項有沒有圖檔類( Content Type 為 "image/*")，若有就用 FileReader.readAsDataURL() 讀入 DataTransferItem.getAsFile()，轉為 <a href="/blog/data-uri/">Data URI 格式</a>在網頁插入圖片元素，輕鬆秒殺。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638436065118158748.gif"></p> <p>下一步是將其轉為上傳檔案。</p> <p>基於安全 <code>&lt;input type="file"&gt;</code> 挑選檔案向來限定人工操作，禁止 JavaScript 染指，因此過去我都是採行繞道作法，例如：用 <code>&lt;input type="hidden"&gt;</code> 傳送 Data URI 編碼、改用 AJAX 方式上傳... 等。不過，隨者 HTML 標準成熟普及，現在多了 <a href="https://developer.mozilla.org/en-US/docs/Web/API/File_API">File API</a> 可用，允許 JavaScript 透過 <code>&lt;input type="file"&gt;</code> <a href="https://developer.mozilla.org/en-US/docs/Web/API/FileList">.files 屬性 (FileList 型別)</a>讀取或設定上傳內容。有個眉角是 FileList 內含的檔案項目不可修改，需透過 DataTransfer 物件建立整個 FileList 指定給 <code>&lt;input type="file"&gt;</code>.files，檔名則由依當下日期時間指定動態產生。</p> <p>寫了一個簡單 ASPX 驗證：</p> <pre><code>&lt;%@ Page Language="C#" %&gt;
&lt;script runat="server"&gt;
protected void Page_Load(object sender, EventArgs e)
{
if (Request.Files.Count &gt; 0) {
HttpPostedFile file = Request.Files[0];
var fileData = new byte[file.ContentLength];
file.InputStream.Read(fileData, 0, file.ContentLength);
Response.ContentType = "text/html";
Response.Write("&lt;body&gt;");
Response.Write("&lt;p&gt;" + file.FileName + " (" + file.ContentLength + " bytes) uploaded&lt;/p&gt;");
Response.Write("&lt;img src='data:" + file.ContentType + ";base64," + Convert.ToBase64String(fileData) + "' /&gt;");
Response.Write("&lt;/body&gt;");
Response.End();
}
}
&lt;/script&gt;
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;貼圖檔上傳&lt;/title&gt;
&lt;style&gt;
html, body { height: 100%; padding: 0; margin: 0; }
.container {
display: flex; flex-direction: column;
height: 100%; padding: 12px; box-sizing: border-box;
}
form { height: 40px; }
iframe {
flex-grow: 1; width: 100%;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class="container"&gt;
&lt;form method="post" target="result" enctype="multipart/form-data"&gt;
&lt;input type="file" name="file" id="uploadFiles" /&gt;
&lt;input type="submit" value="上傳" /&gt;
&lt;/form&gt;
&lt;iframe name="result"&gt;&lt;/iframe&gt;
&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData.items;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
let file = items[i].getAsFile();
const fileExt = file.name.split('.').pop();
file = new File([file],
`Img${new Date().toISOString().substring(0, 19).replace(/[-T:]/g, '')}.${fileExt}`,
{ type: file.type });
const dataTransfer = new DataTransfer();
dataTransfer.items.add(file);
document.getElementById('uploadFiles').files = dataTransfer.files;
break;
}
}
});
&lt;/script&gt;
&lt;/html&gt;
</code></pre> <p>就醬，從今天起，我寫的網站也能貼圖片上傳功能囉~ (灑花)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638436065122356198.gif"></p> <p>註：用 Edge/Chrome/Firefox 測過都沒問題，以上做法應可安心用在供桌機瀏覽的企業內部網站。</p> <p>補充一個小技巧：若想保留 jpg/gif 圖檔格式，不想一律轉成 png，則不要用軟體或瀏覽器開啟圖檔再複製影像，請選取檔案本體複製、貼上，這樣可上傳原格式檔案，示範如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638436086968440933.gif"></p> <p data-slug="paste-to-upload" data-title-en="Paste image to upload" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/13/38/dog-862563_960_720.jpg#960#720">This article explains how to implement clipboard pasting of images on a webpage for uploading to the website.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>在很多網站看過一種方便設計 - 從別處選取圖片複製到剪貼簿，在網頁空白處按 Ctrl-V，便會將圖片上傳到網站。</p> <p>總想著，改天我要為自己的網站也加上這個酷功能，就今天吧!</p> <p>在 IE 退役及Edge 改用 Chromium 引擎後，瀏覽器回到大一統的和平局面(令人想起當年 IE 市佔 95% 的時光)，現在寫桌機用網頁輕鬆許多，很少需要煩惱跨瀏覽器問題。加上 HTML5 標準成熟與普及，要從剪貼簿拿到圖片內容遠比想像簡單，用內建 API 短短幾行便能搞定。(最重要的是現在還多了 <a href="/blog/github-copilot-demo/">Github Copilot 魔法</a>)</p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;從剪貼簿貼上圖片&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;p&gt;按 Ctrl-V 貼上複製的圖片&lt;/p&gt;
&lt;div id="panel"&gt;&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData &amp;&amp; e.clipboardData.items;
if (!items) return;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
const reader = new FileReader();
reader.onload = function (e) {
var image = new Image();
image.src = e.target.result;
document.getElementById('panel').appendChild(image);
};
reader.readAsDataURL(items[i].getAsFile());
}
}
});
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>程式原理很簡單：傾聽 paste 事件，逐一檢查 e.clipboardData.items 的資料選項有沒有圖檔類( Content Type 為 "image/*")，若有就用 FileReader.readAsDataURL() 讀入 DataTransferItem.getAsFile()，轉為 <a href="/blog/data-uri/">Data URI 格式</a>在網頁插入圖片元素，輕鬆秒殺。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638436065118158748.gif"></p> <p>下一步是將其轉為上傳檔案。</p> <p>基於安全 <code>&lt;input type="file"&gt;</code> 挑選檔案向來限定人工操作，禁止 JavaScript 染指，因此過去我都是採行繞道作法，例如：用 <code>&lt;input type="hidden"&gt;</code> 傳送 Data URI 編碼、改用 AJAX 方式上傳... 等。不過，隨者 HTML 標準成熟普及，現在多了 <a href="https://developer.mozilla.org/en-US/docs/Web/API/File_API">File API</a> 可用，允許 JavaScript 透過 <code>&lt;input type="file"&gt;</code> <a href="https://developer.mozilla.org/en-US/docs/Web/API/FileList">.files 屬性 (FileList 型別)</a>讀取或設定上傳內容。有個眉角是 FileList 內含的檔案項目不可修改，需透過 DataTransfer 物件建立整個 FileList 指定給 <code>&lt;input type="file"&gt;</code>.files，檔名則由依當下日期時間指定動態產生。</p> <p>寫了一個簡單 ASPX 驗證：</p> <pre><code>&lt;%@ Page Language="C#" %&gt;
&lt;script runat="server"&gt;
protected void Page_Load(object sender, EventArgs e)
{
if (Request.Files.Count &gt; 0) {
HttpPostedFile file = Request.Files[0];
var fileData = new byte[file.ContentLength];
file.InputStream.Read(fileData, 0, file.ContentLength);
Response.ContentType = "text/html";
Response.Write("&lt;body&gt;");
Response.Write("&lt;p&gt;" + file.FileName + " (" + file.ContentLength + " bytes) uploaded&lt;/p&gt;");
Response.Write("&lt;img src='data:" + file.ContentType + ";base64," + Convert.ToBase64String(fileData) + "' /&gt;");
Response.Write("&lt;/body&gt;");
Response.End();
}
}
&lt;/script&gt;
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;貼圖檔上傳&lt;/title&gt;
&lt;style&gt;
html, body { height: 100%; padding: 0; margin: 0; }
.container {
display: flex; flex-direction: column;
height: 100%; padding: 12px; box-sizing: border-box;
}
form { height: 40px; }
iframe {
flex-grow: 1; width: 100%;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class="container"&gt;
&lt;form method="post" target="result" enctype="multipart/form-data"&gt;
&lt;input type="file" name="file" id="uploadFiles" /&gt;
&lt;input type="submit" value="上傳" /&gt;
&lt;/form&gt;
&lt;iframe name="result"&gt;&lt;/iframe&gt;
&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData.items;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
let file = items[i].getAsFile();
const fileExt = file.name.split('.').pop();
file = new File([file],
`Img${new Date().toISOString().substring(0, 19).replace(/[-T:]/g, '')}.${fileExt}`,
{ type: file.type });
const dataTransfer = new DataTransfer();
dataTransfer.items.add(file);
document.getElementById('uploadFiles').files = dataTransfer.files;
break;
}
}
});
&lt;/script&gt;
&lt;/html&gt;
</code></pre> <p>就醬，從今天起，我寫的網站也能貼圖片上傳功能囉~ (灑花)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638436065122356198.gif"></p> <p>註：用 Edge/Chrome/Firefox 測過都沒問題，以上做法應可安心用在供桌機瀏覽的企業內部網站。</p> <p>補充一個小技巧：若想保留 jpg/gif 圖檔格式，不想一律轉成 png，則不要用軟體或瀏覽器開啟圖檔再複製影像，請選取檔案本體複製、貼上，這樣可上傳原格式檔案，示範如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638436086968440933.gif"></p> <p data-slug="paste-to-upload" data-title-en="Paste image to upload" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/13/38/dog-862563_960_720.jpg#960#720">This article explains how to implement clipboard pasting of images on a webpage for uploading to the website.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>在很多網站看過一種方便設計 - 從別處選取圖片複製到剪貼簿，在網頁空白處按 Ctrl-V，便會將圖片上傳到網站。</p> <p>總想著，改天我要為自己的網站也加上這個酷功能，就今天吧!</p> <p>在 IE 退役及Edge 改用 Chromium 引擎後，瀏覽器回到大一統的和平局面(令人想起當年 IE 市佔 95% 的時光)，現在寫桌機用網頁輕鬆許多，很少需要煩惱跨瀏覽器問題。加上 HTML5 標準成熟與普及，要從剪貼簿拿到圖片內容遠比想像簡單，用內建 API 短短幾行便能搞定。(最重要的是現在還多了 <a href="/blog/github-copilot-demo/">Github Copilot 魔法</a>)</p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;從剪貼簿貼上圖片&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;p&gt;按 Ctrl-V 貼上複製的圖片&lt;/p&gt;
&lt;div id="panel"&gt;&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData &amp;&amp; e.clipboardData.items;
if (!items) return;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
const reader = new FileReader();
reader.onload = function (e) {
var image = new Image();
image.src = e.target.result;
document.getElementById('panel').appendChild(image);
};
reader.readAsDataURL(items[i].getAsFile());
}
}
});
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>程式原理很簡單：傾聽 paste 事件，逐一檢查 e.clipboardData.items 的資料選項有沒有圖檔類( Content Type 為 "image/*")，若有就用 FileReader.readAsDataURL() 讀入 DataTransferItem.getAsFile()，轉為 <a href="/blog/data-uri/">Data URI 格式</a>在網頁插入圖片元素，輕鬆秒殺。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638436065118158748.gif"></p> <p>下一步是將其轉為上傳檔案。</p> <p>基於安全 <code>&lt;input type="file"&gt;</code> 挑選檔案向來限定人工操作，禁止 JavaScript 染指，因此過去我都是採行繞道作法，例如：用 <code>&lt;input type="hidden"&gt;</code> 傳送 Data URI 編碼、改用 AJAX 方式上傳... 等。不過，隨者 HTML 標準成熟普及，現在多了 <a href="https://developer.mozilla.org/en-US/docs/Web/API/File_API">File API</a> 可用，允許 JavaScript 透過 <code>&lt;input type="file"&gt;</code> <a href="https://developer.mozilla.org/en-US/docs/Web/API/FileList">.files 屬性 (FileList 型別)</a>讀取或設定上傳內容。有個眉角是 FileList 內含的檔案項目不可修改，需透過 DataTransfer 物件建立整個 FileList 指定給 <code>&lt;input type="file"&gt;</code>.files，檔名則由依當下日期時間指定動態產生。</p> <p>寫了一個簡單 ASPX 驗證：</p> <pre><code>&lt;%@ Page Language="C#" %&gt;
&lt;script runat="server"&gt;
protected void Page_Load(object sender, EventArgs e)
{
if (Request.Files.Count &gt; 0) {
HttpPostedFile file = Request.Files[0];
var fileData = new byte[file.ContentLength];
file.InputStream.Read(fileData, 0, file.ContentLength);
Response.ContentType = "text/html";
Response.Write("&lt;body&gt;");
Response.Write("&lt;p&gt;" + file.FileName + " (" + file.ContentLength + " bytes) uploaded&lt;/p&gt;");
Response.Write("&lt;img src='data:" + file.ContentType + ";base64," + Convert.ToBase64String(fileData) + "' /&gt;");
Response.Write("&lt;/body&gt;");
Response.End();
}
}
&lt;/script&gt;
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;貼圖檔上傳&lt;/title&gt;
&lt;style&gt;
html, body { height: 100%; padding: 0; margin: 0; }
.container {
display: flex; flex-direction: column;
height: 100%; padding: 12px; box-sizing: border-box;
}
form { height: 40px; }
iframe {
flex-grow: 1; width: 100%;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div class="container"&gt;
&lt;form method="post" target="result" enctype="multipart/form-data"&gt;
&lt;input type="file" name="file" id="uploadFiles" /&gt;
&lt;input type="submit" value="上傳" /&gt;
&lt;/form&gt;
&lt;iframe name="result"&gt;&lt;/iframe&gt;
&lt;/div&gt;
&lt;script&gt;
document.addEventListener('paste', function (e) {
const items = e.clipboardData.items;
for (let i = 0; i &lt; items.length; i++) {
if (items[i].type.indexOf('image') !== -1) {
let file = items[i].getAsFile();
const fileExt = file.name.split('.').pop();
file = new File([file],
`Img${new Date().toISOString().substring(0, 19).replace(/[-T:]/g, '')}.${fileExt}`,
{ type: file.type });
const dataTransfer = new DataTransfer();
dataTransfer.items.add(file);
document.getElementById('uploadFiles').files = dataTransfer.files;
break;
}
}
});
&lt;/script&gt;
&lt;/html&gt;
</code></pre> <p>就醬，從今天起，我寫的網站也能貼圖片上傳功能囉~ (灑花)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638436065122356198.gif"></p> <p>註：用 Edge/Chrome/Firefox 測過都沒問題，以上做法應可安心用在供桌機瀏覽的企業內部網站。</p> <p>補充一個小技巧：若想保留 jpg/gif 圖檔格式，不想一律轉成 png，則不要用軟體或瀏覽器開啟圖檔再複製影像，請選取檔案本體複製、貼上，這樣可上傳原格式檔案，示範如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638436086968440933.gif"></p> <p data-slug="paste-to-upload" data-title-en="Paste image to upload" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/13/38/dog-862563_960_720.jpg#960#720">This article explains how to implement clipboard pasting of images on a webpage for uploading to the website.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638436065118158748.gif"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638436065118158748.gif" medium="image"/>
    <pubDate>Thu, 15 Feb 2024 15:04:06 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 高斯消去法解聯立方程式]]></title>
    <link>https://blog.darkthread.net/blog/guassian-elimination/</link>
    <itunes:title><![CDATA[Coding4Fun - 高斯消去法解聯立方程式]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]>
    </description>
    <content:encoded><![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png" medium="image"/>
    <pubDate>Wed, 14 Feb 2024 04:33:19 GMT</pubDate>
  </item><item>
    <title><![CDATA[小試前端 REST 資料庫 - Supabase]]></title>
    <link>https://blog.darkthread.net/blog/supabase/</link>
    <itunes:title><![CDATA[小試前端 REST 資料庫 - Supabase]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png" medium="image"/>
    <pubDate>Tue, 13 Feb 2024 15:33:31 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 實現 -2 * 1/3 + 3/4 = 1/12 分數運算]]></title>
    <link>https://blog.darkthread.net/blog/cs-fraction-type/</link>
    <itunes:title><![CDATA[Coding4Fun - 實現 -2 * 1/3 + 3/4 = 1/12 分數運算]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png" medium="image"/>
    <pubDate>Sun, 11 Feb 2024 14:48:14 GMT</pubDate>
  </item><item>
    <title><![CDATA[AI 共筆 - 台灣本土版語言模型 - Taiwan LLM 是怎麼煉成的？]]></title>
    <link>https://blog.darkthread.net/blog/how-taiwan-llm-trained/</link>
    <itunes:title><![CDATA[AI 共筆 - 台灣本土版語言模型 - Taiwan LLM 是怎麼煉成的？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" medium="image"/>
    <pubDate>Sat, 10 Feb 2024 15:51:12 GMT</pubDate>
  </item></channel>
</rss>