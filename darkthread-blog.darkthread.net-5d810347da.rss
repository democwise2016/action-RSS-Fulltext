<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
    <title><![CDATA[使用 dotnet user-secrets 儲存專案專屬秘密設定(API Key、密碼)]]></title>
    <link>https://blog.darkthread.net/blog/dotnet-secrets-manager/</link>
    <itunes:title><![CDATA[使用 dotnet user-secrets 儲存專案專屬秘密設定(API Key、密碼)]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>看程式範例學到用 .NET Secret Manager 儲存秘密設定(如 API Key)的小技巧。</p> <p>參考文件：<a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580">在 ASP.NET Core 的開發中安全儲存應用程式秘密</a></p> <p>應用程式會用到的一些具機密性設定 API Key 等，存放在 appsettings.son 或自訂 JSON/XML 容易連同原始碼一起外流，並有不小心簽進版控在雲端裸奔的風險(<a href="https://www.informationsecurity.com.tw/article/article_detail.aspx?aid=10934">資安人：超過18000個API 金鑰外洩! 影響多個主流網站使用之令牌</a>)，改存到環境變數是個選擇。環境變數的缺點是所有專案共用像全域變數，有被其他專案覆寫改掉的風險。.NET Core 3.0+ 推出 Secret Manager (秘密管理員)可用來儲存應用程式開發期間的敏感數據，每個專案擁有個別儲存空間(但要共用也可以)不會彼此干擾，儲存位置和方式等實作細節被 Secret Manager 隱藏 (Windows 在 <code>%APPDATA%\Microsoft\UserSecrets\&lt;user_secrets_id&gt;\secrets.json</code>，Linux/macOS 在 <code>~/.microsoft/usersecrets/&lt;user_secrets_id&gt;/secrets.json</code>)，程式端只需透過 IConfiguration 介面可比照 appsettings.json 或環境變數輕鬆讀取設定值。</p> <p>使用方式很簡單，在專案目錄下執行 <code>dotnet user-secrets init</code>[1] 後，.NET 會隨機產生專案專屬 UserSecretsId 寫入 .csproj [2]，接著便可使用 <code>dotnet user-secrets set "設定名稱" "設定值"</code> 進行設定 [3]：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638464510248193279.png"></p> <p>設定後，到 AppData\Roaming 使用者私有區可找到隨機 UserSecretsId 名稱的資料夾，其中有個 secrets.json，裡面存有設定值：(注意：設定值會以明碼儲存未加密，它主要是方便本機開發測試，勿視為可信任的存放區 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580#secret-manager">參考</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638464510249978653.png"></p> <p>在 .NET 專案如要使用 Secret Manager 儲存的設定，需先參照 Microsoft.Extensions.Configuration.UserSecrets 程式庫 <code>dotnet add package Microsoft.Extensions.Configuration.UserSecrets</code>，之後透過 <code>ConfigurationBuilder.AddUserSecrets&lt;Program&gt;()</code> 加為來源，其餘存取概念與 appsettings.json 一致：</p> <pre><code>using Microsoft.Extensions.Configuration;
IConfiguration config = new ConfigurationBuilder()
.AddUserSecrets&lt;Program&gt;().Build();
Console.WriteLine(config["OpenApiKey"]);
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638464510251620170.png"></p> <p>心得：.NET 內建支援，專案各有專屬存放區不必擔心共用環境變數被覆寫是 Secret Manager 的最大優點。但我對明碼儲存有些疑慮，仍偏向<a href="/blog/secure-apikey-for-console-app/">如何在 .NET 工具程式安全儲存 API Key 設定</a>一文的概念，主張至少用 DPAPI 簡單加密也好，找時間再來研究怎麼摻在一起做瀨尿牛丸。</p> <p data-slug="dotnet-secrets-manager" data-fbimg="https://cdn.pixabay.com/photo/2015/11/10/22/29/odd-1037935_960_720.jpg#960#640"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>看程式範例學到用 .NET Secret Manager 儲存秘密設定(如 API Key)的小技巧。</p> <p>參考文件：<a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580">在 ASP.NET Core 的開發中安全儲存應用程式秘密</a></p> <p>應用程式會用到的一些具機密性設定 API Key 等，存放在 appsettings.son 或自訂 JSON/XML 容易連同原始碼一起外流，並有不小心簽進版控在雲端裸奔的風險(<a href="https://www.informationsecurity.com.tw/article/article_detail.aspx?aid=10934">資安人：超過18000個API 金鑰外洩! 影響多個主流網站使用之令牌</a>)，改存到環境變數是個選擇。環境變數的缺點是所有專案共用像全域變數，有被其他專案覆寫改掉的風險。.NET Core 3.0+ 推出 Secret Manager (秘密管理員)可用來儲存應用程式開發期間的敏感數據，每個專案擁有個別儲存空間(但要共用也可以)不會彼此干擾，儲存位置和方式等實作細節被 Secret Manager 隱藏 (Windows 在 <code>%APPDATA%\Microsoft\UserSecrets\&lt;user_secrets_id&gt;\secrets.json</code>，Linux/macOS 在 <code>~/.microsoft/usersecrets/&lt;user_secrets_id&gt;/secrets.json</code>)，程式端只需透過 IConfiguration 介面可比照 appsettings.json 或環境變數輕鬆讀取設定值。</p> <p>使用方式很簡單，在專案目錄下執行 <code>dotnet user-secrets init</code>[1] 後，.NET 會隨機產生專案專屬 UserSecretsId 寫入 .csproj [2]，接著便可使用 <code>dotnet user-secrets set "設定名稱" "設定值"</code> 進行設定 [3]：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638464510248193279.png"></p> <p>設定後，到 AppData\Roaming 使用者私有區可找到隨機 UserSecretsId 名稱的資料夾，其中有個 secrets.json，裡面存有設定值：(注意：設定值會以明碼儲存未加密，它主要是方便本機開發測試，勿視為可信任的存放區 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580#secret-manager">參考</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638464510249978653.png"></p> <p>在 .NET 專案如要使用 Secret Manager 儲存的設定，需先參照 Microsoft.Extensions.Configuration.UserSecrets 程式庫 <code>dotnet add package Microsoft.Extensions.Configuration.UserSecrets</code>，之後透過 <code>ConfigurationBuilder.AddUserSecrets&lt;Program&gt;()</code> 加為來源，其餘存取概念與 appsettings.json 一致：</p> <pre><code>using Microsoft.Extensions.Configuration;
IConfiguration config = new ConfigurationBuilder()
.AddUserSecrets&lt;Program&gt;().Build();
Console.WriteLine(config["OpenApiKey"]);
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638464510251620170.png"></p> <p>心得：.NET 內建支援，專案各有專屬存放區不必擔心共用環境變數被覆寫是 Secret Manager 的最大優點。但我對明碼儲存有些疑慮，仍偏向<a href="/blog/secure-apikey-for-console-app/">如何在 .NET 工具程式安全儲存 API Key 設定</a>一文的概念，主張至少用 DPAPI 簡單加密也好，找時間再來研究怎麼摻在一起做瀨尿牛丸。</p> <p data-slug="dotnet-secrets-manager" data-fbimg="https://cdn.pixabay.com/photo/2015/11/10/22/29/odd-1037935_960_720.jpg#960#640"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>看程式範例學到用 .NET Secret Manager 儲存秘密設定(如 API Key)的小技巧。</p> <p>參考文件：<a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580">在 ASP.NET Core 的開發中安全儲存應用程式秘密</a></p> <p>應用程式會用到的一些具機密性設定 API Key 等，存放在 appsettings.son 或自訂 JSON/XML 容易連同原始碼一起外流，並有不小心簽進版控在雲端裸奔的風險(<a href="https://www.informationsecurity.com.tw/article/article_detail.aspx?aid=10934">資安人：超過18000個API 金鑰外洩! 影響多個主流網站使用之令牌</a>)，改存到環境變數是個選擇。環境變數的缺點是所有專案共用像全域變數，有被其他專案覆寫改掉的風險。.NET Core 3.0+ 推出 Secret Manager (秘密管理員)可用來儲存應用程式開發期間的敏感數據，每個專案擁有個別儲存空間(但要共用也可以)不會彼此干擾，儲存位置和方式等實作細節被 Secret Manager 隱藏 (Windows 在 <code>%APPDATA%\Microsoft\UserSecrets\&lt;user_secrets_id&gt;\secrets.json</code>，Linux/macOS 在 <code>~/.microsoft/usersecrets/&lt;user_secrets_id&gt;/secrets.json</code>)，程式端只需透過 IConfiguration 介面可比照 appsettings.json 或環境變數輕鬆讀取設定值。</p> <p>使用方式很簡單，在專案目錄下執行 <code>dotnet user-secrets init</code>[1] 後，.NET 會隨機產生專案專屬 UserSecretsId 寫入 .csproj [2]，接著便可使用 <code>dotnet user-secrets set "設定名稱" "設定值"</code> 進行設定 [3]：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638464510248193279.png"></p> <p>設定後，到 AppData\Roaming 使用者私有區可找到隨機 UserSecretsId 名稱的資料夾，其中有個 secrets.json，裡面存有設定值：(注意：設定值會以明碼儲存未加密，它主要是方便本機開發測試，勿視為可信任的存放區 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/security/app-secrets?WT.mc_id=DOP-MVP-37580#secret-manager">參考</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638464510249978653.png"></p> <p>在 .NET 專案如要使用 Secret Manager 儲存的設定，需先參照 Microsoft.Extensions.Configuration.UserSecrets 程式庫 <code>dotnet add package Microsoft.Extensions.Configuration.UserSecrets</code>，之後透過 <code>ConfigurationBuilder.AddUserSecrets&lt;Program&gt;()</code> 加為來源，其餘存取概念與 appsettings.json 一致：</p> <pre><code>using Microsoft.Extensions.Configuration;
IConfiguration config = new ConfigurationBuilder()
.AddUserSecrets&lt;Program&gt;().Build();
Console.WriteLine(config["OpenApiKey"]);
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638464510251620170.png"></p> <p>心得：.NET 內建支援，專案各有專屬存放區不必擔心共用環境變數被覆寫是 Secret Manager 的最大優點。但我對明碼儲存有些疑慮，仍偏向<a href="/blog/secure-apikey-for-console-app/">如何在 .NET 工具程式安全儲存 API Key 設定</a>一文的概念，主張至少用 DPAPI 簡單加密也好，找時間再來研究怎麼摻在一起做瀨尿牛丸。</p> <p data-slug="dotnet-secrets-manager" data-fbimg="https://cdn.pixabay.com/photo/2015/11/10/22/29/odd-1037935_960_720.jpg#960#640"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638464510248193279.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638464510248193279.png" medium="image"/>
    <pubDate>Tue, 19 Mar 2024 13:17:14 GMT</pubDate>
  </item><item>
    <title><![CDATA[冷知識 - Window 資料夾「唯讀」屬性怎麼用？]]></title>
    <link>https://blog.darkthread.net/blog/win-dir-readonly-attrib/</link>
    <itunes:title><![CDATA[冷知識 - Window 資料夾「唯讀」屬性怎麼用？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>同事問起 Windows 資料夾的唯讀屬性。經驗裡我只改過檔案的唯讀屬性解決無法覆寫問題，從沒想過去管資料夾唯讀與否，但檔案總管看資料夾確實有個唯讀設定，它是做什麼用的？有什麼效果？</p> <p>經過實測，使用 attr +r 指令，設定唯讀後，資料夾照樣可以新增、覆寫及刪除檔案：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461624731289737.png"></p> <p>不過我注意到，檔案總管上的唯讀顯示的是 - 而不是打勾，這通常代表部分選取。而唯讀旁有註記 - 僅套用到資料夾中的檔案 (意思是不包含資料夾本身)。若改成勾再套用，會出現提示並只能選取「將變更套用到這個資料夾、子資料夾及檔案」選項。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461631085527335.png"></p> <p>回到資料夾本身的唯讀設定，在本草綱目對 <code>FILE_ATTRIBUTE_READONLY</code> 的<a href="https://learn.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants?WT.mc_id=DOP-MVP-37580#example">記載</a>可找到答案：</p> <blockquote> <p>A file that is read-only. Applications can read the file, but cannot write to it or delete it. This attribute is not honored on directories.<br> 檔案設成唯讀時，程式可讀取但不能覆寫或刪除；而此屬性對目錄無效。</p> </blockquote> <p>在 <a href="https://support.microsoft.com/en-us/topic/you-cannot-view-or-change-the-read-only-or-the-system-attributes-of-folders-in-windows-server-2003-in-windows-xp-in-windows-vista-or-in-windows-7-55bd5ec5-d19e-6173-0df1-8f5b49247165?WT.mc_id=DOP-MVP-37580">You cannot view or change the Read-only or the System attributes of folders in Windows Server 2003, in Windows XP, in Windows Vista or in Windows 7</a> 這篇 KB 則有更詳細說明：</p> <p>Note unlike the Read-only attribute for a file, the Read-only attribute for a folder is typically ignored by Windows, Windows components and accessories, and other programs. For example, you can delete, rename, and change a folder with the Read-only attribute by using Windows Explorer.</p> <p>The Read-only and System attributes is only used by Windows Explorer to determine whether the folder is a special folder, such as a system folder that has its view customized by Windows (for example, My Documents, Favorites, Fonts, Downloaded Program Files), or a folder that you customized by using the Customize tab of the folder's Properties dialog box. As a result, Windows Explorer does not allow you to view or change the Read-only or System attributes of folders. When a folder has the Read-Only attribute set it causes Explorer to request the Desktop.ini of that folder to see if any special folder settings need to be set. It has been seen where if a network share that has a large amount of folders set to Read-only, it can cause Explorer to take longer then what is expected to render the contents of that share while it waits on the retrieval of the Desktop.ini files. The slower the network connectivity to the share the longer this process can take to the point where Explorer may timeout waiting for the data and render nothing or appear to hang.</p> <p>Note In some previous versions of Windows, you can change the Read-only attribute for folders by using the Properties dialog box for the folder, but no versions of Windows permit you to change the System attribute by using Windows Explorer.</p> <p>大意是：</p> <p>與檔案的唯讀屬性不同，資料夾的唯讀屬性通常在 Windows、Windows 元件和附件以及其他程式中都會被忽略。例如，我們可以用 Windows Explorer 刪除、重新命名和更改具有唯讀屬性的資料夾。</p> <p>唯讀和系統屬性只被 Windows Explorer 用來判斷資料夾是否為特殊資料夾，例如 可自訂其檢視的系統資料夾（例如「我的文件」、「最愛」、「字型」、「下載的程式檔案」）或者使用者自訂檢視的資料夾。在 Windows 2003/XP/Vista/7 時代，甚至不允許查看或更改資料夾的唯讀或系統屬性。資料夾設定為唯讀屬性時還可能影響效能，甚至導致 Windows Explorer 超時等待資料無法顯示內容或卡住。KB 提到 在一些較早版本的 Windows 中可以使用資料夾的「屬性」對話方塊來更改唯讀屬性。</p> <p>因此到了 Windows 10/11，檔案總管又可以檢視及設定資料夾唯讀，但主要被用來設定檔案及子資料唯讀與否，資料夾本身的唯讀屬性無關痛癢，結案。</p> <p data-slug="win-dir-readonly-attrib" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>同事問起 Windows 資料夾的唯讀屬性。經驗裡我只改過檔案的唯讀屬性解決無法覆寫問題，從沒想過去管資料夾唯讀與否，但檔案總管看資料夾確實有個唯讀設定，它是做什麼用的？有什麼效果？</p> <p>經過實測，使用 attr +r 指令，設定唯讀後，資料夾照樣可以新增、覆寫及刪除檔案：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461624731289737.png"></p> <p>不過我注意到，檔案總管上的唯讀顯示的是 - 而不是打勾，這通常代表部分選取。而唯讀旁有註記 - 僅套用到資料夾中的檔案 (意思是不包含資料夾本身)。若改成勾再套用，會出現提示並只能選取「將變更套用到這個資料夾、子資料夾及檔案」選項。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461631085527335.png"></p> <p>回到資料夾本身的唯讀設定，在本草綱目對 <code>FILE_ATTRIBUTE_READONLY</code> 的<a href="https://learn.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants?WT.mc_id=DOP-MVP-37580#example">記載</a>可找到答案：</p> <blockquote> <p>A file that is read-only. Applications can read the file, but cannot write to it or delete it. This attribute is not honored on directories.<br> 檔案設成唯讀時，程式可讀取但不能覆寫或刪除；而此屬性對目錄無效。</p> </blockquote> <p>在 <a href="https://support.microsoft.com/en-us/topic/you-cannot-view-or-change-the-read-only-or-the-system-attributes-of-folders-in-windows-server-2003-in-windows-xp-in-windows-vista-or-in-windows-7-55bd5ec5-d19e-6173-0df1-8f5b49247165?WT.mc_id=DOP-MVP-37580">You cannot view or change the Read-only or the System attributes of folders in Windows Server 2003, in Windows XP, in Windows Vista or in Windows 7</a> 這篇 KB 則有更詳細說明：</p> <p>Note unlike the Read-only attribute for a file, the Read-only attribute for a folder is typically ignored by Windows, Windows components and accessories, and other programs. For example, you can delete, rename, and change a folder with the Read-only attribute by using Windows Explorer.</p> <p>The Read-only and System attributes is only used by Windows Explorer to determine whether the folder is a special folder, such as a system folder that has its view customized by Windows (for example, My Documents, Favorites, Fonts, Downloaded Program Files), or a folder that you customized by using the Customize tab of the folder's Properties dialog box. As a result, Windows Explorer does not allow you to view or change the Read-only or System attributes of folders. When a folder has the Read-Only attribute set it causes Explorer to request the Desktop.ini of that folder to see if any special folder settings need to be set. It has been seen where if a network share that has a large amount of folders set to Read-only, it can cause Explorer to take longer then what is expected to render the contents of that share while it waits on the retrieval of the Desktop.ini files. The slower the network connectivity to the share the longer this process can take to the point where Explorer may timeout waiting for the data and render nothing or appear to hang.</p> <p>Note In some previous versions of Windows, you can change the Read-only attribute for folders by using the Properties dialog box for the folder, but no versions of Windows permit you to change the System attribute by using Windows Explorer.</p> <p>大意是：</p> <p>與檔案的唯讀屬性不同，資料夾的唯讀屬性通常在 Windows、Windows 元件和附件以及其他程式中都會被忽略。例如，我們可以用 Windows Explorer 刪除、重新命名和更改具有唯讀屬性的資料夾。</p> <p>唯讀和系統屬性只被 Windows Explorer 用來判斷資料夾是否為特殊資料夾，例如 可自訂其檢視的系統資料夾（例如「我的文件」、「最愛」、「字型」、「下載的程式檔案」）或者使用者自訂檢視的資料夾。在 Windows 2003/XP/Vista/7 時代，甚至不允許查看或更改資料夾的唯讀或系統屬性。資料夾設定為唯讀屬性時還可能影響效能，甚至導致 Windows Explorer 超時等待資料無法顯示內容或卡住。KB 提到 在一些較早版本的 Windows 中可以使用資料夾的「屬性」對話方塊來更改唯讀屬性。</p> <p>因此到了 Windows 10/11，檔案總管又可以檢視及設定資料夾唯讀，但主要被用來設定檔案及子資料唯讀與否，資料夾本身的唯讀屬性無關痛癢，結案。</p> <p data-slug="win-dir-readonly-attrib" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>同事問起 Windows 資料夾的唯讀屬性。經驗裡我只改過檔案的唯讀屬性解決無法覆寫問題，從沒想過去管資料夾唯讀與否，但檔案總管看資料夾確實有個唯讀設定，它是做什麼用的？有什麼效果？</p> <p>經過實測，使用 attr +r 指令，設定唯讀後，資料夾照樣可以新增、覆寫及刪除檔案：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461624731289737.png"></p> <p>不過我注意到，檔案總管上的唯讀顯示的是 - 而不是打勾，這通常代表部分選取。而唯讀旁有註記 - 僅套用到資料夾中的檔案 (意思是不包含資料夾本身)。若改成勾再套用，會出現提示並只能選取「將變更套用到這個資料夾、子資料夾及檔案」選項。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461631085527335.png"></p> <p>回到資料夾本身的唯讀設定，在本草綱目對 <code>FILE_ATTRIBUTE_READONLY</code> 的<a href="https://learn.microsoft.com/en-us/windows/win32/fileio/file-attribute-constants?WT.mc_id=DOP-MVP-37580#example">記載</a>可找到答案：</p> <blockquote> <p>A file that is read-only. Applications can read the file, but cannot write to it or delete it. This attribute is not honored on directories.<br> 檔案設成唯讀時，程式可讀取但不能覆寫或刪除；而此屬性對目錄無效。</p> </blockquote> <p>在 <a href="https://support.microsoft.com/en-us/topic/you-cannot-view-or-change-the-read-only-or-the-system-attributes-of-folders-in-windows-server-2003-in-windows-xp-in-windows-vista-or-in-windows-7-55bd5ec5-d19e-6173-0df1-8f5b49247165?WT.mc_id=DOP-MVP-37580">You cannot view or change the Read-only or the System attributes of folders in Windows Server 2003, in Windows XP, in Windows Vista or in Windows 7</a> 這篇 KB 則有更詳細說明：</p> <p>Note unlike the Read-only attribute for a file, the Read-only attribute for a folder is typically ignored by Windows, Windows components and accessories, and other programs. For example, you can delete, rename, and change a folder with the Read-only attribute by using Windows Explorer.</p> <p>The Read-only and System attributes is only used by Windows Explorer to determine whether the folder is a special folder, such as a system folder that has its view customized by Windows (for example, My Documents, Favorites, Fonts, Downloaded Program Files), or a folder that you customized by using the Customize tab of the folder's Properties dialog box. As a result, Windows Explorer does not allow you to view or change the Read-only or System attributes of folders. When a folder has the Read-Only attribute set it causes Explorer to request the Desktop.ini of that folder to see if any special folder settings need to be set. It has been seen where if a network share that has a large amount of folders set to Read-only, it can cause Explorer to take longer then what is expected to render the contents of that share while it waits on the retrieval of the Desktop.ini files. The slower the network connectivity to the share the longer this process can take to the point where Explorer may timeout waiting for the data and render nothing or appear to hang.</p> <p>Note In some previous versions of Windows, you can change the Read-only attribute for folders by using the Properties dialog box for the folder, but no versions of Windows permit you to change the System attribute by using Windows Explorer.</p> <p>大意是：</p> <p>與檔案的唯讀屬性不同，資料夾的唯讀屬性通常在 Windows、Windows 元件和附件以及其他程式中都會被忽略。例如，我們可以用 Windows Explorer 刪除、重新命名和更改具有唯讀屬性的資料夾。</p> <p>唯讀和系統屬性只被 Windows Explorer 用來判斷資料夾是否為特殊資料夾，例如 可自訂其檢視的系統資料夾（例如「我的文件」、「最愛」、「字型」、「下載的程式檔案」）或者使用者自訂檢視的資料夾。在 Windows 2003/XP/Vista/7 時代，甚至不允許查看或更改資料夾的唯讀或系統屬性。資料夾設定為唯讀屬性時還可能影響效能，甚至導致 Windows Explorer 超時等待資料無法顯示內容或卡住。KB 提到 在一些較早版本的 Windows 中可以使用資料夾的「屬性」對話方塊來更改唯讀屬性。</p> <p>因此到了 Windows 10/11，檔案總管又可以檢視及設定資料夾唯讀，但主要被用來設定檔案及子資料唯讀與否，資料夾本身的唯讀屬性無關痛癢，結案。</p> <p data-slug="win-dir-readonly-attrib" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638461624731289737.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638461624731289737.png" medium="image"/>
    <pubDate>Sat, 16 Mar 2024 05:17:17 GMT</pubDate>
  </item><item>
    <title><![CDATA[【茶包射手日記】FB 無法產生網頁預覽 (HTTP 418 - 我是一個茶壺)]]></title>
    <link>https://blog.darkthread.net/blog/fb-share-debugger-http-418/</link>
    <itunes:title><![CDATA[【茶包射手日記】FB 無法產生網頁預覽 (HTTP 418 - 我是一個茶壺)]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>幫忙查了一個 FB 相關問題。</p> <p>一般我們在 FB 貼文輸入網址時，FB 會擷取網頁資訊帶出標題、摘要、預覽圖，這些資訊來自網頁 HTML 所埋的 og 標籤，例如：</p> <pre><code>  &lt;meta property=og:type content=article&gt;
&lt;meta property=og:title content="Facebook 分享預覽測試"&gt;
&lt;meta property=og:description content="測試透過 og 標籤提供預覽資訊"&gt;
&lt;meta property=og:url content="https://output.jsbin.com/jokufer"&gt;
&lt;meta property=og:image content="https://i.imgur.com/nE2We37.png"&gt;
&lt;meta property=og:image:width content="660"&gt;
&lt;meta property=og:image:height content="378"&gt;
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461134669368356.png"></p> <p>若遇到預覽結果不如預期，FB 有提供一個「分享偵錯工具」幫忙 Debug。(詳細介面請參考這篇：<a href="/blog/open-graph-tags/">網頁小知識 - 美美的 FB/LINE 連結預覽是如何產生的？</a>)</p> <p>本次遇到的問題是，某網站在 FB 分享時預覽出現是舊資訊，而用分享偵錯工具無法更新，出現一個奇怪的 418 錯誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461134671679120.png"></p> <p>查了一下，418 是個奇妙的 HTTP 狀態碼，代表「我是一個茶壺」，是 IETF 網際網路工程任務組在 2014年4月1日發布的惡搞 RFC - <a href="https://zh.wikipedia.org/zh-tw/%E8%B6%85%E6%96%87%E6%9C%AC%E5%92%96%E5%95%A1%E5%A3%B6%E6%8E%A7%E5%88%B6%E5%8D%8F%E8%AE%AE">HTCPCP (Hyper Text Coffee Pot Control Protocol) 超文字咖啡壺控制協定</a>，應用在控制、監測和診斷咖啡壺，後來也被拓展到茶壺。最終成為一個非標準 HTTP 狀態碼。(若想了解這段有趣的歷史，推薦這篇：<a href="https://blog.techbridge.cc/2019/06/15/iam-a-teapot-418/">搶救茶壺大作戰：418 I am a teapot</a>)</p> <p>爬文的心得，418 狀態碼最常出現在 WAF (網站應用程式防火牆) 阻擋爬蟲機器人或惡意程式的回應：</p> <ul><li><a href="https://support.huaweicloud.com/intl/en-us/waf_faq/waf_01_0198.html">Why Am I Seeing Error Code 418? Web Application Firewall FAQs / Service Interruption Check (huaweicloud.com)</a><br> If the request contains malicious load and is intercepted by WAF, error 418 is reported when you access the domain name protected by WAF. You can view WAF protection logs to view the cause. For details about event logs.</li> <li><a href="https://support.rankmath.com/ticket/receiving-error-418-for-image-on-fb/#post-726551">Receiving Error 418 for image on FB - Support - Rank Math</a><br> The 418 response code is showing – Curl Timeout The request to scrape the URL timed out. This can happen if your server’s firewall is blocking Facebook’s crawlers.<br> In this case, we recommend getting in touch with your web host to check your site further.</li></ul> <p>因此推論，最有可能是來自 FB 機器人的 HTTP 存取被 WAF 擋掉，Curl 等到 Timeout。但調閱 WAF 記錄核對時間點有找到 FB 機器人連上來的軌跡，請求並未被阻擋，Log 顯示是客戶端主動切斷連線。我查到 <a href="https://developers.facebook.com/docs/sharing/webmasters/crawler/">FB 文件</a>，查到可用 <code>whois -h whois.radb.net -- '-i origin AS32934' | grep ^route</code> 列舉 Facebook 網路爬蟲來源 IP，用 <code>curl -v --compressed -H "Range: bytes=0-524288" -H "Connection: close" -A "facebookexternalhit/1.1 (+http://www.facebook.com/externalhit_uatext.php)" "$URL"</code>模擬機器人行為。在 Windows 跑了一下可正常得到回應，莫非與來源 IP 有關，但也不可能從 FB 的 IP 發 HTTP GET 測試啊! 情陷入焦著之際，我找了一台 Azure Linux VM 試從美國跑 curl 得到不一樣的結果，案情有了突破，curl 回應 <code>curl: (60) SSL certificate problem: unable to get local issuer certificate</code> 錯誤。在 Windows 跑 curl 跟用瀏覽器連該網站沒問題，用 Linux 跑不行，合理推論是 Linux 不認得該 TLS 憑證的中繼 CA 憑證或根 CA 憑證。</p> <p>用<a href="/blog/show-certs-w-ps/">網站 TLS 憑證 CLI 快速檢視工具</a>檢查，發現是少裝了中繼 CA 憑證跟根 CA 憑證，Windows 平台可能靠自動更新或從其他來源取得中繼 CA 憑證，故可順利過關，應該可解釋為何移到 Linux 測試 curl 才重現問題。問題在補上中繼 CA 憑證後排除，我也學到新經驗：</p> <p>HTTP 418 是 WAF 阻擋機器人常見的回應，但 Facebook 爬蟲機器人遇到 TLS 憑證異常時不會顯示詳細錯誤，而是歸納成 418 錯誤。</p> <p data-slug="fb-share-debugger-http-418" data-fbimg="https://cdn.pixabay.com/photo/2022/10/31/17/31/copper-teapots-7560392_640.jpg#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>幫忙查了一個 FB 相關問題。</p> <p>一般我們在 FB 貼文輸入網址時，FB 會擷取網頁資訊帶出標題、摘要、預覽圖，這些資訊來自網頁 HTML 所埋的 og 標籤，例如：</p> <pre><code>  &lt;meta property=og:type content=article&gt;
&lt;meta property=og:title content="Facebook 分享預覽測試"&gt;
&lt;meta property=og:description content="測試透過 og 標籤提供預覽資訊"&gt;
&lt;meta property=og:url content="https://output.jsbin.com/jokufer"&gt;
&lt;meta property=og:image content="https://i.imgur.com/nE2We37.png"&gt;
&lt;meta property=og:image:width content="660"&gt;
&lt;meta property=og:image:height content="378"&gt;
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461134669368356.png"></p> <p>若遇到預覽結果不如預期，FB 有提供一個「分享偵錯工具」幫忙 Debug。(詳細介面請參考這篇：<a href="/blog/open-graph-tags/">網頁小知識 - 美美的 FB/LINE 連結預覽是如何產生的？</a>)</p> <p>本次遇到的問題是，某網站在 FB 分享時預覽出現是舊資訊，而用分享偵錯工具無法更新，出現一個奇怪的 418 錯誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461134671679120.png"></p> <p>查了一下，418 是個奇妙的 HTTP 狀態碼，代表「我是一個茶壺」，是 IETF 網際網路工程任務組在 2014年4月1日發布的惡搞 RFC - <a href="https://zh.wikipedia.org/zh-tw/%E8%B6%85%E6%96%87%E6%9C%AC%E5%92%96%E5%95%A1%E5%A3%B6%E6%8E%A7%E5%88%B6%E5%8D%8F%E8%AE%AE">HTCPCP (Hyper Text Coffee Pot Control Protocol) 超文字咖啡壺控制協定</a>，應用在控制、監測和診斷咖啡壺，後來也被拓展到茶壺。最終成為一個非標準 HTTP 狀態碼。(若想了解這段有趣的歷史，推薦這篇：<a href="https://blog.techbridge.cc/2019/06/15/iam-a-teapot-418/">搶救茶壺大作戰：418 I am a teapot</a>)</p> <p>爬文的心得，418 狀態碼最常出現在 WAF (網站應用程式防火牆) 阻擋爬蟲機器人或惡意程式的回應：</p> <ul><li><a href="https://support.huaweicloud.com/intl/en-us/waf_faq/waf_01_0198.html">Why Am I Seeing Error Code 418? Web Application Firewall FAQs / Service Interruption Check (huaweicloud.com)</a><br> If the request contains malicious load and is intercepted by WAF, error 418 is reported when you access the domain name protected by WAF. You can view WAF protection logs to view the cause. For details about event logs.</li> <li><a href="https://support.rankmath.com/ticket/receiving-error-418-for-image-on-fb/#post-726551">Receiving Error 418 for image on FB - Support - Rank Math</a><br> The 418 response code is showing – Curl Timeout The request to scrape the URL timed out. This can happen if your server’s firewall is blocking Facebook’s crawlers.<br> In this case, we recommend getting in touch with your web host to check your site further.</li></ul> <p>因此推論，最有可能是來自 FB 機器人的 HTTP 存取被 WAF 擋掉，Curl 等到 Timeout。但調閱 WAF 記錄核對時間點有找到 FB 機器人連上來的軌跡，請求並未被阻擋，Log 顯示是客戶端主動切斷連線。我查到 <a href="https://developers.facebook.com/docs/sharing/webmasters/crawler/">FB 文件</a>，查到可用 <code>whois -h whois.radb.net -- '-i origin AS32934' | grep ^route</code> 列舉 Facebook 網路爬蟲來源 IP，用 <code>curl -v --compressed -H "Range: bytes=0-524288" -H "Connection: close" -A "facebookexternalhit/1.1 (+http://www.facebook.com/externalhit_uatext.php)" "$URL"</code>模擬機器人行為。在 Windows 跑了一下可正常得到回應，莫非與來源 IP 有關，但也不可能從 FB 的 IP 發 HTTP GET 測試啊! 情陷入焦著之際，我找了一台 Azure Linux VM 試從美國跑 curl 得到不一樣的結果，案情有了突破，curl 回應 <code>curl: (60) SSL certificate problem: unable to get local issuer certificate</code> 錯誤。在 Windows 跑 curl 跟用瀏覽器連該網站沒問題，用 Linux 跑不行，合理推論是 Linux 不認得該 TLS 憑證的中繼 CA 憑證或根 CA 憑證。</p> <p>用<a href="/blog/show-certs-w-ps/">網站 TLS 憑證 CLI 快速檢視工具</a>檢查，發現是少裝了中繼 CA 憑證跟根 CA 憑證，Windows 平台可能靠自動更新或從其他來源取得中繼 CA 憑證，故可順利過關，應該可解釋為何移到 Linux 測試 curl 才重現問題。問題在補上中繼 CA 憑證後排除，我也學到新經驗：</p> <p>HTTP 418 是 WAF 阻擋機器人常見的回應，但 Facebook 爬蟲機器人遇到 TLS 憑證異常時不會顯示詳細錯誤，而是歸納成 418 錯誤。</p> <p data-slug="fb-share-debugger-http-418" data-fbimg="https://cdn.pixabay.com/photo/2022/10/31/17/31/copper-teapots-7560392_640.jpg#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>幫忙查了一個 FB 相關問題。</p> <p>一般我們在 FB 貼文輸入網址時，FB 會擷取網頁資訊帶出標題、摘要、預覽圖，這些資訊來自網頁 HTML 所埋的 og 標籤，例如：</p> <pre><code>  &lt;meta property=og:type content=article&gt;
&lt;meta property=og:title content="Facebook 分享預覽測試"&gt;
&lt;meta property=og:description content="測試透過 og 標籤提供預覽資訊"&gt;
&lt;meta property=og:url content="https://output.jsbin.com/jokufer"&gt;
&lt;meta property=og:image content="https://i.imgur.com/nE2We37.png"&gt;
&lt;meta property=og:image:width content="660"&gt;
&lt;meta property=og:image:height content="378"&gt;
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638461134669368356.png"></p> <p>若遇到預覽結果不如預期，FB 有提供一個「分享偵錯工具」幫忙 Debug。(詳細介面請參考這篇：<a href="/blog/open-graph-tags/">網頁小知識 - 美美的 FB/LINE 連結預覽是如何產生的？</a>)</p> <p>本次遇到的問題是，某網站在 FB 分享時預覽出現是舊資訊，而用分享偵錯工具無法更新，出現一個奇怪的 418 錯誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638461134671679120.png"></p> <p>查了一下，418 是個奇妙的 HTTP 狀態碼，代表「我是一個茶壺」，是 IETF 網際網路工程任務組在 2014年4月1日發布的惡搞 RFC - <a href="https://zh.wikipedia.org/zh-tw/%E8%B6%85%E6%96%87%E6%9C%AC%E5%92%96%E5%95%A1%E5%A3%B6%E6%8E%A7%E5%88%B6%E5%8D%8F%E8%AE%AE">HTCPCP (Hyper Text Coffee Pot Control Protocol) 超文字咖啡壺控制協定</a>，應用在控制、監測和診斷咖啡壺，後來也被拓展到茶壺。最終成為一個非標準 HTTP 狀態碼。(若想了解這段有趣的歷史，推薦這篇：<a href="https://blog.techbridge.cc/2019/06/15/iam-a-teapot-418/">搶救茶壺大作戰：418 I am a teapot</a>)</p> <p>爬文的心得，418 狀態碼最常出現在 WAF (網站應用程式防火牆) 阻擋爬蟲機器人或惡意程式的回應：</p> <ul><li><a href="https://support.huaweicloud.com/intl/en-us/waf_faq/waf_01_0198.html">Why Am I Seeing Error Code 418? Web Application Firewall FAQs / Service Interruption Check (huaweicloud.com)</a><br> If the request contains malicious load and is intercepted by WAF, error 418 is reported when you access the domain name protected by WAF. You can view WAF protection logs to view the cause. For details about event logs.</li> <li><a href="https://support.rankmath.com/ticket/receiving-error-418-for-image-on-fb/#post-726551">Receiving Error 418 for image on FB - Support - Rank Math</a><br> The 418 response code is showing – Curl Timeout The request to scrape the URL timed out. This can happen if your server’s firewall is blocking Facebook’s crawlers.<br> In this case, we recommend getting in touch with your web host to check your site further.</li></ul> <p>因此推論，最有可能是來自 FB 機器人的 HTTP 存取被 WAF 擋掉，Curl 等到 Timeout。但調閱 WAF 記錄核對時間點有找到 FB 機器人連上來的軌跡，請求並未被阻擋，Log 顯示是客戶端主動切斷連線。我查到 <a href="https://developers.facebook.com/docs/sharing/webmasters/crawler/">FB 文件</a>，查到可用 <code>whois -h whois.radb.net -- '-i origin AS32934' | grep ^route</code> 列舉 Facebook 網路爬蟲來源 IP，用 <code>curl -v --compressed -H "Range: bytes=0-524288" -H "Connection: close" -A "facebookexternalhit/1.1 (+http://www.facebook.com/externalhit_uatext.php)" "$URL"</code>模擬機器人行為。在 Windows 跑了一下可正常得到回應，莫非與來源 IP 有關，但也不可能從 FB 的 IP 發 HTTP GET 測試啊! 情陷入焦著之際，我找了一台 Azure Linux VM 試從美國跑 curl 得到不一樣的結果，案情有了突破，curl 回應 <code>curl: (60) SSL certificate problem: unable to get local issuer certificate</code> 錯誤。在 Windows 跑 curl 跟用瀏覽器連該網站沒問題，用 Linux 跑不行，合理推論是 Linux 不認得該 TLS 憑證的中繼 CA 憑證或根 CA 憑證。</p> <p>用<a href="/blog/show-certs-w-ps/">網站 TLS 憑證 CLI 快速檢視工具</a>檢查，發現是少裝了中繼 CA 憑證跟根 CA 憑證，Windows 平台可能靠自動更新或從其他來源取得中繼 CA 憑證，故可順利過關，應該可解釋為何移到 Linux 測試 curl 才重現問題。問題在補上中繼 CA 憑證後排除，我也學到新經驗：</p> <p>HTTP 418 是 WAF 阻擋機器人常見的回應，但 Facebook 爬蟲機器人遇到 TLS 憑證異常時不會顯示詳細錯誤，而是歸納成 418 錯誤。</p> <p data-slug="fb-share-debugger-http-418" data-fbimg="https://cdn.pixabay.com/photo/2022/10/31/17/31/copper-teapots-7560392_640.jpg#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig2_638461134669368356.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig2_638461134669368356.png" medium="image"/>
    <pubDate>Fri, 15 Mar 2024 15:30:17 GMT</pubDate>
  </item><item>
    <title><![CDATA[使用 .NET Git 程式庫 LibGit2Sharp 檔案不落地生成異動對照表]]></title>
    <link>https://blog.darkthread.net/blog/libgit2sharp/</link>
    <itunes:title><![CDATA[使用 .NET Git 程式庫 LibGit2Sharp 檔案不落地生成異動對照表]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>遇到一個小需求，想用 .NET 出一份檔案修改前後對照表，我心中最理想的方案是用 <a href="/blog/diff2html-webpage/">git diff + diff2html</a> 產生 HTML 報表，省時省力又好看。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638452451361125707.png"></p> <p>git diff 指令跟產生 diff2html 網頁技能是現成的，最不花腦的解決方法是用 .NET 將修改前後檔案雙雙寫成檔案，呼叫外部程式 git.exe 跑 <code>git diff a.txt b.txt</code> 取得結果，再交給 diff2html 顯示對照表。有沒有更輕巧簡潔的解法？</p> <p><a href="https://libgit2.org/">libgit2</a> 是一個用 C 開發的開源 Git 可攜程式庫，讓你不用安裝 Git 直接引用 Git 的各項功能，<a href="https://github.com/libgit2/libgit2sharp">libgit2sharp</a> 則為 libgit2 提供了 .NET 相容介面，會比呼叫 git.exe 更優雅。</p> <p>在專案執行 <code>dotnet add package libgit2sharp</code> 或從 NuGet 安裝後，就可以在 .NET 程式裡直接進行各項 Git 操作。</p> <p>以下是個簡單範例，示範 用 C# 建立一個 Git 儲存庫，加入 test.txt、Commit、修改 test.txt、再次 Commit、列出 Commit 記錄、顯示 Commit 的異動內容：</p> <pre><code>using LibGit2Sharp;
Func&lt;string&gt; CreateTempRepo = () =&gt;
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
};
var userName = "Tester";
var email = "tester@mail.net";
// Signature 物件，代表 Commit 的作者或提交者
Func&lt;Signature&gt; getSignature = () =&gt; new Signature(userName, email, DateTimeOffset.Now);
var repoPath = CreateTempRepo();
using (var repo = new Repository(repoPath))
{
var fileName = "test.txt";
var path = Path.Combine(repoPath, fileName);
File.WriteAllText(path, "Hello, World!
First Commit");
Commands.Stage(repo, fileName);
repo.Commit("Initial commit",
// Author       Committer
getSignature(), getSignature());
File.AppendAllText(path, "
Second Commit");
Commands.Stage(repo, fileName);
Thread.Sleep(1000);
repo.Commit("Second commit", getSignature(), getSignature());
print("**** Commit 紀錄 ****", ConsoleColor.Magenta);
foreach (var commit in repo.Commits)
{
print("Commit: " + commit.Id, ConsoleColor.Cyan);
print("  " + commit.Message, noNewLine: true);
print($"  by {commit.Author.Name} at {commit.Author.When:HH:mm:ss}", ConsoleColor.Green);
}
var commit1 = repo.Commits.Last();
var commit2 = repo.Commits.First();
var changes = repo.Diff.Compare&lt;Patch&gt;(commit1.Tree, commit2.Tree);
print("**** 異動比對 ****", ConsoleColor.Magenta);
foreach (var change in changes)
{
print(change.Patch, ConsoleColor.DarkYellow);
}
}
void print(string text, ConsoleColor color = ConsoleColor.White, bool noNewLine = false)
{
Console.ForegroundColor = color;
if (noNewLine)
Console.Write(text);
else
Console.WriteLine(text);
Console.ResetColor();
}
</code></pre> <p>輕輕鬆鬆搞定。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638452451363215435.png"></p> <p>最後，來看一下一開始的 XML 修改前後對照是怎麼做出來的？</p> <pre><code>public static void Run()
{
var xml= XDocument.Parse(@"
&lt;list&gt;
&lt;item id=""N1""&gt;Item 1&lt;/item&gt;
&lt;item id=""N2""&gt;Item 2&lt;/item&gt;
&lt;item id=""N3""&gt;Item 3&lt;/item&gt;
&lt;item id=""N4""&gt;Item 4&lt;/item&gt;
&lt;/list&gt;
");
var origin = xml.ToString();
xml.Root.Elements("item").Skip(1).First().SetAttributeValue("id", "N2-new");
xml.Root.Elements("item").Skip(2).First().Value = "Item 3 (modified)";
xml.Root.Add(new XElement("item", new XAttribute("id", "N5"), "Item 5"));
var modified = xml.ToString();
var html = GitDiffTool.GenGitDiffHtmlReport(origin, modified, "origin.xml", "modified.xml");
File.WriteAllText("D:\\Report.html", html);
// 用預設瀏覽器開啟
System.Diagnostics.Process.Start("cmd", @"/c ""start D:\Report.html""");
}
</code></pre> <p>關鍵的 GitDiffTool.GenGitDiffHtmlReport 如下，建立一個暫存目錄，直接將修改前後字串內容轉成 Blob 交由 Diff.Comapre() 比對。結果字串交給 diff2html 顯示，過程我用了 <a href="/blog/gzip-decompression-with-js/">HTML 單檔文件挑戰 - 內嵌 .js GZIP 壓縮檔</a>展示過的技巧，將差異結果用 Base64 內嵌在網頁裡。</p> <pre><code>using System.Text;
using LibGit2Sharp;
public class GitDiffTool
{
static string CreateTempRepo()
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
}
const string html = @"
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;link rel=""stylesheet"" type=""text/css""
href=""https://cdn.jsdelivr.net/npm/diff2html/bundles/css/diff2html.min.css"" /&gt;
&lt;script src=""https://cdn.jsdelivr.net/npm/diff2html/bundles/js/diff2html.min.js""&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script id='diff.txt' type=""text/base64""&gt;
[$diff.txt$]
&lt;/script&gt;
&lt;div id=report&gt;
&lt;/div&gt;
&lt;script&gt;
var diffText = new TextDecoder().decode(
new Uint8Array(
atob(
document.getElementById('diff.txt').innerHTML.replace(/[
\r \t]/g, '')
).split('').map(c =&gt; c.charCodeAt(0))
)
);
var diffHtml = Diff2Html.html(diffText, {
drawFileList: true,
matching: 'lines',
outputFormat: 'side-by-side',
});
document.getElementById('report').innerHTML = diffHtml;
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;";
public static string GenGitDiff(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
using (var repo = new Repository(CreateTempRepo()))
{
var lines = new[] { "Line 1", "Line 2", "Line 3" };
var blob1 = repo.ObjectDatabase.CreateBlob(
// 由字串建立 Blob 物件 (也可改成讀檔案傳入 FileStream)
new MemoryStream(Encoding.UTF8.GetBytes(oldText)));
var blob2 = repo.ObjectDatabase.CreateBlob(
new MemoryStream(Encoding.UTF8.GetBytes(newText)));
// 比較兩個 Blob 物件差異
var patch = $@"
diff --git a/{oldFile} b/{newFile}
index aaaaaaa..bbbbbbb 100644
--- a/{oldFile}
+++ b/{newFile}
{repo.Diff.Compare(blob1, blob2).Patch}";
return patch;
}
}
public static string GenGitDiffHtmlReport(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
var diff = GenGitDiff(oldText, newText, oldFile, newFile);
var base64 = Convert.ToBase64String(Encoding.UTF8.GetBytes(diff));
var pem = new StringBuilder();
const int maxWidth = 160;
for (int i = 0; i &lt; base64.Length; i += maxWidth)
{
pem.AppendLine(base64.Substring(i, Math.Min(maxWidth, base64.Length - i)));
}
return html.Replace("[$diff.txt$]", pem.ToString());
}
}
</code></pre> <p>就醬，又解掉一件任務。</p> <p data-slug="libgit2sharp" data-fbimg="https://i.imgur.com/0UbIRZt.png#960#540"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>遇到一個小需求，想用 .NET 出一份檔案修改前後對照表，我心中最理想的方案是用 <a href="/blog/diff2html-webpage/">git diff + diff2html</a> 產生 HTML 報表，省時省力又好看。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638452451361125707.png"></p> <p>git diff 指令跟產生 diff2html 網頁技能是現成的，最不花腦的解決方法是用 .NET 將修改前後檔案雙雙寫成檔案，呼叫外部程式 git.exe 跑 <code>git diff a.txt b.txt</code> 取得結果，再交給 diff2html 顯示對照表。有沒有更輕巧簡潔的解法？</p> <p><a href="https://libgit2.org/">libgit2</a> 是一個用 C 開發的開源 Git 可攜程式庫，讓你不用安裝 Git 直接引用 Git 的各項功能，<a href="https://github.com/libgit2/libgit2sharp">libgit2sharp</a> 則為 libgit2 提供了 .NET 相容介面，會比呼叫 git.exe 更優雅。</p> <p>在專案執行 <code>dotnet add package libgit2sharp</code> 或從 NuGet 安裝後，就可以在 .NET 程式裡直接進行各項 Git 操作。</p> <p>以下是個簡單範例，示範 用 C# 建立一個 Git 儲存庫，加入 test.txt、Commit、修改 test.txt、再次 Commit、列出 Commit 記錄、顯示 Commit 的異動內容：</p> <pre><code>using LibGit2Sharp;
Func&lt;string&gt; CreateTempRepo = () =&gt;
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
};
var userName = "Tester";
var email = "tester@mail.net";
// Signature 物件，代表 Commit 的作者或提交者
Func&lt;Signature&gt; getSignature = () =&gt; new Signature(userName, email, DateTimeOffset.Now);
var repoPath = CreateTempRepo();
using (var repo = new Repository(repoPath))
{
var fileName = "test.txt";
var path = Path.Combine(repoPath, fileName);
File.WriteAllText(path, "Hello, World!
First Commit");
Commands.Stage(repo, fileName);
repo.Commit("Initial commit",
// Author       Committer
getSignature(), getSignature());
File.AppendAllText(path, "
Second Commit");
Commands.Stage(repo, fileName);
Thread.Sleep(1000);
repo.Commit("Second commit", getSignature(), getSignature());
print("**** Commit 紀錄 ****", ConsoleColor.Magenta);
foreach (var commit in repo.Commits)
{
print("Commit: " + commit.Id, ConsoleColor.Cyan);
print("  " + commit.Message, noNewLine: true);
print($"  by {commit.Author.Name} at {commit.Author.When:HH:mm:ss}", ConsoleColor.Green);
}
var commit1 = repo.Commits.Last();
var commit2 = repo.Commits.First();
var changes = repo.Diff.Compare&lt;Patch&gt;(commit1.Tree, commit2.Tree);
print("**** 異動比對 ****", ConsoleColor.Magenta);
foreach (var change in changes)
{
print(change.Patch, ConsoleColor.DarkYellow);
}
}
void print(string text, ConsoleColor color = ConsoleColor.White, bool noNewLine = false)
{
Console.ForegroundColor = color;
if (noNewLine)
Console.Write(text);
else
Console.WriteLine(text);
Console.ResetColor();
}
</code></pre> <p>輕輕鬆鬆搞定。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638452451363215435.png"></p> <p>最後，來看一下一開始的 XML 修改前後對照是怎麼做出來的？</p> <pre><code>public static void Run()
{
var xml= XDocument.Parse(@"
&lt;list&gt;
&lt;item id=""N1""&gt;Item 1&lt;/item&gt;
&lt;item id=""N2""&gt;Item 2&lt;/item&gt;
&lt;item id=""N3""&gt;Item 3&lt;/item&gt;
&lt;item id=""N4""&gt;Item 4&lt;/item&gt;
&lt;/list&gt;
");
var origin = xml.ToString();
xml.Root.Elements("item").Skip(1).First().SetAttributeValue("id", "N2-new");
xml.Root.Elements("item").Skip(2).First().Value = "Item 3 (modified)";
xml.Root.Add(new XElement("item", new XAttribute("id", "N5"), "Item 5"));
var modified = xml.ToString();
var html = GitDiffTool.GenGitDiffHtmlReport(origin, modified, "origin.xml", "modified.xml");
File.WriteAllText("D:\\Report.html", html);
// 用預設瀏覽器開啟
System.Diagnostics.Process.Start("cmd", @"/c ""start D:\Report.html""");
}
</code></pre> <p>關鍵的 GitDiffTool.GenGitDiffHtmlReport 如下，建立一個暫存目錄，直接將修改前後字串內容轉成 Blob 交由 Diff.Comapre() 比對。結果字串交給 diff2html 顯示，過程我用了 <a href="/blog/gzip-decompression-with-js/">HTML 單檔文件挑戰 - 內嵌 .js GZIP 壓縮檔</a>展示過的技巧，將差異結果用 Base64 內嵌在網頁裡。</p> <pre><code>using System.Text;
using LibGit2Sharp;
public class GitDiffTool
{
static string CreateTempRepo()
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
}
const string html = @"
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;link rel=""stylesheet"" type=""text/css""
href=""https://cdn.jsdelivr.net/npm/diff2html/bundles/css/diff2html.min.css"" /&gt;
&lt;script src=""https://cdn.jsdelivr.net/npm/diff2html/bundles/js/diff2html.min.js""&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script id='diff.txt' type=""text/base64""&gt;
[$diff.txt$]
&lt;/script&gt;
&lt;div id=report&gt;
&lt;/div&gt;
&lt;script&gt;
var diffText = new TextDecoder().decode(
new Uint8Array(
atob(
document.getElementById('diff.txt').innerHTML.replace(/[
\r \t]/g, '')
).split('').map(c =&gt; c.charCodeAt(0))
)
);
var diffHtml = Diff2Html.html(diffText, {
drawFileList: true,
matching: 'lines',
outputFormat: 'side-by-side',
});
document.getElementById('report').innerHTML = diffHtml;
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;";
public static string GenGitDiff(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
using (var repo = new Repository(CreateTempRepo()))
{
var lines = new[] { "Line 1", "Line 2", "Line 3" };
var blob1 = repo.ObjectDatabase.CreateBlob(
// 由字串建立 Blob 物件 (也可改成讀檔案傳入 FileStream)
new MemoryStream(Encoding.UTF8.GetBytes(oldText)));
var blob2 = repo.ObjectDatabase.CreateBlob(
new MemoryStream(Encoding.UTF8.GetBytes(newText)));
// 比較兩個 Blob 物件差異
var patch = $@"
diff --git a/{oldFile} b/{newFile}
index aaaaaaa..bbbbbbb 100644
--- a/{oldFile}
+++ b/{newFile}
{repo.Diff.Compare(blob1, blob2).Patch}";
return patch;
}
}
public static string GenGitDiffHtmlReport(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
var diff = GenGitDiff(oldText, newText, oldFile, newFile);
var base64 = Convert.ToBase64String(Encoding.UTF8.GetBytes(diff));
var pem = new StringBuilder();
const int maxWidth = 160;
for (int i = 0; i &lt; base64.Length; i += maxWidth)
{
pem.AppendLine(base64.Substring(i, Math.Min(maxWidth, base64.Length - i)));
}
return html.Replace("[$diff.txt$]", pem.ToString());
}
}
</code></pre> <p>就醬，又解掉一件任務。</p> <p data-slug="libgit2sharp" data-fbimg="https://i.imgur.com/0UbIRZt.png#960#540"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>遇到一個小需求，想用 .NET 出一份檔案修改前後對照表，我心中最理想的方案是用 <a href="/blog/diff2html-webpage/">git diff + diff2html</a> 產生 HTML 報表，省時省力又好看。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638452451361125707.png"></p> <p>git diff 指令跟產生 diff2html 網頁技能是現成的，最不花腦的解決方法是用 .NET 將修改前後檔案雙雙寫成檔案，呼叫外部程式 git.exe 跑 <code>git diff a.txt b.txt</code> 取得結果，再交給 diff2html 顯示對照表。有沒有更輕巧簡潔的解法？</p> <p><a href="https://libgit2.org/">libgit2</a> 是一個用 C 開發的開源 Git 可攜程式庫，讓你不用安裝 Git 直接引用 Git 的各項功能，<a href="https://github.com/libgit2/libgit2sharp">libgit2sharp</a> 則為 libgit2 提供了 .NET 相容介面，會比呼叫 git.exe 更優雅。</p> <p>在專案執行 <code>dotnet add package libgit2sharp</code> 或從 NuGet 安裝後，就可以在 .NET 程式裡直接進行各項 Git 操作。</p> <p>以下是個簡單範例，示範 用 C# 建立一個 Git 儲存庫，加入 test.txt、Commit、修改 test.txt、再次 Commit、列出 Commit 記錄、顯示 Commit 的異動內容：</p> <pre><code>using LibGit2Sharp;
Func&lt;string&gt; CreateTempRepo = () =&gt;
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
};
var userName = "Tester";
var email = "tester@mail.net";
// Signature 物件，代表 Commit 的作者或提交者
Func&lt;Signature&gt; getSignature = () =&gt; new Signature(userName, email, DateTimeOffset.Now);
var repoPath = CreateTempRepo();
using (var repo = new Repository(repoPath))
{
var fileName = "test.txt";
var path = Path.Combine(repoPath, fileName);
File.WriteAllText(path, "Hello, World!
First Commit");
Commands.Stage(repo, fileName);
repo.Commit("Initial commit",
// Author       Committer
getSignature(), getSignature());
File.AppendAllText(path, "
Second Commit");
Commands.Stage(repo, fileName);
Thread.Sleep(1000);
repo.Commit("Second commit", getSignature(), getSignature());
print("**** Commit 紀錄 ****", ConsoleColor.Magenta);
foreach (var commit in repo.Commits)
{
print("Commit: " + commit.Id, ConsoleColor.Cyan);
print("  " + commit.Message, noNewLine: true);
print($"  by {commit.Author.Name} at {commit.Author.When:HH:mm:ss}", ConsoleColor.Green);
}
var commit1 = repo.Commits.Last();
var commit2 = repo.Commits.First();
var changes = repo.Diff.Compare&lt;Patch&gt;(commit1.Tree, commit2.Tree);
print("**** 異動比對 ****", ConsoleColor.Magenta);
foreach (var change in changes)
{
print(change.Patch, ConsoleColor.DarkYellow);
}
}
void print(string text, ConsoleColor color = ConsoleColor.White, bool noNewLine = false)
{
Console.ForegroundColor = color;
if (noNewLine)
Console.Write(text);
else
Console.WriteLine(text);
Console.ResetColor();
}
</code></pre> <p>輕輕鬆鬆搞定。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638452451363215435.png"></p> <p>最後，來看一下一開始的 XML 修改前後對照是怎麼做出來的？</p> <pre><code>public static void Run()
{
var xml= XDocument.Parse(@"
&lt;list&gt;
&lt;item id=""N1""&gt;Item 1&lt;/item&gt;
&lt;item id=""N2""&gt;Item 2&lt;/item&gt;
&lt;item id=""N3""&gt;Item 3&lt;/item&gt;
&lt;item id=""N4""&gt;Item 4&lt;/item&gt;
&lt;/list&gt;
");
var origin = xml.ToString();
xml.Root.Elements("item").Skip(1).First().SetAttributeValue("id", "N2-new");
xml.Root.Elements("item").Skip(2).First().Value = "Item 3 (modified)";
xml.Root.Add(new XElement("item", new XAttribute("id", "N5"), "Item 5"));
var modified = xml.ToString();
var html = GitDiffTool.GenGitDiffHtmlReport(origin, modified, "origin.xml", "modified.xml");
File.WriteAllText("D:\\Report.html", html);
// 用預設瀏覽器開啟
System.Diagnostics.Process.Start("cmd", @"/c ""start D:\Report.html""");
}
</code></pre> <p>關鍵的 GitDiffTool.GenGitDiffHtmlReport 如下，建立一個暫存目錄，直接將修改前後字串內容轉成 Blob 交由 Diff.Comapre() 比對。結果字串交給 diff2html 顯示，過程我用了 <a href="/blog/gzip-decompression-with-js/">HTML 單檔文件挑戰 - 內嵌 .js GZIP 壓縮檔</a>展示過的技巧，將差異結果用 Base64 內嵌在網頁裡。</p> <pre><code>using System.Text;
using LibGit2Sharp;
public class GitDiffTool
{
static string CreateTempRepo()
{
var path = Path.Combine(Path.GetTempPath(), Guid.NewGuid().ToString());
Directory.CreateDirectory(path);
Repository.Init(path, false);
return path;
}
const string html = @"
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;link rel=""stylesheet"" type=""text/css""
href=""https://cdn.jsdelivr.net/npm/diff2html/bundles/css/diff2html.min.css"" /&gt;
&lt;script src=""https://cdn.jsdelivr.net/npm/diff2html/bundles/js/diff2html.min.js""&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script id='diff.txt' type=""text/base64""&gt;
[$diff.txt$]
&lt;/script&gt;
&lt;div id=report&gt;
&lt;/div&gt;
&lt;script&gt;
var diffText = new TextDecoder().decode(
new Uint8Array(
atob(
document.getElementById('diff.txt').innerHTML.replace(/[
\r \t]/g, '')
).split('').map(c =&gt; c.charCodeAt(0))
)
);
var diffHtml = Diff2Html.html(diffText, {
drawFileList: true,
matching: 'lines',
outputFormat: 'side-by-side',
});
document.getElementById('report').innerHTML = diffHtml;
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;";
public static string GenGitDiff(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
using (var repo = new Repository(CreateTempRepo()))
{
var lines = new[] { "Line 1", "Line 2", "Line 3" };
var blob1 = repo.ObjectDatabase.CreateBlob(
// 由字串建立 Blob 物件 (也可改成讀檔案傳入 FileStream)
new MemoryStream(Encoding.UTF8.GetBytes(oldText)));
var blob2 = repo.ObjectDatabase.CreateBlob(
new MemoryStream(Encoding.UTF8.GetBytes(newText)));
// 比較兩個 Blob 物件差異
var patch = $@"
diff --git a/{oldFile} b/{newFile}
index aaaaaaa..bbbbbbb 100644
--- a/{oldFile}
+++ b/{newFile}
{repo.Diff.Compare(blob1, blob2).Patch}";
return patch;
}
}
public static string GenGitDiffHtmlReport(string oldText, string newText, string oldFile = "old", string newFile = "new")
{
var diff = GenGitDiff(oldText, newText, oldFile, newFile);
var base64 = Convert.ToBase64String(Encoding.UTF8.GetBytes(diff));
var pem = new StringBuilder();
const int maxWidth = 160;
for (int i = 0; i &lt; base64.Length; i += maxWidth)
{
pem.AppendLine(base64.Substring(i, Math.Min(maxWidth, base64.Length - i)));
}
return html.Replace("[$diff.txt$]", pem.ToString());
}
}
</code></pre> <p>就醬，又解掉一件任務。</p> <p data-slug="libgit2sharp" data-fbimg="https://i.imgur.com/0UbIRZt.png#960#540"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638452451361125707.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638452451361125707.png" medium="image"/>
    <pubDate>Tue, 05 Mar 2024 12:28:57 GMT</pubDate>
  </item><item>
    <title><![CDATA[Obsidian 匯出 PDF 之程式區塊顏色調整]]></title>
    <link>https://blog.darkthread.net/blog/change-obsidian-pdf-style/</link>
    <itunes:title><![CDATA[Obsidian 匯出 PDF 之程式區塊顏色調整]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>Obsidian 內建程式區塊 Syntax Highlight 顯示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638450510459234049.png"></p> <p>但匯出 PDF 時有些文字顏色偏淺(例如：WriteLine、GetPercentile、JSON 的屬性名稱)，對比不明顯，程式區塊的背景也淡了點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638450510461459440.png"></p> <p>研究了一下，PDF 輸出樣式可在 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 放個 .css 用 <code>@media pring { ... }</code> 指定列印專用樣性調整。但如果你試過，會發現更改 <code>pre,code { ... }</code> 樣式沒什麼效果。查了文件，發現要透過以下 <a href="https://docs.obsidian.md/Reference/CSS+variables/Editor/Code">CSS 變數</a>指定：</p> <table><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td>--code-background</td><td>Code background color</td></tr><tr><td>--code-white-space</td><td>Code white-space</td></tr><tr><td>--code-size</td><td>Code font size</td></tr><tr><td>--code-normal</td><td>Non-highlighted syntax</td></tr><tr><td>--code-comment</td><td>Comments</td></tr><tr><td>--code-function</td><td>Functions</td></tr><tr><td>--code-important</td><td>Important, regex</td></tr><tr><td>--code-keyword</td><td>Keywords</td></tr><tr><td>--code-operator</td><td>Operators</td></tr><tr><td>--code-property</td><td>Properties</td></tr><tr><td>--code-punctuation</td><td>Punctuation</td></tr><tr><td>--code-string</td><td>Strings</td></tr><tr><td>--code-tag</td><td>Tags, symbols, constants</td></tr><tr><td>--code-value</td><td>Values</td></tr></tbody></table> <p>做了一個 pdf.css 放到 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 目錄：</p> <pre><code>@media print {
body {
--code-background: #ccc;
--code-comment: #474;
--code-normal: #227;
--code-function: purple;
--code-property: drakblue;
--code-string: darkgreen;
}
}
</code></pre> <p>設定啟用：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638450510463773233.png"></p> <p>調整完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638450510465988298.png"></p> <p data-slug="change-obsidian-pdf-style" data-fbimb="https://images.pexels.com/photos/3862631/pexels-photo-3862631.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#2160#750"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>Obsidian 內建程式區塊 Syntax Highlight 顯示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638450510459234049.png"></p> <p>但匯出 PDF 時有些文字顏色偏淺(例如：WriteLine、GetPercentile、JSON 的屬性名稱)，對比不明顯，程式區塊的背景也淡了點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638450510461459440.png"></p> <p>研究了一下，PDF 輸出樣式可在 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 放個 .css 用 <code>@media pring { ... }</code> 指定列印專用樣性調整。但如果你試過，會發現更改 <code>pre,code { ... }</code> 樣式沒什麼效果。查了文件，發現要透過以下 <a href="https://docs.obsidian.md/Reference/CSS+variables/Editor/Code">CSS 變數</a>指定：</p> <table><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td>--code-background</td><td>Code background color</td></tr><tr><td>--code-white-space</td><td>Code white-space</td></tr><tr><td>--code-size</td><td>Code font size</td></tr><tr><td>--code-normal</td><td>Non-highlighted syntax</td></tr><tr><td>--code-comment</td><td>Comments</td></tr><tr><td>--code-function</td><td>Functions</td></tr><tr><td>--code-important</td><td>Important, regex</td></tr><tr><td>--code-keyword</td><td>Keywords</td></tr><tr><td>--code-operator</td><td>Operators</td></tr><tr><td>--code-property</td><td>Properties</td></tr><tr><td>--code-punctuation</td><td>Punctuation</td></tr><tr><td>--code-string</td><td>Strings</td></tr><tr><td>--code-tag</td><td>Tags, symbols, constants</td></tr><tr><td>--code-value</td><td>Values</td></tr></tbody></table> <p>做了一個 pdf.css 放到 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 目錄：</p> <pre><code>@media print {
body {
--code-background: #ccc;
--code-comment: #474;
--code-normal: #227;
--code-function: purple;
--code-property: drakblue;
--code-string: darkgreen;
}
}
</code></pre> <p>設定啟用：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638450510463773233.png"></p> <p>調整完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638450510465988298.png"></p> <p data-slug="change-obsidian-pdf-style" data-fbimb="https://images.pexels.com/photos/3862631/pexels-photo-3862631.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#2160#750"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>Obsidian 內建程式區塊 Syntax Highlight 顯示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638450510459234049.png"></p> <p>但匯出 PDF 時有些文字顏色偏淺(例如：WriteLine、GetPercentile、JSON 的屬性名稱)，對比不明顯，程式區塊的背景也淡了點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638450510461459440.png"></p> <p>研究了一下，PDF 輸出樣式可在 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 放個 .css 用 <code>@media pring { ... }</code> 指定列印專用樣性調整。但如果你試過，會發現更改 <code>pre,code { ... }</code> 樣式沒什麼效果。查了文件，發現要透過以下 <a href="https://docs.obsidian.md/Reference/CSS+variables/Editor/Code">CSS 變數</a>指定：</p> <table><thead><tr><th>Variable</th><th>Description</th></tr></thead><tbody><tr><td>--code-background</td><td>Code background color</td></tr><tr><td>--code-white-space</td><td>Code white-space</td></tr><tr><td>--code-size</td><td>Code font size</td></tr><tr><td>--code-normal</td><td>Non-highlighted syntax</td></tr><tr><td>--code-comment</td><td>Comments</td></tr><tr><td>--code-function</td><td>Functions</td></tr><tr><td>--code-important</td><td>Important, regex</td></tr><tr><td>--code-keyword</td><td>Keywords</td></tr><tr><td>--code-operator</td><td>Operators</td></tr><tr><td>--code-property</td><td>Properties</td></tr><tr><td>--code-punctuation</td><td>Punctuation</td></tr><tr><td>--code-string</td><td>Strings</td></tr><tr><td>--code-tag</td><td>Tags, symbols, constants</td></tr><tr><td>--code-value</td><td>Values</td></tr></tbody></table> <p>做了一個 pdf.css 放到 <code>&lt;vault-folder&gt;\.obsidian\snippets</code> 目錄：</p> <pre><code>@media print {
body {
--code-background: #ccc;
--code-comment: #474;
--code-normal: #227;
--code-function: purple;
--code-property: drakblue;
--code-string: darkgreen;
}
}
</code></pre> <p>設定啟用：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638450510463773233.png"></p> <p>調整完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638450510465988298.png"></p> <p data-slug="change-obsidian-pdf-style" data-fbimb="https://images.pexels.com/photos/3862631/pexels-photo-3862631.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#2160#750"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638450510459234049.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638450510459234049.png" medium="image"/>
    <pubDate>Sun, 03 Mar 2024 08:23:19 GMT</pubDate>
  </item><item>
    <title><![CDATA[GUID 叢集索引測試 3 - 觀察索引重組與索引重建對寫入動作的影響]]></title>
    <link>https://blog.darkthread.net/blog/index-reorg-n-index-rebuild/</link>
    <itunes:title><![CDATA[GUID 叢集索引測試 3 - 觀察索引重組與索引重建對寫入動作的影響]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>接連兩篇談完 GUID 叢集索引會造成 <a href="/blog/how-guid-clust-index-bad-is/">INSERT 變慢</a>以及<a href="/blog/guid-clust-idx-frag-n-space/">資料表虛胖</a>，簡單有效解決是另建 BIGINT 或 INT 自動跳號欄位當叢集索引，GUID 仍可做為 Primary Key。(參考：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>對上線運轉多年的現有系統來說，Schema 很難說改就改，異動資料表欄位如同飛航過程修改飛機骨架，光用想的就頭皮發麻，尋求可以減緩 GUID 叢集索引負面影響的對策才是較務實的做法。套句廣告詞：先求不傷身體，再講求效果。</p> <p>要改善 SQL Server 索引碎片化問題，主要可透過索引重建(Rebuild)或索引重組(Reorganize)，而索引重建再分為離線作業及線上作業(加上 ONLINE = ON 參數) ，故共有三種選項：</p> <ul><li>ALTER INDEX ... ON ... REBUILD<br> 離線執行重建，所需時間最短，但過程需卸除和重新建立索引，期間資料庫無法使用</li> <li>ALTER INDEX ... ON ... REBUILD (ONLINE = ON)<br> 線上索引重建，期間資料表可繼續使用，但修改資料時需更新索引的額外副本，影響效能 <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">參考</a></li> <li>ALTER INDEX ... ON ... REORGANIZE<br> 重新組織索引耗費的資源最少，是官方最建議的做法，期間資料表可繼續查詢或更新，但耗費時間較久</li></ul> <p>延伸閱讀：</p> <ul><li><a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">將索引維護最佳化以改善查詢效能並降低資源耗用量</a></li> <li><a href="https://caryhsu.blogspot.com/2011/08/sql-server.html">SQL Server 上大型資料表的索引效能調整與維護 by CaryHsu</a></li></ul> <p>離線重建索引是最快的，但營運中的系統也不太可能為了重建索引停用資料表，我試在 INSERT 過程執行 <code>ALTER INDEX ... ON ... REBUILD</code>，不意外地當下那筆作被卡住 15 秒直到重建完成，</p> <pre><code>1000001: 0ms
1000002: 0ms
1000003: 0ms
1000004: 0ms
1000005: 15596ms
1000006: 0ms
1000007: 0ms
1000008: 0ms
</code></pre> <p>不管是線上索引重建或索引重組，執行同時一定會影響效能，所以我修改了先前的測試程式，計劃完成三組測試：</p> <ol><li>每 10 萬筆觸發索引重組(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li> <li>每 10 萬筆暫停寫入，執行索引重組(想像成離峰時間執行，不影響日間作業)再繼續，在 GUID 叢集索引資料表 INSERT 1000 萬筆 3. 每 10 萬筆觸發線上索引重建(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li></ol> <p>程式如下，每 10 萬筆觸發另起 Task 同步跑 <code>ALTER INDEX ... ON ... REORGANIZE</code>，用 bool Running 偵測前次若在執行中就略過。</p> <pre><code>using System.Diagnostics;
using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum
{
GuidClustIdx,
IntClustIdx
}
static int CurrRowIdx = 0;
static int NextReorgIdx = 0;
public static void InsertData(TableNameEnum tableName, long rowCount, int reorgCount)
{
NextReorgIdx = reorgCount;
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
CurrRowIdx = i;
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
if (i &gt; NextReorgIdx)
{
Task.Run(() =&gt; ReoraganizeIndex(tableName));
NextReorgIdx += reorgCount;
}
}
logger.Flush();
logger.Dispose();
}
static bool Running = false;
const string logPath = "results\\ReorgIndex.log";
public static void ReoraganizeIndex(TableNameEnum tableName)
{
if (Running) return;
try
{
Running = true;
Console.WriteLine("Reorganizing Index");
int startRowIdx = CurrRowIdx;
var sw = new Stopwatch();
sw.Start();
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"ALTER INDEX PK_{tableName} ON {tableName} REORGANIZE", cn);
cmd.CommandTimeout = 0; // No Timeout
cmd.ExecuteNonQuery();
int endRowIdx = CurrRowIdx;
sw.Stop();
Console.WriteLine($"Reorganize Index Elapsed: {sw.ElapsedMilliseconds}ms");
File.AppendAllText(logPath, $"{tableName} from {startRowIdx} to {endRowIdx} rows, Elapsed: {sw.ElapsedMilliseconds}ms{Environment.NewLine}");
Running = false;
}
catch (Exception ex)
{
Console.WriteLine(ex.Message);
File.AppendAllText(logPath, ex.Message + Environment.NewLine);
}
}
}
</code></pre> <p>先看沒有索引重組或線上索引重建的數據：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448994890435871.png"></p> <p>邊索引重組邊 INSERT 的數據如下，最大值不時會拉長到 1 秒以上，但 99% (綠色) 仍壓在 0.5 秒以下，不算太嚴重。1000 萬筆寫入時間變長兩倍多，由兩小時出頭延長到六小時。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448994894874303.png"></p> <p>將 <code>Task.Run(() =&gt; ReoraganizeIndex(tableName));</code> 改為 <code>ReoraganizeIndex(tableName);</code>，模擬每天離峰索引重組，看看結果比不重組改善多少。有點意外，我沒看出明顯改善：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448994897096397.png"></p> <p>第三組測試比照邊索引重組邊 INSERT，但改成一邊線上索引重建(ALTER INDEX ... ON ... REBUILD (ONLINE = ON) )一邊 INSERT。線上索引重建對寫入效的影響比較明顯，最大值不時突破 10 秒，大部分的寫入還是可以零秒完成，但平均值跟 99 百分位數都拉高了，而 1000 萬筆寫入時間拉長 10 倍跑了 10 小時才完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448994899293319.png"></p> <p>而另一個值得觀察的點是邊 INSERT 邊重組、專心專組、邊 INSERT 邊重建耗費的時間。</p> <p>邊 INSERT 邊索引重組與資料筆數大致成線性關係，由於需待前次做完進行下次，執行時機不固定，最後一次發生在 680 萬筆左右，耗時超過一小時 (4472 秒, 1h14m)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638448994901446551.png"></p> <p>停止 INSERT 專心索引重組耗時與資料筆數也是呈線性關係，但時間大幅縮短一半，1000 萬筆耗時 1352 秒，只要 22 分鐘。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638448994903589827.png"></p> <p>若不考慮執期間影響寫入作業的效能，線上索引重建比線上重組有效率多了，1000 萬也只要 502 秒，八分多鐘，快了 N 倍。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638448994905773432.png"></p> <p>做個結論：</p> <ol><li>索引重組或索引重建對 GUID 叢集索引 INSERT 作業的效能改善不明顯(但原本也不嚴重就是了)，但減少資料分頁數量、消除碎片化及節省磁碟空間有益無害。</li> <li>如果系統有明確的離峰時段(當下極少人使用，且能容忍延遲)，線上索引重建是最有效率的做法；由測試結果，索引重組對 INSERT 效能的影響比預期小，邊重組邊寫入造成的效能拖累不明顯。</li></ol> <p>不過，如果可以，避免用 GUID 當叢集索引(當 PK 沒問題，但好像不少把兩件事綁在一起)是更好的解法。</p> <p data-slug="index-reorg-n-index-rebuild" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>接連兩篇談完 GUID 叢集索引會造成 <a href="/blog/how-guid-clust-index-bad-is/">INSERT 變慢</a>以及<a href="/blog/guid-clust-idx-frag-n-space/">資料表虛胖</a>，簡單有效解決是另建 BIGINT 或 INT 自動跳號欄位當叢集索引，GUID 仍可做為 Primary Key。(參考：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>對上線運轉多年的現有系統來說，Schema 很難說改就改，異動資料表欄位如同飛航過程修改飛機骨架，光用想的就頭皮發麻，尋求可以減緩 GUID 叢集索引負面影響的對策才是較務實的做法。套句廣告詞：先求不傷身體，再講求效果。</p> <p>要改善 SQL Server 索引碎片化問題，主要可透過索引重建(Rebuild)或索引重組(Reorganize)，而索引重建再分為離線作業及線上作業(加上 ONLINE = ON 參數) ，故共有三種選項：</p> <ul><li>ALTER INDEX ... ON ... REBUILD<br> 離線執行重建，所需時間最短，但過程需卸除和重新建立索引，期間資料庫無法使用</li> <li>ALTER INDEX ... ON ... REBUILD (ONLINE = ON)<br> 線上索引重建，期間資料表可繼續使用，但修改資料時需更新索引的額外副本，影響效能 <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">參考</a></li> <li>ALTER INDEX ... ON ... REORGANIZE<br> 重新組織索引耗費的資源最少，是官方最建議的做法，期間資料表可繼續查詢或更新，但耗費時間較久</li></ul> <p>延伸閱讀：</p> <ul><li><a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">將索引維護最佳化以改善查詢效能並降低資源耗用量</a></li> <li><a href="https://caryhsu.blogspot.com/2011/08/sql-server.html">SQL Server 上大型資料表的索引效能調整與維護 by CaryHsu</a></li></ul> <p>離線重建索引是最快的，但營運中的系統也不太可能為了重建索引停用資料表，我試在 INSERT 過程執行 <code>ALTER INDEX ... ON ... REBUILD</code>，不意外地當下那筆作被卡住 15 秒直到重建完成，</p> <pre><code>1000001: 0ms
1000002: 0ms
1000003: 0ms
1000004: 0ms
1000005: 15596ms
1000006: 0ms
1000007: 0ms
1000008: 0ms
</code></pre> <p>不管是線上索引重建或索引重組，執行同時一定會影響效能，所以我修改了先前的測試程式，計劃完成三組測試：</p> <ol><li>每 10 萬筆觸發索引重組(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li> <li>每 10 萬筆暫停寫入，執行索引重組(想像成離峰時間執行，不影響日間作業)再繼續，在 GUID 叢集索引資料表 INSERT 1000 萬筆 3. 每 10 萬筆觸發線上索引重建(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li></ol> <p>程式如下，每 10 萬筆觸發另起 Task 同步跑 <code>ALTER INDEX ... ON ... REORGANIZE</code>，用 bool Running 偵測前次若在執行中就略過。</p> <pre><code>using System.Diagnostics;
using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum
{
GuidClustIdx,
IntClustIdx
}
static int CurrRowIdx = 0;
static int NextReorgIdx = 0;
public static void InsertData(TableNameEnum tableName, long rowCount, int reorgCount)
{
NextReorgIdx = reorgCount;
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
CurrRowIdx = i;
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
if (i &gt; NextReorgIdx)
{
Task.Run(() =&gt; ReoraganizeIndex(tableName));
NextReorgIdx += reorgCount;
}
}
logger.Flush();
logger.Dispose();
}
static bool Running = false;
const string logPath = "results\\ReorgIndex.log";
public static void ReoraganizeIndex(TableNameEnum tableName)
{
if (Running) return;
try
{
Running = true;
Console.WriteLine("Reorganizing Index");
int startRowIdx = CurrRowIdx;
var sw = new Stopwatch();
sw.Start();
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"ALTER INDEX PK_{tableName} ON {tableName} REORGANIZE", cn);
cmd.CommandTimeout = 0; // No Timeout
cmd.ExecuteNonQuery();
int endRowIdx = CurrRowIdx;
sw.Stop();
Console.WriteLine($"Reorganize Index Elapsed: {sw.ElapsedMilliseconds}ms");
File.AppendAllText(logPath, $"{tableName} from {startRowIdx} to {endRowIdx} rows, Elapsed: {sw.ElapsedMilliseconds}ms{Environment.NewLine}");
Running = false;
}
catch (Exception ex)
{
Console.WriteLine(ex.Message);
File.AppendAllText(logPath, ex.Message + Environment.NewLine);
}
}
}
</code></pre> <p>先看沒有索引重組或線上索引重建的數據：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448994890435871.png"></p> <p>邊索引重組邊 INSERT 的數據如下，最大值不時會拉長到 1 秒以上，但 99% (綠色) 仍壓在 0.5 秒以下，不算太嚴重。1000 萬筆寫入時間變長兩倍多，由兩小時出頭延長到六小時。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448994894874303.png"></p> <p>將 <code>Task.Run(() =&gt; ReoraganizeIndex(tableName));</code> 改為 <code>ReoraganizeIndex(tableName);</code>，模擬每天離峰索引重組，看看結果比不重組改善多少。有點意外，我沒看出明顯改善：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448994897096397.png"></p> <p>第三組測試比照邊索引重組邊 INSERT，但改成一邊線上索引重建(ALTER INDEX ... ON ... REBUILD (ONLINE = ON) )一邊 INSERT。線上索引重建對寫入效的影響比較明顯，最大值不時突破 10 秒，大部分的寫入還是可以零秒完成，但平均值跟 99 百分位數都拉高了，而 1000 萬筆寫入時間拉長 10 倍跑了 10 小時才完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448994899293319.png"></p> <p>而另一個值得觀察的點是邊 INSERT 邊重組、專心專組、邊 INSERT 邊重建耗費的時間。</p> <p>邊 INSERT 邊索引重組與資料筆數大致成線性關係，由於需待前次做完進行下次，執行時機不固定，最後一次發生在 680 萬筆左右，耗時超過一小時 (4472 秒, 1h14m)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638448994901446551.png"></p> <p>停止 INSERT 專心索引重組耗時與資料筆數也是呈線性關係，但時間大幅縮短一半，1000 萬筆耗時 1352 秒，只要 22 分鐘。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638448994903589827.png"></p> <p>若不考慮執期間影響寫入作業的效能，線上索引重建比線上重組有效率多了，1000 萬也只要 502 秒，八分多鐘，快了 N 倍。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638448994905773432.png"></p> <p>做個結論：</p> <ol><li>索引重組或索引重建對 GUID 叢集索引 INSERT 作業的效能改善不明顯(但原本也不嚴重就是了)，但減少資料分頁數量、消除碎片化及節省磁碟空間有益無害。</li> <li>如果系統有明確的離峰時段(當下極少人使用，且能容忍延遲)，線上索引重建是最有效率的做法；由測試結果，索引重組對 INSERT 效能的影響比預期小，邊重組邊寫入造成的效能拖累不明顯。</li></ol> <p>不過，如果可以，避免用 GUID 當叢集索引(當 PK 沒問題，但好像不少把兩件事綁在一起)是更好的解法。</p> <p data-slug="index-reorg-n-index-rebuild" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>接連兩篇談完 GUID 叢集索引會造成 <a href="/blog/how-guid-clust-index-bad-is/">INSERT 變慢</a>以及<a href="/blog/guid-clust-idx-frag-n-space/">資料表虛胖</a>，簡單有效解決是另建 BIGINT 或 INT 自動跳號欄位當叢集索引，GUID 仍可做為 Primary Key。(參考：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>對上線運轉多年的現有系統來說，Schema 很難說改就改，異動資料表欄位如同飛航過程修改飛機骨架，光用想的就頭皮發麻，尋求可以減緩 GUID 叢集索引負面影響的對策才是較務實的做法。套句廣告詞：先求不傷身體，再講求效果。</p> <p>要改善 SQL Server 索引碎片化問題，主要可透過索引重建(Rebuild)或索引重組(Reorganize)，而索引重建再分為離線作業及線上作業(加上 ONLINE = ON 參數) ，故共有三種選項：</p> <ul><li>ALTER INDEX ... ON ... REBUILD<br> 離線執行重建，所需時間最短，但過程需卸除和重新建立索引，期間資料庫無法使用</li> <li>ALTER INDEX ... ON ... REBUILD (ONLINE = ON)<br> 線上索引重建，期間資料表可繼續使用，但修改資料時需更新索引的額外副本，影響效能 <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">參考</a></li> <li>ALTER INDEX ... ON ... REORGANIZE<br> 重新組織索引耗費的資源最少，是官方最建議的做法，期間資料表可繼續查詢或更新，但耗費時間較久</li></ul> <p>延伸閱讀：</p> <ul><li><a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/indexes/reorganize-and-rebuild-indexes?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#rebuild-an-index">將索引維護最佳化以改善查詢效能並降低資源耗用量</a></li> <li><a href="https://caryhsu.blogspot.com/2011/08/sql-server.html">SQL Server 上大型資料表的索引效能調整與維護 by CaryHsu</a></li></ul> <p>離線重建索引是最快的，但營運中的系統也不太可能為了重建索引停用資料表，我試在 INSERT 過程執行 <code>ALTER INDEX ... ON ... REBUILD</code>，不意外地當下那筆作被卡住 15 秒直到重建完成，</p> <pre><code>1000001: 0ms
1000002: 0ms
1000003: 0ms
1000004: 0ms
1000005: 15596ms
1000006: 0ms
1000007: 0ms
1000008: 0ms
</code></pre> <p>不管是線上索引重建或索引重組，執行同時一定會影響效能，所以我修改了先前的測試程式，計劃完成三組測試：</p> <ol><li>每 10 萬筆觸發索引重組(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li> <li>每 10 萬筆暫停寫入，執行索引重組(想像成離峰時間執行，不影響日間作業)再繼續，在 GUID 叢集索引資料表 INSERT 1000 萬筆 3. 每 10 萬筆觸發線上索引重建(若前次沒做完就跳過)，在 GUID 叢集索引資料表 INSERT 1000 萬筆</li></ol> <p>程式如下，每 10 萬筆觸發另起 Task 同步跑 <code>ALTER INDEX ... ON ... REORGANIZE</code>，用 bool Running 偵測前次若在執行中就略過。</p> <pre><code>using System.Diagnostics;
using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum
{
GuidClustIdx,
IntClustIdx
}
static int CurrRowIdx = 0;
static int NextReorgIdx = 0;
public static void InsertData(TableNameEnum tableName, long rowCount, int reorgCount)
{
NextReorgIdx = reorgCount;
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
CurrRowIdx = i;
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
if (i &gt; NextReorgIdx)
{
Task.Run(() =&gt; ReoraganizeIndex(tableName));
NextReorgIdx += reorgCount;
}
}
logger.Flush();
logger.Dispose();
}
static bool Running = false;
const string logPath = "results\\ReorgIndex.log";
public static void ReoraganizeIndex(TableNameEnum tableName)
{
if (Running) return;
try
{
Running = true;
Console.WriteLine("Reorganizing Index");
int startRowIdx = CurrRowIdx;
var sw = new Stopwatch();
sw.Start();
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"ALTER INDEX PK_{tableName} ON {tableName} REORGANIZE", cn);
cmd.CommandTimeout = 0; // No Timeout
cmd.ExecuteNonQuery();
int endRowIdx = CurrRowIdx;
sw.Stop();
Console.WriteLine($"Reorganize Index Elapsed: {sw.ElapsedMilliseconds}ms");
File.AppendAllText(logPath, $"{tableName} from {startRowIdx} to {endRowIdx} rows, Elapsed: {sw.ElapsedMilliseconds}ms{Environment.NewLine}");
Running = false;
}
catch (Exception ex)
{
Console.WriteLine(ex.Message);
File.AppendAllText(logPath, ex.Message + Environment.NewLine);
}
}
}
</code></pre> <p>先看沒有索引重組或線上索引重建的數據：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448994890435871.png"></p> <p>邊索引重組邊 INSERT 的數據如下，最大值不時會拉長到 1 秒以上，但 99% (綠色) 仍壓在 0.5 秒以下，不算太嚴重。1000 萬筆寫入時間變長兩倍多，由兩小時出頭延長到六小時。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448994894874303.png"></p> <p>將 <code>Task.Run(() =&gt; ReoraganizeIndex(tableName));</code> 改為 <code>ReoraganizeIndex(tableName);</code>，模擬每天離峰索引重組，看看結果比不重組改善多少。有點意外，我沒看出明顯改善：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448994897096397.png"></p> <p>第三組測試比照邊索引重組邊 INSERT，但改成一邊線上索引重建(ALTER INDEX ... ON ... REBUILD (ONLINE = ON) )一邊 INSERT。線上索引重建對寫入效的影響比較明顯，最大值不時突破 10 秒，大部分的寫入還是可以零秒完成，但平均值跟 99 百分位數都拉高了，而 1000 萬筆寫入時間拉長 10 倍跑了 10 小時才完成。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448994899293319.png"></p> <p>而另一個值得觀察的點是邊 INSERT 邊重組、專心專組、邊 INSERT 邊重建耗費的時間。</p> <p>邊 INSERT 邊索引重組與資料筆數大致成線性關係，由於需待前次做完進行下次，執行時機不固定，最後一次發生在 680 萬筆左右，耗時超過一小時 (4472 秒, 1h14m)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638448994901446551.png"></p> <p>停止 INSERT 專心索引重組耗時與資料筆數也是呈線性關係，但時間大幅縮短一半，1000 萬筆耗時 1352 秒，只要 22 分鐘。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638448994903589827.png"></p> <p>若不考慮執期間影響寫入作業的效能，線上索引重建比線上重組有效率多了，1000 萬也只要 502 秒，八分多鐘，快了 N 倍。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638448994905773432.png"></p> <p>做個結論：</p> <ol><li>索引重組或索引重建對 GUID 叢集索引 INSERT 作業的效能改善不明顯(但原本也不嚴重就是了)，但減少資料分頁數量、消除碎片化及節省磁碟空間有益無害。</li> <li>如果系統有明確的離峰時段(當下極少人使用，且能容忍延遲)，線上索引重建是最有效率的做法；由測試結果，索引重組對 INSERT 效能的影響比預期小，邊重組邊寫入造成的效能拖累不明顯。</li></ol> <p>不過，如果可以，避免用 GUID 當叢集索引(當 PK 沒問題，但好像不少把兩件事綁在一起)是更好的解法。</p> <p data-slug="index-reorg-n-index-rebuild" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638448994890435871.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638448994890435871.png" medium="image"/>
    <pubDate>Fri, 01 Mar 2024 14:12:01 GMT</pubDate>
  </item><item>
    <title><![CDATA[取得部署 IIS 之 ASP.NET Core HTTP 500 錯誤細節]]></title>
    <link>https://blog.darkthread.net/blog/view-aspnet-core-error-detail-in-iis/</link>
    <itunes:title><![CDATA[取得部署 IIS 之 ASP.NET Core HTTP 500 錯誤細節]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>ASP.NET Core 有個預設行為，開發測試階段會顯示錯誤細節，包含錯誤訊息、Stack Trace 等資訊；當部署到 IIS 後，就只會顯示告知狀態碼為 HTTP ERROR 500，以避免程式資訊外洩形成資安風險：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448174433779846.png"></p> <p>嚴譯的系統會設計 Log 機制補捉及記錄錯誤，提供介面查詢或是後送 ELK、Prometheus 之類監控系統，自有管道可查。但若為簡單的 ASP.NET Core 網站，沒有這些機制，要怎麼臨時開啟顯示詳細錯誤訊息？(提醒：顯示詳細錯誤有資安風險，此暫時性做法不宜用於正式環境) .NET Framework 時代的 ASP.NET 可<a href="https://learn.microsoft.com/zh-tw/aspnet/web-forms/overview/older-versions-getting-started/deploying-web-site-projects/displaying-a-custom-error-page-cs?WT.mc_id=DOP-MVP-37580#configuring-which-error-page-is-displayed">透過 <code>&lt;system.web&gt;&lt;customErrors mode="Off/RemoteOnly" /&gt;&lt;/system.web&gt;</code> 開啟</a>，ASP.NET Core 網站沒有 <code>&lt;system.web&gt;</code> 可設，要怎麼處堙？</p> <p>解法一：查事件檢視器</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448174436058381.png"></p> <p>解法二：</p> <p>啟用 ASP.NET Core 執行 Log 檔記錄，web.config 找到 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/host-and-deploy/iis/web-config?view=aspnetcore-8.0&amp;WT.mc_id=DOP-MVP-37580#attributes-of-the-aspnetcore-element">aspNetCore 元素</a>，將 stdoutLogEnabled 設為 true：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="true" stdoutLogFile=".\logs\stdout" hostingModel="inprocess" /&gt;
</code></pre> <p>但這個 Log 不會即時寫入磁碟，若要馬上看結果，可回收 AppPool：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448174438165033.png"></p> <p>解法三：</p> <p>在 aspNetCore 元素加入 environmentVariable 指定 ASPNETCORE_ENVIRONMENT = Development：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="false" stdoutLogFile=".\logs\stdout" hostingModel="inprocess"&gt;
&lt;environmentVariables&gt;
&lt;environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Development" /&gt;
&lt;/environmentVariables&gt;
&lt;/aspNetCore&gt;
</code></pre> <p>如此 ASP.NET Core 網站會以開發模式運行，比照 F5 偵錯或 dotnet run 時顯示錯誤細節：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448174440347930.png"></p> <p data-slug="view-aspnet-core-error-detail-in-iis" data-fbimg="https://cdn.pixabay.com/photo/2017/08/06/19/04/motor-2595269_1280.jpg#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>ASP.NET Core 有個預設行為，開發測試階段會顯示錯誤細節，包含錯誤訊息、Stack Trace 等資訊；當部署到 IIS 後，就只會顯示告知狀態碼為 HTTP ERROR 500，以避免程式資訊外洩形成資安風險：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448174433779846.png"></p> <p>嚴譯的系統會設計 Log 機制補捉及記錄錯誤，提供介面查詢或是後送 ELK、Prometheus 之類監控系統，自有管道可查。但若為簡單的 ASP.NET Core 網站，沒有這些機制，要怎麼臨時開啟顯示詳細錯誤訊息？(提醒：顯示詳細錯誤有資安風險，此暫時性做法不宜用於正式環境) .NET Framework 時代的 ASP.NET 可<a href="https://learn.microsoft.com/zh-tw/aspnet/web-forms/overview/older-versions-getting-started/deploying-web-site-projects/displaying-a-custom-error-page-cs?WT.mc_id=DOP-MVP-37580#configuring-which-error-page-is-displayed">透過 <code>&lt;system.web&gt;&lt;customErrors mode="Off/RemoteOnly" /&gt;&lt;/system.web&gt;</code> 開啟</a>，ASP.NET Core 網站沒有 <code>&lt;system.web&gt;</code> 可設，要怎麼處堙？</p> <p>解法一：查事件檢視器</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448174436058381.png"></p> <p>解法二：</p> <p>啟用 ASP.NET Core 執行 Log 檔記錄，web.config 找到 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/host-and-deploy/iis/web-config?view=aspnetcore-8.0&amp;WT.mc_id=DOP-MVP-37580#attributes-of-the-aspnetcore-element">aspNetCore 元素</a>，將 stdoutLogEnabled 設為 true：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="true" stdoutLogFile=".\logs\stdout" hostingModel="inprocess" /&gt;
</code></pre> <p>但這個 Log 不會即時寫入磁碟，若要馬上看結果，可回收 AppPool：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448174438165033.png"></p> <p>解法三：</p> <p>在 aspNetCore 元素加入 environmentVariable 指定 ASPNETCORE_ENVIRONMENT = Development：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="false" stdoutLogFile=".\logs\stdout" hostingModel="inprocess"&gt;
&lt;environmentVariables&gt;
&lt;environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Development" /&gt;
&lt;/environmentVariables&gt;
&lt;/aspNetCore&gt;
</code></pre> <p>如此 ASP.NET Core 網站會以開發模式運行，比照 F5 偵錯或 dotnet run 時顯示錯誤細節：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448174440347930.png"></p> <p data-slug="view-aspnet-core-error-detail-in-iis" data-fbimg="https://cdn.pixabay.com/photo/2017/08/06/19/04/motor-2595269_1280.jpg#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>ASP.NET Core 有個預設行為，開發測試階段會顯示錯誤細節，包含錯誤訊息、Stack Trace 等資訊；當部署到 IIS 後，就只會顯示告知狀態碼為 HTTP ERROR 500，以避免程式資訊外洩形成資安風險：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638448174433779846.png"></p> <p>嚴譯的系統會設計 Log 機制補捉及記錄錯誤，提供介面查詢或是後送 ELK、Prometheus 之類監控系統，自有管道可查。但若為簡單的 ASP.NET Core 網站，沒有這些機制，要怎麼臨時開啟顯示詳細錯誤訊息？(提醒：顯示詳細錯誤有資安風險，此暫時性做法不宜用於正式環境) .NET Framework 時代的 ASP.NET 可<a href="https://learn.microsoft.com/zh-tw/aspnet/web-forms/overview/older-versions-getting-started/deploying-web-site-projects/displaying-a-custom-error-page-cs?WT.mc_id=DOP-MVP-37580#configuring-which-error-page-is-displayed">透過 <code>&lt;system.web&gt;&lt;customErrors mode="Off/RemoteOnly" /&gt;&lt;/system.web&gt;</code> 開啟</a>，ASP.NET Core 網站沒有 <code>&lt;system.web&gt;</code> 可設，要怎麼處堙？</p> <p>解法一：查事件檢視器</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638448174436058381.png"></p> <p>解法二：</p> <p>啟用 ASP.NET Core 執行 Log 檔記錄，web.config 找到 <a href="https://learn.microsoft.com/zh-tw/aspnet/core/host-and-deploy/iis/web-config?view=aspnetcore-8.0&amp;WT.mc_id=DOP-MVP-37580#attributes-of-the-aspnetcore-element">aspNetCore 元素</a>，將 stdoutLogEnabled 設為 true：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="true" stdoutLogFile=".\logs\stdout" hostingModel="inprocess" /&gt;
</code></pre> <p>但這個 Log 不會即時寫入磁碟，若要馬上看結果，可回收 AppPool：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638448174438165033.png"></p> <p>解法三：</p> <p>在 aspNetCore 元素加入 environmentVariable 指定 ASPNETCORE_ENVIRONMENT = Development：</p> <pre><code>&lt;aspNetCore processPath="dotnet" arguments=".\iis-hosting.dll"
stdoutLogEnabled="false" stdoutLogFile=".\logs\stdout" hostingModel="inprocess"&gt;
&lt;environmentVariables&gt;
&lt;environmentVariable name="ASPNETCORE_ENVIRONMENT" value="Development" /&gt;
&lt;/environmentVariables&gt;
&lt;/aspNetCore&gt;
</code></pre> <p>如此 ASP.NET Core 網站會以開發模式運行，比照 F5 偵錯或 dotnet run 時顯示錯誤細節：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638448174440347930.png"></p> <p data-slug="view-aspnet-core-error-detail-in-iis" data-fbimg="https://cdn.pixabay.com/photo/2017/08/06/19/04/motor-2595269_1280.jpg#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638448174433779846.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638448174433779846.png" medium="image"/>
    <pubDate>Thu, 29 Feb 2024 08:55:04 GMT</pubDate>
  </item><item>
    <title><![CDATA[GUID 叢集索引測試 2：索引碎片化分析與資料表虛胖問題]]></title>
    <link>https://blog.darkthread.net/blog/guid-clust-idx-frag-n-space/</link>
    <itunes:title><![CDATA[GUID 叢集索引測試 2：索引碎片化分析與資料表虛胖問題]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>上篇我們觀察到用 GUID 做為叢集索引，<a href="/blog/how-guid-clust-index-bad-is/">INSERT 耗時較久且偶發慢到 4 ~ 6 秒的狀況</a>。</p> <p>依直覺索引碎片化的影響主要在於讀寫效能不佳(要走過更多資料分頁)，但觀察容量計時我發現另一件有趣的事，同樣是一千萬筆資料，IntClustIdx 比 GuidClustIdx 多一個 BIGINT 欄位，每筆資料多 8 Bytes (2035 vs 2027)，但 IntClustIdx 用的空間比較少，只有 27 GB，少一個欄位的 GuidClustIdx 反而用掉 40GB，這是怎麼一回事？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638447277359190777.png"></p> <p>這也與叢集索引碎片化有關。</p> <p>由於 GUID 無連續性，INSERT 資料時會導致 Page Split，拆分為兩個分頁並<a href="https://dba.stackexchange.com/a/155258/197986">將資料拆成 50/50，各使用一半空間</a> (Internal Fragmentation)。</p> <p>要觀察索引碎片化狀態，<code>dm_db_index_physical_stats</code> 是最效的指標。(相關背景知識可參見保哥文章：<a href="https://blog.miniasp.com/post/2009/01/18/Let-SQL-Server-Tell-You-Which-Indexes-to-Rebuild-or-Reorganize">讓 SQL Server 告訴你有哪些索引應該被重建或重組</a>)</p> <p>使用 <code>dm_db_index_physical_stats</code> 查詢索引碎裂狀態，我們可以得到兩項重要指標：<code>avg_fragmentation_in_percent</code>、<code>avg_page_space_used_in_percent</code>。</p> <p><code>avg_fragmentation_in_percent</code> 可視為碎片化的程度。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/system-dynamic-management-views/sys-dm-db-index-physical-stats-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#logical-fragmentation">Logical fragmentation</a> 官方翻譯為邏輯片段，我覺得譯成「邏輯碎片」更體切，指的是索引資料分頁邏輯順序與實體順序不一致，邏輯上依索引順序應該連續的資料分頁，被放在不連續的實體位置，如此存取將需要更多 IO 動作，拖慢效能。此值介於 10 到 15 之間時建議做索引重組，大於 15 時應考慮進行索引重建。</p> <p><code>avg_page_space_used_in_percent</code> 則是資料分頁存放資料的比率，比例過低將造成空間浪費(本案例的狀況)，代表要走訪更多分頁才能蒐集完所需要資料，勢必影響效能。這個值介於 60 到 75 之間時建議做索引重組，低於 60 時應考慮索引重建。</p> <p>以下 SQL 指令修改自保哥文章，增加顯示筆數與每筆資料長度：</p> <pre><code>SELECT OBJECT_NAME(dt.object_id),
si.name,
dt.index_level,
dt.avg_fragmentation_in_percent,
dt.avg_page_space_used_in_percent,
dt.record_count,
dt.avg_record_size_in_bytes
FROM
(SELECT object_id,
index_id,
index_level,
avg_fragmentation_in_percent,
avg_page_space_used_in_percent,
record_count,
avg_record_size_in_bytes
FROM    sys.dm_db_index_physical_stats (DB_ID(), NULL, NULL, NULL, 'DETAILED')
WHERE   index_id &lt;&gt; 0
) AS dt --does not return information about heaps
INNER JOIN sys.indexes si
ON     si.object_id = dt.object_id
AND si.index_id  = dt.index_id
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638447277361338412.png"></p> <p>利用這個機會，來研究其中數字的奧祕。非 Leaf Page (index_level &gt; 0) 的每筆索引長度(<code>avg_record_size_in_bytes</code>) 為什麼是 23 [1] 及 15 [2]？</p> <p>由<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-nonclustered-index?view=sql-server-ver15&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-variables-for-use-in-steps-2-and-3">官方文件</a>：<code>Index_Row_Size = Fixed_Key_Size + Variable_Key_Size + Index_Null_Bitmap + 1 (for row header overhead of an index row) + 6 (for the child page ID pointer)</code>，當索引沒有 Nullable 欄位，<code>Index_Null_Bitmap</code> = 0，因此 GuidClustIdx 為 16 (Uniqueidentity 長度) + 1 + 6 = 23，IntClustIdx 為 8 (Bigint 長度) + 1 + 6 = 15。</p> <p>至於叢集索引的 Leaf Page (index_level == 0) 會實際儲存資料內容，其結構為 TagA (1) + TagB (1) + Fsize (2) + Fdata (N, 固定欄位資料) + Ncol (2) + Nullbits (Ncol/8) + VarCount (2) + VarOffset (2*VarCount) + VarData(N) + VersioningTag (14 或 0)，LongText 欄位為長度 1000 的字串，以 Unicode 編碼儲存為 2000 Bytes。故 GuidClustIdx 資料列長度為 1 + 1 + 2 + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2027 [3]，IntClustIdx 長度為 1 + 1 + 2 + 8(SeqNo) + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2035 [4]。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-the-space-used-to-store-data-in-the-leaf-level">詳細計算方式</a></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638447277363568068.png"><br> <a href="https://aboutsqlserver.com/2013/10/15/sql-server-storage-engine-data-pages-and-data-rows/">圖片來源</a></p> <p>由此推估，使用 GUID 叢集索引的問題在於碎片化造成資料分散在兩倍以上數量的資料分頁(Data Page)，每個分頁只咬半口，浪費空間是一回事，需走訪更多資料分頁影響效能是更嚴重的問題。每筆 2027 Bytes * 1000 萬筆理應 20GB，因資料分頁利用率 50% [5]，故實際佔用容量加倍成為 40GB。而 IntClustIdx 資料表資料分頁利用率 75% [5]，20G / 0.75 = 27G。</p> <p>追根究根，避用 GUID 當叢集索引是解緩索引碎片化的治本之道，但異動 Schema 工程浩大，不是所有系統都能配合修改，此時只能寄望索引重組或索引重建改善。</p> <p>很少有機會做出這個規模的測試資料，順便試試 120GB 3000 萬筆資料索引重建要花多久？我測了 <code>ALTER INDEX PK_GuidClustIdx ON GuidClustIdx REBUILD WITH (ONLINE = ON)</code> (線上模式，索引作業期間可讓並行使用者存取基礎資料表、叢集索引資料，及任何關聯的非叢集索引。但耗時會比較久且過程效能也會下降)</p> <p>索引重建前，破碎比例 99%，空間使用率 50%，耗用空間 120GB：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638447277365712993.png"></p> <p>實測線上重建三千萬筆資料叢集索引共耗時 15'50"。重建後，使用空間下降到 80G，破碎比例 0%，空間使用率上升到一般標準 75%。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638447277367970610.png"></p> <p>最後來研究索引各層的筆數(Record Count)。</p> <p>下圖是另一次 1000 萬筆測試的 <code>dm_db_index_physical_stats</code> 查詢結果，我在最後補了兩個欄位，每頁實際筆數 = rocord count / page count，每頁實際筆數 = 每頁實際筆數 / avg page space used %：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638447277370181690.png"></p> <p>在上圖中，我們可以看到 GuidClustIdx 跟 IntClustIdx 的叢集索引都是四層，Leaf 層(Level 0)都是 1000 萬筆，但 Level 1，GuidClustIdx 為 5000619 筆，IntClustIdx 為 3333334 筆，Level 2 為 22397 vs 15549。</p> <p>SQL 採用 <a href="https://learn.microsoft.com/en-us/sql/relational-databases/sql-server-index-design-guide?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#clustered-index-architecture">B+ 樹結構</a>：<br> (延伸閱讀與下圖來源：<a href="https://mark-lin.com/posts/20190911/">資料庫層的核心 - 索引結構演化論 B+樹 by 拿鐵派的馬克</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638447277372305002.png"></p> <p>依據<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-2-calculate-the-space-used-to-store-index-information">官方文件</a>算出非 Leaf 層每個 Page 可以容納的索引資料列數為 <code>Index_Rows_Per_Page = 8096 / (Index_Row_Size + 2)</code>，故可得 Guid 叢集索引為 8096 / (23 + 2) = 322、BIGINT 叢集索引為 8096 / (15 + 2) = 474，這數字很接近【每頁原本筆數】(該欄位依實際狀況反推，每頁有多有少，故不會完全相等)，而下層可容納最多索引資數為上層的 322 及 474 倍，故 Level - 1 是 Level 層的 322/474 倍，322/474 為理論值，實際數字要用【每頁實際筆數】計算，GuidClustIdx 可得 106 * 211.29 = 22397，22397 * 223.27 = 5000619；IntClustIdx 為 73 * 214.37 = 15649、15649 * 213.006 = 3333334，數字就對上了。</p> <p>而 Leaf 層每頁可容納筆數的算法為 <code>Rows_Per_Page = 8096 / (Row_Size + 2)</code>，用 2027 或 2035 算約 3.9，依使用率 50.10%、75.48% 推算每頁 2 及 3 筆，Leaf 層筆數為 1000 萬，除以 2 及 3 便是 GuidClustIdx 與 IntClustIdx Level = 1 層 5000619 與 3333334 的由來。而依此估算，當 GuidClustIdx 資料超過 322^3 (約 3339 萬筆) 時，索引結構會用到五層。(註：實驗進行中，結果後補)</p> <p>再累積一些索引碎片化知識。</p> <p data-slug="guid-clust-idx-frag-n-space" data-fbimg="https://cdn.pixabay.com/photo/2016/07/14/19/55/fat-1517449_960_720.jpg#1280#720">The web page shows that using GUID causes more fragmentation, lower page space utilization, and slower INSERT performance than using BIGINT. It also explains the structure and size of the index rows and data pages, and how they affect the storage space and efficiency.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>上篇我們觀察到用 GUID 做為叢集索引，<a href="/blog/how-guid-clust-index-bad-is/">INSERT 耗時較久且偶發慢到 4 ~ 6 秒的狀況</a>。</p> <p>依直覺索引碎片化的影響主要在於讀寫效能不佳(要走過更多資料分頁)，但觀察容量計時我發現另一件有趣的事，同樣是一千萬筆資料，IntClustIdx 比 GuidClustIdx 多一個 BIGINT 欄位，每筆資料多 8 Bytes (2035 vs 2027)，但 IntClustIdx 用的空間比較少，只有 27 GB，少一個欄位的 GuidClustIdx 反而用掉 40GB，這是怎麼一回事？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638447277359190777.png"></p> <p>這也與叢集索引碎片化有關。</p> <p>由於 GUID 無連續性，INSERT 資料時會導致 Page Split，拆分為兩個分頁並<a href="https://dba.stackexchange.com/a/155258/197986">將資料拆成 50/50，各使用一半空間</a> (Internal Fragmentation)。</p> <p>要觀察索引碎片化狀態，<code>dm_db_index_physical_stats</code> 是最效的指標。(相關背景知識可參見保哥文章：<a href="https://blog.miniasp.com/post/2009/01/18/Let-SQL-Server-Tell-You-Which-Indexes-to-Rebuild-or-Reorganize">讓 SQL Server 告訴你有哪些索引應該被重建或重組</a>)</p> <p>使用 <code>dm_db_index_physical_stats</code> 查詢索引碎裂狀態，我們可以得到兩項重要指標：<code>avg_fragmentation_in_percent</code>、<code>avg_page_space_used_in_percent</code>。</p> <p><code>avg_fragmentation_in_percent</code> 可視為碎片化的程度。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/system-dynamic-management-views/sys-dm-db-index-physical-stats-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#logical-fragmentation">Logical fragmentation</a> 官方翻譯為邏輯片段，我覺得譯成「邏輯碎片」更體切，指的是索引資料分頁邏輯順序與實體順序不一致，邏輯上依索引順序應該連續的資料分頁，被放在不連續的實體位置，如此存取將需要更多 IO 動作，拖慢效能。此值介於 10 到 15 之間時建議做索引重組，大於 15 時應考慮進行索引重建。</p> <p><code>avg_page_space_used_in_percent</code> 則是資料分頁存放資料的比率，比例過低將造成空間浪費(本案例的狀況)，代表要走訪更多分頁才能蒐集完所需要資料，勢必影響效能。這個值介於 60 到 75 之間時建議做索引重組，低於 60 時應考慮索引重建。</p> <p>以下 SQL 指令修改自保哥文章，增加顯示筆數與每筆資料長度：</p> <pre><code>SELECT OBJECT_NAME(dt.object_id),
si.name,
dt.index_level,
dt.avg_fragmentation_in_percent,
dt.avg_page_space_used_in_percent,
dt.record_count,
dt.avg_record_size_in_bytes
FROM
(SELECT object_id,
index_id,
index_level,
avg_fragmentation_in_percent,
avg_page_space_used_in_percent,
record_count,
avg_record_size_in_bytes
FROM    sys.dm_db_index_physical_stats (DB_ID(), NULL, NULL, NULL, 'DETAILED')
WHERE   index_id &lt;&gt; 0
) AS dt --does not return information about heaps
INNER JOIN sys.indexes si
ON     si.object_id = dt.object_id
AND si.index_id  = dt.index_id
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638447277361338412.png"></p> <p>利用這個機會，來研究其中數字的奧祕。非 Leaf Page (index_level &gt; 0) 的每筆索引長度(<code>avg_record_size_in_bytes</code>) 為什麼是 23 [1] 及 15 [2]？</p> <p>由<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-nonclustered-index?view=sql-server-ver15&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-variables-for-use-in-steps-2-and-3">官方文件</a>：<code>Index_Row_Size = Fixed_Key_Size + Variable_Key_Size + Index_Null_Bitmap + 1 (for row header overhead of an index row) + 6 (for the child page ID pointer)</code>，當索引沒有 Nullable 欄位，<code>Index_Null_Bitmap</code> = 0，因此 GuidClustIdx 為 16 (Uniqueidentity 長度) + 1 + 6 = 23，IntClustIdx 為 8 (Bigint 長度) + 1 + 6 = 15。</p> <p>至於叢集索引的 Leaf Page (index_level == 0) 會實際儲存資料內容，其結構為 TagA (1) + TagB (1) + Fsize (2) + Fdata (N, 固定欄位資料) + Ncol (2) + Nullbits (Ncol/8) + VarCount (2) + VarOffset (2*VarCount) + VarData(N) + VersioningTag (14 或 0)，LongText 欄位為長度 1000 的字串，以 Unicode 編碼儲存為 2000 Bytes。故 GuidClustIdx 資料列長度為 1 + 1 + 2 + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2027 [3]，IntClustIdx 長度為 1 + 1 + 2 + 8(SeqNo) + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2035 [4]。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-the-space-used-to-store-data-in-the-leaf-level">詳細計算方式</a></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638447277363568068.png"><br> <a href="https://aboutsqlserver.com/2013/10/15/sql-server-storage-engine-data-pages-and-data-rows/">圖片來源</a></p> <p>由此推估，使用 GUID 叢集索引的問題在於碎片化造成資料分散在兩倍以上數量的資料分頁(Data Page)，每個分頁只咬半口，浪費空間是一回事，需走訪更多資料分頁影響效能是更嚴重的問題。每筆 2027 Bytes * 1000 萬筆理應 20GB，因資料分頁利用率 50% [5]，故實際佔用容量加倍成為 40GB。而 IntClustIdx 資料表資料分頁利用率 75% [5]，20G / 0.75 = 27G。</p> <p>追根究根，避用 GUID 當叢集索引是解緩索引碎片化的治本之道，但異動 Schema 工程浩大，不是所有系統都能配合修改，此時只能寄望索引重組或索引重建改善。</p> <p>很少有機會做出這個規模的測試資料，順便試試 120GB 3000 萬筆資料索引重建要花多久？我測了 <code>ALTER INDEX PK_GuidClustIdx ON GuidClustIdx REBUILD WITH (ONLINE = ON)</code> (線上模式，索引作業期間可讓並行使用者存取基礎資料表、叢集索引資料，及任何關聯的非叢集索引。但耗時會比較久且過程效能也會下降)</p> <p>索引重建前，破碎比例 99%，空間使用率 50%，耗用空間 120GB：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638447277365712993.png"></p> <p>實測線上重建三千萬筆資料叢集索引共耗時 15'50"。重建後，使用空間下降到 80G，破碎比例 0%，空間使用率上升到一般標準 75%。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638447277367970610.png"></p> <p>最後來研究索引各層的筆數(Record Count)。</p> <p>下圖是另一次 1000 萬筆測試的 <code>dm_db_index_physical_stats</code> 查詢結果，我在最後補了兩個欄位，每頁實際筆數 = rocord count / page count，每頁實際筆數 = 每頁實際筆數 / avg page space used %：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638447277370181690.png"></p> <p>在上圖中，我們可以看到 GuidClustIdx 跟 IntClustIdx 的叢集索引都是四層，Leaf 層(Level 0)都是 1000 萬筆，但 Level 1，GuidClustIdx 為 5000619 筆，IntClustIdx 為 3333334 筆，Level 2 為 22397 vs 15549。</p> <p>SQL 採用 <a href="https://learn.microsoft.com/en-us/sql/relational-databases/sql-server-index-design-guide?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#clustered-index-architecture">B+ 樹結構</a>：<br> (延伸閱讀與下圖來源：<a href="https://mark-lin.com/posts/20190911/">資料庫層的核心 - 索引結構演化論 B+樹 by 拿鐵派的馬克</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638447277372305002.png"></p> <p>依據<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-2-calculate-the-space-used-to-store-index-information">官方文件</a>算出非 Leaf 層每個 Page 可以容納的索引資料列數為 <code>Index_Rows_Per_Page = 8096 / (Index_Row_Size + 2)</code>，故可得 Guid 叢集索引為 8096 / (23 + 2) = 322、BIGINT 叢集索引為 8096 / (15 + 2) = 474，這數字很接近【每頁原本筆數】(該欄位依實際狀況反推，每頁有多有少，故不會完全相等)，而下層可容納最多索引資數為上層的 322 及 474 倍，故 Level - 1 是 Level 層的 322/474 倍，322/474 為理論值，實際數字要用【每頁實際筆數】計算，GuidClustIdx 可得 106 * 211.29 = 22397，22397 * 223.27 = 5000619；IntClustIdx 為 73 * 214.37 = 15649、15649 * 213.006 = 3333334，數字就對上了。</p> <p>而 Leaf 層每頁可容納筆數的算法為 <code>Rows_Per_Page = 8096 / (Row_Size + 2)</code>，用 2027 或 2035 算約 3.9，依使用率 50.10%、75.48% 推算每頁 2 及 3 筆，Leaf 層筆數為 1000 萬，除以 2 及 3 便是 GuidClustIdx 與 IntClustIdx Level = 1 層 5000619 與 3333334 的由來。而依此估算，當 GuidClustIdx 資料超過 322^3 (約 3339 萬筆) 時，索引結構會用到五層。(註：實驗進行中，結果後補)</p> <p>再累積一些索引碎片化知識。</p> <p data-slug="guid-clust-idx-frag-n-space" data-fbimg="https://cdn.pixabay.com/photo/2016/07/14/19/55/fat-1517449_960_720.jpg#1280#720">The web page shows that using GUID causes more fragmentation, lower page space utilization, and slower INSERT performance than using BIGINT. It also explains the structure and size of the index rows and data pages, and how they affect the storage space and efficiency.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>上篇我們觀察到用 GUID 做為叢集索引，<a href="/blog/how-guid-clust-index-bad-is/">INSERT 耗時較久且偶發慢到 4 ~ 6 秒的狀況</a>。</p> <p>依直覺索引碎片化的影響主要在於讀寫效能不佳(要走過更多資料分頁)，但觀察容量計時我發現另一件有趣的事，同樣是一千萬筆資料，IntClustIdx 比 GuidClustIdx 多一個 BIGINT 欄位，每筆資料多 8 Bytes (2035 vs 2027)，但 IntClustIdx 用的空間比較少，只有 27 GB，少一個欄位的 GuidClustIdx 反而用掉 40GB，這是怎麼一回事？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638447277359190777.png"></p> <p>這也與叢集索引碎片化有關。</p> <p>由於 GUID 無連續性，INSERT 資料時會導致 Page Split，拆分為兩個分頁並<a href="https://dba.stackexchange.com/a/155258/197986">將資料拆成 50/50，各使用一半空間</a> (Internal Fragmentation)。</p> <p>要觀察索引碎片化狀態，<code>dm_db_index_physical_stats</code> 是最效的指標。(相關背景知識可參見保哥文章：<a href="https://blog.miniasp.com/post/2009/01/18/Let-SQL-Server-Tell-You-Which-Indexes-to-Rebuild-or-Reorganize">讓 SQL Server 告訴你有哪些索引應該被重建或重組</a>)</p> <p>使用 <code>dm_db_index_physical_stats</code> 查詢索引碎裂狀態，我們可以得到兩項重要指標：<code>avg_fragmentation_in_percent</code>、<code>avg_page_space_used_in_percent</code>。</p> <p><code>avg_fragmentation_in_percent</code> 可視為碎片化的程度。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/system-dynamic-management-views/sys-dm-db-index-physical-stats-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#logical-fragmentation">Logical fragmentation</a> 官方翻譯為邏輯片段，我覺得譯成「邏輯碎片」更體切，指的是索引資料分頁邏輯順序與實體順序不一致，邏輯上依索引順序應該連續的資料分頁，被放在不連續的實體位置，如此存取將需要更多 IO 動作，拖慢效能。此值介於 10 到 15 之間時建議做索引重組，大於 15 時應考慮進行索引重建。</p> <p><code>avg_page_space_used_in_percent</code> 則是資料分頁存放資料的比率，比例過低將造成空間浪費(本案例的狀況)，代表要走訪更多分頁才能蒐集完所需要資料，勢必影響效能。這個值介於 60 到 75 之間時建議做索引重組，低於 60 時應考慮索引重建。</p> <p>以下 SQL 指令修改自保哥文章，增加顯示筆數與每筆資料長度：</p> <pre><code>SELECT OBJECT_NAME(dt.object_id),
si.name,
dt.index_level,
dt.avg_fragmentation_in_percent,
dt.avg_page_space_used_in_percent,
dt.record_count,
dt.avg_record_size_in_bytes
FROM
(SELECT object_id,
index_id,
index_level,
avg_fragmentation_in_percent,
avg_page_space_used_in_percent,
record_count,
avg_record_size_in_bytes
FROM    sys.dm_db_index_physical_stats (DB_ID(), NULL, NULL, NULL, 'DETAILED')
WHERE   index_id &lt;&gt; 0
) AS dt --does not return information about heaps
INNER JOIN sys.indexes si
ON     si.object_id = dt.object_id
AND si.index_id  = dt.index_id
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638447277361338412.png"></p> <p>利用這個機會，來研究其中數字的奧祕。非 Leaf Page (index_level &gt; 0) 的每筆索引長度(<code>avg_record_size_in_bytes</code>) 為什麼是 23 [1] 及 15 [2]？</p> <p>由<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-nonclustered-index?view=sql-server-ver15&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-variables-for-use-in-steps-2-and-3">官方文件</a>：<code>Index_Row_Size = Fixed_Key_Size + Variable_Key_Size + Index_Null_Bitmap + 1 (for row header overhead of an index row) + 6 (for the child page ID pointer)</code>，當索引沒有 Nullable 欄位，<code>Index_Null_Bitmap</code> = 0，因此 GuidClustIdx 為 16 (Uniqueidentity 長度) + 1 + 6 = 23，IntClustIdx 為 8 (Bigint 長度) + 1 + 6 = 15。</p> <p>至於叢集索引的 Leaf Page (index_level == 0) 會實際儲存資料內容，其結構為 TagA (1) + TagB (1) + Fsize (2) + Fdata (N, 固定欄位資料) + Ncol (2) + Nullbits (Ncol/8) + VarCount (2) + VarOffset (2*VarCount) + VarData(N) + VersioningTag (14 或 0)，LongText 欄位為長度 1000 的字串，以 Unicode 編碼儲存為 2000 Bytes。故 GuidClustIdx 資料列長度為 1 + 1 + 2 + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2027 [3]，IntClustIdx 長度為 1 + 1 + 2 + 8(SeqNo) + 16(PK) + 2 + 1 + 2 + 2 + 2000 = 2035 [4]。<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-1-calculate-the-space-used-to-store-data-in-the-leaf-level">詳細計算方式</a></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638447277363568068.png"><br> <a href="https://aboutsqlserver.com/2013/10/15/sql-server-storage-engine-data-pages-and-data-rows/">圖片來源</a></p> <p>由此推估，使用 GUID 叢集索引的問題在於碎片化造成資料分散在兩倍以上數量的資料分頁(Data Page)，每個分頁只咬半口，浪費空間是一回事，需走訪更多資料分頁影響效能是更嚴重的問題。每筆 2027 Bytes * 1000 萬筆理應 20GB，因資料分頁利用率 50% [5]，故實際佔用容量加倍成為 40GB。而 IntClustIdx 資料表資料分頁利用率 75% [5]，20G / 0.75 = 27G。</p> <p>追根究根，避用 GUID 當叢集索引是解緩索引碎片化的治本之道，但異動 Schema 工程浩大，不是所有系統都能配合修改，此時只能寄望索引重組或索引重建改善。</p> <p>很少有機會做出這個規模的測試資料，順便試試 120GB 3000 萬筆資料索引重建要花多久？我測了 <code>ALTER INDEX PK_GuidClustIdx ON GuidClustIdx REBUILD WITH (ONLINE = ON)</code> (線上模式，索引作業期間可讓並行使用者存取基礎資料表、叢集索引資料，及任何關聯的非叢集索引。但耗時會比較久且過程效能也會下降)</p> <p>索引重建前，破碎比例 99%，空間使用率 50%，耗用空間 120GB：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638447277365712993.png"></p> <p>實測線上重建三千萬筆資料叢集索引共耗時 15'50"。重建後，使用空間下降到 80G，破碎比例 0%，空間使用率上升到一般標準 75%。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638447277367970610.png"></p> <p>最後來研究索引各層的筆數(Record Count)。</p> <p>下圖是另一次 1000 萬筆測試的 <code>dm_db_index_physical_stats</code> 查詢結果，我在最後補了兩個欄位，每頁實際筆數 = rocord count / page count，每頁實際筆數 = 每頁實際筆數 / avg page space used %：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638447277370181690.png"></p> <p>在上圖中，我們可以看到 GuidClustIdx 跟 IntClustIdx 的叢集索引都是四層，Leaf 層(Level 0)都是 1000 萬筆，但 Level 1，GuidClustIdx 為 5000619 筆，IntClustIdx 為 3333334 筆，Level 2 為 22397 vs 15549。</p> <p>SQL 採用 <a href="https://learn.microsoft.com/en-us/sql/relational-databases/sql-server-index-design-guide?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#clustered-index-architecture">B+ 樹結構</a>：<br> (延伸閱讀與下圖來源：<a href="https://mark-lin.com/posts/20190911/">資料庫層的核心 - 索引結構演化論 B+樹 by 拿鐵派的馬克</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638447277372305002.png"></p> <p>依據<a href="https://learn.microsoft.com/en-us/sql/relational-databases/databases/estimate-the-size-of-a-clustered-index?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#step-2-calculate-the-space-used-to-store-index-information">官方文件</a>算出非 Leaf 層每個 Page 可以容納的索引資料列數為 <code>Index_Rows_Per_Page = 8096 / (Index_Row_Size + 2)</code>，故可得 Guid 叢集索引為 8096 / (23 + 2) = 322、BIGINT 叢集索引為 8096 / (15 + 2) = 474，這數字很接近【每頁原本筆數】(該欄位依實際狀況反推，每頁有多有少，故不會完全相等)，而下層可容納最多索引資數為上層的 322 及 474 倍，故 Level - 1 是 Level 層的 322/474 倍，322/474 為理論值，實際數字要用【每頁實際筆數】計算，GuidClustIdx 可得 106 * 211.29 = 22397，22397 * 223.27 = 5000619；IntClustIdx 為 73 * 214.37 = 15649、15649 * 213.006 = 3333334，數字就對上了。</p> <p>而 Leaf 層每頁可容納筆數的算法為 <code>Rows_Per_Page = 8096 / (Row_Size + 2)</code>，用 2027 或 2035 算約 3.9，依使用率 50.10%、75.48% 推算每頁 2 及 3 筆，Leaf 層筆數為 1000 萬，除以 2 及 3 便是 GuidClustIdx 與 IntClustIdx Level = 1 層 5000619 與 3333334 的由來。而依此估算，當 GuidClustIdx 資料超過 322^3 (約 3339 萬筆) 時，索引結構會用到五層。(註：實驗進行中，結果後補)</p> <p>再累積一些索引碎片化知識。</p> <p data-slug="guid-clust-idx-frag-n-space" data-fbimg="https://cdn.pixabay.com/photo/2016/07/14/19/55/fat-1517449_960_720.jpg#1280#720">The web page shows that using GUID causes more fragmentation, lower page space utilization, and slower INSERT performance than using BIGINT. It also explains the structure and size of the index rows and data pages, and how they affect the storage space and efficiency.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638447277359190777.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638447277359190777.png" medium="image"/>
    <pubDate>Wed, 28 Feb 2024 14:35:11 GMT</pubDate>
  </item><item>
    <title><![CDATA[GUID 叢集索引測試 1 - 偶發寫入逾時之三千萬筆實測]]></title>
    <link>https://blog.darkthread.net/blog/how-guid-clust-index-bad-is/</link>
    <itunes:title><![CDATA[GUID 叢集索引測試 1 - 偶發寫入逾時之三千萬筆實測]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>我們都知道，資料庫使用 GUID 作為叢集索引易造成索引碎片化影響效能。若要用 GUID 作為 Primary Key，應另設自動跳號數字欄位當叢集索引。(註：不熟悉此議題的同學可先看這篇：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>近期在古蹟專案遇到偶發 INSERT 逾時錯誤(SqlCommand 執行時間上限預設 30 秒)，由於該系統使用 SQL Uniqueueidentity (GUID) 欄位當 Primary Key 兼叢集索引，資料表又屬千萬筆等級，令人懷疑與 GUID 叢集索引有關，於是我想在實驗室環境模擬，觀察 GUID 叢集索引造成 INSERT 逾時的機率。</p> <p>測試環境為 SQL 2022，8 核 CPU，32G RAM，磁碟讀寫速度約 170MB/s。用以下 SQL 指令建立兩個以 GUID 當 Primary Key 的資料表，其中 GuidClustIdx 直接使用 Guid 型別當叢集索引，IntClustIdx 則另設了 BIGINT 型別自動跳號當叢集索引。</p> <pre><code>CREATE TABLE [dbo].[GuidClustIdx](
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_GuidClustIdx] PRIMARY KEY CLUSTERED
(
[PK] ASC
)
)
GO
CREATE TABLE [dbo].[IntClustIdx](
[SN] [bigint] IDENTITY(1,1) NOT NULL,
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_IntClustIdx] PRIMARY KEY NONCLUSTERED
(
[PK] ASC
),
CONSTRAINT [IX_IntClustIdx] UNIQUE CLUSTERED
(
[SN] ASC
)
)
GO
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638446393336711000.png"></p> <p>SQL 伺服器設定<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/backup-restore/recovery-models-sql-server?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">簡單復原模式</a>，資料庫檔案預先放大到 320GB，避免寫入過程序自動擴充干擾實驗。我寫了以下程式，可對 GuidClustIdx 或 IntClustIdx INSERT 指定筆數資料，NVARCHAR 欄位寫入 1000 個字元長度，過程記錄每次 INSERT 耗時，每 50 筆記錄切一次檔案。每次測試完用<a href="/blog/python-stats-cahrt/">昨天分享的程式</a>解析 Log 檔並繪成圖表。</p> <pre><code>using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum {
GuidClustIdx,
IntClustIdx
}
public static void InsertData(TableNameEnum tableName, long rowCount)
{
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
}
logger.Flush();
logger.Dispose();
}
}
</code></pre> <p>我反覆測了幾次，同樣 INSERT 1000 萬筆，用 GUID 當叢集索引速度慢了一倍，需花約兩個小時，BIGINT 叢集索引則是一個小時出頭做完。若每 100 筆統計一次，繪製平均 INSERT 時間(藍線)、最大時間(紅點)、99 百分位數時間(綠點)圖表如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638446393342883228.png"></p> <p>如上圖所示，GUID 雖然平均時間較慢(不時會到 40ms)，偶爾會有 2 秒以上的最大值，最糟到 4 秒多；INT 的平均值不超過 20ms，最大值也都在一秒以內，極值約 1.5 秒。但值得注意的是綠色部分，99 百分位數代表有 99% 的 INSERT 都低於這個時間，由於是以 100 筆統計，可視為其中第二慢的時間。GUID 的 99 百分位數值約 3ms，可解讀為 100 筆只有一筆特別高，其他 99 筆都在 3ms 以下。由此推測，GUID 叢集索引雖然效率較差，但與自動跳號叢集索引的差異並不懸殊，且在本實驗未重現 30 秒逾時這類嚴重狀況。</p> <p>繼續測試寫入到 2000 萬筆及 3000 萬筆：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638446400739600758.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638446400741931782.png"></p> <p>INT 表現穩定，GUID 也維持平均不超過 50ms，99% 低於 10ms，偶發最大值超過 4 秒的水準，時間並未隨著筆數增加拉長。</p> <p>由以上測試結果，在無其他查詢干擾的單純 INSERT 實驗裡，GUID 雖會造成 INSERT 變慢，100 筆裡可能有一兩筆時間長達 4-6 秒，但不足以直接引發超過 30 秒的逾時錯誤，且偏慢狀況也沒有隨筆數增加出現等比例或雪崩式惡化(因為 B+ Tree 是 O(log n)?) 。但在負載高於硬體效能水準的情境(Disk IO 消化不及應是主因)，偶發的偏慢可能被放大 N 倍，超過 30 秒並非不可能。</p> <p>結論：用 GUID 當叢集索引，雖然偏慢，但並不如想像會在筆數上升後出現崩盤式的惡化。在實驗室環境控制變因測試下，99% INSERT 都能在正常時間內完成，但會偶發慢達數秒的異常速度。若遇到伺服器硬體效能無法勝任負載時(Disk Time 100%、Disk Queue 過長)，偶發偏慢到數秒的 INSERT，惡化到超過 30 秒的機率是會比使用循序叢集索引來得高。</p> <p data-slug="how-guid-clust-index-bad-is" data-title-en="How bad is the performance of using Guid Clustered Index?" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/163016/crash-test-collision-60-km-h-distraction-163016.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">This article verifies whether there are serious performance issues with tables using Guid Clustered Index by empirically testing the insertion of 30 million pieces of data.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>我們都知道，資料庫使用 GUID 作為叢集索引易造成索引碎片化影響效能。若要用 GUID 作為 Primary Key，應另設自動跳號數字欄位當叢集索引。(註：不熟悉此議題的同學可先看這篇：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>近期在古蹟專案遇到偶發 INSERT 逾時錯誤(SqlCommand 執行時間上限預設 30 秒)，由於該系統使用 SQL Uniqueueidentity (GUID) 欄位當 Primary Key 兼叢集索引，資料表又屬千萬筆等級，令人懷疑與 GUID 叢集索引有關，於是我想在實驗室環境模擬，觀察 GUID 叢集索引造成 INSERT 逾時的機率。</p> <p>測試環境為 SQL 2022，8 核 CPU，32G RAM，磁碟讀寫速度約 170MB/s。用以下 SQL 指令建立兩個以 GUID 當 Primary Key 的資料表，其中 GuidClustIdx 直接使用 Guid 型別當叢集索引，IntClustIdx 則另設了 BIGINT 型別自動跳號當叢集索引。</p> <pre><code>CREATE TABLE [dbo].[GuidClustIdx](
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_GuidClustIdx] PRIMARY KEY CLUSTERED
(
[PK] ASC
)
)
GO
CREATE TABLE [dbo].[IntClustIdx](
[SN] [bigint] IDENTITY(1,1) NOT NULL,
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_IntClustIdx] PRIMARY KEY NONCLUSTERED
(
[PK] ASC
),
CONSTRAINT [IX_IntClustIdx] UNIQUE CLUSTERED
(
[SN] ASC
)
)
GO
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638446393336711000.png"></p> <p>SQL 伺服器設定<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/backup-restore/recovery-models-sql-server?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">簡單復原模式</a>，資料庫檔案預先放大到 320GB，避免寫入過程序自動擴充干擾實驗。我寫了以下程式，可對 GuidClustIdx 或 IntClustIdx INSERT 指定筆數資料，NVARCHAR 欄位寫入 1000 個字元長度，過程記錄每次 INSERT 耗時，每 50 筆記錄切一次檔案。每次測試完用<a href="/blog/python-stats-cahrt/">昨天分享的程式</a>解析 Log 檔並繪成圖表。</p> <pre><code>using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum {
GuidClustIdx,
IntClustIdx
}
public static void InsertData(TableNameEnum tableName, long rowCount)
{
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
}
logger.Flush();
logger.Dispose();
}
}
</code></pre> <p>我反覆測了幾次，同樣 INSERT 1000 萬筆，用 GUID 當叢集索引速度慢了一倍，需花約兩個小時，BIGINT 叢集索引則是一個小時出頭做完。若每 100 筆統計一次，繪製平均 INSERT 時間(藍線)、最大時間(紅點)、99 百分位數時間(綠點)圖表如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638446393342883228.png"></p> <p>如上圖所示，GUID 雖然平均時間較慢(不時會到 40ms)，偶爾會有 2 秒以上的最大值，最糟到 4 秒多；INT 的平均值不超過 20ms，最大值也都在一秒以內，極值約 1.5 秒。但值得注意的是綠色部分，99 百分位數代表有 99% 的 INSERT 都低於這個時間，由於是以 100 筆統計，可視為其中第二慢的時間。GUID 的 99 百分位數值約 3ms，可解讀為 100 筆只有一筆特別高，其他 99 筆都在 3ms 以下。由此推測，GUID 叢集索引雖然效率較差，但與自動跳號叢集索引的差異並不懸殊，且在本實驗未重現 30 秒逾時這類嚴重狀況。</p> <p>繼續測試寫入到 2000 萬筆及 3000 萬筆：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638446400739600758.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638446400741931782.png"></p> <p>INT 表現穩定，GUID 也維持平均不超過 50ms，99% 低於 10ms，偶發最大值超過 4 秒的水準，時間並未隨著筆數增加拉長。</p> <p>由以上測試結果，在無其他查詢干擾的單純 INSERT 實驗裡，GUID 雖會造成 INSERT 變慢，100 筆裡可能有一兩筆時間長達 4-6 秒，但不足以直接引發超過 30 秒的逾時錯誤，且偏慢狀況也沒有隨筆數增加出現等比例或雪崩式惡化(因為 B+ Tree 是 O(log n)?) 。但在負載高於硬體效能水準的情境(Disk IO 消化不及應是主因)，偶發的偏慢可能被放大 N 倍，超過 30 秒並非不可能。</p> <p>結論：用 GUID 當叢集索引，雖然偏慢，但並不如想像會在筆數上升後出現崩盤式的惡化。在實驗室環境控制變因測試下，99% INSERT 都能在正常時間內完成，但會偶發慢達數秒的異常速度。若遇到伺服器硬體效能無法勝任負載時(Disk Time 100%、Disk Queue 過長)，偶發偏慢到數秒的 INSERT，惡化到超過 30 秒的機率是會比使用循序叢集索引來得高。</p> <p data-slug="how-guid-clust-index-bad-is" data-title-en="How bad is the performance of using Guid Clustered Index?" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/163016/crash-test-collision-60-km-h-distraction-163016.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">This article verifies whether there are serious performance issues with tables using Guid Clustered Index by empirically testing the insertion of 30 million pieces of data.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>我們都知道，資料庫使用 GUID 作為叢集索引易造成索引碎片化影響效能。若要用 GUID 作為 Primary Key，應另設自動跳號數字欄位當叢集索引。(註：不熟悉此議題的同學可先看這篇：<a href="/blog/guid-as-pk-on-db/">GUID Primary Key 資料庫避雷守則</a>)</p> <p>近期在古蹟專案遇到偶發 INSERT 逾時錯誤(SqlCommand 執行時間上限預設 30 秒)，由於該系統使用 SQL Uniqueueidentity (GUID) 欄位當 Primary Key 兼叢集索引，資料表又屬千萬筆等級，令人懷疑與 GUID 叢集索引有關，於是我想在實驗室環境模擬，觀察 GUID 叢集索引造成 INSERT 逾時的機率。</p> <p>測試環境為 SQL 2022，8 核 CPU，32G RAM，磁碟讀寫速度約 170MB/s。用以下 SQL 指令建立兩個以 GUID 當 Primary Key 的資料表，其中 GuidClustIdx 直接使用 Guid 型別當叢集索引，IntClustIdx 則另設了 BIGINT 型別自動跳號當叢集索引。</p> <pre><code>CREATE TABLE [dbo].[GuidClustIdx](
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_GuidClustIdx] PRIMARY KEY CLUSTERED
(
[PK] ASC
)
)
GO
CREATE TABLE [dbo].[IntClustIdx](
[SN] [bigint] IDENTITY(1,1) NOT NULL,
[PK] [uniqueidentifier] NOT NULL,
[LongText] [nvarchar](max) NOT NULL,
CONSTRAINT [PK_IntClustIdx] PRIMARY KEY NONCLUSTERED
(
[PK] ASC
),
CONSTRAINT [IX_IntClustIdx] UNIQUE CLUSTERED
(
[SN] ASC
)
)
GO
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638446393336711000.png"></p> <p>SQL 伺服器設定<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/backup-restore/recovery-models-sql-server?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">簡單復原模式</a>，資料庫檔案預先放大到 320GB，避免寫入過程序自動擴充干擾實驗。我寫了以下程式，可對 GuidClustIdx 或 IntClustIdx INSERT 指定筆數資料，NVARCHAR 欄位寫入 1000 個字元長度，過程記錄每次 INSERT 耗時，每 50 筆記錄切一次檔案。每次測試完用<a href="/blog/python-stats-cahrt/">昨天分享的程式</a>解析 Log 檔並繪成圖表。</p> <pre><code>using Microsoft.Data.SqlClient;
public class SqlRunner
{
const string cs = "data source=.;initial catalog=FragPerfTest;integrated security=True;encrypt=false";
public enum TableNameEnum {
GuidClustIdx,
IntClustIdx
}
public static void InsertData(TableNameEnum tableName, long rowCount)
{
using var cn = new SqlConnection(cs);
cn.Open();
using var cmd = new SqlCommand($"INSERT INTO {tableName} (PK, LongText) VALUES (@pk, @text)", cn);
cmd.CommandTimeout = 300;
var pPK = cmd.Parameters.Add("@pk", System.Data.SqlDbType.UniqueIdentifier);
var pText = cmd.Parameters.Add("@text", System.Data.SqlDbType.NVarChar, -1);
pText.Value = new string('x', 1000);
var sw = new System.Diagnostics.Stopwatch();
Directory.CreateDirectory("logs");
Func&lt;string&gt; getFileName = () =&gt; $"logs\\Test-{DateTime.Now:MMdd-HHmmss}.txt";
StreamWriter logger = new StreamWriter(getFileName());
for (var i = 0; i &lt; rowCount; i++)
{
pPK.Value = Guid.NewGuid();
sw.Restart();
cmd.ExecuteNonQuery();
sw.Stop();
if (i % 100 == 0) Console.WriteLine($"{i:n0} Rows Inserted");
if (i % 500_000 == 0)
{
logger.Flush();
logger.Dispose();
logger = new StreamWriter(getFileName());
}
logger.WriteLine($"{i + 1}: {sw.ElapsedMilliseconds}ms");
}
logger.Flush();
logger.Dispose();
}
}
</code></pre> <p>我反覆測了幾次，同樣 INSERT 1000 萬筆，用 GUID 當叢集索引速度慢了一倍，需花約兩個小時，BIGINT 叢集索引則是一個小時出頭做完。若每 100 筆統計一次，繪製平均 INSERT 時間(藍線)、最大時間(紅點)、99 百分位數時間(綠點)圖表如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638446393342883228.png"></p> <p>如上圖所示，GUID 雖然平均時間較慢(不時會到 40ms)，偶爾會有 2 秒以上的最大值，最糟到 4 秒多；INT 的平均值不超過 20ms，最大值也都在一秒以內，極值約 1.5 秒。但值得注意的是綠色部分，99 百分位數代表有 99% 的 INSERT 都低於這個時間，由於是以 100 筆統計，可視為其中第二慢的時間。GUID 的 99 百分位數值約 3ms，可解讀為 100 筆只有一筆特別高，其他 99 筆都在 3ms 以下。由此推測，GUID 叢集索引雖然效率較差，但與自動跳號叢集索引的差異並不懸殊，且在本實驗未重現 30 秒逾時這類嚴重狀況。</p> <p>繼續測試寫入到 2000 萬筆及 3000 萬筆：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638446400739600758.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638446400741931782.png"></p> <p>INT 表現穩定，GUID 也維持平均不超過 50ms，99% 低於 10ms，偶發最大值超過 4 秒的水準，時間並未隨著筆數增加拉長。</p> <p>由以上測試結果，在無其他查詢干擾的單純 INSERT 實驗裡，GUID 雖會造成 INSERT 變慢，100 筆裡可能有一兩筆時間長達 4-6 秒，但不足以直接引發超過 30 秒的逾時錯誤，且偏慢狀況也沒有隨筆數增加出現等比例或雪崩式惡化(因為 B+ Tree 是 O(log n)?) 。但在負載高於硬體效能水準的情境(Disk IO 消化不及應是主因)，偶發的偏慢可能被放大 N 倍，超過 30 秒並非不可能。</p> <p>結論：用 GUID 當叢集索引，雖然偏慢，但並不如想像會在筆數上升後出現崩盤式的惡化。在實驗室環境控制變因測試下，99% INSERT 都能在正常時間內完成，但會偶發慢達數秒的異常速度。若遇到伺服器硬體效能無法勝任負載時(Disk Time 100%、Disk Queue 過長)，偶發偏慢到數秒的 INSERT，惡化到超過 30 秒的機率是會比使用循序叢集索引來得高。</p> <p data-slug="how-guid-clust-index-bad-is" data-title-en="How bad is the performance of using Guid Clustered Index?" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/163016/crash-test-collision-60-km-h-distraction-163016.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">This article verifies whether there are serious performance issues with tables using Guid Clustered Index by empirically testing the insertion of 30 million pieces of data.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638446393336711000.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638446393336711000.png" medium="image"/>
    <pubDate>Tue, 27 Feb 2024 13:51:40 GMT</pubDate>
  </item><item>
    <title><![CDATA[分析 Log 計算平均、標準差、95 百分位數並繪製圖表 - 從 C# 到 Python]]></title>
    <link>https://blog.darkthread.net/blog/python-stats-cahrt/</link>
    <itunes:title><![CDATA[分析 Log 計算平均、標準差、95 百分位數並繪製圖表 - 從 C# 到 Python]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>資料庫 INSERT 測試產生了一堆 Log 記錄檔，一個檔每 50 萬筆，每筆格式為<code>序號: 耗時ms</code>：</p> <pre><code>...
9997242: 0ms
9997243: 29ms
9997244: 0ms
9997245: 1ms
9997246: 0ms
9997247: 0ms
9997248: 1ms
9997249: 0ms
...
</code></pre> <p>我想寫個程式，以每 100 筆為單位統計輸出成平均毫秒數、最大值、標準差、90/95/99 百分位數(意指 99% 都低於多少 ms)。</p> <p>這對 .NET 來說是小菜一碟，花了點時間研究 <a href="/blog/calc-percentile/">百分位數</a>，60 行程式打發，用 Parallel.ForEach 平行處理，兩秒內跑完，名符其實的秒殺：</p> <pre><code>using System.Collections.Concurrent;
public class LogParser
{
// 百分位數
static double percentile(int[] sorted, float percent)
{
if (sorted == null || !sorted.Any() || percent &lt; 0 || percent &gt; 100)
throw new ArgumentException();
var k = (sorted.Length - 1) * percent / 100.0;
var i = (int)Math.Floor(k);
var frac = k - i;
return sorted[i] + frac * (sorted[Math.Min(i + 1, sorted.Length - 1)] - sorted[i]);
}
public static void Parse()
{
var filesNames = Directory.GetFiles("data", "Test-*.txt");
var data = new ConcurrentDictionary&lt;int, (int mean, int max, int stdDev, int p90, int p95, int p99)&gt;();
Parallel.ForEach(filesNames, fileName =&gt;
{
var pool = new ConcurrentDictionary&lt;int, List&lt;int&gt;&gt;();
foreach (var line in File.ReadAllLines(fileName))
{
var parts = line.Split(':');
var idx = (int.Parse(parts[0]) - 1) / 100;
var dura = int.Parse(parts[1].Replace("ms", string.Empty));
if (!pool.ContainsKey(idx))
{
pool.TryAdd(idx, new List&lt;int&gt;());
}
pool[idx].Add(dura);
}
foreach (var kvp in pool)
{
var sorted = kvp.Value.OrderBy(x =&gt; x).ToArray();
var count = sorted.Length;
var mean = (int)sorted.Average();
var max = sorted.Max();
// 標準差
var stdDev = (int)Math.Sqrt(sorted.Sum(x =&gt; Math.Pow(x - mean, 2)) / count);
var p90 = (int)percentile(sorted, 90);
var p95 = (int)percentile(sorted, 95);
var p99 = (int)percentile(sorted, 99);
data.TryAdd(kvp.Key, (mean, max, stdDev, p90, p95, p99));
}
});
var ordered = data.OrderBy(kvp =&gt; kvp.Key);
using var sw = new StreamWriter("results\\stats-cs.csv");
sw.WriteLine("Index,Mean,Max,StdDev,P90,P95,P99");
foreach (var kvp in ordered)
{
var v = kvp.Value;
sw.WriteLine($"{kvp.Key},{v.mean},{v.max},{v.stdDev},{v.p90},{v.p95},{v.p99}");
}
}
}
</code></pre> <p>結果如下：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"></p> <p>過往，接下來我應該會開 Excel 匯 CSV 做圖表。今年我打算把 Python 也納入技能樹，決定練習改用 Python 來完成。</p> <p>問我想學 Python 的理由？</p> <p>很簡單，因為</p> <h3>Python 是 ChatGPT 跟 Github Copilot 的母語</h3> <p>可預見未來幾年 AI 將主導資訊系統發展方向，加上在學術研究領域的廣泛應用，簡單說，會 Python 跟懂英文的好處異曲同工，剩下的不需要多解釋了吧。</p> <p>手上已有 C# 版，雖然跟 Python 不熟，但在 Github Copliot 的指導下，我很快寫出一個可執行版本：</p> <pre><code>import glob
import concurrent.futures
import statistics
def parse_file(file):
with open(file, 'r') as f:
# 資料格式：12345: 0ms
lines = f.readlines()
stats = {}
for line in lines:
p = line.split(':')
seq = int(p[0]) - 1
# 註：用 // 除會取整數(無條件捨去)、用 / 除為浮點數
grpSeq = seq//100
dura = int(p[1].strip('ms
'))
# setdefault 可在 Dictionary 無值時建空陣列
stats.setdefault(grpSeq, []).append((seq, dura))
# 計算每組之平均、最大、標準差，90%、95%, 99% 百分位數
data = []
for k, v in stats.items():
duras = [x[1] for x in v]
duras.sort()
count = len(v)
mean = int(statistics.mean(duras))
maxVal = max(duras)
std = int(statistics.pstdev(duras, mu=mean))
p90 = duras[int(count * 0.9) - 1]
p95 = duras[int(count * 0.95) - 1]
p99 = duras[int(count * 0.99) - 1]
data.append((k, mean, maxVal, std, p90, p95, p99))
return data
files = glob.glob('.\data\Test-*.txt')
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
# 將結果組合成一個 list
data = []
for r in results:
data += r
# 依組別排序
data.sort(key=lambda x: x[0])
# 輸出成 csv 檔
with open('results\stats-py.csv', 'w') as f:
f.write('Index,Mean,Max,StdDev,P90,P95,P99
')
for d in data:
f.write(f'{d[0]},{d[1]},{d[2]},{d[3]},{d[4]},{d[5]},{d[6]}
')
</code></pre> <p>用類似的運算原理，換個語言寫，可得到跟 C# 完全相同的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638444298468376971.png"></p> <p>但有個問題，以上的程式跑完要 16 秒，是 C# 的八倍! 不過，這是因為我並沒有用 Python 最擅長的方式處理數學運算，豐富且強大的數學程式庫是 Python 的強項，別什麼都自己來。但應該怎麼改？你為什麼不問問<del>神奇海螺</del> Copilot 呢？</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638444298474866925.png"></p> <p>將程式碼丟給 Copilot 請它給建議，Copilot 建議我用 Pandas (排序、分群、統計用程式庫) 跟 NumPy (矩陣與數學運算百寶箱) 簡化程式，pandas 甚至有函式可以直接讀取 CSV，有現成的函式可以分群，計算百分位數，短短幾行做完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638444298477657963.png"></p> <p>程式只有 Pool() 跑多執行緒的部分有點問題，我改成用 ThreadPoolExecutor()，精簡版就順利完成了。</p> <pre><code>import glob
import pandas as pd
import numpy as np
def parse_file(file):
df = pd.read_csv(file, sep=":", names=["seq", "dura"])
df["dura"] = df["dura"].str.strip("ms
").astype(int)
df["Index"] = (df["seq"] - 1) // 100
grouped = df.groupby("Index")["dura"]
data = pd.concat(
[
grouped.mean().astype(int),
grouped.max(),
grouped.std().astype(int),
grouped.quantile(0.9).astype(int),
grouped.quantile(0.95).astype(int),
grouped.quantile(0.99).astype(int),
],
axis=1,
)
data.columns = ["Mean", "Max", "StdDev", "P90", "P95", "P99"]
return data.reset_index()
files = glob.glob(".\data\Test-*.txt")
with concurrent.futures.ThreadPoolExecutor() as executor:
results = executor.map(parse_file, files)
data = pd.concat(results).sort_values("Index")
data.to_csv("results\stats-py2.csv", index=False)
</code></pre> <p>Copilot 提供的版本 3 秒跑完，跟 C# 不相上下，除了標準差有點不同，其餘數字一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638444298479898674.png"></p> <p>算出結果，下一步靠 Python 豐富的程式庫把結果畫成圖表：(繼續請 Copilot 手把手帶著我寫完)</p> <pre><code>import matplotlib.pyplot as plt
def plot_stats(filename, title):
with open(filename, "r") as f:
stats = f.readlines()
# 略過第一行(Header)
stats = stats[1:]
stats = [list(map(int, row.strip().split(","))) for row in stats]
x = [row[0] for row in stats] # Index
yMean = [row[1] for row in stats] # Mean
yMax = [row[2] for row in stats] # Max
yP99 = [row[6] for row in stats] # P99
# 設定 12 吋寬、4 吋高
fig, ax1 = plt.subplots(figsize=(12, 4))
ax1.plot(x, yMean, alpha=0.8, color="b")
# 偵測 y 軸的最大值，設定 y 軸刻度
maxY = max(yMean)
if maxY &lt; 100:
tick = 10
elif maxY &lt; 1000:
tick = 100
ax1.set_yticks(range(0, int(maxY * 2), tick))
ax1.set_ylabel("Avg(ms)", color="b")
ax1.get_xaxis().set_major_formatter(
plt.FuncFormatter(lambda x, loc: "{:,}".format(int(x / 10)))
)
ax1.set_xlabel("rows (k)")
# 最大值
ax2 = ax1.twinx() # 與 ax1 共用 x 軸
ax2.scatter(x, yMax, alpha=0.6, color="r", s=1)
ax2.set_ylabel("Max/Percentile(ms)")
# 加上 99 百分位數
ax3 = ax1.twinx()
ax3.set_ylim(ax2.get_ylim()) # 與 ax2 Y 軸刻度一致
ax3.scatter(x, yP99, alpha=0.6, color="g", s=1.5)
plt.title(title)
plt.show()
plot_stats("results\stats-cs.csv", "Execution Time")
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638444318567451143.png"></p> <p>就醬，在 Github Copliot 教練的細心指導下，我完成第一支解析測試結果繪成圖表的 Python 程式，技能老樹冒出新芽。未來遇到用 Python 更省時省力的場合，我就有更多選擇囉~</p> <p data-slug="python-stats-cahrt" data-title-en="Using Python to analyze test log and draw statistics chart" data-catg="Python" data-fbimg="https://i.imgur.com/wiGLwug.jpg#1280#720">An example of using Python to analyze test logs and calculate mean, max, percentile and draw statistics chart.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638444298466197269.png" medium="image"/>
    <pubDate>Sun, 25 Feb 2024 03:45:12 GMT</pubDate>
  </item></channel>
</rss>