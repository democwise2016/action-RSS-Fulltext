<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
      <title><![CDATA[AD 跨界整合 - 使用 AD 登入第三方網站]]></title>
      <link>https://blog.darkthread.net/blog/aspnet-adfs-sso/</link>
      <itunes:title><![CDATA[AD 跨界整合 - 使用 AD 登入第三方網站]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>上篇文章 <a href="/blog/adfs-setup">辛苦安裝完 AD FS</a>，接下來試試 AD FS 跨界整合的威力。</p> <p>AD FS 支援多種登入協定，包含 WS-FED、SAML-P、OAuth 等，但 OAtuh 已成當今業界標準，聚焦 OAuth 就好。AD FS 認證身分取得授權的 OAuth 流程有以下幾種：</p> <ol><li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#implicit-grant-flow">Implicit Grant Flow</a><br> 適用純前端(如SPA)，依循 OAuth 2.0 規格，不需要後端伺服器認證交換，讓前端維持登入身分並獲得授權存取 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#authorization-code-grant-flow">Authorization Code Grant Flow</a><br> 適用大部分網站、App，分兩階段取得 authorization_code 及 access_token</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#on-behalf-of-flow">On-Behalf-Of Flow</a><br> WebAPI 代表用者呼叫其他 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#client-credentials-grant-flow">Client Credentials Grant Flow</a><br> 通常用於伺服器對伺服器間互動，背景程式執行期間沒有使用者參與，以應用程式的身分存取其他資源 (AD FS 也允許用憑證取代 Secret 提高安全性)</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#device-code-flow">Device Code Flow</a><br> 用於輸入受限裝置的登入(例如：Smart TV, IoT, 印表機)，使用者在其他裝置開瀏覽器輸入裝置代碼及登入，讓裝置取得授權。</li></ol> <p>以下是一些經典的 AD FS 應用場景：</p> <ul><li>SPA (Single Page Application) 純 JavaScript 開發，使用 MSAL 登入取得權限以存取 MS Graph API、企業 API 等資源</li> <li>網頁應用程式登入</li> <li>原生 App 登入取得授權存取 WebAPI</li> <li>網頁應用程式取得授權存取 WebAPI</li> <li>WebAPI 代表使用者呼叫其他 WebAPI (OBO, On Behalf Of)</li> <li>背景服務呼叫 Web API (使用 Client Credentials)</li> <li>網頁應用程式使用 Client Credentials 呼叫 WebAPI</li> <li>無瀏覽器 App 呼叫 Web API (使用 Device Code)</li></ul> <p>為了簡化開發，微軟提供通用的驗證程式庫 - MSAL - <a href="https://learn.microsoft.com/en-us/entra/msal/overview?WT.mc_id=DOP-MVP-37580">Microsoft Authentication Library</a>，支援 .NET/Java/JavaScript/Python/Android/Go/iOS，除了方便串接 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/v2-overview?WT.mc_id=DOP-MVP-37580">Microsft Identity Platform</a> (v2.0) Endpoint，整合 Azure AD、AD FS，也能整合 MS Account、FB/Google/Linkedin 帳號進行身分認證。</p> <p><img src="/Posts/files/2023/Fig1_638287455575299864.png"><br> <a href="https://learn.microsoft.com/en-us/azure/active-directory/develop/msal-migration?WT.mc_id=DOP-MVP-37580#why-switch-to-msal">圖片來源</a></p> <p>要用 MSAL.NET 整合 AD FS 有兩種做法：透過 Azure AD (Azure AD 與 AD FS 同盟，取得地端帳號、群組資料)，或直接與 AD FS 通訊(AD FS 2019+，要直接連 AD FS 2016 的話要用 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/azuread-dev/active-directory-authentication-libraries?WT.mc_id=DOP-MVP-37580#microsoft-supported-client-libraries">ADAL.NET</a>。<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/msal-net-adfs-support?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>研究了一下，要在雲端使用地端 AD，由 Azure AD 主導身分認證是未來的主流。以 MFA 為例，AD FS 也是<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/authentication/how-to-migrate-mfa-server-to-mfa-with-federation?source=recommendations&amp;WT.mc_id=DOP-MVP-37580#configure-azure-ad-mfa-as-an-authentication-provider-in-ad-fs">靠 Azure AD 加入 MFA 多因子驗證</a> (或是依賴 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/operations/configure-additional-authentication-methods-for-ad-fs?WT.mc_id=DOP-MVP-37580#microsoft-and-third-party-authentication-methods">MFA 協力廠商方案</a>)，前一段提到 MSAL.NET 可支援直接連 AD FS 2019，但查不到範例，幾乎都是 MSAL.NET 整合 Azure AD。</p> <p>我決定這篇文章先用 Microsoft.AspNetCore.Authentication.OpenIdConnect 簡單實現直連 AD FS，之後再玩 Azure AD。</p> <p>新建一個 MVC 專案，<code>dotnet add package Microsoft.AspNetCore.Authentication.OpenIdConnect</code> 引用套件。動手改程式前，先連上 AD FS 新增 Application Gruop：</p> <p><img src="/Posts/files/2023/Fig2_638287455577142855.png"></p> <p>輸入名稱，範本選 Web browser accessing a web application：</p> <p><img src="/Posts/files/2023/Fig3_638287455579332843.png"></p> <p>系統會自動產生 Client Identifier，這個要記下來，稍後程式會用到。Redirect URI 輸入要整合 AD FS 做登入的網站網址，我先在 Visual Studio 測試，填入偵錯模式網址 URL。若已知日後要部署的 URL，也可一併輸入：</p> <p><img src="/Posts/files/2023/Fig4_638287455581170278.png"></p> <p>存取控制有多組預設原則，我們先選 Permit everyone，之後若要強制啟用 MFA，可在此設定：</p> <p><img src="/Posts/files/2023/Fig5_638287455583154251.png"></p> <p>完成這些設定後，網站呼叫 AD FS 完成登入後，由 User.Claims 只會取得使用者帳號，我們可以調整下圖中的 Web Application 設定，把群組資料也帶入：</p> <p><img src="/Posts/files/2023/Fig6_638287455585027639.png"></p> <p>頁籤 Issuance Transform Rules 可設定要從 AD 傳送哪些 Claims 資料出去，我們的目的是送出帳號姓名及所屬群組：</p> <p><img src="/Posts/files/2023/Fig7_638287455586844393.png"></p> <p>做法是按 Add Rule，選 Send LDAP Attributes as Claims：</p> <p><img src="/Posts/files/2023/Fig8_638287455588763427.png"></p> <p>填入 Claim rule name，Attribute store 選 Active Directory，加入 Display-Name 轉 Common Name、Token-Groups - Unqualified Names 轉 Role 兩條設定。</p> <p><img src="/Posts/files/2023/Fig9_638287455590569401.png"></p> <p>至此，AD FS 這邊就設定好了。</p> <p>程式的話，先在 Program.cs 加入中文註解部分的程式碼啟用 OpenIdConnect 認證：</p> <pre><code>using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
using Microsoft.IdentityModel.Protocols.OpenIdConnect;
var builder = WebApplication.CreateBuilder(args);
// 加入 Cookie / OpenIdConnection 認證
builder.Services.AddAuthentication(options =&gt;
{
options.DefaultScheme = CookieAuthenticationDefaults.AuthenticationScheme;
options.DefaultChallengeScheme = OpenIdConnectDefaults.AuthenticationScheme;
})
.AddCookie(options =&gt;
{
options.AccessDeniedPath = "/";
options.LogoutPath = "/";
})
.AddOpenIdConnect(options =&gt;
{
// 由設定檔取得 Metadata URL https://adfs.&lt;domain-name&gt;/adfs/.well-known/openid-configuration
options.MetadataAddress = builder.Configuration["Adfs:MetadataAddress"];
// 由設定檔取得在 AD FS 建立 Appication Group 產生的 Client Id
options.ClientId = builder.Configuration["Adfs:ClientId"];
options.SignInScheme = "Cookies";
options.ResponseType = OpenIdConnectResponseType.Code;
options.UsePkce = false;
options.Scope.Clear();
options.Scope.Add("openid"); // 只要取得 OpenId 身分資料就好
options.SaveTokens = true;
});
// Add services to the container.
builder.Services.AddControllersWithViews();
var app = builder.Build();
// Configure the HTTP request pipeline.
if (!app.Environment.IsDevelopment())
{
app.UseExceptionHandler("/Home/Error");
// The default HSTS value is 30 days. You may want to change this for production scenarios, see https://aka.ms/aspnetcore-hsts.
app.UseHsts();
}
app.UseHttpsRedirection();
app.UseStaticFiles();
app.UseRouting();
//  啟用身分認證
app.UseAuthorization();
app.UseAuthorization();
app.MapControllerRoute(
name: "default",
pattern: "{controller=Home}/{action=Index}/{id?}");
app.Run();
</code></pre> <p>HomeController 的話，借用 Privacy Action，加上 [Authorize] 要求登入，</p> <pre><code>using System.Diagnostics;
using Microsoft.AspNetCore.Mvc;
using adfs_sso.Models;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Authentication;
using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
namespace adfs_sso.Controllers;
public class HomeController : Controller
{
private readonly ILogger&lt;HomeController&gt; _logger;
public HomeController(ILogger&lt;HomeController&gt; logger)
{
_logger = logger;
}
public IActionResult Index()
{
return View();
}
[Authorize] //要求登入
public IActionResult Privacy()
{
return View();
}
// 實作登出
public IActionResult Logout()
{
return SignOut(CookieAuthenticationDefaults.AuthenticationScheme,
OpenIdConnectDefaults.AuthenticationScheme);
}
[ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)]
public IActionResult Error()
{
return View(new ErrorViewModel { RequestId = Activity.Current?.Id ?? HttpContext.TraceIdentifier });
}
}
</code></pre> <p>_Layout.cshtml 加上一個 Logout 連結，在登入模式下才顯示：</p> <pre><code>&lt;ul class="navbar-nav flex-grow-1"&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index"&gt;Home&lt;/a&gt;
&lt;/li&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy"&gt;Privacy&lt;/a&gt;
&lt;/li&gt;
@if (Context.User.Identity.IsAuthenticated)
{
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Logout"&gt;Logout&lt;/a&gt;
&lt;/li&gt;
}
&lt;/ul&gt;
</code></pre> <p>至於 Privacy.cshtml，則換成以下內容：</p> <pre><code>@using System.Security.Claims
&lt;div&gt;
&lt;fieldset&gt;
&lt;legend&gt;User Info&lt;/legend&gt;
&lt;div&gt;User.Identity.Name = @User.Identity.Name&lt;/div&gt;
&lt;div&gt;
Common Name =
@User.FindFirstValue("http://schemas.xmlsoap.org/claims/CommonName")&lt;/div&gt;
&lt;div&gt;Is DomainUsers = @User.IsInRole("Domain Users")&lt;/div&gt;
&lt;div&gt;Is DomainAdmins = @User.IsInRole("Domain Admins")&lt;/div&gt;
&lt;/fieldset&gt;
&lt;fieldset&gt;
&lt;legend&gt;Claims&lt;/legend&gt;
@foreach (var claim in User.Claims)
{
&lt;div&gt;@claim.Type : @claim.Value&lt;/div&gt;
}
&lt;/fieldset&gt;
&lt;/div&gt;
</code></pre> <p>好戲上場。在以下展示中，開啟 Privacy 頁面會被導向 AD FS 登入畫面，輸入 AD 帳號密碼後，回到 Privacy 頁，由 User.Identity.Name、User.Claims 可取得傳回 Claims，而人員所屬群組 (Domain Users) 也被傳回，並可用 User.IsInRole("Domain Users") 成功簽測。</p> <p><a href="https://www.youtube.com/watch?v=_WewPbI-6VU">操作展示</a></p> <p>下一篇，我們再來看如何整合 Azure AD。</p> <p data-slug="aspnet-adfs-sso" data-title-en="Sign in ASP.NET Core with AD FS" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/02/16/13/50/ice-climbing-4000385_960_720.jpg#960#720">Example of ASP.NET Core authentication with AD FS via OpenIdConnect.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>上篇文章 <a href="/blog/adfs-setup">辛苦安裝完 AD FS</a>，接下來試試 AD FS 跨界整合的威力。</p> <p>AD FS 支援多種登入協定，包含 WS-FED、SAML-P、OAuth 等，但 OAtuh 已成當今業界標準，聚焦 OAuth 就好。AD FS 認證身分取得授權的 OAuth 流程有以下幾種：</p> <ol><li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#implicit-grant-flow">Implicit Grant Flow</a><br> 適用純前端(如SPA)，依循 OAuth 2.0 規格，不需要後端伺服器認證交換，讓前端維持登入身分並獲得授權存取 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#authorization-code-grant-flow">Authorization Code Grant Flow</a><br> 適用大部分網站、App，分兩階段取得 authorization_code 及 access_token</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#on-behalf-of-flow">On-Behalf-Of Flow</a><br> WebAPI 代表用者呼叫其他 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#client-credentials-grant-flow">Client Credentials Grant Flow</a><br> 通常用於伺服器對伺服器間互動，背景程式執行期間沒有使用者參與，以應用程式的身分存取其他資源 (AD FS 也允許用憑證取代 Secret 提高安全性)</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#device-code-flow">Device Code Flow</a><br> 用於輸入受限裝置的登入(例如：Smart TV, IoT, 印表機)，使用者在其他裝置開瀏覽器輸入裝置代碼及登入，讓裝置取得授權。</li></ol> <p>以下是一些經典的 AD FS 應用場景：</p> <ul><li>SPA (Single Page Application) 純 JavaScript 開發，使用 MSAL 登入取得權限以存取 MS Graph API、企業 API 等資源</li> <li>網頁應用程式登入</li> <li>原生 App 登入取得授權存取 WebAPI</li> <li>網頁應用程式取得授權存取 WebAPI</li> <li>WebAPI 代表使用者呼叫其他 WebAPI (OBO, On Behalf Of)</li> <li>背景服務呼叫 Web API (使用 Client Credentials)</li> <li>網頁應用程式使用 Client Credentials 呼叫 WebAPI</li> <li>無瀏覽器 App 呼叫 Web API (使用 Device Code)</li></ul> <p>為了簡化開發，微軟提供通用的驗證程式庫 - MSAL - <a href="https://learn.microsoft.com/en-us/entra/msal/overview?WT.mc_id=DOP-MVP-37580">Microsoft Authentication Library</a>，支援 .NET/Java/JavaScript/Python/Android/Go/iOS，除了方便串接 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/v2-overview?WT.mc_id=DOP-MVP-37580">Microsft Identity Platform</a> (v2.0) Endpoint，整合 Azure AD、AD FS，也能整合 MS Account、FB/Google/Linkedin 帳號進行身分認證。</p> <p><img src="/Posts/files/2023/Fig1_638287455575299864.png"><br> <a href="https://learn.microsoft.com/en-us/azure/active-directory/develop/msal-migration?WT.mc_id=DOP-MVP-37580#why-switch-to-msal">圖片來源</a></p> <p>要用 MSAL.NET 整合 AD FS 有兩種做法：透過 Azure AD (Azure AD 與 AD FS 同盟，取得地端帳號、群組資料)，或直接與 AD FS 通訊(AD FS 2019+，要直接連 AD FS 2016 的話要用 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/azuread-dev/active-directory-authentication-libraries?WT.mc_id=DOP-MVP-37580#microsoft-supported-client-libraries">ADAL.NET</a>。<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/msal-net-adfs-support?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>研究了一下，要在雲端使用地端 AD，由 Azure AD 主導身分認證是未來的主流。以 MFA 為例，AD FS 也是<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/authentication/how-to-migrate-mfa-server-to-mfa-with-federation?source=recommendations&amp;WT.mc_id=DOP-MVP-37580#configure-azure-ad-mfa-as-an-authentication-provider-in-ad-fs">靠 Azure AD 加入 MFA 多因子驗證</a> (或是依賴 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/operations/configure-additional-authentication-methods-for-ad-fs?WT.mc_id=DOP-MVP-37580#microsoft-and-third-party-authentication-methods">MFA 協力廠商方案</a>)，前一段提到 MSAL.NET 可支援直接連 AD FS 2019，但查不到範例，幾乎都是 MSAL.NET 整合 Azure AD。</p> <p>我決定這篇文章先用 Microsoft.AspNetCore.Authentication.OpenIdConnect 簡單實現直連 AD FS，之後再玩 Azure AD。</p> <p>新建一個 MVC 專案，<code>dotnet add package Microsoft.AspNetCore.Authentication.OpenIdConnect</code> 引用套件。動手改程式前，先連上 AD FS 新增 Application Gruop：</p> <p><img src="/Posts/files/2023/Fig2_638287455577142855.png"></p> <p>輸入名稱，範本選 Web browser accessing a web application：</p> <p><img src="/Posts/files/2023/Fig3_638287455579332843.png"></p> <p>系統會自動產生 Client Identifier，這個要記下來，稍後程式會用到。Redirect URI 輸入要整合 AD FS 做登入的網站網址，我先在 Visual Studio 測試，填入偵錯模式網址 URL。若已知日後要部署的 URL，也可一併輸入：</p> <p><img src="/Posts/files/2023/Fig4_638287455581170278.png"></p> <p>存取控制有多組預設原則，我們先選 Permit everyone，之後若要強制啟用 MFA，可在此設定：</p> <p><img src="/Posts/files/2023/Fig5_638287455583154251.png"></p> <p>完成這些設定後，網站呼叫 AD FS 完成登入後，由 User.Claims 只會取得使用者帳號，我們可以調整下圖中的 Web Application 設定，把群組資料也帶入：</p> <p><img src="/Posts/files/2023/Fig6_638287455585027639.png"></p> <p>頁籤 Issuance Transform Rules 可設定要從 AD 傳送哪些 Claims 資料出去，我們的目的是送出帳號姓名及所屬群組：</p> <p><img src="/Posts/files/2023/Fig7_638287455586844393.png"></p> <p>做法是按 Add Rule，選 Send LDAP Attributes as Claims：</p> <p><img src="/Posts/files/2023/Fig8_638287455588763427.png"></p> <p>填入 Claim rule name，Attribute store 選 Active Directory，加入 Display-Name 轉 Common Name、Token-Groups - Unqualified Names 轉 Role 兩條設定。</p> <p><img src="/Posts/files/2023/Fig9_638287455590569401.png"></p> <p>至此，AD FS 這邊就設定好了。</p> <p>程式的話，先在 Program.cs 加入中文註解部分的程式碼啟用 OpenIdConnect 認證：</p> <pre><code>using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
using Microsoft.IdentityModel.Protocols.OpenIdConnect;
var builder = WebApplication.CreateBuilder(args);
// 加入 Cookie / OpenIdConnection 認證
builder.Services.AddAuthentication(options =&gt;
{
options.DefaultScheme = CookieAuthenticationDefaults.AuthenticationScheme;
options.DefaultChallengeScheme = OpenIdConnectDefaults.AuthenticationScheme;
})
.AddCookie(options =&gt;
{
options.AccessDeniedPath = "/";
options.LogoutPath = "/";
})
.AddOpenIdConnect(options =&gt;
{
// 由設定檔取得 Metadata URL https://adfs.&lt;domain-name&gt;/adfs/.well-known/openid-configuration
options.MetadataAddress = builder.Configuration["Adfs:MetadataAddress"];
// 由設定檔取得在 AD FS 建立 Appication Group 產生的 Client Id
options.ClientId = builder.Configuration["Adfs:ClientId"];
options.SignInScheme = "Cookies";
options.ResponseType = OpenIdConnectResponseType.Code;
options.UsePkce = false;
options.Scope.Clear();
options.Scope.Add("openid"); // 只要取得 OpenId 身分資料就好
options.SaveTokens = true;
});
// Add services to the container.
builder.Services.AddControllersWithViews();
var app = builder.Build();
// Configure the HTTP request pipeline.
if (!app.Environment.IsDevelopment())
{
app.UseExceptionHandler("/Home/Error");
// The default HSTS value is 30 days. You may want to change this for production scenarios, see https://aka.ms/aspnetcore-hsts.
app.UseHsts();
}
app.UseHttpsRedirection();
app.UseStaticFiles();
app.UseRouting();
//  啟用身分認證
app.UseAuthorization();
app.UseAuthorization();
app.MapControllerRoute(
name: "default",
pattern: "{controller=Home}/{action=Index}/{id?}");
app.Run();
</code></pre> <p>HomeController 的話，借用 Privacy Action，加上 [Authorize] 要求登入，</p> <pre><code>using System.Diagnostics;
using Microsoft.AspNetCore.Mvc;
using adfs_sso.Models;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Authentication;
using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
namespace adfs_sso.Controllers;
public class HomeController : Controller
{
private readonly ILogger&lt;HomeController&gt; _logger;
public HomeController(ILogger&lt;HomeController&gt; logger)
{
_logger = logger;
}
public IActionResult Index()
{
return View();
}
[Authorize] //要求登入
public IActionResult Privacy()
{
return View();
}
// 實作登出
public IActionResult Logout()
{
return SignOut(CookieAuthenticationDefaults.AuthenticationScheme,
OpenIdConnectDefaults.AuthenticationScheme);
}
[ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)]
public IActionResult Error()
{
return View(new ErrorViewModel { RequestId = Activity.Current?.Id ?? HttpContext.TraceIdentifier });
}
}
</code></pre> <p>_Layout.cshtml 加上一個 Logout 連結，在登入模式下才顯示：</p> <pre><code>&lt;ul class="navbar-nav flex-grow-1"&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index"&gt;Home&lt;/a&gt;
&lt;/li&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy"&gt;Privacy&lt;/a&gt;
&lt;/li&gt;
@if (Context.User.Identity.IsAuthenticated)
{
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Logout"&gt;Logout&lt;/a&gt;
&lt;/li&gt;
}
&lt;/ul&gt;
</code></pre> <p>至於 Privacy.cshtml，則換成以下內容：</p> <pre><code>@using System.Security.Claims
&lt;div&gt;
&lt;fieldset&gt;
&lt;legend&gt;User Info&lt;/legend&gt;
&lt;div&gt;User.Identity.Name = @User.Identity.Name&lt;/div&gt;
&lt;div&gt;
Common Name =
@User.FindFirstValue("http://schemas.xmlsoap.org/claims/CommonName")&lt;/div&gt;
&lt;div&gt;Is DomainUsers = @User.IsInRole("Domain Users")&lt;/div&gt;
&lt;div&gt;Is DomainAdmins = @User.IsInRole("Domain Admins")&lt;/div&gt;
&lt;/fieldset&gt;
&lt;fieldset&gt;
&lt;legend&gt;Claims&lt;/legend&gt;
@foreach (var claim in User.Claims)
{
&lt;div&gt;@claim.Type : @claim.Value&lt;/div&gt;
}
&lt;/fieldset&gt;
&lt;/div&gt;
</code></pre> <p>好戲上場。在以下展示中，開啟 Privacy 頁面會被導向 AD FS 登入畫面，輸入 AD 帳號密碼後，回到 Privacy 頁，由 User.Identity.Name、User.Claims 可取得傳回 Claims，而人員所屬群組 (Domain Users) 也被傳回，並可用 User.IsInRole("Domain Users") 成功簽測。</p> <p><a href="https://www.youtube.com/watch?v=_WewPbI-6VU">操作展示</a></p> <p>下一篇，我們再來看如何整合 Azure AD。</p> <p data-slug="aspnet-adfs-sso" data-title-en="Sign in ASP.NET Core with AD FS" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/02/16/13/50/ice-climbing-4000385_960_720.jpg#960#720">Example of ASP.NET Core authentication with AD FS via OpenIdConnect.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>上篇文章 <a href="/blog/adfs-setup">辛苦安裝完 AD FS</a>，接下來試試 AD FS 跨界整合的威力。</p> <p>AD FS 支援多種登入協定，包含 WS-FED、SAML-P、OAuth 等，但 OAtuh 已成當今業界標準，聚焦 OAuth 就好。AD FS 認證身分取得授權的 OAuth 流程有以下幾種：</p> <ol><li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#implicit-grant-flow">Implicit Grant Flow</a><br> 適用純前端(如SPA)，依循 OAuth 2.0 規格，不需要後端伺服器認證交換，讓前端維持登入身分並獲得授權存取 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#authorization-code-grant-flow">Authorization Code Grant Flow</a><br> 適用大部分網站、App，分兩階段取得 authorization_code 及 access_token</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#on-behalf-of-flow">On-Behalf-Of Flow</a><br> WebAPI 代表用者呼叫其他 WebAPI</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#client-credentials-grant-flow">Client Credentials Grant Flow</a><br> 通常用於伺服器對伺服器間互動，背景程式執行期間沒有使用者參與，以應用程式的身分存取其他資源 (AD FS 也允許用憑證取代 Secret 提高安全性)</li> <li><a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/overview/ad-fs-openid-connect-oauth-flows-scenarios?WT.mc_id=DOP-MVP-37580#device-code-flow">Device Code Flow</a><br> 用於輸入受限裝置的登入(例如：Smart TV, IoT, 印表機)，使用者在其他裝置開瀏覽器輸入裝置代碼及登入，讓裝置取得授權。</li></ol> <p>以下是一些經典的 AD FS 應用場景：</p> <ul><li>SPA (Single Page Application) 純 JavaScript 開發，使用 MSAL 登入取得權限以存取 MS Graph API、企業 API 等資源</li> <li>網頁應用程式登入</li> <li>原生 App 登入取得授權存取 WebAPI</li> <li>網頁應用程式取得授權存取 WebAPI</li> <li>WebAPI 代表使用者呼叫其他 WebAPI (OBO, On Behalf Of)</li> <li>背景服務呼叫 Web API (使用 Client Credentials)</li> <li>網頁應用程式使用 Client Credentials 呼叫 WebAPI</li> <li>無瀏覽器 App 呼叫 Web API (使用 Device Code)</li></ul> <p>為了簡化開發，微軟提供通用的驗證程式庫 - MSAL - <a href="https://learn.microsoft.com/en-us/entra/msal/overview?WT.mc_id=DOP-MVP-37580">Microsoft Authentication Library</a>，支援 .NET/Java/JavaScript/Python/Android/Go/iOS，除了方便串接 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/v2-overview?WT.mc_id=DOP-MVP-37580">Microsft Identity Platform</a> (v2.0) Endpoint，整合 Azure AD、AD FS，也能整合 MS Account、FB/Google/Linkedin 帳號進行身分認證。</p> <p><img src="/Posts/files/2023/Fig1_638287455575299864.png"><br> <a href="https://learn.microsoft.com/en-us/azure/active-directory/develop/msal-migration?WT.mc_id=DOP-MVP-37580#why-switch-to-msal">圖片來源</a></p> <p>要用 MSAL.NET 整合 AD FS 有兩種做法：透過 Azure AD (Azure AD 與 AD FS 同盟，取得地端帳號、群組資料)，或直接與 AD FS 通訊(AD FS 2019+，要直接連 AD FS 2016 的話要用 <a href="https://learn.microsoft.com/zh-tw/azure/active-directory/azuread-dev/active-directory-authentication-libraries?WT.mc_id=DOP-MVP-37580#microsoft-supported-client-libraries">ADAL.NET</a>。<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/develop/msal-net-adfs-support?WT.mc_id=DOP-MVP-37580">參考</a></p> <p>研究了一下，要在雲端使用地端 AD，由 Azure AD 主導身分認證是未來的主流。以 MFA 為例，AD FS 也是<a href="https://learn.microsoft.com/zh-tw/azure/active-directory/authentication/how-to-migrate-mfa-server-to-mfa-with-federation?source=recommendations&amp;WT.mc_id=DOP-MVP-37580#configure-azure-ad-mfa-as-an-authentication-provider-in-ad-fs">靠 Azure AD 加入 MFA 多因子驗證</a> (或是依賴 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/operations/configure-additional-authentication-methods-for-ad-fs?WT.mc_id=DOP-MVP-37580#microsoft-and-third-party-authentication-methods">MFA 協力廠商方案</a>)，前一段提到 MSAL.NET 可支援直接連 AD FS 2019，但查不到範例，幾乎都是 MSAL.NET 整合 Azure AD。</p> <p>我決定這篇文章先用 Microsoft.AspNetCore.Authentication.OpenIdConnect 簡單實現直連 AD FS，之後再玩 Azure AD。</p> <p>新建一個 MVC 專案，<code>dotnet add package Microsoft.AspNetCore.Authentication.OpenIdConnect</code> 引用套件。動手改程式前，先連上 AD FS 新增 Application Gruop：</p> <p><img src="/Posts/files/2023/Fig2_638287455577142855.png"></p> <p>輸入名稱，範本選 Web browser accessing a web application：</p> <p><img src="/Posts/files/2023/Fig3_638287455579332843.png"></p> <p>系統會自動產生 Client Identifier，這個要記下來，稍後程式會用到。Redirect URI 輸入要整合 AD FS 做登入的網站網址，我先在 Visual Studio 測試，填入偵錯模式網址 URL。若已知日後要部署的 URL，也可一併輸入：</p> <p><img src="/Posts/files/2023/Fig4_638287455581170278.png"></p> <p>存取控制有多組預設原則，我們先選 Permit everyone，之後若要強制啟用 MFA，可在此設定：</p> <p><img src="/Posts/files/2023/Fig5_638287455583154251.png"></p> <p>完成這些設定後，網站呼叫 AD FS 完成登入後，由 User.Claims 只會取得使用者帳號，我們可以調整下圖中的 Web Application 設定，把群組資料也帶入：</p> <p><img src="/Posts/files/2023/Fig6_638287455585027639.png"></p> <p>頁籤 Issuance Transform Rules 可設定要從 AD 傳送哪些 Claims 資料出去，我們的目的是送出帳號姓名及所屬群組：</p> <p><img src="/Posts/files/2023/Fig7_638287455586844393.png"></p> <p>做法是按 Add Rule，選 Send LDAP Attributes as Claims：</p> <p><img src="/Posts/files/2023/Fig8_638287455588763427.png"></p> <p>填入 Claim rule name，Attribute store 選 Active Directory，加入 Display-Name 轉 Common Name、Token-Groups - Unqualified Names 轉 Role 兩條設定。</p> <p><img src="/Posts/files/2023/Fig9_638287455590569401.png"></p> <p>至此，AD FS 這邊就設定好了。</p> <p>程式的話，先在 Program.cs 加入中文註解部分的程式碼啟用 OpenIdConnect 認證：</p> <pre><code>using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
using Microsoft.IdentityModel.Protocols.OpenIdConnect;
var builder = WebApplication.CreateBuilder(args);
// 加入 Cookie / OpenIdConnection 認證
builder.Services.AddAuthentication(options =&gt;
{
options.DefaultScheme = CookieAuthenticationDefaults.AuthenticationScheme;
options.DefaultChallengeScheme = OpenIdConnectDefaults.AuthenticationScheme;
})
.AddCookie(options =&gt;
{
options.AccessDeniedPath = "/";
options.LogoutPath = "/";
})
.AddOpenIdConnect(options =&gt;
{
// 由設定檔取得 Metadata URL https://adfs.&lt;domain-name&gt;/adfs/.well-known/openid-configuration
options.MetadataAddress = builder.Configuration["Adfs:MetadataAddress"];
// 由設定檔取得在 AD FS 建立 Appication Group 產生的 Client Id
options.ClientId = builder.Configuration["Adfs:ClientId"];
options.SignInScheme = "Cookies";
options.ResponseType = OpenIdConnectResponseType.Code;
options.UsePkce = false;
options.Scope.Clear();
options.Scope.Add("openid"); // 只要取得 OpenId 身分資料就好
options.SaveTokens = true;
});
// Add services to the container.
builder.Services.AddControllersWithViews();
var app = builder.Build();
// Configure the HTTP request pipeline.
if (!app.Environment.IsDevelopment())
{
app.UseExceptionHandler("/Home/Error");
// The default HSTS value is 30 days. You may want to change this for production scenarios, see https://aka.ms/aspnetcore-hsts.
app.UseHsts();
}
app.UseHttpsRedirection();
app.UseStaticFiles();
app.UseRouting();
//  啟用身分認證
app.UseAuthorization();
app.UseAuthorization();
app.MapControllerRoute(
name: "default",
pattern: "{controller=Home}/{action=Index}/{id?}");
app.Run();
</code></pre> <p>HomeController 的話，借用 Privacy Action，加上 [Authorize] 要求登入，</p> <pre><code>using System.Diagnostics;
using Microsoft.AspNetCore.Mvc;
using adfs_sso.Models;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Authentication;
using Microsoft.AspNetCore.Authentication.Cookies;
using Microsoft.AspNetCore.Authentication.OpenIdConnect;
namespace adfs_sso.Controllers;
public class HomeController : Controller
{
private readonly ILogger&lt;HomeController&gt; _logger;
public HomeController(ILogger&lt;HomeController&gt; logger)
{
_logger = logger;
}
public IActionResult Index()
{
return View();
}
[Authorize] //要求登入
public IActionResult Privacy()
{
return View();
}
// 實作登出
public IActionResult Logout()
{
return SignOut(CookieAuthenticationDefaults.AuthenticationScheme,
OpenIdConnectDefaults.AuthenticationScheme);
}
[ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)]
public IActionResult Error()
{
return View(new ErrorViewModel { RequestId = Activity.Current?.Id ?? HttpContext.TraceIdentifier });
}
}
</code></pre> <p>_Layout.cshtml 加上一個 Logout 連結，在登入模式下才顯示：</p> <pre><code>&lt;ul class="navbar-nav flex-grow-1"&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index"&gt;Home&lt;/a&gt;
&lt;/li&gt;
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Privacy"&gt;Privacy&lt;/a&gt;
&lt;/li&gt;
@if (Context.User.Identity.IsAuthenticated)
{
&lt;li class="nav-item"&gt;
&lt;a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Logout"&gt;Logout&lt;/a&gt;
&lt;/li&gt;
}
&lt;/ul&gt;
</code></pre> <p>至於 Privacy.cshtml，則換成以下內容：</p> <pre><code>@using System.Security.Claims
&lt;div&gt;
&lt;fieldset&gt;
&lt;legend&gt;User Info&lt;/legend&gt;
&lt;div&gt;User.Identity.Name = @User.Identity.Name&lt;/div&gt;
&lt;div&gt;
Common Name =
@User.FindFirstValue("http://schemas.xmlsoap.org/claims/CommonName")&lt;/div&gt;
&lt;div&gt;Is DomainUsers = @User.IsInRole("Domain Users")&lt;/div&gt;
&lt;div&gt;Is DomainAdmins = @User.IsInRole("Domain Admins")&lt;/div&gt;
&lt;/fieldset&gt;
&lt;fieldset&gt;
&lt;legend&gt;Claims&lt;/legend&gt;
@foreach (var claim in User.Claims)
{
&lt;div&gt;@claim.Type : @claim.Value&lt;/div&gt;
}
&lt;/fieldset&gt;
&lt;/div&gt;
</code></pre> <p>好戲上場。在以下展示中，開啟 Privacy 頁面會被導向 AD FS 登入畫面，輸入 AD 帳號密碼後，回到 Privacy 頁，由 User.Identity.Name、User.Claims 可取得傳回 Claims，而人員所屬群組 (Domain Users) 也被傳回，並可用 User.IsInRole("Domain Users") 成功簽測。</p> <p><a href="https://www.youtube.com/watch?v=_WewPbI-6VU">操作展示</a></p> <p>下一篇，我們再來看如何整合 Azure AD。</p> <p data-slug="aspnet-adfs-sso" data-title-en="Sign in ASP.NET Core with AD FS" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/02/16/13/50/ice-climbing-4000385_960_720.jpg#960#720">Example of ASP.NET Core authentication with AD FS via OpenIdConnect.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638287455575299864.png"/>
        <media:content url="/Posts/files/2023/Fig1_638287455575299864.png" medium="image"/>
      <pubDate>Sun, 27 Aug 2023 14:53:23 GMT</pubDate>
    </item><item>
      <title><![CDATA[AD 跨界整合 - AD FS 服務安裝]]></title>
      <link>https://blog.darkthread.net/blog/adfs-setup/</link>
      <itunes:title><![CDATA[AD 跨界整合 - AD FS 服務安裝]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>最近頻頻聽到新名詞 - ADFS，腦中卻沒什麼概念，甚為惶恐，趕緊研究做點 Lab 壓壓驚。</p> <p>AD FS 是 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/ad-fs-overview?WT.mc_id=DOP-MVP-37580">Active Directory Federation Service</a> 的縮寫，用白話解釋，傳統印象中，原本只能企業內部限定 Windows 使用的 AD，透過 AD FS 可拓展到公司外部實現身分認證、權限控管、單一登入(Single Sign On, SSO)，讓員工、客戶、供應商使用 AD 帳號存取企業的對外服務，甚至一些第三方 SaaS 服務 (Teams、Office 365、Salesforce)。</p> <p>由於 AD FS 是走 HTTPS 協定並支援 OAuth 標準，原本受限封閉內網使用的 AD 帳號一下子變得開放，可拿來登入 Internet 網站、管控 WebAPI 存取、手機 App 登入、甚至 IoT 裝置。這篇筆記打算在實驗環境把它裝起來，後面幾篇會寫幾行程式，試試跨平台整合 AD 帳號登入。</p> <p>AD FS 的安裝細節有點繁瑣，術語名詞又多，只看官方文件的<a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/deployment/windows-server-2012-r2-ad-fs-deployment-guide?WT.mc_id=DOP-MVP-37580">部署指南</a>要完成有點吃力，我參考了不少文件、YouTube 影片(像是<a href="https://www.youtube.com/watch?v=exv-V462Ppw">這支</a>，體驗傳說中被 YouTube 上有口音的印度小哥拯救)，及 Pluralsight 的<a href="https://app.pluralsight.com/library/courses/implement-windows-server-2019-identity-federation-access-solutions/table-of-contents">教學課程</a>，才摸出頭緒。這個過程，絕對值得寫成筆記。</p> <p>安裝 AD FS 的第一個關卡是要搞定憑證。在建好 DC 主機、建立 AD 網域，備好另一台 AD FS 主機並加入網域後，在 DC <a href="/blog/setup-ad-n-ca/">安裝 CA 服務</a>，開啟 CA 管理介面，在 Certificate Templates 資料夾按右鍵選 Manage，然後在 Certificate Templates Console 找到 Web Server，以其為範本 Duplicate Template 複製修改出 ADFS 用的範本：</p> <p><img src="/Posts/files/2023/Fig1_638286585197893082.png"></p> <p>在 General 頁籤，設定名稱為 ADFS Template：</p> <p><img src="/Posts/files/2023/Fig2_638286585199851927.png"></p> <p>勾選「Allow private key to be exported」：</p> <p><img src="/Posts/files/2023/Fig3_638286585201808849.png"></p> <p>權限部分要讓 Domain Comupters 具有 Enroll 權限：</p> <p><img src="/Posts/files/2023/Fig4_638286585203657494.png"></p> <p>設定好儲存後，用右鍵選單 New / Certificate Template to Issue 將剛才建立的 ADFS Template 加入 Certificate Templates 資料夾：</p> <p><img src="/Posts/files/2023/Fig6_638286585205661825.png"></p> <p>到這裡 CA 準備好了。</p> <p>接著我們連到 AD FS 主機產生 <code>adfs.&lt;domain-name&gt;</code> 憑證。</p> <p>關於 AD FS 的 DNS 名稱，由於後面會用到子網域名稱(如 <code>certauth.adfs.&lt;domain-name&gt;</code>)，故 <code>adfs.&lt;domain-name&gt;</code> 要保留對外使用，AD FS 主機我們命名為 <code>adfs1.&lt;domain-name&gt;</code> 與之區隔，實驗環境只有單一主機，故在 DNS 加入一筆 A 記錄將 adfs 對映到 adfs1 的 IP：</p> <p><img src="/Posts/files/2023/FigL_638286585207629290.png"></p> <p>DNS 設好後，登入 AD FS 主機(記得要先加入網域)，執行 certlm.msc 開啟 Local Computer 的憑證管理介面，在 Personal / Certificates 右鍵 All Tasks / Request New Certificate 向 CA 申請憑證：</p> <p><img src="/Posts/files/2023/Fig7_638286585209545442.png"></p> <p>憑證註冊介面：</p> <p><img src="/Posts/files/2023/Fig8_638286585211375398.png"></p> <p><img src="/Posts/files/2023/Fig5_638286585213304385.png"></p> <p>ADFS Template 需要填寫 Common Name、DNS 等資料：</p> <p><img src="/Posts/files/2023/FigA_638286585215134256.png"></p> <p>建立 Common Name = <code>adfs.&lt;domain-name&gt;</code> 跟三筆 DNS <code>adfs.&lt;domain-name&gt;</code>、 <code>enterpriseregistration.&lt;domain-name&gt;</code>、 <code>certauth.adfs.&lt;domain-name&gt;</code></p> <p><img src="/Posts/files/2023/FigB_638286585217053322.png"></p> <p>取個 Friendly Name：</p> <p><img src="/Posts/files/2023/FigC_638286585218837568.png"></p> <p>程式會連上 CA 取得憑證並存入 Local Computer 憑證儲存區：</p> <p><img src="/Posts/files/2023/FigD_638286585220671876.png"></p> <p>再來，我們希望 AD FS 服務用 <a href="https://learn.microsoft.com/zh-tw/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview?WT.mc_id=DOP-MVP-37580">Group Managed Service Account</a> (gMSA，無密碼帳號，由作業系統自動管理密碼，解決惱人的密碼定期變更問題，很適合用於服務或排程，這個未來有機會再專門寫一篇為大家講解) 執行，準備工作是要登入 DC 建立 Key Distribution Service 的 Root Key。</p> <pre><code># 預設 10 小時後啟用(考量AD複寫時間)，測試環境將時間設定 10 小時前以便馬上使用
Add-KdsRootKey -EffectiveTime ((Get-Date).AddHours(-10))
</code></pre> <p>終於，前置作業完成，可以安裝 AD FS 了。Add Roles and Feature Wizards 新增完 AD FS 會提示需要設定：</p> <p><img src="/Posts/files/2023/FigE_638286585222794605.png"></p> <p>選擇這是 Web Farm 的第一台：</p> <p><img src="/Posts/files/2023/FigF_638286585224993060.png"></p> <p>提供具有管理者權限的網域帳號進行設定：</p> <p><img src="/Posts/files/2023/FigG_638286585226873114.png"></p> <p>選取剛才裝好的 SSL 憑證：</p> <p><img src="/Posts/files/2023/FigH_638286585228985227.png"></p> <p>執行身分選擇新建一個 gMSA 帳號：</p> <p><img src="/Posts/files/2023/FigI_638286585230906088.png"></p> <p>資料庫選擇 Windows Internal Database 即可(漏抓圖)，多台 AD FS 主機時可存在 SQL Server。執行前會再次確認所有設定：</p> <p><img src="/Posts/files/2023/FigJ_638286585232866312.png"></p> <p>由於我們剛建完 Root Key 不到 10-小時就要用 gMSA，隨時改了生效時間，但因建立時間太短會有警告(可忽略)：</p> <p><img src="/Posts/files/2023/FigK_638286585235222141.png"></p> <p>如果沒意外，AD FS 就裝好了。</p> <p>要檢測 AD FS 是否正常，最簡單的方法是使用 IdP Initiated Signon Page 測試，它預設未啟用，需使用以下 PowerShell 指令啟用：</p> <pre><code>Set-AdfsProperties -EnableIdPInitiatedSignonPage $true
</code></pre> <p><img src="/Posts/files/2023/FigN_638286585237033833.png"></p> <p>使用瀏覽器連上 <code>https://adfs.&lt;domain-name&gt;</code>，沒意外的話，你會看到登入畫面，按 Sign In 出現帳號密碼，試試若能 AD 帳號登入，成功!</p> <p><img src="/Posts/files/2023/FigM_638286585243683692.png"></p> <p data-slug="adfs-setup" data-title-en="AD FS notes - installation" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720">Notes of AD FS installation.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>最近頻頻聽到新名詞 - ADFS，腦中卻沒什麼概念，甚為惶恐，趕緊研究做點 Lab 壓壓驚。</p> <p>AD FS 是 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/ad-fs-overview?WT.mc_id=DOP-MVP-37580">Active Directory Federation Service</a> 的縮寫，用白話解釋，傳統印象中，原本只能企業內部限定 Windows 使用的 AD，透過 AD FS 可拓展到公司外部實現身分認證、權限控管、單一登入(Single Sign On, SSO)，讓員工、客戶、供應商使用 AD 帳號存取企業的對外服務，甚至一些第三方 SaaS 服務 (Teams、Office 365、Salesforce)。</p> <p>由於 AD FS 是走 HTTPS 協定並支援 OAuth 標準，原本受限封閉內網使用的 AD 帳號一下子變得開放，可拿來登入 Internet 網站、管控 WebAPI 存取、手機 App 登入、甚至 IoT 裝置。這篇筆記打算在實驗環境把它裝起來，後面幾篇會寫幾行程式，試試跨平台整合 AD 帳號登入。</p> <p>AD FS 的安裝細節有點繁瑣，術語名詞又多，只看官方文件的<a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/deployment/windows-server-2012-r2-ad-fs-deployment-guide?WT.mc_id=DOP-MVP-37580">部署指南</a>要完成有點吃力，我參考了不少文件、YouTube 影片(像是<a href="https://www.youtube.com/watch?v=exv-V462Ppw">這支</a>，體驗傳說中被 YouTube 上有口音的印度小哥拯救)，及 Pluralsight 的<a href="https://app.pluralsight.com/library/courses/implement-windows-server-2019-identity-federation-access-solutions/table-of-contents">教學課程</a>，才摸出頭緒。這個過程，絕對值得寫成筆記。</p> <p>安裝 AD FS 的第一個關卡是要搞定憑證。在建好 DC 主機、建立 AD 網域，備好另一台 AD FS 主機並加入網域後，在 DC <a href="/blog/setup-ad-n-ca/">安裝 CA 服務</a>，開啟 CA 管理介面，在 Certificate Templates 資料夾按右鍵選 Manage，然後在 Certificate Templates Console 找到 Web Server，以其為範本 Duplicate Template 複製修改出 ADFS 用的範本：</p> <p><img src="/Posts/files/2023/Fig1_638286585197893082.png"></p> <p>在 General 頁籤，設定名稱為 ADFS Template：</p> <p><img src="/Posts/files/2023/Fig2_638286585199851927.png"></p> <p>勾選「Allow private key to be exported」：</p> <p><img src="/Posts/files/2023/Fig3_638286585201808849.png"></p> <p>權限部分要讓 Domain Comupters 具有 Enroll 權限：</p> <p><img src="/Posts/files/2023/Fig4_638286585203657494.png"></p> <p>設定好儲存後，用右鍵選單 New / Certificate Template to Issue 將剛才建立的 ADFS Template 加入 Certificate Templates 資料夾：</p> <p><img src="/Posts/files/2023/Fig6_638286585205661825.png"></p> <p>到這裡 CA 準備好了。</p> <p>接著我們連到 AD FS 主機產生 <code>adfs.&lt;domain-name&gt;</code> 憑證。</p> <p>關於 AD FS 的 DNS 名稱，由於後面會用到子網域名稱(如 <code>certauth.adfs.&lt;domain-name&gt;</code>)，故 <code>adfs.&lt;domain-name&gt;</code> 要保留對外使用，AD FS 主機我們命名為 <code>adfs1.&lt;domain-name&gt;</code> 與之區隔，實驗環境只有單一主機，故在 DNS 加入一筆 A 記錄將 adfs 對映到 adfs1 的 IP：</p> <p><img src="/Posts/files/2023/FigL_638286585207629290.png"></p> <p>DNS 設好後，登入 AD FS 主機(記得要先加入網域)，執行 certlm.msc 開啟 Local Computer 的憑證管理介面，在 Personal / Certificates 右鍵 All Tasks / Request New Certificate 向 CA 申請憑證：</p> <p><img src="/Posts/files/2023/Fig7_638286585209545442.png"></p> <p>憑證註冊介面：</p> <p><img src="/Posts/files/2023/Fig8_638286585211375398.png"></p> <p><img src="/Posts/files/2023/Fig5_638286585213304385.png"></p> <p>ADFS Template 需要填寫 Common Name、DNS 等資料：</p> <p><img src="/Posts/files/2023/FigA_638286585215134256.png"></p> <p>建立 Common Name = <code>adfs.&lt;domain-name&gt;</code> 跟三筆 DNS <code>adfs.&lt;domain-name&gt;</code>、 <code>enterpriseregistration.&lt;domain-name&gt;</code>、 <code>certauth.adfs.&lt;domain-name&gt;</code></p> <p><img src="/Posts/files/2023/FigB_638286585217053322.png"></p> <p>取個 Friendly Name：</p> <p><img src="/Posts/files/2023/FigC_638286585218837568.png"></p> <p>程式會連上 CA 取得憑證並存入 Local Computer 憑證儲存區：</p> <p><img src="/Posts/files/2023/FigD_638286585220671876.png"></p> <p>再來，我們希望 AD FS 服務用 <a href="https://learn.microsoft.com/zh-tw/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview?WT.mc_id=DOP-MVP-37580">Group Managed Service Account</a> (gMSA，無密碼帳號，由作業系統自動管理密碼，解決惱人的密碼定期變更問題，很適合用於服務或排程，這個未來有機會再專門寫一篇為大家講解) 執行，準備工作是要登入 DC 建立 Key Distribution Service 的 Root Key。</p> <pre><code># 預設 10 小時後啟用(考量AD複寫時間)，測試環境將時間設定 10 小時前以便馬上使用
Add-KdsRootKey -EffectiveTime ((Get-Date).AddHours(-10))
</code></pre> <p>終於，前置作業完成，可以安裝 AD FS 了。Add Roles and Feature Wizards 新增完 AD FS 會提示需要設定：</p> <p><img src="/Posts/files/2023/FigE_638286585222794605.png"></p> <p>選擇這是 Web Farm 的第一台：</p> <p><img src="/Posts/files/2023/FigF_638286585224993060.png"></p> <p>提供具有管理者權限的網域帳號進行設定：</p> <p><img src="/Posts/files/2023/FigG_638286585226873114.png"></p> <p>選取剛才裝好的 SSL 憑證：</p> <p><img src="/Posts/files/2023/FigH_638286585228985227.png"></p> <p>執行身分選擇新建一個 gMSA 帳號：</p> <p><img src="/Posts/files/2023/FigI_638286585230906088.png"></p> <p>資料庫選擇 Windows Internal Database 即可(漏抓圖)，多台 AD FS 主機時可存在 SQL Server。執行前會再次確認所有設定：</p> <p><img src="/Posts/files/2023/FigJ_638286585232866312.png"></p> <p>由於我們剛建完 Root Key 不到 10-小時就要用 gMSA，隨時改了生效時間，但因建立時間太短會有警告(可忽略)：</p> <p><img src="/Posts/files/2023/FigK_638286585235222141.png"></p> <p>如果沒意外，AD FS 就裝好了。</p> <p>要檢測 AD FS 是否正常，最簡單的方法是使用 IdP Initiated Signon Page 測試，它預設未啟用，需使用以下 PowerShell 指令啟用：</p> <pre><code>Set-AdfsProperties -EnableIdPInitiatedSignonPage $true
</code></pre> <p><img src="/Posts/files/2023/FigN_638286585237033833.png"></p> <p>使用瀏覽器連上 <code>https://adfs.&lt;domain-name&gt;</code>，沒意外的話，你會看到登入畫面，按 Sign In 出現帳號密碼，試試若能 AD 帳號登入，成功!</p> <p><img src="/Posts/files/2023/FigM_638286585243683692.png"></p> <p data-slug="adfs-setup" data-title-en="AD FS notes - installation" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720">Notes of AD FS installation.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>最近頻頻聽到新名詞 - ADFS，腦中卻沒什麼概念，甚為惶恐，趕緊研究做點 Lab 壓壓驚。</p> <p>AD FS 是 <a href="https://learn.microsoft.com/zh-tw/windows-server/identity/ad-fs/ad-fs-overview?WT.mc_id=DOP-MVP-37580">Active Directory Federation Service</a> 的縮寫，用白話解釋，傳統印象中，原本只能企業內部限定 Windows 使用的 AD，透過 AD FS 可拓展到公司外部實現身分認證、權限控管、單一登入(Single Sign On, SSO)，讓員工、客戶、供應商使用 AD 帳號存取企業的對外服務，甚至一些第三方 SaaS 服務 (Teams、Office 365、Salesforce)。</p> <p>由於 AD FS 是走 HTTPS 協定並支援 OAuth 標準，原本受限封閉內網使用的 AD 帳號一下子變得開放，可拿來登入 Internet 網站、管控 WebAPI 存取、手機 App 登入、甚至 IoT 裝置。這篇筆記打算在實驗環境把它裝起來，後面幾篇會寫幾行程式，試試跨平台整合 AD 帳號登入。</p> <p>AD FS 的安裝細節有點繁瑣，術語名詞又多，只看官方文件的<a href="https://learn.microsoft.com/en-us/windows-server/identity/ad-fs/deployment/windows-server-2012-r2-ad-fs-deployment-guide?WT.mc_id=DOP-MVP-37580">部署指南</a>要完成有點吃力，我參考了不少文件、YouTube 影片(像是<a href="https://www.youtube.com/watch?v=exv-V462Ppw">這支</a>，體驗傳說中被 YouTube 上有口音的印度小哥拯救)，及 Pluralsight 的<a href="https://app.pluralsight.com/library/courses/implement-windows-server-2019-identity-federation-access-solutions/table-of-contents">教學課程</a>，才摸出頭緒。這個過程，絕對值得寫成筆記。</p> <p>安裝 AD FS 的第一個關卡是要搞定憑證。在建好 DC 主機、建立 AD 網域，備好另一台 AD FS 主機並加入網域後，在 DC <a href="/blog/setup-ad-n-ca/">安裝 CA 服務</a>，開啟 CA 管理介面，在 Certificate Templates 資料夾按右鍵選 Manage，然後在 Certificate Templates Console 找到 Web Server，以其為範本 Duplicate Template 複製修改出 ADFS 用的範本：</p> <p><img src="/Posts/files/2023/Fig1_638286585197893082.png"></p> <p>在 General 頁籤，設定名稱為 ADFS Template：</p> <p><img src="/Posts/files/2023/Fig2_638286585199851927.png"></p> <p>勾選「Allow private key to be exported」：</p> <p><img src="/Posts/files/2023/Fig3_638286585201808849.png"></p> <p>權限部分要讓 Domain Comupters 具有 Enroll 權限：</p> <p><img src="/Posts/files/2023/Fig4_638286585203657494.png"></p> <p>設定好儲存後，用右鍵選單 New / Certificate Template to Issue 將剛才建立的 ADFS Template 加入 Certificate Templates 資料夾：</p> <p><img src="/Posts/files/2023/Fig6_638286585205661825.png"></p> <p>到這裡 CA 準備好了。</p> <p>接著我們連到 AD FS 主機產生 <code>adfs.&lt;domain-name&gt;</code> 憑證。</p> <p>關於 AD FS 的 DNS 名稱，由於後面會用到子網域名稱(如 <code>certauth.adfs.&lt;domain-name&gt;</code>)，故 <code>adfs.&lt;domain-name&gt;</code> 要保留對外使用，AD FS 主機我們命名為 <code>adfs1.&lt;domain-name&gt;</code> 與之區隔，實驗環境只有單一主機，故在 DNS 加入一筆 A 記錄將 adfs 對映到 adfs1 的 IP：</p> <p><img src="/Posts/files/2023/FigL_638286585207629290.png"></p> <p>DNS 設好後，登入 AD FS 主機(記得要先加入網域)，執行 certlm.msc 開啟 Local Computer 的憑證管理介面，在 Personal / Certificates 右鍵 All Tasks / Request New Certificate 向 CA 申請憑證：</p> <p><img src="/Posts/files/2023/Fig7_638286585209545442.png"></p> <p>憑證註冊介面：</p> <p><img src="/Posts/files/2023/Fig8_638286585211375398.png"></p> <p><img src="/Posts/files/2023/Fig5_638286585213304385.png"></p> <p>ADFS Template 需要填寫 Common Name、DNS 等資料：</p> <p><img src="/Posts/files/2023/FigA_638286585215134256.png"></p> <p>建立 Common Name = <code>adfs.&lt;domain-name&gt;</code> 跟三筆 DNS <code>adfs.&lt;domain-name&gt;</code>、 <code>enterpriseregistration.&lt;domain-name&gt;</code>、 <code>certauth.adfs.&lt;domain-name&gt;</code></p> <p><img src="/Posts/files/2023/FigB_638286585217053322.png"></p> <p>取個 Friendly Name：</p> <p><img src="/Posts/files/2023/FigC_638286585218837568.png"></p> <p>程式會連上 CA 取得憑證並存入 Local Computer 憑證儲存區：</p> <p><img src="/Posts/files/2023/FigD_638286585220671876.png"></p> <p>再來，我們希望 AD FS 服務用 <a href="https://learn.microsoft.com/zh-tw/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview?WT.mc_id=DOP-MVP-37580">Group Managed Service Account</a> (gMSA，無密碼帳號，由作業系統自動管理密碼，解決惱人的密碼定期變更問題，很適合用於服務或排程，這個未來有機會再專門寫一篇為大家講解) 執行，準備工作是要登入 DC 建立 Key Distribution Service 的 Root Key。</p> <pre><code># 預設 10 小時後啟用(考量AD複寫時間)，測試環境將時間設定 10 小時前以便馬上使用
Add-KdsRootKey -EffectiveTime ((Get-Date).AddHours(-10))
</code></pre> <p>終於，前置作業完成，可以安裝 AD FS 了。Add Roles and Feature Wizards 新增完 AD FS 會提示需要設定：</p> <p><img src="/Posts/files/2023/FigE_638286585222794605.png"></p> <p>選擇這是 Web Farm 的第一台：</p> <p><img src="/Posts/files/2023/FigF_638286585224993060.png"></p> <p>提供具有管理者權限的網域帳號進行設定：</p> <p><img src="/Posts/files/2023/FigG_638286585226873114.png"></p> <p>選取剛才裝好的 SSL 憑證：</p> <p><img src="/Posts/files/2023/FigH_638286585228985227.png"></p> <p>執行身分選擇新建一個 gMSA 帳號：</p> <p><img src="/Posts/files/2023/FigI_638286585230906088.png"></p> <p>資料庫選擇 Windows Internal Database 即可(漏抓圖)，多台 AD FS 主機時可存在 SQL Server。執行前會再次確認所有設定：</p> <p><img src="/Posts/files/2023/FigJ_638286585232866312.png"></p> <p>由於我們剛建完 Root Key 不到 10-小時就要用 gMSA，隨時改了生效時間，但因建立時間太短會有警告(可忽略)：</p> <p><img src="/Posts/files/2023/FigK_638286585235222141.png"></p> <p>如果沒意外，AD FS 就裝好了。</p> <p>要檢測 AD FS 是否正常，最簡單的方法是使用 IdP Initiated Signon Page 測試，它預設未啟用，需使用以下 PowerShell 指令啟用：</p> <pre><code>Set-AdfsProperties -EnableIdPInitiatedSignonPage $true
</code></pre> <p><img src="/Posts/files/2023/FigN_638286585237033833.png"></p> <p>使用瀏覽器連上 <code>https://adfs.&lt;domain-name&gt;</code>，沒意外的話，你會看到登入畫面，按 Sign In 出現帳號密碼，試試若能 AD 帳號登入，成功!</p> <p><img src="/Posts/files/2023/FigM_638286585243683692.png"></p> <p data-slug="adfs-setup" data-title-en="AD FS notes - installation" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2019/09/23/13/11/iceberg-4498548_960_720.jpg#960#720">Notes of AD FS installation.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638286585197893082.png"/>
        <media:content url="/Posts/files/2023/Fig1_638286585197893082.png" medium="image"/>
      <pubDate>Sat, 26 Aug 2023 14:55:55 GMT</pubDate>
    </item><item>
      <title><![CDATA[【茶包射手雜記】Azure DevOps Server ElasticSearch 記憶體上限問題]]></title>
      <link>https://blog.darkthread.net/blog/elasticsearch-max-memory/</link>
      <itunes:title><![CDATA[【茶包射手雜記】Azure DevOps Server ElasticSearch 記憶體上限問題]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>幫忙看了一起 Azure DevOps Server 主機剩餘記憶體不足發出告警的案例。(註：Azure DevOps Server 就是 TFS Server 啦，TFS <a href="https://learn.microsoft.com/zh-tw/azure/devops/server/tfs-is-now-azure-devops-server?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">從 2019 版改名</a>叫 Azure DevOps Server，雖有 Azure 之名，但可安裝在地端)</p> <p>重新認識幾件事：</p> <ol><li>Azure DevOps Server/TFS 2018 的程式碼、Wiki 網頁及工作項目的搜尋功能是<a href="https://learn.microsoft.com/en-us/azure/devops/project/search/install-configure-search?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">靠 ElasticSearch 完成的</a></li> <li>ElasticSearch 是個資料庫服務，跟全天下的資料庫一樣，策略上會積極佔用實體記憶體提升效能(也沒錯啦，RAM 買了都買了，拿來當 Cache 加速才不會浪費)</li> <li>Azure DevOps Server 安裝時預設會依主機記憶體多寡，設定 ElasticSearch 使用量</li></ol> <p>案例是台 16G RAM Azure DevOps Server，運作一段時間後出現可用記憶體低於 10% 告警，看了一下，elasticsearch-service-x64.exe 吃掉 5GB 是最大宗，加上 SQL、IIS 都是記憶體大戶，加上 OS 的檔案 Cache 等，導致記憶體剩餘量過低。註：作業系統 Cache 如 Mapped File、Metafile 等理應會在記憶體吃緊時逐步釋出 (參考：<a href="/blog/where-have-all-the-memory-gone/">Windows記憶體都用到哪裡去了?</a>)，但警報一直響很困擾。</p> <p>對照我手上的 VM，ElasticSearch 吃了 2.8GB，沒到 5G 那麼誇張。想了一下，應該是我 VM 一開始只配了 8G RAM，後來發現不夠才調到 16G，因此配給 ElasticSearch 的記憶體較少。</p> <p><img src="/Posts/files/2023/Fig1_638285175103327613.png"></p> <p>爬文查到可使用 <code>C:\Program Files\Azure DevOps Server 2022\Search\ES\v7.17\bin\ealsticsearch-service.bat manager</code> 叫出管理 UI 查看及修改設定，如下圖，我的 VM Initial memory pool 及 Maximum memory pool 都是 2400 MB；而案例中的主機是 4800，剛好兩倍，推測與安裝時主機 RAM 大小成正比。</p> <p><img src="/Posts/files/2023/Fig2_638285175107170451.png"></p> <p>基於不想常收到告警跟 Azure DevOps Server 使用者不多、搜索需求不大，將案例主機的設定也調至 2400 解決問題。結案。</p> <p>補充：由於 ElasticSearch 記憶體用量上限不會自動調整，因效能不足加 RAM 後記得調設定。</p> <p>【參考資料】</p> <ul><li><a href="https://stackoverflow.com/a/42906451/288936">How to set memory limit to ElasticSearch in Windows? - stackoverflow</a></li> <li><a href="https://www.codewrecks.com/post/old/2018/01/increase-ram-for-elasticsearch-for-tfs-search/">Increase RAM for ElasticSearch for TFS Search - Codewrecks</a></li></ul> <p data-slug="elasticsearch-max-memory" data-fbimg="https://cdn.pixabay.com/photo/2014/12/28/02/23/mother-board-581597_960_720.jpg#960#720"> </p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>幫忙看了一起 Azure DevOps Server 主機剩餘記憶體不足發出告警的案例。(註：Azure DevOps Server 就是 TFS Server 啦，TFS <a href="https://learn.microsoft.com/zh-tw/azure/devops/server/tfs-is-now-azure-devops-server?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">從 2019 版改名</a>叫 Azure DevOps Server，雖有 Azure 之名，但可安裝在地端)</p> <p>重新認識幾件事：</p> <ol><li>Azure DevOps Server/TFS 2018 的程式碼、Wiki 網頁及工作項目的搜尋功能是<a href="https://learn.microsoft.com/en-us/azure/devops/project/search/install-configure-search?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">靠 ElasticSearch 完成的</a></li> <li>ElasticSearch 是個資料庫服務，跟全天下的資料庫一樣，策略上會積極佔用實體記憶體提升效能(也沒錯啦，RAM 買了都買了，拿來當 Cache 加速才不會浪費)</li> <li>Azure DevOps Server 安裝時預設會依主機記憶體多寡，設定 ElasticSearch 使用量</li></ol> <p>案例是台 16G RAM Azure DevOps Server，運作一段時間後出現可用記憶體低於 10% 告警，看了一下，elasticsearch-service-x64.exe 吃掉 5GB 是最大宗，加上 SQL、IIS 都是記憶體大戶，加上 OS 的檔案 Cache 等，導致記憶體剩餘量過低。註：作業系統 Cache 如 Mapped File、Metafile 等理應會在記憶體吃緊時逐步釋出 (參考：<a href="/blog/where-have-all-the-memory-gone/">Windows記憶體都用到哪裡去了?</a>)，但警報一直響很困擾。</p> <p>對照我手上的 VM，ElasticSearch 吃了 2.8GB，沒到 5G 那麼誇張。想了一下，應該是我 VM 一開始只配了 8G RAM，後來發現不夠才調到 16G，因此配給 ElasticSearch 的記憶體較少。</p> <p><img src="/Posts/files/2023/Fig1_638285175103327613.png"></p> <p>爬文查到可使用 <code>C:\Program Files\Azure DevOps Server 2022\Search\ES\v7.17\bin\ealsticsearch-service.bat manager</code> 叫出管理 UI 查看及修改設定，如下圖，我的 VM Initial memory pool 及 Maximum memory pool 都是 2400 MB；而案例中的主機是 4800，剛好兩倍，推測與安裝時主機 RAM 大小成正比。</p> <p><img src="/Posts/files/2023/Fig2_638285175107170451.png"></p> <p>基於不想常收到告警跟 Azure DevOps Server 使用者不多、搜索需求不大，將案例主機的設定也調至 2400 解決問題。結案。</p> <p>補充：由於 ElasticSearch 記憶體用量上限不會自動調整，因效能不足加 RAM 後記得調設定。</p> <p>【參考資料】</p> <ul><li><a href="https://stackoverflow.com/a/42906451/288936">How to set memory limit to ElasticSearch in Windows? - stackoverflow</a></li> <li><a href="https://www.codewrecks.com/post/old/2018/01/increase-ram-for-elasticsearch-for-tfs-search/">Increase RAM for ElasticSearch for TFS Search - Codewrecks</a></li></ul> <p data-slug="elasticsearch-max-memory" data-fbimg="https://cdn.pixabay.com/photo/2014/12/28/02/23/mother-board-581597_960_720.jpg#960#720"> </p>]]>
      </description>
      <content:encoded><![CDATA[<p>幫忙看了一起 Azure DevOps Server 主機剩餘記憶體不足發出告警的案例。(註：Azure DevOps Server 就是 TFS Server 啦，TFS <a href="https://learn.microsoft.com/zh-tw/azure/devops/server/tfs-is-now-azure-devops-server?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">從 2019 版改名</a>叫 Azure DevOps Server，雖有 Azure 之名，但可安裝在地端)</p> <p>重新認識幾件事：</p> <ol><li>Azure DevOps Server/TFS 2018 的程式碼、Wiki 網頁及工作項目的搜尋功能是<a href="https://learn.microsoft.com/en-us/azure/devops/project/search/install-configure-search?view=azure-devops&amp;WT.mc_id=DOP-MVP-37580">靠 ElasticSearch 完成的</a></li> <li>ElasticSearch 是個資料庫服務，跟全天下的資料庫一樣，策略上會積極佔用實體記憶體提升效能(也沒錯啦，RAM 買了都買了，拿來當 Cache 加速才不會浪費)</li> <li>Azure DevOps Server 安裝時預設會依主機記憶體多寡，設定 ElasticSearch 使用量</li></ol> <p>案例是台 16G RAM Azure DevOps Server，運作一段時間後出現可用記憶體低於 10% 告警，看了一下，elasticsearch-service-x64.exe 吃掉 5GB 是最大宗，加上 SQL、IIS 都是記憶體大戶，加上 OS 的檔案 Cache 等，導致記憶體剩餘量過低。註：作業系統 Cache 如 Mapped File、Metafile 等理應會在記憶體吃緊時逐步釋出 (參考：<a href="/blog/where-have-all-the-memory-gone/">Windows記憶體都用到哪裡去了?</a>)，但警報一直響很困擾。</p> <p>對照我手上的 VM，ElasticSearch 吃了 2.8GB，沒到 5G 那麼誇張。想了一下，應該是我 VM 一開始只配了 8G RAM，後來發現不夠才調到 16G，因此配給 ElasticSearch 的記憶體較少。</p> <p><img src="/Posts/files/2023/Fig1_638285175103327613.png"></p> <p>爬文查到可使用 <code>C:\Program Files\Azure DevOps Server 2022\Search\ES\v7.17\bin\ealsticsearch-service.bat manager</code> 叫出管理 UI 查看及修改設定，如下圖，我的 VM Initial memory pool 及 Maximum memory pool 都是 2400 MB；而案例中的主機是 4800，剛好兩倍，推測與安裝時主機 RAM 大小成正比。</p> <p><img src="/Posts/files/2023/Fig2_638285175107170451.png"></p> <p>基於不想常收到告警跟 Azure DevOps Server 使用者不多、搜索需求不大，將案例主機的設定也調至 2400 解決問題。結案。</p> <p>補充：由於 ElasticSearch 記憶體用量上限不會自動調整，因效能不足加 RAM 後記得調設定。</p> <p>【參考資料】</p> <ul><li><a href="https://stackoverflow.com/a/42906451/288936">How to set memory limit to ElasticSearch in Windows? - stackoverflow</a></li> <li><a href="https://www.codewrecks.com/post/old/2018/01/increase-ram-for-elasticsearch-for-tfs-search/">Increase RAM for ElasticSearch for TFS Search - Codewrecks</a></li></ul> <p data-slug="elasticsearch-max-memory" data-fbimg="https://cdn.pixabay.com/photo/2014/12/28/02/23/mother-board-581597_960_720.jpg#960#720"> </p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638285175103327613.png"/>
        <media:content url="/Posts/files/2023/Fig1_638285175103327613.png" medium="image"/>
      <pubDate>Thu, 24 Aug 2023 23:44:27 GMT</pubDate>
    </item><item>
      <title><![CDATA[在 CLI 簡易測量網頁回應時間]]></title>
      <link>https://blog.darkthread.net/blog/measure-web-speed-w-curl/</link>
      <itunes:title><![CDATA[在 CLI 簡易測量網頁回應時間]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>工作上有個小需求，想找到簡便方法顯示網頁回應速度，方便快速擷圖對照改善前後的時間差異。</p> <p>相關軟體很多，但我不想為此安裝程式，以既有工具優先；Chrome/Edge F12 開發者工具的網路功能也有每個 HTTP Request 的時間報表，是個不錯的選項：</p> <p><img src="/Posts/files/2023/Fig1_638283996358643493.png"></p> <p>不過，我想找到更簡單的 CLI 做法，貼指令按 Enter 直接看數字，還能在同一個畫面呈現多次測試的數據，感覺更有效率。</p> <p>遠在天邊近在眼前，爬文發現，curl 就能助我<a href="https://stackoverflow.com/a/22625150/288936">完美實現夢想</a>。(在 Windows 想使用 curl 可在 <a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 或 <a href="https://gitforwindows.org/">Git for Windows</a> 找到)</p> <p>curl 有個 --write-out/-w 參數，能在傳輸完成後顯示相關統計，其中包含幾個有用數據：<a href="https://curl.se/docs/manpage.html">參考</a></p> <ul><li>size_download 下載 Bytes 數</li> <li>speed_download 下載速度</li> <li>time_namelookup 從開始到完成 DNS 解析花費的秒數</li> <li>time_connect 從開始到到 TCP 連線建立花費的秒數</li> <li>time_appconnect 從開始到 TLS/SSL/SSH 等交握完成花費的秒數</li> <li>time_redirect 302/301 重導過程耗費的秒數(含 DNS 解析、TCP 連線... 等時間)</li> <li>time_pretransfer 從開始到傳完請求內容伺服器準備傳送回應花費的秒數</li> <li>time_starttransfer 從開始到傳回第一個 Byte 花費的秒數 (TTFB, <a href="https://en.wikipedia.org/wiki/Time_to_first_byte">Time to First Byte</a>)</li> <li>time_total 整個傳輸過程耗費的時間總和</li></ul> <p>由以上時間可以再推算出幾個效能相關數字：</p> <ul><li>time_appconnect - time_connec = TLS/SSL 交握、憑證檢核花費時間</li> <li>time_pretransfer - time_appconnect = 上傳請求內容耗費時間</li> <li>time_starttransfer - time_pretransfer = 伺服器端程式運算處理時間</li> <li>time_total - time_starttransfer = 傳完回應內容資料所耗費時間</li></ul> <p>依上述說明，我組裝了一個簡單的測試指令，用縮寫名稱表達各階段：dns = DNS 解析、tcp = TCP 連線建立、tls = TLS 交握完成、proc = 伺服器端開始處理、ttfb = 回傳第一個 Byte 的時間、totl = 總計時間、redir = 重新導向過程耗時</p> <pre><code>#　-w 指定輸出樣版、-o NUL (Linux/macOS 為 -o /dev/null) 將回傳結果導向黑洞
curl -w " dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download}" -o NUL -s https://www.google.com
# 或將輸出樣版存取檔案
echo  dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download} &gt; tmpl.txt
curl -w "@tmpl.txt" -o NUL -s https://www.google.com
</code></pre> <p>一行指令，輸入不同 URL 得到數字，完全不囉嗦，沒有多餘資訊，是我要的簡潔有力風格。</p> <p><img src="/Posts/files/2023/Fig2_638283996365309443.png"></p> <p>寫一支簡單的 ASPX，驗證它可以突顯不同階段的耗時狀況：</p> <pre><code>&lt;%@Page Language="C#"%&gt;
&lt;script runat="server"&gt;
void Page_Load(object sender, EventArgs e)
{
if (Request["delay"] == "Y")
System.Threading.Thread.Sleep(5000);
if (Request["10M"] == "Y")
for (int i = 0; i &lt; 10*1024 ; i++)
{
Response.Write(new string('A', 1024*1024));
Response.Flush();
}
else
Response.Write("OK");
}
&lt;/script&gt;
</code></pre> <p>實測可觀察到 delay=Y 時從 proc 到 ttfb 耗時五秒，10M=Y 時，執行時間很短，但從第一個 Byte (ttfb) 到全部傳完 (totl) 花了超過 23 秒。</p> <p><img src="/Posts/files/2023/Fig3_638283996376622638.png"></p> <p>最後來測試重導時間，<code>https://microsoft.com</code> 會歷經兩次重導來到 <code>https://www.microsoft.com/zh-tw/</code></p> <p><img src="/Posts/files/2023/Fig4_638283996378618621.png"></p> <p>curl 加上 -L 自動重導到目標，成功觀察到重導過程耗時：</p> <p><img src="/Posts/files/2023/Fig5_638283996380457417.png"></p> <p>工具箱再添順手工具一把。</p> <p data-slug="measure-web-speed-w-curl" data-title-en="Measuring web speed with curl" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/09/29/22/49/blueprint-964630_960_720.jpg#960#720">Tips of using curl to measure response time and transfer speed of web page.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>工作上有個小需求，想找到簡便方法顯示網頁回應速度，方便快速擷圖對照改善前後的時間差異。</p> <p>相關軟體很多，但我不想為此安裝程式，以既有工具優先；Chrome/Edge F12 開發者工具的網路功能也有每個 HTTP Request 的時間報表，是個不錯的選項：</p> <p><img src="/Posts/files/2023/Fig1_638283996358643493.png"></p> <p>不過，我想找到更簡單的 CLI 做法，貼指令按 Enter 直接看數字，還能在同一個畫面呈現多次測試的數據，感覺更有效率。</p> <p>遠在天邊近在眼前，爬文發現，curl 就能助我<a href="https://stackoverflow.com/a/22625150/288936">完美實現夢想</a>。(在 Windows 想使用 curl 可在 <a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 或 <a href="https://gitforwindows.org/">Git for Windows</a> 找到)</p> <p>curl 有個 --write-out/-w 參數，能在傳輸完成後顯示相關統計，其中包含幾個有用數據：<a href="https://curl.se/docs/manpage.html">參考</a></p> <ul><li>size_download 下載 Bytes 數</li> <li>speed_download 下載速度</li> <li>time_namelookup 從開始到完成 DNS 解析花費的秒數</li> <li>time_connect 從開始到到 TCP 連線建立花費的秒數</li> <li>time_appconnect 從開始到 TLS/SSL/SSH 等交握完成花費的秒數</li> <li>time_redirect 302/301 重導過程耗費的秒數(含 DNS 解析、TCP 連線... 等時間)</li> <li>time_pretransfer 從開始到傳完請求內容伺服器準備傳送回應花費的秒數</li> <li>time_starttransfer 從開始到傳回第一個 Byte 花費的秒數 (TTFB, <a href="https://en.wikipedia.org/wiki/Time_to_first_byte">Time to First Byte</a>)</li> <li>time_total 整個傳輸過程耗費的時間總和</li></ul> <p>由以上時間可以再推算出幾個效能相關數字：</p> <ul><li>time_appconnect - time_connec = TLS/SSL 交握、憑證檢核花費時間</li> <li>time_pretransfer - time_appconnect = 上傳請求內容耗費時間</li> <li>time_starttransfer - time_pretransfer = 伺服器端程式運算處理時間</li> <li>time_total - time_starttransfer = 傳完回應內容資料所耗費時間</li></ul> <p>依上述說明，我組裝了一個簡單的測試指令，用縮寫名稱表達各階段：dns = DNS 解析、tcp = TCP 連線建立、tls = TLS 交握完成、proc = 伺服器端開始處理、ttfb = 回傳第一個 Byte 的時間、totl = 總計時間、redir = 重新導向過程耗時</p> <pre><code>#　-w 指定輸出樣版、-o NUL (Linux/macOS 為 -o /dev/null) 將回傳結果導向黑洞
curl -w " dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download}" -o NUL -s https://www.google.com
# 或將輸出樣版存取檔案
echo  dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download} &gt; tmpl.txt
curl -w "@tmpl.txt" -o NUL -s https://www.google.com
</code></pre> <p>一行指令，輸入不同 URL 得到數字，完全不囉嗦，沒有多餘資訊，是我要的簡潔有力風格。</p> <p><img src="/Posts/files/2023/Fig2_638283996365309443.png"></p> <p>寫一支簡單的 ASPX，驗證它可以突顯不同階段的耗時狀況：</p> <pre><code>&lt;%@Page Language="C#"%&gt;
&lt;script runat="server"&gt;
void Page_Load(object sender, EventArgs e)
{
if (Request["delay"] == "Y")
System.Threading.Thread.Sleep(5000);
if (Request["10M"] == "Y")
for (int i = 0; i &lt; 10*1024 ; i++)
{
Response.Write(new string('A', 1024*1024));
Response.Flush();
}
else
Response.Write("OK");
}
&lt;/script&gt;
</code></pre> <p>實測可觀察到 delay=Y 時從 proc 到 ttfb 耗時五秒，10M=Y 時，執行時間很短，但從第一個 Byte (ttfb) 到全部傳完 (totl) 花了超過 23 秒。</p> <p><img src="/Posts/files/2023/Fig3_638283996376622638.png"></p> <p>最後來測試重導時間，<code>https://microsoft.com</code> 會歷經兩次重導來到 <code>https://www.microsoft.com/zh-tw/</code></p> <p><img src="/Posts/files/2023/Fig4_638283996378618621.png"></p> <p>curl 加上 -L 自動重導到目標，成功觀察到重導過程耗時：</p> <p><img src="/Posts/files/2023/Fig5_638283996380457417.png"></p> <p>工具箱再添順手工具一把。</p> <p data-slug="measure-web-speed-w-curl" data-title-en="Measuring web speed with curl" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/09/29/22/49/blueprint-964630_960_720.jpg#960#720">Tips of using curl to measure response time and transfer speed of web page.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>工作上有個小需求，想找到簡便方法顯示網頁回應速度，方便快速擷圖對照改善前後的時間差異。</p> <p>相關軟體很多，但我不想為此安裝程式，以既有工具優先；Chrome/Edge F12 開發者工具的網路功能也有每個 HTTP Request 的時間報表，是個不錯的選項：</p> <p><img src="/Posts/files/2023/Fig1_638283996358643493.png"></p> <p>不過，我想找到更簡單的 CLI 做法，貼指令按 Enter 直接看數字，還能在同一個畫面呈現多次測試的數據，感覺更有效率。</p> <p>遠在天邊近在眼前，爬文發現，curl 就能助我<a href="https://stackoverflow.com/a/22625150/288936">完美實現夢想</a>。(在 Windows 想使用 curl 可在 <a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 或 <a href="https://gitforwindows.org/">Git for Windows</a> 找到)</p> <p>curl 有個 --write-out/-w 參數，能在傳輸完成後顯示相關統計，其中包含幾個有用數據：<a href="https://curl.se/docs/manpage.html">參考</a></p> <ul><li>size_download 下載 Bytes 數</li> <li>speed_download 下載速度</li> <li>time_namelookup 從開始到完成 DNS 解析花費的秒數</li> <li>time_connect 從開始到到 TCP 連線建立花費的秒數</li> <li>time_appconnect 從開始到 TLS/SSL/SSH 等交握完成花費的秒數</li> <li>time_redirect 302/301 重導過程耗費的秒數(含 DNS 解析、TCP 連線... 等時間)</li> <li>time_pretransfer 從開始到傳完請求內容伺服器準備傳送回應花費的秒數</li> <li>time_starttransfer 從開始到傳回第一個 Byte 花費的秒數 (TTFB, <a href="https://en.wikipedia.org/wiki/Time_to_first_byte">Time to First Byte</a>)</li> <li>time_total 整個傳輸過程耗費的時間總和</li></ul> <p>由以上時間可以再推算出幾個效能相關數字：</p> <ul><li>time_appconnect - time_connec = TLS/SSL 交握、憑證檢核花費時間</li> <li>time_pretransfer - time_appconnect = 上傳請求內容耗費時間</li> <li>time_starttransfer - time_pretransfer = 伺服器端程式運算處理時間</li> <li>time_total - time_starttransfer = 傳完回應內容資料所耗費時間</li></ul> <p>依上述說明，我組裝了一個簡單的測試指令，用縮寫名稱表達各階段：dns = DNS 解析、tcp = TCP 連線建立、tls = TLS 交握完成、proc = 伺服器端開始處理、ttfb = 回傳第一個 Byte 的時間、totl = 總計時間、redir = 重新導向過程耗時</p> <pre><code>#　-w 指定輸出樣版、-o NUL (Linux/macOS 為 -o /dev/null) 將回傳結果導向黑洞
curl -w " dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download}" -o NUL -s https://www.google.com
# 或將輸出樣版存取檔案
echo  dns: %{time_namelookup}  tcp: %{time_connect}  tls: %{time_appconnect}  redir: %{time_redirect}
proc: %{time_pretransfer} ttfb: %{time_starttransfer} totl: %{time_total}   size: %{size_download} &gt; tmpl.txt
curl -w "@tmpl.txt" -o NUL -s https://www.google.com
</code></pre> <p>一行指令，輸入不同 URL 得到數字，完全不囉嗦，沒有多餘資訊，是我要的簡潔有力風格。</p> <p><img src="/Posts/files/2023/Fig2_638283996365309443.png"></p> <p>寫一支簡單的 ASPX，驗證它可以突顯不同階段的耗時狀況：</p> <pre><code>&lt;%@Page Language="C#"%&gt;
&lt;script runat="server"&gt;
void Page_Load(object sender, EventArgs e)
{
if (Request["delay"] == "Y")
System.Threading.Thread.Sleep(5000);
if (Request["10M"] == "Y")
for (int i = 0; i &lt; 10*1024 ; i++)
{
Response.Write(new string('A', 1024*1024));
Response.Flush();
}
else
Response.Write("OK");
}
&lt;/script&gt;
</code></pre> <p>實測可觀察到 delay=Y 時從 proc 到 ttfb 耗時五秒，10M=Y 時，執行時間很短，但從第一個 Byte (ttfb) 到全部傳完 (totl) 花了超過 23 秒。</p> <p><img src="/Posts/files/2023/Fig3_638283996376622638.png"></p> <p>最後來測試重導時間，<code>https://microsoft.com</code> 會歷經兩次重導來到 <code>https://www.microsoft.com/zh-tw/</code></p> <p><img src="/Posts/files/2023/Fig4_638283996378618621.png"></p> <p>curl 加上 -L 自動重導到目標，成功觀察到重導過程耗時：</p> <p><img src="/Posts/files/2023/Fig5_638283996380457417.png"></p> <p>工具箱再添順手工具一把。</p> <p data-slug="measure-web-speed-w-curl" data-title-en="Measuring web speed with curl" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/09/29/22/49/blueprint-964630_960_720.jpg#960#720">Tips of using curl to measure response time and transfer speed of web page.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638283996358643493.png"/>
        <media:content url="/Posts/files/2023/Fig1_638283996358643493.png" medium="image"/>
      <pubDate>Wed, 23 Aug 2023 14:52:38 GMT</pubDate>
    </item><item>
      <title><![CDATA[再探 Windows 檔名不分大小寫陷阱 - .NET 處理對策]]></title>
      <link>https://blog.darkthread.net/blog/dotnet-filename-case-issue/</link>
      <itunes:title><![CDATA[再探 Windows 檔名不分大小寫陷阱 - .NET 處理對策]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>先幫沒踩過的同學補充這顆小地雷 - <a href="/blog/win-ci-filename-behavior/">Windows 檔名不分大小寫阱陷</a>。</p> <p>我們都知道 Windows 的檔案系統不區分大小寫，對 Windows 來說，Logo_TW.png 跟 Logo_tw.png 是同一個檔案。因此，你無法在檔案總管將 Logo_TW.png 更名成 Logo_tw.png、logo_tw.png、LOGO_TW.PNG。(如下圖)</p> <p><img src="/Posts/files/Fig1_637599657193586173.gif"></p> <p>一般來說，檔名沒精準調成指定大小寫組合問題不大，反正字元一致便能找對檔案，大小寫不同無所謂。但如果你將檔名拉到 .NET 進行比對，大小寫不一致就會是個要處理的問題。(另一個類似情境是 SQL WHERE 比對時會忽略字尾空白，將欄位讀到 .NET 再比較，結尾空白會導致在 SQL 相等的資料在 .NET 世界不相等，有處理過 CHAR()/NCHAR() 型別的老鳥應該知道讀完先 Trim() 的 SOP)</p> <p>今天再遇到類以的檔案同步情境，又踩到檔名只差在大小寫不同無法覆寫一致檔名的問題。這回我想改變戰略 - 既然檔名由系統全權控制，何不一開始就讓檔名完全一致，而不是每次要比對時都忽略大小寫，感覺更合理有效率。</p> <p>研究了一下，解法出奇簡單，File.WriteAllText()/WriteAllBytes() 遇到只有大小寫不同的同樣檔名會維持舊檔名，但用 FileInfo.Move() 或 File.Move() 即可強制換成指定的大小寫組合，使用以下範例驗證：</p> <pre><code>Action chkFileName = () =&gt; {
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(Directory.GetFiles(".", "*.txt").First());
};
Action&lt;string&gt; printTitle = (msg) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine(msg);
};
Directory.SetCurrentDirectory("D:\\");
var fileName = "test.txt";
if (File.Exists(fileName))
{
File.Delete(fileName);
}
printTitle($"Write {fileName}");
File.WriteAllText(fileName, "Hello World");
chkFileName();
var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
chkFileName();
printTitle($"FileInfo.MoveTo() {upCaseFileName}");
FileInfo fi = new FileInfo(fileName);
fi.MoveTo(upCaseFileName);
chkFileName();
printTitle($"File.Move() {upCaseFileName} to {fileName}");
File.Move(upCaseFileName, fileName);
chkFileName();
Console.ResetColor();
</code></pre> <p>如下圖，File.WriteAll("TEST.TXT", ...) 不會將 "test.txt" 檔名換成 "TEST.TXT"，但 FileInfo.MoveTo()、File.Move() 可以：</p> <p><img src="/Posts/files/2023/Fig1_638283110904967107.png"></p> <p>掌握這點，我們只需在每次寫入檔案後，檢查檔名是否不一致，若不相同就用 FileInof.MoveTo() 更名，搞定收工。</p> <pre><code>var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
// 檢查檔案名稱，若大小寫不同，使用 MoveTo 更名
var fi = Directory.GetFiles(
Path.GetDirectoryName(Path.GetFullPath(upCaseFileName)),
Path.GetFileName(upCaseFileName))
.Select(x =&gt; new FileInfo(x)).FirstOrDefault();
if (fi != null &amp;&amp; fi.Name != Path.GetFileName(upCaseFileName))
{
fi.MoveTo(upCaseFileName);
}
chkFileName();
</code></pre> <p><img src="/Posts/files/2023/Fig2_638283110906941425.png"></p> <p>【同場加映】利用 <code>\\?\</code> 前置詞(延伸閱讀：<a href="/blog/filename-endswith-dot/">冷知識 - 刪不掉的 Windows 檔案</a>)強制更名大小寫的小技巧：</p> <p><img src="/Posts/files/2023/Fig3_638283135917766530.png"></p> <p data-slug="dotnet-filename-case-issue" data-title-en="Set exactly letter case of filename in .NET" data-catg=".NET" data-fbimg="https://i.imgur.com/FruOR6H.jpg#640#427">The file system of Windows always ignore file name letter case sometimes cause issue, this article provide a easy way to fix it.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>先幫沒踩過的同學補充這顆小地雷 - <a href="/blog/win-ci-filename-behavior/">Windows 檔名不分大小寫阱陷</a>。</p> <p>我們都知道 Windows 的檔案系統不區分大小寫，對 Windows 來說，Logo_TW.png 跟 Logo_tw.png 是同一個檔案。因此，你無法在檔案總管將 Logo_TW.png 更名成 Logo_tw.png、logo_tw.png、LOGO_TW.PNG。(如下圖)</p> <p><img src="/Posts/files/Fig1_637599657193586173.gif"></p> <p>一般來說，檔名沒精準調成指定大小寫組合問題不大，反正字元一致便能找對檔案，大小寫不同無所謂。但如果你將檔名拉到 .NET 進行比對，大小寫不一致就會是個要處理的問題。(另一個類似情境是 SQL WHERE 比對時會忽略字尾空白，將欄位讀到 .NET 再比較，結尾空白會導致在 SQL 相等的資料在 .NET 世界不相等，有處理過 CHAR()/NCHAR() 型別的老鳥應該知道讀完先 Trim() 的 SOP)</p> <p>今天再遇到類以的檔案同步情境，又踩到檔名只差在大小寫不同無法覆寫一致檔名的問題。這回我想改變戰略 - 既然檔名由系統全權控制，何不一開始就讓檔名完全一致，而不是每次要比對時都忽略大小寫，感覺更合理有效率。</p> <p>研究了一下，解法出奇簡單，File.WriteAllText()/WriteAllBytes() 遇到只有大小寫不同的同樣檔名會維持舊檔名，但用 FileInfo.Move() 或 File.Move() 即可強制換成指定的大小寫組合，使用以下範例驗證：</p> <pre><code>Action chkFileName = () =&gt; {
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(Directory.GetFiles(".", "*.txt").First());
};
Action&lt;string&gt; printTitle = (msg) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine(msg);
};
Directory.SetCurrentDirectory("D:\\");
var fileName = "test.txt";
if (File.Exists(fileName))
{
File.Delete(fileName);
}
printTitle($"Write {fileName}");
File.WriteAllText(fileName, "Hello World");
chkFileName();
var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
chkFileName();
printTitle($"FileInfo.MoveTo() {upCaseFileName}");
FileInfo fi = new FileInfo(fileName);
fi.MoveTo(upCaseFileName);
chkFileName();
printTitle($"File.Move() {upCaseFileName} to {fileName}");
File.Move(upCaseFileName, fileName);
chkFileName();
Console.ResetColor();
</code></pre> <p>如下圖，File.WriteAll("TEST.TXT", ...) 不會將 "test.txt" 檔名換成 "TEST.TXT"，但 FileInfo.MoveTo()、File.Move() 可以：</p> <p><img src="/Posts/files/2023/Fig1_638283110904967107.png"></p> <p>掌握這點，我們只需在每次寫入檔案後，檢查檔名是否不一致，若不相同就用 FileInof.MoveTo() 更名，搞定收工。</p> <pre><code>var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
// 檢查檔案名稱，若大小寫不同，使用 MoveTo 更名
var fi = Directory.GetFiles(
Path.GetDirectoryName(Path.GetFullPath(upCaseFileName)),
Path.GetFileName(upCaseFileName))
.Select(x =&gt; new FileInfo(x)).FirstOrDefault();
if (fi != null &amp;&amp; fi.Name != Path.GetFileName(upCaseFileName))
{
fi.MoveTo(upCaseFileName);
}
chkFileName();
</code></pre> <p><img src="/Posts/files/2023/Fig2_638283110906941425.png"></p> <p>【同場加映】利用 <code>\\?\</code> 前置詞(延伸閱讀：<a href="/blog/filename-endswith-dot/">冷知識 - 刪不掉的 Windows 檔案</a>)強制更名大小寫的小技巧：</p> <p><img src="/Posts/files/2023/Fig3_638283135917766530.png"></p> <p data-slug="dotnet-filename-case-issue" data-title-en="Set exactly letter case of filename in .NET" data-catg=".NET" data-fbimg="https://i.imgur.com/FruOR6H.jpg#640#427">The file system of Windows always ignore file name letter case sometimes cause issue, this article provide a easy way to fix it.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>先幫沒踩過的同學補充這顆小地雷 - <a href="/blog/win-ci-filename-behavior/">Windows 檔名不分大小寫阱陷</a>。</p> <p>我們都知道 Windows 的檔案系統不區分大小寫，對 Windows 來說，Logo_TW.png 跟 Logo_tw.png 是同一個檔案。因此，你無法在檔案總管將 Logo_TW.png 更名成 Logo_tw.png、logo_tw.png、LOGO_TW.PNG。(如下圖)</p> <p><img src="/Posts/files/Fig1_637599657193586173.gif"></p> <p>一般來說，檔名沒精準調成指定大小寫組合問題不大，反正字元一致便能找對檔案，大小寫不同無所謂。但如果你將檔名拉到 .NET 進行比對，大小寫不一致就會是個要處理的問題。(另一個類似情境是 SQL WHERE 比對時會忽略字尾空白，將欄位讀到 .NET 再比較，結尾空白會導致在 SQL 相等的資料在 .NET 世界不相等，有處理過 CHAR()/NCHAR() 型別的老鳥應該知道讀完先 Trim() 的 SOP)</p> <p>今天再遇到類以的檔案同步情境，又踩到檔名只差在大小寫不同無法覆寫一致檔名的問題。這回我想改變戰略 - 既然檔名由系統全權控制，何不一開始就讓檔名完全一致，而不是每次要比對時都忽略大小寫，感覺更合理有效率。</p> <p>研究了一下，解法出奇簡單，File.WriteAllText()/WriteAllBytes() 遇到只有大小寫不同的同樣檔名會維持舊檔名，但用 FileInfo.Move() 或 File.Move() 即可強制換成指定的大小寫組合，使用以下範例驗證：</p> <pre><code>Action chkFileName = () =&gt; {
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(Directory.GetFiles(".", "*.txt").First());
};
Action&lt;string&gt; printTitle = (msg) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine(msg);
};
Directory.SetCurrentDirectory("D:\\");
var fileName = "test.txt";
if (File.Exists(fileName))
{
File.Delete(fileName);
}
printTitle($"Write {fileName}");
File.WriteAllText(fileName, "Hello World");
chkFileName();
var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
chkFileName();
printTitle($"FileInfo.MoveTo() {upCaseFileName}");
FileInfo fi = new FileInfo(fileName);
fi.MoveTo(upCaseFileName);
chkFileName();
printTitle($"File.Move() {upCaseFileName} to {fileName}");
File.Move(upCaseFileName, fileName);
chkFileName();
Console.ResetColor();
</code></pre> <p>如下圖，File.WriteAll("TEST.TXT", ...) 不會將 "test.txt" 檔名換成 "TEST.TXT"，但 FileInfo.MoveTo()、File.Move() 可以：</p> <p><img src="/Posts/files/2023/Fig1_638283110904967107.png"></p> <p>掌握這點，我們只需在每次寫入檔案後，檢查檔名是否不一致，若不相同就用 FileInof.MoveTo() 更名，搞定收工。</p> <pre><code>var upCaseFileName = fileName.ToUpper();
printTitle($"Write {upCaseFileName}");
File.WriteAllText(upCaseFileName, "Hello World");
// 檢查檔案名稱，若大小寫不同，使用 MoveTo 更名
var fi = Directory.GetFiles(
Path.GetDirectoryName(Path.GetFullPath(upCaseFileName)),
Path.GetFileName(upCaseFileName))
.Select(x =&gt; new FileInfo(x)).FirstOrDefault();
if (fi != null &amp;&amp; fi.Name != Path.GetFileName(upCaseFileName))
{
fi.MoveTo(upCaseFileName);
}
chkFileName();
</code></pre> <p><img src="/Posts/files/2023/Fig2_638283110906941425.png"></p> <p>【同場加映】利用 <code>\\?\</code> 前置詞(延伸閱讀：<a href="/blog/filename-endswith-dot/">冷知識 - 刪不掉的 Windows 檔案</a>)強制更名大小寫的小技巧：</p> <p><img src="/Posts/files/2023/Fig3_638283135917766530.png"></p> <p data-slug="dotnet-filename-case-issue" data-title-en="Set exactly letter case of filename in .NET" data-catg=".NET" data-fbimg="https://i.imgur.com/FruOR6H.jpg#640#427">The file system of Windows always ignore file name letter case sometimes cause issue, this article provide a easy way to fix it.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/Fig1_637599657193586173.gif"/>
        <media:content url="/Posts/files/Fig1_637599657193586173.gif" medium="image"/>
      <pubDate>Tue, 22 Aug 2023 14:23:08 GMT</pubDate>
    </item><item>
      <title><![CDATA[讀書筆記 - LINE BOT 開發實戰]]></title>
      <link>https://blog.darkthread.net/blog/line-bot-sdk-notes/</link>
      <itunes:title><![CDATA[讀書筆記 - LINE BOT 開發實戰]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>週末啃完 David 老師「LINE BOT 與人工智慧辨別開發實戰」一書(再一次<a href="/blog/chatgpt-console-chat/">慢半拍</a>，哈)，依慣例筆記備忘。</p> <p>先前<a href="/blog/aspnetcore-heroku-postgresql/">寫過 LINE 機器人</a>，平日也依賴 <a href="/blog/linenotify-linelogin-tips/">LINE Notify 發系統通報</a>，嚴格來說我不算 LINE 程式開發生手。但相較於程式用到什麼查什麼學什麼的遊擊式學習，書籍課程提供較完整的知識覆蓋面，讓你知道面對某個需求時有哪些可用選項，比起查到哪一篇範例就學哪一種寫法的福袋(盲盒)式開發，更能有效率地精巧解決問題。雖然 Google 、ChatGPT 跟 <a href="/blog/github-copilot-demo/">Copilot</a> 很方便，想要功力大增，一本好書或一門課程總能讓你事半功倍。所以，網路時代還是要讀書呀~</p> <ul><li>Line Message API 申請重點：建立公司和個人 Provider，取得 Channel Access Token 及 User ID，透過掃瞄 QR Code 將機器人加為好友</li> <li>格式化呈現選項讓使用者操作 - Template Message，分為 Buttons Template, Confirm Template, Carosul Template (最多十個）</li> <li>Actions 為使用者可點擊的選項，有以下形式：Post Action (觸發Webhook), Message Action (代使用者送出一句話）, Uri Action (開網頁）</li> <li>Template Message 在 PC 版會顯示公版不支援行動提示，可用 altText 自訂提示</li> <li>Carousel Template 直接用一張圖片當選項按鈕</li> <li>Quick Reply 文字輸入區上方一排小按鈕供使用者選取</li> <li>Flex Message 用 JSON 自訂訊息排版樣式。有 Flex Message Simulator 工具預覽 JSON 定義呈現效果</li> <li>LINE Bot 管理後台 <a href="https://developer.line.biz">https://developer.line.biz</a></li> <li>LIFF App (LINE Front-end Framework) 嵌入 HTML，呼叫 <code>https://api.line.me/liff/v1/app</code> 傳入 URL 得到 ID，寫成 line://app/11111111111-oxoxoxoxox 可在 LINE 開啟</li> <li>LIFF 網頁載入 <code>https://d.line-scdn.net/liff/1.0/sdk.js</code> 可用 JavaScript 取得使用者身分、送訊息等 <pre><code>liff.init((data) =&gt; {
data.context.userId; //使用者 ID
liff.sendMessage([
{ type:'text', text: 'message' }
]).then(() =&gt; { ... });
});
</code></pre> </li> <li>.NET 開發 LINE 機器人可使用 NuGet 安裝 <a href="https://www.nuget.org/packages/LineBotSDK">LineBotSDK</a> 簡化開發 (以下簡稱 SDK)</li> <li>SDK 有提供方法建立及列舉 LIFF 連結，另外也可使用 LINE 管理後台操作</li> <li>WebHook 以網站形式接受 LINE 呼叫，做出回應</li> <li>Push API 要錢，可主動送訊息給 User Id, Reply API 免費，用 ReplyToken 回覆使用者發言，時效很短 (註：約 30 秒)</li> <li>WebHook 收到 ReceivedMessage events 一般只有一筆，但高負載情境下可能多筆。每個event可能來自不同使用者，想像成多個包裹一次送到</li> <li>event.source.userId 為使用者識別，SDK 有 GetUserInfo() 可取姓名、大頭照、狀態文字等</li> <li>event.text 訊息內容 event.packageId stickerId 貼圖資訊</li> <li>API 也可送貼圖，package ID/sticker ID 對照表可查 <a href="https://developers.line.biz/en/docs/messaging-api/sticker-list/">List of available stickers</a> (書上的 PDF 網址已失效)</li> <li>訊息也可能是GPS座標，由 address, latitude, longitude 取值</li> <li>訊息型別包含 Text, Sticker, Image, Video, Audio, File, Location</li> <li>event 類別有 <ul><li>Follow 加好友</li> <li>Unfollow 封鎖</li> <li>Join 被加入聊天室</li> <li>Leave 離開聊天室</li> <li>Postback 收到 Postback 訊息 (來自 Template Message，new isRock.LineBot.PostbackAction()</li> <li>Beacon 收到 Beacon 訊號</li></ul> </li> <li>聊天室有兩種：隨意邀請人建立 - Room、透過選單建立群組 - Group</li> <li>每個群組只能有一個Bot, Bot 不知被誰加，可識別目前說話者（不限好友）, 可主動退出<br> event.source.type==room/group, event.source.roomId/.groupId &lt;= 傳入 PushMessage 參數主動對群組發話</li> <li>抓傳來的圖檔、聲音用 SDK GetUserUploadedContent(message.id,...) 取得內容</li> <li>可繼承 SDK LineWebHookControllerBase 實作 ApiController。免自己解析 JSON，由 ReceivedMessage 取得訊息內容</li> <li>ngrok 將 localhost 特定 Port 轉接到 Internet 的 HTTPS，方便用 VS 逐行偵錯<br> 註：也可用 <a href="/blog/ssh-tunnel-as-ngrok/">SSH Tunneling 或 Cloudflare Tunnel</a></li> <li>防止有人假冒 LINE 服務呼叫你的 WebHook - 檢查 Channel Secret (X-Line-Signature HTTP Header) / isRock.LineBot.Utility.SignatureValidation(this);</li> <li>免費通知服務 LINE Notify Service，申請後得到 Client ID、Client Secret</li> <li>允許發 LINE Nontify 給使用者需經 OAuth 驗證：引導頁轉到 LINE 官方 OAuth，確認身分並同意授權後導回我們網取並附上 code (還有自訂的 state，一般用來識別使用者是誰)，後端傳 code 給 API 取得 Token 儲存，以後用此 Token 傳訊給該使用者</li> <li>Rich Menu 是官方帳號常見下方兩排六格的快捷選單(有 3+3, 2x2, 1+3, 1+2, 1+1, 滿版等版型)</li> <li>LUIS - Language Understanding Intelligent Services, 理解輸入語句，歸納出意圖(Intent)，並抓出關鍵 Entities (在 ChatGPT 等 LLM AI 出現後，LUIS 的學習價值待重新評估)</li> <li>Azure Cognitive Service：Computer Vision API、Translater API... 等章節，基於 AI 剛邁向新時代，這部分也先擱著。</li></ul> <p>最後，用一個簡單練習結束這回合。ASP.NET Core <a href="/blog/minimal-api/">Minimal API</a> 搭配 LineBotSDK，寫一個接收使用者傳送文字及傳回 TemplateMessage 的小範例：</p> <p><img src="/Posts/files/2023/Fig1_638282222859676878.jpg"></p> <p>程式碼如下，整支程式 50 行 C# 搞定：</p> <pre><code>using isRock.LineBot;
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();
var chanAccToken = 取得被安全儲存的ChannelAccessToken();
var bot = new isRock.LineBot.Bot(chanAccToken);
app.MapGet("/", () =&gt; "Hello World!");
app.MapPost("/WebHook", async (HttpRequest request) =&gt;
{
using var sr = new StreamReader(request.Body);
var reqBody = await sr.ReadToEndAsync();
var rcvMsg = isRock.LineBot.Utility.Parsing(reqBody);
foreach (var e in rcvMsg.events)
{
if (!string.IsNullOrEmpty(e.message?.text))
{
var replyToken = e.replyToken;
var userId = e.source.userId;
var userName = bot.GetUserInfo(userId).displayName;
var userMsg = e.message.text;
if (userMsg == "/buttons")
{
var btnTmpl = new isRock.LineBot.ButtonsTemplate()
{
altText = "請用手機檢視",
text = "請選擇",
title = "敲指令管 Azure，你選哪一種？",
thumbnailImageUrl = new Uri("... Fig2_638130550931190042.png"),
actions = new List&lt;isRock.LineBot.TemplateActionBase&gt;() {
new isRock.LineBot.MessageAction() {
label = "Azure CLI",
text = "Azure CLI"
},
new isRock.LineBot.MessageAction() {
label = "Azure PowerShell",
text = "Azure PowerShell"
},
new isRock.LineBot.UriAction() {
label = "看文章",
uri = new Uri("https://blog.darkthread.net/blog/azure-cli-or-azure-powershell/")
}
}
};
bot.ReplyMessage(replyToken, new TemplateMessage(btnTmpl));
}
else
bot.ReplyMessage(replyToken, $"[記錄]{userName}說：{userMsg}");
}
}
return Results.Ok();
});
app.Run();
</code></pre> <p data-slug="line-bot-sdk-notes" data-title-en="LineBotSDK notes" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2017/11/13/16/26/snail-2946075_1280.jpg#1280#853">Notes about LineBotSDK and a ASP.NET Core Minimal API example to response message from LINE.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>週末啃完 David 老師「LINE BOT 與人工智慧辨別開發實戰」一書(再一次<a href="/blog/chatgpt-console-chat/">慢半拍</a>，哈)，依慣例筆記備忘。</p> <p>先前<a href="/blog/aspnetcore-heroku-postgresql/">寫過 LINE 機器人</a>，平日也依賴 <a href="/blog/linenotify-linelogin-tips/">LINE Notify 發系統通報</a>，嚴格來說我不算 LINE 程式開發生手。但相較於程式用到什麼查什麼學什麼的遊擊式學習，書籍課程提供較完整的知識覆蓋面，讓你知道面對某個需求時有哪些可用選項，比起查到哪一篇範例就學哪一種寫法的福袋(盲盒)式開發，更能有效率地精巧解決問題。雖然 Google 、ChatGPT 跟 <a href="/blog/github-copilot-demo/">Copilot</a> 很方便，想要功力大增，一本好書或一門課程總能讓你事半功倍。所以，網路時代還是要讀書呀~</p> <ul><li>Line Message API 申請重點：建立公司和個人 Provider，取得 Channel Access Token 及 User ID，透過掃瞄 QR Code 將機器人加為好友</li> <li>格式化呈現選項讓使用者操作 - Template Message，分為 Buttons Template, Confirm Template, Carosul Template (最多十個）</li> <li>Actions 為使用者可點擊的選項，有以下形式：Post Action (觸發Webhook), Message Action (代使用者送出一句話）, Uri Action (開網頁）</li> <li>Template Message 在 PC 版會顯示公版不支援行動提示，可用 altText 自訂提示</li> <li>Carousel Template 直接用一張圖片當選項按鈕</li> <li>Quick Reply 文字輸入區上方一排小按鈕供使用者選取</li> <li>Flex Message 用 JSON 自訂訊息排版樣式。有 Flex Message Simulator 工具預覽 JSON 定義呈現效果</li> <li>LINE Bot 管理後台 <a href="https://developer.line.biz">https://developer.line.biz</a></li> <li>LIFF App (LINE Front-end Framework) 嵌入 HTML，呼叫 <code>https://api.line.me/liff/v1/app</code> 傳入 URL 得到 ID，寫成 line://app/11111111111-oxoxoxoxox 可在 LINE 開啟</li> <li>LIFF 網頁載入 <code>https://d.line-scdn.net/liff/1.0/sdk.js</code> 可用 JavaScript 取得使用者身分、送訊息等 <pre><code>liff.init((data) =&gt; {
data.context.userId; //使用者 ID
liff.sendMessage([
{ type:'text', text: 'message' }
]).then(() =&gt; { ... });
});
</code></pre> </li> <li>.NET 開發 LINE 機器人可使用 NuGet 安裝 <a href="https://www.nuget.org/packages/LineBotSDK">LineBotSDK</a> 簡化開發 (以下簡稱 SDK)</li> <li>SDK 有提供方法建立及列舉 LIFF 連結，另外也可使用 LINE 管理後台操作</li> <li>WebHook 以網站形式接受 LINE 呼叫，做出回應</li> <li>Push API 要錢，可主動送訊息給 User Id, Reply API 免費，用 ReplyToken 回覆使用者發言，時效很短 (註：約 30 秒)</li> <li>WebHook 收到 ReceivedMessage events 一般只有一筆，但高負載情境下可能多筆。每個event可能來自不同使用者，想像成多個包裹一次送到</li> <li>event.source.userId 為使用者識別，SDK 有 GetUserInfo() 可取姓名、大頭照、狀態文字等</li> <li>event.text 訊息內容 event.packageId stickerId 貼圖資訊</li> <li>API 也可送貼圖，package ID/sticker ID 對照表可查 <a href="https://developers.line.biz/en/docs/messaging-api/sticker-list/">List of available stickers</a> (書上的 PDF 網址已失效)</li> <li>訊息也可能是GPS座標，由 address, latitude, longitude 取值</li> <li>訊息型別包含 Text, Sticker, Image, Video, Audio, File, Location</li> <li>event 類別有 <ul><li>Follow 加好友</li> <li>Unfollow 封鎖</li> <li>Join 被加入聊天室</li> <li>Leave 離開聊天室</li> <li>Postback 收到 Postback 訊息 (來自 Template Message，new isRock.LineBot.PostbackAction()</li> <li>Beacon 收到 Beacon 訊號</li></ul> </li> <li>聊天室有兩種：隨意邀請人建立 - Room、透過選單建立群組 - Group</li> <li>每個群組只能有一個Bot, Bot 不知被誰加，可識別目前說話者（不限好友）, 可主動退出<br> event.source.type==room/group, event.source.roomId/.groupId &lt;= 傳入 PushMessage 參數主動對群組發話</li> <li>抓傳來的圖檔、聲音用 SDK GetUserUploadedContent(message.id,...) 取得內容</li> <li>可繼承 SDK LineWebHookControllerBase 實作 ApiController。免自己解析 JSON，由 ReceivedMessage 取得訊息內容</li> <li>ngrok 將 localhost 特定 Port 轉接到 Internet 的 HTTPS，方便用 VS 逐行偵錯<br> 註：也可用 <a href="/blog/ssh-tunnel-as-ngrok/">SSH Tunneling 或 Cloudflare Tunnel</a></li> <li>防止有人假冒 LINE 服務呼叫你的 WebHook - 檢查 Channel Secret (X-Line-Signature HTTP Header) / isRock.LineBot.Utility.SignatureValidation(this);</li> <li>免費通知服務 LINE Notify Service，申請後得到 Client ID、Client Secret</li> <li>允許發 LINE Nontify 給使用者需經 OAuth 驗證：引導頁轉到 LINE 官方 OAuth，確認身分並同意授權後導回我們網取並附上 code (還有自訂的 state，一般用來識別使用者是誰)，後端傳 code 給 API 取得 Token 儲存，以後用此 Token 傳訊給該使用者</li> <li>Rich Menu 是官方帳號常見下方兩排六格的快捷選單(有 3+3, 2x2, 1+3, 1+2, 1+1, 滿版等版型)</li> <li>LUIS - Language Understanding Intelligent Services, 理解輸入語句，歸納出意圖(Intent)，並抓出關鍵 Entities (在 ChatGPT 等 LLM AI 出現後，LUIS 的學習價值待重新評估)</li> <li>Azure Cognitive Service：Computer Vision API、Translater API... 等章節，基於 AI 剛邁向新時代，這部分也先擱著。</li></ul> <p>最後，用一個簡單練習結束這回合。ASP.NET Core <a href="/blog/minimal-api/">Minimal API</a> 搭配 LineBotSDK，寫一個接收使用者傳送文字及傳回 TemplateMessage 的小範例：</p> <p><img src="/Posts/files/2023/Fig1_638282222859676878.jpg"></p> <p>程式碼如下，整支程式 50 行 C# 搞定：</p> <pre><code>using isRock.LineBot;
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();
var chanAccToken = 取得被安全儲存的ChannelAccessToken();
var bot = new isRock.LineBot.Bot(chanAccToken);
app.MapGet("/", () =&gt; "Hello World!");
app.MapPost("/WebHook", async (HttpRequest request) =&gt;
{
using var sr = new StreamReader(request.Body);
var reqBody = await sr.ReadToEndAsync();
var rcvMsg = isRock.LineBot.Utility.Parsing(reqBody);
foreach (var e in rcvMsg.events)
{
if (!string.IsNullOrEmpty(e.message?.text))
{
var replyToken = e.replyToken;
var userId = e.source.userId;
var userName = bot.GetUserInfo(userId).displayName;
var userMsg = e.message.text;
if (userMsg == "/buttons")
{
var btnTmpl = new isRock.LineBot.ButtonsTemplate()
{
altText = "請用手機檢視",
text = "請選擇",
title = "敲指令管 Azure，你選哪一種？",
thumbnailImageUrl = new Uri("... Fig2_638130550931190042.png"),
actions = new List&lt;isRock.LineBot.TemplateActionBase&gt;() {
new isRock.LineBot.MessageAction() {
label = "Azure CLI",
text = "Azure CLI"
},
new isRock.LineBot.MessageAction() {
label = "Azure PowerShell",
text = "Azure PowerShell"
},
new isRock.LineBot.UriAction() {
label = "看文章",
uri = new Uri("https://blog.darkthread.net/blog/azure-cli-or-azure-powershell/")
}
}
};
bot.ReplyMessage(replyToken, new TemplateMessage(btnTmpl));
}
else
bot.ReplyMessage(replyToken, $"[記錄]{userName}說：{userMsg}");
}
}
return Results.Ok();
});
app.Run();
</code></pre> <p data-slug="line-bot-sdk-notes" data-title-en="LineBotSDK notes" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2017/11/13/16/26/snail-2946075_1280.jpg#1280#853">Notes about LineBotSDK and a ASP.NET Core Minimal API example to response message from LINE.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>週末啃完 David 老師「LINE BOT 與人工智慧辨別開發實戰」一書(再一次<a href="/blog/chatgpt-console-chat/">慢半拍</a>，哈)，依慣例筆記備忘。</p> <p>先前<a href="/blog/aspnetcore-heroku-postgresql/">寫過 LINE 機器人</a>，平日也依賴 <a href="/blog/linenotify-linelogin-tips/">LINE Notify 發系統通報</a>，嚴格來說我不算 LINE 程式開發生手。但相較於程式用到什麼查什麼學什麼的遊擊式學習，書籍課程提供較完整的知識覆蓋面，讓你知道面對某個需求時有哪些可用選項，比起查到哪一篇範例就學哪一種寫法的福袋(盲盒)式開發，更能有效率地精巧解決問題。雖然 Google 、ChatGPT 跟 <a href="/blog/github-copilot-demo/">Copilot</a> 很方便，想要功力大增，一本好書或一門課程總能讓你事半功倍。所以，網路時代還是要讀書呀~</p> <ul><li>Line Message API 申請重點：建立公司和個人 Provider，取得 Channel Access Token 及 User ID，透過掃瞄 QR Code 將機器人加為好友</li> <li>格式化呈現選項讓使用者操作 - Template Message，分為 Buttons Template, Confirm Template, Carosul Template (最多十個）</li> <li>Actions 為使用者可點擊的選項，有以下形式：Post Action (觸發Webhook), Message Action (代使用者送出一句話）, Uri Action (開網頁）</li> <li>Template Message 在 PC 版會顯示公版不支援行動提示，可用 altText 自訂提示</li> <li>Carousel Template 直接用一張圖片當選項按鈕</li> <li>Quick Reply 文字輸入區上方一排小按鈕供使用者選取</li> <li>Flex Message 用 JSON 自訂訊息排版樣式。有 Flex Message Simulator 工具預覽 JSON 定義呈現效果</li> <li>LINE Bot 管理後台 <a href="https://developer.line.biz">https://developer.line.biz</a></li> <li>LIFF App (LINE Front-end Framework) 嵌入 HTML，呼叫 <code>https://api.line.me/liff/v1/app</code> 傳入 URL 得到 ID，寫成 line://app/11111111111-oxoxoxoxox 可在 LINE 開啟</li> <li>LIFF 網頁載入 <code>https://d.line-scdn.net/liff/1.0/sdk.js</code> 可用 JavaScript 取得使用者身分、送訊息等 <pre><code>liff.init((data) =&gt; {
data.context.userId; //使用者 ID
liff.sendMessage([
{ type:'text', text: 'message' }
]).then(() =&gt; { ... });
});
</code></pre> </li> <li>.NET 開發 LINE 機器人可使用 NuGet 安裝 <a href="https://www.nuget.org/packages/LineBotSDK">LineBotSDK</a> 簡化開發 (以下簡稱 SDK)</li> <li>SDK 有提供方法建立及列舉 LIFF 連結，另外也可使用 LINE 管理後台操作</li> <li>WebHook 以網站形式接受 LINE 呼叫，做出回應</li> <li>Push API 要錢，可主動送訊息給 User Id, Reply API 免費，用 ReplyToken 回覆使用者發言，時效很短 (註：約 30 秒)</li> <li>WebHook 收到 ReceivedMessage events 一般只有一筆，但高負載情境下可能多筆。每個event可能來自不同使用者，想像成多個包裹一次送到</li> <li>event.source.userId 為使用者識別，SDK 有 GetUserInfo() 可取姓名、大頭照、狀態文字等</li> <li>event.text 訊息內容 event.packageId stickerId 貼圖資訊</li> <li>API 也可送貼圖，package ID/sticker ID 對照表可查 <a href="https://developers.line.biz/en/docs/messaging-api/sticker-list/">List of available stickers</a> (書上的 PDF 網址已失效)</li> <li>訊息也可能是GPS座標，由 address, latitude, longitude 取值</li> <li>訊息型別包含 Text, Sticker, Image, Video, Audio, File, Location</li> <li>event 類別有 <ul><li>Follow 加好友</li> <li>Unfollow 封鎖</li> <li>Join 被加入聊天室</li> <li>Leave 離開聊天室</li> <li>Postback 收到 Postback 訊息 (來自 Template Message，new isRock.LineBot.PostbackAction()</li> <li>Beacon 收到 Beacon 訊號</li></ul> </li> <li>聊天室有兩種：隨意邀請人建立 - Room、透過選單建立群組 - Group</li> <li>每個群組只能有一個Bot, Bot 不知被誰加，可識別目前說話者（不限好友）, 可主動退出<br> event.source.type==room/group, event.source.roomId/.groupId &lt;= 傳入 PushMessage 參數主動對群組發話</li> <li>抓傳來的圖檔、聲音用 SDK GetUserUploadedContent(message.id,...) 取得內容</li> <li>可繼承 SDK LineWebHookControllerBase 實作 ApiController。免自己解析 JSON，由 ReceivedMessage 取得訊息內容</li> <li>ngrok 將 localhost 特定 Port 轉接到 Internet 的 HTTPS，方便用 VS 逐行偵錯<br> 註：也可用 <a href="/blog/ssh-tunnel-as-ngrok/">SSH Tunneling 或 Cloudflare Tunnel</a></li> <li>防止有人假冒 LINE 服務呼叫你的 WebHook - 檢查 Channel Secret (X-Line-Signature HTTP Header) / isRock.LineBot.Utility.SignatureValidation(this);</li> <li>免費通知服務 LINE Notify Service，申請後得到 Client ID、Client Secret</li> <li>允許發 LINE Nontify 給使用者需經 OAuth 驗證：引導頁轉到 LINE 官方 OAuth，確認身分並同意授權後導回我們網取並附上 code (還有自訂的 state，一般用來識別使用者是誰)，後端傳 code 給 API 取得 Token 儲存，以後用此 Token 傳訊給該使用者</li> <li>Rich Menu 是官方帳號常見下方兩排六格的快捷選單(有 3+3, 2x2, 1+3, 1+2, 1+1, 滿版等版型)</li> <li>LUIS - Language Understanding Intelligent Services, 理解輸入語句，歸納出意圖(Intent)，並抓出關鍵 Entities (在 ChatGPT 等 LLM AI 出現後，LUIS 的學習價值待重新評估)</li> <li>Azure Cognitive Service：Computer Vision API、Translater API... 等章節，基於 AI 剛邁向新時代，這部分也先擱著。</li></ul> <p>最後，用一個簡單練習結束這回合。ASP.NET Core <a href="/blog/minimal-api/">Minimal API</a> 搭配 LineBotSDK，寫一個接收使用者傳送文字及傳回 TemplateMessage 的小範例：</p> <p><img src="/Posts/files/2023/Fig1_638282222859676878.jpg"></p> <p>程式碼如下，整支程式 50 行 C# 搞定：</p> <pre><code>using isRock.LineBot;
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();
var chanAccToken = 取得被安全儲存的ChannelAccessToken();
var bot = new isRock.LineBot.Bot(chanAccToken);
app.MapGet("/", () =&gt; "Hello World!");
app.MapPost("/WebHook", async (HttpRequest request) =&gt;
{
using var sr = new StreamReader(request.Body);
var reqBody = await sr.ReadToEndAsync();
var rcvMsg = isRock.LineBot.Utility.Parsing(reqBody);
foreach (var e in rcvMsg.events)
{
if (!string.IsNullOrEmpty(e.message?.text))
{
var replyToken = e.replyToken;
var userId = e.source.userId;
var userName = bot.GetUserInfo(userId).displayName;
var userMsg = e.message.text;
if (userMsg == "/buttons")
{
var btnTmpl = new isRock.LineBot.ButtonsTemplate()
{
altText = "請用手機檢視",
text = "請選擇",
title = "敲指令管 Azure，你選哪一種？",
thumbnailImageUrl = new Uri("... Fig2_638130550931190042.png"),
actions = new List&lt;isRock.LineBot.TemplateActionBase&gt;() {
new isRock.LineBot.MessageAction() {
label = "Azure CLI",
text = "Azure CLI"
},
new isRock.LineBot.MessageAction() {
label = "Azure PowerShell",
text = "Azure PowerShell"
},
new isRock.LineBot.UriAction() {
label = "看文章",
uri = new Uri("https://blog.darkthread.net/blog/azure-cli-or-azure-powershell/")
}
}
};
bot.ReplyMessage(replyToken, new TemplateMessage(btnTmpl));
}
else
bot.ReplyMessage(replyToken, $"[記錄]{userName}說：{userMsg}");
}
}
return Results.Ok();
});
app.Run();
</code></pre> <p data-slug="line-bot-sdk-notes" data-title-en="LineBotSDK notes" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2017/11/13/16/26/snail-2946075_1280.jpg#1280#853">Notes about LineBotSDK and a ASP.NET Core Minimal API example to response message from LINE.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638282222859676878.jpg"/>
        <media:content url="/Posts/files/2023/Fig1_638282222859676878.jpg" medium="image"/>
      <pubDate>Mon, 21 Aug 2023 13:49:52 GMT</pubDate>
    </item><item>
      <title><![CDATA[Nginx Docker + SSH Tunneling 取代 ngrok 實現 Webhook 本機偵錯]]></title>
      <link>https://blog.darkthread.net/blog/ssh-tunnel-as-ngrok/</link>
      <itunes:title><![CDATA[Nginx Docker + SSH Tunneling 取代 ngrok 實現 Webhook 本機偵錯]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>LINE Bot 程式需要一個對 Internet 公開的 HTTPS URL 作為 Webhook 端點，以接收來自 LINE 聊天室的訊息、使用者回應及檔案。在開發偵測階段，我們通常是用 <code>http://localhost:12345</code> 這樣的本機隨機 Port 執行網站專案，每次都得先上傳雲端或對外開放主機測試太沒效率，更何況程式得在本機執行較易設定中斷點及 Line by Line 偵錯，不浪費 Visual Studio/VSCode 的強大功能。</p> <p>這類問題的常見解法是使用 <a href="https://ngrok.com/">ngrok</a>，透過一支 ngrok.exe 將 <code>http://localhost:12345</code> 導向 ngrok 公司的對外網站，對映成 <code>https://1234abcd.ngrok.io</code> 的隨機配發 URL 網址(而且有 HTTPS，當今跑網站的必要條件)，外界送到該網址的 HTTP 請求將轉到你的本機 12345 Port，如此既可在本機修改偵錯，同時又能接受來自 Internet 的請求，兩全其美。(若是要用 ngrok 在 Visual Studio 偵錯 Webhook 程式，可參考 David 老師的<a href="https://youtu.be/kCga1_E-ijs?t=244">教學影片</a>)</p> <p>如果你跟我一樣，嫌用 ngrok 技術含量過低，想多學些突破網路限制的野外求生技巧，還剛好在雲端有台 Linux VM 跑 Docker Nginx ，這篇用 SSH Tunneling 實現 Webhook 本機偵錯的做法可以參考一下。</p> <p>首先，我們要在雲端建一台小 VM 跑 Linux 並支援 HTTPS，如果有<a href="/blog/my-first-azure-ai-lab/"> Azure 學生帳戶、Visual Studio 訂閱的免費額度</a>，在 Azure 開台 <a href="/blog/azure-b1s-vm/">B1s 小機器跑 Ubuntu 或 Debian</a>應該綽綽有餘，不測試時關掉，幾乎用不到什麼額度。要在 Linux 安裝 HTTPS 網站基本環境可以參考這兩篇 - <a href="/blog/nginx-certbot-auto-setup/">HTTPS Nginx Docker 之懶人安裝法</a>、<a href="/blog/create-azure-debian-vm-w-cli/">一行指令建好 Azure VM 跑 HTTPS 網站</a>，大約五到十分鐘搞定。(另外建議<a href="/blog/azure-batch-set-nsg/">限定 SSH 存取來源 IP</a>以策安全)</p> <p>依上述懶人包安裝，Linux 上會有 Nginx + Certbot 負責自動申請及更新 TLS 憑證，用 <code>https://your-vm-name.region.cloudapp.azure.com</code> 將連到 ASP.NET Core 範例網站。為了要跑 SSH Tunneling，我們要先 ssh 登入 Linux 主機用 <code>sudo docker-compose down</code> 停用 ASP.NET Core 範例網站，把 localhost:5000 Port 讓出來。</p> <p><img src="/Posts/files/2023/Fig1_638281435856420058.png"></p> <p>隨便在本機建個 ASP.NET Core 專案跑起來，確認網站的 HTTP Port 為 5130，順便在 MapGet() 加上自訂程式及中斷點：</p> <p><img src="/Posts/files/2023/Fig2_638281435859399366.png"></p> <p>下一步，用以下 ssh 指令將本機 5130 Port 導向到 Linux VM 的 localhost 5000 Port：</p> <pre><code>ssh -R 5000:localhost:5130 my-ngrok.eastasia.cloudapp.azure.com
</code></pre> <p>上述 SSH 指令如一般登入進入 Shell 環境，但此時本機端的 5130 Port 已對映到 Linux 端的 5000 Port：</p> <p><img src="/Posts/files/2023/Fig3_638281435862024347.png"></p> <p>換言之，當外界呼叫 <code>https://my-ngrok.eastasia.cloudapp.azure.com/</code>，Linux 主機上的 Nginx Docker 容器會將 80 Port 對映到 localhost 5000 Port，SSH Tunneling 再將其轉送到我們本機的 5130 Port，觸發 VSCode 設定的中斷點：</p> <p><img src="/Posts/files/2023/Fig4_638281435864039435.png"></p> <p>就醬，一個類似 ngrok 的私人服務就完成囉。</p> <p>SSH Tunneling 功能十分強大，除了將本機 Port 透過 SSH 伺服器提供外部存取(Remote Port Forwarding)，也能將 SSH 伺服器當跳板去存取伺服器本身特定 Port 或再連到遠端主機特定 Port (Local Port Forwarding)，甚至當成 Socket Proxy 轉接任意主機 (Dynamic Port Forwarding)，相關介紹推薦這篇：<a href="https://johnliu55.tw/ssh-tunnel.html">SSH Tunneling (Port Forwarding) 詳解 by John Engineer Stuff</a>。</p> <p>[2023-08-21 補充]</p> <p>補上許多讀者大力推薦的 <a href="https://www.cloudflare.com/zh-tw/products/tunnel/">Cloudflare Tunnel</a>，可免費使用，且可自訂 DNS 網域名稱，就近分配節點並有一些安全防護，功能比 ngrok 強大，跟 ngrok 一樣可做為本機開發測試階段轉接對外連線，甚至用於正式營運。</p> <p>延伸閱讀：</p> <ul><li><a href="https://editor.leonh.space/2021/cloudflare-tunnel/">Cloudflare Tunnel 簡介 by 六小編</a></li> <li><a href="https://medium.com/@zetavg/%E4%BD%BF%E7%94%A8-cloudflare-tunnel-%E4%BD%9C%E7%82%BA%E4%BD%8E%E6%88%90%E6%9C%AC%E7%9A%84-ngrok-%E6%9B%BF%E4%BB%A3%E5%93%81-6b0aaef97557">使用 Cloudflare Tunnel 作為低成本的 ngrok 替代品 by Pokai Chang</a></li></ul> <p data-slug="ssh-tunnel-as-ngrok" data-title-en="Using SSH tunnel as ngrok-like service" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/01/31/15/32/geese-618595_960_720.jpg#960#720">Tutorial of using SSH tunneling to simulate ngrok function to enable ASP.NET Core debugging for webhook.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>LINE Bot 程式需要一個對 Internet 公開的 HTTPS URL 作為 Webhook 端點，以接收來自 LINE 聊天室的訊息、使用者回應及檔案。在開發偵測階段，我們通常是用 <code>http://localhost:12345</code> 這樣的本機隨機 Port 執行網站專案，每次都得先上傳雲端或對外開放主機測試太沒效率，更何況程式得在本機執行較易設定中斷點及 Line by Line 偵錯，不浪費 Visual Studio/VSCode 的強大功能。</p> <p>這類問題的常見解法是使用 <a href="https://ngrok.com/">ngrok</a>，透過一支 ngrok.exe 將 <code>http://localhost:12345</code> 導向 ngrok 公司的對外網站，對映成 <code>https://1234abcd.ngrok.io</code> 的隨機配發 URL 網址(而且有 HTTPS，當今跑網站的必要條件)，外界送到該網址的 HTTP 請求將轉到你的本機 12345 Port，如此既可在本機修改偵錯，同時又能接受來自 Internet 的請求，兩全其美。(若是要用 ngrok 在 Visual Studio 偵錯 Webhook 程式，可參考 David 老師的<a href="https://youtu.be/kCga1_E-ijs?t=244">教學影片</a>)</p> <p>如果你跟我一樣，嫌用 ngrok 技術含量過低，想多學些突破網路限制的野外求生技巧，還剛好在雲端有台 Linux VM 跑 Docker Nginx ，這篇用 SSH Tunneling 實現 Webhook 本機偵錯的做法可以參考一下。</p> <p>首先，我們要在雲端建一台小 VM 跑 Linux 並支援 HTTPS，如果有<a href="/blog/my-first-azure-ai-lab/"> Azure 學生帳戶、Visual Studio 訂閱的免費額度</a>，在 Azure 開台 <a href="/blog/azure-b1s-vm/">B1s 小機器跑 Ubuntu 或 Debian</a>應該綽綽有餘，不測試時關掉，幾乎用不到什麼額度。要在 Linux 安裝 HTTPS 網站基本環境可以參考這兩篇 - <a href="/blog/nginx-certbot-auto-setup/">HTTPS Nginx Docker 之懶人安裝法</a>、<a href="/blog/create-azure-debian-vm-w-cli/">一行指令建好 Azure VM 跑 HTTPS 網站</a>，大約五到十分鐘搞定。(另外建議<a href="/blog/azure-batch-set-nsg/">限定 SSH 存取來源 IP</a>以策安全)</p> <p>依上述懶人包安裝，Linux 上會有 Nginx + Certbot 負責自動申請及更新 TLS 憑證，用 <code>https://your-vm-name.region.cloudapp.azure.com</code> 將連到 ASP.NET Core 範例網站。為了要跑 SSH Tunneling，我們要先 ssh 登入 Linux 主機用 <code>sudo docker-compose down</code> 停用 ASP.NET Core 範例網站，把 localhost:5000 Port 讓出來。</p> <p><img src="/Posts/files/2023/Fig1_638281435856420058.png"></p> <p>隨便在本機建個 ASP.NET Core 專案跑起來，確認網站的 HTTP Port 為 5130，順便在 MapGet() 加上自訂程式及中斷點：</p> <p><img src="/Posts/files/2023/Fig2_638281435859399366.png"></p> <p>下一步，用以下 ssh 指令將本機 5130 Port 導向到 Linux VM 的 localhost 5000 Port：</p> <pre><code>ssh -R 5000:localhost:5130 my-ngrok.eastasia.cloudapp.azure.com
</code></pre> <p>上述 SSH 指令如一般登入進入 Shell 環境，但此時本機端的 5130 Port 已對映到 Linux 端的 5000 Port：</p> <p><img src="/Posts/files/2023/Fig3_638281435862024347.png"></p> <p>換言之，當外界呼叫 <code>https://my-ngrok.eastasia.cloudapp.azure.com/</code>，Linux 主機上的 Nginx Docker 容器會將 80 Port 對映到 localhost 5000 Port，SSH Tunneling 再將其轉送到我們本機的 5130 Port，觸發 VSCode 設定的中斷點：</p> <p><img src="/Posts/files/2023/Fig4_638281435864039435.png"></p> <p>就醬，一個類似 ngrok 的私人服務就完成囉。</p> <p>SSH Tunneling 功能十分強大，除了將本機 Port 透過 SSH 伺服器提供外部存取(Remote Port Forwarding)，也能將 SSH 伺服器當跳板去存取伺服器本身特定 Port 或再連到遠端主機特定 Port (Local Port Forwarding)，甚至當成 Socket Proxy 轉接任意主機 (Dynamic Port Forwarding)，相關介紹推薦這篇：<a href="https://johnliu55.tw/ssh-tunnel.html">SSH Tunneling (Port Forwarding) 詳解 by John Engineer Stuff</a>。</p> <p>[2023-08-21 補充]</p> <p>補上許多讀者大力推薦的 <a href="https://www.cloudflare.com/zh-tw/products/tunnel/">Cloudflare Tunnel</a>，可免費使用，且可自訂 DNS 網域名稱，就近分配節點並有一些安全防護，功能比 ngrok 強大，跟 ngrok 一樣可做為本機開發測試階段轉接對外連線，甚至用於正式營運。</p> <p>延伸閱讀：</p> <ul><li><a href="https://editor.leonh.space/2021/cloudflare-tunnel/">Cloudflare Tunnel 簡介 by 六小編</a></li> <li><a href="https://medium.com/@zetavg/%E4%BD%BF%E7%94%A8-cloudflare-tunnel-%E4%BD%9C%E7%82%BA%E4%BD%8E%E6%88%90%E6%9C%AC%E7%9A%84-ngrok-%E6%9B%BF%E4%BB%A3%E5%93%81-6b0aaef97557">使用 Cloudflare Tunnel 作為低成本的 ngrok 替代品 by Pokai Chang</a></li></ul> <p data-slug="ssh-tunnel-as-ngrok" data-title-en="Using SSH tunnel as ngrok-like service" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/01/31/15/32/geese-618595_960_720.jpg#960#720">Tutorial of using SSH tunneling to simulate ngrok function to enable ASP.NET Core debugging for webhook.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>LINE Bot 程式需要一個對 Internet 公開的 HTTPS URL 作為 Webhook 端點，以接收來自 LINE 聊天室的訊息、使用者回應及檔案。在開發偵測階段，我們通常是用 <code>http://localhost:12345</code> 這樣的本機隨機 Port 執行網站專案，每次都得先上傳雲端或對外開放主機測試太沒效率，更何況程式得在本機執行較易設定中斷點及 Line by Line 偵錯，不浪費 Visual Studio/VSCode 的強大功能。</p> <p>這類問題的常見解法是使用 <a href="https://ngrok.com/">ngrok</a>，透過一支 ngrok.exe 將 <code>http://localhost:12345</code> 導向 ngrok 公司的對外網站，對映成 <code>https://1234abcd.ngrok.io</code> 的隨機配發 URL 網址(而且有 HTTPS，當今跑網站的必要條件)，外界送到該網址的 HTTP 請求將轉到你的本機 12345 Port，如此既可在本機修改偵錯，同時又能接受來自 Internet 的請求，兩全其美。(若是要用 ngrok 在 Visual Studio 偵錯 Webhook 程式，可參考 David 老師的<a href="https://youtu.be/kCga1_E-ijs?t=244">教學影片</a>)</p> <p>如果你跟我一樣，嫌用 ngrok 技術含量過低，想多學些突破網路限制的野外求生技巧，還剛好在雲端有台 Linux VM 跑 Docker Nginx ，這篇用 SSH Tunneling 實現 Webhook 本機偵錯的做法可以參考一下。</p> <p>首先，我們要在雲端建一台小 VM 跑 Linux 並支援 HTTPS，如果有<a href="/blog/my-first-azure-ai-lab/"> Azure 學生帳戶、Visual Studio 訂閱的免費額度</a>，在 Azure 開台 <a href="/blog/azure-b1s-vm/">B1s 小機器跑 Ubuntu 或 Debian</a>應該綽綽有餘，不測試時關掉，幾乎用不到什麼額度。要在 Linux 安裝 HTTPS 網站基本環境可以參考這兩篇 - <a href="/blog/nginx-certbot-auto-setup/">HTTPS Nginx Docker 之懶人安裝法</a>、<a href="/blog/create-azure-debian-vm-w-cli/">一行指令建好 Azure VM 跑 HTTPS 網站</a>，大約五到十分鐘搞定。(另外建議<a href="/blog/azure-batch-set-nsg/">限定 SSH 存取來源 IP</a>以策安全)</p> <p>依上述懶人包安裝，Linux 上會有 Nginx + Certbot 負責自動申請及更新 TLS 憑證，用 <code>https://your-vm-name.region.cloudapp.azure.com</code> 將連到 ASP.NET Core 範例網站。為了要跑 SSH Tunneling，我們要先 ssh 登入 Linux 主機用 <code>sudo docker-compose down</code> 停用 ASP.NET Core 範例網站，把 localhost:5000 Port 讓出來。</p> <p><img src="/Posts/files/2023/Fig1_638281435856420058.png"></p> <p>隨便在本機建個 ASP.NET Core 專案跑起來，確認網站的 HTTP Port 為 5130，順便在 MapGet() 加上自訂程式及中斷點：</p> <p><img src="/Posts/files/2023/Fig2_638281435859399366.png"></p> <p>下一步，用以下 ssh 指令將本機 5130 Port 導向到 Linux VM 的 localhost 5000 Port：</p> <pre><code>ssh -R 5000:localhost:5130 my-ngrok.eastasia.cloudapp.azure.com
</code></pre> <p>上述 SSH 指令如一般登入進入 Shell 環境，但此時本機端的 5130 Port 已對映到 Linux 端的 5000 Port：</p> <p><img src="/Posts/files/2023/Fig3_638281435862024347.png"></p> <p>換言之，當外界呼叫 <code>https://my-ngrok.eastasia.cloudapp.azure.com/</code>，Linux 主機上的 Nginx Docker 容器會將 80 Port 對映到 localhost 5000 Port，SSH Tunneling 再將其轉送到我們本機的 5130 Port，觸發 VSCode 設定的中斷點：</p> <p><img src="/Posts/files/2023/Fig4_638281435864039435.png"></p> <p>就醬，一個類似 ngrok 的私人服務就完成囉。</p> <p>SSH Tunneling 功能十分強大，除了將本機 Port 透過 SSH 伺服器提供外部存取(Remote Port Forwarding)，也能將 SSH 伺服器當跳板去存取伺服器本身特定 Port 或再連到遠端主機特定 Port (Local Port Forwarding)，甚至當成 Socket Proxy 轉接任意主機 (Dynamic Port Forwarding)，相關介紹推薦這篇：<a href="https://johnliu55.tw/ssh-tunnel.html">SSH Tunneling (Port Forwarding) 詳解 by John Engineer Stuff</a>。</p> <p>[2023-08-21 補充]</p> <p>補上許多讀者大力推薦的 <a href="https://www.cloudflare.com/zh-tw/products/tunnel/">Cloudflare Tunnel</a>，可免費使用，且可自訂 DNS 網域名稱，就近分配節點並有一些安全防護，功能比 ngrok 強大，跟 ngrok 一樣可做為本機開發測試階段轉接對外連線，甚至用於正式營運。</p> <p>延伸閱讀：</p> <ul><li><a href="https://editor.leonh.space/2021/cloudflare-tunnel/">Cloudflare Tunnel 簡介 by 六小編</a></li> <li><a href="https://medium.com/@zetavg/%E4%BD%BF%E7%94%A8-cloudflare-tunnel-%E4%BD%9C%E7%82%BA%E4%BD%8E%E6%88%90%E6%9C%AC%E7%9A%84-ngrok-%E6%9B%BF%E4%BB%A3%E5%93%81-6b0aaef97557">使用 Cloudflare Tunnel 作為低成本的 ngrok 替代品 by Pokai Chang</a></li></ul> <p data-slug="ssh-tunnel-as-ngrok" data-title-en="Using SSH tunnel as ngrok-like service" data-catg="ASP.NET/IIS" data-fbimg="https://cdn.pixabay.com/photo/2015/01/31/15/32/geese-618595_960_720.jpg#960#720">Tutorial of using SSH tunneling to simulate ngrok function to enable ASP.NET Core debugging for webhook.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638281435856420058.png"/>
        <media:content url="/Posts/files/2023/Fig1_638281435856420058.png" medium="image"/>
      <pubDate>Sun, 20 Aug 2023 15:52:35 GMT</pubDate>
    </item><item>
      <title><![CDATA[在 C# 使用 USB 實體金鑰加解密]]></title>
      <link>https://blog.darkthread.net/blog/gpg-encrypt-with-csharp/</link>
      <itunes:title><![CDATA[在 C# 使用 USB 實體金鑰加解密]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>用 .NET 程式加解密不是新鮮話題，但如果是用 .NET 程式整合 USB 實體金鑰加密資料，做到沒實體金鑰不知 PIN 碼就解不開，聽起來是不是就有點意思了？</p> <p><a href="/blog/low-cost-diy-usb-security-key/">土砲 USB 金鑰</a> Side Project 持續進行，<a href="/blog/gpg-keytocard/">產生及設定金鑰</a>、使用 GPG 加解密、<a href="/blog/usb-token-ssh-key/">登入 SSH</a> 都沒啥問題，下一步我想拿來做些有趣應用。第一個想做的是用實體金鑰保護日常祕密，像是電腦密碼、金融密碼，甚至是 API Key。上回介紹<a href="/blog/secure-apikey-for-console-app/">用 ProtectedData 保護工具程式用的 API Key</a>，ProtectedData 固然無腦方便又安全，但只要駭客能冒用你的身分遠端控制你的電腦，ProtectedData 便瞬間破功。若相關設定是用實體金鑰加密，則還有 PIN 碼跟按下實體按鈕這兩關，尤其是後者，不但要遠端控制你的電腦，還要用社交攻擊找人幫忙按 USB 金鑰上的開關，難度頓時上升 N 倍，讓人格外安心。</p> <p>有名的 USB 實體金鑰 - YubiKey 其實已能<a href="https://support.yubico.com/hc/en-us/articles/360013779759-Using-Your-YubiKey-with-KeePass">整合密碼保管軟體 KeePass</a>，但其做法對我來說複雜了點而且僅限應用於特定軟體，我希望自己有能力整合，需要什麼可以自己寫。我對保密軟體的要求很簡單 - 最好採用行之有年的公開加密標準，代表其安全性及方便性通過考驗；採用公開標準的好處是除了自己寫的程式，用第三方工具也能加解密，處理問題多了一些選擇。依此標準，<a href="/blog/gpg-keytocard/">GPG + 自製 USB 金鑰</a>是絕佳選擇，做三把鑰匙不用兩百元，多兩支備份超安心。(YubiKey 要買到三支，口袋得夠深呀)</p> <p>其實在 Windows 裝好 gpg4win，將保密資料寫進文字檔或 Word，下指令 <code>gpg -ear Jeffrey -o secret.asc secret.txt</code> 加密，<code>gpg -d secret.asc</code> 解密，就是最簡陋的加解密解決方案，不怎麼好用。</p> <p>我的想法是寫支程式把 GPG 加解密程序包起來，並提供基本的資料檔管理、關鍵字查詢... 等功能，甚至比照 Azure Key Vault 做成超強防護的本機保險箱服務。想用 .NET 整合 GPG，最無腦的解法是用 Process.Start() 呼叫 gpg.exe，所有手敲指令辦得到的工作都能轉由用 .NET 執行。不過，GPG 有更優雅的解法 - <a href="https://wiki.gnupg.org/APIs">GPG Made Easy，簡稱 GPGME</a>，一套 C 開發的 GnuPG API 介面。想在 C# 使用 GPGME 的話，最簡單的方法是從 NuGet 下載 <a href="https://github.com/gpgme-sharp/gpgme-sharp">gpgme-sharp</a>，在 Windows 依賴 Gpg4Win (限 32 位元)、在 Linux 要先裝 libgpgme11，便能透過 .NET 程直接執行金鑰管理、加解密、數位簽章等 GPG 作業。</p> <p>參考 Github 上的範例，我寫了一支小程式完成加密及解密：</p> <pre><code>using System;
using System.Globalization;
using System.Text;
using System.IO;
using Libgpgme;
using System.Linq;
Context ctx = new Context();
// 如果要改用 Console.ReadLine() 輸入 PIN：
// ctx.PinentryMode = PinentryMode.Loopback;
// ctx.SetPassphraseFunction(MyPassphraseCallback);
// PassphraseResult MyPassphraseCallback(Context ctx, PassphraseInfo info, ref char[] passwd)
if (ctx.Protocol != Protocol.OpenPGP)
ctx.SetEngineInfo(Protocol.OpenPGP, null, null);
// 尋找名為 Jeffrey 的金鑰
const string SEARCHSTR = "Jeffrey";
// 取得第一把符合的金鑰
var key = ctx.KeyStore.GetKeyList(SEARCHSTR, false)?
.OfType&lt;PgpKey&gt;() // 需為 PgpKey 型別
// Uid 及 Fingerprint 不可為 null
.Where(o =&gt; o.Uid != null &amp;&amp; o.Fingerprint != null).FirstOrDefault();
if (key == null)
{
Console.WriteLine("未找到金鑰");
return;
}
Console.WriteLine($"找到金鑰 {key.Uid.Name} / {key.Fingerprint}");
Action&lt;string, string&gt; print = (message, title) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine($"** {title} **");
Console.ResetColor();
Console.WriteLine(message);
};
var secrettext = "Hello, world!";
print(secrettext, "明文內容");
// 宣告記憶體緩衝區存放明文
var plain = new GpgmeMemoryData();
//plain.FileName = "my_document.txt";
// 建立 BinaryWriter 物件，以 UTF8 編碼寫入記憶體緩衝區
var binwriter = new BinaryWriter(plain, Encoding.UTF8);
binwriter.Write(secrettext.ToCharArray());
binwriter.Flush();
plain.Seek(0, SeekOrigin.Begin);
// 指定純文字輸出 (Armor)
ctx.Armor = true;
// 宣告記憶體緩衝區存放密文
var cipher = new GpgmeMemoryData();
//cipher.FileName = "my_document.txt";
ctx.Encrypt(new Key[] { key }, EncryptFlags.AlwaysTrust, plain, cipher);
// 顯示加密結果
cipher.Seek(0, SeekOrigin.Begin);
string encrypted;
using (var srEnc = new StreamReader(cipher, Encoding.UTF8)) {
encrypted = srEnc.ReadToEnd();
print(encrypted, "加密結果");
}
// 將密文串流指向開頭
var encData = new GpgmeMemoryData();
var encWriter = new BinaryWriter(encData, Encoding.UTF8);
encWriter.Write(encrypted.ToCharArray());
encData.Seek(0, SeekOrigin.Begin);
var decryptedData = new GpgmeMemoryData();
var decrst = ctx.Decrypt(encData, decryptedData);
// 讀取解密結果
decryptedData.Seek(0, SeekOrigin.Begin);
using var srDes = new StreamReader(decryptedData, Encoding.UTF8);
print(srDes.ReadToEnd(), "解密結果");
// 顯示加密金鑰資訊
Console.ForegroundColor = ConsoleColor.Cyan;
if (decrst.Recipients != null)
Console.WriteLine(
string.Join("
",
decrst.Recipients!
.Select(r =&gt; $"金鑰={r.KeyId} 加密演算法={Gpgme.GetPubkeyAlgoName(r.KeyAlgorithm)}").ToArray()));
Console.ResetColor();
</code></pre> <p>程式使用 gpgme-sharp 程式庫，先建立 Context 物件，從 KeyStore 以使用者名稱找到金鑰，進行加密。解密時由密文附加資訊識別為實體金鑰，要求 PIN 碼及按確認鈕才能解密。如此，一個用 C# 整合實體金鑰加解密的 PoC 就完成囉~</p> <p><a href="https://www.youtube.com/watch?v=GP3zQG-_cWw">C# GPG 解密實測</a></p> <p data-slug="gpg-encrypt-with-csharp" data-title-en="Using C# to decrypt with GPG USB Key Token" data-catg=".NET" data-fbimg="https://i.imgur.com/6qdjJS5.jpg#1280#720">Example of using gpgme-sharp library to encrypt and decrypt data with GPG USB tokey.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>用 .NET 程式加解密不是新鮮話題，但如果是用 .NET 程式整合 USB 實體金鑰加密資料，做到沒實體金鑰不知 PIN 碼就解不開，聽起來是不是就有點意思了？</p> <p><a href="/blog/low-cost-diy-usb-security-key/">土砲 USB 金鑰</a> Side Project 持續進行，<a href="/blog/gpg-keytocard/">產生及設定金鑰</a>、使用 GPG 加解密、<a href="/blog/usb-token-ssh-key/">登入 SSH</a> 都沒啥問題，下一步我想拿來做些有趣應用。第一個想做的是用實體金鑰保護日常祕密，像是電腦密碼、金融密碼，甚至是 API Key。上回介紹<a href="/blog/secure-apikey-for-console-app/">用 ProtectedData 保護工具程式用的 API Key</a>，ProtectedData 固然無腦方便又安全，但只要駭客能冒用你的身分遠端控制你的電腦，ProtectedData 便瞬間破功。若相關設定是用實體金鑰加密，則還有 PIN 碼跟按下實體按鈕這兩關，尤其是後者，不但要遠端控制你的電腦，還要用社交攻擊找人幫忙按 USB 金鑰上的開關，難度頓時上升 N 倍，讓人格外安心。</p> <p>有名的 USB 實體金鑰 - YubiKey 其實已能<a href="https://support.yubico.com/hc/en-us/articles/360013779759-Using-Your-YubiKey-with-KeePass">整合密碼保管軟體 KeePass</a>，但其做法對我來說複雜了點而且僅限應用於特定軟體，我希望自己有能力整合，需要什麼可以自己寫。我對保密軟體的要求很簡單 - 最好採用行之有年的公開加密標準，代表其安全性及方便性通過考驗；採用公開標準的好處是除了自己寫的程式，用第三方工具也能加解密，處理問題多了一些選擇。依此標準，<a href="/blog/gpg-keytocard/">GPG + 自製 USB 金鑰</a>是絕佳選擇，做三把鑰匙不用兩百元，多兩支備份超安心。(YubiKey 要買到三支，口袋得夠深呀)</p> <p>其實在 Windows 裝好 gpg4win，將保密資料寫進文字檔或 Word，下指令 <code>gpg -ear Jeffrey -o secret.asc secret.txt</code> 加密，<code>gpg -d secret.asc</code> 解密，就是最簡陋的加解密解決方案，不怎麼好用。</p> <p>我的想法是寫支程式把 GPG 加解密程序包起來，並提供基本的資料檔管理、關鍵字查詢... 等功能，甚至比照 Azure Key Vault 做成超強防護的本機保險箱服務。想用 .NET 整合 GPG，最無腦的解法是用 Process.Start() 呼叫 gpg.exe，所有手敲指令辦得到的工作都能轉由用 .NET 執行。不過，GPG 有更優雅的解法 - <a href="https://wiki.gnupg.org/APIs">GPG Made Easy，簡稱 GPGME</a>，一套 C 開發的 GnuPG API 介面。想在 C# 使用 GPGME 的話，最簡單的方法是從 NuGet 下載 <a href="https://github.com/gpgme-sharp/gpgme-sharp">gpgme-sharp</a>，在 Windows 依賴 Gpg4Win (限 32 位元)、在 Linux 要先裝 libgpgme11，便能透過 .NET 程直接執行金鑰管理、加解密、數位簽章等 GPG 作業。</p> <p>參考 Github 上的範例，我寫了一支小程式完成加密及解密：</p> <pre><code>using System;
using System.Globalization;
using System.Text;
using System.IO;
using Libgpgme;
using System.Linq;
Context ctx = new Context();
// 如果要改用 Console.ReadLine() 輸入 PIN：
// ctx.PinentryMode = PinentryMode.Loopback;
// ctx.SetPassphraseFunction(MyPassphraseCallback);
// PassphraseResult MyPassphraseCallback(Context ctx, PassphraseInfo info, ref char[] passwd)
if (ctx.Protocol != Protocol.OpenPGP)
ctx.SetEngineInfo(Protocol.OpenPGP, null, null);
// 尋找名為 Jeffrey 的金鑰
const string SEARCHSTR = "Jeffrey";
// 取得第一把符合的金鑰
var key = ctx.KeyStore.GetKeyList(SEARCHSTR, false)?
.OfType&lt;PgpKey&gt;() // 需為 PgpKey 型別
// Uid 及 Fingerprint 不可為 null
.Where(o =&gt; o.Uid != null &amp;&amp; o.Fingerprint != null).FirstOrDefault();
if (key == null)
{
Console.WriteLine("未找到金鑰");
return;
}
Console.WriteLine($"找到金鑰 {key.Uid.Name} / {key.Fingerprint}");
Action&lt;string, string&gt; print = (message, title) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine($"** {title} **");
Console.ResetColor();
Console.WriteLine(message);
};
var secrettext = "Hello, world!";
print(secrettext, "明文內容");
// 宣告記憶體緩衝區存放明文
var plain = new GpgmeMemoryData();
//plain.FileName = "my_document.txt";
// 建立 BinaryWriter 物件，以 UTF8 編碼寫入記憶體緩衝區
var binwriter = new BinaryWriter(plain, Encoding.UTF8);
binwriter.Write(secrettext.ToCharArray());
binwriter.Flush();
plain.Seek(0, SeekOrigin.Begin);
// 指定純文字輸出 (Armor)
ctx.Armor = true;
// 宣告記憶體緩衝區存放密文
var cipher = new GpgmeMemoryData();
//cipher.FileName = "my_document.txt";
ctx.Encrypt(new Key[] { key }, EncryptFlags.AlwaysTrust, plain, cipher);
// 顯示加密結果
cipher.Seek(0, SeekOrigin.Begin);
string encrypted;
using (var srEnc = new StreamReader(cipher, Encoding.UTF8)) {
encrypted = srEnc.ReadToEnd();
print(encrypted, "加密結果");
}
// 將密文串流指向開頭
var encData = new GpgmeMemoryData();
var encWriter = new BinaryWriter(encData, Encoding.UTF8);
encWriter.Write(encrypted.ToCharArray());
encData.Seek(0, SeekOrigin.Begin);
var decryptedData = new GpgmeMemoryData();
var decrst = ctx.Decrypt(encData, decryptedData);
// 讀取解密結果
decryptedData.Seek(0, SeekOrigin.Begin);
using var srDes = new StreamReader(decryptedData, Encoding.UTF8);
print(srDes.ReadToEnd(), "解密結果");
// 顯示加密金鑰資訊
Console.ForegroundColor = ConsoleColor.Cyan;
if (decrst.Recipients != null)
Console.WriteLine(
string.Join("
",
decrst.Recipients!
.Select(r =&gt; $"金鑰={r.KeyId} 加密演算法={Gpgme.GetPubkeyAlgoName(r.KeyAlgorithm)}").ToArray()));
Console.ResetColor();
</code></pre> <p>程式使用 gpgme-sharp 程式庫，先建立 Context 物件，從 KeyStore 以使用者名稱找到金鑰，進行加密。解密時由密文附加資訊識別為實體金鑰，要求 PIN 碼及按確認鈕才能解密。如此，一個用 C# 整合實體金鑰加解密的 PoC 就完成囉~</p> <p><a href="https://www.youtube.com/watch?v=GP3zQG-_cWw">C# GPG 解密實測</a></p> <p data-slug="gpg-encrypt-with-csharp" data-title-en="Using C# to decrypt with GPG USB Key Token" data-catg=".NET" data-fbimg="https://i.imgur.com/6qdjJS5.jpg#1280#720">Example of using gpgme-sharp library to encrypt and decrypt data with GPG USB tokey.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>用 .NET 程式加解密不是新鮮話題，但如果是用 .NET 程式整合 USB 實體金鑰加密資料，做到沒實體金鑰不知 PIN 碼就解不開，聽起來是不是就有點意思了？</p> <p><a href="/blog/low-cost-diy-usb-security-key/">土砲 USB 金鑰</a> Side Project 持續進行，<a href="/blog/gpg-keytocard/">產生及設定金鑰</a>、使用 GPG 加解密、<a href="/blog/usb-token-ssh-key/">登入 SSH</a> 都沒啥問題，下一步我想拿來做些有趣應用。第一個想做的是用實體金鑰保護日常祕密，像是電腦密碼、金融密碼，甚至是 API Key。上回介紹<a href="/blog/secure-apikey-for-console-app/">用 ProtectedData 保護工具程式用的 API Key</a>，ProtectedData 固然無腦方便又安全，但只要駭客能冒用你的身分遠端控制你的電腦，ProtectedData 便瞬間破功。若相關設定是用實體金鑰加密，則還有 PIN 碼跟按下實體按鈕這兩關，尤其是後者，不但要遠端控制你的電腦，還要用社交攻擊找人幫忙按 USB 金鑰上的開關，難度頓時上升 N 倍，讓人格外安心。</p> <p>有名的 USB 實體金鑰 - YubiKey 其實已能<a href="https://support.yubico.com/hc/en-us/articles/360013779759-Using-Your-YubiKey-with-KeePass">整合密碼保管軟體 KeePass</a>，但其做法對我來說複雜了點而且僅限應用於特定軟體，我希望自己有能力整合，需要什麼可以自己寫。我對保密軟體的要求很簡單 - 最好採用行之有年的公開加密標準，代表其安全性及方便性通過考驗；採用公開標準的好處是除了自己寫的程式，用第三方工具也能加解密，處理問題多了一些選擇。依此標準，<a href="/blog/gpg-keytocard/">GPG + 自製 USB 金鑰</a>是絕佳選擇，做三把鑰匙不用兩百元，多兩支備份超安心。(YubiKey 要買到三支，口袋得夠深呀)</p> <p>其實在 Windows 裝好 gpg4win，將保密資料寫進文字檔或 Word，下指令 <code>gpg -ear Jeffrey -o secret.asc secret.txt</code> 加密，<code>gpg -d secret.asc</code> 解密，就是最簡陋的加解密解決方案，不怎麼好用。</p> <p>我的想法是寫支程式把 GPG 加解密程序包起來，並提供基本的資料檔管理、關鍵字查詢... 等功能，甚至比照 Azure Key Vault 做成超強防護的本機保險箱服務。想用 .NET 整合 GPG，最無腦的解法是用 Process.Start() 呼叫 gpg.exe，所有手敲指令辦得到的工作都能轉由用 .NET 執行。不過，GPG 有更優雅的解法 - <a href="https://wiki.gnupg.org/APIs">GPG Made Easy，簡稱 GPGME</a>，一套 C 開發的 GnuPG API 介面。想在 C# 使用 GPGME 的話，最簡單的方法是從 NuGet 下載 <a href="https://github.com/gpgme-sharp/gpgme-sharp">gpgme-sharp</a>，在 Windows 依賴 Gpg4Win (限 32 位元)、在 Linux 要先裝 libgpgme11，便能透過 .NET 程直接執行金鑰管理、加解密、數位簽章等 GPG 作業。</p> <p>參考 Github 上的範例，我寫了一支小程式完成加密及解密：</p> <pre><code>using System;
using System.Globalization;
using System.Text;
using System.IO;
using Libgpgme;
using System.Linq;
Context ctx = new Context();
// 如果要改用 Console.ReadLine() 輸入 PIN：
// ctx.PinentryMode = PinentryMode.Loopback;
// ctx.SetPassphraseFunction(MyPassphraseCallback);
// PassphraseResult MyPassphraseCallback(Context ctx, PassphraseInfo info, ref char[] passwd)
if (ctx.Protocol != Protocol.OpenPGP)
ctx.SetEngineInfo(Protocol.OpenPGP, null, null);
// 尋找名為 Jeffrey 的金鑰
const string SEARCHSTR = "Jeffrey";
// 取得第一把符合的金鑰
var key = ctx.KeyStore.GetKeyList(SEARCHSTR, false)?
.OfType&lt;PgpKey&gt;() // 需為 PgpKey 型別
// Uid 及 Fingerprint 不可為 null
.Where(o =&gt; o.Uid != null &amp;&amp; o.Fingerprint != null).FirstOrDefault();
if (key == null)
{
Console.WriteLine("未找到金鑰");
return;
}
Console.WriteLine($"找到金鑰 {key.Uid.Name} / {key.Fingerprint}");
Action&lt;string, string&gt; print = (message, title) =&gt; {
Console.ForegroundColor = ConsoleColor.Yellow;
Console.WriteLine($"** {title} **");
Console.ResetColor();
Console.WriteLine(message);
};
var secrettext = "Hello, world!";
print(secrettext, "明文內容");
// 宣告記憶體緩衝區存放明文
var plain = new GpgmeMemoryData();
//plain.FileName = "my_document.txt";
// 建立 BinaryWriter 物件，以 UTF8 編碼寫入記憶體緩衝區
var binwriter = new BinaryWriter(plain, Encoding.UTF8);
binwriter.Write(secrettext.ToCharArray());
binwriter.Flush();
plain.Seek(0, SeekOrigin.Begin);
// 指定純文字輸出 (Armor)
ctx.Armor = true;
// 宣告記憶體緩衝區存放密文
var cipher = new GpgmeMemoryData();
//cipher.FileName = "my_document.txt";
ctx.Encrypt(new Key[] { key }, EncryptFlags.AlwaysTrust, plain, cipher);
// 顯示加密結果
cipher.Seek(0, SeekOrigin.Begin);
string encrypted;
using (var srEnc = new StreamReader(cipher, Encoding.UTF8)) {
encrypted = srEnc.ReadToEnd();
print(encrypted, "加密結果");
}
// 將密文串流指向開頭
var encData = new GpgmeMemoryData();
var encWriter = new BinaryWriter(encData, Encoding.UTF8);
encWriter.Write(encrypted.ToCharArray());
encData.Seek(0, SeekOrigin.Begin);
var decryptedData = new GpgmeMemoryData();
var decrst = ctx.Decrypt(encData, decryptedData);
// 讀取解密結果
decryptedData.Seek(0, SeekOrigin.Begin);
using var srDes = new StreamReader(decryptedData, Encoding.UTF8);
print(srDes.ReadToEnd(), "解密結果");
// 顯示加密金鑰資訊
Console.ForegroundColor = ConsoleColor.Cyan;
if (decrst.Recipients != null)
Console.WriteLine(
string.Join("
",
decrst.Recipients!
.Select(r =&gt; $"金鑰={r.KeyId} 加密演算法={Gpgme.GetPubkeyAlgoName(r.KeyAlgorithm)}").ToArray()));
Console.ResetColor();
</code></pre> <p>程式使用 gpgme-sharp 程式庫，先建立 Context 物件，從 KeyStore 以使用者名稱找到金鑰，進行加密。解密時由密文附加資訊識別為實體金鑰，要求 PIN 碼及按確認鈕才能解密。如此，一個用 C# 整合實體金鑰加解密的 PoC 就完成囉~</p> <p><a href="https://www.youtube.com/watch?v=GP3zQG-_cWw">C# GPG 解密實測</a></p> <p data-slug="gpg-encrypt-with-csharp" data-title-en="Using C# to decrypt with GPG USB Key Token" data-catg=".NET" data-fbimg="https://i.imgur.com/6qdjJS5.jpg#1280#720">Example of using gpgme-sharp library to encrypt and decrypt data with GPG USB tokey.</p>]]></content:encoded>
      
      <pubDate>Sat, 19 Aug 2023 14:11:03 GMT</pubDate>
    </item><item>
      <title><![CDATA[我的 Windows 新裝機 Chocolatey 安裝清單]]></title>
      <link>https://blog.darkthread.net/blog/choco-install-notes-for-new-windows/</link>
      <itunes:title><![CDATA[我的 Windows 新裝機 Chocolatey 安裝清單]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>近一年買了新主機跟開始玩 Azure，開 Hyper-V 或 Azure VM 裝 Windows 的頻率變高，裝好 OS 總有一堆基本工具要安裝，工作環境備妥才好做事。</p> <p>體驗過 Linux agt/yum 跟 <a href="/blog/chocolatey-scoop-winget/">Chocolatey CLI</a> 敲指令按 Enter 不必動滑鼠鍵盤程式便裝完程式的流暢，很難再回去 Google 查詢、下載執行安裝檔、下一步下一步、最後按結束的日子。我現在要裝程式，除了伺服器或特定軟體(像是 Wireshark 安裝檔會一併安裝 WinPCap 較方便)，清一色都是敲指令解決。(Windows 功能則用 PowerShell：<a href="/blog/setup-web-with-powershell-2/"> IIS 與 ASP.NET 網站自動安裝設定 - 使用 PowerShell</a>)</p> <p>由於每次要裝的軟體大同小異，為節省日後查詢工夫，特整理成小抄備用：<br> (項目將陸續補充，大家如果有常用的順手工具也可分享)</p> <ol><li>起手式，安裝 Chocolatey <pre><code>Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
</code></pre> </li> <li>Notepad++、7-Zip、.NET SDK (最新版本) <pre><code>choco install -y notepadplusplus
choco install -y 7zip
choco install -y dotnet-sdk
</code></pre> </li> <li>Git for Windows<br> <code>choco install -y git</code><br> 同場加映，設定使用者名稱及 Email <pre><code>git config -g user.name "你的名字"
git config -g user.email "信箱@mail-domain"
</code></pre> </li> <li>Cmder<br> <code>choco install -y cmder</code><br> Cmder 預設提示字元 λ 會導致<a href="/blog/cmder-lambda-symbol-issue/">中文排版錯亂</a>，開啟 Cmder 產生設定檔後使用以下 PowerShell 改成 $ <pre><code>(Get-Content -Raw 'C:\tools\Cmder\config\cmder_prompt_config.lua' -Encoding utf8).Replace('prompt_lambSymbol = "λ"', 'prompt_lambSymbol = "$"') | Out-File C:\tools\Cmder\config\cmder_prompt_config.lua -Encoding utf8
</code></pre> </li> <li>安裝 Edge/Chrome <pre><code>choco install -y microsoft-edge
choco install -y googlechrome
</code></pre> </li> <li>VSCode<br> <code>choco install -y vscode</code></li></ol> <p data-slug="choco-install-notes-for-new-windows" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960"> </p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>近一年買了新主機跟開始玩 Azure，開 Hyper-V 或 Azure VM 裝 Windows 的頻率變高，裝好 OS 總有一堆基本工具要安裝，工作環境備妥才好做事。</p> <p>體驗過 Linux agt/yum 跟 <a href="/blog/chocolatey-scoop-winget/">Chocolatey CLI</a> 敲指令按 Enter 不必動滑鼠鍵盤程式便裝完程式的流暢，很難再回去 Google 查詢、下載執行安裝檔、下一步下一步、最後按結束的日子。我現在要裝程式，除了伺服器或特定軟體(像是 Wireshark 安裝檔會一併安裝 WinPCap 較方便)，清一色都是敲指令解決。(Windows 功能則用 PowerShell：<a href="/blog/setup-web-with-powershell-2/"> IIS 與 ASP.NET 網站自動安裝設定 - 使用 PowerShell</a>)</p> <p>由於每次要裝的軟體大同小異，為節省日後查詢工夫，特整理成小抄備用：<br> (項目將陸續補充，大家如果有常用的順手工具也可分享)</p> <ol><li>起手式，安裝 Chocolatey <pre><code>Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
</code></pre> </li> <li>Notepad++、7-Zip、.NET SDK (最新版本) <pre><code>choco install -y notepadplusplus
choco install -y 7zip
choco install -y dotnet-sdk
</code></pre> </li> <li>Git for Windows<br> <code>choco install -y git</code><br> 同場加映，設定使用者名稱及 Email <pre><code>git config -g user.name "你的名字"
git config -g user.email "信箱@mail-domain"
</code></pre> </li> <li>Cmder<br> <code>choco install -y cmder</code><br> Cmder 預設提示字元 λ 會導致<a href="/blog/cmder-lambda-symbol-issue/">中文排版錯亂</a>，開啟 Cmder 產生設定檔後使用以下 PowerShell 改成 $ <pre><code>(Get-Content -Raw 'C:\tools\Cmder\config\cmder_prompt_config.lua' -Encoding utf8).Replace('prompt_lambSymbol = "λ"', 'prompt_lambSymbol = "$"') | Out-File C:\tools\Cmder\config\cmder_prompt_config.lua -Encoding utf8
</code></pre> </li> <li>安裝 Edge/Chrome <pre><code>choco install -y microsoft-edge
choco install -y googlechrome
</code></pre> </li> <li>VSCode<br> <code>choco install -y vscode</code></li></ol> <p data-slug="choco-install-notes-for-new-windows" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960"> </p>]]>
      </description>
      <content:encoded><![CDATA[<p>近一年買了新主機跟開始玩 Azure，開 Hyper-V 或 Azure VM 裝 Windows 的頻率變高，裝好 OS 總有一堆基本工具要安裝，工作環境備妥才好做事。</p> <p>體驗過 Linux agt/yum 跟 <a href="/blog/chocolatey-scoop-winget/">Chocolatey CLI</a> 敲指令按 Enter 不必動滑鼠鍵盤程式便裝完程式的流暢，很難再回去 Google 查詢、下載執行安裝檔、下一步下一步、最後按結束的日子。我現在要裝程式，除了伺服器或特定軟體(像是 Wireshark 安裝檔會一併安裝 WinPCap 較方便)，清一色都是敲指令解決。(Windows 功能則用 PowerShell：<a href="/blog/setup-web-with-powershell-2/"> IIS 與 ASP.NET 網站自動安裝設定 - 使用 PowerShell</a>)</p> <p>由於每次要裝的軟體大同小異，為節省日後查詢工夫，特整理成小抄備用：<br> (項目將陸續補充，大家如果有常用的順手工具也可分享)</p> <ol><li>起手式，安裝 Chocolatey <pre><code>Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
</code></pre> </li> <li>Notepad++、7-Zip、.NET SDK (最新版本) <pre><code>choco install -y notepadplusplus
choco install -y 7zip
choco install -y dotnet-sdk
</code></pre> </li> <li>Git for Windows<br> <code>choco install -y git</code><br> 同場加映，設定使用者名稱及 Email <pre><code>git config -g user.name "你的名字"
git config -g user.email "信箱@mail-domain"
</code></pre> </li> <li>Cmder<br> <code>choco install -y cmder</code><br> Cmder 預設提示字元 λ 會導致<a href="/blog/cmder-lambda-symbol-issue/">中文排版錯亂</a>，開啟 Cmder 產生設定檔後使用以下 PowerShell 改成 $ <pre><code>(Get-Content -Raw 'C:\tools\Cmder\config\cmder_prompt_config.lua' -Encoding utf8).Replace('prompt_lambSymbol = "λ"', 'prompt_lambSymbol = "$"') | Out-File C:\tools\Cmder\config\cmder_prompt_config.lua -Encoding utf8
</code></pre> </li> <li>安裝 Edge/Chrome <pre><code>choco install -y microsoft-edge
choco install -y googlechrome
</code></pre> </li> <li>VSCode<br> <code>choco install -y vscode</code></li></ol> <p data-slug="choco-install-notes-for-new-windows" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960"> </p>]]></content:encoded>
      
      <pubDate>Fri, 18 Aug 2023 14:28:10 GMT</pubDate>
    </item><item>
      <title><![CDATA[ChatGPT 聊天程式練習 - 使用 .NET + Azure OpenAI API]]></title>
      <link>https://blog.darkthread.net/blog/chatgpt-console-chat/</link>
      <itunes:title><![CDATA[ChatGPT 聊天程式練習 - 使用 .NET + Azure OpenAI API]]></itunes:title>
      <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
      <itunes:summary>
        <![CDATA[<p>半年過去，大家已學會平常心看待 ChatGPT，了解它的長處跟弱點，不再過度神化，什麼都問再靠北它瞎扯。我認為這才是面對 ChatGPT 的正確心態，認知到生成式 AI 的產出從來就不保證正確，需自負查核複檢之責，方能善用新科技提升競爭力，而不是亂用搞到<a href="https://news.ltn.com.tw/news/world/breakingnews/4316350">可能飯碗不保</a>。</p> <p>現在才開始學寫 ChatGPT 程式，明顯慢半拍，不過倒也符合我的風格 - 等技術成熟點再開始用，功能成熟穩定，學習資源多好上手，適合已跑不快的老人。舉個明顯的例子：Azure OpenAI Studio 在幾個月前還只有 JSON、Python、curl 範例，不提供 C#，現在已經有了 SDK 也有現成範例。</p> <p><img src="/Posts/files/2023/Fig1_638278830793134643.png"></p> <p>關於 Azure OpenAPI Service 的概念、申請、費用、Token 計算、開發... 大家若跟我一樣剛要下水，MVP <a href="https://dotblogs.com.tw/anyun">黯雲</a>有一系列文章是不錯的入門，建議可從這篇 <a href="https://dotblogs.com.tw/anyun/2023/03/11/184908">Azure OpenAI Service - Azure OpenAI Service 概觀</a>開始，裡面有所有文章的連結。</p> <p>基本上使用 ChatGPT API，可以接 OpenAI 提供的 API 也可以接 Azure OpenAI API，基本上兩個 API 規格相容，改 URL 跟 Api Key 就可切換來源。Azure 版本的好處是若企業已導入 Azure，Azure OpenAPI Service 在 SLA 保證、多區域備援、AD 整合、虛擬網路整合、合規要求上較符企業需要。至於個人使用者，若你手上有學生方案或 Visual Studio 訂閱，有一些免費額度可以玩(<a href="/blog/my-first-azure-ai-lab/">學生 100 USD/年、VS Enterprise 訂閱 150 USD/月</a>、或有信用卡註冊前 30 天 200 USD 額度，等於可以免費玩玩 ChatGPT API，這些是值得試試 Azure OpenAI Service 的理由。</p> <p>不過 Azure OpenAI Service 採取申請審核制(理由是要管控 AI 技術不被誤用濫用或惡意使用)，需用組織、公司信箱(不接受 Hotmail、Outlook、Gmail 等免費個人信箱)提出申請，網路上成功分享蠻多的，感覺不難通過申請，學生用學校名義即可，總之試試無妨。(不知是否因為 AI 熱潮已退，現在核准很快，我不到一個工作天便收到回覆)</p> <p>但有個壞消息，申請核准後只會有 ChatGPT 3.5，想用 ChatGPT 4 需另外再申請，得乖乖排隊。<br> (David 老師前幾天開心分享他<a href="https://www.facebook.com/DotNetWalker/posts/pfbid02r7BcYAntbLknzCBA1FMFSci6AahA8NAB4forUMTVFGVKtohEhGL8NqeKgtZFJXYBl">排超久終於輪到了</a>，登楞! 上週才提申請的我應該還有得等...)</p> <p><img src="/Posts/files/2023/Fig2_638278830797421959.png"></p> <p>ChatGPT 3.5 或 4 只是模型不同，程式寫法一樣，不妨礙學習，那就來個簡單練習：用 C# 呼叫 Azure OpenAI API 寫一個可以跟 ChatGPT 聊天的 .NET Console 程式。</p> <p>我開了一個 .NET 7 Console 專案，用 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 加入 Beta 版 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai?WT.mc_id=DOP-MVP-37580">OpenAI API 程式庫</a>。參考 Azure OpenAI Studio 遊樂場給的 C# 範例，先寫好服務類別。</p> <p>要跟 ChatGPT 聊天時，若要 ChatGPT 記得先前的對話內容繼續延伸，需將先前雙方的對話內容以 ChatMessage 物件形式當參數傳給 ChatGPT。每個 ChatMessage 用 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai.chatrole?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">ChatRole</a> 識別為 System (初始化提示，定義 ChatGPT 扮演角色)、User (使用者)、Assistant (ChatGPT 的回應內容)、<a href="https://dotblogs.com.tw/anyun/2023/07/23/171942">Function Calling</a> (完成函式)。</p> <p>由於呼叫傳入的所有對話內容會拆分成 Token <a href="https://dotblogs.com.tw/anyun/2023/03/12/180209">參考</a>，而一次所能傳入的 Token 數量有上限且要算錢，故不能無限累積所有對話內容，一般常見做法是取最近的 N 則。我採用的實作方法是宣告一個自訂類別 ChatRecord 儲存對話內容，使用 <code>Queue&lt;ChatRecord&gt;</code> 保存對話過程，當數量大於 10 筆時，捨棄最早的內容直到筆數等於 10。(更精緻的做法應是請 ChatGPT 摘要先前對話濃縮成簡短大綱)</p> <p>ChatGPT 生成內容有很多參數可以調，例如：回應字數上限、溫度、頂端 P (類似溫度可控制隨機性)、頻率罰則 及 目前狀態罰則 (用來減少文字重複性)... 等，剛開始學走路就都用預設值吧。</p> <p>生成結果回傳方式有兩種，<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsAsync()</a> 是全部生成好再一次回傳、<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsstreamingasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsStreamingAsync()</a> 則是產生過程以串流方式傳回。</p> <p>至於結果，型別為 ChatCompletions，Choices 為多個聊天回覆選項的集合，從中擇一(我用 .First()) 取 .Message.Content 即為回覆文字。</p> <p>我先從簡單的同步回傳開始，以下是一次取回結果的版本：</p> <pre><code>using Azure;
using Azure.AI.OpenAI;
public class ChatRecord
{
public DateTime Time { get; set; }
public string Role { get; set; } // U-User, A-Assistant
public string Message { get; set;}
public ChatRecord(string msg, string role = "U") {
this.Message = msg;
this.Role = role;
}
}
public class ChatGptService
{
private readonly string apiUrl;
private readonly string apiKey;
private readonly string deployName;
OpenAIClient client;
public float Temperature = (float)0.7;
public int MaxTokens = 800;
public float NucleusSamplingFactor = (float)0.95;
public int FrequencyPenalty = 0;
public int PresencePenalty = 0;
public string SystemPrompt = "你是一名 AI 助理，使用繁體中文提供解答";
public ChatGptService(string apiUrl, string apiKey, string deployName)
{
this.apiUrl = apiUrl;
this.apiKey = apiKey;
this.deployName = deployName;
client = new OpenAIClient(
new Uri(apiUrl),
new AzureKeyCredential(apiKey));
}
public async Task&lt;string&gt; Generate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;ChatCompletions&gt; response = await client.GetChatCompletionsAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
ChatCompletions completions = response.Value;
return completions.Choices.First().Message.Content;
}
}
</code></pre> <p>Console 輸入流程我簡單搭一下，能動就好。裡面有用上昨天介紹的<a href="/blog/secure-apikey-for-console-app/">環境變數保密儲存技巧</a>保存 API Key，ChatGPT 回應需要時間，我用了 <a href="/blog/tpl-threadpool-usage/">Task.Run() + CancellationToken</a> 技巧在等待過程印出 ... 減少使用者焦慮(謎：只有你這種急性子才需要吧？)，程式範例如下：</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Security.Cryptography;
using System.Text.RegularExpressions;
Func&lt;string, string&gt; GetOrSetEnvVar = (varName) =&gt;
{
var val = Environment.GetEnvironmentVariable(varName, EnvironmentVariableTarget.User);
if (!string.IsNullOrEmpty(val))
{
try
{
val = Encoding.UTF8.GetString(
ProtectedData.Unprotect(Convert.FromBase64String(val), null, DataProtectionScope.CurrentUser));
return val;
}
catch
{
Console.WriteLine("非有效加密格式，請重新輸入");
}
}
Console.Write($"請設定[{varName}]：");
val = Console.ReadLine() ?? string.Empty;
//加密後存入環境變數
var enc =
Convert.ToBase64String(
ProtectedData.Protect(Encoding.UTF8.GetBytes(val), null, DataProtectionScope.CurrentUser));
Environment.SetEnvironmentVariable(varName, enc, EnvironmentVariableTarget.User);
return val;
};
var apiUrl = GetOrSetEnvVar("OpenAI_Url");
var apiKey = GetOrSetEnvVar("OpenAI_Key");
var deployName = "GPT35-16K";
var chatSvc = new ChatGptService(apiUrl, apiKey, deployName);
var context = new Queue&lt;ChatRecord&gt;();
var sb = new StringBuilder();
Console.WriteLine("ChatGPT API 練習");
Console.WriteLine("/new 開始新對話、/quit 結束程式");
Action showPrompt = () =&gt;
{
Console.ForegroundColor = ConsoleColor.Yellow;
Console.Write("&gt;");
};
showPrompt();
while (true)
{
var line = Console.ReadLine();
if (line == "/quit")
{
Print("再見，下次再聊", ConsoleColor.Green);
Console.ResetColor();
break;
}
else if (line == "/new")
{
Print("清空對話，重新開始", ConsoleColor.Magenta);
context.Clear();
}
else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S"))
{
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
Console.ForegroundColor = ConsoleColor.Cyan;
Console.Write("傳送中，請稍侯");
var cts = new CancellationTokenSource();
var cancelToken = cts.Token;
var progress = Task.Run(async () =&gt;
{
while (!cancelToken.IsCancellationRequested)
{
await Task.Delay(500, cancelToken);
Console.Write(".");
}
}, cancelToken);
var ts = new Stopwatch();
ts.Start();
var res = await chatSvc.Generate(context);
ts.Stop();
// 將回答也納入上下文
context.Enqueue(new ChatRecord(res, "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
// 印出回答
cts.Cancel();
Console.WriteLine($" {ts.ElapsedMilliseconds:n0}ms");
Print(res, ConsoleColor.White);
}
else
{
sb.AppendLine(line);
}
}
void Print(string msg, ConsoleColor color = ConsoleColor.White)
{
Console.ForegroundColor = color;
Console.WriteLine(msg);
showPrompt();
}
</code></pre> <p>實測，由於我們有傳送先前對話內容，如下圖所示，在問完 JavaScript 排序後，加問一句 "那 C# 呢？"，ChatGPT 便知道是同一個題目改用 C# 解：</p> <p><img src="/Posts/files/2023/Fig3_638278830800892212.png"></p> <p>但由以上測試會發現，使用者需要乾等 9 秒及 6 秒才會看到結果，而不像 ChatGPT 網站會分段輸出。而 Azure OpenAI API 程式庫也有提供串流形式的回傳方式。我們修改 ChatGptService 加入一個 版本：</p> <pre><code>    public async IAsyncEnumerable&lt;String&gt; StreamingGenerate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;StreamingChatCompletions&gt; response = await client.GetChatCompletionsStreamingAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
// https://blog.darkthread.net/blog/iasyncenumerable-in-mvc/
await foreach (var choice in  response.Value.GetChoicesStreaming()) {
await foreach (var msg in choice.GetMessageStreaming()) {
yield return msg.Content;
}
}
}
</code></pre> <p>GetChatCompletionsStreamingAsync() 傳回的 StreamingChatCompletions 型別，Choice 是以 <code>IAsyncEnumerable&lt;StreamingChatChoice&gt;</code> 形式傳回，而其下的 Message 則是以 <code>IAsyncEnumerable&lt;ChatMessage&gt;</code> 形式傳回，幸好之前研究過 <a href="/blog/iasyncenumerable-in-mvc/">ASP.NET Core IAsyncEnumerable 與 yield return</a>，現在派上用場，我用兩層 await foreach 加 yield return 接收，這個串流版函式的回傳型別也要改成 <code>IAsyncEnumerable&lt;String&gt;</code>，再次驗證了 <a href="/blog/async-aspnet/">async 病毒傳染性</a>。</p> <p>由於 ChatGPT 的回答是以 <code>IAsyncEnumerable&lt;String&gt;</code> 形式分批傳回，呼叫端這裡也要小小改寫：</p> <pre><code>    else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S")) {
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
sb.Clear();
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine("傳送中，請稍侯...");
Console.ForegroundColor = ConsoleColor.White;
await foreach (var msg in chatSvc.StreamingGenerate(context)) {
sb.Append(msg);
Console.Write(msg);
}
// 將回答也納入上下文
context.Enqueue(new ChatRecord(sb.ToString(), "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
Print("");
}
</code></pre> <p>改為串流後，回答會分段顯示出來(雖然分批間隔有點久)，接近 ChatGPT 網頁的操作體驗，比空等十秒再一次冒出結果順暢一點。</p> <p><img src="/Posts/files/2023/Fig4_638278830805919648.gif"></p> <p>練習完畢。</p> <p data-slug="chatgpt-console-chat" data-title-en="Writing a console interface ChatGPT" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/128880/parachute-skydiving-parachuting-jumping-128880.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Example of using Azure OpenAI ChatGPT model to implement a simple console chat interface.</p>]]>
      </itunes:summary>
      <description>
        <![CDATA[<p>半年過去，大家已學會平常心看待 ChatGPT，了解它的長處跟弱點，不再過度神化，什麼都問再靠北它瞎扯。我認為這才是面對 ChatGPT 的正確心態，認知到生成式 AI 的產出從來就不保證正確，需自負查核複檢之責，方能善用新科技提升競爭力，而不是亂用搞到<a href="https://news.ltn.com.tw/news/world/breakingnews/4316350">可能飯碗不保</a>。</p> <p>現在才開始學寫 ChatGPT 程式，明顯慢半拍，不過倒也符合我的風格 - 等技術成熟點再開始用，功能成熟穩定，學習資源多好上手，適合已跑不快的老人。舉個明顯的例子：Azure OpenAI Studio 在幾個月前還只有 JSON、Python、curl 範例，不提供 C#，現在已經有了 SDK 也有現成範例。</p> <p><img src="/Posts/files/2023/Fig1_638278830793134643.png"></p> <p>關於 Azure OpenAPI Service 的概念、申請、費用、Token 計算、開發... 大家若跟我一樣剛要下水，MVP <a href="https://dotblogs.com.tw/anyun">黯雲</a>有一系列文章是不錯的入門，建議可從這篇 <a href="https://dotblogs.com.tw/anyun/2023/03/11/184908">Azure OpenAI Service - Azure OpenAI Service 概觀</a>開始，裡面有所有文章的連結。</p> <p>基本上使用 ChatGPT API，可以接 OpenAI 提供的 API 也可以接 Azure OpenAI API，基本上兩個 API 規格相容，改 URL 跟 Api Key 就可切換來源。Azure 版本的好處是若企業已導入 Azure，Azure OpenAPI Service 在 SLA 保證、多區域備援、AD 整合、虛擬網路整合、合規要求上較符企業需要。至於個人使用者，若你手上有學生方案或 Visual Studio 訂閱，有一些免費額度可以玩(<a href="/blog/my-first-azure-ai-lab/">學生 100 USD/年、VS Enterprise 訂閱 150 USD/月</a>、或有信用卡註冊前 30 天 200 USD 額度，等於可以免費玩玩 ChatGPT API，這些是值得試試 Azure OpenAI Service 的理由。</p> <p>不過 Azure OpenAI Service 採取申請審核制(理由是要管控 AI 技術不被誤用濫用或惡意使用)，需用組織、公司信箱(不接受 Hotmail、Outlook、Gmail 等免費個人信箱)提出申請，網路上成功分享蠻多的，感覺不難通過申請，學生用學校名義即可，總之試試無妨。(不知是否因為 AI 熱潮已退，現在核准很快，我不到一個工作天便收到回覆)</p> <p>但有個壞消息，申請核准後只會有 ChatGPT 3.5，想用 ChatGPT 4 需另外再申請，得乖乖排隊。<br> (David 老師前幾天開心分享他<a href="https://www.facebook.com/DotNetWalker/posts/pfbid02r7BcYAntbLknzCBA1FMFSci6AahA8NAB4forUMTVFGVKtohEhGL8NqeKgtZFJXYBl">排超久終於輪到了</a>，登楞! 上週才提申請的我應該還有得等...)</p> <p><img src="/Posts/files/2023/Fig2_638278830797421959.png"></p> <p>ChatGPT 3.5 或 4 只是模型不同，程式寫法一樣，不妨礙學習，那就來個簡單練習：用 C# 呼叫 Azure OpenAI API 寫一個可以跟 ChatGPT 聊天的 .NET Console 程式。</p> <p>我開了一個 .NET 7 Console 專案，用 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 加入 Beta 版 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai?WT.mc_id=DOP-MVP-37580">OpenAI API 程式庫</a>。參考 Azure OpenAI Studio 遊樂場給的 C# 範例，先寫好服務類別。</p> <p>要跟 ChatGPT 聊天時，若要 ChatGPT 記得先前的對話內容繼續延伸，需將先前雙方的對話內容以 ChatMessage 物件形式當參數傳給 ChatGPT。每個 ChatMessage 用 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai.chatrole?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">ChatRole</a> 識別為 System (初始化提示，定義 ChatGPT 扮演角色)、User (使用者)、Assistant (ChatGPT 的回應內容)、<a href="https://dotblogs.com.tw/anyun/2023/07/23/171942">Function Calling</a> (完成函式)。</p> <p>由於呼叫傳入的所有對話內容會拆分成 Token <a href="https://dotblogs.com.tw/anyun/2023/03/12/180209">參考</a>，而一次所能傳入的 Token 數量有上限且要算錢，故不能無限累積所有對話內容，一般常見做法是取最近的 N 則。我採用的實作方法是宣告一個自訂類別 ChatRecord 儲存對話內容，使用 <code>Queue&lt;ChatRecord&gt;</code> 保存對話過程，當數量大於 10 筆時，捨棄最早的內容直到筆數等於 10。(更精緻的做法應是請 ChatGPT 摘要先前對話濃縮成簡短大綱)</p> <p>ChatGPT 生成內容有很多參數可以調，例如：回應字數上限、溫度、頂端 P (類似溫度可控制隨機性)、頻率罰則 及 目前狀態罰則 (用來減少文字重複性)... 等，剛開始學走路就都用預設值吧。</p> <p>生成結果回傳方式有兩種，<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsAsync()</a> 是全部生成好再一次回傳、<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsstreamingasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsStreamingAsync()</a> 則是產生過程以串流方式傳回。</p> <p>至於結果，型別為 ChatCompletions，Choices 為多個聊天回覆選項的集合，從中擇一(我用 .First()) 取 .Message.Content 即為回覆文字。</p> <p>我先從簡單的同步回傳開始，以下是一次取回結果的版本：</p> <pre><code>using Azure;
using Azure.AI.OpenAI;
public class ChatRecord
{
public DateTime Time { get; set; }
public string Role { get; set; } // U-User, A-Assistant
public string Message { get; set;}
public ChatRecord(string msg, string role = "U") {
this.Message = msg;
this.Role = role;
}
}
public class ChatGptService
{
private readonly string apiUrl;
private readonly string apiKey;
private readonly string deployName;
OpenAIClient client;
public float Temperature = (float)0.7;
public int MaxTokens = 800;
public float NucleusSamplingFactor = (float)0.95;
public int FrequencyPenalty = 0;
public int PresencePenalty = 0;
public string SystemPrompt = "你是一名 AI 助理，使用繁體中文提供解答";
public ChatGptService(string apiUrl, string apiKey, string deployName)
{
this.apiUrl = apiUrl;
this.apiKey = apiKey;
this.deployName = deployName;
client = new OpenAIClient(
new Uri(apiUrl),
new AzureKeyCredential(apiKey));
}
public async Task&lt;string&gt; Generate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;ChatCompletions&gt; response = await client.GetChatCompletionsAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
ChatCompletions completions = response.Value;
return completions.Choices.First().Message.Content;
}
}
</code></pre> <p>Console 輸入流程我簡單搭一下，能動就好。裡面有用上昨天介紹的<a href="/blog/secure-apikey-for-console-app/">環境變數保密儲存技巧</a>保存 API Key，ChatGPT 回應需要時間，我用了 <a href="/blog/tpl-threadpool-usage/">Task.Run() + CancellationToken</a> 技巧在等待過程印出 ... 減少使用者焦慮(謎：只有你這種急性子才需要吧？)，程式範例如下：</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Security.Cryptography;
using System.Text.RegularExpressions;
Func&lt;string, string&gt; GetOrSetEnvVar = (varName) =&gt;
{
var val = Environment.GetEnvironmentVariable(varName, EnvironmentVariableTarget.User);
if (!string.IsNullOrEmpty(val))
{
try
{
val = Encoding.UTF8.GetString(
ProtectedData.Unprotect(Convert.FromBase64String(val), null, DataProtectionScope.CurrentUser));
return val;
}
catch
{
Console.WriteLine("非有效加密格式，請重新輸入");
}
}
Console.Write($"請設定[{varName}]：");
val = Console.ReadLine() ?? string.Empty;
//加密後存入環境變數
var enc =
Convert.ToBase64String(
ProtectedData.Protect(Encoding.UTF8.GetBytes(val), null, DataProtectionScope.CurrentUser));
Environment.SetEnvironmentVariable(varName, enc, EnvironmentVariableTarget.User);
return val;
};
var apiUrl = GetOrSetEnvVar("OpenAI_Url");
var apiKey = GetOrSetEnvVar("OpenAI_Key");
var deployName = "GPT35-16K";
var chatSvc = new ChatGptService(apiUrl, apiKey, deployName);
var context = new Queue&lt;ChatRecord&gt;();
var sb = new StringBuilder();
Console.WriteLine("ChatGPT API 練習");
Console.WriteLine("/new 開始新對話、/quit 結束程式");
Action showPrompt = () =&gt;
{
Console.ForegroundColor = ConsoleColor.Yellow;
Console.Write("&gt;");
};
showPrompt();
while (true)
{
var line = Console.ReadLine();
if (line == "/quit")
{
Print("再見，下次再聊", ConsoleColor.Green);
Console.ResetColor();
break;
}
else if (line == "/new")
{
Print("清空對話，重新開始", ConsoleColor.Magenta);
context.Clear();
}
else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S"))
{
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
Console.ForegroundColor = ConsoleColor.Cyan;
Console.Write("傳送中，請稍侯");
var cts = new CancellationTokenSource();
var cancelToken = cts.Token;
var progress = Task.Run(async () =&gt;
{
while (!cancelToken.IsCancellationRequested)
{
await Task.Delay(500, cancelToken);
Console.Write(".");
}
}, cancelToken);
var ts = new Stopwatch();
ts.Start();
var res = await chatSvc.Generate(context);
ts.Stop();
// 將回答也納入上下文
context.Enqueue(new ChatRecord(res, "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
// 印出回答
cts.Cancel();
Console.WriteLine($" {ts.ElapsedMilliseconds:n0}ms");
Print(res, ConsoleColor.White);
}
else
{
sb.AppendLine(line);
}
}
void Print(string msg, ConsoleColor color = ConsoleColor.White)
{
Console.ForegroundColor = color;
Console.WriteLine(msg);
showPrompt();
}
</code></pre> <p>實測，由於我們有傳送先前對話內容，如下圖所示，在問完 JavaScript 排序後，加問一句 "那 C# 呢？"，ChatGPT 便知道是同一個題目改用 C# 解：</p> <p><img src="/Posts/files/2023/Fig3_638278830800892212.png"></p> <p>但由以上測試會發現，使用者需要乾等 9 秒及 6 秒才會看到結果，而不像 ChatGPT 網站會分段輸出。而 Azure OpenAI API 程式庫也有提供串流形式的回傳方式。我們修改 ChatGptService 加入一個 版本：</p> <pre><code>    public async IAsyncEnumerable&lt;String&gt; StreamingGenerate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;StreamingChatCompletions&gt; response = await client.GetChatCompletionsStreamingAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
// https://blog.darkthread.net/blog/iasyncenumerable-in-mvc/
await foreach (var choice in  response.Value.GetChoicesStreaming()) {
await foreach (var msg in choice.GetMessageStreaming()) {
yield return msg.Content;
}
}
}
</code></pre> <p>GetChatCompletionsStreamingAsync() 傳回的 StreamingChatCompletions 型別，Choice 是以 <code>IAsyncEnumerable&lt;StreamingChatChoice&gt;</code> 形式傳回，而其下的 Message 則是以 <code>IAsyncEnumerable&lt;ChatMessage&gt;</code> 形式傳回，幸好之前研究過 <a href="/blog/iasyncenumerable-in-mvc/">ASP.NET Core IAsyncEnumerable 與 yield return</a>，現在派上用場，我用兩層 await foreach 加 yield return 接收，這個串流版函式的回傳型別也要改成 <code>IAsyncEnumerable&lt;String&gt;</code>，再次驗證了 <a href="/blog/async-aspnet/">async 病毒傳染性</a>。</p> <p>由於 ChatGPT 的回答是以 <code>IAsyncEnumerable&lt;String&gt;</code> 形式分批傳回，呼叫端這裡也要小小改寫：</p> <pre><code>    else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S")) {
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
sb.Clear();
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine("傳送中，請稍侯...");
Console.ForegroundColor = ConsoleColor.White;
await foreach (var msg in chatSvc.StreamingGenerate(context)) {
sb.Append(msg);
Console.Write(msg);
}
// 將回答也納入上下文
context.Enqueue(new ChatRecord(sb.ToString(), "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
Print("");
}
</code></pre> <p>改為串流後，回答會分段顯示出來(雖然分批間隔有點久)，接近 ChatGPT 網頁的操作體驗，比空等十秒再一次冒出結果順暢一點。</p> <p><img src="/Posts/files/2023/Fig4_638278830805919648.gif"></p> <p>練習完畢。</p> <p data-slug="chatgpt-console-chat" data-title-en="Writing a console interface ChatGPT" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/128880/parachute-skydiving-parachuting-jumping-128880.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Example of using Azure OpenAI ChatGPT model to implement a simple console chat interface.</p>]]>
      </description>
      <content:encoded><![CDATA[<p>半年過去，大家已學會平常心看待 ChatGPT，了解它的長處跟弱點，不再過度神化，什麼都問再靠北它瞎扯。我認為這才是面對 ChatGPT 的正確心態，認知到生成式 AI 的產出從來就不保證正確，需自負查核複檢之責，方能善用新科技提升競爭力，而不是亂用搞到<a href="https://news.ltn.com.tw/news/world/breakingnews/4316350">可能飯碗不保</a>。</p> <p>現在才開始學寫 ChatGPT 程式，明顯慢半拍，不過倒也符合我的風格 - 等技術成熟點再開始用，功能成熟穩定，學習資源多好上手，適合已跑不快的老人。舉個明顯的例子：Azure OpenAI Studio 在幾個月前還只有 JSON、Python、curl 範例，不提供 C#，現在已經有了 SDK 也有現成範例。</p> <p><img src="/Posts/files/2023/Fig1_638278830793134643.png"></p> <p>關於 Azure OpenAPI Service 的概念、申請、費用、Token 計算、開發... 大家若跟我一樣剛要下水，MVP <a href="https://dotblogs.com.tw/anyun">黯雲</a>有一系列文章是不錯的入門，建議可從這篇 <a href="https://dotblogs.com.tw/anyun/2023/03/11/184908">Azure OpenAI Service - Azure OpenAI Service 概觀</a>開始，裡面有所有文章的連結。</p> <p>基本上使用 ChatGPT API，可以接 OpenAI 提供的 API 也可以接 Azure OpenAI API，基本上兩個 API 規格相容，改 URL 跟 Api Key 就可切換來源。Azure 版本的好處是若企業已導入 Azure，Azure OpenAPI Service 在 SLA 保證、多區域備援、AD 整合、虛擬網路整合、合規要求上較符企業需要。至於個人使用者，若你手上有學生方案或 Visual Studio 訂閱，有一些免費額度可以玩(<a href="/blog/my-first-azure-ai-lab/">學生 100 USD/年、VS Enterprise 訂閱 150 USD/月</a>、或有信用卡註冊前 30 天 200 USD 額度，等於可以免費玩玩 ChatGPT API，這些是值得試試 Azure OpenAI Service 的理由。</p> <p>不過 Azure OpenAI Service 採取申請審核制(理由是要管控 AI 技術不被誤用濫用或惡意使用)，需用組織、公司信箱(不接受 Hotmail、Outlook、Gmail 等免費個人信箱)提出申請，網路上成功分享蠻多的，感覺不難通過申請，學生用學校名義即可，總之試試無妨。(不知是否因為 AI 熱潮已退，現在核准很快，我不到一個工作天便收到回覆)</p> <p>但有個壞消息，申請核准後只會有 ChatGPT 3.5，想用 ChatGPT 4 需另外再申請，得乖乖排隊。<br> (David 老師前幾天開心分享他<a href="https://www.facebook.com/DotNetWalker/posts/pfbid02r7BcYAntbLknzCBA1FMFSci6AahA8NAB4forUMTVFGVKtohEhGL8NqeKgtZFJXYBl">排超久終於輪到了</a>，登楞! 上週才提申請的我應該還有得等...)</p> <p><img src="/Posts/files/2023/Fig2_638278830797421959.png"></p> <p>ChatGPT 3.5 或 4 只是模型不同，程式寫法一樣，不妨礙學習，那就來個簡單練習：用 C# 呼叫 Azure OpenAI API 寫一個可以跟 ChatGPT 聊天的 .NET Console 程式。</p> <p>我開了一個 .NET 7 Console 專案，用 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 加入 Beta 版 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai?WT.mc_id=DOP-MVP-37580">OpenAI API 程式庫</a>。參考 Azure OpenAI Studio 遊樂場給的 C# 範例，先寫好服務類別。</p> <p>要跟 ChatGPT 聊天時，若要 ChatGPT 記得先前的對話內容繼續延伸，需將先前雙方的對話內容以 ChatMessage 物件形式當參數傳給 ChatGPT。每個 ChatMessage 用 <a href="https://learn.microsoft.com/en-us/dotnet/api/azure.ai.openai.chatrole?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">ChatRole</a> 識別為 System (初始化提示，定義 ChatGPT 扮演角色)、User (使用者)、Assistant (ChatGPT 的回應內容)、<a href="https://dotblogs.com.tw/anyun/2023/07/23/171942">Function Calling</a> (完成函式)。</p> <p>由於呼叫傳入的所有對話內容會拆分成 Token <a href="https://dotblogs.com.tw/anyun/2023/03/12/180209">參考</a>，而一次所能傳入的 Token 數量有上限且要算錢，故不能無限累積所有對話內容，一般常見做法是取最近的 N 則。我採用的實作方法是宣告一個自訂類別 ChatRecord 儲存對話內容，使用 <code>Queue&lt;ChatRecord&gt;</code> 保存對話過程，當數量大於 10 筆時，捨棄最早的內容直到筆數等於 10。(更精緻的做法應是請 ChatGPT 摘要先前對話濃縮成簡短大綱)</p> <p>ChatGPT 生成內容有很多參數可以調，例如：回應字數上限、溫度、頂端 P (類似溫度可控制隨機性)、頻率罰則 及 目前狀態罰則 (用來減少文字重複性)... 等，剛開始學走路就都用預設值吧。</p> <p>生成結果回傳方式有兩種，<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsAsync()</a> 是全部生成好再一次回傳、<a href="https://learn.microsoft.com/zh-tw/dotnet/api/azure.ai.openai.openaiclient.getchatcompletionsstreamingasync?view=azure-dotnet-preview&amp;WT.mc_id=DOP-MVP-37580">GetChatCompletionsStreamingAsync()</a> 則是產生過程以串流方式傳回。</p> <p>至於結果，型別為 ChatCompletions，Choices 為多個聊天回覆選項的集合，從中擇一(我用 .First()) 取 .Message.Content 即為回覆文字。</p> <p>我先從簡單的同步回傳開始，以下是一次取回結果的版本：</p> <pre><code>using Azure;
using Azure.AI.OpenAI;
public class ChatRecord
{
public DateTime Time { get; set; }
public string Role { get; set; } // U-User, A-Assistant
public string Message { get; set;}
public ChatRecord(string msg, string role = "U") {
this.Message = msg;
this.Role = role;
}
}
public class ChatGptService
{
private readonly string apiUrl;
private readonly string apiKey;
private readonly string deployName;
OpenAIClient client;
public float Temperature = (float)0.7;
public int MaxTokens = 800;
public float NucleusSamplingFactor = (float)0.95;
public int FrequencyPenalty = 0;
public int PresencePenalty = 0;
public string SystemPrompt = "你是一名 AI 助理，使用繁體中文提供解答";
public ChatGptService(string apiUrl, string apiKey, string deployName)
{
this.apiUrl = apiUrl;
this.apiKey = apiKey;
this.deployName = deployName;
client = new OpenAIClient(
new Uri(apiUrl),
new AzureKeyCredential(apiKey));
}
public async Task&lt;string&gt; Generate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;ChatCompletions&gt; response = await client.GetChatCompletionsAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
ChatCompletions completions = response.Value;
return completions.Choices.First().Message.Content;
}
}
</code></pre> <p>Console 輸入流程我簡單搭一下，能動就好。裡面有用上昨天介紹的<a href="/blog/secure-apikey-for-console-app/">環境變數保密儲存技巧</a>保存 API Key，ChatGPT 回應需要時間，我用了 <a href="/blog/tpl-threadpool-usage/">Task.Run() + CancellationToken</a> 技巧在等待過程印出 ... 減少使用者焦慮(謎：只有你這種急性子才需要吧？)，程式範例如下：</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Security.Cryptography;
using System.Text.RegularExpressions;
Func&lt;string, string&gt; GetOrSetEnvVar = (varName) =&gt;
{
var val = Environment.GetEnvironmentVariable(varName, EnvironmentVariableTarget.User);
if (!string.IsNullOrEmpty(val))
{
try
{
val = Encoding.UTF8.GetString(
ProtectedData.Unprotect(Convert.FromBase64String(val), null, DataProtectionScope.CurrentUser));
return val;
}
catch
{
Console.WriteLine("非有效加密格式，請重新輸入");
}
}
Console.Write($"請設定[{varName}]：");
val = Console.ReadLine() ?? string.Empty;
//加密後存入環境變數
var enc =
Convert.ToBase64String(
ProtectedData.Protect(Encoding.UTF8.GetBytes(val), null, DataProtectionScope.CurrentUser));
Environment.SetEnvironmentVariable(varName, enc, EnvironmentVariableTarget.User);
return val;
};
var apiUrl = GetOrSetEnvVar("OpenAI_Url");
var apiKey = GetOrSetEnvVar("OpenAI_Key");
var deployName = "GPT35-16K";
var chatSvc = new ChatGptService(apiUrl, apiKey, deployName);
var context = new Queue&lt;ChatRecord&gt;();
var sb = new StringBuilder();
Console.WriteLine("ChatGPT API 練習");
Console.WriteLine("/new 開始新對話、/quit 結束程式");
Action showPrompt = () =&gt;
{
Console.ForegroundColor = ConsoleColor.Yellow;
Console.Write("&gt;");
};
showPrompt();
while (true)
{
var line = Console.ReadLine();
if (line == "/quit")
{
Print("再見，下次再聊", ConsoleColor.Green);
Console.ResetColor();
break;
}
else if (line == "/new")
{
Print("清空對話，重新開始", ConsoleColor.Magenta);
context.Clear();
}
else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S"))
{
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
Console.ForegroundColor = ConsoleColor.Cyan;
Console.Write("傳送中，請稍侯");
var cts = new CancellationTokenSource();
var cancelToken = cts.Token;
var progress = Task.Run(async () =&gt;
{
while (!cancelToken.IsCancellationRequested)
{
await Task.Delay(500, cancelToken);
Console.Write(".");
}
}, cancelToken);
var ts = new Stopwatch();
ts.Start();
var res = await chatSvc.Generate(context);
ts.Stop();
// 將回答也納入上下文
context.Enqueue(new ChatRecord(res, "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
// 印出回答
cts.Cancel();
Console.WriteLine($" {ts.ElapsedMilliseconds:n0}ms");
Print(res, ConsoleColor.White);
}
else
{
sb.AppendLine(line);
}
}
void Print(string msg, ConsoleColor color = ConsoleColor.White)
{
Console.ForegroundColor = color;
Console.WriteLine(msg);
showPrompt();
}
</code></pre> <p>實測，由於我們有傳送先前對話內容，如下圖所示，在問完 JavaScript 排序後，加問一句 "那 C# 呢？"，ChatGPT 便知道是同一個題目改用 C# 解：</p> <p><img src="/Posts/files/2023/Fig3_638278830800892212.png"></p> <p>但由以上測試會發現，使用者需要乾等 9 秒及 6 秒才會看到結果，而不像 ChatGPT 網站會分段輸出。而 Azure OpenAI API 程式庫也有提供串流形式的回傳方式。我們修改 ChatGptService 加入一個 版本：</p> <pre><code>    public async IAsyncEnumerable&lt;String&gt; StreamingGenerate(IEnumerable&lt;ChatRecord&gt; contextMessags)
{
var chatMessages = new List&lt;ChatMessage&gt;() {
new ChatMessage(ChatRole.System, SystemPrompt)
};
chatMessages.AddRange(contextMessags.Select(m =&gt; new ChatMessage(
m.Role == "A" ? ChatRole.Assistant : ChatRole.User,
m.Message
)));
Response&lt;StreamingChatCompletions&gt; response = await client.GetChatCompletionsStreamingAsync(
deploymentOrModelName: deployName,
new ChatCompletionsOptions(chatMessages)
{
Temperature = Temperature,
MaxTokens = MaxTokens,
NucleusSamplingFactor = NucleusSamplingFactor,
FrequencyPenalty = FrequencyPenalty,
PresencePenalty = PresencePenalty
});
// https://blog.darkthread.net/blog/iasyncenumerable-in-mvc/
await foreach (var choice in  response.Value.GetChoicesStreaming()) {
await foreach (var msg in choice.GetMessageStreaming()) {
yield return msg.Content;
}
}
}
</code></pre> <p>GetChatCompletionsStreamingAsync() 傳回的 StreamingChatCompletions 型別，Choice 是以 <code>IAsyncEnumerable&lt;StreamingChatChoice&gt;</code> 形式傳回，而其下的 Message 則是以 <code>IAsyncEnumerable&lt;ChatMessage&gt;</code> 形式傳回，幸好之前研究過 <a href="/blog/iasyncenumerable-in-mvc/">ASP.NET Core IAsyncEnumerable 與 yield return</a>，現在派上用場，我用兩層 await foreach 加 yield return 接收，這個串流版函式的回傳型別也要改成 <code>IAsyncEnumerable&lt;String&gt;</code>，再次驗證了 <a href="/blog/async-aspnet/">async 病毒傳染性</a>。</p> <p>由於 ChatGPT 的回答是以 <code>IAsyncEnumerable&lt;String&gt;</code> 形式分批傳回，呼叫端這裡也要小小改寫：</p> <pre><code>    else if (string.IsNullOrEmpty(line) &amp;&amp; Regex.IsMatch(sb.ToString(), "\\S")) {
// 送出內容
context.Enqueue(new ChatRecord(sb.ToString()));
sb.Clear();
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine("傳送中，請稍侯...");
Console.ForegroundColor = ConsoleColor.White;
await foreach (var msg in chatSvc.StreamingGenerate(context)) {
sb.Append(msg);
Console.Write(msg);
}
// 將回答也納入上下文
context.Enqueue(new ChatRecord(sb.ToString(), "A"));
// 上下文內容有 Token 數上限，不能無限累加，取最新十次對話
// TODO: 更精巧做法是將先前對話摘要成精簡版
while (context.Count &gt; 10) context.Dequeue();
Print("");
}
</code></pre> <p>改為串流後，回答會分段顯示出來(雖然分批間隔有點久)，接近 ChatGPT 網頁的操作體驗，比空等十秒再一次冒出結果順暢一點。</p> <p><img src="/Posts/files/2023/Fig4_638278830805919648.gif"></p> <p>練習完畢。</p> <p data-slug="chatgpt-console-chat" data-title-en="Writing a console interface ChatGPT" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/128880/parachute-skydiving-parachuting-jumping-128880.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">Example of using Azure OpenAI ChatGPT model to implement a simple console chat interface.</p>]]></content:encoded>
      <itunes:image href="/Posts/files/2023/Fig1_638278830793134643.png"/>
        <media:content url="/Posts/files/2023/Fig1_638278830793134643.png" medium="image"/>
      <pubDate>Thu, 17 Aug 2023 15:21:24 GMT</pubDate>
    </item></channel>
</rss>