<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
    <title><![CDATA[筆記 - 在 Linux 平台使用 .NET Native AOT]]></title>
    <link>https://blog.darkthread.net/blog/publish-native-aot-on-linux/</link>
    <itunes:title><![CDATA[筆記 - 在 Linux 平台使用 .NET Native AOT]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>接續前一篇<a href="/blog/native-aot-w-python/">用 .NET Native AOT 編譯程式庫給 Python 呼叫</a>的實驗。</p> <p>下一步我打算搬到 Linux 執行，過程有些小波折，編譯原生二進位檔案有優點，但領教過比較複雜繁瑣的編譯程序，倒也襯托出 .NET Runtime 的好處。</p> <p>參考官方文件 <a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT deployment</a>，先簡單整理重點。</p> <ol><li>.NET 8 對 Native AOT 的支援比較完整，反正 Native AOT 不涉及目標主機安裝的 .NET Runtime 版本，建議用 .NET 8。</li> <li>在 .csproj 加入 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code> 後 <code>dotnet publish</code> 將改編譯成 Native AOT，並會啟動編輯及編譯階段動態程式碼使用分析 (Dynamic Code-Usage Analysis)。</li> <li>原生二進位檔對平台版本的依賴很深，如果你用 Ubuntu 20.04 編譯的 Native AOT 檔案，只能在 Ubuntu 20.04 以後的作業系統執行，無法在 Ubuntu 18.04 用。</li> <li>.csproj 加入 <code>&lt;IsAotCompatible&gt;true&lt;/IsAotCompatible&gt;</code> 可標示程式庫相容 Native AOT (等同啟用以下設定：IsTrimmable、EnableTrimAnalyzer、EnableSingleFileAnalyzer、EnableAotAnalyzer)</li> <li>Native AOT 編譯後會產生偵錯檔 .dbg(Linux)/.pdb(Windows)/.dSYM(macOS) 可協助 Crash Dump 偵錯，在非 Windows 平台，設定 <code>&lt;StripSymbols&gt;false&lt;/StripSymbols&gt;</code> 可將其併入原生二進位檔。</li> <li>.NET Native AOT 支援以下平台<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414057660330135.png"></li> <li>作業系統必須安裝 C++ 編譯工具才能編譯 Native AOT，Winows 要安裝 VS2022 Desktop Development with C++，Ubuntu 要 <code>sudo apt-get install clang zlib1g-dev</code>、macOS 要安裝 Command Line Tools for XCode。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a></li></ol> <p>要編譯 Linux 用的 Native AOT 原生程式庫需要用 Linux 平台上編譯(編譯平台與目標平台需相同)，嘗試在 Windows 平台執行 <code>dotnet publish -r linux-x64</code> 會得到 <code>error : Cross-OS native compilation is not supported.</code></p> <p>我是將專案原始檔複製到 Debian，一開始忘了裝安裝 gcc 或 clang，<code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 會發生 <code>collect2 : error : ld returned 1 exit status</code> 錯誤 <a href="https://github.com/godotengine/godot/issues/85476#issuecomment-1831464914">參考</a>)，安裝 clang、zlib1g-dev 後成功編譯出 .so 檔：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414057667181162.png"></p> <p>在 Linux 平台跑 test.py，有三處要修改：1) Windows 載入 .dll，Linux 要載入 .so 2) .NET 端用 Marshal.StringToHGlobalAnsi(result) 傳回字串，在 Windows 是 BIG5 編碼，在 Linux 則是 UTF-8 3) Python 呼叫 .NET Marshal.FreeHGlobal() 釋放 Unmanaged 記憶體配置的做法在 Linux 會出錯，出現 <code>munmap_chunk(): invalid pointer in linux</code> (這部分超出我的知識範圍，期待具相關經驗的前輩指點解惑)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414057669437006.png"></p> <p>以上就是本次的 Native AOT 登陸 Linux 經驗分享。<br> (串接 Managed 及 Unmanaged 世界這塊水頗深，我目前還在新手村打史萊姆... )</p> <p data-slug="publish-native-aot-on-linux" data-title-en="Tips of publishing Native AOT on Linux" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/09/58/traverse-862283_960_720.jpg#960#720">Tips of how to publish Native AOT library on Linux.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>接續前一篇<a href="/blog/native-aot-w-python/">用 .NET Native AOT 編譯程式庫給 Python 呼叫</a>的實驗。</p> <p>下一步我打算搬到 Linux 執行，過程有些小波折，編譯原生二進位檔案有優點，但領教過比較複雜繁瑣的編譯程序，倒也襯托出 .NET Runtime 的好處。</p> <p>參考官方文件 <a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT deployment</a>，先簡單整理重點。</p> <ol><li>.NET 8 對 Native AOT 的支援比較完整，反正 Native AOT 不涉及目標主機安裝的 .NET Runtime 版本，建議用 .NET 8。</li> <li>在 .csproj 加入 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code> 後 <code>dotnet publish</code> 將改編譯成 Native AOT，並會啟動編輯及編譯階段動態程式碼使用分析 (Dynamic Code-Usage Analysis)。</li> <li>原生二進位檔對平台版本的依賴很深，如果你用 Ubuntu 20.04 編譯的 Native AOT 檔案，只能在 Ubuntu 20.04 以後的作業系統執行，無法在 Ubuntu 18.04 用。</li> <li>.csproj 加入 <code>&lt;IsAotCompatible&gt;true&lt;/IsAotCompatible&gt;</code> 可標示程式庫相容 Native AOT (等同啟用以下設定：IsTrimmable、EnableTrimAnalyzer、EnableSingleFileAnalyzer、EnableAotAnalyzer)</li> <li>Native AOT 編譯後會產生偵錯檔 .dbg(Linux)/.pdb(Windows)/.dSYM(macOS) 可協助 Crash Dump 偵錯，在非 Windows 平台，設定 <code>&lt;StripSymbols&gt;false&lt;/StripSymbols&gt;</code> 可將其併入原生二進位檔。</li> <li>.NET Native AOT 支援以下平台<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414057660330135.png"></li> <li>作業系統必須安裝 C++ 編譯工具才能編譯 Native AOT，Winows 要安裝 VS2022 Desktop Development with C++，Ubuntu 要 <code>sudo apt-get install clang zlib1g-dev</code>、macOS 要安裝 Command Line Tools for XCode。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a></li></ol> <p>要編譯 Linux 用的 Native AOT 原生程式庫需要用 Linux 平台上編譯(編譯平台與目標平台需相同)，嘗試在 Windows 平台執行 <code>dotnet publish -r linux-x64</code> 會得到 <code>error : Cross-OS native compilation is not supported.</code></p> <p>我是將專案原始檔複製到 Debian，一開始忘了裝安裝 gcc 或 clang，<code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 會發生 <code>collect2 : error : ld returned 1 exit status</code> 錯誤 <a href="https://github.com/godotengine/godot/issues/85476#issuecomment-1831464914">參考</a>)，安裝 clang、zlib1g-dev 後成功編譯出 .so 檔：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414057667181162.png"></p> <p>在 Linux 平台跑 test.py，有三處要修改：1) Windows 載入 .dll，Linux 要載入 .so 2) .NET 端用 Marshal.StringToHGlobalAnsi(result) 傳回字串，在 Windows 是 BIG5 編碼，在 Linux 則是 UTF-8 3) Python 呼叫 .NET Marshal.FreeHGlobal() 釋放 Unmanaged 記憶體配置的做法在 Linux 會出錯，出現 <code>munmap_chunk(): invalid pointer in linux</code> (這部分超出我的知識範圍，期待具相關經驗的前輩指點解惑)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414057669437006.png"></p> <p>以上就是本次的 Native AOT 登陸 Linux 經驗分享。<br> (串接 Managed 及 Unmanaged 世界這塊水頗深，我目前還在新手村打史萊姆... )</p> <p data-slug="publish-native-aot-on-linux" data-title-en="Tips of publishing Native AOT on Linux" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/09/58/traverse-862283_960_720.jpg#960#720">Tips of how to publish Native AOT library on Linux.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>接續前一篇<a href="/blog/native-aot-w-python/">用 .NET Native AOT 編譯程式庫給 Python 呼叫</a>的實驗。</p> <p>下一步我打算搬到 Linux 執行，過程有些小波折，編譯原生二進位檔案有優點，但領教過比較複雜繁瑣的編譯程序，倒也襯托出 .NET Runtime 的好處。</p> <p>參考官方文件 <a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT deployment</a>，先簡單整理重點。</p> <ol><li>.NET 8 對 Native AOT 的支援比較完整，反正 Native AOT 不涉及目標主機安裝的 .NET Runtime 版本，建議用 .NET 8。</li> <li>在 .csproj 加入 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code> 後 <code>dotnet publish</code> 將改編譯成 Native AOT，並會啟動編輯及編譯階段動態程式碼使用分析 (Dynamic Code-Usage Analysis)。</li> <li>原生二進位檔對平台版本的依賴很深，如果你用 Ubuntu 20.04 編譯的 Native AOT 檔案，只能在 Ubuntu 20.04 以後的作業系統執行，無法在 Ubuntu 18.04 用。</li> <li>.csproj 加入 <code>&lt;IsAotCompatible&gt;true&lt;/IsAotCompatible&gt;</code> 可標示程式庫相容 Native AOT (等同啟用以下設定：IsTrimmable、EnableTrimAnalyzer、EnableSingleFileAnalyzer、EnableAotAnalyzer)</li> <li>Native AOT 編譯後會產生偵錯檔 .dbg(Linux)/.pdb(Windows)/.dSYM(macOS) 可協助 Crash Dump 偵錯，在非 Windows 平台，設定 <code>&lt;StripSymbols&gt;false&lt;/StripSymbols&gt;</code> 可將其併入原生二進位檔。</li> <li>.NET Native AOT 支援以下平台<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414057660330135.png"></li> <li>作業系統必須安裝 C++ 編譯工具才能編譯 Native AOT，Winows 要安裝 VS2022 Desktop Development with C++，Ubuntu 要 <code>sudo apt-get install clang zlib1g-dev</code>、macOS 要安裝 Command Line Tools for XCode。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a></li></ol> <p>要編譯 Linux 用的 Native AOT 原生程式庫需要用 Linux 平台上編譯(編譯平台與目標平台需相同)，嘗試在 Windows 平台執行 <code>dotnet publish -r linux-x64</code> 會得到 <code>error : Cross-OS native compilation is not supported.</code></p> <p>我是將專案原始檔複製到 Debian，一開始忘了裝安裝 gcc 或 clang，<code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 會發生 <code>collect2 : error : ld returned 1 exit status</code> 錯誤 <a href="https://github.com/godotengine/godot/issues/85476#issuecomment-1831464914">參考</a>)，安裝 clang、zlib1g-dev 後成功編譯出 .so 檔：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414057667181162.png"></p> <p>在 Linux 平台跑 test.py，有三處要修改：1) Windows 載入 .dll，Linux 要載入 .so 2) .NET 端用 Marshal.StringToHGlobalAnsi(result) 傳回字串，在 Windows 是 BIG5 編碼，在 Linux 則是 UTF-8 3) Python 呼叫 .NET Marshal.FreeHGlobal() 釋放 Unmanaged 記憶體配置的做法在 Linux 會出錯，出現 <code>munmap_chunk(): invalid pointer in linux</code> (這部分超出我的知識範圍，期待具相關經驗的前輩指點解惑)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414057669437006.png"></p> <p>以上就是本次的 Native AOT 登陸 Linux 經驗分享。<br> (串接 Managed 及 Unmanaged 世界這塊水頗深，我目前還在新手村打史萊姆... )</p> <p data-slug="publish-native-aot-on-linux" data-title-en="Tips of publishing Native AOT on Linux" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/07/27/09/58/traverse-862283_960_720.jpg#960#720">Tips of how to publish Native AOT library on Linux.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638414057660330135.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638414057660330135.png" medium="image"/>
    <pubDate>Sun, 21 Jan 2024 04:27:10 GMT</pubDate>
  </item><item>
    <title><![CDATA[用 .NET 開發程式庫供 Python 呼叫 - Native AOT 應用]]></title>
    <link>https://blog.darkthread.net/blog/native-aot-w-python/</link>
    <itunes:title><![CDATA[用 .NET 開發程式庫供 Python 呼叫 - Native AOT 應用]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>Python 是當今火紅的程式語言，為 AI/Mechine Learning 領域的奧林匹克指定開發語言，在這些領域，你得說 Python 才能享有一等國民的待遇。</p> <p>身為 C# 已經寫到得心應手的老人，若在 Python 場子遇到刁鑽需求，但用 C# 可以秒殺或已有現成程式庫，此時我有三種選擇：</p> <ol><li>尋找對應的 Python 程式庫或範例<br> 閃開讓專家來是最上策，但有時看緣分，踏破鐵鞋也未有所得</li> <li>用 C# 寫好叫 ChatGPT 轉成 Python<br> 成功率視狀況，看語法是否單純，若涉及 .NET 獨有 API，轉換得費番手腳。但不依賴額外程式庫讓架構簡潔單純，為次佳解</li> <li>用 C# 寫程式庫給 Python 呼叫<br> 也是個選擇。好處是可直接沿用 C# 成果不必費心移植，編成一顆 .dll (Windows)/.dylib (macOS)/.so (Linux) 給 Python 參考即可</li></ol> <p>今天這篇就來研究第 3 種做法。</p> <p>這個技術叫 <a href="https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT</a>，是 .NET 7 正式加入的功能(更早之前為實驗版，7.0 正式移入 core/runtime，8.0 功能更完整)，概念是將 .NET 程式編成原生二進位檔，讓執行檔又小(不用 Runtime)又快(不需 JIT 編譯)。</p> <p>但 NativeAOT 使用上也有一些限制，像是不支援 Assembly.LoadFile、System.Reflection.Emit、C++/CLI、Built-in COM... 等。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580#limitations-of-native-aot-deployment">參考</a></p> <p>補充：MVP Poy 有場 <a href="https://www.youtube.com/watch?v=GmtJELtTyqA">NativeAOT 線上講座</a>，推薦給想深入了解 NativeAOT 來龍去脈的同學。</p> <p>我打算把 <a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>包成程式庫給 Python 呼叫，體驗用原生程式庫跨平台跨語言整合。</p> <p>將 .NET 專案寫成 Native AOT 程式庫供 Unmanaged 世界呼叫的做法我主要參考這篇：<a href="https://github.com/dotnet/samples/blob/main/core/nativeaot/NativeLibrary/README.md">Building Native Libraries with NativeAOT</a>。</p> <p>完整專案已上傳 <a href="https://github.com/darkthread/chtnumconv-naot">Github</a>，這裡只簡要說明重點：</p> <ol><li>用 <code>dotnet net classlib -o chtnumconv-naot</code> 建立專案</li> <li>修改 .csproj 加上 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code></li> <li>將 Class1.cs 換成 ChtNumConverter.cs</li> <li>用 <code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 編譯 (註：Windows 要安裝 VS2022 Desktop Development with C++，<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a>)<br> 若為 Windows 平台，在 bin\Release
et8.0\win-x64\public 下會有 chtnumconv-naot.dll 及 .pdb，即為 Native AOT 版本的原生二進位程式庫<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414000165790417.png"><br> 大小僅 1.9MB<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414000168016426.png"></li> <li>NativeLib 參數有 Shared 與 Static 兩種，Static 的話，程式庫檔案會在編譯時合併入，包含在應用程式執行檔裡，此時的輸出格式是 .lib (Windows) / .a (Linux/macOS)；Shared 的話，程式庫檔為 .dll (Windows) / .so (Linux) / .dylib (macOS)，可供多個應用程式共用，並以動態方式載入。如果要給 Python 用，只能選 Shared。<a href="https://medium.com/@sixpeteunder/how-to-build-a-shared-library-in-c-sharp-and-call-it-from-java-code-6931260d01e5">參考</a></li></ol> <p>ChtNumConvert.cs 的改寫重點如下：</p> <pre><code>public class ChtNumConverter
{
// 解析中文數字
[UnmanagedCallersOnly(EntryPoint = "parse_cht_num")]
public static long ParseChtNum(IntPtr chtNumStringPtr)
{
string chtNumString = Marshal.PtrToStringUTF8(chtNumStringPtr)!;
var isNegative = false;
/* 略 */
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
[UnmanagedCallersOnly(EntryPoint = "to_cht_num")]
public static IntPtr ToChtNum(long n)
{
var negtive = n &lt; 0;
t = Regex.Replace(t, "^一十", "十");
/* 略 */
var result = (negtive ? "負" : string.Empty) + t;
// TODO 研究傳回 UTF-8 的方法
return Marshal.StringToHGlobalAnsi(result);
}
// 釋放記憶體
[UnmanagedCallersOnly(EntryPoint = "free_mem")]
public static void FreeMem(IntPtr ptr) =&gt; Marshal.FreeHGlobal(ptr);
}
</code></pre> <p>ParseChtNum() 及 ToChtNum() 加上 <code>[UnmanagedCallersOnly(EntryPoint = "method-name")]</code>，string 參數型別或傳回型別改為 IntPtr，用 Marshal.PtrToStringUTF8(chtNumStringPtr) 轉成字串，用 Marshal.StringToHGlobalAnsi(result) 將字串傳成 IntPtr，由於 StringToHGlobalAnsi() 會配置 Unmanaged 記憶體，無法由 GC 機制回收，我看範例程式不太有 人處理這個問題，但我還是加了一個 FreeMem(IntPtr ptr) 讓 Python 呼叫以釋放配置的記憶體。(或是有其他更正確的做法，歡迎指正補充)</p> <p>Marshal.StringToHGlobalAnsi() 會傳回 ANSI/BIG-5 編碼中文，我沒試出傳回 UTF-8 或 Unicode 編碼的好方法，歡迎知道的朋友補充。</p> <p>再來看 Python 端，我先研究了 Python 呼叫 C/C++ 程式庫的做法，找到這篇：<a href="https://bushyang.medium.com/python-c-c-%E6%95%B4%E5%90%88-adbfbf6051fd">Python c/c++ 整合</a>。</p> <p>簡單歸納 Python 引用 C/C++ 程式庫常用的方案：Python/C API、ctypes、cffi、pybind11、Cython</p> <ol><li>Python/C API：維護成本高、相容性差，不推薦直接使用</li> <li>ctypes：Python 內建，不需 Wrapper，不支援 C++</li> <li>cffi：依賴額外程式庫，不需 Wrapper，不支援 C++</li> <li>pybind11：特色是支援 C++，優點對 .NET 無效</li> <li>Cython：把 Python 程式碼編譯成 C 程式碼，等於不是寫 Python 換了新語言，要換 Toolchain 異動偏大</li></ol> <p>評估後決定用 ctypes。</p> <pre><code>import ctypes
chtnumconverter = ctypes.cdll.LoadLibrary("X:/Github/chtnumconv-naot/bin/Release/net8.0/win-x64/publish/chtnumconv-naot.dll")
s = '一千零二十四'
n = chtnumconverter.parse_cht_num(s.encode('utf-8'))
print(n)
chtnumconverter.to_cht_num.restype = ctypes.c_char_p
p = chtnumconverter.to_cht_num(65536) # p = pointer to string
# TODO 找出回傳 UTF-8 編碼的方法
print(p.decode('big5'))
chtnumconverter.free_mem(p); # free memory
</code></pre> <p>我最後拼湊出的 Python 程式如上，似懂非懂，但執行成功了!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414000170500224.png"></p> <p>這是人類的一小步(謎之音：別自我膨脹，頂多是咬冷笱抖一下)，卻是我的一大步。</p> <p data-slug="native-aot-w-python" data-title-en="Example of calling .NET Native AOT library in Python" data-catg=".NET" data-fbimg="https://i.imgur.com/Kss6ms1.jpg#990#566">A exmaple to build a .NET Native AOT library for Python.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>Python 是當今火紅的程式語言，為 AI/Mechine Learning 領域的奧林匹克指定開發語言，在這些領域，你得說 Python 才能享有一等國民的待遇。</p> <p>身為 C# 已經寫到得心應手的老人，若在 Python 場子遇到刁鑽需求，但用 C# 可以秒殺或已有現成程式庫，此時我有三種選擇：</p> <ol><li>尋找對應的 Python 程式庫或範例<br> 閃開讓專家來是最上策，但有時看緣分，踏破鐵鞋也未有所得</li> <li>用 C# 寫好叫 ChatGPT 轉成 Python<br> 成功率視狀況，看語法是否單純，若涉及 .NET 獨有 API，轉換得費番手腳。但不依賴額外程式庫讓架構簡潔單純，為次佳解</li> <li>用 C# 寫程式庫給 Python 呼叫<br> 也是個選擇。好處是可直接沿用 C# 成果不必費心移植，編成一顆 .dll (Windows)/.dylib (macOS)/.so (Linux) 給 Python 參考即可</li></ol> <p>今天這篇就來研究第 3 種做法。</p> <p>這個技術叫 <a href="https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT</a>，是 .NET 7 正式加入的功能(更早之前為實驗版，7.0 正式移入 core/runtime，8.0 功能更完整)，概念是將 .NET 程式編成原生二進位檔，讓執行檔又小(不用 Runtime)又快(不需 JIT 編譯)。</p> <p>但 NativeAOT 使用上也有一些限制，像是不支援 Assembly.LoadFile、System.Reflection.Emit、C++/CLI、Built-in COM... 等。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580#limitations-of-native-aot-deployment">參考</a></p> <p>補充：MVP Poy 有場 <a href="https://www.youtube.com/watch?v=GmtJELtTyqA">NativeAOT 線上講座</a>，推薦給想深入了解 NativeAOT 來龍去脈的同學。</p> <p>我打算把 <a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>包成程式庫給 Python 呼叫，體驗用原生程式庫跨平台跨語言整合。</p> <p>將 .NET 專案寫成 Native AOT 程式庫供 Unmanaged 世界呼叫的做法我主要參考這篇：<a href="https://github.com/dotnet/samples/blob/main/core/nativeaot/NativeLibrary/README.md">Building Native Libraries with NativeAOT</a>。</p> <p>完整專案已上傳 <a href="https://github.com/darkthread/chtnumconv-naot">Github</a>，這裡只簡要說明重點：</p> <ol><li>用 <code>dotnet net classlib -o chtnumconv-naot</code> 建立專案</li> <li>修改 .csproj 加上 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code></li> <li>將 Class1.cs 換成 ChtNumConverter.cs</li> <li>用 <code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 編譯 (註：Windows 要安裝 VS2022 Desktop Development with C++，<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a>)<br> 若為 Windows 平台，在 bin\Release
et8.0\win-x64\public 下會有 chtnumconv-naot.dll 及 .pdb，即為 Native AOT 版本的原生二進位程式庫<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414000165790417.png"><br> 大小僅 1.9MB<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414000168016426.png"></li> <li>NativeLib 參數有 Shared 與 Static 兩種，Static 的話，程式庫檔案會在編譯時合併入，包含在應用程式執行檔裡，此時的輸出格式是 .lib (Windows) / .a (Linux/macOS)；Shared 的話，程式庫檔為 .dll (Windows) / .so (Linux) / .dylib (macOS)，可供多個應用程式共用，並以動態方式載入。如果要給 Python 用，只能選 Shared。<a href="https://medium.com/@sixpeteunder/how-to-build-a-shared-library-in-c-sharp-and-call-it-from-java-code-6931260d01e5">參考</a></li></ol> <p>ChtNumConvert.cs 的改寫重點如下：</p> <pre><code>public class ChtNumConverter
{
// 解析中文數字
[UnmanagedCallersOnly(EntryPoint = "parse_cht_num")]
public static long ParseChtNum(IntPtr chtNumStringPtr)
{
string chtNumString = Marshal.PtrToStringUTF8(chtNumStringPtr)!;
var isNegative = false;
/* 略 */
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
[UnmanagedCallersOnly(EntryPoint = "to_cht_num")]
public static IntPtr ToChtNum(long n)
{
var negtive = n &lt; 0;
t = Regex.Replace(t, "^一十", "十");
/* 略 */
var result = (negtive ? "負" : string.Empty) + t;
// TODO 研究傳回 UTF-8 的方法
return Marshal.StringToHGlobalAnsi(result);
}
// 釋放記憶體
[UnmanagedCallersOnly(EntryPoint = "free_mem")]
public static void FreeMem(IntPtr ptr) =&gt; Marshal.FreeHGlobal(ptr);
}
</code></pre> <p>ParseChtNum() 及 ToChtNum() 加上 <code>[UnmanagedCallersOnly(EntryPoint = "method-name")]</code>，string 參數型別或傳回型別改為 IntPtr，用 Marshal.PtrToStringUTF8(chtNumStringPtr) 轉成字串，用 Marshal.StringToHGlobalAnsi(result) 將字串傳成 IntPtr，由於 StringToHGlobalAnsi() 會配置 Unmanaged 記憶體，無法由 GC 機制回收，我看範例程式不太有 人處理這個問題，但我還是加了一個 FreeMem(IntPtr ptr) 讓 Python 呼叫以釋放配置的記憶體。(或是有其他更正確的做法，歡迎指正補充)</p> <p>Marshal.StringToHGlobalAnsi() 會傳回 ANSI/BIG-5 編碼中文，我沒試出傳回 UTF-8 或 Unicode 編碼的好方法，歡迎知道的朋友補充。</p> <p>再來看 Python 端，我先研究了 Python 呼叫 C/C++ 程式庫的做法，找到這篇：<a href="https://bushyang.medium.com/python-c-c-%E6%95%B4%E5%90%88-adbfbf6051fd">Python c/c++ 整合</a>。</p> <p>簡單歸納 Python 引用 C/C++ 程式庫常用的方案：Python/C API、ctypes、cffi、pybind11、Cython</p> <ol><li>Python/C API：維護成本高、相容性差，不推薦直接使用</li> <li>ctypes：Python 內建，不需 Wrapper，不支援 C++</li> <li>cffi：依賴額外程式庫，不需 Wrapper，不支援 C++</li> <li>pybind11：特色是支援 C++，優點對 .NET 無效</li> <li>Cython：把 Python 程式碼編譯成 C 程式碼，等於不是寫 Python 換了新語言，要換 Toolchain 異動偏大</li></ol> <p>評估後決定用 ctypes。</p> <pre><code>import ctypes
chtnumconverter = ctypes.cdll.LoadLibrary("X:/Github/chtnumconv-naot/bin/Release/net8.0/win-x64/publish/chtnumconv-naot.dll")
s = '一千零二十四'
n = chtnumconverter.parse_cht_num(s.encode('utf-8'))
print(n)
chtnumconverter.to_cht_num.restype = ctypes.c_char_p
p = chtnumconverter.to_cht_num(65536) # p = pointer to string
# TODO 找出回傳 UTF-8 編碼的方法
print(p.decode('big5'))
chtnumconverter.free_mem(p); # free memory
</code></pre> <p>我最後拼湊出的 Python 程式如上，似懂非懂，但執行成功了!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414000170500224.png"></p> <p>這是人類的一小步(謎之音：別自我膨脹，頂多是咬冷笱抖一下)，卻是我的一大步。</p> <p data-slug="native-aot-w-python" data-title-en="Example of calling .NET Native AOT library in Python" data-catg=".NET" data-fbimg="https://i.imgur.com/Kss6ms1.jpg#990#566">A exmaple to build a .NET Native AOT library for Python.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>Python 是當今火紅的程式語言，為 AI/Mechine Learning 領域的奧林匹克指定開發語言，在這些領域，你得說 Python 才能享有一等國民的待遇。</p> <p>身為 C# 已經寫到得心應手的老人，若在 Python 場子遇到刁鑽需求，但用 C# 可以秒殺或已有現成程式庫，此時我有三種選擇：</p> <ol><li>尋找對應的 Python 程式庫或範例<br> 閃開讓專家來是最上策，但有時看緣分，踏破鐵鞋也未有所得</li> <li>用 C# 寫好叫 ChatGPT 轉成 Python<br> 成功率視狀況，看語法是否單純，若涉及 .NET 獨有 API，轉換得費番手腳。但不依賴額外程式庫讓架構簡潔單純，為次佳解</li> <li>用 C# 寫程式庫給 Python 呼叫<br> 也是個選擇。好處是可直接沿用 C# 成果不必費心移植，編成一顆 .dll (Windows)/.dylib (macOS)/.so (Linux) 給 Python 參考即可</li></ol> <p>今天這篇就來研究第 3 種做法。</p> <p>這個技術叫 <a href="https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580">Native AOT</a>，是 .NET 7 正式加入的功能(更早之前為實驗版，7.0 正式移入 core/runtime，8.0 功能更完整)，概念是將 .NET 程式編成原生二進位檔，讓執行檔又小(不用 Runtime)又快(不需 JIT 編譯)。</p> <p>但 NativeAOT 使用上也有一些限制，像是不支援 Assembly.LoadFile、System.Reflection.Emit、C++/CLI、Built-in COM... 等。<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Cwindows&amp;WT.mc_id=DOP-MVP-37580#limitations-of-native-aot-deployment">參考</a></p> <p>補充：MVP Poy 有場 <a href="https://www.youtube.com/watch?v=GmtJELtTyqA">NativeAOT 線上講座</a>，推薦給想深入了解 NativeAOT 來龍去脈的同學。</p> <p>我打算把 <a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>包成程式庫給 Python 呼叫，體驗用原生程式庫跨平台跨語言整合。</p> <p>將 .NET 專案寫成 Native AOT 程式庫供 Unmanaged 世界呼叫的做法我主要參考這篇：<a href="https://github.com/dotnet/samples/blob/main/core/nativeaot/NativeLibrary/README.md">Building Native Libraries with NativeAOT</a>。</p> <p>完整專案已上傳 <a href="https://github.com/darkthread/chtnumconv-naot">Github</a>，這裡只簡要說明重點：</p> <ol><li>用 <code>dotnet net classlib -o chtnumconv-naot</code> 建立專案</li> <li>修改 .csproj 加上 <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code></li> <li>將 Class1.cs 換成 ChtNumConverter.cs</li> <li>用 <code>dotnet publish /p:NativeLib=Shared --use-current-runtime</code> 編譯 (註：Windows 要安裝 VS2022 Desktop Development with C++，<a href="https://learn.microsoft.com/zh-tw/dotnet/core/deploying/native-aot/?tabs=net7%2Clinux-ubuntu&amp;WT.mc_id=DOP-MVP-37580#prerequisites">參考</a>)<br> 若為 Windows 平台，在 bin\Release
et8.0\win-x64\public 下會有 chtnumconv-naot.dll 及 .pdb，即為 Native AOT 版本的原生二進位程式庫<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638414000165790417.png"><br> 大小僅 1.9MB<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638414000168016426.png"></li> <li>NativeLib 參數有 Shared 與 Static 兩種，Static 的話，程式庫檔案會在編譯時合併入，包含在應用程式執行檔裡，此時的輸出格式是 .lib (Windows) / .a (Linux/macOS)；Shared 的話，程式庫檔為 .dll (Windows) / .so (Linux) / .dylib (macOS)，可供多個應用程式共用，並以動態方式載入。如果要給 Python 用，只能選 Shared。<a href="https://medium.com/@sixpeteunder/how-to-build-a-shared-library-in-c-sharp-and-call-it-from-java-code-6931260d01e5">參考</a></li></ol> <p>ChtNumConvert.cs 的改寫重點如下：</p> <pre><code>public class ChtNumConverter
{
// 解析中文數字
[UnmanagedCallersOnly(EntryPoint = "parse_cht_num")]
public static long ParseChtNum(IntPtr chtNumStringPtr)
{
string chtNumString = Marshal.PtrToStringUTF8(chtNumStringPtr)!;
var isNegative = false;
/* 略 */
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
[UnmanagedCallersOnly(EntryPoint = "to_cht_num")]
public static IntPtr ToChtNum(long n)
{
var negtive = n &lt; 0;
t = Regex.Replace(t, "^一十", "十");
/* 略 */
var result = (negtive ? "負" : string.Empty) + t;
// TODO 研究傳回 UTF-8 的方法
return Marshal.StringToHGlobalAnsi(result);
}
// 釋放記憶體
[UnmanagedCallersOnly(EntryPoint = "free_mem")]
public static void FreeMem(IntPtr ptr) =&gt; Marshal.FreeHGlobal(ptr);
}
</code></pre> <p>ParseChtNum() 及 ToChtNum() 加上 <code>[UnmanagedCallersOnly(EntryPoint = "method-name")]</code>，string 參數型別或傳回型別改為 IntPtr，用 Marshal.PtrToStringUTF8(chtNumStringPtr) 轉成字串，用 Marshal.StringToHGlobalAnsi(result) 將字串傳成 IntPtr，由於 StringToHGlobalAnsi() 會配置 Unmanaged 記憶體，無法由 GC 機制回收，我看範例程式不太有 人處理這個問題，但我還是加了一個 FreeMem(IntPtr ptr) 讓 Python 呼叫以釋放配置的記憶體。(或是有其他更正確的做法，歡迎指正補充)</p> <p>Marshal.StringToHGlobalAnsi() 會傳回 ANSI/BIG-5 編碼中文，我沒試出傳回 UTF-8 或 Unicode 編碼的好方法，歡迎知道的朋友補充。</p> <p>再來看 Python 端，我先研究了 Python 呼叫 C/C++ 程式庫的做法，找到這篇：<a href="https://bushyang.medium.com/python-c-c-%E6%95%B4%E5%90%88-adbfbf6051fd">Python c/c++ 整合</a>。</p> <p>簡單歸納 Python 引用 C/C++ 程式庫常用的方案：Python/C API、ctypes、cffi、pybind11、Cython</p> <ol><li>Python/C API：維護成本高、相容性差，不推薦直接使用</li> <li>ctypes：Python 內建，不需 Wrapper，不支援 C++</li> <li>cffi：依賴額外程式庫，不需 Wrapper，不支援 C++</li> <li>pybind11：特色是支援 C++，優點對 .NET 無效</li> <li>Cython：把 Python 程式碼編譯成 C 程式碼，等於不是寫 Python 換了新語言，要換 Toolchain 異動偏大</li></ol> <p>評估後決定用 ctypes。</p> <pre><code>import ctypes
chtnumconverter = ctypes.cdll.LoadLibrary("X:/Github/chtnumconv-naot/bin/Release/net8.0/win-x64/publish/chtnumconv-naot.dll")
s = '一千零二十四'
n = chtnumconverter.parse_cht_num(s.encode('utf-8'))
print(n)
chtnumconverter.to_cht_num.restype = ctypes.c_char_p
p = chtnumconverter.to_cht_num(65536) # p = pointer to string
# TODO 找出回傳 UTF-8 編碼的方法
print(p.decode('big5'))
chtnumconverter.free_mem(p); # free memory
</code></pre> <p>我最後拼湊出的 Python 程式如上，似懂非懂，但執行成功了!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638414000170500224.png"></p> <p>這是人類的一小步(謎之音：別自我膨脹，頂多是咬冷笱抖一下)，卻是我的一大步。</p> <p data-slug="native-aot-w-python" data-title-en="Example of calling .NET Native AOT library in Python" data-catg=".NET" data-fbimg="https://i.imgur.com/Kss6ms1.jpg#990#566">A exmaple to build a .NET Native AOT library for Python.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638414000165790417.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638414000165790417.png" medium="image"/>
    <pubDate>Sun, 21 Jan 2024 01:45:24 GMT</pubDate>
  </item><item>
    <title><![CDATA[會寫程式不值錢了嗎？ChatGPT C# 轉 JavaScript 轉 Python 實測]]></title>
    <link>https://blog.darkthread.net/blog/chatgpt-conv-program-language/</link>
    <itunes:title><![CDATA[會寫程式不值錢了嗎？ChatGPT C# 轉 JavaScript 轉 Python 實測]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>貼文有讀者提到：如果將來不用 C#，豈不要重寫？</p> <p>好問題，其實寫函式時我就有想到這點，當時想法是，之所以擺脫 Visual Studio 程式庫從頭用 C# 重寫，便是著眼於相同演算法可移植到其他程式語言，更甭提我們現在有 ChatGPT 當靠山，這種沒用什麼平台專屬 API 的純演算法小功能，以 ChatGPT 4 的聰慧，理論上可以無痛轉移才對。</p> <p>理論歸理論，但實際上呢？實測一下便知。我打算將這個工具函式轉成 JavaScript 跟 Python 版本，看看需要花多少力氣。</p> <p>把程式碼貼給 ChatGPT-4，這點小事用不著情勒催眠角色扮演，Prompt 寫一句 <code>convert this c# to javascript</code> 即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638413132860843124.png"></p> <p>C# 轉 JavaScript 的部分，ChatGPT 偷懶了。兩個核心方法 ParseChtNum 及 ToChtNum，ChatGPT 只給了註解，說他知道一種美妙的寫法，可惜這裡太小寫不下 XD 但有提示如果有需要可另外提問。把 ChtNumConvert 型別程式單獨拿出來再問一次，這回便拿到完整轉換結果。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638413132864943894.png"></p> <p>ChatGPT 轉換的 JavaScript 可直接執行，沒編譯或 Runtime 錯誤：(註：有部分「零」沒有處理好，畫面沒截到)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638413132867339908.png"></p> <p>轉完 JavaScript，再來轉成 Python 試試。由於 ChatGPT 對 JavaScript 比對 C# 熟 (JavaScript 訓練資料較多)，我選擇請 ChatGPT 將剛轉好的 JavaScript 轉成 Python 而非從 C# 轉。這回 ChatGPT 很乾脆，整段程式一次轉完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638413132869610719.png"></p> <p>Python 版本也可順利編譯執行，但結果的錯誤更明顯，出現「零零零」、「零三百十」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638413132871744554.png"></p> <p>動手 Debug，JavaScript 版的問題出在 C# Trim('零') 邏輯沒轉對，用 RegExp 只換了結尾，補上開頭置換就 OK 了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638413132873912983.png"></p> <p>Python 部分幾個則是幾個 C# Regular Expression 處理沒正確轉譯。雖然不熟 Python，在 <a href="/blog/github-copilot-demo/">Github Copilot</a> 協助下沒費什麼力氣就修正好了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638413132876070410.png"></p> <p>修改後，C#、JavaScript 與 Python 三個版本產生的結果一致，確認轉換成功。</p> <p>總結一下本次實測結果：</p> <ol><li>本範例約 100 行 C# 程式，使用 ChatGPT 4 轉換耗時約一分鐘左右可拿到結果，轉換出來的 JavaScript 及 Python 版本均可直接執行，沒有編譯及 Runtime 錯誤，但執行結果有部分不正確</li> <li>JavaScript 有兩處需要修改，Python 有三處，但都是很明顯很容易修改的錯誤，前後不到五分鐘改好</li> <li>修改後，比對 C#、JavaScript、Python 三個版本執行結果一致，判定轉換成功</li></ol> <p>在本次 ChatGPT 火力展示，由於程式碼不長，未涉及專屬程式庫呼叫並以演算法為主，ChatGPT 從 A 程式語言轉成 B 程式語言的完成度及正確率頗高，人工確認結果及修 Bug 免不了，但比起徒手翻寫不知快 N 倍。即使面對不熟悉的程式語言(本例中的 Python)，靠著 ChatGPT 先轉換，利用 Github Copilot 協助修正，我也能在短短幾分鐘內自力完成翻寫，在 ChatGPT 出來之前，我沒想過有會有這麼一天。</p> <p>阿貓阿狗都能轉程式改程式，這意味著會寫程式不值錢了嗎？好像不得不承認是這樣沒錯。</p> <p>ChatGPT / AI 大幅降低了寫程式的門檻，新手生手不需要歷經漫長的學習也能生出可用的程式，老鳥的不可取代性下降(當然，品質效能擴充維護性有別，但很多時候，能解決眼前問題的堪用程式已值千金)；但很公平的是一件事是，新手有 AI 可以依靠，老鳥一樣也得到 AI 加持，像我不熟 Python，一樣只花幾分鐘就寫好 Python 版函式。</p> <p>正確地說，應該是入門到中級的程式技能貶值了。過去會寫程式在一般人眼中就是高手，會有很多人巴著你請你幫忙，排隊等你心情好賞賜幾支程式解決問題。</p> <p>如今，一般人靠著 ChatGPT/RPA/NoCode 工具也能寫出滿足需求的程式，沒必要把會寫程式的人當成寶。除非對效能、資源用量、擴充維護性有較高要求的情境，此時若經驗與知識不足，即便有 ChatGPT 無法精準提問也是枉然，此時仍需經驗知識豐富的老鳥才能應付，但程式能力只有一般水平是派不上用場的。由此推論，寫得出程式真的不值錢了，很會寫程式懂偵錯且具備效能、架構、擴充維護性知識才算值錢本事。</p> <p>靠會一點程式便有恃無恐的時代結束了，得再跑快一點惹。</p> <p>註：轉換結果我有丟上 <a href="https://github.com/darkthread/cht-num-string">Github</a>，如果有人想參考的話</p> <p data-slug="chatgpt-conv-program-language" data-fbimg="https://cdn.pixabay.com/photo/2019/08/09/06/12/car-racing-4394450_1280.jpg#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>貼文有讀者提到：如果將來不用 C#，豈不要重寫？</p> <p>好問題，其實寫函式時我就有想到這點，當時想法是，之所以擺脫 Visual Studio 程式庫從頭用 C# 重寫，便是著眼於相同演算法可移植到其他程式語言，更甭提我們現在有 ChatGPT 當靠山，這種沒用什麼平台專屬 API 的純演算法小功能，以 ChatGPT 4 的聰慧，理論上可以無痛轉移才對。</p> <p>理論歸理論，但實際上呢？實測一下便知。我打算將這個工具函式轉成 JavaScript 跟 Python 版本，看看需要花多少力氣。</p> <p>把程式碼貼給 ChatGPT-4，這點小事用不著情勒催眠角色扮演，Prompt 寫一句 <code>convert this c# to javascript</code> 即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638413132860843124.png"></p> <p>C# 轉 JavaScript 的部分，ChatGPT 偷懶了。兩個核心方法 ParseChtNum 及 ToChtNum，ChatGPT 只給了註解，說他知道一種美妙的寫法，可惜這裡太小寫不下 XD 但有提示如果有需要可另外提問。把 ChtNumConvert 型別程式單獨拿出來再問一次，這回便拿到完整轉換結果。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638413132864943894.png"></p> <p>ChatGPT 轉換的 JavaScript 可直接執行，沒編譯或 Runtime 錯誤：(註：有部分「零」沒有處理好，畫面沒截到)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638413132867339908.png"></p> <p>轉完 JavaScript，再來轉成 Python 試試。由於 ChatGPT 對 JavaScript 比對 C# 熟 (JavaScript 訓練資料較多)，我選擇請 ChatGPT 將剛轉好的 JavaScript 轉成 Python 而非從 C# 轉。這回 ChatGPT 很乾脆，整段程式一次轉完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638413132869610719.png"></p> <p>Python 版本也可順利編譯執行，但結果的錯誤更明顯，出現「零零零」、「零三百十」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638413132871744554.png"></p> <p>動手 Debug，JavaScript 版的問題出在 C# Trim('零') 邏輯沒轉對，用 RegExp 只換了結尾，補上開頭置換就 OK 了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638413132873912983.png"></p> <p>Python 部分幾個則是幾個 C# Regular Expression 處理沒正確轉譯。雖然不熟 Python，在 <a href="/blog/github-copilot-demo/">Github Copilot</a> 協助下沒費什麼力氣就修正好了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638413132876070410.png"></p> <p>修改後，C#、JavaScript 與 Python 三個版本產生的結果一致，確認轉換成功。</p> <p>總結一下本次實測結果：</p> <ol><li>本範例約 100 行 C# 程式，使用 ChatGPT 4 轉換耗時約一分鐘左右可拿到結果，轉換出來的 JavaScript 及 Python 版本均可直接執行，沒有編譯及 Runtime 錯誤，但執行結果有部分不正確</li> <li>JavaScript 有兩處需要修改，Python 有三處，但都是很明顯很容易修改的錯誤，前後不到五分鐘改好</li> <li>修改後，比對 C#、JavaScript、Python 三個版本執行結果一致，判定轉換成功</li></ol> <p>在本次 ChatGPT 火力展示，由於程式碼不長，未涉及專屬程式庫呼叫並以演算法為主，ChatGPT 從 A 程式語言轉成 B 程式語言的完成度及正確率頗高，人工確認結果及修 Bug 免不了，但比起徒手翻寫不知快 N 倍。即使面對不熟悉的程式語言(本例中的 Python)，靠著 ChatGPT 先轉換，利用 Github Copilot 協助修正，我也能在短短幾分鐘內自力完成翻寫，在 ChatGPT 出來之前，我沒想過有會有這麼一天。</p> <p>阿貓阿狗都能轉程式改程式，這意味著會寫程式不值錢了嗎？好像不得不承認是這樣沒錯。</p> <p>ChatGPT / AI 大幅降低了寫程式的門檻，新手生手不需要歷經漫長的學習也能生出可用的程式，老鳥的不可取代性下降(當然，品質效能擴充維護性有別，但很多時候，能解決眼前問題的堪用程式已值千金)；但很公平的是一件事是，新手有 AI 可以依靠，老鳥一樣也得到 AI 加持，像我不熟 Python，一樣只花幾分鐘就寫好 Python 版函式。</p> <p>正確地說，應該是入門到中級的程式技能貶值了。過去會寫程式在一般人眼中就是高手，會有很多人巴著你請你幫忙，排隊等你心情好賞賜幾支程式解決問題。</p> <p>如今，一般人靠著 ChatGPT/RPA/NoCode 工具也能寫出滿足需求的程式，沒必要把會寫程式的人當成寶。除非對效能、資源用量、擴充維護性有較高要求的情境，此時若經驗與知識不足，即便有 ChatGPT 無法精準提問也是枉然，此時仍需經驗知識豐富的老鳥才能應付，但程式能力只有一般水平是派不上用場的。由此推論，寫得出程式真的不值錢了，很會寫程式懂偵錯且具備效能、架構、擴充維護性知識才算值錢本事。</p> <p>靠會一點程式便有恃無恐的時代結束了，得再跑快一點惹。</p> <p>註：轉換結果我有丟上 <a href="https://github.com/darkthread/cht-num-string">Github</a>，如果有人想參考的話</p> <p data-slug="chatgpt-conv-program-language" data-fbimg="https://cdn.pixabay.com/photo/2019/08/09/06/12/car-racing-4394450_1280.jpg#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="/blog/cht-num-converter/">阿拉伯數字與中文數字雙向轉換 .NET 函式</a>貼文有讀者提到：如果將來不用 C#，豈不要重寫？</p> <p>好問題，其實寫函式時我就有想到這點，當時想法是，之所以擺脫 Visual Studio 程式庫從頭用 C# 重寫，便是著眼於相同演算法可移植到其他程式語言，更甭提我們現在有 ChatGPT 當靠山，這種沒用什麼平台專屬 API 的純演算法小功能，以 ChatGPT 4 的聰慧，理論上可以無痛轉移才對。</p> <p>理論歸理論，但實際上呢？實測一下便知。我打算將這個工具函式轉成 JavaScript 跟 Python 版本，看看需要花多少力氣。</p> <p>把程式碼貼給 ChatGPT-4，這點小事用不著情勒催眠角色扮演，Prompt 寫一句 <code>convert this c# to javascript</code> 即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638413132860843124.png"></p> <p>C# 轉 JavaScript 的部分，ChatGPT 偷懶了。兩個核心方法 ParseChtNum 及 ToChtNum，ChatGPT 只給了註解，說他知道一種美妙的寫法，可惜這裡太小寫不下 XD 但有提示如果有需要可另外提問。把 ChtNumConvert 型別程式單獨拿出來再問一次，這回便拿到完整轉換結果。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638413132864943894.png"></p> <p>ChatGPT 轉換的 JavaScript 可直接執行，沒編譯或 Runtime 錯誤：(註：有部分「零」沒有處理好，畫面沒截到)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638413132867339908.png"></p> <p>轉完 JavaScript，再來轉成 Python 試試。由於 ChatGPT 對 JavaScript 比對 C# 熟 (JavaScript 訓練資料較多)，我選擇請 ChatGPT 將剛轉好的 JavaScript 轉成 Python 而非從 C# 轉。這回 ChatGPT 很乾脆，整段程式一次轉完。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638413132869610719.png"></p> <p>Python 版本也可順利編譯執行，但結果的錯誤更明顯，出現「零零零」、「零三百十」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638413132871744554.png"></p> <p>動手 Debug，JavaScript 版的問題出在 C# Trim('零') 邏輯沒轉對，用 RegExp 只換了結尾，補上開頭置換就 OK 了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638413132873912983.png"></p> <p>Python 部分幾個則是幾個 C# Regular Expression 處理沒正確轉譯。雖然不熟 Python，在 <a href="/blog/github-copilot-demo/">Github Copilot</a> 協助下沒費什麼力氣就修正好了。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638413132876070410.png"></p> <p>修改後，C#、JavaScript 與 Python 三個版本產生的結果一致，確認轉換成功。</p> <p>總結一下本次實測結果：</p> <ol><li>本範例約 100 行 C# 程式，使用 ChatGPT 4 轉換耗時約一分鐘左右可拿到結果，轉換出來的 JavaScript 及 Python 版本均可直接執行，沒有編譯及 Runtime 錯誤，但執行結果有部分不正確</li> <li>JavaScript 有兩處需要修改，Python 有三處，但都是很明顯很容易修改的錯誤，前後不到五分鐘改好</li> <li>修改後，比對 C#、JavaScript、Python 三個版本執行結果一致，判定轉換成功</li></ol> <p>在本次 ChatGPT 火力展示，由於程式碼不長，未涉及專屬程式庫呼叫並以演算法為主，ChatGPT 從 A 程式語言轉成 B 程式語言的完成度及正確率頗高，人工確認結果及修 Bug 免不了，但比起徒手翻寫不知快 N 倍。即使面對不熟悉的程式語言(本例中的 Python)，靠著 ChatGPT 先轉換，利用 Github Copilot 協助修正，我也能在短短幾分鐘內自力完成翻寫，在 ChatGPT 出來之前，我沒想過有會有這麼一天。</p> <p>阿貓阿狗都能轉程式改程式，這意味著會寫程式不值錢了嗎？好像不得不承認是這樣沒錯。</p> <p>ChatGPT / AI 大幅降低了寫程式的門檻，新手生手不需要歷經漫長的學習也能生出可用的程式，老鳥的不可取代性下降(當然，品質效能擴充維護性有別，但很多時候，能解決眼前問題的堪用程式已值千金)；但很公平的是一件事是，新手有 AI 可以依靠，老鳥一樣也得到 AI 加持，像我不熟 Python，一樣只花幾分鐘就寫好 Python 版函式。</p> <p>正確地說，應該是入門到中級的程式技能貶值了。過去會寫程式在一般人眼中就是高手，會有很多人巴著你請你幫忙，排隊等你心情好賞賜幾支程式解決問題。</p> <p>如今，一般人靠著 ChatGPT/RPA/NoCode 工具也能寫出滿足需求的程式，沒必要把會寫程式的人當成寶。除非對效能、資源用量、擴充維護性有較高要求的情境，此時若經驗與知識不足，即便有 ChatGPT 無法精準提問也是枉然，此時仍需經驗知識豐富的老鳥才能應付，但程式能力只有一般水平是派不上用場的。由此推論，寫得出程式真的不值錢了，很會寫程式懂偵錯且具備效能、架構、擴充維護性知識才算值錢本事。</p> <p>靠會一點程式便有恃無恐的時代結束了，得再跑快一點惹。</p> <p>註：轉換結果我有丟上 <a href="https://github.com/darkthread/cht-num-string">Github</a>，如果有人想參考的話</p> <p data-slug="chatgpt-conv-program-language" data-fbimg="https://cdn.pixabay.com/photo/2019/08/09/06/12/car-racing-4394450_1280.jpg#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638413132860843124.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638413132860843124.png" medium="image"/>
    <pubDate>Sat, 20 Jan 2024 02:04:17 GMT</pubDate>
  </item><item>
    <title><![CDATA[.NET 工具函式 - 阿拉伯數字與中文數字雙向轉換]]></title>
    <link>https://blog.darkthread.net/blog/cht-num-converter/</link>
    <itunes:title><![CDATA[.NET 工具函式 - 阿拉伯數字與中文數字雙向轉換]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>十四年前我寫過<a href="/blog/chinese-number-char/">地址阿拉伯數字轉中文大寫</a>的 .NET 函式，但它有兩個問題：一是依賴 Visual Studio International Feature Pack，二是當年只做了阿拉伯數字轉中文，沒有中文數字逆解回阿拉伯數字的能力。</p> <p>總之，十四年後我把這個題目當成暖身練習，打算不靠第三方程式庫，用 C# 從頭寫一個阿拉伯數字與中文數字雙向轉換函式。</p> <p>為了充分測試各種狀況，我寫了一小段程式列舉從零到「1234 兆 1234 億 1234 萬 1234」的 16<sup>4</sup> = 65536 種組合，將數字轉中文，再將中文轉回數字，並比對還原後數字是否相符。</p> <pre><code>var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[4];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, -16:n0} {restored, -16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638411846253089805.png"></p> <p>隨意抽樣幾個較易出狀況的情境看起來都 OK：</p> <pre><code>          100,102           100,102 十萬零一百零二
1,021,023         1,021,023 一百零二萬一千零二十三
12,000,012        12,000,012 一千二百萬零一十二
103,100,310       103,100,310 一億零三百一十萬零三百一十
1,000,101,234     1,000,101,234 十億零一十萬一千二百三十四
1,000,000,000,310 1,000,000,000,310 一兆零三百一十
1,000,000,100,000 1,000,000,100,000 一兆零一十萬
</code></pre> <p>附上完整程式碼(含測試)，需要的同學請自取，如發現 Bug 歡迎留言反應，感謝。</p> <pre><code>using System.Text;
using System.Text.RegularExpressions;
var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[3];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, 16:n0} {restored, 16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
public class ChtNumConverter
{
public static string ChtNums = "零一二三四五六七八九";
public static Dictionary&lt;string, long&gt; ChtUnits = new Dictionary&lt;string, long&gt;{
{"十", 10},
{"百", 100},
{"千", 1000},
{"萬", 10000},
{"億", 100000000},
{"兆", 1000000000000}
};
// 解析中文數字
public static long ParseChtNum(string chtNumString)
{
var isNegative = false;
if (chtNumString.StartsWith("負"))
{
chtNumString = chtNumString.Substring(1);
isNegative = true;
}
long num = 0;
// 處理千百十範圍的四位數
Func&lt;string, long&gt; Parse4Digits = (s) =&gt;
{
long lastDigit = 0;
long subNum = 0;
foreach (var rawChar in s)
{
var c = rawChar.ToString().Replace("〇", "零");
if (ChtNums.Contains(c))
{
lastDigit = (long)ChtNums.IndexOf(c);
}
else if (ChtUnits.ContainsKey(c))
{
if (c == "十" &amp;&amp; lastDigit == 0) lastDigit = 1;
long unit = ChtUnits[c];
subNum += lastDigit * unit;
lastDigit = 0;
}
else
{
throw new ArgumentException($"包含無法解析的中文數字：{c}");
}
}
subNum += lastDigit;
return subNum;
};
// 以兆億萬分割四位值個別解析
foreach (var splitUnit in "兆億萬".ToArray())
{
var pos = chtNumString.IndexOf(splitUnit);
if (pos == -1) continue;
var subNumString = chtNumString.Substring(0, pos);
chtNumString = chtNumString.Substring(pos + 1);
num += Parse4Digits(subNumString) * ChtUnits[splitUnit.ToString()];
}
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
public static string ToChtNum(long n)
{
var negtive = n &lt; 0;
if (negtive) n = -n;
if (n &gt;= 10000 * ChtUnits["兆"])
throw new ArgumentException("數字超出可轉換範圍");
var unitChars = "千百十".ToArray();
// 處理 0000 ~ 9999 範圍數字
Func&lt;long, string&gt; Conv4Digits = (subNum) =&gt;
{
var sb = new StringBuilder();
foreach (var c in unitChars)
{
if (subNum &gt;= ChtUnits[c.ToString()])
{
var digit = subNum / ChtUnits[c.ToString()];
subNum = subNum % ChtUnits[c.ToString()];
sb.Append($"{ChtNums[(int)digit]}{c}");
}
else sb.Append("零");
}
sb.Append(ChtNums[(int)subNum]);
return sb.ToString();
};
var numString = new StringBuilder();
var forceRun = false;
foreach (var splitUnit in "兆億萬".ToArray())
{
var unit = ChtUnits[splitUnit.ToString()];
if (n &lt; unit)
{
if (forceRun) numString.Append("零");
continue;
}
forceRun = true;
var subNum = n / unit;
n = n % unit;
if (subNum &gt; 0)
numString.Append(Conv4Digits(subNum).TrimEnd('零') + splitUnit);
else numString.Append("零");
}
numString.Append(Conv4Digits(n));
var t = Regex.Replace(numString.ToString(), "[零]+", "零");
if (t.Length &gt; 1) t = t.Trim('零');
t = Regex.Replace(t, "^一十", "十");
return (negtive ? "負" : string.Empty) + t;
}
}
</code></pre> <p data-slug="cht-num-converter" data-title-en="Chinese numberals and Arabic numerals conversion functions by .NET" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2022/02/18/04/34/abacus-7019994_1280.jpg#1280#720">This article provide example of converting Arabic numerals and Chinese numerals.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>十四年前我寫過<a href="/blog/chinese-number-char/">地址阿拉伯數字轉中文大寫</a>的 .NET 函式，但它有兩個問題：一是依賴 Visual Studio International Feature Pack，二是當年只做了阿拉伯數字轉中文，沒有中文數字逆解回阿拉伯數字的能力。</p> <p>總之，十四年後我把這個題目當成暖身練習，打算不靠第三方程式庫，用 C# 從頭寫一個阿拉伯數字與中文數字雙向轉換函式。</p> <p>為了充分測試各種狀況，我寫了一小段程式列舉從零到「1234 兆 1234 億 1234 萬 1234」的 16<sup>4</sup> = 65536 種組合，將數字轉中文，再將中文轉回數字，並比對還原後數字是否相符。</p> <pre><code>var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[4];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, -16:n0} {restored, -16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638411846253089805.png"></p> <p>隨意抽樣幾個較易出狀況的情境看起來都 OK：</p> <pre><code>          100,102           100,102 十萬零一百零二
1,021,023         1,021,023 一百零二萬一千零二十三
12,000,012        12,000,012 一千二百萬零一十二
103,100,310       103,100,310 一億零三百一十萬零三百一十
1,000,101,234     1,000,101,234 十億零一十萬一千二百三十四
1,000,000,000,310 1,000,000,000,310 一兆零三百一十
1,000,000,100,000 1,000,000,100,000 一兆零一十萬
</code></pre> <p>附上完整程式碼(含測試)，需要的同學請自取，如發現 Bug 歡迎留言反應，感謝。</p> <pre><code>using System.Text;
using System.Text.RegularExpressions;
var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[3];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, 16:n0} {restored, 16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
public class ChtNumConverter
{
public static string ChtNums = "零一二三四五六七八九";
public static Dictionary&lt;string, long&gt; ChtUnits = new Dictionary&lt;string, long&gt;{
{"十", 10},
{"百", 100},
{"千", 1000},
{"萬", 10000},
{"億", 100000000},
{"兆", 1000000000000}
};
// 解析中文數字
public static long ParseChtNum(string chtNumString)
{
var isNegative = false;
if (chtNumString.StartsWith("負"))
{
chtNumString = chtNumString.Substring(1);
isNegative = true;
}
long num = 0;
// 處理千百十範圍的四位數
Func&lt;string, long&gt; Parse4Digits = (s) =&gt;
{
long lastDigit = 0;
long subNum = 0;
foreach (var rawChar in s)
{
var c = rawChar.ToString().Replace("〇", "零");
if (ChtNums.Contains(c))
{
lastDigit = (long)ChtNums.IndexOf(c);
}
else if (ChtUnits.ContainsKey(c))
{
if (c == "十" &amp;&amp; lastDigit == 0) lastDigit = 1;
long unit = ChtUnits[c];
subNum += lastDigit * unit;
lastDigit = 0;
}
else
{
throw new ArgumentException($"包含無法解析的中文數字：{c}");
}
}
subNum += lastDigit;
return subNum;
};
// 以兆億萬分割四位值個別解析
foreach (var splitUnit in "兆億萬".ToArray())
{
var pos = chtNumString.IndexOf(splitUnit);
if (pos == -1) continue;
var subNumString = chtNumString.Substring(0, pos);
chtNumString = chtNumString.Substring(pos + 1);
num += Parse4Digits(subNumString) * ChtUnits[splitUnit.ToString()];
}
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
public static string ToChtNum(long n)
{
var negtive = n &lt; 0;
if (negtive) n = -n;
if (n &gt;= 10000 * ChtUnits["兆"])
throw new ArgumentException("數字超出可轉換範圍");
var unitChars = "千百十".ToArray();
// 處理 0000 ~ 9999 範圍數字
Func&lt;long, string&gt; Conv4Digits = (subNum) =&gt;
{
var sb = new StringBuilder();
foreach (var c in unitChars)
{
if (subNum &gt;= ChtUnits[c.ToString()])
{
var digit = subNum / ChtUnits[c.ToString()];
subNum = subNum % ChtUnits[c.ToString()];
sb.Append($"{ChtNums[(int)digit]}{c}");
}
else sb.Append("零");
}
sb.Append(ChtNums[(int)subNum]);
return sb.ToString();
};
var numString = new StringBuilder();
var forceRun = false;
foreach (var splitUnit in "兆億萬".ToArray())
{
var unit = ChtUnits[splitUnit.ToString()];
if (n &lt; unit)
{
if (forceRun) numString.Append("零");
continue;
}
forceRun = true;
var subNum = n / unit;
n = n % unit;
if (subNum &gt; 0)
numString.Append(Conv4Digits(subNum).TrimEnd('零') + splitUnit);
else numString.Append("零");
}
numString.Append(Conv4Digits(n));
var t = Regex.Replace(numString.ToString(), "[零]+", "零");
if (t.Length &gt; 1) t = t.Trim('零');
t = Regex.Replace(t, "^一十", "十");
return (negtive ? "負" : string.Empty) + t;
}
}
</code></pre> <p data-slug="cht-num-converter" data-title-en="Chinese numberals and Arabic numerals conversion functions by .NET" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2022/02/18/04/34/abacus-7019994_1280.jpg#1280#720">This article provide example of converting Arabic numerals and Chinese numerals.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>十四年前我寫過<a href="/blog/chinese-number-char/">地址阿拉伯數字轉中文大寫</a>的 .NET 函式，但它有兩個問題：一是依賴 Visual Studio International Feature Pack，二是當年只做了阿拉伯數字轉中文，沒有中文數字逆解回阿拉伯數字的能力。</p> <p>總之，十四年後我把這個題目當成暖身練習，打算不靠第三方程式庫，用 C# 從頭寫一個阿拉伯數字與中文數字雙向轉換函式。</p> <p>為了充分測試各種狀況，我寫了一小段程式列舉從零到「1234 兆 1234 億 1234 萬 1234」的 16<sup>4</sup> = 65536 種組合，將數字轉中文，再將中文轉回數字，並比對還原後數字是否相符。</p> <pre><code>var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[4];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, -16:n0} {restored, -16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638411846253089805.png"></p> <p>隨意抽樣幾個較易出狀況的情境看起來都 OK：</p> <pre><code>          100,102           100,102 十萬零一百零二
1,021,023         1,021,023 一百零二萬一千零二十三
12,000,012        12,000,012 一千二百萬零一十二
103,100,310       103,100,310 一億零三百一十萬零三百一十
1,000,101,234     1,000,101,234 十億零一十萬一千二百三十四
1,000,000,000,310 1,000,000,000,310 一兆零三百一十
1,000,000,100,000 1,000,000,100,000 一兆零一十萬
</code></pre> <p>附上完整程式碼(含測試)，需要的同學請自取，如發現 Bug 歡迎留言反應，感謝。</p> <pre><code>using System.Text;
using System.Text.RegularExpressions;
var numSets = new[] {
"0000", "0001", "0010", "0012",
"0100", "0102", "0310", "0123",
"1000", "1002", "1020", "1023",
"1200", "1203", "1230", "1234"
};
// 列出所有可能的組合
var count = numSets.Length;
var idxs = new int[3];
var lv = idxs.Length - 1;
while (true)
{
var sb = new StringBuilder();
for (var i = 0; i &lt; idxs.Length; i++)
{
sb.Append(numSets[idxs[i]]);
}
long n = long.Parse(sb.ToString());
var cht = ChtNumConverter.ToChtNum(n);
var restored = ChtNumConverter.ParseChtNum(cht);
Console.WriteLine($"{n, 16:n0} {restored, 16:n0} " +
$"\x1b[{(n == restored ? "32mPASS" : "31mFAIL")}\x1b[0m {cht} ");
if (n != restored)
throw new ApplicationException($"數字轉換錯誤 {n} vs {restored}");
if (idxs.All(o =&gt; o == count - 1)) break;
idxs[lv]++;
while (idxs[lv] == count)
{
idxs[lv] = 0;
lv--;
if (lv &lt; 0) break;
idxs[lv]++;
}
lv = idxs.Length - 1;
}
public class ChtNumConverter
{
public static string ChtNums = "零一二三四五六七八九";
public static Dictionary&lt;string, long&gt; ChtUnits = new Dictionary&lt;string, long&gt;{
{"十", 10},
{"百", 100},
{"千", 1000},
{"萬", 10000},
{"億", 100000000},
{"兆", 1000000000000}
};
// 解析中文數字
public static long ParseChtNum(string chtNumString)
{
var isNegative = false;
if (chtNumString.StartsWith("負"))
{
chtNumString = chtNumString.Substring(1);
isNegative = true;
}
long num = 0;
// 處理千百十範圍的四位數
Func&lt;string, long&gt; Parse4Digits = (s) =&gt;
{
long lastDigit = 0;
long subNum = 0;
foreach (var rawChar in s)
{
var c = rawChar.ToString().Replace("〇", "零");
if (ChtNums.Contains(c))
{
lastDigit = (long)ChtNums.IndexOf(c);
}
else if (ChtUnits.ContainsKey(c))
{
if (c == "十" &amp;&amp; lastDigit == 0) lastDigit = 1;
long unit = ChtUnits[c];
subNum += lastDigit * unit;
lastDigit = 0;
}
else
{
throw new ArgumentException($"包含無法解析的中文數字：{c}");
}
}
subNum += lastDigit;
return subNum;
};
// 以兆億萬分割四位值個別解析
foreach (var splitUnit in "兆億萬".ToArray())
{
var pos = chtNumString.IndexOf(splitUnit);
if (pos == -1) continue;
var subNumString = chtNumString.Substring(0, pos);
chtNumString = chtNumString.Substring(pos + 1);
num += Parse4Digits(subNumString) * ChtUnits[splitUnit.ToString()];
}
num += Parse4Digits(chtNumString);
return isNegative ? -num : num;
}
// 轉換為中文數字
public static string ToChtNum(long n)
{
var negtive = n &lt; 0;
if (negtive) n = -n;
if (n &gt;= 10000 * ChtUnits["兆"])
throw new ArgumentException("數字超出可轉換範圍");
var unitChars = "千百十".ToArray();
// 處理 0000 ~ 9999 範圍數字
Func&lt;long, string&gt; Conv4Digits = (subNum) =&gt;
{
var sb = new StringBuilder();
foreach (var c in unitChars)
{
if (subNum &gt;= ChtUnits[c.ToString()])
{
var digit = subNum / ChtUnits[c.ToString()];
subNum = subNum % ChtUnits[c.ToString()];
sb.Append($"{ChtNums[(int)digit]}{c}");
}
else sb.Append("零");
}
sb.Append(ChtNums[(int)subNum]);
return sb.ToString();
};
var numString = new StringBuilder();
var forceRun = false;
foreach (var splitUnit in "兆億萬".ToArray())
{
var unit = ChtUnits[splitUnit.ToString()];
if (n &lt; unit)
{
if (forceRun) numString.Append("零");
continue;
}
forceRun = true;
var subNum = n / unit;
n = n % unit;
if (subNum &gt; 0)
numString.Append(Conv4Digits(subNum).TrimEnd('零') + splitUnit);
else numString.Append("零");
}
numString.Append(Conv4Digits(n));
var t = Regex.Replace(numString.ToString(), "[零]+", "零");
if (t.Length &gt; 1) t = t.Trim('零');
t = Regex.Replace(t, "^一十", "十");
return (negtive ? "負" : string.Empty) + t;
}
}
</code></pre> <p data-slug="cht-num-converter" data-title-en="Chinese numberals and Arabic numerals conversion functions by .NET" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2022/02/18/04/34/abacus-7019994_1280.jpg#1280#720">This article provide example of converting Arabic numerals and Chinese numerals.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638411846253089805.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638411846253089805.png" medium="image"/>
    <pubDate>Thu, 18 Jan 2024 14:23:54 GMT</pubDate>
  </item><item>
    <title><![CDATA[Dapper 之字串參數查詢效能地雷]]></title>
    <link>https://blog.darkthread.net/blog/dapper-dbstring/</link>
    <itunes:title><![CDATA[Dapper 之字串參數查詢效能地雷]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>同事踩到 Dapper 的雷，我跟著上了一課，筆記備忘。</p> <p>用一個 2,000 萬筆資料的 Oracle 資料表重現問題。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638410980848373581.png"></p> <p>用 Dapper 執行 <code>UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id</code>，id 參數分別用匿名型別 <code>new { id }</code> 及 <code>var p = new DynamicParameters(); p.Add("id", id);</code> 傳入，總共測 5 次，兩種寫法的執行速度天差地別，相差 100 倍：<br> (註：有使用 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping 修正 Unicode 字元寫入問題</a>)</p> <pre><code>static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest();
}
Console.ReadLine();
}
static void RunTest()
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
}
}
</code></pre> <p>使用昨天說的 <a href="/blog/spy-parameter-types-in-dapper/">SimulateDapperDbParameters()</a> 檢查二者產生的參數型別，發現關鍵差異：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410977560824052.png"></p> <p>使用匿名型別傳參數時，對映出來的 OracleParameter 型別是 NVarChar2，而 DynamicParameters 則對映到 VarChar2。</p> <p>這裡藏了一個陷阱，當欄位型別是 VarChar，若用 NVarChar 型別參數進行比對，將因型別不同無法使用索引(從 Index Seek 變成 Index Scan，細節可參考<a href="https://dotblogs.com.tw/supershowwei/2019/08/12/232213">軟體主廚的實測</a>或這篇 <a href="https://lostechies.com/jimmybogard/2012/07/18/troubleshooting-sql-index-performance-on-varchar-columns/">Troubleshooting SQL index performance on varchar columns</a>)，當資料筆數很多，有沒有索引便會形成明顯差異。資料筆數愈多，差距愈大。</p> <p>知道原因就不難解決。針對 WHERE 條件式中參數須使用 VarChar 型別確保查詢效能的情境，Dapper 提供的解法是透過 <a href="https://github.com/DapperLib/Dapper?tab=readme-ov-file#ansi-strings-and-varchar">DbString 類別 IsAnsi 屬性</a>指定將 string 對映成 VarChar (SQL) / VarChar2 (Oracle) 型別。在本案例改成 <code>new { id = new DbString { Value = id, IsAnsi = true, Length = id.Length } }</code> 即可確保 Dapper 用 VarChar2 傳送 id 參數。依我的個性，肯定受不了程式碼冒出一大堆又臭又長的 <code>new DbString { Value = ..., IsAnsi = true... }</code>，我選擇寫個擴充方法簡化成 <code>new { id = id.ToConstVarChar() }</code>：</p> <pre><code>internal class Program
{
static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest(i == 4);
}
Console.ReadLine();
}
static void RunTest(bool printParamTypes)
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id = id.ToConstVarChar() };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, anonymousType);
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, p);
}
}
}
public static class ExtMethods
{
public static DbString ToConstVarChar(this string str) =&gt;
new DbString { Value = str, IsAnsi = true, Length = str.Length };
}
</code></pre> <p>改用 DbString 後，參數型別變成 VarChar2，執行速度提升到跟 DynamicParameter 一樣快(註：第一次冷啟動耗時較久不計)，問題排除。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410977563209027.png"></p> <p>這是個很容易踩到的陷阱，若 Dapper (其實用 ADO.NET 也會中) 有用到 WHERE 比對 VarChar / VarChar2，切記要確認型別以免寫出效能不佳的程式碼。</p> <p>【延伸閱讀】</p> <ul><li><a href="/blog/dapper-odpnet-unicode-issue/">Hacking樂無窮：修正Dapper＋ODP.NET無法寫入Unicode問題</a></li> <li><a href="/blog/dapper-dynaparam-odpnet-unicode-issue-cause/">Dapper DynamicParameters 不會換 NVarChar</a></li></ul> <p data-slug="dapper-dbstring" data-title-en="Performance issue of NVarChar parameter type in where when using Dapper" data-catg=".NET" data-fbimg="https://en.wikipedia.org/wiki/Anti-tank_mine#/media/File:TM-46_AP-mine.JPEG#2850#1880">Introduce the potential performance issues that may occur when mapping a string to NVarChar in Dapper and how to avoid them using DbString.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>同事踩到 Dapper 的雷，我跟著上了一課，筆記備忘。</p> <p>用一個 2,000 萬筆資料的 Oracle 資料表重現問題。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638410980848373581.png"></p> <p>用 Dapper 執行 <code>UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id</code>，id 參數分別用匿名型別 <code>new { id }</code> 及 <code>var p = new DynamicParameters(); p.Add("id", id);</code> 傳入，總共測 5 次，兩種寫法的執行速度天差地別，相差 100 倍：<br> (註：有使用 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping 修正 Unicode 字元寫入問題</a>)</p> <pre><code>static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest();
}
Console.ReadLine();
}
static void RunTest()
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
}
}
</code></pre> <p>使用昨天說的 <a href="/blog/spy-parameter-types-in-dapper/">SimulateDapperDbParameters()</a> 檢查二者產生的參數型別，發現關鍵差異：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410977560824052.png"></p> <p>使用匿名型別傳參數時，對映出來的 OracleParameter 型別是 NVarChar2，而 DynamicParameters 則對映到 VarChar2。</p> <p>這裡藏了一個陷阱，當欄位型別是 VarChar，若用 NVarChar 型別參數進行比對，將因型別不同無法使用索引(從 Index Seek 變成 Index Scan，細節可參考<a href="https://dotblogs.com.tw/supershowwei/2019/08/12/232213">軟體主廚的實測</a>或這篇 <a href="https://lostechies.com/jimmybogard/2012/07/18/troubleshooting-sql-index-performance-on-varchar-columns/">Troubleshooting SQL index performance on varchar columns</a>)，當資料筆數很多，有沒有索引便會形成明顯差異。資料筆數愈多，差距愈大。</p> <p>知道原因就不難解決。針對 WHERE 條件式中參數須使用 VarChar 型別確保查詢效能的情境，Dapper 提供的解法是透過 <a href="https://github.com/DapperLib/Dapper?tab=readme-ov-file#ansi-strings-and-varchar">DbString 類別 IsAnsi 屬性</a>指定將 string 對映成 VarChar (SQL) / VarChar2 (Oracle) 型別。在本案例改成 <code>new { id = new DbString { Value = id, IsAnsi = true, Length = id.Length } }</code> 即可確保 Dapper 用 VarChar2 傳送 id 參數。依我的個性，肯定受不了程式碼冒出一大堆又臭又長的 <code>new DbString { Value = ..., IsAnsi = true... }</code>，我選擇寫個擴充方法簡化成 <code>new { id = id.ToConstVarChar() }</code>：</p> <pre><code>internal class Program
{
static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest(i == 4);
}
Console.ReadLine();
}
static void RunTest(bool printParamTypes)
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id = id.ToConstVarChar() };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, anonymousType);
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, p);
}
}
}
public static class ExtMethods
{
public static DbString ToConstVarChar(this string str) =&gt;
new DbString { Value = str, IsAnsi = true, Length = str.Length };
}
</code></pre> <p>改用 DbString 後，參數型別變成 VarChar2，執行速度提升到跟 DynamicParameter 一樣快(註：第一次冷啟動耗時較久不計)，問題排除。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410977563209027.png"></p> <p>這是個很容易踩到的陷阱，若 Dapper (其實用 ADO.NET 也會中) 有用到 WHERE 比對 VarChar / VarChar2，切記要確認型別以免寫出效能不佳的程式碼。</p> <p>【延伸閱讀】</p> <ul><li><a href="/blog/dapper-odpnet-unicode-issue/">Hacking樂無窮：修正Dapper＋ODP.NET無法寫入Unicode問題</a></li> <li><a href="/blog/dapper-dynaparam-odpnet-unicode-issue-cause/">Dapper DynamicParameters 不會換 NVarChar</a></li></ul> <p data-slug="dapper-dbstring" data-title-en="Performance issue of NVarChar parameter type in where when using Dapper" data-catg=".NET" data-fbimg="https://en.wikipedia.org/wiki/Anti-tank_mine#/media/File:TM-46_AP-mine.JPEG#2850#1880">Introduce the potential performance issues that may occur when mapping a string to NVarChar in Dapper and how to avoid them using DbString.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>同事踩到 Dapper 的雷，我跟著上了一課，筆記備忘。</p> <p>用一個 2,000 萬筆資料的 Oracle 資料表重現問題。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638410980848373581.png"></p> <p>用 Dapper 執行 <code>UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id</code>，id 參數分別用匿名型別 <code>new { id }</code> 及 <code>var p = new DynamicParameters(); p.Add("id", id);</code> 傳入，總共測 5 次，兩種寫法的執行速度天差地別，相差 100 倍：<br> (註：有使用 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping 修正 Unicode 字元寫入問題</a>)</p> <pre><code>static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest();
}
Console.ReadLine();
}
static void RunTest()
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
}
}
</code></pre> <p>使用昨天說的 <a href="/blog/spy-parameter-types-in-dapper/">SimulateDapperDbParameters()</a> 檢查二者產生的參數型別，發現關鍵差異：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410977560824052.png"></p> <p>使用匿名型別傳參數時，對映出來的 OracleParameter 型別是 NVarChar2，而 DynamicParameters 則對映到 VarChar2。</p> <p>這裡藏了一個陷阱，當欄位型別是 VarChar，若用 NVarChar 型別參數進行比對，將因型別不同無法使用索引(從 Index Seek 變成 Index Scan，細節可參考<a href="https://dotblogs.com.tw/supershowwei/2019/08/12/232213">軟體主廚的實測</a>或這篇 <a href="https://lostechies.com/jimmybogard/2012/07/18/troubleshooting-sql-index-performance-on-varchar-columns/">Troubleshooting SQL index performance on varchar columns</a>)，當資料筆數很多，有沒有索引便會形成明顯差異。資料筆數愈多，差距愈大。</p> <p>知道原因就不難解決。針對 WHERE 條件式中參數須使用 VarChar 型別確保查詢效能的情境，Dapper 提供的解法是透過 <a href="https://github.com/DapperLib/Dapper?tab=readme-ov-file#ansi-strings-and-varchar">DbString 類別 IsAnsi 屬性</a>指定將 string 對映成 VarChar (SQL) / VarChar2 (Oracle) 型別。在本案例改成 <code>new { id = new DbString { Value = id, IsAnsi = true, Length = id.Length } }</code> 即可確保 Dapper 用 VarChar2 傳送 id 參數。依我的個性，肯定受不了程式碼冒出一大堆又臭又長的 <code>new DbString { Value = ..., IsAnsi = true... }</code>，我選擇寫個擴充方法簡化成 <code>new { id = id.ToConstVarChar() }</code>：</p> <pre><code>internal class Program
{
static void Main(string[] args)
{
FixOdpNetDbTypeStringMapping();
for (var i = 0; i &lt; 5; i++)
{
RunTest(i == 4);
}
Console.ReadLine();
}
static void RunTest(bool printParamTypes)
{
var sql = "UPDATE EXAMPLE SET Score = Score + 1 WHERE Id = :id";
var rnd = new Random();
var rowCount = 20000000;
using (var cn = new OracleConnection(cs))
{
var id = $"{rnd.Next(rowCount):x8}";
var anonymousType = new { id = id.ToConstVarChar() };
var sw = new Stopwatch();
sw.Start();
var affected = cn.Execute(sql, anonymousType);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, anonymousType);
id = $"{rnd.Next(rowCount):x8}";
var p = new DynamicParameters();
p.Add("id", id);
sw.Restart();
affected = cn.Execute(sql, p);
sw.Stop();
Console.WriteLine($"{id} - {affected} row updated. ({sw.ElapsedMilliseconds} ms)");
if (printParamTypes) SimulateDapperDbParameters(sql, cn, p);
}
}
}
public static class ExtMethods
{
public static DbString ToConstVarChar(this string str) =&gt;
new DbString { Value = str, IsAnsi = true, Length = str.Length };
}
</code></pre> <p>改用 DbString 後，參數型別變成 VarChar2，執行速度提升到跟 DynamicParameter 一樣快(註：第一次冷啟動耗時較久不計)，問題排除。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410977563209027.png"></p> <p>這是個很容易踩到的陷阱，若 Dapper (其實用 ADO.NET 也會中) 有用到 WHERE 比對 VarChar / VarChar2，切記要確認型別以免寫出效能不佳的程式碼。</p> <p>【延伸閱讀】</p> <ul><li><a href="/blog/dapper-odpnet-unicode-issue/">Hacking樂無窮：修正Dapper＋ODP.NET無法寫入Unicode問題</a></li> <li><a href="/blog/dapper-dynaparam-odpnet-unicode-issue-cause/">Dapper DynamicParameters 不會換 NVarChar</a></li></ul> <p data-slug="dapper-dbstring" data-title-en="Performance issue of NVarChar parameter type in where when using Dapper" data-catg=".NET" data-fbimg="https://en.wikipedia.org/wiki/Anti-tank_mine#/media/File:TM-46_AP-mine.JPEG#2850#1880">Introduce the potential performance issues that may occur when mapping a string to NVarChar in Dapper and how to avoid them using DbString.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig3_638410980848373581.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig3_638410980848373581.png" medium="image"/>
    <pubDate>Wed, 17 Jan 2024 14:16:24 GMT</pubDate>
  </item><item>
    <title><![CDATA[偷看 Dapper 產生的 SQL 指令與參數型別]]></title>
    <link>https://blog.darkthread.net/blog/spy-parameter-types-in-dapper/</link>
    <itunes:title><![CDATA[偷看 Dapper 產生的 SQL 指令與參數型別]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>這是調查 Dapper 參數茶包的副產品，檢查底層對映參數型別的工具函式。</p> <p>遇到一個 Dapper 問題，懷疑與匿名參數物件屬性對映的 OracleParameter 型別有關。而 Oracle 不像 MSSQL 有 Profiler 可以偷看實體指令，我便想從程式端突破，設法在執行時取得參數資訊。原本我寄望於現成程式庫，但找到最接近的解決方案是 <a href="https://www.nuget.org/packages/MiniProfiler.Integrations/">MiniProfiler.Integrations </a>，預設只支援 SQL Server / MySQL，如果要用在 Oracle，得寫幾行程式為 OracleConnection 實作 <code>IDbConnectionFactory</code>：</p> <pre><code>public class OracleDbConnectionFactory : IDbConnectionFactory
{
private readonly string _connectionString;
public OracleDbConnectionFactory(string connectionString)
{
_connectionString = connectionString;
}
public DbConnection CreateConnection()
{
return new OracleConnection(_connectionString);
}
}
</code></pre> <p>是有成功印出 SQL 指令跟參數，它的參數資訊為 <code>Dictionary&lt;string, object&gt;</code> 型別，只看得到參數名稱跟參數值，無法得知 OracleParameter.OracleDbType：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410080049520570.png"></p> <p>懶得再繼續試其他程式庫，不如動手自己寫吧。我追進 Dapper 原始碼，發現 IDbCommand 的參數會<a href="https://github.com/DapperLib/Dapper/blob/8f369914ac4a03967fb68b3af6b0a85582473e3c/Dapper/CommandDefinition.cs#L144">透過 CacheInfo.ParamReader(IDbCommand, object?) 產生</a>，只是 CacheInfo 的相關型別、方法、建構式都屬內部 API，不開放外部呼叫。所幸，有 Reflection 在手，即使標成 internal、private 也攔不住我們，層層解開，終於一窺底層樣貌：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410080055877544.png"></p> <p>SimulateDapperDbParameters() 會模擬 Dapper 執行 IDbCommand 的過程，將參數一一對映成 OracleParameter 或 SqlParameter (若需支援其他資料庫可自行增加)，實測 Oracle 及 SQL 可抓到實際的參數物件型別，還順便驗證 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping</a> 確實可修正將 string 對映到 NVarChar2，證明有抓取到正確結果。</p> <p>範例程式碼如下，有需要的同學請自取修改利用：(注意：GetConstructors() 取建構式部分我偷懶寫死 [1] 取陣列第二個 Overloading，而 Dapper 新舊版的參數數量及型別可能不同，若不相容請自行調整)</p> <pre><code>using Dapper;
using Oracle.ManagedDataAccess.Client;
using StackExchange.Profiling.Internal;
using System;
using System.Data;
using System.Data.Common;
using System.Data.SqlClient;
using System.Diagnostics;
using System.Dynamic;
using System.Linq;
using System.Reflection;
using System.Transactions;
namespace NetFxConsole
{
internal class Program
{
// TODO: encrypt the connection string and store it in the app.config
private const string csOracle =
"Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=SqlDB)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=XEPDB1)));User Id=LAB;Password=****;";
private const string csMsSql = "Data Source=SqlDB;Initial Catalog=LAB;User Id=LAB;Password=****;";
static object GetSqlMapperParamReader(CommandDefinition cmd, IDbConnection cnn, object paramArg)
{
var sqlMapperType = typeof(SqlMapper);
var sqlMapperIdentityType = sqlMapperType.Assembly.GetType("Dapper.SqlMapper+Identity");
// TODO: hard-coded here, array index and parameters may differ in different versions of Dapper
var identity = sqlMapperIdentityType.GetConstructors(
BindingFlags.Instance | BindingFlags.NonPublic)[1].Invoke(new object[]
{
cmd.CommandText, cmd.CommandType, string.Empty, typeof(string), paramArg.GetType(), null, 0
});
dynamic cacheInfo = sqlMapperType.GetMethod("GetCacheInfo", BindingFlags.Static | BindingFlags.NonPublic)
.Invoke(null, new object[] { identity, paramArg, false });
return cacheInfo.GetType().GetProperty("ParamReader").GetValue(cacheInfo, null);
}
public static void SimulateDapperDbParameters(string commandText, IDbConnection cnn, object paramArg)
{
var command = new CommandDefinition(commandText, paramArg);
var paramReader = GetSqlMapperParamReader(command, cnn, paramArg);
var mi = command.GetType().GetMethod("SetupCommand", BindingFlags.Instance | BindingFlags.NonPublic);
var cmd = mi.Invoke(command, new object[] { cnn, paramReader });
switch (cmd)
{
case OracleCommand oc:
Console.WriteLine($"CommandText: \x1b[33m{oc.CommandText}\x1b[0m");
foreach (OracleParameter p in oc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.OracleDbType}({p.Size})",-16} \u001b[0m = {p.Value}");
}
break;
case SqlCommand sc:
Console.WriteLine($"CommandText: \x1b[33m{sc.CommandText}\x1b[0m");
foreach (SqlParameter p in sc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.SqlDbType}({p.Size})",-16}\u001b[0m = {p.Value}");
}
break;
default:
throw new NotSupportedException();
}
}
static void Main(string[] args)
{
var sql = "INSERT INTO SAMPLE VALUES (:name, :regDate, :score)";
var paramObject = new
{
name = "Jeffrey",
regDate = new DateTime(2012, 12, 21),
score = 32767
};
using (var cn = new OracleConnection(csOracle))
{
SimulateDapperDbParameters(sql, cn, paramObject);
FixOdpNetDbTypeStringMapping();
SimulateDapperDbParameters(sql, cn, paramObject);
}
using (var cn = new SqlConnection(csMsSql))
{
sql = sql.Replace(":", "@");
SimulateDapperDbParameters(sql, cn, paramObject);
}
Console.ReadLine();
}
// https://blog.darkthread.net/blog/dapper-odpnet-unicode-issue/
static void FixOdpNetDbTypeStringMapping()
{
Assembly asm = typeof(OracleConnection).Assembly;
Type tOraDb_DbTypeTable = asm.GetType("Oracle.ManagedDataAccess.Client.OraDb_DbTypeTable");
var fldDbTypeMapping = tOraDb_DbTypeTable.GetField("dbTypeToOracleDbTypeMapping",
BindingFlags.Static | BindingFlags.NonPublic);
int[] mappings = (int[])fldDbTypeMapping.GetValue(null);
mappings[(int)System.Data.DbType.String] = (int)OracleDbType.NVarchar2;
fldDbTypeMapping.SetValue(null, mappings);
}
}
}
</code></pre> <p>補充顯示資料時用到兩則小技巧：</p> <ul><li><a href="/blog/ansi-escape-code/">ANSI 顏色控制碼 - 讓 Console 輸出五彩繽紛</a></li> <li><a href="/blog/string-format-alignment/">【笨問題】C# 字串補空白與靠左右對齊</a></li></ul> <p data-slug="spy-parameter-types-in-dapper" data-title-en="Inspect the parameter type generated by Dapper" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">A function to inspect the parameter types generated by Dapper with reflection.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>這是調查 Dapper 參數茶包的副產品，檢查底層對映參數型別的工具函式。</p> <p>遇到一個 Dapper 問題，懷疑與匿名參數物件屬性對映的 OracleParameter 型別有關。而 Oracle 不像 MSSQL 有 Profiler 可以偷看實體指令，我便想從程式端突破，設法在執行時取得參數資訊。原本我寄望於現成程式庫，但找到最接近的解決方案是 <a href="https://www.nuget.org/packages/MiniProfiler.Integrations/">MiniProfiler.Integrations </a>，預設只支援 SQL Server / MySQL，如果要用在 Oracle，得寫幾行程式為 OracleConnection 實作 <code>IDbConnectionFactory</code>：</p> <pre><code>public class OracleDbConnectionFactory : IDbConnectionFactory
{
private readonly string _connectionString;
public OracleDbConnectionFactory(string connectionString)
{
_connectionString = connectionString;
}
public DbConnection CreateConnection()
{
return new OracleConnection(_connectionString);
}
}
</code></pre> <p>是有成功印出 SQL 指令跟參數，它的參數資訊為 <code>Dictionary&lt;string, object&gt;</code> 型別，只看得到參數名稱跟參數值，無法得知 OracleParameter.OracleDbType：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410080049520570.png"></p> <p>懶得再繼續試其他程式庫，不如動手自己寫吧。我追進 Dapper 原始碼，發現 IDbCommand 的參數會<a href="https://github.com/DapperLib/Dapper/blob/8f369914ac4a03967fb68b3af6b0a85582473e3c/Dapper/CommandDefinition.cs#L144">透過 CacheInfo.ParamReader(IDbCommand, object?) 產生</a>，只是 CacheInfo 的相關型別、方法、建構式都屬內部 API，不開放外部呼叫。所幸，有 Reflection 在手，即使標成 internal、private 也攔不住我們，層層解開，終於一窺底層樣貌：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410080055877544.png"></p> <p>SimulateDapperDbParameters() 會模擬 Dapper 執行 IDbCommand 的過程，將參數一一對映成 OracleParameter 或 SqlParameter (若需支援其他資料庫可自行增加)，實測 Oracle 及 SQL 可抓到實際的參數物件型別，還順便驗證 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping</a> 確實可修正將 string 對映到 NVarChar2，證明有抓取到正確結果。</p> <p>範例程式碼如下，有需要的同學請自取修改利用：(注意：GetConstructors() 取建構式部分我偷懶寫死 [1] 取陣列第二個 Overloading，而 Dapper 新舊版的參數數量及型別可能不同，若不相容請自行調整)</p> <pre><code>using Dapper;
using Oracle.ManagedDataAccess.Client;
using StackExchange.Profiling.Internal;
using System;
using System.Data;
using System.Data.Common;
using System.Data.SqlClient;
using System.Diagnostics;
using System.Dynamic;
using System.Linq;
using System.Reflection;
using System.Transactions;
namespace NetFxConsole
{
internal class Program
{
// TODO: encrypt the connection string and store it in the app.config
private const string csOracle =
"Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=SqlDB)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=XEPDB1)));User Id=LAB;Password=****;";
private const string csMsSql = "Data Source=SqlDB;Initial Catalog=LAB;User Id=LAB;Password=****;";
static object GetSqlMapperParamReader(CommandDefinition cmd, IDbConnection cnn, object paramArg)
{
var sqlMapperType = typeof(SqlMapper);
var sqlMapperIdentityType = sqlMapperType.Assembly.GetType("Dapper.SqlMapper+Identity");
// TODO: hard-coded here, array index and parameters may differ in different versions of Dapper
var identity = sqlMapperIdentityType.GetConstructors(
BindingFlags.Instance | BindingFlags.NonPublic)[1].Invoke(new object[]
{
cmd.CommandText, cmd.CommandType, string.Empty, typeof(string), paramArg.GetType(), null, 0
});
dynamic cacheInfo = sqlMapperType.GetMethod("GetCacheInfo", BindingFlags.Static | BindingFlags.NonPublic)
.Invoke(null, new object[] { identity, paramArg, false });
return cacheInfo.GetType().GetProperty("ParamReader").GetValue(cacheInfo, null);
}
public static void SimulateDapperDbParameters(string commandText, IDbConnection cnn, object paramArg)
{
var command = new CommandDefinition(commandText, paramArg);
var paramReader = GetSqlMapperParamReader(command, cnn, paramArg);
var mi = command.GetType().GetMethod("SetupCommand", BindingFlags.Instance | BindingFlags.NonPublic);
var cmd = mi.Invoke(command, new object[] { cnn, paramReader });
switch (cmd)
{
case OracleCommand oc:
Console.WriteLine($"CommandText: \x1b[33m{oc.CommandText}\x1b[0m");
foreach (OracleParameter p in oc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.OracleDbType}({p.Size})",-16} \u001b[0m = {p.Value}");
}
break;
case SqlCommand sc:
Console.WriteLine($"CommandText: \x1b[33m{sc.CommandText}\x1b[0m");
foreach (SqlParameter p in sc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.SqlDbType}({p.Size})",-16}\u001b[0m = {p.Value}");
}
break;
default:
throw new NotSupportedException();
}
}
static void Main(string[] args)
{
var sql = "INSERT INTO SAMPLE VALUES (:name, :regDate, :score)";
var paramObject = new
{
name = "Jeffrey",
regDate = new DateTime(2012, 12, 21),
score = 32767
};
using (var cn = new OracleConnection(csOracle))
{
SimulateDapperDbParameters(sql, cn, paramObject);
FixOdpNetDbTypeStringMapping();
SimulateDapperDbParameters(sql, cn, paramObject);
}
using (var cn = new SqlConnection(csMsSql))
{
sql = sql.Replace(":", "@");
SimulateDapperDbParameters(sql, cn, paramObject);
}
Console.ReadLine();
}
// https://blog.darkthread.net/blog/dapper-odpnet-unicode-issue/
static void FixOdpNetDbTypeStringMapping()
{
Assembly asm = typeof(OracleConnection).Assembly;
Type tOraDb_DbTypeTable = asm.GetType("Oracle.ManagedDataAccess.Client.OraDb_DbTypeTable");
var fldDbTypeMapping = tOraDb_DbTypeTable.GetField("dbTypeToOracleDbTypeMapping",
BindingFlags.Static | BindingFlags.NonPublic);
int[] mappings = (int[])fldDbTypeMapping.GetValue(null);
mappings[(int)System.Data.DbType.String] = (int)OracleDbType.NVarchar2;
fldDbTypeMapping.SetValue(null, mappings);
}
}
}
</code></pre> <p>補充顯示資料時用到兩則小技巧：</p> <ul><li><a href="/blog/ansi-escape-code/">ANSI 顏色控制碼 - 讓 Console 輸出五彩繽紛</a></li> <li><a href="/blog/string-format-alignment/">【笨問題】C# 字串補空白與靠左右對齊</a></li></ul> <p data-slug="spy-parameter-types-in-dapper" data-title-en="Inspect the parameter type generated by Dapper" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">A function to inspect the parameter types generated by Dapper with reflection.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>這是調查 Dapper 參數茶包的副產品，檢查底層對映參數型別的工具函式。</p> <p>遇到一個 Dapper 問題，懷疑與匿名參數物件屬性對映的 OracleParameter 型別有關。而 Oracle 不像 MSSQL 有 Profiler 可以偷看實體指令，我便想從程式端突破，設法在執行時取得參數資訊。原本我寄望於現成程式庫，但找到最接近的解決方案是 <a href="https://www.nuget.org/packages/MiniProfiler.Integrations/">MiniProfiler.Integrations </a>，預設只支援 SQL Server / MySQL，如果要用在 Oracle，得寫幾行程式為 OracleConnection 實作 <code>IDbConnectionFactory</code>：</p> <pre><code>public class OracleDbConnectionFactory : IDbConnectionFactory
{
private readonly string _connectionString;
public OracleDbConnectionFactory(string connectionString)
{
_connectionString = connectionString;
}
public DbConnection CreateConnection()
{
return new OracleConnection(_connectionString);
}
}
</code></pre> <p>是有成功印出 SQL 指令跟參數，它的參數資訊為 <code>Dictionary&lt;string, object&gt;</code> 型別，只看得到參數名稱跟參數值，無法得知 OracleParameter.OracleDbType：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638410080049520570.png"></p> <p>懶得再繼續試其他程式庫，不如動手自己寫吧。我追進 Dapper 原始碼，發現 IDbCommand 的參數會<a href="https://github.com/DapperLib/Dapper/blob/8f369914ac4a03967fb68b3af6b0a85582473e3c/Dapper/CommandDefinition.cs#L144">透過 CacheInfo.ParamReader(IDbCommand, object?) 產生</a>，只是 CacheInfo 的相關型別、方法、建構式都屬內部 API，不開放外部呼叫。所幸，有 Reflection 在手，即使標成 internal、private 也攔不住我們，層層解開，終於一窺底層樣貌：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638410080055877544.png"></p> <p>SimulateDapperDbParameters() 會模擬 Dapper 執行 IDbCommand 的過程，將參數一一對映成 OracleParameter 或 SqlParameter (若需支援其他資料庫可自行增加)，實測 Oracle 及 SQL 可抓到實際的參數物件型別，還順便驗證 <a href="/blog/dapper-odpnet-unicode-issue/">FixOdpNetDbTypeStringMapping</a> 確實可修正將 string 對映到 NVarChar2，證明有抓取到正確結果。</p> <p>範例程式碼如下，有需要的同學請自取修改利用：(注意：GetConstructors() 取建構式部分我偷懶寫死 [1] 取陣列第二個 Overloading，而 Dapper 新舊版的參數數量及型別可能不同，若不相容請自行調整)</p> <pre><code>using Dapper;
using Oracle.ManagedDataAccess.Client;
using StackExchange.Profiling.Internal;
using System;
using System.Data;
using System.Data.Common;
using System.Data.SqlClient;
using System.Diagnostics;
using System.Dynamic;
using System.Linq;
using System.Reflection;
using System.Transactions;
namespace NetFxConsole
{
internal class Program
{
// TODO: encrypt the connection string and store it in the app.config
private const string csOracle =
"Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=SqlDB)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=XEPDB1)));User Id=LAB;Password=****;";
private const string csMsSql = "Data Source=SqlDB;Initial Catalog=LAB;User Id=LAB;Password=****;";
static object GetSqlMapperParamReader(CommandDefinition cmd, IDbConnection cnn, object paramArg)
{
var sqlMapperType = typeof(SqlMapper);
var sqlMapperIdentityType = sqlMapperType.Assembly.GetType("Dapper.SqlMapper+Identity");
// TODO: hard-coded here, array index and parameters may differ in different versions of Dapper
var identity = sqlMapperIdentityType.GetConstructors(
BindingFlags.Instance | BindingFlags.NonPublic)[1].Invoke(new object[]
{
cmd.CommandText, cmd.CommandType, string.Empty, typeof(string), paramArg.GetType(), null, 0
});
dynamic cacheInfo = sqlMapperType.GetMethod("GetCacheInfo", BindingFlags.Static | BindingFlags.NonPublic)
.Invoke(null, new object[] { identity, paramArg, false });
return cacheInfo.GetType().GetProperty("ParamReader").GetValue(cacheInfo, null);
}
public static void SimulateDapperDbParameters(string commandText, IDbConnection cnn, object paramArg)
{
var command = new CommandDefinition(commandText, paramArg);
var paramReader = GetSqlMapperParamReader(command, cnn, paramArg);
var mi = command.GetType().GetMethod("SetupCommand", BindingFlags.Instance | BindingFlags.NonPublic);
var cmd = mi.Invoke(command, new object[] { cnn, paramReader });
switch (cmd)
{
case OracleCommand oc:
Console.WriteLine($"CommandText: \x1b[33m{oc.CommandText}\x1b[0m");
foreach (OracleParameter p in oc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.OracleDbType}({p.Size})",-16} \u001b[0m = {p.Value}");
}
break;
case SqlCommand sc:
Console.WriteLine($"CommandText: \x1b[33m{sc.CommandText}\x1b[0m");
foreach (SqlParameter p in sc.Parameters)
{
Console.WriteLine($" * \u001b[36m{p.ParameterName,-8}\u001b[0m / {p.DbType,-8} / \u001b[35m{$"{p.SqlDbType}({p.Size})",-16}\u001b[0m = {p.Value}");
}
break;
default:
throw new NotSupportedException();
}
}
static void Main(string[] args)
{
var sql = "INSERT INTO SAMPLE VALUES (:name, :regDate, :score)";
var paramObject = new
{
name = "Jeffrey",
regDate = new DateTime(2012, 12, 21),
score = 32767
};
using (var cn = new OracleConnection(csOracle))
{
SimulateDapperDbParameters(sql, cn, paramObject);
FixOdpNetDbTypeStringMapping();
SimulateDapperDbParameters(sql, cn, paramObject);
}
using (var cn = new SqlConnection(csMsSql))
{
sql = sql.Replace(":", "@");
SimulateDapperDbParameters(sql, cn, paramObject);
}
Console.ReadLine();
}
// https://blog.darkthread.net/blog/dapper-odpnet-unicode-issue/
static void FixOdpNetDbTypeStringMapping()
{
Assembly asm = typeof(OracleConnection).Assembly;
Type tOraDb_DbTypeTable = asm.GetType("Oracle.ManagedDataAccess.Client.OraDb_DbTypeTable");
var fldDbTypeMapping = tOraDb_DbTypeTable.GetField("dbTypeToOracleDbTypeMapping",
BindingFlags.Static | BindingFlags.NonPublic);
int[] mappings = (int[])fldDbTypeMapping.GetValue(null);
mappings[(int)System.Data.DbType.String] = (int)OracleDbType.NVarchar2;
fldDbTypeMapping.SetValue(null, mappings);
}
}
}
</code></pre> <p>補充顯示資料時用到兩則小技巧：</p> <ul><li><a href="/blog/ansi-escape-code/">ANSI 顏色控制碼 - 讓 Console 輸出五彩繽紛</a></li> <li><a href="/blog/string-format-alignment/">【笨問題】C# 字串補空白與靠左右對齊</a></li></ul> <p data-slug="spy-parameter-types-in-dapper" data-title-en="Inspect the parameter type generated by Dapper" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/296357/pexels-photo-296357.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=750&amp;w=1260#1260#750">A function to inspect the parameter types generated by Dapper with reflection.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638410080049520570.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638410080049520570.png" medium="image"/>
    <pubDate>Tue, 16 Jan 2024 12:53:01 GMT</pubDate>
  </item><item>
    <title><![CDATA[動手玩 AI - 使用 OpenAI Embedding 模型比對文字相似性]]></title>
    <link>https://blog.darkthread.net/blog/text-embedding-ada-002-lab/</link>
    <itunes:title><![CDATA[動手玩 AI - 使用 OpenAI Embedding 模型比對文字相似性]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>在 <a href="/blog/rag/">ChatGPT 整合自有資料的 RAG 測試</a>學到不少新東西，其中一個是 Embedding。(嵌入、內嵌，這些翻譯我都覺得怪，文章會用英文原文)</p> <p><a href="https://zh.wikipedia.org/zh-tw/%E8%AF%8D%E5%B5%8C%E5%85%A5">Word Embedding</a> 是自然語言處理(NLP)中將文字內容轉成多維向量數值的一種技術，概念是將原本每個詞一個維度(數量以萬起跳)的高維空間嵌入到一個維度低很多的向量空間，文字特徵或表徵(Feature/Representation)被轉成數字，便可運用數學方法進行運算，用以評估文字相似度，實現分類(找出垃圾郵件、情感分析)、文字檢索、機器翻譯、推薦系統... 等各式文字應用。</p> <p>RAG 範例中用了 <a href="https://www.ithome.com.tw/news/154772">OpenAI text-embedding-ada-002 模型</a>將文字內容轉成 Embedding 並存入 Azure AI Search 索引庫，查詢時則將查詢問題也轉 Embedding，交由 AI Search 服務比對找出相關內容。</p> <p>我過去沒碰過 NLP，現在才發立最基本的概念，打算用見識豬走路的心態實測一下 text-embedding-ada-002。我計劃寫段 .NET 程式做簡單測試，將多段文字使用 OpenAI Embedding 模型轉成 Embedding，透過比對 Embedding 測試彼此間的相似度，是否符合一般的認知，以上就是本次的企劃。</p> <p>MS Learn 上有 <a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">C# 呼叫 OpenAI Embedding API 範例</a>，照抄即能可輕易將一段文字轉成一串 float 浮點數。(註：float 數量共 1536 個，即 text-embedding-ada-002 使用的向量陣列長度，身為門外漢覺得龐大，但其實它只有 davinci-001 的 1/8，已經縮小很多了)</p> <p>如此我們便可將任意文字都轉成 1536 個 float，但要怎麼用它來判斷文字是否相似？</p> <p>Cosine Similarity (餘弦相似性) 是依據 Embedding 向量計算文件相似性的常用做法，背後有一堆數學理論，若你跟我一樣只想有個概念不想深入，大概的意思是相近文字的對映向量角度也會相似，兩個向量形成的夾角愈小，Cosine 值例大，夾角為 0 時 Cosine 等於 1，完全無關夾角 90 度 Cosine 等於 0。如下圖，Cat 與 Dog Cosine = 0.998，Dog 與 Apple 關聯較少 Cosine 為 0.4284：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407950821627000.png"><br> <a href="https://spencerporter2.medium.com/understanding-cosine-similarity-and-word-embeddings-dbf19362a3c">圖片來源</a></p> <p>以下是我的測試程式，輸入一串 string[]，呼叫 OpenAI API 取得 Embedding 向量值，兩兩計算 Cosine Similarity，以 HTML 表單呈現結果，數值為 Consine Similarity 計算值，數字愈高文字顏色愈紅，底色愈黃，代表二者愈相似。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407950823783513.png"></p> <p>由上圖，恨與怒高度相似，其次是 hate 與恨，再其次是 love 與愛、love 與 hate。Cosine Similarity 值愈大意味文字同時出現機率愈高，二者愈相關，但未必是同義字，像是 love 跟 hate 常伴隨出現，隨然二者詞義相反，但相關性排第四。其中相關度最低的是 love 與怒，愈風馬牛不相及，值愈低。</p> <p>再試另外一組：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407950825904572.png"></p> <p>最相關的是炸雞跟薯條，有趣的是，炸雞與反應爐關聯性頗高(因為炸跟爐嗎？)；而企鵝跟薯條、炸雞的關聯比企鵝跟南極還密切。天外飛來一筆的氟，跟所有其他項目的關聯最低，其次是南極，也屬於本群中較邊緣的項目。</p> <p>最後試一下文字段落的比對，我準備了以下內容：</p> <pre><code>var texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
</code></pre> <p>「一棵開花的樹」與「誓言版求佛」相近，兩段血糖運作原因描述相近，兩段談內崁的文字相近，符合一般的預期：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407950828021390.png"></p> <p>體驗完畢，對 Word Embedding 有了粗淺的認識，後續再看 RAG 流程就不會霧煞煞囉。</p> <p>最後附上範例程式，也想動手玩看看的同學可以參考：(專案使用 Azure OpenAI 服務，需 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 引用相關程序庫，<a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">官方教學</a>)</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Text.Json;
using Azure;
using Azure.AI.OpenAI;
Uri oaiEndpoint = new("https://&lt;your-host-name&gt;.openai.azure.com");
string oaiKey = "&lt;your-key&gt;";
AzureKeyCredential credentials = new(oaiKey);
OpenAIClient openAIClient = new(oaiEndpoint, credentials);
Func&lt;string, float[]&gt; GetEmbedding = (text) =&gt;
{
EmbeddingsOptions embeddingOptions = new()
{
DeploymentName = "embedding",
Input = { text },
};
return openAIClient.GetEmbeddings(embeddingOptions).Value.Data[0].Embedding.ToArray();
};
// 暫存 Embedding 查詢結果，若測試文字未變，不需每次重新查詢
var results = new Dictionary&lt;string, float[]&gt;();
var cacheName = "results.json";
if (File.Exists(cacheName))
results = JsonSerializer.Deserialize&lt;Dictionary&lt;string, float[]&gt;&gt;(File.ReadAllText(cacheName))!;
string[] texts;
texts = new string[] {
"love",
"hate",
"愛",
"恨",
"怒"
};
texts = new string[] {
"漢堡",
"薯條",
"可樂",
"炸雞",
"反應爐",
"氟",
"企鵝",
"南極"
};
texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
// 是否文字都是查過的
if (!texts.All(results.ContainsKey))
{
results = texts.ToDictionary(text =&gt; text, GetEmbedding);
File.WriteAllText(cacheName, JsonSerializer.Serialize(results, new JsonSerializerOptions { WriteIndented = true }));
}
Func&lt;string, string&gt; SimpTrim = t =&gt; t.Substring(0, Math.Min(t.Length, 7)) + (t.Length &gt; 7 ? "..." : "");
var keys = results.Keys.ToArray();
var sb = new StringBuilder();
sb.AppendLine(@"
&lt;!DOCTYPE html&gt;
&lt;style&gt;
td.inv { border: none; }
td { width: 100px; }
td.val { text-align: right; padding-right: 6px; }
td.word { background-color: #bfef45; font-size: 9pt; }
&lt;/style&gt;
&lt;table border=1&gt;
");
var simVals = new List&lt;float&gt;();
for (var a = 0; a &lt; keys.Length; a++)
{
sb.Append($"&lt;tr&gt;");
if (a &gt; 0) sb.Append($"&lt;td colspan={a} class=inv&gt;&lt;/td&gt;");
sb.Append($"&lt;td class=word&gt;{SimpTrim(keys[a])}&lt;/td&gt;");
for (var b = a + 1; b &lt; keys.Length; b++)
{
var sim = CosineSimilarity(results[keys[a]], results[keys[b]]);
simVals.Add(sim);
sb.Append($"&lt;td class=val&gt;{sim:n4}&lt;/td&gt;");
}
sb.AppendLine("&lt;/tr&gt;");
}
sb.AppendLine("&lt;/table&gt;");
var avg = simVals.Average();
var min = simVals.Min();
var max =  simVals.Max();
var range = max - min;
sb.AppendLine($"&lt;p&gt;平均相似度: {avg:n4}, 最高: {max:n4}, 最低：{min:n4}&lt;/p&gt;");
sb.AppendLine(@$"
&lt;script&gt;
const avg = {avg}, max = {max}, min = {min}, range = {range};
const tds = document.querySelectorAll('td');
function toHex(n) {{
return parseInt(n).toString(16).padStart(2, '0');
}}
for (const td of tds) {{
if (td.innerText === '' || isNaN(td.innerText)) continue;
const sim = parseFloat(td.innerText);
const diff = Math.abs(sim - min);
td.style.backgroundColor = '#ffff00' + toHex(255 * diff / range);
td.style.color = '#' + toHex(191 * diff / range + 64) + '0000';
}}
&lt;/script&gt;
");
var tmpFile = Path.GetTempFileName() + ".html";
File.WriteAllText(tmpFile, sb.ToString());
Process.Start(new ProcessStartInfo("cmd", $"/c start {tmpFile}")
{ CreateNoWindow = true });
// 1 = 一致, 0 = 無關
static float CosineSimilarity(float[] vector1, float[] vector2)
{
if (vector1.Length != vector2.Length)
{
throw new Exception("Vectors are not the same dimensionality");
}
float dotProduct = 0.0f;
float norm1 = 0.0f;
float norm2 = 0.0f;
for (int i = 0; i &lt; vector1.Length; i++)
{
dotProduct += vector1[i] * vector2[i];
norm1 += vector1[i] * vector1[i];
norm2 += vector2[i] * vector2[i];
}
return (float)(dotProduct / (Math.Sqrt(norm1) * Math.Sqrt(norm2)));
}
</code></pre> <p data-slug="text-embedding-ada-002-lab" data-title-en="OpenAI embedding model lab" data-catg=".NET" data-fbimg="https://i.imgur.com/xBkx8lK.png#800#450">Example of using text-embedding-ada-002 model to get embedding and calculate cosine similarity to find similarity between words and paragraphs.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>在 <a href="/blog/rag/">ChatGPT 整合自有資料的 RAG 測試</a>學到不少新東西，其中一個是 Embedding。(嵌入、內嵌，這些翻譯我都覺得怪，文章會用英文原文)</p> <p><a href="https://zh.wikipedia.org/zh-tw/%E8%AF%8D%E5%B5%8C%E5%85%A5">Word Embedding</a> 是自然語言處理(NLP)中將文字內容轉成多維向量數值的一種技術，概念是將原本每個詞一個維度(數量以萬起跳)的高維空間嵌入到一個維度低很多的向量空間，文字特徵或表徵(Feature/Representation)被轉成數字，便可運用數學方法進行運算，用以評估文字相似度，實現分類(找出垃圾郵件、情感分析)、文字檢索、機器翻譯、推薦系統... 等各式文字應用。</p> <p>RAG 範例中用了 <a href="https://www.ithome.com.tw/news/154772">OpenAI text-embedding-ada-002 模型</a>將文字內容轉成 Embedding 並存入 Azure AI Search 索引庫，查詢時則將查詢問題也轉 Embedding，交由 AI Search 服務比對找出相關內容。</p> <p>我過去沒碰過 NLP，現在才發立最基本的概念，打算用見識豬走路的心態實測一下 text-embedding-ada-002。我計劃寫段 .NET 程式做簡單測試，將多段文字使用 OpenAI Embedding 模型轉成 Embedding，透過比對 Embedding 測試彼此間的相似度，是否符合一般的認知，以上就是本次的企劃。</p> <p>MS Learn 上有 <a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">C# 呼叫 OpenAI Embedding API 範例</a>，照抄即能可輕易將一段文字轉成一串 float 浮點數。(註：float 數量共 1536 個，即 text-embedding-ada-002 使用的向量陣列長度，身為門外漢覺得龐大，但其實它只有 davinci-001 的 1/8，已經縮小很多了)</p> <p>如此我們便可將任意文字都轉成 1536 個 float，但要怎麼用它來判斷文字是否相似？</p> <p>Cosine Similarity (餘弦相似性) 是依據 Embedding 向量計算文件相似性的常用做法，背後有一堆數學理論，若你跟我一樣只想有個概念不想深入，大概的意思是相近文字的對映向量角度也會相似，兩個向量形成的夾角愈小，Cosine 值例大，夾角為 0 時 Cosine 等於 1，完全無關夾角 90 度 Cosine 等於 0。如下圖，Cat 與 Dog Cosine = 0.998，Dog 與 Apple 關聯較少 Cosine 為 0.4284：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407950821627000.png"><br> <a href="https://spencerporter2.medium.com/understanding-cosine-similarity-and-word-embeddings-dbf19362a3c">圖片來源</a></p> <p>以下是我的測試程式，輸入一串 string[]，呼叫 OpenAI API 取得 Embedding 向量值，兩兩計算 Cosine Similarity，以 HTML 表單呈現結果，數值為 Consine Similarity 計算值，數字愈高文字顏色愈紅，底色愈黃，代表二者愈相似。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407950823783513.png"></p> <p>由上圖，恨與怒高度相似，其次是 hate 與恨，再其次是 love 與愛、love 與 hate。Cosine Similarity 值愈大意味文字同時出現機率愈高，二者愈相關，但未必是同義字，像是 love 跟 hate 常伴隨出現，隨然二者詞義相反，但相關性排第四。其中相關度最低的是 love 與怒，愈風馬牛不相及，值愈低。</p> <p>再試另外一組：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407950825904572.png"></p> <p>最相關的是炸雞跟薯條，有趣的是，炸雞與反應爐關聯性頗高(因為炸跟爐嗎？)；而企鵝跟薯條、炸雞的關聯比企鵝跟南極還密切。天外飛來一筆的氟，跟所有其他項目的關聯最低，其次是南極，也屬於本群中較邊緣的項目。</p> <p>最後試一下文字段落的比對，我準備了以下內容：</p> <pre><code>var texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
</code></pre> <p>「一棵開花的樹」與「誓言版求佛」相近，兩段血糖運作原因描述相近，兩段談內崁的文字相近，符合一般的預期：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407950828021390.png"></p> <p>體驗完畢，對 Word Embedding 有了粗淺的認識，後續再看 RAG 流程就不會霧煞煞囉。</p> <p>最後附上範例程式，也想動手玩看看的同學可以參考：(專案使用 Azure OpenAI 服務，需 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 引用相關程序庫，<a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">官方教學</a>)</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Text.Json;
using Azure;
using Azure.AI.OpenAI;
Uri oaiEndpoint = new("https://&lt;your-host-name&gt;.openai.azure.com");
string oaiKey = "&lt;your-key&gt;";
AzureKeyCredential credentials = new(oaiKey);
OpenAIClient openAIClient = new(oaiEndpoint, credentials);
Func&lt;string, float[]&gt; GetEmbedding = (text) =&gt;
{
EmbeddingsOptions embeddingOptions = new()
{
DeploymentName = "embedding",
Input = { text },
};
return openAIClient.GetEmbeddings(embeddingOptions).Value.Data[0].Embedding.ToArray();
};
// 暫存 Embedding 查詢結果，若測試文字未變，不需每次重新查詢
var results = new Dictionary&lt;string, float[]&gt;();
var cacheName = "results.json";
if (File.Exists(cacheName))
results = JsonSerializer.Deserialize&lt;Dictionary&lt;string, float[]&gt;&gt;(File.ReadAllText(cacheName))!;
string[] texts;
texts = new string[] {
"love",
"hate",
"愛",
"恨",
"怒"
};
texts = new string[] {
"漢堡",
"薯條",
"可樂",
"炸雞",
"反應爐",
"氟",
"企鵝",
"南極"
};
texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
// 是否文字都是查過的
if (!texts.All(results.ContainsKey))
{
results = texts.ToDictionary(text =&gt; text, GetEmbedding);
File.WriteAllText(cacheName, JsonSerializer.Serialize(results, new JsonSerializerOptions { WriteIndented = true }));
}
Func&lt;string, string&gt; SimpTrim = t =&gt; t.Substring(0, Math.Min(t.Length, 7)) + (t.Length &gt; 7 ? "..." : "");
var keys = results.Keys.ToArray();
var sb = new StringBuilder();
sb.AppendLine(@"
&lt;!DOCTYPE html&gt;
&lt;style&gt;
td.inv { border: none; }
td { width: 100px; }
td.val { text-align: right; padding-right: 6px; }
td.word { background-color: #bfef45; font-size: 9pt; }
&lt;/style&gt;
&lt;table border=1&gt;
");
var simVals = new List&lt;float&gt;();
for (var a = 0; a &lt; keys.Length; a++)
{
sb.Append($"&lt;tr&gt;");
if (a &gt; 0) sb.Append($"&lt;td colspan={a} class=inv&gt;&lt;/td&gt;");
sb.Append($"&lt;td class=word&gt;{SimpTrim(keys[a])}&lt;/td&gt;");
for (var b = a + 1; b &lt; keys.Length; b++)
{
var sim = CosineSimilarity(results[keys[a]], results[keys[b]]);
simVals.Add(sim);
sb.Append($"&lt;td class=val&gt;{sim:n4}&lt;/td&gt;");
}
sb.AppendLine("&lt;/tr&gt;");
}
sb.AppendLine("&lt;/table&gt;");
var avg = simVals.Average();
var min = simVals.Min();
var max =  simVals.Max();
var range = max - min;
sb.AppendLine($"&lt;p&gt;平均相似度: {avg:n4}, 最高: {max:n4}, 最低：{min:n4}&lt;/p&gt;");
sb.AppendLine(@$"
&lt;script&gt;
const avg = {avg}, max = {max}, min = {min}, range = {range};
const tds = document.querySelectorAll('td');
function toHex(n) {{
return parseInt(n).toString(16).padStart(2, '0');
}}
for (const td of tds) {{
if (td.innerText === '' || isNaN(td.innerText)) continue;
const sim = parseFloat(td.innerText);
const diff = Math.abs(sim - min);
td.style.backgroundColor = '#ffff00' + toHex(255 * diff / range);
td.style.color = '#' + toHex(191 * diff / range + 64) + '0000';
}}
&lt;/script&gt;
");
var tmpFile = Path.GetTempFileName() + ".html";
File.WriteAllText(tmpFile, sb.ToString());
Process.Start(new ProcessStartInfo("cmd", $"/c start {tmpFile}")
{ CreateNoWindow = true });
// 1 = 一致, 0 = 無關
static float CosineSimilarity(float[] vector1, float[] vector2)
{
if (vector1.Length != vector2.Length)
{
throw new Exception("Vectors are not the same dimensionality");
}
float dotProduct = 0.0f;
float norm1 = 0.0f;
float norm2 = 0.0f;
for (int i = 0; i &lt; vector1.Length; i++)
{
dotProduct += vector1[i] * vector2[i];
norm1 += vector1[i] * vector1[i];
norm2 += vector2[i] * vector2[i];
}
return (float)(dotProduct / (Math.Sqrt(norm1) * Math.Sqrt(norm2)));
}
</code></pre> <p data-slug="text-embedding-ada-002-lab" data-title-en="OpenAI embedding model lab" data-catg=".NET" data-fbimg="https://i.imgur.com/xBkx8lK.png#800#450">Example of using text-embedding-ada-002 model to get embedding and calculate cosine similarity to find similarity between words and paragraphs.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>在 <a href="/blog/rag/">ChatGPT 整合自有資料的 RAG 測試</a>學到不少新東西，其中一個是 Embedding。(嵌入、內嵌，這些翻譯我都覺得怪，文章會用英文原文)</p> <p><a href="https://zh.wikipedia.org/zh-tw/%E8%AF%8D%E5%B5%8C%E5%85%A5">Word Embedding</a> 是自然語言處理(NLP)中將文字內容轉成多維向量數值的一種技術，概念是將原本每個詞一個維度(數量以萬起跳)的高維空間嵌入到一個維度低很多的向量空間，文字特徵或表徵(Feature/Representation)被轉成數字，便可運用數學方法進行運算，用以評估文字相似度，實現分類(找出垃圾郵件、情感分析)、文字檢索、機器翻譯、推薦系統... 等各式文字應用。</p> <p>RAG 範例中用了 <a href="https://www.ithome.com.tw/news/154772">OpenAI text-embedding-ada-002 模型</a>將文字內容轉成 Embedding 並存入 Azure AI Search 索引庫，查詢時則將查詢問題也轉 Embedding，交由 AI Search 服務比對找出相關內容。</p> <p>我過去沒碰過 NLP，現在才發立最基本的概念，打算用見識豬走路的心態實測一下 text-embedding-ada-002。我計劃寫段 .NET 程式做簡單測試，將多段文字使用 OpenAI Embedding 模型轉成 Embedding，透過比對 Embedding 測試彼此間的相似度，是否符合一般的認知，以上就是本次的企劃。</p> <p>MS Learn 上有 <a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">C# 呼叫 OpenAI Embedding API 範例</a>，照抄即能可輕易將一段文字轉成一串 float 浮點數。(註：float 數量共 1536 個，即 text-embedding-ada-002 使用的向量陣列長度，身為門外漢覺得龐大，但其實它只有 davinci-001 的 1/8，已經縮小很多了)</p> <p>如此我們便可將任意文字都轉成 1536 個 float，但要怎麼用它來判斷文字是否相似？</p> <p>Cosine Similarity (餘弦相似性) 是依據 Embedding 向量計算文件相似性的常用做法，背後有一堆數學理論，若你跟我一樣只想有個概念不想深入，大概的意思是相近文字的對映向量角度也會相似，兩個向量形成的夾角愈小，Cosine 值例大，夾角為 0 時 Cosine 等於 1，完全無關夾角 90 度 Cosine 等於 0。如下圖，Cat 與 Dog Cosine = 0.998，Dog 與 Apple 關聯較少 Cosine 為 0.4284：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407950821627000.png"><br> <a href="https://spencerporter2.medium.com/understanding-cosine-similarity-and-word-embeddings-dbf19362a3c">圖片來源</a></p> <p>以下是我的測試程式，輸入一串 string[]，呼叫 OpenAI API 取得 Embedding 向量值，兩兩計算 Cosine Similarity，以 HTML 表單呈現結果，數值為 Consine Similarity 計算值，數字愈高文字顏色愈紅，底色愈黃，代表二者愈相似。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407950823783513.png"></p> <p>由上圖，恨與怒高度相似，其次是 hate 與恨，再其次是 love 與愛、love 與 hate。Cosine Similarity 值愈大意味文字同時出現機率愈高，二者愈相關，但未必是同義字，像是 love 跟 hate 常伴隨出現，隨然二者詞義相反，但相關性排第四。其中相關度最低的是 love 與怒，愈風馬牛不相及，值愈低。</p> <p>再試另外一組：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407950825904572.png"></p> <p>最相關的是炸雞跟薯條，有趣的是，炸雞與反應爐關聯性頗高(因為炸跟爐嗎？)；而企鵝跟薯條、炸雞的關聯比企鵝跟南極還密切。天外飛來一筆的氟，跟所有其他項目的關聯最低，其次是南極，也屬於本群中較邊緣的項目。</p> <p>最後試一下文字段落的比對，我準備了以下內容：</p> <pre><code>var texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
</code></pre> <p>「一棵開花的樹」與「誓言版求佛」相近，兩段血糖運作原因描述相近，兩段談內崁的文字相近，符合一般的預期：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407950828021390.png"></p> <p>體驗完畢，對 Word Embedding 有了粗淺的認識，後續再看 RAG 流程就不會霧煞煞囉。</p> <p>最後附上範例程式，也想動手玩看看的同學可以參考：(專案使用 Azure OpenAI 服務，需 <code>dotnet add package Azure.AI.OpenAI --prerelease</code> 引用相關程序庫，<a href="https://learn.microsoft.com/zh-tw/azure/ai-services/openai/how-to/embeddings?tabs=csharp&amp;WT.mc_id=DOP-MVP-37580">官方教學</a>)</p> <pre><code>using System.Diagnostics;
using System.Text;
using System.Text.Json;
using Azure;
using Azure.AI.OpenAI;
Uri oaiEndpoint = new("https://&lt;your-host-name&gt;.openai.azure.com");
string oaiKey = "&lt;your-key&gt;";
AzureKeyCredential credentials = new(oaiKey);
OpenAIClient openAIClient = new(oaiEndpoint, credentials);
Func&lt;string, float[]&gt; GetEmbedding = (text) =&gt;
{
EmbeddingsOptions embeddingOptions = new()
{
DeploymentName = "embedding",
Input = { text },
};
return openAIClient.GetEmbeddings(embeddingOptions).Value.Data[0].Embedding.ToArray();
};
// 暫存 Embedding 查詢結果，若測試文字未變，不需每次重新查詢
var results = new Dictionary&lt;string, float[]&gt;();
var cacheName = "results.json";
if (File.Exists(cacheName))
results = JsonSerializer.Deserialize&lt;Dictionary&lt;string, float[]&gt;&gt;(File.ReadAllText(cacheName))!;
string[] texts;
texts = new string[] {
"love",
"hate",
"愛",
"恨",
"怒"
};
texts = new string[] {
"漢堡",
"薯條",
"可樂",
"炸雞",
"反應爐",
"氟",
"企鵝",
"南極"
};
texts = new string[] {
"我達達的馬蹄是美麗的錯誤 我不是歸人，是個過客",
"如何讓你遇見我 在我最美麗的時刻 為這 我已在佛前 求了五百年 求他讓我們結一段塵緣",
"我們還能不能再見面 我在佛前苦苦求了幾千年 願意用幾世換我們一世情缘 希望可以感動上天",
"我們吃下碳水化合物，消化後將轉為葡萄糖進入血液，血糖上升會刺激胰臟 β 細胞分泌「胰島素」，讓血糖進入細胞給細胞利用，並將多餘血糖合成肝醣儲存到肝臟及肌肉",
"當血糖被消耗，血糖濃度會下降，此時胰臟 α 細胞會分泌「升糖素」，分解肝醣釋出葡萄糖以及糖質新生（把體內其他原料轉變為葡萄糖）提高血糖濃度",
"內嵌是一種特殊的數據表示格式，可供機器學習模型和演算法輕鬆使用。內嵌是文字片段語意意義的資訊密集表示法。",
"每個內嵌都是浮點數的向量，因此，在向量空間中兩個內嵌之間的距離與原始格式兩個輸入之間的語意相似度相互關聯。",
};
// 是否文字都是查過的
if (!texts.All(results.ContainsKey))
{
results = texts.ToDictionary(text =&gt; text, GetEmbedding);
File.WriteAllText(cacheName, JsonSerializer.Serialize(results, new JsonSerializerOptions { WriteIndented = true }));
}
Func&lt;string, string&gt; SimpTrim = t =&gt; t.Substring(0, Math.Min(t.Length, 7)) + (t.Length &gt; 7 ? "..." : "");
var keys = results.Keys.ToArray();
var sb = new StringBuilder();
sb.AppendLine(@"
&lt;!DOCTYPE html&gt;
&lt;style&gt;
td.inv { border: none; }
td { width: 100px; }
td.val { text-align: right; padding-right: 6px; }
td.word { background-color: #bfef45; font-size: 9pt; }
&lt;/style&gt;
&lt;table border=1&gt;
");
var simVals = new List&lt;float&gt;();
for (var a = 0; a &lt; keys.Length; a++)
{
sb.Append($"&lt;tr&gt;");
if (a &gt; 0) sb.Append($"&lt;td colspan={a} class=inv&gt;&lt;/td&gt;");
sb.Append($"&lt;td class=word&gt;{SimpTrim(keys[a])}&lt;/td&gt;");
for (var b = a + 1; b &lt; keys.Length; b++)
{
var sim = CosineSimilarity(results[keys[a]], results[keys[b]]);
simVals.Add(sim);
sb.Append($"&lt;td class=val&gt;{sim:n4}&lt;/td&gt;");
}
sb.AppendLine("&lt;/tr&gt;");
}
sb.AppendLine("&lt;/table&gt;");
var avg = simVals.Average();
var min = simVals.Min();
var max =  simVals.Max();
var range = max - min;
sb.AppendLine($"&lt;p&gt;平均相似度: {avg:n4}, 最高: {max:n4}, 最低：{min:n4}&lt;/p&gt;");
sb.AppendLine(@$"
&lt;script&gt;
const avg = {avg}, max = {max}, min = {min}, range = {range};
const tds = document.querySelectorAll('td');
function toHex(n) {{
return parseInt(n).toString(16).padStart(2, '0');
}}
for (const td of tds) {{
if (td.innerText === '' || isNaN(td.innerText)) continue;
const sim = parseFloat(td.innerText);
const diff = Math.abs(sim - min);
td.style.backgroundColor = '#ffff00' + toHex(255 * diff / range);
td.style.color = '#' + toHex(191 * diff / range + 64) + '0000';
}}
&lt;/script&gt;
");
var tmpFile = Path.GetTempFileName() + ".html";
File.WriteAllText(tmpFile, sb.ToString());
Process.Start(new ProcessStartInfo("cmd", $"/c start {tmpFile}")
{ CreateNoWindow = true });
// 1 = 一致, 0 = 無關
static float CosineSimilarity(float[] vector1, float[] vector2)
{
if (vector1.Length != vector2.Length)
{
throw new Exception("Vectors are not the same dimensionality");
}
float dotProduct = 0.0f;
float norm1 = 0.0f;
float norm2 = 0.0f;
for (int i = 0; i &lt; vector1.Length; i++)
{
dotProduct += vector1[i] * vector2[i];
norm1 += vector1[i] * vector1[i];
norm2 += vector2[i] * vector2[i];
}
return (float)(dotProduct / (Math.Sqrt(norm1) * Math.Sqrt(norm2)));
}
</code></pre> <p data-slug="text-embedding-ada-002-lab" data-title-en="OpenAI embedding model lab" data-catg=".NET" data-fbimg="https://i.imgur.com/xBkx8lK.png#800#450">Example of using text-embedding-ada-002 model to get embedding and calculate cosine similarity to find similarity between words and paragraphs.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638407950821627000.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638407950821627000.png" medium="image"/>
    <pubDate>Sun, 14 Jan 2024 02:11:19 GMT</pubDate>
  </item><item>
    <title><![CDATA[.NET 6+ 可以直接參照 .NET Framework 編譯的 DLL 嗎？]]></title>
    <link>https://blog.darkthread.net/blog/netcore-ref-netfx-dll/</link>
    <itunes:title><![CDATA[.NET 6+ 可以直接參照 .NET Framework 編譯的 DLL 嗎？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>如果你是 .NET 開發者，考個簡單問題：</p> <p>.NET 6+ 寫的程式可以直接參照引用 .NET Framework 編譯的 DLL 嗎？<br> (註：非 .NET Standard，純 .NET Framework 2.0/3.5/4.0/4.5 ~ 4.8，參考：<a href="/blog/netstandard-class-library/">淺談 .NET 類別程式庫跨平台開發</a>)</p> <p>我答錯了。</p> <p>在我的印象中，.NET Framework 寫的專案必須轉成 .NET 6+ 或 .NET Standard 才能供 .NET 6+ (過去叫 .NET Core) 叫用。前幾天幫看問題，大驚! 事情怎麼跟我想得不一樣。如下圖範例，.NET 8 Console Application 居然直接參照 .NET Framework 4.8 寫的類別程式庫(實際案例是參照 .NET 2.0 跟 .NET 3.5 寫的程式庫)，程式編譯成功，執行正常。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407271052730779.png"></p> <p>為此爬文，補齊一些知識。</p> <p>相關文件在此 - <a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/third-party-deps?WT.mc_id=DOP-MVP-37580#net-framework-compatibility-mode">.NET Framework 相容性模式</a>。</p> <p>.NET Standard 2.0 起加入了 .NET Framework 相容性模式，允許 .NET Standard 和 .NET Core 專案參考 .NET Framework 程式庫。但有個陷阱，並非所有專案都適用，如果 .NET Framework 程式庫用到 .NET 6+ 或 .NET Standard 不支援的 API，程式將會出錯，而且是編譯過程一片祥和，等執行階段再炸裂。</p> <p>例如：我在 .NET 4.8 類別故意用了 .NET Core 不支援的另建 AppDomain、System.Web.Services.WebService (延伸閱讀：<a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/?WT.mc_id=DOP-MVP-37580#unavailable-technologies">.NET 6+ 無法使用的 .NET Framework 技術</a>)</p> <pre><code>using System;
namespace NetFXLib
{
public class Foo
{
public string SayHello()
=&gt; "Hello from .NET Framework Class Library";
public string TestAppDomain()
=&gt; AppDomain.CreateDomain("TestAppDomain").FriendlyName;
public string TestWebService()
{
var ws = new System.Web.Services.WebService();
return ws.GetType().FullName;
}
}
}
</code></pre> <p>.NET 8 Console Program.cs 長這樣：</p> <pre><code>var foo = new NetFXLib.Foo();
TryRun(() =&gt; Console.WriteLine(foo.SayHello()));
TryRun(() =&gt; Console.WriteLine(foo.TestAppDomain()));
TryRun(() =&gt; Console.WriteLine(foo.TestWebService()));
void TryRun(Action action)
{
try
{
action.Invoke();
}
catch (Exception ex)
{
Console.WriteLine($"ERROR - {ex.Message}");
}
}
</code></pre> <p>編譯沒問題，但執行時新增 AppDomain 及引用 System.Web.Services 相關型別的功能會出錯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407271054930502.png"></p> <p><code>dotnet publish -r linux-x64 --no-self-contained</code> 丟到 Linux 執行，結局相同：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407271057035875.png"></p> <p>相當然爾，若直接在 Linux 嘗試編譯，因 NetFXLib 是 .NET Framework 專案，是不可能成功的!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407271059274866.png"></p> <p>但若改成直接參照編譯好的 .NET Framework DLL，倒是可以成功編譯，但該出錯的還是要出錯。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638407271061505145.png"></p> <p>由此可知，雖然 .NET 6+ 可以參照 .NET Framework DLL，但可能出現執行階段才冒出來的不相容錯誤，不能算是可靠的做法，等同失去 .NET 在編譯階段揪出錯誤的強型別優勢。故原則上還是建議將專案升級成 .NET 6+ 或 .NET Standard，會是較嚴謹可信賴的做法。</p> <p>要將 .NET Framework 專案升級成 .NET 6+/.NET Standard 專案，微軟有個 <a href="https://marketplace.visualstudio.com/items?itemName=ms-dotnettools.upgradeassistant">.NET Upgrade Assistant</a> 能協助升級，可多加利用。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638407271063696617.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638407271065862520.png"></p> <p data-slug="netcore-ref-netfx-dll" data-title-en="Can .NET 6+ reference .NET Framework dll directly?" data-catg=".NET" data-fbimg="https://i.imgur.com/lXlrMc4.jpg#1280#854">A discussion on whether a .NET 6+ program can directly reference .NET Framework library.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>如果你是 .NET 開發者，考個簡單問題：</p> <p>.NET 6+ 寫的程式可以直接參照引用 .NET Framework 編譯的 DLL 嗎？<br> (註：非 .NET Standard，純 .NET Framework 2.0/3.5/4.0/4.5 ~ 4.8，參考：<a href="/blog/netstandard-class-library/">淺談 .NET 類別程式庫跨平台開發</a>)</p> <p>我答錯了。</p> <p>在我的印象中，.NET Framework 寫的專案必須轉成 .NET 6+ 或 .NET Standard 才能供 .NET 6+ (過去叫 .NET Core) 叫用。前幾天幫看問題，大驚! 事情怎麼跟我想得不一樣。如下圖範例，.NET 8 Console Application 居然直接參照 .NET Framework 4.8 寫的類別程式庫(實際案例是參照 .NET 2.0 跟 .NET 3.5 寫的程式庫)，程式編譯成功，執行正常。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407271052730779.png"></p> <p>為此爬文，補齊一些知識。</p> <p>相關文件在此 - <a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/third-party-deps?WT.mc_id=DOP-MVP-37580#net-framework-compatibility-mode">.NET Framework 相容性模式</a>。</p> <p>.NET Standard 2.0 起加入了 .NET Framework 相容性模式，允許 .NET Standard 和 .NET Core 專案參考 .NET Framework 程式庫。但有個陷阱，並非所有專案都適用，如果 .NET Framework 程式庫用到 .NET 6+ 或 .NET Standard 不支援的 API，程式將會出錯，而且是編譯過程一片祥和，等執行階段再炸裂。</p> <p>例如：我在 .NET 4.8 類別故意用了 .NET Core 不支援的另建 AppDomain、System.Web.Services.WebService (延伸閱讀：<a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/?WT.mc_id=DOP-MVP-37580#unavailable-technologies">.NET 6+ 無法使用的 .NET Framework 技術</a>)</p> <pre><code>using System;
namespace NetFXLib
{
public class Foo
{
public string SayHello()
=&gt; "Hello from .NET Framework Class Library";
public string TestAppDomain()
=&gt; AppDomain.CreateDomain("TestAppDomain").FriendlyName;
public string TestWebService()
{
var ws = new System.Web.Services.WebService();
return ws.GetType().FullName;
}
}
}
</code></pre> <p>.NET 8 Console Program.cs 長這樣：</p> <pre><code>var foo = new NetFXLib.Foo();
TryRun(() =&gt; Console.WriteLine(foo.SayHello()));
TryRun(() =&gt; Console.WriteLine(foo.TestAppDomain()));
TryRun(() =&gt; Console.WriteLine(foo.TestWebService()));
void TryRun(Action action)
{
try
{
action.Invoke();
}
catch (Exception ex)
{
Console.WriteLine($"ERROR - {ex.Message}");
}
}
</code></pre> <p>編譯沒問題，但執行時新增 AppDomain 及引用 System.Web.Services 相關型別的功能會出錯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407271054930502.png"></p> <p><code>dotnet publish -r linux-x64 --no-self-contained</code> 丟到 Linux 執行，結局相同：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407271057035875.png"></p> <p>相當然爾，若直接在 Linux 嘗試編譯，因 NetFXLib 是 .NET Framework 專案，是不可能成功的!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407271059274866.png"></p> <p>但若改成直接參照編譯好的 .NET Framework DLL，倒是可以成功編譯，但該出錯的還是要出錯。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638407271061505145.png"></p> <p>由此可知，雖然 .NET 6+ 可以參照 .NET Framework DLL，但可能出現執行階段才冒出來的不相容錯誤，不能算是可靠的做法，等同失去 .NET 在編譯階段揪出錯誤的強型別優勢。故原則上還是建議將專案升級成 .NET 6+ 或 .NET Standard，會是較嚴謹可信賴的做法。</p> <p>要將 .NET Framework 專案升級成 .NET 6+/.NET Standard 專案，微軟有個 <a href="https://marketplace.visualstudio.com/items?itemName=ms-dotnettools.upgradeassistant">.NET Upgrade Assistant</a> 能協助升級，可多加利用。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638407271063696617.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638407271065862520.png"></p> <p data-slug="netcore-ref-netfx-dll" data-title-en="Can .NET 6+ reference .NET Framework dll directly?" data-catg=".NET" data-fbimg="https://i.imgur.com/lXlrMc4.jpg#1280#854">A discussion on whether a .NET 6+ program can directly reference .NET Framework library.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>如果你是 .NET 開發者，考個簡單問題：</p> <p>.NET 6+ 寫的程式可以直接參照引用 .NET Framework 編譯的 DLL 嗎？<br> (註：非 .NET Standard，純 .NET Framework 2.0/3.5/4.0/4.5 ~ 4.8，參考：<a href="/blog/netstandard-class-library/">淺談 .NET 類別程式庫跨平台開發</a>)</p> <p>我答錯了。</p> <p>在我的印象中，.NET Framework 寫的專案必須轉成 .NET 6+ 或 .NET Standard 才能供 .NET 6+ (過去叫 .NET Core) 叫用。前幾天幫看問題，大驚! 事情怎麼跟我想得不一樣。如下圖範例，.NET 8 Console Application 居然直接參照 .NET Framework 4.8 寫的類別程式庫(實際案例是參照 .NET 2.0 跟 .NET 3.5 寫的程式庫)，程式編譯成功，執行正常。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638407271052730779.png"></p> <p>為此爬文，補齊一些知識。</p> <p>相關文件在此 - <a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/third-party-deps?WT.mc_id=DOP-MVP-37580#net-framework-compatibility-mode">.NET Framework 相容性模式</a>。</p> <p>.NET Standard 2.0 起加入了 .NET Framework 相容性模式，允許 .NET Standard 和 .NET Core 專案參考 .NET Framework 程式庫。但有個陷阱，並非所有專案都適用，如果 .NET Framework 程式庫用到 .NET 6+ 或 .NET Standard 不支援的 API，程式將會出錯，而且是編譯過程一片祥和，等執行階段再炸裂。</p> <p>例如：我在 .NET 4.8 類別故意用了 .NET Core 不支援的另建 AppDomain、System.Web.Services.WebService (延伸閱讀：<a href="https://learn.microsoft.com/zh-tw/dotnet/core/porting/?WT.mc_id=DOP-MVP-37580#unavailable-technologies">.NET 6+ 無法使用的 .NET Framework 技術</a>)</p> <pre><code>using System;
namespace NetFXLib
{
public class Foo
{
public string SayHello()
=&gt; "Hello from .NET Framework Class Library";
public string TestAppDomain()
=&gt; AppDomain.CreateDomain("TestAppDomain").FriendlyName;
public string TestWebService()
{
var ws = new System.Web.Services.WebService();
return ws.GetType().FullName;
}
}
}
</code></pre> <p>.NET 8 Console Program.cs 長這樣：</p> <pre><code>var foo = new NetFXLib.Foo();
TryRun(() =&gt; Console.WriteLine(foo.SayHello()));
TryRun(() =&gt; Console.WriteLine(foo.TestAppDomain()));
TryRun(() =&gt; Console.WriteLine(foo.TestWebService()));
void TryRun(Action action)
{
try
{
action.Invoke();
}
catch (Exception ex)
{
Console.WriteLine($"ERROR - {ex.Message}");
}
}
</code></pre> <p>編譯沒問題，但執行時新增 AppDomain 及引用 System.Web.Services 相關型別的功能會出錯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638407271054930502.png"></p> <p><code>dotnet publish -r linux-x64 --no-self-contained</code> 丟到 Linux 執行，結局相同：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638407271057035875.png"></p> <p>相當然爾，若直接在 Linux 嘗試編譯，因 NetFXLib 是 .NET Framework 專案，是不可能成功的!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638407271059274866.png"></p> <p>但若改成直接參照編譯好的 .NET Framework DLL，倒是可以成功編譯，但該出錯的還是要出錯。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638407271061505145.png"></p> <p>由此可知，雖然 .NET 6+ 可以參照 .NET Framework DLL，但可能出現執行階段才冒出來的不相容錯誤，不能算是可靠的做法，等同失去 .NET 在編譯階段揪出錯誤的強型別優勢。故原則上還是建議將專案升級成 .NET 6+ 或 .NET Standard，會是較嚴謹可信賴的做法。</p> <p>要將 .NET Framework 專案升級成 .NET 6+/.NET Standard 專案，微軟有個 <a href="https://marketplace.visualstudio.com/items?itemName=ms-dotnettools.upgradeassistant">.NET Upgrade Assistant</a> 能協助升級，可多加利用。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638407271063696617.png"></p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638407271065862520.png"></p> <p data-slug="netcore-ref-netfx-dll" data-title-en="Can .NET 6+ reference .NET Framework dll directly?" data-catg=".NET" data-fbimg="https://i.imgur.com/lXlrMc4.jpg#1280#854">A discussion on whether a .NET 6+ program can directly reference .NET Framework library.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638407271052730779.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638407271052730779.png" medium="image"/>
    <pubDate>Sat, 13 Jan 2024 07:16:25 GMT</pubDate>
  </item><item>
    <title><![CDATA[「工人」智慧 CAPTCHA 破解服務]]></title>
    <link>https://blog.darkthread.net/blog/2captcha/</link>
    <itunes:title><![CDATA[「工人」智慧 CAPTCHA 破解服務]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>我的部落格被某個印尼廣告商盯上，即使垃圾留言會被檢核機制阻擋，他還是孜孜不倦定期來貼不會有人看到的廣告留言，讓我很無言。過年前<a href="/blog/hcaptcha/">部落格換了 hCaptcha</a>原以為可以改善，很不幸<a href="https://www.facebook.com/darkthread.net/posts/pfbid02NxM8xgMPVfvymnTFSphvzGttcFkJUxUvgcqhTzvtrzsaHutpsk56PwrEueRMMXJjl">沒能阻止對方繼續徒勞無功</a>...</p> <p>貼文的留言區讀者俞俞丟了個關鍵字 - 2captcha，引導我認識有趣的產業 - 「在家解 CAPTCHA 賺錢 + 付費 CAPTCHA 破解服務」的線上媒合平台。</p> <p>平台服務分成兩塊：「在家接任務解 CAPTCHA 賺外快」跟「付錢找人跳過 CAPTCHA 樂開懷」。需要破解 CAPTCHA 的客戶呼叫 API 上傳相關參數，平台自動將工作派發給在線上的 Worker (Worker，名符其實的「工人」智慧啊)解題，真人解答後上傳結果回覆給委託者。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638405791136868264.png"></p> <p>由於是委託真人破解，理論上沒有解不掉的 CAPTCHA，而依 CAPTCHA 種類不同，耗費時間跟收費也不同，每 1000 次的價格從 USD 1 塊到 2.99 不等，等於一次不到台幣一毛塊(工人拿到的報酬只會更低，十足廉價勞工)，耗時從 3 秒到 25 不等。reCaptcha、hCaptcha 是大宗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638405791141358204.png"></p> <p>Cloudflare Turnstile 也沒問題，1000 次 1.45 USD，耗時 22 秒：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638405791143596966.png"></p> <p>至於我之前自製加減算數 CAPTCHA，平台有種上傳圖檔得到答案的玩法，所以即便是獨創的土砲版(像是我的加減法算數)也難逃被破解的命運，但至少對方得花時間看程式分析 CAPTCHA 圖檔及填寫位置，不像 reCaptcha、hCaptcha、Turnstile 有現成範例套用。</p> <pre><code>Normal captcha = new Normal();
captcha.SetFile("path/to/captcha.jpg");
captcha.SetNumeric(4);
captcha.SetMinLen(4);
captcha.SetMaxLen(20);
captcha.SetPhrase(true);
captcha.SetCaseSensitive(true);
captcha.SetCalc(false);
captcha.SetLang("en");
captcha.SetHintImg(new FileInfo("path/to/hint.jpg"));
captcha.SetHintText("Type red symbols only");
</code></pre> <p>搞懂攻擊手法，該怎麼防守就有了頭緒，我已經想到一些反制手段(最少要製造麻煩，增加破解成本)，過陣子找時間試試。</p> <p data-slug="2captcha" data-fbimg="https://i.imgur.com/jsQGn8x.png#1123#627"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>我的部落格被某個印尼廣告商盯上，即使垃圾留言會被檢核機制阻擋，他還是孜孜不倦定期來貼不會有人看到的廣告留言，讓我很無言。過年前<a href="/blog/hcaptcha/">部落格換了 hCaptcha</a>原以為可以改善，很不幸<a href="https://www.facebook.com/darkthread.net/posts/pfbid02NxM8xgMPVfvymnTFSphvzGttcFkJUxUvgcqhTzvtrzsaHutpsk56PwrEueRMMXJjl">沒能阻止對方繼續徒勞無功</a>...</p> <p>貼文的留言區讀者俞俞丟了個關鍵字 - 2captcha，引導我認識有趣的產業 - 「在家解 CAPTCHA 賺錢 + 付費 CAPTCHA 破解服務」的線上媒合平台。</p> <p>平台服務分成兩塊：「在家接任務解 CAPTCHA 賺外快」跟「付錢找人跳過 CAPTCHA 樂開懷」。需要破解 CAPTCHA 的客戶呼叫 API 上傳相關參數，平台自動將工作派發給在線上的 Worker (Worker，名符其實的「工人」智慧啊)解題，真人解答後上傳結果回覆給委託者。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638405791136868264.png"></p> <p>由於是委託真人破解，理論上沒有解不掉的 CAPTCHA，而依 CAPTCHA 種類不同，耗費時間跟收費也不同，每 1000 次的價格從 USD 1 塊到 2.99 不等，等於一次不到台幣一毛塊(工人拿到的報酬只會更低，十足廉價勞工)，耗時從 3 秒到 25 不等。reCaptcha、hCaptcha 是大宗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638405791141358204.png"></p> <p>Cloudflare Turnstile 也沒問題，1000 次 1.45 USD，耗時 22 秒：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638405791143596966.png"></p> <p>至於我之前自製加減算數 CAPTCHA，平台有種上傳圖檔得到答案的玩法，所以即便是獨創的土砲版(像是我的加減法算數)也難逃被破解的命運，但至少對方得花時間看程式分析 CAPTCHA 圖檔及填寫位置，不像 reCaptcha、hCaptcha、Turnstile 有現成範例套用。</p> <pre><code>Normal captcha = new Normal();
captcha.SetFile("path/to/captcha.jpg");
captcha.SetNumeric(4);
captcha.SetMinLen(4);
captcha.SetMaxLen(20);
captcha.SetPhrase(true);
captcha.SetCaseSensitive(true);
captcha.SetCalc(false);
captcha.SetLang("en");
captcha.SetHintImg(new FileInfo("path/to/hint.jpg"));
captcha.SetHintText("Type red symbols only");
</code></pre> <p>搞懂攻擊手法，該怎麼防守就有了頭緒，我已經想到一些反制手段(最少要製造麻煩，增加破解成本)，過陣子找時間試試。</p> <p data-slug="2captcha" data-fbimg="https://i.imgur.com/jsQGn8x.png#1123#627"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>我的部落格被某個印尼廣告商盯上，即使垃圾留言會被檢核機制阻擋，他還是孜孜不倦定期來貼不會有人看到的廣告留言，讓我很無言。過年前<a href="/blog/hcaptcha/">部落格換了 hCaptcha</a>原以為可以改善，很不幸<a href="https://www.facebook.com/darkthread.net/posts/pfbid02NxM8xgMPVfvymnTFSphvzGttcFkJUxUvgcqhTzvtrzsaHutpsk56PwrEueRMMXJjl">沒能阻止對方繼續徒勞無功</a>...</p> <p>貼文的留言區讀者俞俞丟了個關鍵字 - 2captcha，引導我認識有趣的產業 - 「在家解 CAPTCHA 賺錢 + 付費 CAPTCHA 破解服務」的線上媒合平台。</p> <p>平台服務分成兩塊：「在家接任務解 CAPTCHA 賺外快」跟「付錢找人跳過 CAPTCHA 樂開懷」。需要破解 CAPTCHA 的客戶呼叫 API 上傳相關參數，平台自動將工作派發給在線上的 Worker (Worker，名符其實的「工人」智慧啊)解題，真人解答後上傳結果回覆給委託者。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638405791136868264.png"></p> <p>由於是委託真人破解，理論上沒有解不掉的 CAPTCHA，而依 CAPTCHA 種類不同，耗費時間跟收費也不同，每 1000 次的價格從 USD 1 塊到 2.99 不等，等於一次不到台幣一毛塊(工人拿到的報酬只會更低，十足廉價勞工)，耗時從 3 秒到 25 不等。reCaptcha、hCaptcha 是大宗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638405791141358204.png"></p> <p>Cloudflare Turnstile 也沒問題，1000 次 1.45 USD，耗時 22 秒：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638405791143596966.png"></p> <p>至於我之前自製加減算數 CAPTCHA，平台有種上傳圖檔得到答案的玩法，所以即便是獨創的土砲版(像是我的加減法算數)也難逃被破解的命運，但至少對方得花時間看程式分析 CAPTCHA 圖檔及填寫位置，不像 reCaptcha、hCaptcha、Turnstile 有現成範例套用。</p> <pre><code>Normal captcha = new Normal();
captcha.SetFile("path/to/captcha.jpg");
captcha.SetNumeric(4);
captcha.SetMinLen(4);
captcha.SetMaxLen(20);
captcha.SetPhrase(true);
captcha.SetCaseSensitive(true);
captcha.SetCalc(false);
captcha.SetLang("en");
captcha.SetHintImg(new FileInfo("path/to/hint.jpg"));
captcha.SetHintText("Type red symbols only");
</code></pre> <p>搞懂攻擊手法，該怎麼防守就有了頭緒，我已經想到一些反制手段(最少要製造麻煩，增加破解成本)，過陣子找時間試試。</p> <p data-slug="2captcha" data-fbimg="https://i.imgur.com/jsQGn8x.png#1123#627"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638405791136868264.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638405791136868264.png" medium="image"/>
    <pubDate>Thu, 11 Jan 2024 01:05:29 GMT</pubDate>
  </item><item>
    <title><![CDATA[依背景色決定文字顏色的正確姿勢 - W3C 標準]]></title>
    <link>https://blog.darkthread.net/blog/wcag-g18-color-contrast/</link>
    <itunes:title><![CDATA[依背景色決定文字顏色的正確姿勢 - W3C 標準]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>再來聊聊「依據背景色切換黑字或白字，確保文字明顯容易閱讀」這檔事。</p> <p>過去我都用 Github Copilot 教我的<a href="https://www.facebook.com/darkthread.net/posts/pfbid02PXepN99eFUnXJCP9zbRczzdY1RETaBhtbeYf2jS5XD27JoJxywxYtCjVwCavQBQnl">186 魔術數字公式</a>：<code>complementary = (r * 0.299 + g * 0.587 + b * 0.114) &gt; 186 ? '#000000' : '#ffffff'</code>，但上回在 <a href="/blog/20-distinct-colors/">20 種差異鮮明色彩組合</a>用這招決定文字顏色，讀者 lke 提醒，我為 #42d4f4 配了白字，看起來不太明顯。對照原文網頁範例，同一顏色配了黑字，看起來的確比白字明顯許多。這讓我開始懷疑，莫非 186 公式有缺陷？</p> <p>剛好在 FB 留言看到李奎翰大大分享的重要情資：關於文字顏色對比，W3C 的 <a href="https://www.w3.org/TR/WCAG21/">WCAG 網站內容無障礙指南</a>其實已有明確標準 - <a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18: Ensuring that a contrast ratio of at least 4.5:1 exists between text (and images of text) and background behind the text</a>，而文字與背景色對比度有兩個認證等級：(貫徹無障礙網站的難度不亞於下油鍋，這裡淺嚐就好，恕不再深入)</p> <ul><li>AA - 文字與背景對比值需大於 4.5:1</li> <li>AAA - 文字與背景對比值需大於 7:1</li></ul> <p>我找到<a href="ttps://webaim.org/resources/contrastchecker/?fcolor=000000&amp;bcolor=42D4F4">檢查對比值是否符合 WCAG 標準的網站</a>，確認 #42d4f4 應該配黑字才是正解，配白字只對比只有 1.75:1，死當!!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638404939661095048.png"></p> <p><a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18 標準</a>有提供完整的 RGB 值換算相對亮度(Relative Luminance)及對比值的公式，我將公式轉成 JavaScript 函式：</p> <pre><code>const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
</code></pre> <p>成功算出 1.7565 跟 11.9553，與檢測網站的計算結果一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638404939662987293.png"></p> <p>再來便是在已知背景色時，決定該用 #000000 還是 #fffff。</p> <p>依 G18 文件，若有兩種顏色，其相對亮度分別為 L<sub>1</sub> 及 L<sub>2</sub>，則二者之對比值可表示為：</p> <blockquote> <p>(L<sub>1</sub> + 0.05) / (L<sub>2</sub> + 0.05)</p> </blockquote> <p>黑字的使用時機應為「黑字與背景色的對比值」大於「白字與背景色的對比值」，黑色與白色的相對亮度分別為 0、1，若背景色之相對亮度為 L，代入上方公式可得：</p> <blockquote> <p>(L + 0.05) / (0 + 0.05) &gt; (1 + 0.05) / (L + 0.05)</p> </blockquote> <p>化簡以上公式，會得到魔術數字 0.179：</p> <pre><code>  (L + 0.05) / 0.05 &gt; 1.05 / (L + 0.05)
→ (L + 0.05)^2 / 0.05 &gt; 1.05
→ (L + 0.05)^2 &gt; 1.05 * 0.05
→ L + 0.05 &gt; sqrt(1.05 * 0.05)
→ L &gt; sqrt(1.05 * 0.05) - 0.05
→ L &gt; 0.179
</code></pre> <p>寫成黑白字判斷函式：</p> <pre><code>const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
</code></pre> <p>最後，比對一下 186 版及 G18 版文字配色效果：<a href="https://output.jsbin.com/gisavit">線上展示</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset="utf-8" /&gt;
&lt;script src="https://unpkg.com/vue@3"&gt;&lt;/script&gt;
&lt;style&gt;
.palette {
display: flex; margin-bottom: 12px;
flex-direction: row; flex-wrap: wrap;
width: 900px; font-size: 10pt;
font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
.palette&gt;div {
position: relative; width: 72px; height: 150px;
margin: 2px; padding: 6px;
}
.palette .chk {
background-color: white; text-align: center;
margin-top: 8px; margin-bottom: 8px;
}
.palette .chk span {
color: red; text-decoration: line-through;
margin-right: 4px;
}
.palette .chk span.pass {
text-decoration: none; color: green;
font-weight: bolder;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id="app"&gt;
&lt;div class="palette"&gt;
&lt;div v-for="(color,idx) in colors" :style="{ backgroundColor: color.bg }"&gt;
&lt;div v-for="fg in color.fgColors" :style="{ color: fg.fg }"&gt;
&lt;div&gt;{{ color.bg }}&lt;/div&gt;
&lt;div&gt;{{ fg.ratio.toFixed(2) }}&lt;/div&gt;
&lt;div class="chk"&gt;
&lt;span :class="{'pass':fg.aa}"&gt;AA&lt;/span&gt;
&lt;span :class="{'pass':fg.aaa}"&gt;AAA&lt;/span&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
const pool = ['#e6194B', '#3cb44b', '#ffe119', '#4363d8', '#f58231', '#911eb4', '#42d4f4', '#f032e6', '#bfef45', '#fabed4', '#469990', '#dcbeff', '#9A6324', '#fffac8', '#800000', '#aaffc3', '#808000', '#ffd8b1', '#000075', '#a9a9a9', '#ffffff', '#000000'];
const getTextColor186 = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
return (parseInt(m[1], 16) * 0.299 + parseInt(m[2], 16) * 0.587 + parseInt(m[3], 16) * 0.114) &gt; 186 ? '#000000' : '#ffffff';
}
const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
const colors = pool.map(hex =&gt;
({
bg: hex,
fgColors:
[getTextColor186(hex), getTextColorWcag(hex)].map(fg =&gt; {
const ratio = calcContrastRatio(fg, hex);
return {
fg, ratio, aa: ratio &gt;= 4.5, aaa: ratio &gt;= 7
};
})
}));
const app = Vue.createApp({
data() {
return {
colors: colors
}
}
});
var vm = app.mount('#app');
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>每個背景色有兩組結果，上方為 186 公式計算結果(對比值、是否符合 AA、AAA 標準)，下方為 G18 公式計算結果，抓出 186 公式為草綠(Green)、橘色(Orange)、青色(Cyan)、品紅(Magenta)、青色(Teal)、橄欖(Olive)、灰(Gray)配了白色，對比值不符合 AA 標準。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638404939664926165.png"></p> <p>而 G18 公式計算結果全部符合 AA，但有七種未達 AAA 標準，實務應用應避免以提高網頁可讀性。</p> <p data-slug="wcag-g18-color-contrast" data-title-en="WCAG G18 text and background contrast ratio" data-catg="Web Development" data-fbimg="https://i.imgur.com/pTR5tyX.jpg#960#640">Knowledge of WCAG G18 guildeline to decide correct text color for different background color.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>再來聊聊「依據背景色切換黑字或白字，確保文字明顯容易閱讀」這檔事。</p> <p>過去我都用 Github Copilot 教我的<a href="https://www.facebook.com/darkthread.net/posts/pfbid02PXepN99eFUnXJCP9zbRczzdY1RETaBhtbeYf2jS5XD27JoJxywxYtCjVwCavQBQnl">186 魔術數字公式</a>：<code>complementary = (r * 0.299 + g * 0.587 + b * 0.114) &gt; 186 ? '#000000' : '#ffffff'</code>，但上回在 <a href="/blog/20-distinct-colors/">20 種差異鮮明色彩組合</a>用這招決定文字顏色，讀者 lke 提醒，我為 #42d4f4 配了白字，看起來不太明顯。對照原文網頁範例，同一顏色配了黑字，看起來的確比白字明顯許多。這讓我開始懷疑，莫非 186 公式有缺陷？</p> <p>剛好在 FB 留言看到李奎翰大大分享的重要情資：關於文字顏色對比，W3C 的 <a href="https://www.w3.org/TR/WCAG21/">WCAG 網站內容無障礙指南</a>其實已有明確標準 - <a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18: Ensuring that a contrast ratio of at least 4.5:1 exists between text (and images of text) and background behind the text</a>，而文字與背景色對比度有兩個認證等級：(貫徹無障礙網站的難度不亞於下油鍋，這裡淺嚐就好，恕不再深入)</p> <ul><li>AA - 文字與背景對比值需大於 4.5:1</li> <li>AAA - 文字與背景對比值需大於 7:1</li></ul> <p>我找到<a href="ttps://webaim.org/resources/contrastchecker/?fcolor=000000&amp;bcolor=42D4F4">檢查對比值是否符合 WCAG 標準的網站</a>，確認 #42d4f4 應該配黑字才是正解，配白字只對比只有 1.75:1，死當!!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638404939661095048.png"></p> <p><a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18 標準</a>有提供完整的 RGB 值換算相對亮度(Relative Luminance)及對比值的公式，我將公式轉成 JavaScript 函式：</p> <pre><code>const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
</code></pre> <p>成功算出 1.7565 跟 11.9553，與檢測網站的計算結果一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638404939662987293.png"></p> <p>再來便是在已知背景色時，決定該用 #000000 還是 #fffff。</p> <p>依 G18 文件，若有兩種顏色，其相對亮度分別為 L<sub>1</sub> 及 L<sub>2</sub>，則二者之對比值可表示為：</p> <blockquote> <p>(L<sub>1</sub> + 0.05) / (L<sub>2</sub> + 0.05)</p> </blockquote> <p>黑字的使用時機應為「黑字與背景色的對比值」大於「白字與背景色的對比值」，黑色與白色的相對亮度分別為 0、1，若背景色之相對亮度為 L，代入上方公式可得：</p> <blockquote> <p>(L + 0.05) / (0 + 0.05) &gt; (1 + 0.05) / (L + 0.05)</p> </blockquote> <p>化簡以上公式，會得到魔術數字 0.179：</p> <pre><code>  (L + 0.05) / 0.05 &gt; 1.05 / (L + 0.05)
→ (L + 0.05)^2 / 0.05 &gt; 1.05
→ (L + 0.05)^2 &gt; 1.05 * 0.05
→ L + 0.05 &gt; sqrt(1.05 * 0.05)
→ L &gt; sqrt(1.05 * 0.05) - 0.05
→ L &gt; 0.179
</code></pre> <p>寫成黑白字判斷函式：</p> <pre><code>const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
</code></pre> <p>最後，比對一下 186 版及 G18 版文字配色效果：<a href="https://output.jsbin.com/gisavit">線上展示</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset="utf-8" /&gt;
&lt;script src="https://unpkg.com/vue@3"&gt;&lt;/script&gt;
&lt;style&gt;
.palette {
display: flex; margin-bottom: 12px;
flex-direction: row; flex-wrap: wrap;
width: 900px; font-size: 10pt;
font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
.palette&gt;div {
position: relative; width: 72px; height: 150px;
margin: 2px; padding: 6px;
}
.palette .chk {
background-color: white; text-align: center;
margin-top: 8px; margin-bottom: 8px;
}
.palette .chk span {
color: red; text-decoration: line-through;
margin-right: 4px;
}
.palette .chk span.pass {
text-decoration: none; color: green;
font-weight: bolder;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id="app"&gt;
&lt;div class="palette"&gt;
&lt;div v-for="(color,idx) in colors" :style="{ backgroundColor: color.bg }"&gt;
&lt;div v-for="fg in color.fgColors" :style="{ color: fg.fg }"&gt;
&lt;div&gt;{{ color.bg }}&lt;/div&gt;
&lt;div&gt;{{ fg.ratio.toFixed(2) }}&lt;/div&gt;
&lt;div class="chk"&gt;
&lt;span :class="{'pass':fg.aa}"&gt;AA&lt;/span&gt;
&lt;span :class="{'pass':fg.aaa}"&gt;AAA&lt;/span&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
const pool = ['#e6194B', '#3cb44b', '#ffe119', '#4363d8', '#f58231', '#911eb4', '#42d4f4', '#f032e6', '#bfef45', '#fabed4', '#469990', '#dcbeff', '#9A6324', '#fffac8', '#800000', '#aaffc3', '#808000', '#ffd8b1', '#000075', '#a9a9a9', '#ffffff', '#000000'];
const getTextColor186 = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
return (parseInt(m[1], 16) * 0.299 + parseInt(m[2], 16) * 0.587 + parseInt(m[3], 16) * 0.114) &gt; 186 ? '#000000' : '#ffffff';
}
const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
const colors = pool.map(hex =&gt;
({
bg: hex,
fgColors:
[getTextColor186(hex), getTextColorWcag(hex)].map(fg =&gt; {
const ratio = calcContrastRatio(fg, hex);
return {
fg, ratio, aa: ratio &gt;= 4.5, aaa: ratio &gt;= 7
};
})
}));
const app = Vue.createApp({
data() {
return {
colors: colors
}
}
});
var vm = app.mount('#app');
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>每個背景色有兩組結果，上方為 186 公式計算結果(對比值、是否符合 AA、AAA 標準)，下方為 G18 公式計算結果，抓出 186 公式為草綠(Green)、橘色(Orange)、青色(Cyan)、品紅(Magenta)、青色(Teal)、橄欖(Olive)、灰(Gray)配了白色，對比值不符合 AA 標準。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638404939664926165.png"></p> <p>而 G18 公式計算結果全部符合 AA，但有七種未達 AAA 標準，實務應用應避免以提高網頁可讀性。</p> <p data-slug="wcag-g18-color-contrast" data-title-en="WCAG G18 text and background contrast ratio" data-catg="Web Development" data-fbimg="https://i.imgur.com/pTR5tyX.jpg#960#640">Knowledge of WCAG G18 guildeline to decide correct text color for different background color.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>再來聊聊「依據背景色切換黑字或白字，確保文字明顯容易閱讀」這檔事。</p> <p>過去我都用 Github Copilot 教我的<a href="https://www.facebook.com/darkthread.net/posts/pfbid02PXepN99eFUnXJCP9zbRczzdY1RETaBhtbeYf2jS5XD27JoJxywxYtCjVwCavQBQnl">186 魔術數字公式</a>：<code>complementary = (r * 0.299 + g * 0.587 + b * 0.114) &gt; 186 ? '#000000' : '#ffffff'</code>，但上回在 <a href="/blog/20-distinct-colors/">20 種差異鮮明色彩組合</a>用這招決定文字顏色，讀者 lke 提醒，我為 #42d4f4 配了白字，看起來不太明顯。對照原文網頁範例，同一顏色配了黑字，看起來的確比白字明顯許多。這讓我開始懷疑，莫非 186 公式有缺陷？</p> <p>剛好在 FB 留言看到李奎翰大大分享的重要情資：關於文字顏色對比，W3C 的 <a href="https://www.w3.org/TR/WCAG21/">WCAG 網站內容無障礙指南</a>其實已有明確標準 - <a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18: Ensuring that a contrast ratio of at least 4.5:1 exists between text (and images of text) and background behind the text</a>，而文字與背景色對比度有兩個認證等級：(貫徹無障礙網站的難度不亞於下油鍋，這裡淺嚐就好，恕不再深入)</p> <ul><li>AA - 文字與背景對比值需大於 4.5:1</li> <li>AAA - 文字與背景對比值需大於 7:1</li></ul> <p>我找到<a href="ttps://webaim.org/resources/contrastchecker/?fcolor=000000&amp;bcolor=42D4F4">檢查對比值是否符合 WCAG 標準的網站</a>，確認 #42d4f4 應該配黑字才是正解，配白字只對比只有 1.75:1，死當!!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638404939661095048.png"></p> <p><a href="https://www.w3.org/TR/WCAG20-TECHS/G18.html">G18 標準</a>有提供完整的 RGB 值換算相對亮度(Relative Luminance)及對比值的公式，我將公式轉成 JavaScript 函式：</p> <pre><code>const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
</code></pre> <p>成功算出 1.7565 跟 11.9553，與檢測網站的計算結果一致。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638404939662987293.png"></p> <p>再來便是在已知背景色時，決定該用 #000000 還是 #fffff。</p> <p>依 G18 文件，若有兩種顏色，其相對亮度分別為 L<sub>1</sub> 及 L<sub>2</sub>，則二者之對比值可表示為：</p> <blockquote> <p>(L<sub>1</sub> + 0.05) / (L<sub>2</sub> + 0.05)</p> </blockquote> <p>黑字的使用時機應為「黑字與背景色的對比值」大於「白字與背景色的對比值」，黑色與白色的相對亮度分別為 0、1，若背景色之相對亮度為 L，代入上方公式可得：</p> <blockquote> <p>(L + 0.05) / (0 + 0.05) &gt; (1 + 0.05) / (L + 0.05)</p> </blockquote> <p>化簡以上公式，會得到魔術數字 0.179：</p> <pre><code>  (L + 0.05) / 0.05 &gt; 1.05 / (L + 0.05)
→ (L + 0.05)^2 / 0.05 &gt; 1.05
→ (L + 0.05)^2 &gt; 1.05 * 0.05
→ L + 0.05 &gt; sqrt(1.05 * 0.05)
→ L &gt; sqrt(1.05 * 0.05) - 0.05
→ L &gt; 0.179
</code></pre> <p>寫成黑白字判斷函式：</p> <pre><code>const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
</code></pre> <p>最後，比對一下 186 版及 G18 版文字配色效果：<a href="https://output.jsbin.com/gisavit">線上展示</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;meta charset="utf-8" /&gt;
&lt;script src="https://unpkg.com/vue@3"&gt;&lt;/script&gt;
&lt;style&gt;
.palette {
display: flex; margin-bottom: 12px;
flex-direction: row; flex-wrap: wrap;
width: 900px; font-size: 10pt;
font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}
.palette&gt;div {
position: relative; width: 72px; height: 150px;
margin: 2px; padding: 6px;
}
.palette .chk {
background-color: white; text-align: center;
margin-top: 8px; margin-bottom: 8px;
}
.palette .chk span {
color: red; text-decoration: line-through;
margin-right: 4px;
}
.palette .chk span.pass {
text-decoration: none; color: green;
font-weight: bolder;
}
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;div id="app"&gt;
&lt;div class="palette"&gt;
&lt;div v-for="(color,idx) in colors" :style="{ backgroundColor: color.bg }"&gt;
&lt;div v-for="fg in color.fgColors" :style="{ color: fg.fg }"&gt;
&lt;div&gt;{{ color.bg }}&lt;/div&gt;
&lt;div&gt;{{ fg.ratio.toFixed(2) }}&lt;/div&gt;
&lt;div class="chk"&gt;
&lt;span :class="{'pass':fg.aa}"&gt;AA&lt;/span&gt;
&lt;span :class="{'pass':fg.aaa}"&gt;AAA&lt;/span&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;script&gt;
const pool = ['#e6194B', '#3cb44b', '#ffe119', '#4363d8', '#f58231', '#911eb4', '#42d4f4', '#f032e6', '#bfef45', '#fabed4', '#469990', '#dcbeff', '#9A6324', '#fffac8', '#800000', '#aaffc3', '#808000', '#ffd8b1', '#000075', '#a9a9a9', '#ffffff', '#000000'];
const getTextColor186 = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
return (parseInt(m[1], 16) * 0.299 + parseInt(m[2], 16) * 0.587 + parseInt(m[3], 16) * 0.114) &gt; 186 ? '#000000' : '#ffffff';
}
const calcLuminance = (hex) =&gt; {
const m = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
const colors = [parseInt(m[1], 16), parseInt(m[2], 16), parseInt(m[3], 16)];
const [r, g, b] = colors.map(c =&gt; {
const r = c / 255.0;
if (r &lt;= 0.03928) {
return r / 12.92;
} else {
return Math.pow((r + 0.055) / 1.055, 2.4);
}
});
return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}
const calcContrastRatio = (hex1, hex2) =&gt; {
let L1 = calcLuminance(hex1);
let L2 = calcLuminance(hex2);
if (L1 &lt; L2) {
[L1, L2] = [L2, L1];
}
return (L1 + 0.05) / (L2 + 0.05);
}
const getTextColorWcag = (hex) =&gt;
calcLuminance(hex) &gt; 0.179 ? '#000000' : '#ffffff';
const colors = pool.map(hex =&gt;
({
bg: hex,
fgColors:
[getTextColor186(hex), getTextColorWcag(hex)].map(fg =&gt; {
const ratio = calcContrastRatio(fg, hex);
return {
fg, ratio, aa: ratio &gt;= 4.5, aaa: ratio &gt;= 7
};
})
}));
const app = Vue.createApp({
data() {
return {
colors: colors
}
}
});
var vm = app.mount('#app');
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>每個背景色有兩組結果，上方為 186 公式計算結果(對比值、是否符合 AA、AAA 標準)，下方為 G18 公式計算結果，抓出 186 公式為草綠(Green)、橘色(Orange)、青色(Cyan)、品紅(Magenta)、青色(Teal)、橄欖(Olive)、灰(Gray)配了白色，對比值不符合 AA 標準。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638404939664926165.png"></p> <p>而 G18 公式計算結果全部符合 AA，但有七種未達 AAA 標準，實務應用應避免以提高網頁可讀性。</p> <p data-slug="wcag-g18-color-contrast" data-title-en="WCAG G18 text and background contrast ratio" data-catg="Web Development" data-fbimg="https://i.imgur.com/pTR5tyX.jpg#960#640">Knowledge of WCAG G18 guildeline to decide correct text color for different background color.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638404939661095048.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638404939661095048.png" medium="image"/>
    <pubDate>Wed, 10 Jan 2024 14:07:32 GMT</pubDate>
  </item></channel>
</rss>