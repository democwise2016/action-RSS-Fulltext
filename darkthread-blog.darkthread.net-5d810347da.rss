<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
    <title><![CDATA[Coding4Fun - 高斯消去法解聯立方程式]]></title>
    <link>https://blog.darkthread.net/blog/guassian-elimination/</link>
    <itunes:title><![CDATA[Coding4Fun - 高斯消去法解聯立方程式]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]>
    </description>
    <content:encoded><![CDATA[<p>用電腦解<a href="https://zh.wikipedia.org/wiki/%E6%96%B9%E7%A8%8B%E7%BB%84">聯立方程式</a>，一般會使用<a href="https://zh.wikipedia.org/zh-tw/%E9%AB%98%E6%96%AF%E6%B6%88%E5%8E%BB%E6%B3%95">高斯消去法</a>用矩陣求解：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"></p> <p>在 .NET 做矩陣運算，<a href="https://zh.wikipedia.org/zh-tw/Math.NET_Numerics">Math.NET Numerics 程式庫</a>是首選。</p> <p>Math.Net Numerics 是個<a href="https://github.com/mathnet/mathnet-numerics">開源數學程式庫</a>，囊括矩陣、線性代數求解、機率、線性回歸、積分、傅立葉轉換... 等各種你想得到的數學運算，且被學術論文和期刊廣泛引用，其專業性毋庸置疑。總之，遇到 .NET 算數學需求，先找 Math.Net Numerics 就對了，別急著自己造輪子。</p> <p>練習題為假設有聯立方程式組如下，求解 x, y, z 為何？</p> <pre><code>7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
</code></pre> <p>先用 MathNet.Numerics.LinearAlgebra 函式庫求解。在專案 <code>dotnet add package MathNet.Numerics</code> 加入參照，只需幾行程式建個 Matrix 物件呼叫 .Solve() 便能完成：<a href="https://numerics.mathdotnet.com/LinearEquations">參考</a></p> <pre><code>using MathNet.Numerics.LinearAlgebra;
public class MathNetDemo
{
public static void Solve()
{
var matrix = Matrix&lt;double&gt;.Build.DenseOfArray(new double[,]
{
{7, -2, 3},
{2, -1, 4},
{-1, 3, -1}
});
var vector = Vector&lt;double&gt;.Build.Dense(new double[] {12, 12, 2});
var result = matrix.Solve(vector);
Console.WriteLine($"x = {result[0]}, y = {result[1]}, z = {result[2]}");
}
}
</code></pre> <p>小缺點是 MathNet 的 Matrix/Vector 的數字只支援 Double、Single、Complex 及 Complex32 四種型別(<code>Matrices and vectors of type 'Frac' are not supported. Only Double, Single, Complex or Complex32 are supported at this point.</code>)，使用 double 浮點數計算會包含浮點數誤差(Floating-Point Error)，得到 <code>x = 1.0000000000000002, y = 2, z = 2.9999999999999996</code>，有時遇到分數無法整除只能以循環小數表示，雖然實務應用不致造成問題，但在做數學習題時，這答案就是不完美。</p> <p>前幾天寫的<a href="/blog/cs-fraction-type/">分數型別</a>剛好可以避開上述問題，而高斯消去法邏輯不難，我打算自己實作玩看看，順便把解題過程印出來幫助了解原理，以上就是本次的計劃。</p> <p>用矩陣運算求解聯立方程式的概念是將常數值轉成矩陣形式(我用 Frac[,] 二維陣列模擬)，先排序將每一行最大值移到對角線，接著將對角線係數調成 1，對角線以下的係數調成 0 形成<a href="https://zh.wikipedia.org/zh-tw/%E9%98%B6%E6%A2%AF%E5%BD%A2%E7%9F%A9%E9%98%B5">列階梯形矩陣</a>，最後再將非對角線係數也調成 0，即可得解。</p> <p>實作程式邏輯如下：(註：我有為 Frac 增加 Abs() 方法 <code>public Frac Abs() =&gt; this &gt; 0 ? this : -this;</code> 計算絕對值)</p> <pre><code>public class FracMatrixDemo
{
public static void GaussianEliminationDemo()
{
/*
聯立方程組
7x - 2y + 3z = 12
2x - y + 4z = 12
-x + 3y - z = 2
*/
Frac[,] matrix = new Frac[3, 4]
{
{7, -2, 3, 12},
{2, -1, 4, 12},
{-1, 3, -1, 2}
};
PrintMatrix(matrix, "原矩陣");
// 排序將絕對值最大的列放在最上面
SortRows(matrix);
PrintMatrix(matrix, "排序後");
// 將矩陣化為上三角矩陣
Console.WriteLine("列階梯形矩陣");
ToRowEchelonForm(matrix);
// 將對角線以外係數調成 0
Console.WriteLine("簡化列階梯形矩陣");
ToDiagonal(matrix);
}
static void ToRowEchelonForm(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
// 將對角線係數調成 1
var diag = matrix[i, i];
for (int j = i; j &lt; colCount; j++)
{
matrix[i, j] /= diag;
}
PrintMatrix(matrix, $"({i}, {i}) 改為 1");
// 將對角線以下係數調成 0
for (int k = i + 1; k &lt; rowCount; k++)
{
var factor = matrix[k, i];
for (int l = i; l &lt; colCount; l++)
{
matrix[k, l] -= matrix[i, l] * factor;
if (k == l &amp;&amp; matrix[k, l] == 0)
{
throw new ApplicationException("無限多組解");
}
}
}
if (i &lt; rowCount - 1)
PrintMatrix(matrix, $"({i}, {i}) 以下改為 0");
}
}
static void ToDiagonal(Frac[,] matrix)
{
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = rowCount - 1; i &gt; 0; i--)
{
Console.WriteLine($"** 第 {i + 1} 列 **");
for (int j = i - 1; j &gt;= 0; j--)
{
var factor = matrix[j, i];
for (int k = i; k &lt; colCount; k++)
{
matrix[j, k] -= matrix[i, k] * factor;
}
}
PrintMatrix(matrix, $"({i}, {i}) 以上改為 0");
}
}
static void SortRows(Frac[,] matrix)
{
// 排序將對角線元素絕對值較大者放在最上面
var rowCount = matrix.GetLength(0);
var colCount = matrix.GetLength(1);
for (int i = 0; i &lt; rowCount; i++)
{
int maxRow = i;
for (int j = i + 1; j &lt; rowCount; j++)
{
if (matrix[j, i].Abs() &gt; matrix[maxRow, i].Abs())
{
maxRow = j;
}
}
// 與最大列交換
if (maxRow != i)
{
for (int k = 0; k &lt; colCount; k++)
{
var temp = matrix[i, k];
matrix[i, k] = matrix[maxRow, k];
matrix[maxRow, k] = temp;
}
}
}
}
public static void PrintMatrix(Frac[,] matrix, string title)
{
var len = Math.Max(5, matrix.Cast&lt;Frac&gt;().Max(o =&gt; o.ToString().Length) + 2);
Console.ForegroundColor = ConsoleColor.Cyan;
Console.WriteLine(title);
Console.ForegroundColor = ConsoleColor.Yellow;
for (int i = 0; i &lt; matrix.GetLength(0); i++)
{
for (int j = 0; j &lt; matrix.GetLength(1); j++)
{
Console.Write(matrix[i, j].ToString().PadLeft(len));
}
Console.WriteLine();
}
Console.WriteLine();
Console.ResetColor();
}
}
</code></pre> <p>執行結果如下，成功。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638435121926223442.png"></p> <p>註：範例程式已更新至 <a href="https://github.com/darkthread/frac-type">Github</a></p> <p data-slug="guassian-elimination" data-title-en="Implementating Gussian elimination with C#" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2018/12/09/08/50/pattern-3864533_960_720.jpg#960#720">This article introduces how to solve a system of simultaneous equations using the MathNet library and implement the Gaussian elimination method with a custom fraction type to obtain accurate solutions</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638435121923942262.png" medium="image"/>
    <pubDate>Wed, 14 Feb 2024 04:33:19 GMT</pubDate>
  </item><item>
    <title><![CDATA[小試前端 REST 資料庫 - Supabase]]></title>
    <link>https://blog.darkthread.net/blog/supabase/</link>
    <itunes:title><![CDATA[小試前端 REST 資料庫 - Supabase]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>最近玩了自架 ChatGPT 網站的開源專案 <a href="https://github.com/mckaywrigley/chatbot-ui">Chatbot UI</a>，網站以 React 前端程式為核心，1.0 原以 localStorate 儲存資料，基於安全疑慮、大小限制、無法跨機器共用等因素，改版時將資料改存到後端，用了一個我沒看過的資料庫 - <a href="https://supabase.com/">Supabase</a>。</p> <p>Supabase是一個開源的後端服務平台(取代 Google 的閉源服務 - <a href="https://tw.alphacamp.co/blog/2016-07-22-firebase">Firebase</a>)，核心概念是把 PostgreSQL 包成 RESTful API/ GraphQL，允許 JavaScript 從瀏覽器端讀寫資料庫內容；但 Supabase 功能不只於此，它還整合前端常用的使用者驗證、資料更新時主動通知、Embedding 向量資料搜尋... 等功能。Supabase 提供這類通知功能，我自己是習慣開發自訂 Web API 實作，不喜歡資料庫直接對外讓前端程式自由操作(有安全及效能疑慮)。但將資料操作邏輯寫在前端的概念簡單粗暴，不用多花心力搞一套專屬 API，也可選擇雲端版服務(連後端人員都省了)，加上同一套程式前後端都能跑... 猜想這是前端工程師愛用的原因吧。</p> <p>雖然我個人還是覺得：放任前端自由操作資料庫，對安全與效能是件危險的事，但因緣際會碰上了，老狗再學點新把戲長見識唄。</p> <p>我演練了 CLI 工具安裝、新增專案、建資料表到用 JavaScript 程式讀寫資料的過程，記錄重點如下：</p> <h3>安裝 Supabase CLI</h3> <p>Supabase 有<a href="https://supabase.com/">雲端服務</a> (有免費也有<a href="https://supabase.com/pricing">付費方案</a>)，但也可以跑 Docker 容器在地端執行，我則是在 Linux Ubuntu 22.04 跑的測試，先裝好 Docker 服務，接著安裝 <a href="https://supabase.com/docs/guides/cli/getting-started?platform=linux#installing-the-supabase-cli">Supabase CLI</a> (Linux 可以用 Homebrew 或下載 .deb/.rpm 安裝)。</p> <h3>建立專案</h3> <p>建立專案資料夾，在目錄下執行 <code>supabase init</code>，CLI 會建立 <code>supabase</code> 子資料夾，執行 <code>supabase start</code> CLI 會下載並為這個專案建立 12 個 Docker 容器 [1]、Web API 在 54321 Port [2]，管理介面在 54323 Port [3]，anon key 與 service_role Key [4] 存取時會用到，另外還建了五個 Volume [5] 用來放資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"></p> <p>Supabase 使用 anon 及 service_role 兩支 JWT 長效金鑰區隔權限，anon 通常配合 RLS(<a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>) 使用，每個人只能讀寫自己寫入的資料，service_role 則能讀寫所有資料。故 anon Key 類似公鑰，可明碼寫入網頁送到前端，反正讀寫資料時會一併檢查登入身分，判斷是否有權存取。而 sevice_role Key 則如同系統管理密碼，需嚴加保護。一般只在後端使用(為了怕有人失手 Push 到 Github，Github 有加了防呆檢查)，sevice_role Key 一旦外流等同資料完全對外公開，務必謹慎處理。(這也是我不愛資料庫直接對外的理由)</p> <h3>建立資料表</h3> <p>接著我打算建立一個測試資料表 players。執行 <code>supabase migration new create_players_table</code> 會建立 <code>supabase/migrations/&lt;timestamp&gt;_create_players_table.sql</code> 空白檔，填入新增資料表 SQL 指令：</p> <pre><code>CREATE TABLE players (
id SERIAL PRIMARY KEY,
name VARCHAR(16) UNIQUE NOT NULL,
score INTEGER NOT NULL,
regdate TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
);
</code></pre> <p>在 <code>supabase/seed.sql</code> 填入新增資料指令：</p> <pre><code>INSERT INTO players (name, score) VALUES
('Jeffrey', 32767);
</code></pre> <p>接著執行 <code>supabase db reset</code> 初始化資料庫、建立資料表並寫入資料：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638434361976033743.png"></p> <p>使用 <code>http://127.0.0.1:54323/</code> 管理介面可查到寫入的內容：(介面有提醒目前資料表允許匿名讀寫，實際應用常設為 <a href="https://supabase.com/docs/guides/auth/row-level-security">Row Level Security</a>)</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638434361978672972.png"></p> <h3>node.js 讀寫資料</h3> <p>再來測試用 Node.js 跑 JavaScript 讀取資料。開始前先 <code>npm install @supabase/supabase-js</code> 安裝程式庫，讀取程式如下：</p> <pre><code>import { createClient } from '@supabase/supabase-js'
// TODO: read from configuration file
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
// Create a single supabase client for interacting with your database
const supabase = createClient(apiUrl, anonKey);
// Read data from players table
const readData = async () =&gt; {
const { data, error } = await supabase
.from('players')
.select('*')
if (error) {
console.error('Error fetching data:', error)
return
}
console.log('Data:', data)
}
readData();
</code></pre> <p>讀取成功!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638434361986184756.png"></p> <h3>把程式搬進網頁執行</h3> <p>我們將上面的程式碼稍作修改，搬進網頁執行看看：<a href="https://github.com/supabase/supabase-js?tab=readme-ov-file#umd">參考</a></p> <pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Read Data&lt;/title&gt;
&lt;script src="https://unpkg.com/@supabase/supabase-js@2"&gt;&lt;/script&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;pre&gt;&lt;/pre&gt;
&lt;script&gt;
const { createClient } = supabase;
const apiUrl = 'http://127.0.0.1:54321';
const anonKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.***';
const _supabase = createClient(apiUrl, anonKey);
const readData = async () =&gt; {
const { data, error } = await _supabase
.from('players')
.select('*')
if (error) {
alert('Error fetching data:', error)
return
}
document.querySelector('pre').textContent = JSON.stringify(data, null, 2)
}
readData();
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
</code></pre> <p>薑! 薑! 薑! 薑~~~ 只要前端能存取 54321 Port，同一段程式可以放進瀏覽器執行，一魚兩吃。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638434361988770111.png"></p> <p>由以上展示，有點能了解為什麼 Supabase 這種解決方案對前端工程師特別有吸引力。</p> <h3>REST API</h3> <p>最後，有一行 curl 呼叫 REST API 範例結束這回合~</p> <pre><code>curl 'https://&lt;PROJECT_REF&gt;.supabase.co/rest/v1/todos' \
-H "apikey: &lt;ANON_KEY&gt;" \
-H "Authorization: Bearer &lt;ANON_KEY&gt;"
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638434361991182745.png"></p> <p>後記：chatbot-ui 目前這個搭配 Supabase 的版本，架構與部署有點複雜，很難做到即插即用。作者有提到<a href="https://github.com/mckaywrigley/chatbot-ui/issues/1376#issuecomment-1928550602">目前已在研發 Sqlite 版</a>(這類應用，Sqlite 也是我心中的首選)預計幾週後推出，我決定先不跟 Subabase 糾纏，等熟悉的 Sqlite 版出來再玩。</p> <p data-slug="supabase" data-title-en="Introduce to Supabase" data-catg="Web Development" data-fbimg="https://i.imgur.com/XV3EMas.jpg#1280#720">Introduce to the front-end data service solution - Supabase.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638434361971381959.png" medium="image"/>
    <pubDate>Tue, 13 Feb 2024 15:33:31 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 實現 -2 * 1/3 + 3/4 = 1/12 分數運算]]></title>
    <link>https://blog.darkthread.net/blog/cs-fraction-type/</link>
    <itunes:title><![CDATA[Coding4Fun - 實現 -2 * 1/3 + 3/4 = 1/12 分數運算]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>.NET 內建的數字型別裡有整數、浮點數，但如果我們計算 <code>-2 * 1/3 + 3/4</code> 想得到 <code>1/12</code> 而不是 <code>0.08333333333</code>，該怎麼做？</p> <p>就來寫個可以加減乘除的自訂分數型別吧。就像下面這樣，是不是很酷？</p> <pre><code>Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
// -2 * 1/3 + 3/4 = 1/12
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}");
</code></pre> <p>看似神奇的效果，不過是用到 C# 隱含轉型及自訂運算子特性，要實作一點也不難。<a href="https://learn.microsoft.com/zh-tw/dotnet/csharp/language-reference/operators/operator-overloading?WT.mc_id=DOP-MVP-37580">官方文件</a>剛好有個雛型範例，已實作了分數加減乘除，我計劃再擴充讓它更完整，包含以下功能：</p> <ul><li>由字串(例："2/3"、"3/4")建立分數物件</li> <li>以整數建立分數物件</li> <li>將字串或整數轉為分數物作，例如：(Frac)"2/3"</li> <li>約分簡化函數 Simpkify() (例：4/6 變成 2/3)</li> <li>指定分母 WithDenominator(n) (例：2/3 指定分母 9 得到 6/9)</li> <li>相等比較 (==、Equals()) (可識別約分後相等，如 2/3 == 4/6)</li> <li>大於小於比較</li></ul> <p>Frac 型別是個唯讀結構(struc)，標示唯讀明確表示資料賦值後即不可變(Immutability)，有助於程式碼理解及推理(不必考慮狀態變化)、避免多執行緒衝突(省去同步化的效能損耗)、減少改變值導致錯誤的風險。我寫了一個簡單範例如下：</p> <pre><code>public readonly struct Frac
{
private readonly int num;
private readonly int den;
// 標準建構式：分子、分母
public Frac(int numerator, int denominator)
{
if (denominator == 0)
{
throw new ArgumentException("Denominator cannot be zero.", nameof(denominator));
}
if (denominator &lt; 0)
{
numerator = -numerator;
denominator = -denominator;
}
num = numerator;
den = denominator;
}
// ValueTuple 建構式
public Frac((int numerator, int denominator) frac) : this(frac.numerator, frac.denominator) { }
// 由字串解析分子分母
static Func&lt;string, (int numerator, int denominator)&gt; ParseFrac = fracStr =&gt;
{
var parts = fracStr.Split('/');
if (parts.Length != 2)
{
throw new ArgumentException("Invalid fraction string.", nameof(fracStr));
}
return (int.Parse(parts[0]), int.Parse(parts[1]));
};
// 使用字串建立分數
public Frac(string fracStr) : this(ParseFrac(fracStr)) { }
public static implicit operator Frac(string fracStr) =&gt; new Frac(fracStr);
// 使用整數建立分數
public Frac(int number) : this(number, 1) { }
public static implicit operator Frac(int number) =&gt; new Frac(number);
// 正、負、加減乘除
public static Frac operator +(Frac a) =&gt; a;
public static Frac operator -(Frac a) =&gt; new Frac(-a.num, a.den);
public static Frac operator +(Frac a, Frac b)
=&gt; new Frac(a.num * b.den + b.num * a.den, a.den * b.den).Simplify();
public static Frac operator -(Frac a, Frac b)
=&gt; a + (-b);
public static Frac operator *(Frac a, Frac b)
=&gt; new Frac(a.num * b.num, a.den * b.den).Simplify();
public static Frac operator /(Frac a, Frac b)
{
if (b.num == 0)
{
throw new DivideByZeroException();
}
return new Frac(a.num * b.den, a.den * b.num).Simplify();
}
// 指定分母
public Frac WithDenominator(int newDenominator)
{
// 若無法整除，則無法指定分母
if (newDenominator == 0 || newDenominator % den != 0)
{
throw new ArgumentException($"Cannot specify the denominator - {newDenominator}.", nameof(newDenominator));
}
return new Frac(num * newDenominator / den, newDenominator);
}
public override string ToString() =&gt;
den == 1 ? num.ToString() : $"{num}/{den}";
// 約分
public Frac Simplify()
{
int gcd = Gcd(num, den);
return new Frac(num / gcd, den / gcd);
}
// 通分
public static (Frac, Frac) CommonDenominator(Frac a, Frac b)
{
int lcm = Lcm(a.den, b.den);
return (new Frac(a.num * lcm / a.den, lcm), new Frac(b.num * lcm / b.den, lcm));
}
// 最大公約数
private static int Gcd(int a, int b)
{
if (b == 0)
{
return a;
}
return Gcd(b, a % b);
}
// 最小公倍数
private static int Lcm(int a, int b)
{
return a * b / Gcd(a, b);
}
// 相等比較 (先約分再比較)
// 註：Equals、GetHashCode、==、!= 要一起實作
public override bool Equals(object? obj) =&gt; obj is Frac other
&amp;&amp; this.Simplify().ToString() == other.Simplify().ToString();
public override int GetHashCode() =&gt; Simplify().ToString().GetHashCode();
public static bool operator ==(Frac a, Frac b) =&gt; a.Equals(b);
public static bool operator !=(Frac a, Frac b) =&gt; !a.Equals(b);
// 大小比較 (先通分再比較) 註：大於小放要一起實作
public static bool operator &lt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &lt; CommonDenominator(a, b).Item2.num;
public static bool operator &gt;(Frac a, Frac b) =&gt;
CommonDenominator(a, b).Item1.num &gt; CommonDenominator(a, b).Item2.num;
public static bool operator &lt;=(Frac a, Frac b) =&gt; a &lt; b || a == b;
public static bool operator &gt;=(Frac a, Frac b) =&gt; a &gt; b || a == b;
}
</code></pre> <p>驗證程式如下：</p> <pre><code>// 使用自訂分數型別進行數學運算
Frac a = new Frac(1, 2);
Frac b = new Frac("3/4");
Frac c = new Frac(2);
Frac d = "2/3";
Frac e = 3;
Frac f = "4/6";
Console.WriteLine($"a = {a}"); // 1 / 2
Console.WriteLine($"正號 +a = {a}"); // 1 / 2
Console.WriteLine($"負號 -a = {-a}"); // -1 / 2
Console.WriteLine($"加法 {a} + {b} = {a + b}"); // 5 / 4
Console.WriteLine($"減法 {a} - {b} = {a - b}"); // -1 / 4
Console.WriteLine($"乘法 {a} * {b} = {a * b}"); // 3 / 8
Console.WriteLine($"除法 {a} / {b} = {a / b}"); // 2 / 3
Console.WriteLine($"乘整數 {b} * -3 = {a * -3}"); // -3 / 2
Console.WriteLine($"除整數 {b} / 3 = {a / 3}"); // 1 / 6
Console.WriteLine($"大於 {b} &gt; {d} = {b &gt; d}"); // True
Console.WriteLine($"小於 {b} &lt; {d} = {b &lt; d}"); // False
Console.WriteLine($"等於 {d} == {f} = {d == f}"); // True
Console.WriteLine($"大於等於 {d} &gt;= {f} = {d &gt;= f}"); // True
Console.WriteLine($"相等 {d}.Equals({f}) = {d.Equals(f)}"); // True
Console.WriteLine($"指定分母(9) {d} == {d.WithDenominator(9)}"); // 6 / 9
try {
Console.WriteLine($"指定分母(7) {d} == {d.WithDenominator(7)}");
}
catch (ArgumentException ex)
{
Console.WriteLine(ex.Message);
}
// -2 * 1/3 + 3/4 = 1/12
Frac g = -2 * (Frac)"1/3" + new Frac(3, 4);
Console.WriteLine($"-2 * 1/3 + 3/4 = {g}"); // 1 / 12
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"></p> <p>坦白一件事，以上程式大約只有 30 ~ 40% 由我輸入，其餘都是給個大方向寫個函數名稱 Github Copilot 便幫忙寫完程式邏輯，我再檢查跟微調。成對匹配的項目，像是 == 與 !=，&gt; 跟 &lt;，更是寫完第一個 Copilot 自動補上另一個。而最大公約數 Gcd()、最小公倍數 Lcm() 連同函式名稱含內容都是 Copilot 生出來的。</p> <p>使用 Github 初期有點不太習慣，連公約數公倍數怎麼算還沒想，程式就寫完了，有種被餵飯的感覺，心裡不太踏實。但想想，過去 Intellisense 的出現幫大腦減輕了記憶方法、參數以及打字的負擔；Github Copilot 只是再進一步減輕大腦「記憶及構思演算法細節」的負擔，雖然失去部分鍛練大腦思考的機會，只要確保有看懂邏輯並有能力修正及優化，就安心享受開發加速的快感吧。</p> <p>當改用先進工具已是不可擋的趨勢，討論該不該接受已無意義，不跟進就像人人都用電動起子你堅持用羅賴把鎖螺絲釘，自此失去競爭力。抱持正確心態，別對工具過度依賴，要求自己看懂 Copilot 生成的每一行程式碼，與湊到程式會動不知所以然的開發者形成區隔，方能立於不敗之地。</p> <p data-slug="cs-fraction-type" data-title-en="Writing a C# fraction type" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2020/06/30/15/03/table-5356682_960_720.jpg#960#720">An example using operator overloading and implicit conversion to create a useful fraction structure.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638432598005863202.png" medium="image"/>
    <pubDate>Sun, 11 Feb 2024 14:48:14 GMT</pubDate>
  </item><item>
    <title><![CDATA[AI 共筆 - 台灣本土版語言模型 - Taiwan LLM 是怎麼煉成的？]]></title>
    <link>https://blog.darkthread.net/blog/how-taiwan-llm-trained/</link>
    <itunes:title><![CDATA[AI 共筆 - 台灣本土版語言模型 - Taiwan LLM 是怎麼煉成的？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>2024 新年第一天，拜年文就由 AI 打頭陣吧! 跟風附上 AI 生成圖，祝大家在新的一年神采飛揚，龍光煥發!</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" alt="thumbnail"></p> <p>在<a href="/blog/how-llm-training-burn-money/">訓練大型語言模型有多燒錢？</a>一文學到：標榜最有台灣味的 Taiwan-LLM 語言模型，是以 Meta LLaMA 2 為基礎的全參數微調模型，大幅提升繁體中文能力並融入台灣文化。Taiwan LLM 採用的訓練方式包含 8 x A100 兩週 Continuous Pretraining (接續預訓練，讓 LLaMA 2 學中文) 及 8 x H100 12 小時的 Instruction Fine-Tuning。我對它的訓練方式格外好奇，意外找到 Taiwan LLM 計劃主要參與者，台大博士生林彥廷同學的第一現場報導影片 - <a href="https://www.youtube.com/watch?v=3Y3-79NZQZg">Taiwan LLM 解析台灣第一個大型對話式語言模型</a>，內容還算淺顯，只稍具機器學習基本概念如我，即便看不懂門道，也能有點收獲，特記錄重點備忘。</p> <ol><li>LLaMA 2 不是原本就會講中文？為什麼還要學中文？<br> LLM 的訓練資料繁體中文佔比很低，在全世界的文本可能佔不到 0.1% (英文 54%、西班牙文 30%，中文僅 1%；其中簡體中文 90%，繁體中文 10%)。語言不只是翻譯就好，在文化、價值觀、常識(例如：發票載具)方面也要對齊才算在地化，使用者多聊兩句便能感受這是會講中文的外國機器人，還是真的具有台灣魂？要融入本地語精神，需要用大量台灣文本進行預訓練才能實現。</li> <li>Taiwan LLM 的訓練程序為何？<br> 分為三階段：Continuous-Pretraining (使用台灣語料 Corpus 進行接續預訓練, cPT) -&gt; Supervised Fine Tuning (SFT) -&gt; Real User Interaction and Reinforcement (上網公測，蒐集回饋)</li> <li>Taiwan LLM 用了哪些台灣語料進行 cPT？<br> V1 用維基百科跟網路資料(CommonCrawl)，但發現網路資料品質不佳(包含太多詐騙、色情、政治性)，需要大量事後調校修正。V2 改用社群媒體、法律文件、新聞時事取代網路資料，共 35B。(相較國外文本 1 ~ 2T 起跳，其實還是很少)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638431770931819580.png"><br> V2 拿掉網路資料後，cPT 完的結果價值觀回答方式已經很接近台灣人，偏差小很多，大幅減少微調工程。結論：從源頭控管可以省很多工夫。<br> cPT Context 長度上限 4000 (相當於短期記憶)、每個文本只看一次 (1 Epoch)、加速技巧：Fully Sharded Data Parallel(FSDP、DeepSpeed)、Flash Attention v2 (利用 GPU 結構減少 Memory 搬運次數，A100/H100 支援)、bfloat16 (源自 Meta RD 的血淚心得，bfloat 16 (Range 大，精準度低) + 更多資料 = 效果較好，目前較流行 fp16、bfloat16，開始有人用 fp8)</li> <li>為什麼用 cPT 而非重新訓練？<br> Google DeepMind 研究，<a href="https://www.toolify.ai/ai-news/unveiling-the-scaling-secrets-of-chinchillas-31431">Chinchilla Scaling Laws</a> 一個參數至少要訓練在 20 個 Token 以上，35B / 20 &lt; 2B，這個資料量只能從頭訓練不到 2B 的模型。故以 LLaMA 2 7B、13B 模型為基礎接續訓練，成本效益較好。</li> <li>cPT 過程有遇到什麼困難？<br> 下圖紫線是 Taiwan LLM 的 Loss 曲線，隨 global step 步數增加 Loss 會下降，但會有上下波動的細線叫 Loss Spike，這類不穩定常來自於 Memory 之類的錯誤，解法是 Rollback 忽略會出錯的資料。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638431770934302734.png"></li> <li>SFT 如何進行？<br> 蒐集 Instruction Tuning 資料集(人工蒐集 + ChatGPT 翻譯產生 50 萬條、用史丹佛 Alpaca 手法生成)，共 100 萬條(事後覺得太多，浪費過多算力)。範圍包含：數學題、程式題、知識問答、推理題、文章摘要、創意寫作、翻譯題、角色扮演、閒聊...<br> 初版模型不夠「台」，解法是手寫 100 條 QA，再請 ChatGPT 換句話說衍生資料。</li> <li>Taiwan LLM 有進行公開測試，大家都怎麼跟機器人聊天？<br> 事後分析 Log，互動幾乎都傾向知識問答，幾把機器人考倒為樂。如要商業應用，這是個值得思考的問題，所有聊天服務都被拿來跟 ChatGPT 4 比較，7B/13B 的模型可能會遠低於對方期望。建議明確聲明定位及能力範圍，降低使用者期待。</li> <li>如何客觀評測 Taiwan LLM 表現？<br> 有個資料集叫 Taiwan Trivia QA (MediaTek Research 聯發創新提供，題例：濁水溪、孫越、櫻花勾吻鮭)，Taiwan LLM 小勝 ChatGPT，不如 ChatGPT 4。(註：評測結果未必與體感一致，數字很好看，但使用者感受未必是好的)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638431770936531611.png"><br> 案例：NTU 是什麼的縮寫、22K 是什麼、發票載具是什麼意思</li> <li>Taiwan LLM 的適用場合？ <ol><li>要落地在自家伺服器執行(資料不允許外流) 2) 口袋不夠深玩不起 ChatGPT 微調，改微調 Taiwan LLaMA</li></ol> </li> <li>Mixture of Experts (MoE) 是什麼？<br> 由 Mistral AI 引領風潮，<a href="https://www.jiqizhixin.com/articles/2024-01-22-2">MoE 8x7B</a> 逼近 GPT 4。多個小的 FFN Layer，將任務拆成不同子任務，由 Router 交給不同專長的 FFN 處理。Taiwan LLM MoE 已開始 Pilot Run。<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638431770938801072.png"><br> 硬體支援問題：MoE 不宜跨節點，是現行數量不超過 8 的原因(八卡機)，未來應會有突破。</li> <li>有無可能蒐集更多台灣語料 (Corpus)？<br> 電子書是個好來源，但因著作權問題，在現行法律下難以引用，或許可從法令解釋面解套。</li></ol> <p data-slug="how-taiwan-llm-trained" data-fbimg="https://i.imgur.com/UVrVDqX.png#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig5_638431770929064733.jpg" medium="image"/>
    <pubDate>Sat, 10 Feb 2024 15:51:12 GMT</pubDate>
  </item><item>
    <title><![CDATA[源碼檢測軟體冷門選擇 - PVS-Studio]]></title>
    <link>https://blog.darkthread.net/blog/pvs-studio-survey/</link>
    <itunes:title><![CDATA[源碼檢測軟體冷門選擇 - PVS-Studio]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://pvs-studio.com/en/">PVS-Studio</a> 是一套源碼檢測軟體(小知識：專業術語叫 SCA，(<a href="https://zh.wikipedia.org/zh-tw/%E9%9D%9C%E6%85%8B%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90">Static Code Analysis 靜態程式分析</a>)，知名度遠不及 Fortify、Checkmarx 或 SonarQube，不過，PVS-Studio 專注於 C、C#、C++、Java 四種語言，且有給學生及開源專案免費授權方案，好像也值得試看看。以下是我在 Linux 上安裝並掃瞄 .NET 專案的筆記。</p> <p>官方的安裝及使用說明很清楚，下載及執行方式設計得蠻簡便，下面我這次主要參考的文件：</p> <ul><li><a href="https://pvs-studio.com/en/docs/manual/0051/">PVS-Studio C# installation on Linux and macOS</a></li> <li><a href="https://pvs-studio.com/en/docs/manual/0035/">Analyzing Visual Studio / MSBuild / .NET projects from the command line using PVS-Studio</a></li></ul> <p>安裝設定摘要：(我的測試作業系統為 Ubuntu 22.04)</p> <ol><li>需先<a href="https://learn.microsoft.com/en-us/dotnet/core/install/linux?WT.mc_id=DOP-MVP-37580">安裝 .NET 8 SDK</a></li> <li>PVS-Studio C# Analyzer 依賴 PVS-Studio C++ Analyzer (pvs-studio)，但有包成 apt 套件會一併下載安裝，基本上可無腦完成<br> 執行以下指令新增 apt 套件來源及安裝 <pre><code>wget -q -O - https://cdn.pvs-studio.com/etc/pubkey.txt | \
sudo apt-key add -
sudo wget -O /etc/apt/sources.list.d/viva64.list \
https://cdn.pvs-studio.com/etc/viva64.list
sudo apt-get update
sudo apt-get install pvs-studio-dotnet
# 若看到版號代表安裝完成
pvs-studio-dotnet --version
</code></pre> </li> <li>輸入授權序號啟用，授權資料會寫入 <code>~/.config/PVS-Studio/PVS-Studio.lic</code>，<a href="https://pvs-studio.com/en/order/for-students/">學生、教師</a>及<a href="https://pvs-studio.com/en/order/open-source-license/">開源專案開發者</a>可免費使用： <pre><code>pvs-studio-analyzer credentials ${USER_NAME} ${LICENSE_KEY}
# 學生、教師免費使用之序號設定方式，每個檔案開頭需加指定註解文字
# https://pvs-studio.com/en/order/for-students/
pvs-studio-analyzer credentials PVS-Studio Free FREE-FREE-FREE-FREE
# 開源專案需填表申請，並在 README.md 加註 Credit 資訊
# https://pvs-studio.com/en/order/open-source-license/
</code></pre> </li> <li>OWASP 資安掃瞄是停用的，要編輯 <code>~/.config/PVS-Studio/Settings.xml</code> 將 <a href="https://pvs-studio.com/en/docs/manual/6653/">DisableOWASPAnalysisCs 選項</a>設成 false： <pre><code>&lt;?xml version="1.0" encoding="utf-8"?&gt;
&lt;ApplicationSettings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema"&gt;
&lt;!-- ... --&gt;
&lt;DisableOWASPAnalysisCs&gt;false&lt;/DisableOWASPAnalysisCs&gt;
&lt;!-- ... --&gt;
&lt;/ApplicationSettings&gt;
</code></pre> </li> <li>與 SonarQube 相比，PVS-Studio 掃瞄 .NET 專案相對簡單，一行指令搞定，結果會寫成 JSON 或 XML： <pre><code>pvs-studio-dotnet -t vunerable-sample.sln -o result.json --progress
</code></pre> </li></ol> <p>但實測 PSV-Studio 的偵測能力不如預期，參考官方文件案例，用以下程式片段測試 PVS-Studio 抓得出 SQL Injection：</p> <pre><code>using System.Data.SqlClient;
var db = new DBHelper();
db.ProcessUserInfo(args[0]);
public class DBHelper
{
string _connectionString = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
public void ProcessUserInfo(String userName)
{
var command = "SELECT * FROM Users WHERE userName = '" + userName + "'";
ExecuteCommand(command);
}
private void ExecuteCommand(String rawCommand)
{
using (SqlConnection connection = new SqlConnection(_connectionString))
{
using (var sqlCommand = new SqlCommand(rawCommand, connection))
{
using (var reader = sqlCommand.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
}
}
</code></pre> <p>但改成以下寫法就抓不到了：</p> <pre><code>using System.Data.SqlClient;
Test(args[0]);
void Test(string userName)
{
var cnStr = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
using (var cn = new SqlConnection(cnStr))
{
cn.Open();
var cmd = new SqlCommand("SELECT * FROM Users WHERE userName = '" + userName + "'", cn);
using (var reader = cmd.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
</code></pre> <p>為了對照，我寫了一個混合版，同時用 PVS-Studio 與 SonarQube 社群版掃瞄，比較結果。</p> <p>PVS-Studio 只抓到 DbHelper.ProcessUserInfo() 的 SQL Injection (Line 31)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638430402768822402.png"></p> <p>SonarQube 社群版抓到寫死密碼及 Test() 的 SQL Injection，但遺漏了 DbHelper.ProcessUserInfo()：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638430402773642493.png"></p> <p>順便再測了 Github Copilot，表現讓人驚豔，兩處 SQL Injection 跟寫死密碼都有被抓出來：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638430402777722712.png"></p> <p>綜合以上的簡單體驗，PVS-Studio 安裝及批次掃瞄夠簡便，但在簡單 C# SQL Injection 漏洞程式碼測試表現比 SonarQube 社群版(CE)弱些(有可能 PVS-Studio 的強項是 C/C++)，但意外有抓到 SonarQube CE 未檢測到的項目，個人覺得若要使用，<a href="https://pvs-studio.com/en/docs/manual/0037/">整合作為 SonarQube 擴充套件</a>，與 SonarQube 互補盲點是較好的做法。</p> <p data-slug="pvs-studio-survey" data-title-en="SAST tool PVS-Studio survey" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2016/06/17/18/45/chile-1463830_960_720.jpg#960#720">Simple test of SAST tool - PVS-Studio.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://pvs-studio.com/en/">PVS-Studio</a> 是一套源碼檢測軟體(小知識：專業術語叫 SCA，(<a href="https://zh.wikipedia.org/zh-tw/%E9%9D%9C%E6%85%8B%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90">Static Code Analysis 靜態程式分析</a>)，知名度遠不及 Fortify、Checkmarx 或 SonarQube，不過，PVS-Studio 專注於 C、C#、C++、Java 四種語言，且有給學生及開源專案免費授權方案，好像也值得試看看。以下是我在 Linux 上安裝並掃瞄 .NET 專案的筆記。</p> <p>官方的安裝及使用說明很清楚，下載及執行方式設計得蠻簡便，下面我這次主要參考的文件：</p> <ul><li><a href="https://pvs-studio.com/en/docs/manual/0051/">PVS-Studio C# installation on Linux and macOS</a></li> <li><a href="https://pvs-studio.com/en/docs/manual/0035/">Analyzing Visual Studio / MSBuild / .NET projects from the command line using PVS-Studio</a></li></ul> <p>安裝設定摘要：(我的測試作業系統為 Ubuntu 22.04)</p> <ol><li>需先<a href="https://learn.microsoft.com/en-us/dotnet/core/install/linux?WT.mc_id=DOP-MVP-37580">安裝 .NET 8 SDK</a></li> <li>PVS-Studio C# Analyzer 依賴 PVS-Studio C++ Analyzer (pvs-studio)，但有包成 apt 套件會一併下載安裝，基本上可無腦完成<br> 執行以下指令新增 apt 套件來源及安裝 <pre><code>wget -q -O - https://cdn.pvs-studio.com/etc/pubkey.txt | \
sudo apt-key add -
sudo wget -O /etc/apt/sources.list.d/viva64.list \
https://cdn.pvs-studio.com/etc/viva64.list
sudo apt-get update
sudo apt-get install pvs-studio-dotnet
# 若看到版號代表安裝完成
pvs-studio-dotnet --version
</code></pre> </li> <li>輸入授權序號啟用，授權資料會寫入 <code>~/.config/PVS-Studio/PVS-Studio.lic</code>，<a href="https://pvs-studio.com/en/order/for-students/">學生、教師</a>及<a href="https://pvs-studio.com/en/order/open-source-license/">開源專案開發者</a>可免費使用： <pre><code>pvs-studio-analyzer credentials ${USER_NAME} ${LICENSE_KEY}
# 學生、教師免費使用之序號設定方式，每個檔案開頭需加指定註解文字
# https://pvs-studio.com/en/order/for-students/
pvs-studio-analyzer credentials PVS-Studio Free FREE-FREE-FREE-FREE
# 開源專案需填表申請，並在 README.md 加註 Credit 資訊
# https://pvs-studio.com/en/order/open-source-license/
</code></pre> </li> <li>OWASP 資安掃瞄是停用的，要編輯 <code>~/.config/PVS-Studio/Settings.xml</code> 將 <a href="https://pvs-studio.com/en/docs/manual/6653/">DisableOWASPAnalysisCs 選項</a>設成 false： <pre><code>&lt;?xml version="1.0" encoding="utf-8"?&gt;
&lt;ApplicationSettings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema"&gt;
&lt;!-- ... --&gt;
&lt;DisableOWASPAnalysisCs&gt;false&lt;/DisableOWASPAnalysisCs&gt;
&lt;!-- ... --&gt;
&lt;/ApplicationSettings&gt;
</code></pre> </li> <li>與 SonarQube 相比，PVS-Studio 掃瞄 .NET 專案相對簡單，一行指令搞定，結果會寫成 JSON 或 XML： <pre><code>pvs-studio-dotnet -t vunerable-sample.sln -o result.json --progress
</code></pre> </li></ol> <p>但實測 PSV-Studio 的偵測能力不如預期，參考官方文件案例，用以下程式片段測試 PVS-Studio 抓得出 SQL Injection：</p> <pre><code>using System.Data.SqlClient;
var db = new DBHelper();
db.ProcessUserInfo(args[0]);
public class DBHelper
{
string _connectionString = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
public void ProcessUserInfo(String userName)
{
var command = "SELECT * FROM Users WHERE userName = '" + userName + "'";
ExecuteCommand(command);
}
private void ExecuteCommand(String rawCommand)
{
using (SqlConnection connection = new SqlConnection(_connectionString))
{
using (var sqlCommand = new SqlCommand(rawCommand, connection))
{
using (var reader = sqlCommand.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
}
}
</code></pre> <p>但改成以下寫法就抓不到了：</p> <pre><code>using System.Data.SqlClient;
Test(args[0]);
void Test(string userName)
{
var cnStr = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
using (var cn = new SqlConnection(cnStr))
{
cn.Open();
var cmd = new SqlCommand("SELECT * FROM Users WHERE userName = '" + userName + "'", cn);
using (var reader = cmd.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
</code></pre> <p>為了對照，我寫了一個混合版，同時用 PVS-Studio 與 SonarQube 社群版掃瞄，比較結果。</p> <p>PVS-Studio 只抓到 DbHelper.ProcessUserInfo() 的 SQL Injection (Line 31)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638430402768822402.png"></p> <p>SonarQube 社群版抓到寫死密碼及 Test() 的 SQL Injection，但遺漏了 DbHelper.ProcessUserInfo()：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638430402773642493.png"></p> <p>順便再測了 Github Copilot，表現讓人驚豔，兩處 SQL Injection 跟寫死密碼都有被抓出來：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638430402777722712.png"></p> <p>綜合以上的簡單體驗，PVS-Studio 安裝及批次掃瞄夠簡便，但在簡單 C# SQL Injection 漏洞程式碼測試表現比 SonarQube 社群版(CE)弱些(有可能 PVS-Studio 的強項是 C/C++)，但意外有抓到 SonarQube CE 未檢測到的項目，個人覺得若要使用，<a href="https://pvs-studio.com/en/docs/manual/0037/">整合作為 SonarQube 擴充套件</a>，與 SonarQube 互補盲點是較好的做法。</p> <p data-slug="pvs-studio-survey" data-title-en="SAST tool PVS-Studio survey" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2016/06/17/18/45/chile-1463830_960_720.jpg#960#720">Simple test of SAST tool - PVS-Studio.</p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://pvs-studio.com/en/">PVS-Studio</a> 是一套源碼檢測軟體(小知識：專業術語叫 SCA，(<a href="https://zh.wikipedia.org/zh-tw/%E9%9D%9C%E6%85%8B%E7%A8%8B%E5%BA%8F%E5%88%86%E6%9E%90">Static Code Analysis 靜態程式分析</a>)，知名度遠不及 Fortify、Checkmarx 或 SonarQube，不過，PVS-Studio 專注於 C、C#、C++、Java 四種語言，且有給學生及開源專案免費授權方案，好像也值得試看看。以下是我在 Linux 上安裝並掃瞄 .NET 專案的筆記。</p> <p>官方的安裝及使用說明很清楚，下載及執行方式設計得蠻簡便，下面我這次主要參考的文件：</p> <ul><li><a href="https://pvs-studio.com/en/docs/manual/0051/">PVS-Studio C# installation on Linux and macOS</a></li> <li><a href="https://pvs-studio.com/en/docs/manual/0035/">Analyzing Visual Studio / MSBuild / .NET projects from the command line using PVS-Studio</a></li></ul> <p>安裝設定摘要：(我的測試作業系統為 Ubuntu 22.04)</p> <ol><li>需先<a href="https://learn.microsoft.com/en-us/dotnet/core/install/linux?WT.mc_id=DOP-MVP-37580">安裝 .NET 8 SDK</a></li> <li>PVS-Studio C# Analyzer 依賴 PVS-Studio C++ Analyzer (pvs-studio)，但有包成 apt 套件會一併下載安裝，基本上可無腦完成<br> 執行以下指令新增 apt 套件來源及安裝 <pre><code>wget -q -O - https://cdn.pvs-studio.com/etc/pubkey.txt | \
sudo apt-key add -
sudo wget -O /etc/apt/sources.list.d/viva64.list \
https://cdn.pvs-studio.com/etc/viva64.list
sudo apt-get update
sudo apt-get install pvs-studio-dotnet
# 若看到版號代表安裝完成
pvs-studio-dotnet --version
</code></pre> </li> <li>輸入授權序號啟用，授權資料會寫入 <code>~/.config/PVS-Studio/PVS-Studio.lic</code>，<a href="https://pvs-studio.com/en/order/for-students/">學生、教師</a>及<a href="https://pvs-studio.com/en/order/open-source-license/">開源專案開發者</a>可免費使用： <pre><code>pvs-studio-analyzer credentials ${USER_NAME} ${LICENSE_KEY}
# 學生、教師免費使用之序號設定方式，每個檔案開頭需加指定註解文字
# https://pvs-studio.com/en/order/for-students/
pvs-studio-analyzer credentials PVS-Studio Free FREE-FREE-FREE-FREE
# 開源專案需填表申請，並在 README.md 加註 Credit 資訊
# https://pvs-studio.com/en/order/open-source-license/
</code></pre> </li> <li>OWASP 資安掃瞄是停用的，要編輯 <code>~/.config/PVS-Studio/Settings.xml</code> 將 <a href="https://pvs-studio.com/en/docs/manual/6653/">DisableOWASPAnalysisCs 選項</a>設成 false： <pre><code>&lt;?xml version="1.0" encoding="utf-8"?&gt;
&lt;ApplicationSettings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema"&gt;
&lt;!-- ... --&gt;
&lt;DisableOWASPAnalysisCs&gt;false&lt;/DisableOWASPAnalysisCs&gt;
&lt;!-- ... --&gt;
&lt;/ApplicationSettings&gt;
</code></pre> </li> <li>與 SonarQube 相比，PVS-Studio 掃瞄 .NET 專案相對簡單，一行指令搞定，結果會寫成 JSON 或 XML： <pre><code>pvs-studio-dotnet -t vunerable-sample.sln -o result.json --progress
</code></pre> </li></ol> <p>但實測 PSV-Studio 的偵測能力不如預期，參考官方文件案例，用以下程式片段測試 PVS-Studio 抓得出 SQL Injection：</p> <pre><code>using System.Data.SqlClient;
var db = new DBHelper();
db.ProcessUserInfo(args[0]);
public class DBHelper
{
string _connectionString = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
public void ProcessUserInfo(String userName)
{
var command = "SELECT * FROM Users WHERE userName = '" + userName + "'";
ExecuteCommand(command);
}
private void ExecuteCommand(String rawCommand)
{
using (SqlConnection connection = new SqlConnection(_connectionString))
{
using (var sqlCommand = new SqlCommand(rawCommand, connection))
{
using (var reader = sqlCommand.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
}
}
</code></pre> <p>但改成以下寫法就抓不到了：</p> <pre><code>using System.Data.SqlClient;
Test(args[0]);
void Test(string userName)
{
var cnStr = "Server=myServerAddress;Database=myDataBase;User Id=myUsername;Password=myPassword;";
using (var cn = new SqlConnection(cnStr))
{
cn.Open();
var cmd = new SqlCommand("SELECT * FROM Users WHERE userName = '" + userName + "'", cn);
using (var reader = cmd.ExecuteReader())
{
while (reader.Read())
{
// Process the data
}
}
}
}
</code></pre> <p>為了對照，我寫了一個混合版，同時用 PVS-Studio 與 SonarQube 社群版掃瞄，比較結果。</p> <p>PVS-Studio 只抓到 DbHelper.ProcessUserInfo() 的 SQL Injection (Line 31)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638430402768822402.png"></p> <p>SonarQube 社群版抓到寫死密碼及 Test() 的 SQL Injection，但遺漏了 DbHelper.ProcessUserInfo()：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638430402773642493.png"></p> <p>順便再測了 Github Copilot，表現讓人驚豔，兩處 SQL Injection 跟寫死密碼都有被抓出來：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638430402777722712.png"></p> <p>綜合以上的簡單體驗，PVS-Studio 安裝及批次掃瞄夠簡便，但在簡單 C# SQL Injection 漏洞程式碼測試表現比 SonarQube 社群版(CE)弱些(有可能 PVS-Studio 的強項是 C/C++)，但意外有抓到 SonarQube CE 未檢測到的項目，個人覺得若要使用，<a href="https://pvs-studio.com/en/docs/manual/0037/">整合作為 SonarQube 擴充套件</a>，與 SonarQube 互補盲點是較好的做法。</p> <p data-slug="pvs-studio-survey" data-title-en="SAST tool PVS-Studio survey" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2016/06/17/18/45/chile-1463830_960_720.jpg#960#720">Simple test of SAST tool - PVS-Studio.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig2_638430402768822402.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig2_638430402768822402.png" medium="image"/>
    <pubDate>Fri, 09 Feb 2024 01:50:12 GMT</pubDate>
  </item><item>
    <title><![CDATA[筆記 - SonarQube Docker 安裝與 .NET 6+ 專案掃瞄]]></title>
    <link>https://blog.darkthread.net/blog/sonarqube-docker/</link>
    <itunes:title><![CDATA[筆記 - SonarQube Docker 安裝與 .NET 6+ 專案掃瞄]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://www.sonarsource.com/products/sonarqube/">SonarQube</a> 是一套程式碼品質掃瞄工具，可以分析你的程式的寫法是否存在 Bug、漏洞或<a href="https://zh.wikipedia.org/zh-tw/%E4%BB%A3%E7%A0%81%E5%BC%82%E5%91%B3">不好的寫法(Code Smell)</a>。</p> <p>這篇簡單記錄我如何在 Linux 主機上安裝 SonarQube Docker，並用它在 Linux 掃瞄 .NET 專案。</p> <p>安裝步驟我主要參考這篇：<a href="https://learningsky.io/building-sonarqube-community-edition-on-ubuntu-using-docker/">於 Ubuntu 透過 Docker 建置 SonarQube Community Edition by Archer</a>，記錄重點如下：</p> <ol><li>調整 Linux 作業系統<a href="https://docs.sonarsource.com/sonarqube/9.9/requirements/prerequisites-and-overview/#linux">記憶體及檔案參數</a><br> <code>vm.max_map_count</code> 要大於 524288、<code>fs.file-max</code> 大於 131072</li> <li>docker-compose.yml 如下： <pre><code>version: "3"
services:
sonarqube:
image: sonarqube:community
depends_on:
- db
environment:
SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonar
SONAR_JDBC_USERNAME: sonar
SONAR_JDBC_PASSWORD: sonar
volumes:
- sonarqube_data:/opt/sonarqube/data
- sonarqube_extensions:/opt/sonarqube/extensions
- sonarqube_logs:/opt/sonarqube/logs
ports:
- "9000:9000"
db:
image: postgres:12
environment:
POSTGRES_USER: sonar
POSTGRES_PASSWORD: sonar
volumes:
- postgresql:/var/lib/postgresql
- postgresql_data:/var/lib/postgresql/data
volumes:
sonarqube_data:
sonarqube_extensions:
sonarqube_logs:
postgresql:
postgresql_data:
</code></pre> </li></ol> <p>之後開啟 <code>http://localhost:9000</code> 用 admin/admin 登入並修改密碼，即可開始使用 SonarQube。</p> <p>SonarQube 的最佳用法是串接 Azure DevOps、Github... 等版控系統，每次 Commit 後進行掃瞄實現 CI/CD。新建專案介面會提示你設登入版控系統：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429815938407673.png"></p> <p>但我是掃瞄本地專案，故要下載安裝 SonarScanner CLI。Linux 下載路徑可由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner/">官方網站</a>取得，使用 curl 或 wget 下載 ZIP 檔並解壓縮，將檔案移至 opt 目錄，在 .bashrc 設定 $PATH 方便從任意目錄執行：參考 <a href="https://pccicblog.wordpress.com/2021/12/14/sonarqube-%E7%A8%8B%E5%BC%8F%E7%A2%BC%E5%93%81%E8%B3%AA%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8%E6%95%99%E5%AD%B8/">SonarQube 程式碼品質分析工具使用教學 by 張志合</a></p> <pre><code># 下載 zip
wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
# 解壓縮
unzip sonar-scanner-cli-5.0.1.3006-linux.zip
# 將 SonarScanner 資料夾搬到 /opt/ 目錄
sudo mv sonar-scanner-5.0.1.3006-linux /opt/
# 將 SonarScanner 資料夾加入程式搜尋路徑
echo 'export PATH=$PATH:/opt/sonar-scanner-5.0.1.3006-linux/bin' &gt;&gt; ~/.bashrc
# 重載生效
source ~/.bashrc
# 檢查是否可執行 SonarScanner
sonar-scanner -h
</code></pre> <p>執行 SonarScanner CLI 時需指定 SonarQuber 伺服器 URL 及 Access Token，若想簡化可修改 /opt/sonar-scanner-5.0.1.3006-linux/conf/sonar-scanner.properties 加入以下設定：</p> <pre><code>#----- Default SonarQube server
sonar.host.url=http://localhost:9000
</code></pre> <p>Access Token 有<a href="https://docs.sonarsource.com/sonarqube/latest/user-guide/user-account/generating-and-using-tokens/">三種</a>，User Token (用於存取 WebAPI，多配合 IntelliJ、VS、VSCode、Eclipse 使用)、Project Analysis Token (範圍只限該專案，較安全) 以及 Global Analysis Token (可分析任何專案，適合批次或服務)。若不想每次掃瞄傳 Access Token 參數，可將其設成環境變數 <code>export SONAR_TOKEN="sqa_85d63b6c71d9325e5bef822e8db32316948726a1"</code>。</p> <p>使用 sonar-scanner 掃瞄 .NET 8 專案，故意用有問題的寫法卻掃不出來，健檢一切正常。摸索一陣子才發現我搞錯了，.NET 專案不能直接用 SonarScanner CLI 掃，需另外透過專用工具 - <a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">SonarScanner for .NET</a> 間接呼叫。(線索在 sonar-scanner 輸出訊息，有條 <code>INFO: Sensor C# [csharp] WARN: Your project contains C# files which cannot be analyzed with the scanner you are using. To analyze C# or VB.NET, you must use the SonarScanner for .NET 5.x or higher, see https://redirect.sonarsource.com/doc/install-configure-scanner-msbuild.html</code>，從掃瞄結果完全看不出來，不過是我沒耐性把文件看過一遍，算我的責任失分)</p> <p>SonarScanner for .NET 要分別安裝 .NET Core Global Tool 及 SonarScanner 程式。.NET Core Global Tool 使用 dotnet 指令安裝 <code>dotnet tool install --global dotnet-sonarscanner --version 6.1.0</code>，SonarScanner 程式則需由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">網站</a>下載，</p> <pre><code>sudo unzip sonar-scanner-6.1.0.83647-net.zip -d /opt/sonar-scanner-6.1.0.83647-net
sudo chmod +x /opt/sonar-scanner-6.1.0.83647-net/sonar-scanner-5.0.1.3006/bin/*
echo 'export PATH=$PATH:/opt/sonar-scanner-6.1.0.83647-net' &gt;&gt; ~/.bashrc
source ~/.bashrc
# 需要 Java Runtime，default-jre 不行，要裝 55+ 版本
sudo apt install -y openjdk-18-jre-headless
</code></pre> <p>.NET 版的 SonarQube 網址及 Access Token 設定位置在 /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml，這樣可不用每次都帶 Access Token 參數。</p> <pre><code>&lt;SonarQubeAnalysisProperties  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns="http://www.sonarsource.com/msbuild/integration/2015/1"&gt;
&lt;Property Name="sonar.host.url"&gt;http://localhost:9000&lt;/Property&gt;
&lt;Property Name="sonar.token"&gt;sqa_85...a1&lt;/Property&gt;
&lt;/SonarQubeAnalysisProperties&gt;
</code></pre> <p>.NET 掃瞄程序較麻煩，要先 dotnet sonarscanner begin、dotnet build，再 dotnet sonarscanner end，官方範例如下：</p> <pre><code>dotnet sonarscanner begin /k:"project-key" /d:sonar.token="&lt;token&gt;"
dotnet build &lt;path to project file or .sln file&gt; --no-incremental
dotnet sonarscanner end /d:sonar.token="&lt;token&gt;"
</code></pre> <p>我有在 SonarQube.Analysis.xml 設定 sonar.token，理論上會自動抓設定檔，但實測不 OK 得指定設定檔路徑 <code>/s:&lt;path-to-onarQube.Analysis.xml&gt;</code> 才會生效。 <a href="https://community.sonarsource.com/t/sonarqube-analysis-xml-being-ignored/45087/3">參考</a></p> <p>我的解法是用 <code>ln -s /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml /home/jeffrey/sqconf.xml</code> 將檔案連結到 ~/sqconf.xml，然後將以下指令存成 sca.sh：</p> <pre><code>#!/bin/bash
dotnet sonarscanner begin /k:my:vulerable-sample /s:"$HOME/sqconf.xml"
dotnet build --no-incremental
dotnet sonarscanner end
</code></pre> <p>這樣就能成功掃瞄囉~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429815943182578.png"></p> <p>掃瞄結果有抓到 SQL Injection 問題點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429815945978605.png"></p> <p>歷經波折，再學會一件新工具。</p> <p data-slug="sonarqube-docker" data-title-en="Using SonarQube docker to scan .NET 6+ project" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/289927/pexels-photo-289927.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">Tutorial of installing Sonar docker and using it to scan .NET 6+ projects.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://www.sonarsource.com/products/sonarqube/">SonarQube</a> 是一套程式碼品質掃瞄工具，可以分析你的程式的寫法是否存在 Bug、漏洞或<a href="https://zh.wikipedia.org/zh-tw/%E4%BB%A3%E7%A0%81%E5%BC%82%E5%91%B3">不好的寫法(Code Smell)</a>。</p> <p>這篇簡單記錄我如何在 Linux 主機上安裝 SonarQube Docker，並用它在 Linux 掃瞄 .NET 專案。</p> <p>安裝步驟我主要參考這篇：<a href="https://learningsky.io/building-sonarqube-community-edition-on-ubuntu-using-docker/">於 Ubuntu 透過 Docker 建置 SonarQube Community Edition by Archer</a>，記錄重點如下：</p> <ol><li>調整 Linux 作業系統<a href="https://docs.sonarsource.com/sonarqube/9.9/requirements/prerequisites-and-overview/#linux">記憶體及檔案參數</a><br> <code>vm.max_map_count</code> 要大於 524288、<code>fs.file-max</code> 大於 131072</li> <li>docker-compose.yml 如下： <pre><code>version: "3"
services:
sonarqube:
image: sonarqube:community
depends_on:
- db
environment:
SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonar
SONAR_JDBC_USERNAME: sonar
SONAR_JDBC_PASSWORD: sonar
volumes:
- sonarqube_data:/opt/sonarqube/data
- sonarqube_extensions:/opt/sonarqube/extensions
- sonarqube_logs:/opt/sonarqube/logs
ports:
- "9000:9000"
db:
image: postgres:12
environment:
POSTGRES_USER: sonar
POSTGRES_PASSWORD: sonar
volumes:
- postgresql:/var/lib/postgresql
- postgresql_data:/var/lib/postgresql/data
volumes:
sonarqube_data:
sonarqube_extensions:
sonarqube_logs:
postgresql:
postgresql_data:
</code></pre> </li></ol> <p>之後開啟 <code>http://localhost:9000</code> 用 admin/admin 登入並修改密碼，即可開始使用 SonarQube。</p> <p>SonarQube 的最佳用法是串接 Azure DevOps、Github... 等版控系統，每次 Commit 後進行掃瞄實現 CI/CD。新建專案介面會提示你設登入版控系統：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429815938407673.png"></p> <p>但我是掃瞄本地專案，故要下載安裝 SonarScanner CLI。Linux 下載路徑可由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner/">官方網站</a>取得，使用 curl 或 wget 下載 ZIP 檔並解壓縮，將檔案移至 opt 目錄，在 .bashrc 設定 $PATH 方便從任意目錄執行：參考 <a href="https://pccicblog.wordpress.com/2021/12/14/sonarqube-%E7%A8%8B%E5%BC%8F%E7%A2%BC%E5%93%81%E8%B3%AA%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8%E6%95%99%E5%AD%B8/">SonarQube 程式碼品質分析工具使用教學 by 張志合</a></p> <pre><code># 下載 zip
wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
# 解壓縮
unzip sonar-scanner-cli-5.0.1.3006-linux.zip
# 將 SonarScanner 資料夾搬到 /opt/ 目錄
sudo mv sonar-scanner-5.0.1.3006-linux /opt/
# 將 SonarScanner 資料夾加入程式搜尋路徑
echo 'export PATH=$PATH:/opt/sonar-scanner-5.0.1.3006-linux/bin' &gt;&gt; ~/.bashrc
# 重載生效
source ~/.bashrc
# 檢查是否可執行 SonarScanner
sonar-scanner -h
</code></pre> <p>執行 SonarScanner CLI 時需指定 SonarQuber 伺服器 URL 及 Access Token，若想簡化可修改 /opt/sonar-scanner-5.0.1.3006-linux/conf/sonar-scanner.properties 加入以下設定：</p> <pre><code>#----- Default SonarQube server
sonar.host.url=http://localhost:9000
</code></pre> <p>Access Token 有<a href="https://docs.sonarsource.com/sonarqube/latest/user-guide/user-account/generating-and-using-tokens/">三種</a>，User Token (用於存取 WebAPI，多配合 IntelliJ、VS、VSCode、Eclipse 使用)、Project Analysis Token (範圍只限該專案，較安全) 以及 Global Analysis Token (可分析任何專案，適合批次或服務)。若不想每次掃瞄傳 Access Token 參數，可將其設成環境變數 <code>export SONAR_TOKEN="sqa_85d63b6c71d9325e5bef822e8db32316948726a1"</code>。</p> <p>使用 sonar-scanner 掃瞄 .NET 8 專案，故意用有問題的寫法卻掃不出來，健檢一切正常。摸索一陣子才發現我搞錯了，.NET 專案不能直接用 SonarScanner CLI 掃，需另外透過專用工具 - <a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">SonarScanner for .NET</a> 間接呼叫。(線索在 sonar-scanner 輸出訊息，有條 <code>INFO: Sensor C# [csharp] WARN: Your project contains C# files which cannot be analyzed with the scanner you are using. To analyze C# or VB.NET, you must use the SonarScanner for .NET 5.x or higher, see https://redirect.sonarsource.com/doc/install-configure-scanner-msbuild.html</code>，從掃瞄結果完全看不出來，不過是我沒耐性把文件看過一遍，算我的責任失分)</p> <p>SonarScanner for .NET 要分別安裝 .NET Core Global Tool 及 SonarScanner 程式。.NET Core Global Tool 使用 dotnet 指令安裝 <code>dotnet tool install --global dotnet-sonarscanner --version 6.1.0</code>，SonarScanner 程式則需由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">網站</a>下載，</p> <pre><code>sudo unzip sonar-scanner-6.1.0.83647-net.zip -d /opt/sonar-scanner-6.1.0.83647-net
sudo chmod +x /opt/sonar-scanner-6.1.0.83647-net/sonar-scanner-5.0.1.3006/bin/*
echo 'export PATH=$PATH:/opt/sonar-scanner-6.1.0.83647-net' &gt;&gt; ~/.bashrc
source ~/.bashrc
# 需要 Java Runtime，default-jre 不行，要裝 55+ 版本
sudo apt install -y openjdk-18-jre-headless
</code></pre> <p>.NET 版的 SonarQube 網址及 Access Token 設定位置在 /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml，這樣可不用每次都帶 Access Token 參數。</p> <pre><code>&lt;SonarQubeAnalysisProperties  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns="http://www.sonarsource.com/msbuild/integration/2015/1"&gt;
&lt;Property Name="sonar.host.url"&gt;http://localhost:9000&lt;/Property&gt;
&lt;Property Name="sonar.token"&gt;sqa_85...a1&lt;/Property&gt;
&lt;/SonarQubeAnalysisProperties&gt;
</code></pre> <p>.NET 掃瞄程序較麻煩，要先 dotnet sonarscanner begin、dotnet build，再 dotnet sonarscanner end，官方範例如下：</p> <pre><code>dotnet sonarscanner begin /k:"project-key" /d:sonar.token="&lt;token&gt;"
dotnet build &lt;path to project file or .sln file&gt; --no-incremental
dotnet sonarscanner end /d:sonar.token="&lt;token&gt;"
</code></pre> <p>我有在 SonarQube.Analysis.xml 設定 sonar.token，理論上會自動抓設定檔，但實測不 OK 得指定設定檔路徑 <code>/s:&lt;path-to-onarQube.Analysis.xml&gt;</code> 才會生效。 <a href="https://community.sonarsource.com/t/sonarqube-analysis-xml-being-ignored/45087/3">參考</a></p> <p>我的解法是用 <code>ln -s /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml /home/jeffrey/sqconf.xml</code> 將檔案連結到 ~/sqconf.xml，然後將以下指令存成 sca.sh：</p> <pre><code>#!/bin/bash
dotnet sonarscanner begin /k:my:vulerable-sample /s:"$HOME/sqconf.xml"
dotnet build --no-incremental
dotnet sonarscanner end
</code></pre> <p>這樣就能成功掃瞄囉~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429815943182578.png"></p> <p>掃瞄結果有抓到 SQL Injection 問題點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429815945978605.png"></p> <p>歷經波折，再學會一件新工具。</p> <p data-slug="sonarqube-docker" data-title-en="Using SonarQube docker to scan .NET 6+ project" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/289927/pexels-photo-289927.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">Tutorial of installing Sonar docker and using it to scan .NET 6+ projects.</p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://www.sonarsource.com/products/sonarqube/">SonarQube</a> 是一套程式碼品質掃瞄工具，可以分析你的程式的寫法是否存在 Bug、漏洞或<a href="https://zh.wikipedia.org/zh-tw/%E4%BB%A3%E7%A0%81%E5%BC%82%E5%91%B3">不好的寫法(Code Smell)</a>。</p> <p>這篇簡單記錄我如何在 Linux 主機上安裝 SonarQube Docker，並用它在 Linux 掃瞄 .NET 專案。</p> <p>安裝步驟我主要參考這篇：<a href="https://learningsky.io/building-sonarqube-community-edition-on-ubuntu-using-docker/">於 Ubuntu 透過 Docker 建置 SonarQube Community Edition by Archer</a>，記錄重點如下：</p> <ol><li>調整 Linux 作業系統<a href="https://docs.sonarsource.com/sonarqube/9.9/requirements/prerequisites-and-overview/#linux">記憶體及檔案參數</a><br> <code>vm.max_map_count</code> 要大於 524288、<code>fs.file-max</code> 大於 131072</li> <li>docker-compose.yml 如下： <pre><code>version: "3"
services:
sonarqube:
image: sonarqube:community
depends_on:
- db
environment:
SONAR_JDBC_URL: jdbc:postgresql://db:5432/sonar
SONAR_JDBC_USERNAME: sonar
SONAR_JDBC_PASSWORD: sonar
volumes:
- sonarqube_data:/opt/sonarqube/data
- sonarqube_extensions:/opt/sonarqube/extensions
- sonarqube_logs:/opt/sonarqube/logs
ports:
- "9000:9000"
db:
image: postgres:12
environment:
POSTGRES_USER: sonar
POSTGRES_PASSWORD: sonar
volumes:
- postgresql:/var/lib/postgresql
- postgresql_data:/var/lib/postgresql/data
volumes:
sonarqube_data:
sonarqube_extensions:
sonarqube_logs:
postgresql:
postgresql_data:
</code></pre> </li></ol> <p>之後開啟 <code>http://localhost:9000</code> 用 admin/admin 登入並修改密碼，即可開始使用 SonarQube。</p> <p>SonarQube 的最佳用法是串接 Azure DevOps、Github... 等版控系統，每次 Commit 後進行掃瞄實現 CI/CD。新建專案介面會提示你設登入版控系統：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429815938407673.png"></p> <p>但我是掃瞄本地專案，故要下載安裝 SonarScanner CLI。Linux 下載路徑可由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner/">官方網站</a>取得，使用 curl 或 wget 下載 ZIP 檔並解壓縮，將檔案移至 opt 目錄，在 .bashrc 設定 $PATH 方便從任意目錄執行：參考 <a href="https://pccicblog.wordpress.com/2021/12/14/sonarqube-%E7%A8%8B%E5%BC%8F%E7%A2%BC%E5%93%81%E8%B3%AA%E5%88%86%E6%9E%90%E5%B7%A5%E5%85%B7%E4%BD%BF%E7%94%A8%E6%95%99%E5%AD%B8/">SonarQube 程式碼品質分析工具使用教學 by 張志合</a></p> <pre><code># 下載 zip
wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
# 解壓縮
unzip sonar-scanner-cli-5.0.1.3006-linux.zip
# 將 SonarScanner 資料夾搬到 /opt/ 目錄
sudo mv sonar-scanner-5.0.1.3006-linux /opt/
# 將 SonarScanner 資料夾加入程式搜尋路徑
echo 'export PATH=$PATH:/opt/sonar-scanner-5.0.1.3006-linux/bin' &gt;&gt; ~/.bashrc
# 重載生效
source ~/.bashrc
# 檢查是否可執行 SonarScanner
sonar-scanner -h
</code></pre> <p>執行 SonarScanner CLI 時需指定 SonarQuber 伺服器 URL 及 Access Token，若想簡化可修改 /opt/sonar-scanner-5.0.1.3006-linux/conf/sonar-scanner.properties 加入以下設定：</p> <pre><code>#----- Default SonarQube server
sonar.host.url=http://localhost:9000
</code></pre> <p>Access Token 有<a href="https://docs.sonarsource.com/sonarqube/latest/user-guide/user-account/generating-and-using-tokens/">三種</a>，User Token (用於存取 WebAPI，多配合 IntelliJ、VS、VSCode、Eclipse 使用)、Project Analysis Token (範圍只限該專案，較安全) 以及 Global Analysis Token (可分析任何專案，適合批次或服務)。若不想每次掃瞄傳 Access Token 參數，可將其設成環境變數 <code>export SONAR_TOKEN="sqa_85d63b6c71d9325e5bef822e8db32316948726a1"</code>。</p> <p>使用 sonar-scanner 掃瞄 .NET 8 專案，故意用有問題的寫法卻掃不出來，健檢一切正常。摸索一陣子才發現我搞錯了，.NET 專案不能直接用 SonarScanner CLI 掃，需另外透過專用工具 - <a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">SonarScanner for .NET</a> 間接呼叫。(線索在 sonar-scanner 輸出訊息，有條 <code>INFO: Sensor C# [csharp] WARN: Your project contains C# files which cannot be analyzed with the scanner you are using. To analyze C# or VB.NET, you must use the SonarScanner for .NET 5.x or higher, see https://redirect.sonarsource.com/doc/install-configure-scanner-msbuild.html</code>，從掃瞄結果完全看不出來，不過是我沒耐性把文件看過一遍，算我的責任失分)</p> <p>SonarScanner for .NET 要分別安裝 .NET Core Global Tool 及 SonarScanner 程式。.NET Core Global Tool 使用 dotnet 指令安裝 <code>dotnet tool install --global dotnet-sonarscanner --version 6.1.0</code>，SonarScanner 程式則需由<a href="https://docs.sonarsource.com/sonarqube/latest/analyzing-source-code/scanners/sonarscanner-for-dotnet/">網站</a>下載，</p> <pre><code>sudo unzip sonar-scanner-6.1.0.83647-net.zip -d /opt/sonar-scanner-6.1.0.83647-net
sudo chmod +x /opt/sonar-scanner-6.1.0.83647-net/sonar-scanner-5.0.1.3006/bin/*
echo 'export PATH=$PATH:/opt/sonar-scanner-6.1.0.83647-net' &gt;&gt; ~/.bashrc
source ~/.bashrc
# 需要 Java Runtime，default-jre 不行，要裝 55+ 版本
sudo apt install -y openjdk-18-jre-headless
</code></pre> <p>.NET 版的 SonarQube 網址及 Access Token 設定位置在 /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml，這樣可不用每次都帶 Access Token 參數。</p> <pre><code>&lt;SonarQubeAnalysisProperties  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns="http://www.sonarsource.com/msbuild/integration/2015/1"&gt;
&lt;Property Name="sonar.host.url"&gt;http://localhost:9000&lt;/Property&gt;
&lt;Property Name="sonar.token"&gt;sqa_85...a1&lt;/Property&gt;
&lt;/SonarQubeAnalysisProperties&gt;
</code></pre> <p>.NET 掃瞄程序較麻煩，要先 dotnet sonarscanner begin、dotnet build，再 dotnet sonarscanner end，官方範例如下：</p> <pre><code>dotnet sonarscanner begin /k:"project-key" /d:sonar.token="&lt;token&gt;"
dotnet build &lt;path to project file or .sln file&gt; --no-incremental
dotnet sonarscanner end /d:sonar.token="&lt;token&gt;"
</code></pre> <p>我有在 SonarQube.Analysis.xml 設定 sonar.token，理論上會自動抓設定檔，但實測不 OK 得指定設定檔路徑 <code>/s:&lt;path-to-onarQube.Analysis.xml&gt;</code> 才會生效。 <a href="https://community.sonarsource.com/t/sonarqube-analysis-xml-being-ignored/45087/3">參考</a></p> <p>我的解法是用 <code>ln -s /opt/sonar-scanner-6.1.0.83647-net/SonarQube.Analysis.xml /home/jeffrey/sqconf.xml</code> 將檔案連結到 ~/sqconf.xml，然後將以下指令存成 sca.sh：</p> <pre><code>#!/bin/bash
dotnet sonarscanner begin /k:my:vulerable-sample /s:"$HOME/sqconf.xml"
dotnet build --no-incremental
dotnet sonarscanner end
</code></pre> <p>這樣就能成功掃瞄囉~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429815943182578.png"></p> <p>掃瞄結果有抓到 SQL Injection 問題點：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429815945978605.png"></p> <p>歷經波折，再學會一件新工具。</p> <p data-slug="sonarqube-docker" data-title-en="Using SonarQube docker to scan .NET 6+ project" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/289927/pexels-photo-289927.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">Tutorial of installing Sonar docker and using it to scan .NET 6+ projects.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638429815938407673.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638429815938407673.png" medium="image"/>
    <pubDate>Thu, 08 Feb 2024 09:21:49 GMT</pubDate>
  </item><item>
    <title><![CDATA[使用 VSCode 完美遠端開發與測試 Linux 專案]]></title>
    <link>https://blog.darkthread.net/blog/vscode-remote-ssh/</link>
    <itunes:title><![CDATA[使用 VSCode 完美遠端開發與測試 Linux 專案]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>前天分享完<a href="/blog/jupyter-on-remote-linux/">在遠端 Linux 執行 Jupyter Notebook</a>，許多讀者提到用 Remote SSH 能直接開啟在 Linux 上的專案並在 Linux 執行及測試，強力推薦我更方便的做法 - Remote SSH。</p> <p>透過 Remote SSH，VSCode 可開啟 Linux 主機上的專案，F5 偵錯也是在 Linux 執行，VSCode Terminal 變身 SSH 終端介面，一整個把 Linux 當自己家，如此無縫整合的體驗，完全符合 Visual Studio 的一貫風格，聽起來超棒!</p> <p>那還等什麼，就來試試吧!</p> <p>第一步要先安裝「Remote - SSH」擴充套件：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429200597290312.png"></p> <p>Ctrl-Shift-P 輸入 remote ssh，找到 Remote-SSH: Connect to Host...</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638429200599475234.png"></p> <p>若從沒連過 SSH 主機，按 Add New SSH Host... 設定：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429200601622906.png"></p> <p>輸入「帳號@主機名稱或 IP」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429200603726881.png"></p> <p>VSCode 將儲存主機設定並問你要不要連線：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638429200605895299.png"></p> <p>連線前需選擇遠端主機平台：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638429200608025655.png"></p> <p>輸入密碼連線後，即可開啟 Linux 端資料夾，修改專案並在 Linux 執行，Terminal 出現的也 Linux 的 Bash，測試 Environment.OSVersion 傳回 Unix 6.2.0.1019，證明是在 Linux 執行無誤：</p> <p><a href="https://www.youtube.com/watch?v=E867UHoI-Hs">VSCode Remote SSH 示範</a></p> <p>但仔細看卻個問題 - 魔法使 Github Copilot 怎麼不見了？</p> <p>開啟擴充套件清單找到 Copliot，VSCode 貼心提醒遠端需要安裝 <a href="https://code.visualstudio.com/api/advanced-topics/remote-extensions#architecture-and-extension-types">Remote Extension Host</a> 才能正常使用 Copilot。Remote SSH 背後原理有點小複雜，遠端主機需執行 Workspace Extension Host，其中的 Workspace Extension 負責存取檔案、支援偵錯，以及執行多檔案的複雜作業... 等等，與 UI Extension 溝通完成各項功能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638429200610264858.png"></p> <p>不過，複雜的部分 VSCode 都做掉了，我們只需點 「Install in SSH:&lt;host-name&gt;」無腦安裝即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638429200612512980.png"></p> <p>就醬，Github Copilot 回來了~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638429200614683711.png"></p> <p>同樣的，如果用 F5 偵錯將彈出安裝 C# 擴充套件的提示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638429200616910728.png"></p> <p>狀況與 Github Copilot 類似，也是要在遠端安裝相關元件，但一樣都是無腦安裝：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638429200619246161.png"></p> <p>安裝後重啟 VSCode，逐行偵錯、觀察變數、Debug Console 互動操作一樣都沒少，操作體驗滿分。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig12_638429200621465204.png"></p> <p>到了久違的呼口號時間：</p> <h4>VSCode 好威呀~~</h4> <p data-slug="vscode-remote-ssh" data-title-en="VSCode Remote SSH" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/05/08/02/22/game-2294201_1280.jpg#1280#720">Introduction to the Remote SSH function of VSCode and tips to enable Copilot and debugging features for remote development.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>前天分享完<a href="/blog/jupyter-on-remote-linux/">在遠端 Linux 執行 Jupyter Notebook</a>，許多讀者提到用 Remote SSH 能直接開啟在 Linux 上的專案並在 Linux 執行及測試，強力推薦我更方便的做法 - Remote SSH。</p> <p>透過 Remote SSH，VSCode 可開啟 Linux 主機上的專案，F5 偵錯也是在 Linux 執行，VSCode Terminal 變身 SSH 終端介面，一整個把 Linux 當自己家，如此無縫整合的體驗，完全符合 Visual Studio 的一貫風格，聽起來超棒!</p> <p>那還等什麼，就來試試吧!</p> <p>第一步要先安裝「Remote - SSH」擴充套件：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429200597290312.png"></p> <p>Ctrl-Shift-P 輸入 remote ssh，找到 Remote-SSH: Connect to Host...</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638429200599475234.png"></p> <p>若從沒連過 SSH 主機，按 Add New SSH Host... 設定：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429200601622906.png"></p> <p>輸入「帳號@主機名稱或 IP」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429200603726881.png"></p> <p>VSCode 將儲存主機設定並問你要不要連線：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638429200605895299.png"></p> <p>連線前需選擇遠端主機平台：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638429200608025655.png"></p> <p>輸入密碼連線後，即可開啟 Linux 端資料夾，修改專案並在 Linux 執行，Terminal 出現的也 Linux 的 Bash，測試 Environment.OSVersion 傳回 Unix 6.2.0.1019，證明是在 Linux 執行無誤：</p> <p><a href="https://www.youtube.com/watch?v=E867UHoI-Hs">VSCode Remote SSH 示範</a></p> <p>但仔細看卻個問題 - 魔法使 Github Copilot 怎麼不見了？</p> <p>開啟擴充套件清單找到 Copliot，VSCode 貼心提醒遠端需要安裝 <a href="https://code.visualstudio.com/api/advanced-topics/remote-extensions#architecture-and-extension-types">Remote Extension Host</a> 才能正常使用 Copilot。Remote SSH 背後原理有點小複雜，遠端主機需執行 Workspace Extension Host，其中的 Workspace Extension 負責存取檔案、支援偵錯，以及執行多檔案的複雜作業... 等等，與 UI Extension 溝通完成各項功能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638429200610264858.png"></p> <p>不過，複雜的部分 VSCode 都做掉了，我們只需點 「Install in SSH:&lt;host-name&gt;」無腦安裝即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638429200612512980.png"></p> <p>就醬，Github Copilot 回來了~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638429200614683711.png"></p> <p>同樣的，如果用 F5 偵錯將彈出安裝 C# 擴充套件的提示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638429200616910728.png"></p> <p>狀況與 Github Copilot 類似，也是要在遠端安裝相關元件，但一樣都是無腦安裝：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638429200619246161.png"></p> <p>安裝後重啟 VSCode，逐行偵錯、觀察變數、Debug Console 互動操作一樣都沒少，操作體驗滿分。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig12_638429200621465204.png"></p> <p>到了久違的呼口號時間：</p> <h4>VSCode 好威呀~~</h4> <p data-slug="vscode-remote-ssh" data-title-en="VSCode Remote SSH" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/05/08/02/22/game-2294201_1280.jpg#1280#720">Introduction to the Remote SSH function of VSCode and tips to enable Copilot and debugging features for remote development.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>前天分享完<a href="/blog/jupyter-on-remote-linux/">在遠端 Linux 執行 Jupyter Notebook</a>，許多讀者提到用 Remote SSH 能直接開啟在 Linux 上的專案並在 Linux 執行及測試，強力推薦我更方便的做法 - Remote SSH。</p> <p>透過 Remote SSH，VSCode 可開啟 Linux 主機上的專案，F5 偵錯也是在 Linux 執行，VSCode Terminal 變身 SSH 終端介面，一整個把 Linux 當自己家，如此無縫整合的體驗，完全符合 Visual Studio 的一貫風格，聽起來超棒!</p> <p>那還等什麼，就來試試吧!</p> <p>第一步要先安裝「Remote - SSH」擴充套件：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638429200597290312.png"></p> <p>Ctrl-Shift-P 輸入 remote ssh，找到 Remote-SSH: Connect to Host...</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638429200599475234.png"></p> <p>若從沒連過 SSH 主機，按 Add New SSH Host... 設定：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638429200601622906.png"></p> <p>輸入「帳號@主機名稱或 IP」：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638429200603726881.png"></p> <p>VSCode 將儲存主機設定並問你要不要連線：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638429200605895299.png"></p> <p>連線前需選擇遠端主機平台：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638429200608025655.png"></p> <p>輸入密碼連線後，即可開啟 Linux 端資料夾，修改專案並在 Linux 執行，Terminal 出現的也 Linux 的 Bash，測試 Environment.OSVersion 傳回 Unix 6.2.0.1019，證明是在 Linux 執行無誤：</p> <p><a href="https://www.youtube.com/watch?v=E867UHoI-Hs">VSCode Remote SSH 示範</a></p> <p>但仔細看卻個問題 - 魔法使 Github Copilot 怎麼不見了？</p> <p>開啟擴充套件清單找到 Copliot，VSCode 貼心提醒遠端需要安裝 <a href="https://code.visualstudio.com/api/advanced-topics/remote-extensions#architecture-and-extension-types">Remote Extension Host</a> 才能正常使用 Copilot。Remote SSH 背後原理有點小複雜，遠端主機需執行 Workspace Extension Host，其中的 Workspace Extension 負責存取檔案、支援偵錯，以及執行多檔案的複雜作業... 等等，與 UI Extension 溝通完成各項功能：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638429200610264858.png"></p> <p>不過，複雜的部分 VSCode 都做掉了，我們只需點 「Install in SSH:&lt;host-name&gt;」無腦安裝即可：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638429200612512980.png"></p> <p>就醬，Github Copilot 回來了~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638429200614683711.png"></p> <p>同樣的，如果用 F5 偵錯將彈出安裝 C# 擴充套件的提示：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638429200616910728.png"></p> <p>狀況與 Github Copilot 類似，也是要在遠端安裝相關元件，但一樣都是無腦安裝：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638429200619246161.png"></p> <p>安裝後重啟 VSCode，逐行偵錯、觀察變數、Debug Console 互動操作一樣都沒少，操作體驗滿分。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig12_638429200621465204.png"></p> <p>到了久違的呼口號時間：</p> <h4>VSCode 好威呀~~</h4> <p data-slug="vscode-remote-ssh" data-title-en="VSCode Remote SSH" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/05/08/02/22/game-2294201_1280.jpg#1280#720">Introduction to the Remote SSH function of VSCode and tips to enable Copilot and debugging features for remote development.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638429200597290312.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638429200597290312.png" medium="image"/>
    <pubDate>Wed, 07 Feb 2024 16:20:00 GMT</pubDate>
  </item><item>
    <title><![CDATA[AI 閱讀筆記 - 訓練大型語言模型有多燒錢？]]></title>
    <link>https://blog.darkthread.net/blog/how-llm-training-burn-money/</link>
    <itunes:title><![CDATA[AI 閱讀筆記 - 訓練大型語言模型有多燒錢？]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>前幾天研究<a href="/blog/llama-cpp/">電腦沒有獨立顯卡，只靠 CPU 也能跑大型語言模型嗎？</a>學到要跑 LLM 大型語言模型除了 GPU，顯卡記憶體也是關鍵。以 FB 公司 Meta 釋出的 LLaMA 2 模型為例，70B、13B、7B 四種參數規模不同的大小模型，分別需要 320GB、50GB、30GB GPU 記憶體 <a href="https://docs.nvidia.com/ai-enterprise/workflows-generative-ai/0.1.0/sizing-guide.html">參考</a>，透過量化 (Quantization) 技術可以用損失精確度換取記憶體用量減半、1/4 甚至到 1/8，若你能忍受跟吞吞吐吐的機器人聊天，不裝獨顯純用 CPU 跑 LLaMA 2 也不是不行。</p> <p>能力雖然輸給 ChatGPT，但 Meta 公司釋出的開源 LLM 專案 - LLaMA 2，除了可以在自家主機跑可商業應用，還有一大優點就是允許你重新訓練或進行微調(Fine-Tune)將模型變成你想要的形狀，此門一開，開啟了無限可能...</p> <p>不過，訓練與微調 LLM 是很燒錢的，<a href="/blog/rag/">上回談 RAG</a>時提過 OpenAI 模型的微調訓練成本是每小時 34 ~ 103 USD，若我們起心動念想微調一下 LLaMA 2，要如何預估成本，要燒多少錢？</p> <p>心存疑問但不知道去哪找答案，直接無意間查到好東西 - 國家高速網路與計算中心(NCHC，簡稱國網中心)莊朝鈞研究員的簡報 - <a href="https://covid-19.nchc.org.tw/agenda/202311_llms_Handouts_0110.pdf">跟你組織內的知識庫對話</a>，裡面有不少參考數據。</p> <p>依 LLaMA 公開的資訊，7B 版本的訓練時數是 184,320 GPU 小時、70B 為 1,720,320 GPU 小時 (每 GPU 小時為一張 A100 跑一小時)，故 7B 模型的完整訓練，若只用一張 A100 跑的話要跑 21 年! 當然，Meta 是用大量 A100 去跑，7B 花了 27.6 萬 USD、70B 花了 170 萬 USD。</p> <p>至於不同硬體的算力比較，我找到一篇好主章：<a href="https://zhuanlan.zhihu.com/p/655402388">A100/H100 太贵，何不用 4090？ by 李博杰</a></p> <p>A100 跟 4090 算力相差不大，但跑 LLM GPU 記憶體容量跟傳輸頻寬很關鍵。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428658982070858.png"><br> <a href="https://zhuanlan.zhihu.com/p/655402388">圖片來源</a></p> <p>在 PyTorch 訓練測試中，A100 Throughput 是 4090 的 1.4 倍，H100 是 4090 的 1.6 到 2.5 倍：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428658986120475.png"><br> <a href="https://lambdalabs.com/gpu-benchmarks">圖片來源：Deep Learning GPU Benchmarks by Lambda Labs</a></p> <p>至於訓練算力估算可以用 6 * 模型參數量 * 訓練數據 Token 數 (Flops) 預估。</p> <p>而國網中心的算力價格，V100 每 GPU 小時，商用 2.5 USD、國科會 0.5 USD。尋常百姓較容易買到零售算力管道應該是 Google 的 Colab 平台，Colab Pro 會員每月 10.49 USD 每月有 100 個運算單元，Pro+ 會員 52.49 USD 則為 500 個運算單元 <a href="https://colab.research.google.com/signup">方案說明</a>，A100 每 GPU 小時消耗約 13 個運算單元，換算一個 GPU 小時約 1.36 USD。</p> <p>簡報有整理一些 LLaMA 2 模型接續訓練及微調案例(有部分包含成本數據)：</p> <ul><li>中研院繁體中文 LLM 的接續訓練 Continue Pretrainging (CP) 專案 CKIP-Llama-2-7b，用了 5.8G 檔案(中文維基、台灣碩博士論文摘要、中研究漢語平衡語料庫、徐志摩詩歌全集、朱自清散文全集)，20B Token 的資料接續訓練 Llama-2-7b。</li> <li>Meta 用了 27,540 條 (Supervised Fine-Tuning SFT) 高品質註釋示例有效提升品質 - Llama-2-7b-chat</li> <li>Taiwan-LLaMA 是基於 LLaMA 2 的全參數微調模型，強化繁體中文能力，包含預訓練 + 微調<br> Pretraining: 8 x A100 兩週 (LLaMA 2 學中文)<br> Instruction Finetuning: 8 x H100 12 小時</li> <li><a href="https://crfm.stanford.edu/2023/03/13/alpaca.html">Stanford-Alpaca</a> - 用 179 題，174 種不同類型的高品質多樣性 Dataset 微調，可顯著提升效果<br> (高品質特色：Step by Step、詳細解釋、額外知識，1000 條 Instruction Tuning Data 就能明顯提升表現)<br> Self-Instruct：Stanford Alpaca 從 ChatGPT 取得 5 萬 2 千條資料，用 4 x A100 GPU 一天訓練完 7B LLaMA，達到類似 text-daviinc-003 等級<br> 成本：ChatGPT API 費用 = 500 USD；A100 GPU 每小時 1 USD，4 片 * 24 hr 約 100 USD<br> 人工準備 175 產生 Instruction 的任務種子，用 ChatGPT 生成 5 萬筆 Fine-Tune 用的資料(意思是：LLaMA 你看看 ChatGPT 的回答多好，快學學人家)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428658990780164.png"></li> <li><a href="https://zhuanlan.zhihu.com/p/619426866">Alpaca-LoRA</a> 用 LoRA 方式訓練 20 分鐘完成，1 USD<br> 註：LoRA 是種用較少記憶體及計算資源執行 Fine-Tune 的做法，Alpaca-LoRA 用消費級顯卡就能完成</li></ul> <p>有了以上資訊，對於訓練與微調成本大概有點概念，訓練果然是個燒錢的活動(而且往往你得試了又試、試了再試、試了還試，永無止境...)，LoRA 微調可用少量資源達成不錯效果，用 4090 顯卡就有機會實現，但課金買裝備仍免不了燒錢。呵~</p> <p>另外，簡報整理了「何時該用 RAG、何時用 Fine-Tuning？」的評估建議，也很實用：</p> <ul><li>RAG 適用情境：需要外部知識、必須減少幻覺、資料需即時更新、需要附上資料依據</li> <li>Fine-Tuning 適用情境：需自訂模型寫作風格、有訓練資料可用</li></ul> <p data-slug="how-llm-training-burn-money" data-fbimg="https://cdn.pixabay.com/photo/2023/07/10/13/41/technology-8118380_1280.jpg#1280#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>前幾天研究<a href="/blog/llama-cpp/">電腦沒有獨立顯卡，只靠 CPU 也能跑大型語言模型嗎？</a>學到要跑 LLM 大型語言模型除了 GPU，顯卡記憶體也是關鍵。以 FB 公司 Meta 釋出的 LLaMA 2 模型為例，70B、13B、7B 四種參數規模不同的大小模型，分別需要 320GB、50GB、30GB GPU 記憶體 <a href="https://docs.nvidia.com/ai-enterprise/workflows-generative-ai/0.1.0/sizing-guide.html">參考</a>，透過量化 (Quantization) 技術可以用損失精確度換取記憶體用量減半、1/4 甚至到 1/8，若你能忍受跟吞吞吐吐的機器人聊天，不裝獨顯純用 CPU 跑 LLaMA 2 也不是不行。</p> <p>能力雖然輸給 ChatGPT，但 Meta 公司釋出的開源 LLM 專案 - LLaMA 2，除了可以在自家主機跑可商業應用，還有一大優點就是允許你重新訓練或進行微調(Fine-Tune)將模型變成你想要的形狀，此門一開，開啟了無限可能...</p> <p>不過，訓練與微調 LLM 是很燒錢的，<a href="/blog/rag/">上回談 RAG</a>時提過 OpenAI 模型的微調訓練成本是每小時 34 ~ 103 USD，若我們起心動念想微調一下 LLaMA 2，要如何預估成本，要燒多少錢？</p> <p>心存疑問但不知道去哪找答案，直接無意間查到好東西 - 國家高速網路與計算中心(NCHC，簡稱國網中心)莊朝鈞研究員的簡報 - <a href="https://covid-19.nchc.org.tw/agenda/202311_llms_Handouts_0110.pdf">跟你組織內的知識庫對話</a>，裡面有不少參考數據。</p> <p>依 LLaMA 公開的資訊，7B 版本的訓練時數是 184,320 GPU 小時、70B 為 1,720,320 GPU 小時 (每 GPU 小時為一張 A100 跑一小時)，故 7B 模型的完整訓練，若只用一張 A100 跑的話要跑 21 年! 當然，Meta 是用大量 A100 去跑，7B 花了 27.6 萬 USD、70B 花了 170 萬 USD。</p> <p>至於不同硬體的算力比較，我找到一篇好主章：<a href="https://zhuanlan.zhihu.com/p/655402388">A100/H100 太贵，何不用 4090？ by 李博杰</a></p> <p>A100 跟 4090 算力相差不大，但跑 LLM GPU 記憶體容量跟傳輸頻寬很關鍵。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428658982070858.png"><br> <a href="https://zhuanlan.zhihu.com/p/655402388">圖片來源</a></p> <p>在 PyTorch 訓練測試中，A100 Throughput 是 4090 的 1.4 倍，H100 是 4090 的 1.6 到 2.5 倍：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428658986120475.png"><br> <a href="https://lambdalabs.com/gpu-benchmarks">圖片來源：Deep Learning GPU Benchmarks by Lambda Labs</a></p> <p>至於訓練算力估算可以用 6 * 模型參數量 * 訓練數據 Token 數 (Flops) 預估。</p> <p>而國網中心的算力價格，V100 每 GPU 小時，商用 2.5 USD、國科會 0.5 USD。尋常百姓較容易買到零售算力管道應該是 Google 的 Colab 平台，Colab Pro 會員每月 10.49 USD 每月有 100 個運算單元，Pro+ 會員 52.49 USD 則為 500 個運算單元 <a href="https://colab.research.google.com/signup">方案說明</a>，A100 每 GPU 小時消耗約 13 個運算單元，換算一個 GPU 小時約 1.36 USD。</p> <p>簡報有整理一些 LLaMA 2 模型接續訓練及微調案例(有部分包含成本數據)：</p> <ul><li>中研院繁體中文 LLM 的接續訓練 Continue Pretrainging (CP) 專案 CKIP-Llama-2-7b，用了 5.8G 檔案(中文維基、台灣碩博士論文摘要、中研究漢語平衡語料庫、徐志摩詩歌全集、朱自清散文全集)，20B Token 的資料接續訓練 Llama-2-7b。</li> <li>Meta 用了 27,540 條 (Supervised Fine-Tuning SFT) 高品質註釋示例有效提升品質 - Llama-2-7b-chat</li> <li>Taiwan-LLaMA 是基於 LLaMA 2 的全參數微調模型，強化繁體中文能力，包含預訓練 + 微調<br> Pretraining: 8 x A100 兩週 (LLaMA 2 學中文)<br> Instruction Finetuning: 8 x H100 12 小時</li> <li><a href="https://crfm.stanford.edu/2023/03/13/alpaca.html">Stanford-Alpaca</a> - 用 179 題，174 種不同類型的高品質多樣性 Dataset 微調，可顯著提升效果<br> (高品質特色：Step by Step、詳細解釋、額外知識，1000 條 Instruction Tuning Data 就能明顯提升表現)<br> Self-Instruct：Stanford Alpaca 從 ChatGPT 取得 5 萬 2 千條資料，用 4 x A100 GPU 一天訓練完 7B LLaMA，達到類似 text-daviinc-003 等級<br> 成本：ChatGPT API 費用 = 500 USD；A100 GPU 每小時 1 USD，4 片 * 24 hr 約 100 USD<br> 人工準備 175 產生 Instruction 的任務種子，用 ChatGPT 生成 5 萬筆 Fine-Tune 用的資料(意思是：LLaMA 你看看 ChatGPT 的回答多好，快學學人家)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428658990780164.png"></li> <li><a href="https://zhuanlan.zhihu.com/p/619426866">Alpaca-LoRA</a> 用 LoRA 方式訓練 20 分鐘完成，1 USD<br> 註：LoRA 是種用較少記憶體及計算資源執行 Fine-Tune 的做法，Alpaca-LoRA 用消費級顯卡就能完成</li></ul> <p>有了以上資訊，對於訓練與微調成本大概有點概念，訓練果然是個燒錢的活動(而且往往你得試了又試、試了再試、試了還試，永無止境...)，LoRA 微調可用少量資源達成不錯效果，用 4090 顯卡就有機會實現，但課金買裝備仍免不了燒錢。呵~</p> <p>另外，簡報整理了「何時該用 RAG、何時用 Fine-Tuning？」的評估建議，也很實用：</p> <ul><li>RAG 適用情境：需要外部知識、必須減少幻覺、資料需即時更新、需要附上資料依據</li> <li>Fine-Tuning 適用情境：需自訂模型寫作風格、有訓練資料可用</li></ul> <p data-slug="how-llm-training-burn-money" data-fbimg="https://cdn.pixabay.com/photo/2023/07/10/13/41/technology-8118380_1280.jpg#1280#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>前幾天研究<a href="/blog/llama-cpp/">電腦沒有獨立顯卡，只靠 CPU 也能跑大型語言模型嗎？</a>學到要跑 LLM 大型語言模型除了 GPU，顯卡記憶體也是關鍵。以 FB 公司 Meta 釋出的 LLaMA 2 模型為例，70B、13B、7B 四種參數規模不同的大小模型，分別需要 320GB、50GB、30GB GPU 記憶體 <a href="https://docs.nvidia.com/ai-enterprise/workflows-generative-ai/0.1.0/sizing-guide.html">參考</a>，透過量化 (Quantization) 技術可以用損失精確度換取記憶體用量減半、1/4 甚至到 1/8，若你能忍受跟吞吞吐吐的機器人聊天，不裝獨顯純用 CPU 跑 LLaMA 2 也不是不行。</p> <p>能力雖然輸給 ChatGPT，但 Meta 公司釋出的開源 LLM 專案 - LLaMA 2，除了可以在自家主機跑可商業應用，還有一大優點就是允許你重新訓練或進行微調(Fine-Tune)將模型變成你想要的形狀，此門一開，開啟了無限可能...</p> <p>不過，訓練與微調 LLM 是很燒錢的，<a href="/blog/rag/">上回談 RAG</a>時提過 OpenAI 模型的微調訓練成本是每小時 34 ~ 103 USD，若我們起心動念想微調一下 LLaMA 2，要如何預估成本，要燒多少錢？</p> <p>心存疑問但不知道去哪找答案，直接無意間查到好東西 - 國家高速網路與計算中心(NCHC，簡稱國網中心)莊朝鈞研究員的簡報 - <a href="https://covid-19.nchc.org.tw/agenda/202311_llms_Handouts_0110.pdf">跟你組織內的知識庫對話</a>，裡面有不少參考數據。</p> <p>依 LLaMA 公開的資訊，7B 版本的訓練時數是 184,320 GPU 小時、70B 為 1,720,320 GPU 小時 (每 GPU 小時為一張 A100 跑一小時)，故 7B 模型的完整訓練，若只用一張 A100 跑的話要跑 21 年! 當然，Meta 是用大量 A100 去跑，7B 花了 27.6 萬 USD、70B 花了 170 萬 USD。</p> <p>至於不同硬體的算力比較，我找到一篇好主章：<a href="https://zhuanlan.zhihu.com/p/655402388">A100/H100 太贵，何不用 4090？ by 李博杰</a></p> <p>A100 跟 4090 算力相差不大，但跑 LLM GPU 記憶體容量跟傳輸頻寬很關鍵。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428658982070858.png"><br> <a href="https://zhuanlan.zhihu.com/p/655402388">圖片來源</a></p> <p>在 PyTorch 訓練測試中，A100 Throughput 是 4090 的 1.4 倍，H100 是 4090 的 1.6 到 2.5 倍：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428658986120475.png"><br> <a href="https://lambdalabs.com/gpu-benchmarks">圖片來源：Deep Learning GPU Benchmarks by Lambda Labs</a></p> <p>至於訓練算力估算可以用 6 * 模型參數量 * 訓練數據 Token 數 (Flops) 預估。</p> <p>而國網中心的算力價格，V100 每 GPU 小時，商用 2.5 USD、國科會 0.5 USD。尋常百姓較容易買到零售算力管道應該是 Google 的 Colab 平台，Colab Pro 會員每月 10.49 USD 每月有 100 個運算單元，Pro+ 會員 52.49 USD 則為 500 個運算單元 <a href="https://colab.research.google.com/signup">方案說明</a>，A100 每 GPU 小時消耗約 13 個運算單元，換算一個 GPU 小時約 1.36 USD。</p> <p>簡報有整理一些 LLaMA 2 模型接續訓練及微調案例(有部分包含成本數據)：</p> <ul><li>中研院繁體中文 LLM 的接續訓練 Continue Pretrainging (CP) 專案 CKIP-Llama-2-7b，用了 5.8G 檔案(中文維基、台灣碩博士論文摘要、中研究漢語平衡語料庫、徐志摩詩歌全集、朱自清散文全集)，20B Token 的資料接續訓練 Llama-2-7b。</li> <li>Meta 用了 27,540 條 (Supervised Fine-Tuning SFT) 高品質註釋示例有效提升品質 - Llama-2-7b-chat</li> <li>Taiwan-LLaMA 是基於 LLaMA 2 的全參數微調模型，強化繁體中文能力，包含預訓練 + 微調<br> Pretraining: 8 x A100 兩週 (LLaMA 2 學中文)<br> Instruction Finetuning: 8 x H100 12 小時</li> <li><a href="https://crfm.stanford.edu/2023/03/13/alpaca.html">Stanford-Alpaca</a> - 用 179 題，174 種不同類型的高品質多樣性 Dataset 微調，可顯著提升效果<br> (高品質特色：Step by Step、詳細解釋、額外知識，1000 條 Instruction Tuning Data 就能明顯提升表現)<br> Self-Instruct：Stanford Alpaca 從 ChatGPT 取得 5 萬 2 千條資料，用 4 x A100 GPU 一天訓練完 7B LLaMA，達到類似 text-daviinc-003 等級<br> 成本：ChatGPT API 費用 = 500 USD；A100 GPU 每小時 1 USD，4 片 * 24 hr 約 100 USD<br> 人工準備 175 產生 Instruction 的任務種子，用 ChatGPT 生成 5 萬筆 Fine-Tune 用的資料(意思是：LLaMA 你看看 ChatGPT 的回答多好，快學學人家)<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428658990780164.png"></li> <li><a href="https://zhuanlan.zhihu.com/p/619426866">Alpaca-LoRA</a> 用 LoRA 方式訓練 20 分鐘完成，1 USD<br> 註：LoRA 是種用較少記憶體及計算資源執行 Fine-Tune 的做法，Alpaca-LoRA 用消費級顯卡就能完成</li></ul> <p>有了以上資訊，對於訓練與微調成本大概有點概念，訓練果然是個燒錢的活動(而且往往你得試了又試、試了再試、試了還試，永無止境...)，LoRA 微調可用少量資源達成不錯效果，用 4090 顯卡就有機會實現，但課金買裝備仍免不了燒錢。呵~</p> <p>另外，簡報整理了「何時該用 RAG、何時用 Fine-Tuning？」的評估建議，也很實用：</p> <ul><li>RAG 適用情境：需要外部知識、必須減少幻覺、資料需即時更新、需要附上資料依據</li> <li>Fine-Tuning 適用情境：需自訂模型寫作風格、有訓練資料可用</li></ul> <p data-slug="how-llm-training-burn-money" data-fbimg="https://cdn.pixabay.com/photo/2023/07/10/13/41/technology-8118380_1280.jpg#1280#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638428658982070858.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638428658982070858.png" medium="image"/>
    <pubDate>Wed, 07 Feb 2024 01:19:05 GMT</pubDate>
  </item><item>
    <title><![CDATA[在遠端 Linux 執行 Jupyter Notebook]]></title>
    <link>https://blog.darkthread.net/blog/jupyter-on-remote-linux/</link>
    <itunes:title><![CDATA[在遠端 Linux 執行 Jupyter Notebook]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>最近想試試從 Windows 工作機遠端連上 Linux 伺服器跑 Python，ssh 連上去用終端機跑當然沒問題，但許多學習資源是 <a href="/blog/jupyter-notebook/">Jupyter Notebook</a>，要怎麼從 Windows 在 Linux 跑 Jupyter Notebook 呢？</p> <p>啟用 RDP 或 VNC 走遠端桌面是無腦解法，但實務上可能因權限不足、無 GUI 環境或防火牆限制無法實現。Jupyter Notebook 本身是個網站，故在 Linux 跑 Jupyter Notebook 再用 SSH Tunneling 連上去也是個解法，只需開通 SSH Port 即可使用。</p> <p>以下是我的安裝設定筆記。</p> <p>我選擇在 Linux 上安裝 Anaconda，依據官方文件：<a href="https://docs.anaconda.com/free/anaconda/install/linux/">Installing Anaconda on Linux</a>，先在 <a href="https://repo.anaconda.com/archive/">Repo 網頁</a>查最新版本是 2023-09-0，使用以下指令下載安裝：</p> <pre><code>sudo apt-get install libgl1-mesa-glx libegl1-mesa libxrandr2 libxrandr2 libxss1 libxcursor1 libxcomposite1 libasound2 libxi6 libxtst6
curl -O https://repo.anaconda.com/archive/Anaconda3-2023.09-0-Linux-x86_64.sh
bash ./Anaconda3-2023.09-0-Linux-x86_64.sh
</code></pre> <p>安裝過程注意事項如下：</p> <ol><li>安裝程式會顯示版權同意書，按 Enter 開始，空白鍵下一頁，最後輸入 yes 按 Enter 表示同意。</li> <li>預設會安裝在 <code>/home/&lt;username&gt;/anaconda3</code>，沒特殊考量的話就用預設路徑</li> <li>下載安裝要花個幾分鐘：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428177241050909.png"></li> <li>最後步驟是設定每次登入後自動啟用 conda，輸入 yes 同意：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428177245270826.png"><br> 註：若選擇不自動啟用 conda，手動指令如下： <pre><code>source ~/anaconda3/bin/activate
conda init
# 用以下指令啟用或關閉自動啟用 conda
conda config --set auto_activate_base True
conda config --set auto_activate_base False
</code></pre> </li> <li>重新登入或 <code>source ~/.bashrc</code> 啟動 conda</li></ol> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428177247427041.png"></p> <p>conda 啟用後，終端機提示指令前方會出現 (base) 顯示目前所在虛擬環境，執行 <code>conda list</code> 顯示目前安裝的套件及版本，代表安裝成功。</p> <p>開始前先將 jupyter 升級到新版(不然一直有升級提示，頗煩)，用 <code>jupyter notebook</code> 啟動 Notebook，Windows 端則另開一個 ssh 啟用 Tunneling，將 Linux 的 Jupyter Notebook TCP Port 導向到 Windows 來。</p> <pre><code># 將 jupyter 升級到新版 (7)
conda update jupyter
# 在伺服器啟動 Jupyter Notebook
jupyter notebook --no-browser --port=9527
# 在本機執行 ssh 啟用 Tunneling 將遠端 9527 Port 導向本機 9527 Port
# 註：兩邊 Port 設一樣，開啟 Notebook URL 可直接用較方便
# 參數 -L local-port:remote-ip:remote-port、-N 不執行指令
ssh -N -L 9527:localhost:9527 &lt;user&gt;@&lt;host&gt;
</code></pre> <p>上面有提到本機跟 Linux 的 Port 設一樣會比較方便，理由是瀏覽器開啟 Jupyter Notebook 需要帶一個 token 參數，在 Linux 跑 jupyter notebook 顯示訊息會有連結(下圖箭頭處)，Port 相同的話可直接點選連結在 Windows 開啟。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638428177251760526.png"></p> <p>這樣就能從本機在 Linux 伺服器跑 Python，充分利用伺服器的算力。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638428177253902281.png"></p> <p>不過，身為 Python 新手，沒有 Copilot 的我恐怕剛出新手村就被史萊姆圍毆至死，還是研究一下怎麼用 VSCode 寫 Jupyter Notebook 吧。參考：<a href="https://code.visualstudio.com/docs/datascience/jupyter-kernel-management#_existing-jupyter-server">Manage Jupyter Kernels in VS Code</a></p> <p>用 Create: New Jupyter Notebook 建一本新筆記：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638428177256048971.png"></p> <p>點右上側的 Select Kernel / Existing Jupyter Server... 輸入含 token 的 URL：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638428177258625023.gif"></p> <p>並取個好記的簡短名稱：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638428177260749031.png"></p> <p>再選取 Linux 上的 Python Kernel (箭頭即為上一步輸入的名稱)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638428177262890038.png"></p> <p>輸入函數註解，Copilot 手把手教你怎麼用 Python 實作出來，學習新程式語言就該這樣，讓 Copilot 帶你飛~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638428177265042455.png"></p> <p>最後實測印出 OS 資訊，證明程式是在 Linux 上執行無誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638428177267217464.png"></p> <p>雖然整體操作的體驗遠不如本機 Jupyter Notebook 流暢，會<a href="https://code.visualstudio.com/docs/datascience/notebooks-web#_limitations">損失一些功能</a>(例如：無法逐行偵錯、不能使用 VSCode 終端介面)，但此種做法方便從 Windows 調用 Linux 主機 CPU/GPU 算力或網路環境進行作業，仍不失為一個選項。</p> <p data-slug="jupyter-on-remote-linux" data-title-en="Running Jupyter Notebook on remote Linux" data-catg="Python" data-fbimg="https://cdn.pixabay.com/photo/2019/05/26/23/00/remote-control-4231492_960_720.jpg#960#720">Notes about how to install Anaconda in Linux box and run Jupyter Notebook from Windows client.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>最近想試試從 Windows 工作機遠端連上 Linux 伺服器跑 Python，ssh 連上去用終端機跑當然沒問題，但許多學習資源是 <a href="/blog/jupyter-notebook/">Jupyter Notebook</a>，要怎麼從 Windows 在 Linux 跑 Jupyter Notebook 呢？</p> <p>啟用 RDP 或 VNC 走遠端桌面是無腦解法，但實務上可能因權限不足、無 GUI 環境或防火牆限制無法實現。Jupyter Notebook 本身是個網站，故在 Linux 跑 Jupyter Notebook 再用 SSH Tunneling 連上去也是個解法，只需開通 SSH Port 即可使用。</p> <p>以下是我的安裝設定筆記。</p> <p>我選擇在 Linux 上安裝 Anaconda，依據官方文件：<a href="https://docs.anaconda.com/free/anaconda/install/linux/">Installing Anaconda on Linux</a>，先在 <a href="https://repo.anaconda.com/archive/">Repo 網頁</a>查最新版本是 2023-09-0，使用以下指令下載安裝：</p> <pre><code>sudo apt-get install libgl1-mesa-glx libegl1-mesa libxrandr2 libxrandr2 libxss1 libxcursor1 libxcomposite1 libasound2 libxi6 libxtst6
curl -O https://repo.anaconda.com/archive/Anaconda3-2023.09-0-Linux-x86_64.sh
bash ./Anaconda3-2023.09-0-Linux-x86_64.sh
</code></pre> <p>安裝過程注意事項如下：</p> <ol><li>安裝程式會顯示版權同意書，按 Enter 開始，空白鍵下一頁，最後輸入 yes 按 Enter 表示同意。</li> <li>預設會安裝在 <code>/home/&lt;username&gt;/anaconda3</code>，沒特殊考量的話就用預設路徑</li> <li>下載安裝要花個幾分鐘：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428177241050909.png"></li> <li>最後步驟是設定每次登入後自動啟用 conda，輸入 yes 同意：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428177245270826.png"><br> 註：若選擇不自動啟用 conda，手動指令如下： <pre><code>source ~/anaconda3/bin/activate
conda init
# 用以下指令啟用或關閉自動啟用 conda
conda config --set auto_activate_base True
conda config --set auto_activate_base False
</code></pre> </li> <li>重新登入或 <code>source ~/.bashrc</code> 啟動 conda</li></ol> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428177247427041.png"></p> <p>conda 啟用後，終端機提示指令前方會出現 (base) 顯示目前所在虛擬環境，執行 <code>conda list</code> 顯示目前安裝的套件及版本，代表安裝成功。</p> <p>開始前先將 jupyter 升級到新版(不然一直有升級提示，頗煩)，用 <code>jupyter notebook</code> 啟動 Notebook，Windows 端則另開一個 ssh 啟用 Tunneling，將 Linux 的 Jupyter Notebook TCP Port 導向到 Windows 來。</p> <pre><code># 將 jupyter 升級到新版 (7)
conda update jupyter
# 在伺服器啟動 Jupyter Notebook
jupyter notebook --no-browser --port=9527
# 在本機執行 ssh 啟用 Tunneling 將遠端 9527 Port 導向本機 9527 Port
# 註：兩邊 Port 設一樣，開啟 Notebook URL 可直接用較方便
# 參數 -L local-port:remote-ip:remote-port、-N 不執行指令
ssh -N -L 9527:localhost:9527 &lt;user&gt;@&lt;host&gt;
</code></pre> <p>上面有提到本機跟 Linux 的 Port 設一樣會比較方便，理由是瀏覽器開啟 Jupyter Notebook 需要帶一個 token 參數，在 Linux 跑 jupyter notebook 顯示訊息會有連結(下圖箭頭處)，Port 相同的話可直接點選連結在 Windows 開啟。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638428177251760526.png"></p> <p>這樣就能從本機在 Linux 伺服器跑 Python，充分利用伺服器的算力。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638428177253902281.png"></p> <p>不過，身為 Python 新手，沒有 Copilot 的我恐怕剛出新手村就被史萊姆圍毆至死，還是研究一下怎麼用 VSCode 寫 Jupyter Notebook 吧。參考：<a href="https://code.visualstudio.com/docs/datascience/jupyter-kernel-management#_existing-jupyter-server">Manage Jupyter Kernels in VS Code</a></p> <p>用 Create: New Jupyter Notebook 建一本新筆記：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638428177256048971.png"></p> <p>點右上側的 Select Kernel / Existing Jupyter Server... 輸入含 token 的 URL：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638428177258625023.gif"></p> <p>並取個好記的簡短名稱：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638428177260749031.png"></p> <p>再選取 Linux 上的 Python Kernel (箭頭即為上一步輸入的名稱)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638428177262890038.png"></p> <p>輸入函數註解，Copilot 手把手教你怎麼用 Python 實作出來，學習新程式語言就該這樣，讓 Copilot 帶你飛~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638428177265042455.png"></p> <p>最後實測印出 OS 資訊，證明程式是在 Linux 上執行無誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638428177267217464.png"></p> <p>雖然整體操作的體驗遠不如本機 Jupyter Notebook 流暢，會<a href="https://code.visualstudio.com/docs/datascience/notebooks-web#_limitations">損失一些功能</a>(例如：無法逐行偵錯、不能使用 VSCode 終端介面)，但此種做法方便從 Windows 調用 Linux 主機 CPU/GPU 算力或網路環境進行作業，仍不失為一個選項。</p> <p data-slug="jupyter-on-remote-linux" data-title-en="Running Jupyter Notebook on remote Linux" data-catg="Python" data-fbimg="https://cdn.pixabay.com/photo/2019/05/26/23/00/remote-control-4231492_960_720.jpg#960#720">Notes about how to install Anaconda in Linux box and run Jupyter Notebook from Windows client.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>最近想試試從 Windows 工作機遠端連上 Linux 伺服器跑 Python，ssh 連上去用終端機跑當然沒問題，但許多學習資源是 <a href="/blog/jupyter-notebook/">Jupyter Notebook</a>，要怎麼從 Windows 在 Linux 跑 Jupyter Notebook 呢？</p> <p>啟用 RDP 或 VNC 走遠端桌面是無腦解法，但實務上可能因權限不足、無 GUI 環境或防火牆限制無法實現。Jupyter Notebook 本身是個網站，故在 Linux 跑 Jupyter Notebook 再用 SSH Tunneling 連上去也是個解法，只需開通 SSH Port 即可使用。</p> <p>以下是我的安裝設定筆記。</p> <p>我選擇在 Linux 上安裝 Anaconda，依據官方文件：<a href="https://docs.anaconda.com/free/anaconda/install/linux/">Installing Anaconda on Linux</a>，先在 <a href="https://repo.anaconda.com/archive/">Repo 網頁</a>查最新版本是 2023-09-0，使用以下指令下載安裝：</p> <pre><code>sudo apt-get install libgl1-mesa-glx libegl1-mesa libxrandr2 libxrandr2 libxss1 libxcursor1 libxcomposite1 libasound2 libxi6 libxtst6
curl -O https://repo.anaconda.com/archive/Anaconda3-2023.09-0-Linux-x86_64.sh
bash ./Anaconda3-2023.09-0-Linux-x86_64.sh
</code></pre> <p>安裝過程注意事項如下：</p> <ol><li>安裝程式會顯示版權同意書，按 Enter 開始，空白鍵下一頁，最後輸入 yes 按 Enter 表示同意。</li> <li>預設會安裝在 <code>/home/&lt;username&gt;/anaconda3</code>，沒特殊考量的話就用預設路徑</li> <li>下載安裝要花個幾分鐘：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638428177241050909.png"></li> <li>最後步驟是設定每次登入後自動啟用 conda，輸入 yes 同意：<br> <img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638428177245270826.png"><br> 註：若選擇不自動啟用 conda，手動指令如下： <pre><code>source ~/anaconda3/bin/activate
conda init
# 用以下指令啟用或關閉自動啟用 conda
conda config --set auto_activate_base True
conda config --set auto_activate_base False
</code></pre> </li> <li>重新登入或 <code>source ~/.bashrc</code> 啟動 conda</li></ol> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638428177247427041.png"></p> <p>conda 啟用後，終端機提示指令前方會出現 (base) 顯示目前所在虛擬環境，執行 <code>conda list</code> 顯示目前安裝的套件及版本，代表安裝成功。</p> <p>開始前先將 jupyter 升級到新版(不然一直有升級提示，頗煩)，用 <code>jupyter notebook</code> 啟動 Notebook，Windows 端則另開一個 ssh 啟用 Tunneling，將 Linux 的 Jupyter Notebook TCP Port 導向到 Windows 來。</p> <pre><code># 將 jupyter 升級到新版 (7)
conda update jupyter
# 在伺服器啟動 Jupyter Notebook
jupyter notebook --no-browser --port=9527
# 在本機執行 ssh 啟用 Tunneling 將遠端 9527 Port 導向本機 9527 Port
# 註：兩邊 Port 設一樣，開啟 Notebook URL 可直接用較方便
# 參數 -L local-port:remote-ip:remote-port、-N 不執行指令
ssh -N -L 9527:localhost:9527 &lt;user&gt;@&lt;host&gt;
</code></pre> <p>上面有提到本機跟 Linux 的 Port 設一樣會比較方便，理由是瀏覽器開啟 Jupyter Notebook 需要帶一個 token 參數，在 Linux 跑 jupyter notebook 顯示訊息會有連結(下圖箭頭處)，Port 相同的話可直接點選連結在 Windows 開啟。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig9_638428177251760526.png"></p> <p>這樣就能從本機在 Linux 伺服器跑 Python，充分利用伺服器的算力。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig10_638428177253902281.png"></p> <p>不過，身為 Python 新手，沒有 Copilot 的我恐怕剛出新手村就被史萊姆圍毆至死，還是研究一下怎麼用 VSCode 寫 Jupyter Notebook 吧。參考：<a href="https://code.visualstudio.com/docs/datascience/jupyter-kernel-management#_existing-jupyter-server">Manage Jupyter Kernels in VS Code</a></p> <p>用 Create: New Jupyter Notebook 建一本新筆記：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638428177256048971.png"></p> <p>點右上側的 Select Kernel / Existing Jupyter Server... 輸入含 token 的 URL：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig5_638428177258625023.gif"></p> <p>並取個好記的簡短名稱：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig6_638428177260749031.png"></p> <p>再選取 Linux 上的 Python Kernel (箭頭即為上一步輸入的名稱)：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig8_638428177262890038.png"></p> <p>輸入函數註解，Copilot 手把手教你怎麼用 Python 實作出來，學習新程式語言就該這樣，讓 Copilot 帶你飛~~</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig11_638428177265042455.png"></p> <p>最後實測印出 OS 資訊，證明程式是在 Linux 上執行無誤：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig7_638428177267217464.png"></p> <p>雖然整體操作的體驗遠不如本機 Jupyter Notebook 流暢，會<a href="https://code.visualstudio.com/docs/datascience/notebooks-web#_limitations">損失一些功能</a>(例如：無法逐行偵錯、不能使用 VSCode 終端介面)，但此種做法方便從 Windows 調用 Linux 主機 CPU/GPU 算力或網路環境進行作業，仍不失為一個選項。</p> <p data-slug="jupyter-on-remote-linux" data-title-en="Running Jupyter Notebook on remote Linux" data-catg="Python" data-fbimg="https://cdn.pixabay.com/photo/2019/05/26/23/00/remote-control-4231492_960_720.jpg#960#720">Notes about how to install Anaconda in Linux box and run Jupyter Notebook from Windows client.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638428177241050909.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638428177241050909.png" medium="image"/>
    <pubDate>Tue, 06 Feb 2024 11:54:29 GMT</pubDate>
  </item><item>
    <title><![CDATA[.NET 小技巧 - 將 List/Dictionary 設為唯讀防止誤用]]></title>
    <link>https://blog.darkthread.net/blog/ireadonlylist-and-ireadonlydictionary/</link>
    <itunes:title><![CDATA[.NET 小技巧 - 將 List/Dictionary 設為唯讀防止誤用]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>今天踩到的低級錯誤，用以下範例重現。</p> <p>假設有物件 Foo，建構時傳入字串，透過 <code>List&lt;string&gt; List</code> 與 <code>Dictionary&lt;string, int&gt; Stats</code> 兩個唯讀屬性傳回包含字元清單及出現次數統計：</p> <pre><code>public class Foo {
public List&lt;string&gt; List =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.Select(g =&gt; g.Key.ToString())
.ToList();
public Dictionary&lt;string, int&gt; Stats =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.ToDictionary(g =&gt; g.Key.ToString(), g =&gt; g.Count());
string rawData ;
public Foo(string data)
{
rawData = data;
}
}
</code></pre> <p>每次讀取重新統計有損效能，但原始情境很單純又，只有一處程式用到，兩個屬性也只會讀一次，簡單寫寫就好。</p> <p>不過呢，你永遠料想不到別人會怎麼用你的元件(這裡的別人包含未來的自己)。在其他模組借用了這顆元件，看到元件傳回 <code>List&lt;string&gt;</code> 的是 ``Dictionary&lt;string, int&gt;`，依直覺加工後想用在別的地方：</p> <pre><code>var foo = new Foo("AADBDACABC");
foo.List.Add("E");
foo.Stats.Add("E", 2);
Console.WriteLine(string.Join(", ",
foo.Stats.Select(o=&gt; $"{o.Key}:{o.Value}")));
Console.WriteLine(string.Join(", ", foo.List));
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638427368511716539.png"></p> <p>結局是：明明用 Add() 做了修改，實際輸出卻什麼都沒變，追了一會兒才猛然想起問題出「List 跟 Stats 是現點現做的」。</p> <p>先不探究這類別介面設計得有問題，把焦點放在「若規格就是要 List 或 Dictionary，有沒有辦法將其設成唯讀防止呼叫端誤用」？</p> <p>心血來潮，這回不 Google 求解，改問<del>神奇海螺</del> Github Copilot 看能否得到答案？</p> <p>使用 Copilot Chat[1]，用 <code>how to prevent user from altering the properties or elements of readonly properties (ex: Stats and List)?</code> 提問[2]，由 Copilot 回答我學到 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlylist-1?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyList</a> 及 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlydictionary-2?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyDictionary</a>，而 <code>Dictionary&lt;T,T&gt;</code>、<code>List&lt;T&gt;</code> 都支援 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.collections.generic.collectionextensions.asreadonly?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">AsReadOnly()</a> 傳回 <code>ReadOnlyDictionary&lt;TKey,TValue&gt;</code>、<code>ReadOnlyCollection&lt;T&gt;</code>。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638427368518040232.png"></p> <p>另一種更快的做法是，選取 List 與 Stats 屬性的程式片段[1]，按 Ctrl - I 叫出提示輸入列，請 Copilot 將二者換成唯讀版[2]，Copilot 會直接改寫，按下 Accept 即可套用，而套用前已可預覽修改後上方的兩個 Add() 出現紅蚯蚓[3]無法編譯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638427368520286698.png"></p> <p>注意：IReadOnlyList、IReadOnlyDictionary 只防止對清單跟雜湊表進行增刪或置換，無法防止清單或雜湊元素被修改，如下例：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638427368522510001.png"></p> <p data-slug="ireadonlylist-and-ireadonlydictionary" data-title-en="IReadOnlyList and IReadOnlyDictionary" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/01/08/13/01/kettle-bell-592905_960_720.jpg#960#720">Example of using IReadOnlyList and IReadOnlyDictionary to prevent modifictaion.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>今天踩到的低級錯誤，用以下範例重現。</p> <p>假設有物件 Foo，建構時傳入字串，透過 <code>List&lt;string&gt; List</code> 與 <code>Dictionary&lt;string, int&gt; Stats</code> 兩個唯讀屬性傳回包含字元清單及出現次數統計：</p> <pre><code>public class Foo {
public List&lt;string&gt; List =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.Select(g =&gt; g.Key.ToString())
.ToList();
public Dictionary&lt;string, int&gt; Stats =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.ToDictionary(g =&gt; g.Key.ToString(), g =&gt; g.Count());
string rawData ;
public Foo(string data)
{
rawData = data;
}
}
</code></pre> <p>每次讀取重新統計有損效能，但原始情境很單純又，只有一處程式用到，兩個屬性也只會讀一次，簡單寫寫就好。</p> <p>不過呢，你永遠料想不到別人會怎麼用你的元件(這裡的別人包含未來的自己)。在其他模組借用了這顆元件，看到元件傳回 <code>List&lt;string&gt;</code> 的是 ``Dictionary&lt;string, int&gt;`，依直覺加工後想用在別的地方：</p> <pre><code>var foo = new Foo("AADBDACABC");
foo.List.Add("E");
foo.Stats.Add("E", 2);
Console.WriteLine(string.Join(", ",
foo.Stats.Select(o=&gt; $"{o.Key}:{o.Value}")));
Console.WriteLine(string.Join(", ", foo.List));
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638427368511716539.png"></p> <p>結局是：明明用 Add() 做了修改，實際輸出卻什麼都沒變，追了一會兒才猛然想起問題出「List 跟 Stats 是現點現做的」。</p> <p>先不探究這類別介面設計得有問題，把焦點放在「若規格就是要 List 或 Dictionary，有沒有辦法將其設成唯讀防止呼叫端誤用」？</p> <p>心血來潮，這回不 Google 求解，改問<del>神奇海螺</del> Github Copilot 看能否得到答案？</p> <p>使用 Copilot Chat[1]，用 <code>how to prevent user from altering the properties or elements of readonly properties (ex: Stats and List)?</code> 提問[2]，由 Copilot 回答我學到 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlylist-1?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyList</a> 及 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlydictionary-2?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyDictionary</a>，而 <code>Dictionary&lt;T,T&gt;</code>、<code>List&lt;T&gt;</code> 都支援 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.collections.generic.collectionextensions.asreadonly?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">AsReadOnly()</a> 傳回 <code>ReadOnlyDictionary&lt;TKey,TValue&gt;</code>、<code>ReadOnlyCollection&lt;T&gt;</code>。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638427368518040232.png"></p> <p>另一種更快的做法是，選取 List 與 Stats 屬性的程式片段[1]，按 Ctrl - I 叫出提示輸入列，請 Copilot 將二者換成唯讀版[2]，Copilot 會直接改寫，按下 Accept 即可套用，而套用前已可預覽修改後上方的兩個 Add() 出現紅蚯蚓[3]無法編譯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638427368520286698.png"></p> <p>注意：IReadOnlyList、IReadOnlyDictionary 只防止對清單跟雜湊表進行增刪或置換，無法防止清單或雜湊元素被修改，如下例：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638427368522510001.png"></p> <p data-slug="ireadonlylist-and-ireadonlydictionary" data-title-en="IReadOnlyList and IReadOnlyDictionary" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/01/08/13/01/kettle-bell-592905_960_720.jpg#960#720">Example of using IReadOnlyList and IReadOnlyDictionary to prevent modifictaion.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>今天踩到的低級錯誤，用以下範例重現。</p> <p>假設有物件 Foo，建構時傳入字串，透過 <code>List&lt;string&gt; List</code> 與 <code>Dictionary&lt;string, int&gt; Stats</code> 兩個唯讀屬性傳回包含字元清單及出現次數統計：</p> <pre><code>public class Foo {
public List&lt;string&gt; List =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.Select(g =&gt; g.Key.ToString())
.ToList();
public Dictionary&lt;string, int&gt; Stats =&gt;
rawData.ToCharArray().GroupBy(c =&gt; c)
.OrderBy(g =&gt; g.Key)
.ToDictionary(g =&gt; g.Key.ToString(), g =&gt; g.Count());
string rawData ;
public Foo(string data)
{
rawData = data;
}
}
</code></pre> <p>每次讀取重新統計有損效能，但原始情境很單純又，只有一處程式用到，兩個屬性也只會讀一次，簡單寫寫就好。</p> <p>不過呢，你永遠料想不到別人會怎麼用你的元件(這裡的別人包含未來的自己)。在其他模組借用了這顆元件，看到元件傳回 <code>List&lt;string&gt;</code> 的是 ``Dictionary&lt;string, int&gt;`，依直覺加工後想用在別的地方：</p> <pre><code>var foo = new Foo("AADBDACABC");
foo.List.Add("E");
foo.Stats.Add("E", 2);
Console.WriteLine(string.Join(", ",
foo.Stats.Select(o=&gt; $"{o.Key}:{o.Value}")));
Console.WriteLine(string.Join(", ", foo.List));
</code></pre> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig1_638427368511716539.png"></p> <p>結局是：明明用 Add() 做了修改，實際輸出卻什麼都沒變，追了一會兒才猛然想起問題出「List 跟 Stats 是現點現做的」。</p> <p>先不探究這類別介面設計得有問題，把焦點放在「若規格就是要 List 或 Dictionary，有沒有辦法將其設成唯讀防止呼叫端誤用」？</p> <p>心血來潮，這回不 Google 求解，改問<del>神奇海螺</del> Github Copilot 看能否得到答案？</p> <p>使用 Copilot Chat[1]，用 <code>how to prevent user from altering the properties or elements of readonly properties (ex: Stats and List)?</code> 提問[2]，由 Copilot 回答我學到 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlylist-1?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyList</a> 及 <a href="https://learn.microsoft.com/zh-tw/dotnet/api/system.collections.generic.ireadonlydictionary-2?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">IReadOnlyDictionary</a>，而 <code>Dictionary&lt;T,T&gt;</code>、<code>List&lt;T&gt;</code> 都支援 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.collections.generic.collectionextensions.asreadonly?view=net-8.0&amp;WT.mc_id=DOP-MVP-37580">AsReadOnly()</a> 傳回 <code>ReadOnlyDictionary&lt;TKey,TValue&gt;</code>、<code>ReadOnlyCollection&lt;T&gt;</code>。</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig2_638427368518040232.png"></p> <p>另一種更快的做法是，選取 List 與 Stats 屬性的程式片段[1]，按 Ctrl - I 叫出提示輸入列，請 Copilot 將二者換成唯讀版[2]，Copilot 會直接改寫，按下 Accept 即可套用，而套用前已可預覽修改後上方的兩個 Add() 出現紅蚯蚓[3]無法編譯：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig3_638427368520286698.png"></p> <p>注意：IReadOnlyList、IReadOnlyDictionary 只防止對清單跟雜湊表進行增刪或置換，無法防止清單或雜湊元素被修改，如下例：</p> <p><img src="https://blog.darkthread.net/Posts/files/2024/Fig4_638427368522510001.png"></p> <p data-slug="ireadonlylist-and-ireadonlydictionary" data-title-en="IReadOnlyList and IReadOnlyDictionary" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2015/01/08/13/01/kettle-bell-592905_960_720.jpg#960#720">Example of using IReadOnlyList and IReadOnlyDictionary to prevent modifictaion.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2024/Fig1_638427368511716539.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2024/Fig1_638427368511716539.png" medium="image"/>
    <pubDate>Mon, 05 Feb 2024 13:35:30 GMT</pubDate>
  </item></channel>
</rss>