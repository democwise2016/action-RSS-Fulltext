<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[黑暗執行緒[RSS+]]]></title>
    <link>undefined</link>
    <image>
      <url>https://blog.darkthread.net/img/icon512x512.png</url>
      <title>黑暗執行緒[RSS+]</title>
      <link>undefined</link>
    </image>
    <language>en-us</language>
    <atom:link href="https://blog.darkthread.net/feed/rss/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[黑暗執行緒[RSS+]]]></copyright>
    <itunes:author><![CDATA[黑暗執行緒[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      黑暗執行緒部落格 - 一位在技術領域打滾多年老士官長的生活點滴<br />
<br />
<a href="https://blog.darkthread.net/feed/rss/" target="_blank">https://blog.darkthread.net/feed/rss/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[黑暗執行緒[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://blog.darkthread.net/img/icon512x512.png"/>
<item>
    <title><![CDATA[小工具：快速檢測密碼是否外洩或被列入已知清單]]></title>
    <link>https://blog.darkthread.net/blog/pwd-hibp-check/</link>
    <itunes:title><![CDATA[小工具：快速檢測密碼是否外洩或被列入已知清單]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://haveibeenpwned.com/">Have I Been Pwned (HIBP)</a> 是澳洲安全專家 Troy Hunt 在 2013 年建立的外洩密碼查詢平臺，連美國 FBI 跟英國 NCA (國家犯罪調查局）都會將蒐集到的外洩密碼貢獻給 HIBP 收藏 (參考：<a href="https://www.ithome.com.tw/news/148479">繼美國之後，英國也加入提供外洩密碼予Have I Been Pwned的行列 by iThome</a>)，說 HIBP 是全世界最權威的外洩密碼資料庫絕不為過。</p> <p>HIBP 有個介面可輸入 Email 查詢，檢查該 Email 是否在外洩事件出現過，但這只對已發生過的外洩事件有效。從另個角度想，這批外洩密碼會是駭客猜密碼的首選，先把已知密碼試過一輪再跑吃力不討好的暴力破解。換言之，若你使用的密碼已在 HIBP 資料庫出現過，以昨<a href="/blog/pwd-crack-time/">提到的 NVIDIA RTX 4070 算力</a>預估，每秒 218 億次 SHA256、509 億次 SHA1，現有六億多筆已知密碼試過一次只要 0.03s，就算密碼長達 20 位，照樣會被秒殺。(註：駭客未必能拿到外洩密碼明文，非連續性資料的每秒計算次數照理要低一些，但料敵從寬，就當一秒吧)</p> <p>要避免自己的密碼在駭客暖身階段被猜中，最好的做法是先確認它沒有出現在 HIBP 資料庫。HIBP 有密碼資料檔可下載，但資料量龐大還要自己寫程式處理。透過 HIBP 提供的 API 查詢是較便捷的做法。</p> <p>等等，在網站上輸入密碼檢查有沒有外洩聽起來超蠢，原本沒外流的密碼在輸入過程外流怎麼辦？這根本是在送頭吧？ (我想起「誘使失主開保險箱檢查東西還在不在，小偷在旁觀察伺機下手」的橋段，以及「上網查自己有沒有在教召名單就收到教召令」的都市傳說)</p> <p>HIBP 身為資安相關網站，提供 API 時自然不會犯下這類低級錯誤，它想了一個巧妙做法：要使用者自己先算好密碼的 SHA1 雜湊，用雜湊前五碼當查詢條件，API 傳回所有前五碼相符的密碼雜湊，以及該筆密碼在資料庫出現次數。查詢者取回清單自行比對後 35 碼，若找到就代表該筆密碼已被收錄，次數愈高代表愈多人用，認證為菜市場密碼無誤。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638357457698896864.png"></p> <p>如此，使用者可確認密碼是否在資料庫中，而 HIBP 站方只知前五碼，無從得知使用者想查的密碼雜湊是哪一筆，兼顧方便與安全，好個聰明做法。</p> <p>發現這麼用好的服務，我馬上想寫個小工具呼叫 API 快速比對密碼雜湊，讓 SHA1 計算、資料下載、比對全在自家完成，將密碼外流風險降到最低。為證明檢查程式光明磊落，不會搞出假查詢真偷看的勾當，坦蕩蕩不怕你看，又通吃 Windows/MacOS/Linux 的 PowerShell，是本次開發語言首選。</p> <pre><code>$ProgressPreference = 'SilentlyContinue'
while ($true) {
$secString = Read-Host "請輸入要檢查的密碼" -AsSecureString
if ($secString.Length -eq 0) {
Write-Host "Bye~"
break
}
# https://github.com/PowerShell/PowerShell/issues/13494#issuecomment-678150857
$ptr = [System.Runtime.InteropServices.Marshal]::SecureStringToGlobalAllocUnicode($secString)
$passwd = [System.Runtime.InteropServices.Marshal]::PtrToStringUni($ptr)
[System.Runtime.InteropServices.Marshal]::ZeroFreeGlobalAllocUnicode($ptr)
$pwdBytes = [System.Text.Encoding]::UTF8.GetBytes($passwd)
$hash = [System.BitConverter]::ToString([System.Security.Cryptography.SHA1]::Create().ComputeHash($pwdBytes)).Replace('-', '')
$prefix = $hash.Substring(0, 5)
$remaining = $hash.Substring(5)
Write-Host "$prefix - $remaining" -ForegroundColor Cyan
$found = (Invoke-WebRequest "https://api.pwnedpasswords.com/range/$prefix").Content.Split("`n") | Select-String -Pattern $remaining -CaseSensitive
if ($found) {
$p = $found -split ':'
Write-Host "警告!此密碼在 HIBP 資料庫出現 " -ForegroundColor Red -NoNewline
Write-Host (+$p[1]).ToString('n0') -ForegroundColor Yellow -NoNewline
Write-Host " 次，不建議使用" -ForegroundColor Red
Write-Host $found -ForegroundColor Cyan
}
else {
Write-Host "呼~ HIBP 資料庫未收錄此密碼" -ForegroundColor Green
$charPool = 0
$setNames = @();
if ($passwd -match '[0-9]') { $charPool += 10; $setNames += '數字' }
if ($passwd -match '[a-z]') { $charPool += 26; $setNames += '小寫' }
if ($passwd -match '[A-Z]') { $charPool += 26; $setNames += '大寫' }
if ($passwd -match '[^0-9a-zA-Z]') { $charPool += 32; $setNames += '符號' }
$length = $passwd.Length
[bigint]$count = [bigint]::Pow($charPool, $length)
$rtx4090Power = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$secs = [bigint]::Divide($count, $rtx4090Power)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$time = "$([bigint]::Divide($secs,86400 * 365).ToString('n0')) 年"
}
Write-Host "長度 $length ($($setNames -join '、')) 預估暴力破解時間約：$time (NVIDIA RTX-4090 / SHA256)" -ForegroundColor Magenta
}
Write-Host
}
</code></pre> <p>將上述內容存成 Check-PasswdCollision.ps1：(編碼記得選 UTF8 with BOM)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638357457700846056.png"></p> <p>之後從 CMD 呼叫或從檔案總管選「以 PowerShell 執行」，便可快速查詢密碼是否出現在資料庫中：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638357457704612435.png"></p> <p>我寫了迴圈方便反覆輸入密碼查詢，若沒出現在 HIBP 資料庫，則程式會用長度，字元組合複雜度(數字、大小寫、有無符號)簡單推算，如果密碼用 SHA256 保存，駭客買張 NVIDIA RTX 4090 顯卡，要花多久時間可以破解你的密碼。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638357457706641648.png"></p> <p>實測後你會發現，ABCD1234、qwerasdf 這類簡單拼湊密碼幾乎全部都被人用過了，甚至像「我的密碼」注音符號輸入 - ji32k7au4a83 看似難猜，但它出現 249 次。ABCDEabcde1234 長達 14 碼的簡單組合照樣有人用過。14 碼隨機純數字，就算沒人用過，暴力破解只需一小時。但含大小寫短文字拼接 - TueFriTueSun 的表現挺不錯，用四位數字轉星期，只需 12 碼，好記又難破。(但這招既然說破了，已不再安全 XD)</p> <p>查詢完成，密碼輸入直接按 Enter 即可結束程式。</p> <p>最後來驗證 PowerShell 的跨平台特性，在 Linux / MacOS 實測一下。</p> <p>Linux OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638357457708723412.png"></p> <p>MacOS OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638357457710669944.png"></p> <p>工具箱再添實用小工具一枚。</p> <p data-slug="pwd-hibp-check" data-title-en="PowerShell to check password with HIBP API safely" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A handy PowerShell tool to call HIBP API to check if password exists in breach password database.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://haveibeenpwned.com/">Have I Been Pwned (HIBP)</a> 是澳洲安全專家 Troy Hunt 在 2013 年建立的外洩密碼查詢平臺，連美國 FBI 跟英國 NCA (國家犯罪調查局）都會將蒐集到的外洩密碼貢獻給 HIBP 收藏 (參考：<a href="https://www.ithome.com.tw/news/148479">繼美國之後，英國也加入提供外洩密碼予Have I Been Pwned的行列 by iThome</a>)，說 HIBP 是全世界最權威的外洩密碼資料庫絕不為過。</p> <p>HIBP 有個介面可輸入 Email 查詢，檢查該 Email 是否在外洩事件出現過，但這只對已發生過的外洩事件有效。從另個角度想，這批外洩密碼會是駭客猜密碼的首選，先把已知密碼試過一輪再跑吃力不討好的暴力破解。換言之，若你使用的密碼已在 HIBP 資料庫出現過，以昨<a href="/blog/pwd-crack-time/">提到的 NVIDIA RTX 4070 算力</a>預估，每秒 218 億次 SHA256、509 億次 SHA1，現有六億多筆已知密碼試過一次只要 0.03s，就算密碼長達 20 位，照樣會被秒殺。(註：駭客未必能拿到外洩密碼明文，非連續性資料的每秒計算次數照理要低一些，但料敵從寬，就當一秒吧)</p> <p>要避免自己的密碼在駭客暖身階段被猜中，最好的做法是先確認它沒有出現在 HIBP 資料庫。HIBP 有密碼資料檔可下載，但資料量龐大還要自己寫程式處理。透過 HIBP 提供的 API 查詢是較便捷的做法。</p> <p>等等，在網站上輸入密碼檢查有沒有外洩聽起來超蠢，原本沒外流的密碼在輸入過程外流怎麼辦？這根本是在送頭吧？ (我想起「誘使失主開保險箱檢查東西還在不在，小偷在旁觀察伺機下手」的橋段，以及「上網查自己有沒有在教召名單就收到教召令」的都市傳說)</p> <p>HIBP 身為資安相關網站，提供 API 時自然不會犯下這類低級錯誤，它想了一個巧妙做法：要使用者自己先算好密碼的 SHA1 雜湊，用雜湊前五碼當查詢條件，API 傳回所有前五碼相符的密碼雜湊，以及該筆密碼在資料庫出現次數。查詢者取回清單自行比對後 35 碼，若找到就代表該筆密碼已被收錄，次數愈高代表愈多人用，認證為菜市場密碼無誤。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638357457698896864.png"></p> <p>如此，使用者可確認密碼是否在資料庫中，而 HIBP 站方只知前五碼，無從得知使用者想查的密碼雜湊是哪一筆，兼顧方便與安全，好個聰明做法。</p> <p>發現這麼用好的服務，我馬上想寫個小工具呼叫 API 快速比對密碼雜湊，讓 SHA1 計算、資料下載、比對全在自家完成，將密碼外流風險降到最低。為證明檢查程式光明磊落，不會搞出假查詢真偷看的勾當，坦蕩蕩不怕你看，又通吃 Windows/MacOS/Linux 的 PowerShell，是本次開發語言首選。</p> <pre><code>$ProgressPreference = 'SilentlyContinue'
while ($true) {
$secString = Read-Host "請輸入要檢查的密碼" -AsSecureString
if ($secString.Length -eq 0) {
Write-Host "Bye~"
break
}
# https://github.com/PowerShell/PowerShell/issues/13494#issuecomment-678150857
$ptr = [System.Runtime.InteropServices.Marshal]::SecureStringToGlobalAllocUnicode($secString)
$passwd = [System.Runtime.InteropServices.Marshal]::PtrToStringUni($ptr)
[System.Runtime.InteropServices.Marshal]::ZeroFreeGlobalAllocUnicode($ptr)
$pwdBytes = [System.Text.Encoding]::UTF8.GetBytes($passwd)
$hash = [System.BitConverter]::ToString([System.Security.Cryptography.SHA1]::Create().ComputeHash($pwdBytes)).Replace('-', '')
$prefix = $hash.Substring(0, 5)
$remaining = $hash.Substring(5)
Write-Host "$prefix - $remaining" -ForegroundColor Cyan
$found = (Invoke-WebRequest "https://api.pwnedpasswords.com/range/$prefix").Content.Split("`n") | Select-String -Pattern $remaining -CaseSensitive
if ($found) {
$p = $found -split ':'
Write-Host "警告!此密碼在 HIBP 資料庫出現 " -ForegroundColor Red -NoNewline
Write-Host (+$p[1]).ToString('n0') -ForegroundColor Yellow -NoNewline
Write-Host " 次，不建議使用" -ForegroundColor Red
Write-Host $found -ForegroundColor Cyan
}
else {
Write-Host "呼~ HIBP 資料庫未收錄此密碼" -ForegroundColor Green
$charPool = 0
$setNames = @();
if ($passwd -match '[0-9]') { $charPool += 10; $setNames += '數字' }
if ($passwd -match '[a-z]') { $charPool += 26; $setNames += '小寫' }
if ($passwd -match '[A-Z]') { $charPool += 26; $setNames += '大寫' }
if ($passwd -match '[^0-9a-zA-Z]') { $charPool += 32; $setNames += '符號' }
$length = $passwd.Length
[bigint]$count = [bigint]::Pow($charPool, $length)
$rtx4090Power = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$secs = [bigint]::Divide($count, $rtx4090Power)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$time = "$([bigint]::Divide($secs,86400 * 365).ToString('n0')) 年"
}
Write-Host "長度 $length ($($setNames -join '、')) 預估暴力破解時間約：$time (NVIDIA RTX-4090 / SHA256)" -ForegroundColor Magenta
}
Write-Host
}
</code></pre> <p>將上述內容存成 Check-PasswdCollision.ps1：(編碼記得選 UTF8 with BOM)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638357457700846056.png"></p> <p>之後從 CMD 呼叫或從檔案總管選「以 PowerShell 執行」，便可快速查詢密碼是否出現在資料庫中：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638357457704612435.png"></p> <p>我寫了迴圈方便反覆輸入密碼查詢，若沒出現在 HIBP 資料庫，則程式會用長度，字元組合複雜度(數字、大小寫、有無符號)簡單推算，如果密碼用 SHA256 保存，駭客買張 NVIDIA RTX 4090 顯卡，要花多久時間可以破解你的密碼。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638357457706641648.png"></p> <p>實測後你會發現，ABCD1234、qwerasdf 這類簡單拼湊密碼幾乎全部都被人用過了，甚至像「我的密碼」注音符號輸入 - ji32k7au4a83 看似難猜，但它出現 249 次。ABCDEabcde1234 長達 14 碼的簡單組合照樣有人用過。14 碼隨機純數字，就算沒人用過，暴力破解只需一小時。但含大小寫短文字拼接 - TueFriTueSun 的表現挺不錯，用四位數字轉星期，只需 12 碼，好記又難破。(但這招既然說破了，已不再安全 XD)</p> <p>查詢完成，密碼輸入直接按 Enter 即可結束程式。</p> <p>最後來驗證 PowerShell 的跨平台特性，在 Linux / MacOS 實測一下。</p> <p>Linux OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638357457708723412.png"></p> <p>MacOS OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638357457710669944.png"></p> <p>工具箱再添實用小工具一枚。</p> <p data-slug="pwd-hibp-check" data-title-en="PowerShell to check password with HIBP API safely" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A handy PowerShell tool to call HIBP API to check if password exists in breach password database.</p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://haveibeenpwned.com/">Have I Been Pwned (HIBP)</a> 是澳洲安全專家 Troy Hunt 在 2013 年建立的外洩密碼查詢平臺，連美國 FBI 跟英國 NCA (國家犯罪調查局）都會將蒐集到的外洩密碼貢獻給 HIBP 收藏 (參考：<a href="https://www.ithome.com.tw/news/148479">繼美國之後，英國也加入提供外洩密碼予Have I Been Pwned的行列 by iThome</a>)，說 HIBP 是全世界最權威的外洩密碼資料庫絕不為過。</p> <p>HIBP 有個介面可輸入 Email 查詢，檢查該 Email 是否在外洩事件出現過，但這只對已發生過的外洩事件有效。從另個角度想，這批外洩密碼會是駭客猜密碼的首選，先把已知密碼試過一輪再跑吃力不討好的暴力破解。換言之，若你使用的密碼已在 HIBP 資料庫出現過，以昨<a href="/blog/pwd-crack-time/">提到的 NVIDIA RTX 4070 算力</a>預估，每秒 218 億次 SHA256、509 億次 SHA1，現有六億多筆已知密碼試過一次只要 0.03s，就算密碼長達 20 位，照樣會被秒殺。(註：駭客未必能拿到外洩密碼明文，非連續性資料的每秒計算次數照理要低一些，但料敵從寬，就當一秒吧)</p> <p>要避免自己的密碼在駭客暖身階段被猜中，最好的做法是先確認它沒有出現在 HIBP 資料庫。HIBP 有密碼資料檔可下載，但資料量龐大還要自己寫程式處理。透過 HIBP 提供的 API 查詢是較便捷的做法。</p> <p>等等，在網站上輸入密碼檢查有沒有外洩聽起來超蠢，原本沒外流的密碼在輸入過程外流怎麼辦？這根本是在送頭吧？ (我想起「誘使失主開保險箱檢查東西還在不在，小偷在旁觀察伺機下手」的橋段，以及「上網查自己有沒有在教召名單就收到教召令」的都市傳說)</p> <p>HIBP 身為資安相關網站，提供 API 時自然不會犯下這類低級錯誤，它想了一個巧妙做法：要使用者自己先算好密碼的 SHA1 雜湊，用雜湊前五碼當查詢條件，API 傳回所有前五碼相符的密碼雜湊，以及該筆密碼在資料庫出現次數。查詢者取回清單自行比對後 35 碼，若找到就代表該筆密碼已被收錄，次數愈高代表愈多人用，認證為菜市場密碼無誤。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638357457698896864.png"></p> <p>如此，使用者可確認密碼是否在資料庫中，而 HIBP 站方只知前五碼，無從得知使用者想查的密碼雜湊是哪一筆，兼顧方便與安全，好個聰明做法。</p> <p>發現這麼用好的服務，我馬上想寫個小工具呼叫 API 快速比對密碼雜湊，讓 SHA1 計算、資料下載、比對全在自家完成，將密碼外流風險降到最低。為證明檢查程式光明磊落，不會搞出假查詢真偷看的勾當，坦蕩蕩不怕你看，又通吃 Windows/MacOS/Linux 的 PowerShell，是本次開發語言首選。</p> <pre><code>$ProgressPreference = 'SilentlyContinue'
while ($true) {
$secString = Read-Host "請輸入要檢查的密碼" -AsSecureString
if ($secString.Length -eq 0) {
Write-Host "Bye~"
break
}
# https://github.com/PowerShell/PowerShell/issues/13494#issuecomment-678150857
$ptr = [System.Runtime.InteropServices.Marshal]::SecureStringToGlobalAllocUnicode($secString)
$passwd = [System.Runtime.InteropServices.Marshal]::PtrToStringUni($ptr)
[System.Runtime.InteropServices.Marshal]::ZeroFreeGlobalAllocUnicode($ptr)
$pwdBytes = [System.Text.Encoding]::UTF8.GetBytes($passwd)
$hash = [System.BitConverter]::ToString([System.Security.Cryptography.SHA1]::Create().ComputeHash($pwdBytes)).Replace('-', '')
$prefix = $hash.Substring(0, 5)
$remaining = $hash.Substring(5)
Write-Host "$prefix - $remaining" -ForegroundColor Cyan
$found = (Invoke-WebRequest "https://api.pwnedpasswords.com/range/$prefix").Content.Split("`n") | Select-String -Pattern $remaining -CaseSensitive
if ($found) {
$p = $found -split ':'
Write-Host "警告!此密碼在 HIBP 資料庫出現 " -ForegroundColor Red -NoNewline
Write-Host (+$p[1]).ToString('n0') -ForegroundColor Yellow -NoNewline
Write-Host " 次，不建議使用" -ForegroundColor Red
Write-Host $found -ForegroundColor Cyan
}
else {
Write-Host "呼~ HIBP 資料庫未收錄此密碼" -ForegroundColor Green
$charPool = 0
$setNames = @();
if ($passwd -match '[0-9]') { $charPool += 10; $setNames += '數字' }
if ($passwd -match '[a-z]') { $charPool += 26; $setNames += '小寫' }
if ($passwd -match '[A-Z]') { $charPool += 26; $setNames += '大寫' }
if ($passwd -match '[^0-9a-zA-Z]') { $charPool += 32; $setNames += '符號' }
$length = $passwd.Length
[bigint]$count = [bigint]::Pow($charPool, $length)
$rtx4090Power = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$secs = [bigint]::Divide($count, $rtx4090Power)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$time = "$([bigint]::Divide($secs,86400 * 365).ToString('n0')) 年"
}
Write-Host "長度 $length ($($setNames -join '、')) 預估暴力破解時間約：$time (NVIDIA RTX-4090 / SHA256)" -ForegroundColor Magenta
}
Write-Host
}
</code></pre> <p>將上述內容存成 Check-PasswdCollision.ps1：(編碼記得選 UTF8 with BOM)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638357457700846056.png"></p> <p>之後從 CMD 呼叫或從檔案總管選「以 PowerShell 執行」，便可快速查詢密碼是否出現在資料庫中：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638357457704612435.png"></p> <p>我寫了迴圈方便反覆輸入密碼查詢，若沒出現在 HIBP 資料庫，則程式會用長度，字元組合複雜度(數字、大小寫、有無符號)簡單推算，如果密碼用 SHA256 保存，駭客買張 NVIDIA RTX 4090 顯卡，要花多久時間可以破解你的密碼。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638357457706641648.png"></p> <p>實測後你會發現，ABCD1234、qwerasdf 這類簡單拼湊密碼幾乎全部都被人用過了，甚至像「我的密碼」注音符號輸入 - ji32k7au4a83 看似難猜，但它出現 249 次。ABCDEabcde1234 長達 14 碼的簡單組合照樣有人用過。14 碼隨機純數字，就算沒人用過，暴力破解只需一小時。但含大小寫短文字拼接 - TueFriTueSun 的表現挺不錯，用四位數字轉星期，只需 12 碼，好記又難破。(但這招既然說破了，已不再安全 XD)</p> <p>查詢完成，密碼輸入直接按 Enter 即可結束程式。</p> <p>最後來驗證 PowerShell 的跨平台特性，在 Linux / MacOS 實測一下。</p> <p>Linux OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638357457708723412.png"></p> <p>MacOS OK!</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638357457710669944.png"></p> <p>工具箱再添實用小工具一枚。</p> <p data-slug="pwd-hibp-check" data-title-en="PowerShell to check password with HIBP API safely" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A handy PowerShell tool to call HIBP API to check if password exists in breach password database.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638357457698896864.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638357457698896864.png" medium="image"/>
    <pubDate>Thu, 16 Nov 2023 15:26:55 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 計算不同密碼長度與複雜度所需暴力破解時間]]></title>
    <link>https://blog.darkthread.net/blog/pwd-crack-time/</link>
    <itunes:title><![CDATA[Coding4Fun - 計算不同密碼長度與複雜度所需暴力破解時間]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>前陣子參加一個 SHA1 暴力破解挑戰，寫了 .NET 程式用上 Parallel.ForEach 平行處理，幾經調校將嘗試次數推上每秒 300 萬次， 64 顆 CPU 齊上 85% 真是壯觀，是次有趣體驗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638356571416462406.png"></p> <p>這類大量破解運算用 CPU 跑吃力不討好。但決定入手<a href="/blog/deskmini-i5/">迷你電腦</a>當工作機的那一刻，便註定跟高階顯卡無緣，當時覺得自己用不到圖形顯示不必浪費錢浪費電浪費空間，這一年來發現機器學習、密碼學領域有好多地方 CUDA 平行運算可以派上用場，好像錯過了一些好玩的東西。</p> <p>我的不專業暴力 SHA1 破解程式配 64 核 CPU 每秒可以算 300 萬筆 SHA1，如果換成目前常見入門獨顯 RTX 4070、甚至消費市場封頂的 RTX 4090，可以跑出什麼樣的成績。最專業的密碼<del>破解</del>還原軟體莫過於 <a href="https://hashcat.net/hashcat/">Hashcat</a>，其支援 CUDA，網路上可以找到不少效能測試數字，隨便查了一下得到答案： <a href="https://hashcat.net/forum/thread-11394.html">4070 參考</a>、<a href="https://hashcat.net/forum/thread-11277.html">4090 參考</a></p> <table><thead><tr><th>顯卡</th><th>MD5</th><th>SHA1</th><th>SHA2-256</th></tr></thead><tbody><tr><td>NVIDIA RTX 4070</td><td>56165.1 MH/s</td><td>18456.5 MH/s</td><td>7933.2 MH/s</td></tr><tr><td>NVIDIA RTX 4090</td><td>155.9 GH/s</td><td>50894.8 MH/s</td><td>21791.7 MH/s</td></tr></tbody></table> <p>嘿，不多不多，是我 CPU 版程式的 17,000 倍而已 (RTX 有 16394 CUDA 核心) XD</p> <p>每秒嘗試次數已知，由組成字元複雜度可推算可能組合(例如：12 碼純數字是 10<sup>12，8 碼大小寫混合是 52</sup>8)，若不考慮使用已知密碼表及字典攻擊，嘗試所有可能組合做暴力攻擊，二者相除就可以算出所需秒數，可以生出一張不同密碼複雜度與長度所需暴力破解時間，感覺挺有趣的。於是乎我就用 PowerShell 寫了一個，之前可以計算無限位整數的 <a href="/blog/biginteger/">BigInteger</a> 派上用場，程式範例如下：</p> <pre><code>$title = 'NVIDIA RTX 4090 / SHA256'
$hitsPerSec = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$complexity = [ordered]@{
'純數字'       = '0';
'全大寫/全小寫'   = 'A';
'大小寫混合'     = 'Aa';
'大小寫+數字'    = 'Aa0';
'大小寫+數字+符號' = 'Aa0!';
}
$html = @"
&lt;style&gt;
table { border-collapse: collapse; text-align: center; font-size: 9pt; }
td, th { border: 1px solid #888; padding: 0.5em; }
thead th { background-color: #ccc; }
tbody td { text-align: right; width: 8em; padding: 0.5em 1em; }
tbody td:first-child { text-align: center; width: 3em; background-color: #ccc; }
.hps { margin-left: 2em; color: blue; }
&lt;/style&gt;
&lt;h3&gt;
$title &lt;span class=hps&gt;$($hitsPerSec.ToString('n0')) 次/秒&lt;/span&gt;
&lt;/h3&gt;
&lt;table&gt;&lt;thead&gt;
&lt;tr&gt;&lt;th&gt;長度&lt;/th&gt;
$($complexity.Keys | ForEach-Object { "&lt;th&gt;$_&lt;/th&gt;" })&lt;/tr&gt;
&lt;/tr&gt;
&lt;/thead&gt;&lt;tbody&gt;
"@
$units = @('', '萬', '億')
For ($len = 6; $len -lt 25; $len++) {
$html += "&lt;tr&gt;&lt;td&gt;$len&lt;/td&gt;"
$complexity.Keys | ForEach-Object {
$pattern = $complexity.$_
$charCount = 0;
if ($pattern -match '[0-9]') { $charCount += 10; }
if ($pattern -cmatch '[a-z]') { $charCount += 26; }
if ($pattern -cmatch '[A-Z]') { $charCount += 26; }
if ($pattern -match '[^0-9a-zA-Z]') { $charCount += 32; }
#Write-Host $_ $charCount
[bigint]$count = [bigint]::Pow($charCount, $len)
$secs = [bigint]::Divide($count, $hitsPerSec)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$years = [bigint]::Divide($secs, 86400 * 365)
$unitIdx = 0
$unit = $units[$unitIdx]
while ($years -gt 10000 -and $unitIdx -lt $units.Length - 1) {
$years = [bigint]::Divide($years, 10000)
$unit = $units[++$unitIdx]
}
if ($years -gt 100000000) {
$years = $years.ToString('e')
$m = [regex]::Match($years, '(?&lt;n&gt;\d.\d+)e\+(?&lt;t&gt;\d+)')
$years = [float]::Parse($m.Groups['n'].Value).ToString('n2') + ' x 10&lt;sup&gt;' + [int]::Parse($m.Groups['t'].Value) + '&lt;/sup&gt;'
}
else {
$years = $years.ToString('n0')
}
$time = $years + ' ' + $unit + '年'
}
$html += "&lt;td&gt;$time&lt;/td&gt;"
}
$html += "&lt;/tr&gt;"
}
$html += "&lt;/tbody&gt;&lt;/table&gt;"
$tempFilePath = [System.IO.Path]::GetTempFileName() + ".html"
$html | Out-File $tempFilePath -Encoding UTF8
Start-Process $tempFilePath
</code></pre> <p>如此，我們就能得到一張「以家用電腦暴力破解不同長度與複雜度密碼 SHA256 所需時間表」：(希望沒計算錯誤 XD)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638356571420211127.png"></p> <p>以當今的硬體算力，八個字元以下的密碼幾乎都像豆腐毫無防禦力；純數字密碼除非加長到 20 碼，不然也像砲灰；當今有一派主張密碼不要強迫混用特殊符號，用多個容易記憶的短單字或縮寫拼裝，讓密碼拉長到 12 到 14 碼，即使只用大小寫無數字，也有 568 年到 153 萬年的強度，混入數字則可再拉高至 4694 年到 1804 萬年；反觀用上大小寫數字加符號的最高複雜度，若只有短短 8 碼，用 4090 跑三天便能破防。短一寸、險一寸，這年頭密碼要安全，愈長愈保險。</p> <p data-slug="pwd-crack-time" data-title-en="Using PowerShell to calculate time required to crack password hash" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A simple PowerShell example to calculate to time required to brute-force crack password hash.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>前陣子參加一個 SHA1 暴力破解挑戰，寫了 .NET 程式用上 Parallel.ForEach 平行處理，幾經調校將嘗試次數推上每秒 300 萬次， 64 顆 CPU 齊上 85% 真是壯觀，是次有趣體驗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638356571416462406.png"></p> <p>這類大量破解運算用 CPU 跑吃力不討好。但決定入手<a href="/blog/deskmini-i5/">迷你電腦</a>當工作機的那一刻，便註定跟高階顯卡無緣，當時覺得自己用不到圖形顯示不必浪費錢浪費電浪費空間，這一年來發現機器學習、密碼學領域有好多地方 CUDA 平行運算可以派上用場，好像錯過了一些好玩的東西。</p> <p>我的不專業暴力 SHA1 破解程式配 64 核 CPU 每秒可以算 300 萬筆 SHA1，如果換成目前常見入門獨顯 RTX 4070、甚至消費市場封頂的 RTX 4090，可以跑出什麼樣的成績。最專業的密碼<del>破解</del>還原軟體莫過於 <a href="https://hashcat.net/hashcat/">Hashcat</a>，其支援 CUDA，網路上可以找到不少效能測試數字，隨便查了一下得到答案： <a href="https://hashcat.net/forum/thread-11394.html">4070 參考</a>、<a href="https://hashcat.net/forum/thread-11277.html">4090 參考</a></p> <table><thead><tr><th>顯卡</th><th>MD5</th><th>SHA1</th><th>SHA2-256</th></tr></thead><tbody><tr><td>NVIDIA RTX 4070</td><td>56165.1 MH/s</td><td>18456.5 MH/s</td><td>7933.2 MH/s</td></tr><tr><td>NVIDIA RTX 4090</td><td>155.9 GH/s</td><td>50894.8 MH/s</td><td>21791.7 MH/s</td></tr></tbody></table> <p>嘿，不多不多，是我 CPU 版程式的 17,000 倍而已 (RTX 有 16394 CUDA 核心) XD</p> <p>每秒嘗試次數已知，由組成字元複雜度可推算可能組合(例如：12 碼純數字是 10<sup>12，8 碼大小寫混合是 52</sup>8)，若不考慮使用已知密碼表及字典攻擊，嘗試所有可能組合做暴力攻擊，二者相除就可以算出所需秒數，可以生出一張不同密碼複雜度與長度所需暴力破解時間，感覺挺有趣的。於是乎我就用 PowerShell 寫了一個，之前可以計算無限位整數的 <a href="/blog/biginteger/">BigInteger</a> 派上用場，程式範例如下：</p> <pre><code>$title = 'NVIDIA RTX 4090 / SHA256'
$hitsPerSec = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$complexity = [ordered]@{
'純數字'       = '0';
'全大寫/全小寫'   = 'A';
'大小寫混合'     = 'Aa';
'大小寫+數字'    = 'Aa0';
'大小寫+數字+符號' = 'Aa0!';
}
$html = @"
&lt;style&gt;
table { border-collapse: collapse; text-align: center; font-size: 9pt; }
td, th { border: 1px solid #888; padding: 0.5em; }
thead th { background-color: #ccc; }
tbody td { text-align: right; width: 8em; padding: 0.5em 1em; }
tbody td:first-child { text-align: center; width: 3em; background-color: #ccc; }
.hps { margin-left: 2em; color: blue; }
&lt;/style&gt;
&lt;h3&gt;
$title &lt;span class=hps&gt;$($hitsPerSec.ToString('n0')) 次/秒&lt;/span&gt;
&lt;/h3&gt;
&lt;table&gt;&lt;thead&gt;
&lt;tr&gt;&lt;th&gt;長度&lt;/th&gt;
$($complexity.Keys | ForEach-Object { "&lt;th&gt;$_&lt;/th&gt;" })&lt;/tr&gt;
&lt;/tr&gt;
&lt;/thead&gt;&lt;tbody&gt;
"@
$units = @('', '萬', '億')
For ($len = 6; $len -lt 25; $len++) {
$html += "&lt;tr&gt;&lt;td&gt;$len&lt;/td&gt;"
$complexity.Keys | ForEach-Object {
$pattern = $complexity.$_
$charCount = 0;
if ($pattern -match '[0-9]') { $charCount += 10; }
if ($pattern -cmatch '[a-z]') { $charCount += 26; }
if ($pattern -cmatch '[A-Z]') { $charCount += 26; }
if ($pattern -match '[^0-9a-zA-Z]') { $charCount += 32; }
#Write-Host $_ $charCount
[bigint]$count = [bigint]::Pow($charCount, $len)
$secs = [bigint]::Divide($count, $hitsPerSec)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$years = [bigint]::Divide($secs, 86400 * 365)
$unitIdx = 0
$unit = $units[$unitIdx]
while ($years -gt 10000 -and $unitIdx -lt $units.Length - 1) {
$years = [bigint]::Divide($years, 10000)
$unit = $units[++$unitIdx]
}
if ($years -gt 100000000) {
$years = $years.ToString('e')
$m = [regex]::Match($years, '(?&lt;n&gt;\d.\d+)e\+(?&lt;t&gt;\d+)')
$years = [float]::Parse($m.Groups['n'].Value).ToString('n2') + ' x 10&lt;sup&gt;' + [int]::Parse($m.Groups['t'].Value) + '&lt;/sup&gt;'
}
else {
$years = $years.ToString('n0')
}
$time = $years + ' ' + $unit + '年'
}
$html += "&lt;td&gt;$time&lt;/td&gt;"
}
$html += "&lt;/tr&gt;"
}
$html += "&lt;/tbody&gt;&lt;/table&gt;"
$tempFilePath = [System.IO.Path]::GetTempFileName() + ".html"
$html | Out-File $tempFilePath -Encoding UTF8
Start-Process $tempFilePath
</code></pre> <p>如此，我們就能得到一張「以家用電腦暴力破解不同長度與複雜度密碼 SHA256 所需時間表」：(希望沒計算錯誤 XD)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638356571420211127.png"></p> <p>以當今的硬體算力，八個字元以下的密碼幾乎都像豆腐毫無防禦力；純數字密碼除非加長到 20 碼，不然也像砲灰；當今有一派主張密碼不要強迫混用特殊符號，用多個容易記憶的短單字或縮寫拼裝，讓密碼拉長到 12 到 14 碼，即使只用大小寫無數字，也有 568 年到 153 萬年的強度，混入數字則可再拉高至 4694 年到 1804 萬年；反觀用上大小寫數字加符號的最高複雜度，若只有短短 8 碼，用 4090 跑三天便能破防。短一寸、險一寸，這年頭密碼要安全，愈長愈保險。</p> <p data-slug="pwd-crack-time" data-title-en="Using PowerShell to calculate time required to crack password hash" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A simple PowerShell example to calculate to time required to brute-force crack password hash.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>前陣子參加一個 SHA1 暴力破解挑戰，寫了 .NET 程式用上 Parallel.ForEach 平行處理，幾經調校將嘗試次數推上每秒 300 萬次， 64 顆 CPU 齊上 85% 真是壯觀，是次有趣體驗：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638356571416462406.png"></p> <p>這類大量破解運算用 CPU 跑吃力不討好。但決定入手<a href="/blog/deskmini-i5/">迷你電腦</a>當工作機的那一刻，便註定跟高階顯卡無緣，當時覺得自己用不到圖形顯示不必浪費錢浪費電浪費空間，這一年來發現機器學習、密碼學領域有好多地方 CUDA 平行運算可以派上用場，好像錯過了一些好玩的東西。</p> <p>我的不專業暴力 SHA1 破解程式配 64 核 CPU 每秒可以算 300 萬筆 SHA1，如果換成目前常見入門獨顯 RTX 4070、甚至消費市場封頂的 RTX 4090，可以跑出什麼樣的成績。最專業的密碼<del>破解</del>還原軟體莫過於 <a href="https://hashcat.net/hashcat/">Hashcat</a>，其支援 CUDA，網路上可以找到不少效能測試數字，隨便查了一下得到答案： <a href="https://hashcat.net/forum/thread-11394.html">4070 參考</a>、<a href="https://hashcat.net/forum/thread-11277.html">4090 參考</a></p> <table><thead><tr><th>顯卡</th><th>MD5</th><th>SHA1</th><th>SHA2-256</th></tr></thead><tbody><tr><td>NVIDIA RTX 4070</td><td>56165.1 MH/s</td><td>18456.5 MH/s</td><td>7933.2 MH/s</td></tr><tr><td>NVIDIA RTX 4090</td><td>155.9 GH/s</td><td>50894.8 MH/s</td><td>21791.7 MH/s</td></tr></tbody></table> <p>嘿，不多不多，是我 CPU 版程式的 17,000 倍而已 (RTX 有 16394 CUDA 核心) XD</p> <p>每秒嘗試次數已知，由組成字元複雜度可推算可能組合(例如：12 碼純數字是 10<sup>12，8 碼大小寫混合是 52</sup>8)，若不考慮使用已知密碼表及字典攻擊，嘗試所有可能組合做暴力攻擊，二者相除就可以算出所需秒數，可以生出一張不同密碼複雜度與長度所需暴力破解時間，感覺挺有趣的。於是乎我就用 PowerShell 寫了一個，之前可以計算無限位整數的 <a href="/blog/biginteger/">BigInteger</a> 派上用場，程式範例如下：</p> <pre><code>$title = 'NVIDIA RTX 4090 / SHA256'
$hitsPerSec = 21791700000; #SHA2-256 21791.7 MH/s https://hashcat.net/forum/thread-11277.html
$complexity = [ordered]@{
'純數字'       = '0';
'全大寫/全小寫'   = 'A';
'大小寫混合'     = 'Aa';
'大小寫+數字'    = 'Aa0';
'大小寫+數字+符號' = 'Aa0!';
}
$html = @"
&lt;style&gt;
table { border-collapse: collapse; text-align: center; font-size: 9pt; }
td, th { border: 1px solid #888; padding: 0.5em; }
thead th { background-color: #ccc; }
tbody td { text-align: right; width: 8em; padding: 0.5em 1em; }
tbody td:first-child { text-align: center; width: 3em; background-color: #ccc; }
.hps { margin-left: 2em; color: blue; }
&lt;/style&gt;
&lt;h3&gt;
$title &lt;span class=hps&gt;$($hitsPerSec.ToString('n0')) 次/秒&lt;/span&gt;
&lt;/h3&gt;
&lt;table&gt;&lt;thead&gt;
&lt;tr&gt;&lt;th&gt;長度&lt;/th&gt;
$($complexity.Keys | ForEach-Object { "&lt;th&gt;$_&lt;/th&gt;" })&lt;/tr&gt;
&lt;/tr&gt;
&lt;/thead&gt;&lt;tbody&gt;
"@
$units = @('', '萬', '億')
For ($len = 6; $len -lt 25; $len++) {
$html += "&lt;tr&gt;&lt;td&gt;$len&lt;/td&gt;"
$complexity.Keys | ForEach-Object {
$pattern = $complexity.$_
$charCount = 0;
if ($pattern -match '[0-9]') { $charCount += 10; }
if ($pattern -cmatch '[a-z]') { $charCount += 26; }
if ($pattern -cmatch '[A-Z]') { $charCount += 26; }
if ($pattern -match '[^0-9a-zA-Z]') { $charCount += 32; }
#Write-Host $_ $charCount
[bigint]$count = [bigint]::Pow($charCount, $len)
$secs = [bigint]::Divide($count, $hitsPerSec)
if ($secs -lt 60) { $time = "$secs 秒" }
elseif ($secs -lt 3600) { $time = "$([bigint]::Divide($secs, 60)) 分鐘" }
elseif ($secs -lt 86400) { $time = "$([bigint]::Divide($secs,3600)) 小時" }
elseif ($secs -lt 86400 * 365) { $time = "$([bigint]::Divide($secs,86400)) 天" }
else {
$years = [bigint]::Divide($secs, 86400 * 365)
$unitIdx = 0
$unit = $units[$unitIdx]
while ($years -gt 10000 -and $unitIdx -lt $units.Length - 1) {
$years = [bigint]::Divide($years, 10000)
$unit = $units[++$unitIdx]
}
if ($years -gt 100000000) {
$years = $years.ToString('e')
$m = [regex]::Match($years, '(?&lt;n&gt;\d.\d+)e\+(?&lt;t&gt;\d+)')
$years = [float]::Parse($m.Groups['n'].Value).ToString('n2') + ' x 10&lt;sup&gt;' + [int]::Parse($m.Groups['t'].Value) + '&lt;/sup&gt;'
}
else {
$years = $years.ToString('n0')
}
$time = $years + ' ' + $unit + '年'
}
$html += "&lt;td&gt;$time&lt;/td&gt;"
}
$html += "&lt;/tr&gt;"
}
$html += "&lt;/tbody&gt;&lt;/table&gt;"
$tempFilePath = [System.IO.Path]::GetTempFileName() + ".html"
$html | Out-File $tempFilePath -Encoding UTF8
Start-Process $tempFilePath
</code></pre> <p>如此，我們就能得到一張「以家用電腦暴力破解不同長度與複雜度密碼 SHA256 所需時間表」：(希望沒計算錯誤 XD)</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638356571420211127.png"></p> <p>以當今的硬體算力，八個字元以下的密碼幾乎都像豆腐毫無防禦力；純數字密碼除非加長到 20 碼，不然也像砲灰；當今有一派主張密碼不要強迫混用特殊符號，用多個容易記憶的短單字或縮寫拼裝，讓密碼拉長到 12 到 14 碼，即使只用大小寫無數字，也有 568 年到 153 萬年的強度，混入數字則可再拉高至 4694 年到 1804 萬年；反觀用上大小寫數字加符號的最高複雜度，若只有短短 8 碼，用 4090 跑三天便能破防。短一寸、險一寸，這年頭密碼要安全，愈長愈保險。</p> <p data-slug="pwd-crack-time" data-title-en="Using PowerShell to calculate time required to crack password hash" data-catg=".NET" data-fbimg="https://images.pexels.com/photos/1089438/pexels-photo-1089438.jpeg?auto=compress&amp;cs=tinysrgb&amp;dpr=2&amp;h=650&amp;w=940#940#650">A simple PowerShell example to calculate to time required to brute-force crack password hash.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638356571416462406.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638356571416462406.png" medium="image"/>
    <pubDate>Wed, 15 Nov 2023 14:55:42 GMT</pubDate>
  </item><item>
    <title><![CDATA[PowerShell 命令加了 -ErrorAction SilentlyContinue 照樣出錯]]></title>
    <link>https://blog.darkthread.net/blog/ps-slientlycontinue-not-work/</link>
    <itunes:title><![CDATA[PowerShell 命令加了 -ErrorAction SilentlyContinue 照樣出錯]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>PowerShell 有個 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.core/about/about_commonparameters?view=powershell-7.3&amp;WT.mc_id=DOP-MVP-37580#-erroraction"><code>-ErrorAction SilentlyContinue</code> 參數</a>可在出錯時隱藏錯誤訊息，並繼續執行命令。</p> <p>今天發現，ErrorAction 參數不是對所有命令都有效，以下是個簡單測試，用 Get-LocalGroup 跟 Get-ADGroup 查詢本機及 AD 群組，故意輸入不存在的群組名稱會出現錯誤訊息。Get-LocalGroup 可加上 -ErrorAction SilentlyContinue 不顯示錯誤，但 Get-ADGroup 加了卻沒效。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638355728967818218.png"></p> <p>查了文件發現，PowerShell 命令有兩種拋出錯誤的方式，<a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.cmdlet.throwterminatingerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#remarks">Cmdlet.ThrowTerminatingError(ErrorRecord)</a> 跟 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.icommandruntime.writeerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#system-management-automation-icommandruntime-writeerror(system-management-automation-errorrecord)">ICommandRuntime.WriteError(ErrorRecord)</a>，ErrorAction 參數只對後者有效，前者會中斷程式，但可以用 Try Catch 捕捉例外，所以真要出錯時繼續，用 Try Catch 包起來就對了。</p> <pre><code>Try {
$Grp = Get-ADGroup 'NoSuchGroup'
}
Catch {
# NOT FOUND OR OTHER ERROR
}
</code></pre> <p>好奇心起，追了一下原始碼找到 Get-ADGroup 是用 ThrowTerminatingError 的證據：</p> <p>Microsoft.ActiveDirectory.Management.Commands.ADCmdletBase 型別</p> <pre><code>internal virtual void ProcessError(Exception e)
{
if (e is RuntimeException)
{
throw e;
}
bool flag = true;
this._recordExceptionHandler.ProcessingRecord = this._processingRecord;
if (this != null &amp;&amp; ((IADCustomExceptionFiltering)this).ExceptionFilter.FilterException(e, ref flag) &amp;&amp; !flag)
{
this.WriteErrorBuffered(this.ConstructErrorRecord(e));
return;
}
base.ThrowTerminatingError(this.ConstructErrorRecord(e));
}
</code></pre> <p>Micrsoft.PowerShell.LocalAccounts.GetLocalGroupCommand 則是用 WriteError：</p> <pre><code>private void ProcessNames()
{
if (this.Name != null)
{
foreach (string text in this.Name)
{
try
{
if (WildcardPattern.ContainsWildcardCharacters(text))
{
WildcardPattern pattern = new WildcardPattern(text, WildcardOptions.Compiled | WildcardOptions.IgnoreCase);
Sam sam = this.sam;
Predicate&lt;string&gt; pred;
Predicate&lt;string&gt; &lt;&gt;9__0;
if ((pred = &lt;&gt;9__0) == null)
{
pred = (&lt;&gt;9__0 = ((string n) =&gt; pattern.IsMatch(n)));
}
using (IEnumerator&lt;LocalGroup&gt; enumerator = sam.GetMatchingLocalGroups(pred).GetEnumerator())
{
while (enumerator.MoveNext())
{
LocalGroup sendToPipeline = enumerator.Current;
base.WriteObject(sendToPipeline);
}
goto IL_A7;
}
}
base.WriteObject(this.sam.GetLocalGroup(text));
IL_A7:;
}
catch (Exception ex)
{
base.WriteError(ex.MakeErrorRecord(null));
}
}
}
}
</code></pre> <p>打完收工。</p> <p data-slug="ps-slientlycontinue-not-work" data-title-en="Why ErrorAction SilentlyContinue doen't work?" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2014/01/03/01/54/recycle-237874_960_720.jpg#960#720">The reason why ErrorAction SilentlyContinue doesn't work on some cmdlets is because it depends on whether the cmdlets use WriteError or ThrowTerminatingError. For the latter, you need to use a try-catch block.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>PowerShell 有個 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.core/about/about_commonparameters?view=powershell-7.3&amp;WT.mc_id=DOP-MVP-37580#-erroraction"><code>-ErrorAction SilentlyContinue</code> 參數</a>可在出錯時隱藏錯誤訊息，並繼續執行命令。</p> <p>今天發現，ErrorAction 參數不是對所有命令都有效，以下是個簡單測試，用 Get-LocalGroup 跟 Get-ADGroup 查詢本機及 AD 群組，故意輸入不存在的群組名稱會出現錯誤訊息。Get-LocalGroup 可加上 -ErrorAction SilentlyContinue 不顯示錯誤，但 Get-ADGroup 加了卻沒效。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638355728967818218.png"></p> <p>查了文件發現，PowerShell 命令有兩種拋出錯誤的方式，<a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.cmdlet.throwterminatingerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#remarks">Cmdlet.ThrowTerminatingError(ErrorRecord)</a> 跟 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.icommandruntime.writeerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#system-management-automation-icommandruntime-writeerror(system-management-automation-errorrecord)">ICommandRuntime.WriteError(ErrorRecord)</a>，ErrorAction 參數只對後者有效，前者會中斷程式，但可以用 Try Catch 捕捉例外，所以真要出錯時繼續，用 Try Catch 包起來就對了。</p> <pre><code>Try {
$Grp = Get-ADGroup 'NoSuchGroup'
}
Catch {
# NOT FOUND OR OTHER ERROR
}
</code></pre> <p>好奇心起，追了一下原始碼找到 Get-ADGroup 是用 ThrowTerminatingError 的證據：</p> <p>Microsoft.ActiveDirectory.Management.Commands.ADCmdletBase 型別</p> <pre><code>internal virtual void ProcessError(Exception e)
{
if (e is RuntimeException)
{
throw e;
}
bool flag = true;
this._recordExceptionHandler.ProcessingRecord = this._processingRecord;
if (this != null &amp;&amp; ((IADCustomExceptionFiltering)this).ExceptionFilter.FilterException(e, ref flag) &amp;&amp; !flag)
{
this.WriteErrorBuffered(this.ConstructErrorRecord(e));
return;
}
base.ThrowTerminatingError(this.ConstructErrorRecord(e));
}
</code></pre> <p>Micrsoft.PowerShell.LocalAccounts.GetLocalGroupCommand 則是用 WriteError：</p> <pre><code>private void ProcessNames()
{
if (this.Name != null)
{
foreach (string text in this.Name)
{
try
{
if (WildcardPattern.ContainsWildcardCharacters(text))
{
WildcardPattern pattern = new WildcardPattern(text, WildcardOptions.Compiled | WildcardOptions.IgnoreCase);
Sam sam = this.sam;
Predicate&lt;string&gt; pred;
Predicate&lt;string&gt; &lt;&gt;9__0;
if ((pred = &lt;&gt;9__0) == null)
{
pred = (&lt;&gt;9__0 = ((string n) =&gt; pattern.IsMatch(n)));
}
using (IEnumerator&lt;LocalGroup&gt; enumerator = sam.GetMatchingLocalGroups(pred).GetEnumerator())
{
while (enumerator.MoveNext())
{
LocalGroup sendToPipeline = enumerator.Current;
base.WriteObject(sendToPipeline);
}
goto IL_A7;
}
}
base.WriteObject(this.sam.GetLocalGroup(text));
IL_A7:;
}
catch (Exception ex)
{
base.WriteError(ex.MakeErrorRecord(null));
}
}
}
}
</code></pre> <p>打完收工。</p> <p data-slug="ps-slientlycontinue-not-work" data-title-en="Why ErrorAction SilentlyContinue doen't work?" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2014/01/03/01/54/recycle-237874_960_720.jpg#960#720">The reason why ErrorAction SilentlyContinue doesn't work on some cmdlets is because it depends on whether the cmdlets use WriteError or ThrowTerminatingError. For the latter, you need to use a try-catch block.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>PowerShell 有個 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.core/about/about_commonparameters?view=powershell-7.3&amp;WT.mc_id=DOP-MVP-37580#-erroraction"><code>-ErrorAction SilentlyContinue</code> 參數</a>可在出錯時隱藏錯誤訊息，並繼續執行命令。</p> <p>今天發現，ErrorAction 參數不是對所有命令都有效，以下是個簡單測試，用 Get-LocalGroup 跟 Get-ADGroup 查詢本機及 AD 群組，故意輸入不存在的群組名稱會出現錯誤訊息。Get-LocalGroup 可加上 -ErrorAction SilentlyContinue 不顯示錯誤，但 Get-ADGroup 加了卻沒效。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638355728967818218.png"></p> <p>查了文件發現，PowerShell 命令有兩種拋出錯誤的方式，<a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.cmdlet.throwterminatingerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#remarks">Cmdlet.ThrowTerminatingError(ErrorRecord)</a> 跟 <a href="https://learn.microsoft.com/en-us/dotnet/api/system.management.automation.icommandruntime.writeerror?view=powershellsdk-7.3.0&amp;WT.mc_id=DOP-MVP-37580#system-management-automation-icommandruntime-writeerror(system-management-automation-errorrecord)">ICommandRuntime.WriteError(ErrorRecord)</a>，ErrorAction 參數只對後者有效，前者會中斷程式，但可以用 Try Catch 捕捉例外，所以真要出錯時繼續，用 Try Catch 包起來就對了。</p> <pre><code>Try {
$Grp = Get-ADGroup 'NoSuchGroup'
}
Catch {
# NOT FOUND OR OTHER ERROR
}
</code></pre> <p>好奇心起，追了一下原始碼找到 Get-ADGroup 是用 ThrowTerminatingError 的證據：</p> <p>Microsoft.ActiveDirectory.Management.Commands.ADCmdletBase 型別</p> <pre><code>internal virtual void ProcessError(Exception e)
{
if (e is RuntimeException)
{
throw e;
}
bool flag = true;
this._recordExceptionHandler.ProcessingRecord = this._processingRecord;
if (this != null &amp;&amp; ((IADCustomExceptionFiltering)this).ExceptionFilter.FilterException(e, ref flag) &amp;&amp; !flag)
{
this.WriteErrorBuffered(this.ConstructErrorRecord(e));
return;
}
base.ThrowTerminatingError(this.ConstructErrorRecord(e));
}
</code></pre> <p>Micrsoft.PowerShell.LocalAccounts.GetLocalGroupCommand 則是用 WriteError：</p> <pre><code>private void ProcessNames()
{
if (this.Name != null)
{
foreach (string text in this.Name)
{
try
{
if (WildcardPattern.ContainsWildcardCharacters(text))
{
WildcardPattern pattern = new WildcardPattern(text, WildcardOptions.Compiled | WildcardOptions.IgnoreCase);
Sam sam = this.sam;
Predicate&lt;string&gt; pred;
Predicate&lt;string&gt; &lt;&gt;9__0;
if ((pred = &lt;&gt;9__0) == null)
{
pred = (&lt;&gt;9__0 = ((string n) =&gt; pattern.IsMatch(n)));
}
using (IEnumerator&lt;LocalGroup&gt; enumerator = sam.GetMatchingLocalGroups(pred).GetEnumerator())
{
while (enumerator.MoveNext())
{
LocalGroup sendToPipeline = enumerator.Current;
base.WriteObject(sendToPipeline);
}
goto IL_A7;
}
}
base.WriteObject(this.sam.GetLocalGroup(text));
IL_A7:;
}
catch (Exception ex)
{
base.WriteError(ex.MakeErrorRecord(null));
}
}
}
}
</code></pre> <p>打完收工。</p> <p data-slug="ps-slientlycontinue-not-work" data-title-en="Why ErrorAction SilentlyContinue doen't work?" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2014/01/03/01/54/recycle-237874_960_720.jpg#960#720">The reason why ErrorAction SilentlyContinue doesn't work on some cmdlets is because it depends on whether the cmdlets use WriteError or ThrowTerminatingError. For the latter, you need to use a try-catch block.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638355728967818218.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638355728967818218.png" medium="image"/>
    <pubDate>Tue, 14 Nov 2023 15:34:21 GMT</pubDate>
  </item><item>
    <title><![CDATA[「機碼用在特定狀態時無效」錯誤與 Windows 無腦加密冷知識]]></title>
    <link>https://blog.darkthread.net/blog/key-not-valid-error-n-dpapi-master-keys/</link>
    <itunes:title><![CDATA[「機碼用在特定狀態時無效」錯誤與 Windows 無腦加密冷知識]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>我寫了個 PowerShell 小工具，用 <a href="/blog/ps-secure-string/">ConvertTo-SecureString/ConvertFrom-SecureString 加密保存資料</a>，背後是靠 Windows DPAPI (Data Protection API) 運作，一套 Windows 內建，與使用者帳號與所在主機整合的加解密機制，不用設密碼或金鑰就能無腦加解密。</p> <p>接到報案，小工具噴出沒看過的「機碼用在特定狀態時無效」錯誤，用中文訊息爬文找到的資料不多，大多是程式安裝過程出錯的案例，由錯誤碼 0x8009000B 查到其原文是 Key not valid for use in specified state.，改用英文訊息爬文，鎖定與錯誤 RSA 加解密有關，而中文錯誤訊息中的「機碼」應是 Key (加密金鑰) 的錯誤翻譯。</p> <p>詢問報案人，原來是最近換新電腦，忘了手冊有提醒加密資料只限原機原帳號使用，從舊電腦複製加密檔到新機器上執行，造成以上錯誤，而我被訊息錯翻的「機碼」一詞迷惑，沒能馬上意會到問題。</p> <p>DPAPI 堪稱 Windows 內建的<a href="/blog/protecteddata/">無腦加解密機制</a>，使用者不用想密碼，Windows 自己有一套安全可靠的簡單方法實現該使用者在這台機器加密的資料，只有該使用者在這台機器上可以解密。加密機制會認帳號及機器，加密資料交給其他使用者或移到其他機器也會失效。(註：但也可設定認機器不限使用者)</p> <p>DPAPI 的背後原理是靠主金鑰(Master Key)加解密，Windows 有自己的系統主金鑰(System Master Key)，每個使用者也有專屬的使用者主金鑰(User Master Key)，使用者主金鑰是用使用者的 Windows 密碼加密保存，因此，必須知道知道密碼才能取出金鑰，因此即便系統管理者強行拿走檔案也解不開。每次變更密碼時，需用原密碼解密使用者主金鑰，再用新密碼加密，因此管理者在不知原密碼的狀況重設密碼，雖然能登入主機仍無法解密，連管理者都能防，有相當的安全性。這意味著 1) 系統管理者無法透過重設密碼手段偷取使用者的加密資料 2) 使用者若自己忘了密碼，連管理者也無法幫忙解開。</p> <p>參考：<a href="https://ftp.zx.net.nz/pub/archive/ftp.microsoft.com/MISC/KB/en-us/309/408.HTM#7">古老微軟文件</a>：</p> <p>The DPAPI is invoked by the Winlogon component during password change operations in an Active Directory domain:</p> <ul><li>DPAPI receives notification from Winlogon during a password change operation.</li> <li>DPAPI decrypts all master keys that were encrypted with the user's old passwords.</li> <li>DPAPI re-encrypts all master keys with the user's new password.</li></ul> <p>A password reset is more complex than a password change. Because the administrator is not logged on as the user and does not have access to the user's old password, that old password cannot be used to decrypt the old master key and re-encrypt it with the new password.</p> <p>最後，我做了小實驗觀察修改密碼時 Windows 重新加密主金鑰的行為。DPAPI 用起來很無腦但官方技術文件很少，理由是正常人不需要知道這麼多，會想摸透通常是想幹壞事的人，因此要靠破解相關文件解惑。個人的主金鑰存放在 <code>C:\Users\&lt;user&gt;\AppData\Roaming\Microsoft\Protect\&lt;SID&gt;</code> 目錄下，存在以 GUID 為名的檔案，是加密過的主金鑰。我用 PowerShell 指令改密碼，每改一次多一個新檔，查了文件發現 Windows 會保存舊金鑰，有個 Preferred 檔指向最新一支。而每次改密碼，所有檔案都會被覆寫更新寫入時間，推測就是重新用使用者密碼加密的行為。有興趣的話可以找到工具剖析檔案內容甚至破解金鑰，但我志不在此，就此打住。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638353896954918646.png"></p> <p>【延伸閱讀】</p> <ul><li><a href="https://www.passcape.com/windows_password_recovery_dpapi_master_key">Windows Password Recovery - DPAPI Master Key analysis</a></li> <li><a href="https://serverfault.com/a/678266/174734">使用 ADSI 更換密碼</a></li> <li><a href="https://www.synacktiv.com/ressources/univershell_2017_dpapi.pdf">DPAPI exploitation during pentest and password cracking</a></li> <li><a href="https://www.insecurity.be/blog/2020/12/24/dpapi-in-depth-with-tooling-standalone-dpapi/">DPAPI-in-depth with tooling: standalone DPAPI</a></li></ul> <p data-slug="key-not-valid-error-n-dpapi-master-keys" data-fbimg="https://cdn.pixabay.com/photo/2018/03/15/20/58/locks-3229455_960_720.jpg#960#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>我寫了個 PowerShell 小工具，用 <a href="/blog/ps-secure-string/">ConvertTo-SecureString/ConvertFrom-SecureString 加密保存資料</a>，背後是靠 Windows DPAPI (Data Protection API) 運作，一套 Windows 內建，與使用者帳號與所在主機整合的加解密機制，不用設密碼或金鑰就能無腦加解密。</p> <p>接到報案，小工具噴出沒看過的「機碼用在特定狀態時無效」錯誤，用中文訊息爬文找到的資料不多，大多是程式安裝過程出錯的案例，由錯誤碼 0x8009000B 查到其原文是 Key not valid for use in specified state.，改用英文訊息爬文，鎖定與錯誤 RSA 加解密有關，而中文錯誤訊息中的「機碼」應是 Key (加密金鑰) 的錯誤翻譯。</p> <p>詢問報案人，原來是最近換新電腦，忘了手冊有提醒加密資料只限原機原帳號使用，從舊電腦複製加密檔到新機器上執行，造成以上錯誤，而我被訊息錯翻的「機碼」一詞迷惑，沒能馬上意會到問題。</p> <p>DPAPI 堪稱 Windows 內建的<a href="/blog/protecteddata/">無腦加解密機制</a>，使用者不用想密碼，Windows 自己有一套安全可靠的簡單方法實現該使用者在這台機器加密的資料，只有該使用者在這台機器上可以解密。加密機制會認帳號及機器，加密資料交給其他使用者或移到其他機器也會失效。(註：但也可設定認機器不限使用者)</p> <p>DPAPI 的背後原理是靠主金鑰(Master Key)加解密，Windows 有自己的系統主金鑰(System Master Key)，每個使用者也有專屬的使用者主金鑰(User Master Key)，使用者主金鑰是用使用者的 Windows 密碼加密保存，因此，必須知道知道密碼才能取出金鑰，因此即便系統管理者強行拿走檔案也解不開。每次變更密碼時，需用原密碼解密使用者主金鑰，再用新密碼加密，因此管理者在不知原密碼的狀況重設密碼，雖然能登入主機仍無法解密，連管理者都能防，有相當的安全性。這意味著 1) 系統管理者無法透過重設密碼手段偷取使用者的加密資料 2) 使用者若自己忘了密碼，連管理者也無法幫忙解開。</p> <p>參考：<a href="https://ftp.zx.net.nz/pub/archive/ftp.microsoft.com/MISC/KB/en-us/309/408.HTM#7">古老微軟文件</a>：</p> <p>The DPAPI is invoked by the Winlogon component during password change operations in an Active Directory domain:</p> <ul><li>DPAPI receives notification from Winlogon during a password change operation.</li> <li>DPAPI decrypts all master keys that were encrypted with the user's old passwords.</li> <li>DPAPI re-encrypts all master keys with the user's new password.</li></ul> <p>A password reset is more complex than a password change. Because the administrator is not logged on as the user and does not have access to the user's old password, that old password cannot be used to decrypt the old master key and re-encrypt it with the new password.</p> <p>最後，我做了小實驗觀察修改密碼時 Windows 重新加密主金鑰的行為。DPAPI 用起來很無腦但官方技術文件很少，理由是正常人不需要知道這麼多，會想摸透通常是想幹壞事的人，因此要靠破解相關文件解惑。個人的主金鑰存放在 <code>C:\Users\&lt;user&gt;\AppData\Roaming\Microsoft\Protect\&lt;SID&gt;</code> 目錄下，存在以 GUID 為名的檔案，是加密過的主金鑰。我用 PowerShell 指令改密碼，每改一次多一個新檔，查了文件發現 Windows 會保存舊金鑰，有個 Preferred 檔指向最新一支。而每次改密碼，所有檔案都會被覆寫更新寫入時間，推測就是重新用使用者密碼加密的行為。有興趣的話可以找到工具剖析檔案內容甚至破解金鑰，但我志不在此，就此打住。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638353896954918646.png"></p> <p>【延伸閱讀】</p> <ul><li><a href="https://www.passcape.com/windows_password_recovery_dpapi_master_key">Windows Password Recovery - DPAPI Master Key analysis</a></li> <li><a href="https://serverfault.com/a/678266/174734">使用 ADSI 更換密碼</a></li> <li><a href="https://www.synacktiv.com/ressources/univershell_2017_dpapi.pdf">DPAPI exploitation during pentest and password cracking</a></li> <li><a href="https://www.insecurity.be/blog/2020/12/24/dpapi-in-depth-with-tooling-standalone-dpapi/">DPAPI-in-depth with tooling: standalone DPAPI</a></li></ul> <p data-slug="key-not-valid-error-n-dpapi-master-keys" data-fbimg="https://cdn.pixabay.com/photo/2018/03/15/20/58/locks-3229455_960_720.jpg#960#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>我寫了個 PowerShell 小工具，用 <a href="/blog/ps-secure-string/">ConvertTo-SecureString/ConvertFrom-SecureString 加密保存資料</a>，背後是靠 Windows DPAPI (Data Protection API) 運作，一套 Windows 內建，與使用者帳號與所在主機整合的加解密機制，不用設密碼或金鑰就能無腦加解密。</p> <p>接到報案，小工具噴出沒看過的「機碼用在特定狀態時無效」錯誤，用中文訊息爬文找到的資料不多，大多是程式安裝過程出錯的案例，由錯誤碼 0x8009000B 查到其原文是 Key not valid for use in specified state.，改用英文訊息爬文，鎖定與錯誤 RSA 加解密有關，而中文錯誤訊息中的「機碼」應是 Key (加密金鑰) 的錯誤翻譯。</p> <p>詢問報案人，原來是最近換新電腦，忘了手冊有提醒加密資料只限原機原帳號使用，從舊電腦複製加密檔到新機器上執行，造成以上錯誤，而我被訊息錯翻的「機碼」一詞迷惑，沒能馬上意會到問題。</p> <p>DPAPI 堪稱 Windows 內建的<a href="/blog/protecteddata/">無腦加解密機制</a>，使用者不用想密碼，Windows 自己有一套安全可靠的簡單方法實現該使用者在這台機器加密的資料，只有該使用者在這台機器上可以解密。加密機制會認帳號及機器，加密資料交給其他使用者或移到其他機器也會失效。(註：但也可設定認機器不限使用者)</p> <p>DPAPI 的背後原理是靠主金鑰(Master Key)加解密，Windows 有自己的系統主金鑰(System Master Key)，每個使用者也有專屬的使用者主金鑰(User Master Key)，使用者主金鑰是用使用者的 Windows 密碼加密保存，因此，必須知道知道密碼才能取出金鑰，因此即便系統管理者強行拿走檔案也解不開。每次變更密碼時，需用原密碼解密使用者主金鑰，再用新密碼加密，因此管理者在不知原密碼的狀況重設密碼，雖然能登入主機仍無法解密，連管理者都能防，有相當的安全性。這意味著 1) 系統管理者無法透過重設密碼手段偷取使用者的加密資料 2) 使用者若自己忘了密碼，連管理者也無法幫忙解開。</p> <p>參考：<a href="https://ftp.zx.net.nz/pub/archive/ftp.microsoft.com/MISC/KB/en-us/309/408.HTM#7">古老微軟文件</a>：</p> <p>The DPAPI is invoked by the Winlogon component during password change operations in an Active Directory domain:</p> <ul><li>DPAPI receives notification from Winlogon during a password change operation.</li> <li>DPAPI decrypts all master keys that were encrypted with the user's old passwords.</li> <li>DPAPI re-encrypts all master keys with the user's new password.</li></ul> <p>A password reset is more complex than a password change. Because the administrator is not logged on as the user and does not have access to the user's old password, that old password cannot be used to decrypt the old master key and re-encrypt it with the new password.</p> <p>最後，我做了小實驗觀察修改密碼時 Windows 重新加密主金鑰的行為。DPAPI 用起來很無腦但官方技術文件很少，理由是正常人不需要知道這麼多，會想摸透通常是想幹壞事的人，因此要靠破解相關文件解惑。個人的主金鑰存放在 <code>C:\Users\&lt;user&gt;\AppData\Roaming\Microsoft\Protect\&lt;SID&gt;</code> 目錄下，存在以 GUID 為名的檔案，是加密過的主金鑰。我用 PowerShell 指令改密碼，每改一次多一個新檔，查了文件發現 Windows 會保存舊金鑰，有個 Preferred 檔指向最新一支。而每次改密碼，所有檔案都會被覆寫更新寫入時間，推測就是重新用使用者密碼加密的行為。有興趣的話可以找到工具剖析檔案內容甚至破解金鑰，但我志不在此，就此打住。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638353896954918646.png"></p> <p>【延伸閱讀】</p> <ul><li><a href="https://www.passcape.com/windows_password_recovery_dpapi_master_key">Windows Password Recovery - DPAPI Master Key analysis</a></li> <li><a href="https://serverfault.com/a/678266/174734">使用 ADSI 更換密碼</a></li> <li><a href="https://www.synacktiv.com/ressources/univershell_2017_dpapi.pdf">DPAPI exploitation during pentest and password cracking</a></li> <li><a href="https://www.insecurity.be/blog/2020/12/24/dpapi-in-depth-with-tooling-standalone-dpapi/">DPAPI-in-depth with tooling: standalone DPAPI</a></li></ul> <p data-slug="key-not-valid-error-n-dpapi-master-keys" data-fbimg="https://cdn.pixabay.com/photo/2018/03/15/20/58/locks-3229455_960_720.jpg#960#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638353896954918646.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638353896954918646.png" medium="image"/>
    <pubDate>Sun, 12 Nov 2023 12:41:10 GMT</pubDate>
  </item><item>
    <title><![CDATA[深入 SQL Parameter Sniffing 與執行計劃效能問題]]></title>
    <link>https://blog.darkthread.net/blog/inside-param-sniffing-n-exec-plan/</link>
    <itunes:title><![CDATA[深入 SQL Parameter Sniffing 與執行計劃效能問題]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>【前言】關於 SQL 執行計劃如何影響查詢效能議題很久之前研究過了(<a href="/blog/index-scan-vs-seek">SQL筆記：Index Scan vs Index Seek</a>、<a href="/blog/literal-var-param-and-exec-plan/">SQL筆記：Literal, Variable與Parameter</a>)，昨天研究 <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>找到 SQL Server MVP Erland Sommarskog 解釋 Parameter Sniffing 議題的經典文章 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，在 Parameter Sniffing 議題上有深入剖析，並附有完整範例、檢測工具，不讀完對不起自己，完食整理筆記如下。<br> (以下假設大家已知道執行計劃、Parameter Sniffing、執行計劃快取、清除快取重新建立時機... 等概念，提到這些名詞時不再多做解釋)</p> <p>開始前先釐清一個觀念， <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>一文提到 SSMS 因 SET ARITHABORT ON 查詢比網站快，不是因為 ARITHABORT 改變查詢行為，純粹是因參數 ARITHABORT ON/OFF 差異導致 SSMS 使用了跟網站不同的執行計劃，而這個執行計劃比網站在用的執行計劃更適合當時的傳入參數，所以查詢效能良好。 換言之，問題的核心在於 SQL 會進行 Parameter Sniffing 依據參數決定最適合執行計劃，當不同參數產生的執行計劃差異很大時，就可能發生用參數用 A 值推算的執行計劃，查詢 A 只要一秒，用來查詢 B 卻要一分鐘。因此，問題出執行計劃對不同參數的執行效能差異過大，ARITHABORT 差異有影響源自改用另一個較沒問題的查詢計劃避開，與 ARITHABORT 本身沒有關係 (引述 Erland <a href="https://learn.microsoft.com/en-us/answers/questions/128817/what-is-arithabort-setting?WT.mc_id=DOP-MVP-37580">說法</a>：Under normal circumstances there is absolutely no difference between ARITHABORT ON and OFF)，真正要解決的是「依參數值 A 產生執行計劃，用來查 B 效能會悲劇」這個問題。而要解決這類問題，必須了解 Parameter Sniffing 與執行計劃的關係，而 Erland 的文章是絕佳敲門磚。</p> <ol><li>當遇到 SP 因為快取的執行計劃不利於特定參數查詢而異常緩慢，最簡單粗暴的解法：<code>EXEC sp_recompile problem_sp</code>，如此可強制清掉快取，於下次查詢時重新產生執行計劃，若問題不再發生，結案。</li> <li>如果不幸問題持續發生，代表你需要找出有問題的執行計劃，分析什麼參數值造成執行計劃不佳並設法改善。</li> <li>偵查 Parameter Sniffing 問題前應先掌握以下情資： <ul><li>是哪個指令慢？</li> <li>快的跟慢的執行計劃分別長什麼樣子？</li> <li>SQL 依據哪些參數決定執行計劃？</li> <li>資料表與索引結構如何？</li> <li>資料分佈統計如何？是否有更新？</li></ul> </li> <li>若狀況為 SSMS 快網站慢，同時取得快、慢執行計劃的方法是分別設 ARITHABORT ON 及 OFF 各跑一次： <pre><code>SET ARITHABORT ON
go
EXEC that_very_sp 4711, 123, 1
go
SET ARITHABORT OFF
go
EXEC that_very_sp 4711, 123, 1
</code></pre> </li> <li>執行計劃最右邊的 SELECT/INSERT... 按右鍵選 Property 找 ParameterList / Parameter Compiled Value 即嗅探值(Sniffed Value)，多半就是 Parameter Sniffing 問題的來源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638352239195576657.png"></li> <li>執行計劃圖還有幾個地方可以留意，Cost [1] 數字愈高愈是瓶頸所在(但要注意它是預測值，有可能失真)，相對的箭頭會愈粗[2]，另外，實際筆數與預測筆數落差[3]也很具參考價值。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638352239197801887.png"></li> <li>在無法使用 SSMS 的場合，以下這段 SQL 可取得執行計劃：(註：需 GRANT VIEW SERVER PERFORMANCE STATE) <pre><code>DECLARE @dbname    nvarchar(256),
@procname  nvarchar(256)
SELECT @dbname = 'Northwind', -- DB 名稱
@procname = 'dbo.List_orders_2' -- SP 名稱
; WITH basedata AS (
SELECT qs.statement_start_offset/2 AS stmt_start,
qs.statement_end_offset/2 AS stmt_end,
est.encrypted AS isencrypted, est.text AS sqltext,
epa.value AS set_options, qp.query_plan,
charindex('&lt;ParameterList&gt;', qp.query_plan) + len('&lt;ParameterList&gt;')
AS paramstart,
charindex('&lt;/ParameterList&gt;', qp.query_plan) AS paramend
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY sys.dm_exec_text_query_plan(qs.plan_handle,
qs.statement_start_offset,
qs.statement_end_offset) qp
CROSS  APPLY sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  est.objectid  = object_id (@procname)
AND  est.dbid      = db_id(@dbname)
AND  epa.attribute = 'set_options'
), next_level AS (
SELECT stmt_start, set_options, query_plan,
CASE WHEN isencrypted = 1 THEN '-- ENCRYPTED'
WHEN stmt_start &gt;= 0
THEN substring(sqltext, stmt_start + 1,
CASE stmt_end
WHEN 0 THEN datalength(sqltext)
ELSE stmt_end - stmt_start + 1
END)
END AS Statement,
CASE WHEN paramend &gt; paramstart
THEN CAST (substring(query_plan, paramstart,
paramend - paramstart) AS xml)
END AS params
FROM   basedata
)
SELECT set_options AS [SET], n.stmt_start AS Pos, n.Statement,
CR.c.value('@Column', 'nvarchar(128)') AS Parameter,
CR.c.value('@ParameterCompiledValue', 'nvarchar(128)') AS [Sniffed Value],
CAST (query_plan AS xml) AS [Query plan]
FROM   next_level n
CROSS  APPLY   n.params.nodes('ColumnReference') AS CR(c)
ORDER  BY n.set_options, n.stmt_start, Parameter
</code></pre> 上述查詢可取得該 SP 的執行計劃(Query Plan 的 XML 用 SSMS 開啟即為執行計劃流程圖)及其對映嗅探值，非常方便：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638352239199824875.png"><br> (SET - ARITHABORT 等選項值組合、POS - 查詢指令在 SP 的起始位置(第幾個字元))</li> <li>以下指令可用來清查資料表及索引： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, i.index_id, i.name, i.type_desc,
substring(ikey.cols, 3, len(ikey.cols)) AS key_cols,
substring(inc.cols, 3, len(inc.cols)) AS included_cols,
stats_date(o.object_id, i.index_id) AS stats_date,
i.filter_definition
FROM   sys.objects o
JOIN   sys.indexes i ON i.object_id = o.object_id
OUTER  APPLY (SELECT ', ' + c.name +
CASE ic.is_descending_key
WHEN 1 THEN ' DESC'
ELSE ''
END
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 0
ORDER  BY ic.key_ordinal
FOR XML PATH('')) AS ikey(cols)
OUTER  APPLY (SELECT ', ' + c.name
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 1
ORDER  BY ic.index_column_id
FOR XML PATH('')) AS inc(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, i.index_id
</code></pre> 資料表的欄位、索引資訊一目了然：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638352239201922600.png"></li> <li>最後是統計資料狀態： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, s.stats_id, s.name, s.auto_created, s.user_created,
substring(scols.cols, 3, len(scols.cols)) AS stat_cols,
stats_date(o.object_id, s.stats_id) AS stats_date,
s.filter_definition
FROM   sys.objects o
JOIN   sys.stats s ON s.object_id = o.object_id
CROSS  APPLY (SELECT ', ' + c.name
FROM   sys.stats_columns sc
JOIN   sys.columns c ON sc.object_id = c.object_id
AND sc.column_id = c.column_id
WHERE  sc.object_id = s.object_id
AND  sc.stats_id  = s.stats_id
ORDER  BY sc.stats_column_id
FOR XML PATH('')) AS scols(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, s.stats_id
</code></pre> stats_date 為最後更新時間，若年代久遠，則有可能是造成 Parameter Sniffing 問題的根源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638352239203911205.png"><br> 若確認統計資料過期，可用 <code>UPDATE STATISTICS tblName</code> 更新，它會以取樣方式更新，一般都能精準反映現況。若取樣更新結果不理想，再嘗試 <code>UPDATE STATISTICS tblName WITH FULL SCAN, INDEX</code> (加 INDEX 只更新索引統計，可縮執行時間)，若只更新特定索引，則可 <code>UPDATE STATISTICS tblName indexName WITH FULLSCAN</code>。更新統計會觸發重新編譯及 Parameter Sniffing，一般可馬上看到成效。</li> <li>有一些情況會導致 Parameter Sniffing 相關效能問題反覆發生，例如： <ol><li>查詢寫法不適合使用 Parameter Sniffing，依據 A 參數值產生的執行計劃用在 B 參數很慘，反之亦然</li> <li>有少數參數對映的執行計劃與其他大部分參數完全不同，若先用它跑 Parameter Sniffing，則後面的查詢只剩一個慘字</li> <li>查詢條件沒有完美的索引可用，但有好幾個可用索引供挑選，挑到不同索引執行計劃效能差異很大 實際狀況很多種，沒有一體適用的解法，需依個案分析解決。</li></ol> </li> <li>因此，在程式加上 <code>SET ARITHABORT ON</code> 並不是真的解決問題，它類似 <code>sp_recompile</code> 會強迫用現在的參數重新產生執行計劃，若 Parameter Sniffing 導致不良執行計劃的狀況仍在，遲早會再發生。</li> <li>案例：查指定客戶特定日期區間的訂單，客戶有很多訂單用 OrderDate Index Seek，若只有一筆用 CustomerId Index Seek：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638352239205934277.png"></li> <li>大絕：在 SP 加上 <code>OPTION (RECOMPILE)</code> 強迫每次重新編譯，犧牲一點效能避開快取執行計劃不適用特定參數值的嚴重問題。但若呼叫頻率很高或 SP 複雜編譯耗時，要小心副作用反撲。</li> <li>常見問題：動態搜尋條件通常不利於 Parameter Sniffing，如 <code>WHERE (CustomerID = @custid OR @custid IS NULL) AND (OrderDate = @orderdate OR @orderdate IS NULL)...</code>，可考慮 <code>OPTION (RECOMPILE)</code>。</li> <li>查索引值分佈可用 <a href="https://learn.microsoft.com/en-us/sql/t-sql/database-console-commands/dbcc-show-statistics-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">DBCC SHOW_STATISTICS</a><br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig7_638352239207900029.png"><br> 以 1996-07-19 為例，RANGE_ROWS = 8 表有 8 筆資料介於 07/08 與 07/19 間(不含)，EQ_ROWS = 2 表有兩筆等於 7/19，DISTINCT_RANGE_ROWS = 8 表是 RANGE_ROWS 有 8 筆不同值，AVG_RANGE_ROWS = RANGE_ROWS / DISTINCT_RANGE_ROWS。</li> <li>案例：索引放在 Col3，Col3 資料分佈不均勻，有些類別只有 17 筆，有些有 421,121 筆 (可用)，理論上不適合用於 Index Seek，但因為第一筆查詢參數是個不存在的分類，其預測值取 1，SQL 認定其是適合的索引，造成悲劇。<br> 幾種解法： <ol><li>OPTION (RECOMPILE)</li> <li>新增完全吻合該查詢條件的專用索引(注意佔用空間及增加資料更新負擔)</li> <li>調整或刪除 Col3 索引(用 <code>sys.dm_db_index_usage_stats</code> 佐證它很廢)</li> <li>加上索引提示，如 <code>JOIN Table_A a WITH (INDEX = Combo_ix) ON a.Col4 = b.Col1</code></li> <li>加上 OPTIMIZE FOR <code>OPTION (OPTIMIZE FOR (@p3 = 'KIWI'))</code></li> <li>將 Col3 比對參數轉成區域變數(避開 Sniffing)</li> <li>調整程式行為(例如：避免先用不存在的分類查詢)</li></ol> </li> <li>SSMS 觀察執行計劃一般要等到執行完，若查詢很耗時，可使用 Live Query Plan 搶先看。(版本需求：SQL2014+/SSMS2016+)<br> 選項位於 Query/Include Live Query Statistics<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig8_638352239209836862.png"><br> 另外一個做法是從 Active Monitor / Active Expensive Queries 找到正在執行中查詢，由右鍵選單開啟：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig9_638352239211846663.png"><br> 除此外 SQL 2016+ 新增了一個 View <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-statistics-xml-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_statistics_xml</code></a> 也可用來取得執行中查詢的執行計劃</li> <li>SQL2019 新加 <code>ALTER DATABASE SCOPED CONFIGURATION SET LAST_QUERY_PLAN_STATS = ON</code> 方式可保存最近的執行計劃。參考：<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-plan-stats-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_plan_stats</code></a></li> <li>用 Profiler 追蹤也可以取得執行計劃，但對效能影響較大，很少會這麼做</li> <li>.NET <code>SqlCommand.Commtext = "SELECT * FROM dbo.Orders WHERE CustomerID = @c"</code> 寫法也會產生執行計劃快取，快取 Key 由查詢語法文字雜湊產生。所以 <code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code>、<code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code> (結尾多一個空白)、<code>SELECT * FROM Orders WHERE CustomerID = @c</code> (拿掉dbo.)、<code>select * from dbo.Orders where CustomerID = @c</code> (大小寫不同)會產生四份執行計劃快取。</li> <li>注意：.NET SqlParameter 未指定長度可能導致多份執行計劃 <pre><code>cmd.CommandText = "SELECT * FROM dbo.Orders WHERE CustomerID = @c";
cmd.Parameters.Add("@c", SqlDbType.NVarChar).Value = TextBox.Value;
--會轉成如下指令，由 TextBox.Value 決定 nvarchar(n)
/*
exec sp_executesql N'SELECT * FROM Orders WHERE CustomerID = @c',
N'@c nvarchar(5)',@c=N'ALFKI'
*/
--字串長度不同，產生的 SQL 語法 nvarchar(n) 的 n 不同，會產生多份執行計劃
--若要避免請指定長度
cmd.Parameters.Add("@c", SqlDbType.NVarChar, 5).Value = TextBox.Value;
</code></pre> </li> <li>不同 DEFAULT_SCHEMA 的使用者不共用執行計劃快取，下圖為 DEFAULT_SCHEMA 為 dbo 或 Schema2 使用者查詢 <code>SELECT * FROM Orders WHERE ...</code> 留下的快取，user_id = 1 for dbo、user_id = 5 for Schema2。若指定 Schema <code>SELECT * FROM dbo.Orders WHERE ...</code>，則 user_id = -2 多使用者可共用執行計劃快取，這是表格、View、UDF 名稱一律加上 Schema 的好處(例：dbo.TableName，但 SP 例外，理由是 SP 會以自己所屬 Schema 為準，不隨使用者改變)，如果你的 DB 有區分 Schema，而非共用 dbo 的話。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig10_638352239214049580.png"></li> <li>Auto-Parameterisation - SQL 將常數自動轉成參數的行為，有兩種模式，Simple、Forced，由資料庫設定決定，預設為 Simple，並只有在 SQL 語句複雜度很低時才啟用(只會產生一種執行計劃)，故不會因 Parameter Sniffing 出問題。Forced 模式則會將所有常數參數化，例如： <pre><code>ALTER DATABASE Northwind SET PARAMETERIZATION FORCED
DBCC FREEPROCCACHE
go
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
SELECT * FROM Orders WHERE OrderDate &gt; '19970101'
go
</code></pre> 在查詢計劃 SQL 語法會轉成 <code>select * from Orders where OrderDate &gt; @0</code>，兩次共用同一個查詢計劃；若設為 Simple 模式，SQL 發現有兩種執行計劃(前者適合 Index Seek + Key Lookup，後者適合 Clusterd Index Scan)，則會停用自動參數化。</li> <li>在 SSMS 模擬應用程式執行 SQL 動作，SET ARITHABORT OFF 之外的小技巧： <ol><li><code>EXECUTE AS USER = 'appuser'</code> 以應用程式使用的帳號執行，結束時 <code>REVERT</code> 復原</li> <li>用以下指令查應用程式送出的 SQL 指令</li></ol> <pre><code>SELECT '&lt;' + est.text + '&gt;'
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
WHERE  est.text LIKE '%some significant SQL text here%'
</code></pre> <ol start="3"><li>Retain CR/LF on copy or save &lt;-- 存檔保留原始換行符號</li> <li>若沒有 Server 管理權限，<code>ALTER TRACE</code> 可授權追蹤、<code>VIEW SERVER (PERFORMANCE) STATE</code> 可查詢 <code>sys.dm_exec_query_stats</code> 及 <code>sys.dm_exec_sql_text</code></li></ol> </li> <li>進階技巧，導引產生想到的執行計劃(Plan Guide)，以下例子指定對 OrderDate Index Seek： <pre><code>DBCC FREEPROCCACHE
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19960101'
go
EXEC sp_create_plan_guide
@name = N'MyGuide',
@stmt = N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
@type = N'SQL',
@module_or_batch = NULL,
@params = N'@orderdate datetime',
@hints =  N'OPTION (TABLE HINT (dbo.Orders , INDEX (OrderDate)))'
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19980101'
go
EXEC sp_control_plan_guide N'DROP', N'MyGuide'
</code></pre> </li> <li>進階技巧，若 Parameter Sniffing 會產生好的或壞的執行計劃，先用特定參數執行產生好的執行計劃，用 <code>sp_create_plan_guide_from_handle</code> 將其標註為建議使用的版本。</li> <li>SQL 2016+，可啟用 Query Store 長期保存執行計劃及其統計資訊，以利事後分析應用。 <img src="https://blog.darkthread.net/Posts/files/2023/Fig11_638352239216183853.png"></li></ol> <p>不得不說，Erland 的文章滿滿乾貨，但也乾到不配溫開水難以吞服，最後附上我的精簡版精華結束這回合：</p> <ol><li>遇到網站跑 SP/SQL 很慢，移到 SSMS 飛快情況，起因為 Parameter Sniffing 會產生多個版本的執行計劃，有些執行計劃遇到特定參數值效能奇差。</li> <li>SSMS 因預設 ARITHABORT ON，會另外產生執行計劃，避開不良的執行計劃，感覺解決了問題。 ARITHABORT 本身對查詢並無作用響，將應用程式也設成 ARITHABORT ON 並不能解決問題。</li> <li><code>sp_recompile &lt;sp_name&gt;</code> 會強制清空執行計劃快取，有機會解決問題</li> <li>使用 SSMS、<code>dm_exec_text_query_plan</code> 等方式取得好、壞兩種執行計劃對照分析，透過一些技巧避免產生效率低落的執行計劃</li> <li>可用技巧：OPTION (RECOMPILE)、增刪索引、加上索引提示、加上 OPTIMIZE 提示、將外界傳入參數轉成區域變數再使用可避免 Parameter Sniffing</li></ol> <p data-slug="inside-param-sniffing-n-exec-plan" data-title-en="Inside SQL parameter sniffing and execution plan" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2019/09/22/19/05/cats-4496744_1280.jpg#1280#720">This article summarizes some tips of trouble-shooting SQL parameter sniffing related issues.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>【前言】關於 SQL 執行計劃如何影響查詢效能議題很久之前研究過了(<a href="/blog/index-scan-vs-seek">SQL筆記：Index Scan vs Index Seek</a>、<a href="/blog/literal-var-param-and-exec-plan/">SQL筆記：Literal, Variable與Parameter</a>)，昨天研究 <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>找到 SQL Server MVP Erland Sommarskog 解釋 Parameter Sniffing 議題的經典文章 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，在 Parameter Sniffing 議題上有深入剖析，並附有完整範例、檢測工具，不讀完對不起自己，完食整理筆記如下。<br> (以下假設大家已知道執行計劃、Parameter Sniffing、執行計劃快取、清除快取重新建立時機... 等概念，提到這些名詞時不再多做解釋)</p> <p>開始前先釐清一個觀念， <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>一文提到 SSMS 因 SET ARITHABORT ON 查詢比網站快，不是因為 ARITHABORT 改變查詢行為，純粹是因參數 ARITHABORT ON/OFF 差異導致 SSMS 使用了跟網站不同的執行計劃，而這個執行計劃比網站在用的執行計劃更適合當時的傳入參數，所以查詢效能良好。 換言之，問題的核心在於 SQL 會進行 Parameter Sniffing 依據參數決定最適合執行計劃，當不同參數產生的執行計劃差異很大時，就可能發生用參數用 A 值推算的執行計劃，查詢 A 只要一秒，用來查詢 B 卻要一分鐘。因此，問題出執行計劃對不同參數的執行效能差異過大，ARITHABORT 差異有影響源自改用另一個較沒問題的查詢計劃避開，與 ARITHABORT 本身沒有關係 (引述 Erland <a href="https://learn.microsoft.com/en-us/answers/questions/128817/what-is-arithabort-setting?WT.mc_id=DOP-MVP-37580">說法</a>：Under normal circumstances there is absolutely no difference between ARITHABORT ON and OFF)，真正要解決的是「依參數值 A 產生執行計劃，用來查 B 效能會悲劇」這個問題。而要解決這類問題，必須了解 Parameter Sniffing 與執行計劃的關係，而 Erland 的文章是絕佳敲門磚。</p> <ol><li>當遇到 SP 因為快取的執行計劃不利於特定參數查詢而異常緩慢，最簡單粗暴的解法：<code>EXEC sp_recompile problem_sp</code>，如此可強制清掉快取，於下次查詢時重新產生執行計劃，若問題不再發生，結案。</li> <li>如果不幸問題持續發生，代表你需要找出有問題的執行計劃，分析什麼參數值造成執行計劃不佳並設法改善。</li> <li>偵查 Parameter Sniffing 問題前應先掌握以下情資： <ul><li>是哪個指令慢？</li> <li>快的跟慢的執行計劃分別長什麼樣子？</li> <li>SQL 依據哪些參數決定執行計劃？</li> <li>資料表與索引結構如何？</li> <li>資料分佈統計如何？是否有更新？</li></ul> </li> <li>若狀況為 SSMS 快網站慢，同時取得快、慢執行計劃的方法是分別設 ARITHABORT ON 及 OFF 各跑一次： <pre><code>SET ARITHABORT ON
go
EXEC that_very_sp 4711, 123, 1
go
SET ARITHABORT OFF
go
EXEC that_very_sp 4711, 123, 1
</code></pre> </li> <li>執行計劃最右邊的 SELECT/INSERT... 按右鍵選 Property 找 ParameterList / Parameter Compiled Value 即嗅探值(Sniffed Value)，多半就是 Parameter Sniffing 問題的來源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638352239195576657.png"></li> <li>執行計劃圖還有幾個地方可以留意，Cost [1] 數字愈高愈是瓶頸所在(但要注意它是預測值，有可能失真)，相對的箭頭會愈粗[2]，另外，實際筆數與預測筆數落差[3]也很具參考價值。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638352239197801887.png"></li> <li>在無法使用 SSMS 的場合，以下這段 SQL 可取得執行計劃：(註：需 GRANT VIEW SERVER PERFORMANCE STATE) <pre><code>DECLARE @dbname    nvarchar(256),
@procname  nvarchar(256)
SELECT @dbname = 'Northwind', -- DB 名稱
@procname = 'dbo.List_orders_2' -- SP 名稱
; WITH basedata AS (
SELECT qs.statement_start_offset/2 AS stmt_start,
qs.statement_end_offset/2 AS stmt_end,
est.encrypted AS isencrypted, est.text AS sqltext,
epa.value AS set_options, qp.query_plan,
charindex('&lt;ParameterList&gt;', qp.query_plan) + len('&lt;ParameterList&gt;')
AS paramstart,
charindex('&lt;/ParameterList&gt;', qp.query_plan) AS paramend
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY sys.dm_exec_text_query_plan(qs.plan_handle,
qs.statement_start_offset,
qs.statement_end_offset) qp
CROSS  APPLY sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  est.objectid  = object_id (@procname)
AND  est.dbid      = db_id(@dbname)
AND  epa.attribute = 'set_options'
), next_level AS (
SELECT stmt_start, set_options, query_plan,
CASE WHEN isencrypted = 1 THEN '-- ENCRYPTED'
WHEN stmt_start &gt;= 0
THEN substring(sqltext, stmt_start + 1,
CASE stmt_end
WHEN 0 THEN datalength(sqltext)
ELSE stmt_end - stmt_start + 1
END)
END AS Statement,
CASE WHEN paramend &gt; paramstart
THEN CAST (substring(query_plan, paramstart,
paramend - paramstart) AS xml)
END AS params
FROM   basedata
)
SELECT set_options AS [SET], n.stmt_start AS Pos, n.Statement,
CR.c.value('@Column', 'nvarchar(128)') AS Parameter,
CR.c.value('@ParameterCompiledValue', 'nvarchar(128)') AS [Sniffed Value],
CAST (query_plan AS xml) AS [Query plan]
FROM   next_level n
CROSS  APPLY   n.params.nodes('ColumnReference') AS CR(c)
ORDER  BY n.set_options, n.stmt_start, Parameter
</code></pre> 上述查詢可取得該 SP 的執行計劃(Query Plan 的 XML 用 SSMS 開啟即為執行計劃流程圖)及其對映嗅探值，非常方便：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638352239199824875.png"><br> (SET - ARITHABORT 等選項值組合、POS - 查詢指令在 SP 的起始位置(第幾個字元))</li> <li>以下指令可用來清查資料表及索引： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, i.index_id, i.name, i.type_desc,
substring(ikey.cols, 3, len(ikey.cols)) AS key_cols,
substring(inc.cols, 3, len(inc.cols)) AS included_cols,
stats_date(o.object_id, i.index_id) AS stats_date,
i.filter_definition
FROM   sys.objects o
JOIN   sys.indexes i ON i.object_id = o.object_id
OUTER  APPLY (SELECT ', ' + c.name +
CASE ic.is_descending_key
WHEN 1 THEN ' DESC'
ELSE ''
END
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 0
ORDER  BY ic.key_ordinal
FOR XML PATH('')) AS ikey(cols)
OUTER  APPLY (SELECT ', ' + c.name
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 1
ORDER  BY ic.index_column_id
FOR XML PATH('')) AS inc(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, i.index_id
</code></pre> 資料表的欄位、索引資訊一目了然：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638352239201922600.png"></li> <li>最後是統計資料狀態： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, s.stats_id, s.name, s.auto_created, s.user_created,
substring(scols.cols, 3, len(scols.cols)) AS stat_cols,
stats_date(o.object_id, s.stats_id) AS stats_date,
s.filter_definition
FROM   sys.objects o
JOIN   sys.stats s ON s.object_id = o.object_id
CROSS  APPLY (SELECT ', ' + c.name
FROM   sys.stats_columns sc
JOIN   sys.columns c ON sc.object_id = c.object_id
AND sc.column_id = c.column_id
WHERE  sc.object_id = s.object_id
AND  sc.stats_id  = s.stats_id
ORDER  BY sc.stats_column_id
FOR XML PATH('')) AS scols(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, s.stats_id
</code></pre> stats_date 為最後更新時間，若年代久遠，則有可能是造成 Parameter Sniffing 問題的根源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638352239203911205.png"><br> 若確認統計資料過期，可用 <code>UPDATE STATISTICS tblName</code> 更新，它會以取樣方式更新，一般都能精準反映現況。若取樣更新結果不理想，再嘗試 <code>UPDATE STATISTICS tblName WITH FULL SCAN, INDEX</code> (加 INDEX 只更新索引統計，可縮執行時間)，若只更新特定索引，則可 <code>UPDATE STATISTICS tblName indexName WITH FULLSCAN</code>。更新統計會觸發重新編譯及 Parameter Sniffing，一般可馬上看到成效。</li> <li>有一些情況會導致 Parameter Sniffing 相關效能問題反覆發生，例如： <ol><li>查詢寫法不適合使用 Parameter Sniffing，依據 A 參數值產生的執行計劃用在 B 參數很慘，反之亦然</li> <li>有少數參數對映的執行計劃與其他大部分參數完全不同，若先用它跑 Parameter Sniffing，則後面的查詢只剩一個慘字</li> <li>查詢條件沒有完美的索引可用，但有好幾個可用索引供挑選，挑到不同索引執行計劃效能差異很大 實際狀況很多種，沒有一體適用的解法，需依個案分析解決。</li></ol> </li> <li>因此，在程式加上 <code>SET ARITHABORT ON</code> 並不是真的解決問題，它類似 <code>sp_recompile</code> 會強迫用現在的參數重新產生執行計劃，若 Parameter Sniffing 導致不良執行計劃的狀況仍在，遲早會再發生。</li> <li>案例：查指定客戶特定日期區間的訂單，客戶有很多訂單用 OrderDate Index Seek，若只有一筆用 CustomerId Index Seek：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638352239205934277.png"></li> <li>大絕：在 SP 加上 <code>OPTION (RECOMPILE)</code> 強迫每次重新編譯，犧牲一點效能避開快取執行計劃不適用特定參數值的嚴重問題。但若呼叫頻率很高或 SP 複雜編譯耗時，要小心副作用反撲。</li> <li>常見問題：動態搜尋條件通常不利於 Parameter Sniffing，如 <code>WHERE (CustomerID = @custid OR @custid IS NULL) AND (OrderDate = @orderdate OR @orderdate IS NULL)...</code>，可考慮 <code>OPTION (RECOMPILE)</code>。</li> <li>查索引值分佈可用 <a href="https://learn.microsoft.com/en-us/sql/t-sql/database-console-commands/dbcc-show-statistics-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">DBCC SHOW_STATISTICS</a><br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig7_638352239207900029.png"><br> 以 1996-07-19 為例，RANGE_ROWS = 8 表有 8 筆資料介於 07/08 與 07/19 間(不含)，EQ_ROWS = 2 表有兩筆等於 7/19，DISTINCT_RANGE_ROWS = 8 表是 RANGE_ROWS 有 8 筆不同值，AVG_RANGE_ROWS = RANGE_ROWS / DISTINCT_RANGE_ROWS。</li> <li>案例：索引放在 Col3，Col3 資料分佈不均勻，有些類別只有 17 筆，有些有 421,121 筆 (可用)，理論上不適合用於 Index Seek，但因為第一筆查詢參數是個不存在的分類，其預測值取 1，SQL 認定其是適合的索引，造成悲劇。<br> 幾種解法： <ol><li>OPTION (RECOMPILE)</li> <li>新增完全吻合該查詢條件的專用索引(注意佔用空間及增加資料更新負擔)</li> <li>調整或刪除 Col3 索引(用 <code>sys.dm_db_index_usage_stats</code> 佐證它很廢)</li> <li>加上索引提示，如 <code>JOIN Table_A a WITH (INDEX = Combo_ix) ON a.Col4 = b.Col1</code></li> <li>加上 OPTIMIZE FOR <code>OPTION (OPTIMIZE FOR (@p3 = 'KIWI'))</code></li> <li>將 Col3 比對參數轉成區域變數(避開 Sniffing)</li> <li>調整程式行為(例如：避免先用不存在的分類查詢)</li></ol> </li> <li>SSMS 觀察執行計劃一般要等到執行完，若查詢很耗時，可使用 Live Query Plan 搶先看。(版本需求：SQL2014+/SSMS2016+)<br> 選項位於 Query/Include Live Query Statistics<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig8_638352239209836862.png"><br> 另外一個做法是從 Active Monitor / Active Expensive Queries 找到正在執行中查詢，由右鍵選單開啟：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig9_638352239211846663.png"><br> 除此外 SQL 2016+ 新增了一個 View <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-statistics-xml-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_statistics_xml</code></a> 也可用來取得執行中查詢的執行計劃</li> <li>SQL2019 新加 <code>ALTER DATABASE SCOPED CONFIGURATION SET LAST_QUERY_PLAN_STATS = ON</code> 方式可保存最近的執行計劃。參考：<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-plan-stats-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_plan_stats</code></a></li> <li>用 Profiler 追蹤也可以取得執行計劃，但對效能影響較大，很少會這麼做</li> <li>.NET <code>SqlCommand.Commtext = "SELECT * FROM dbo.Orders WHERE CustomerID = @c"</code> 寫法也會產生執行計劃快取，快取 Key 由查詢語法文字雜湊產生。所以 <code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code>、<code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code> (結尾多一個空白)、<code>SELECT * FROM Orders WHERE CustomerID = @c</code> (拿掉dbo.)、<code>select * from dbo.Orders where CustomerID = @c</code> (大小寫不同)會產生四份執行計劃快取。</li> <li>注意：.NET SqlParameter 未指定長度可能導致多份執行計劃 <pre><code>cmd.CommandText = "SELECT * FROM dbo.Orders WHERE CustomerID = @c";
cmd.Parameters.Add("@c", SqlDbType.NVarChar).Value = TextBox.Value;
--會轉成如下指令，由 TextBox.Value 決定 nvarchar(n)
/*
exec sp_executesql N'SELECT * FROM Orders WHERE CustomerID = @c',
N'@c nvarchar(5)',@c=N'ALFKI'
*/
--字串長度不同，產生的 SQL 語法 nvarchar(n) 的 n 不同，會產生多份執行計劃
--若要避免請指定長度
cmd.Parameters.Add("@c", SqlDbType.NVarChar, 5).Value = TextBox.Value;
</code></pre> </li> <li>不同 DEFAULT_SCHEMA 的使用者不共用執行計劃快取，下圖為 DEFAULT_SCHEMA 為 dbo 或 Schema2 使用者查詢 <code>SELECT * FROM Orders WHERE ...</code> 留下的快取，user_id = 1 for dbo、user_id = 5 for Schema2。若指定 Schema <code>SELECT * FROM dbo.Orders WHERE ...</code>，則 user_id = -2 多使用者可共用執行計劃快取，這是表格、View、UDF 名稱一律加上 Schema 的好處(例：dbo.TableName，但 SP 例外，理由是 SP 會以自己所屬 Schema 為準，不隨使用者改變)，如果你的 DB 有區分 Schema，而非共用 dbo 的話。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig10_638352239214049580.png"></li> <li>Auto-Parameterisation - SQL 將常數自動轉成參數的行為，有兩種模式，Simple、Forced，由資料庫設定決定，預設為 Simple，並只有在 SQL 語句複雜度很低時才啟用(只會產生一種執行計劃)，故不會因 Parameter Sniffing 出問題。Forced 模式則會將所有常數參數化，例如： <pre><code>ALTER DATABASE Northwind SET PARAMETERIZATION FORCED
DBCC FREEPROCCACHE
go
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
SELECT * FROM Orders WHERE OrderDate &gt; '19970101'
go
</code></pre> 在查詢計劃 SQL 語法會轉成 <code>select * from Orders where OrderDate &gt; @0</code>，兩次共用同一個查詢計劃；若設為 Simple 模式，SQL 發現有兩種執行計劃(前者適合 Index Seek + Key Lookup，後者適合 Clusterd Index Scan)，則會停用自動參數化。</li> <li>在 SSMS 模擬應用程式執行 SQL 動作，SET ARITHABORT OFF 之外的小技巧： <ol><li><code>EXECUTE AS USER = 'appuser'</code> 以應用程式使用的帳號執行，結束時 <code>REVERT</code> 復原</li> <li>用以下指令查應用程式送出的 SQL 指令</li></ol> <pre><code>SELECT '&lt;' + est.text + '&gt;'
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
WHERE  est.text LIKE '%some significant SQL text here%'
</code></pre> <ol start="3"><li>Retain CR/LF on copy or save &lt;-- 存檔保留原始換行符號</li> <li>若沒有 Server 管理權限，<code>ALTER TRACE</code> 可授權追蹤、<code>VIEW SERVER (PERFORMANCE) STATE</code> 可查詢 <code>sys.dm_exec_query_stats</code> 及 <code>sys.dm_exec_sql_text</code></li></ol> </li> <li>進階技巧，導引產生想到的執行計劃(Plan Guide)，以下例子指定對 OrderDate Index Seek： <pre><code>DBCC FREEPROCCACHE
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19960101'
go
EXEC sp_create_plan_guide
@name = N'MyGuide',
@stmt = N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
@type = N'SQL',
@module_or_batch = NULL,
@params = N'@orderdate datetime',
@hints =  N'OPTION (TABLE HINT (dbo.Orders , INDEX (OrderDate)))'
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19980101'
go
EXEC sp_control_plan_guide N'DROP', N'MyGuide'
</code></pre> </li> <li>進階技巧，若 Parameter Sniffing 會產生好的或壞的執行計劃，先用特定參數執行產生好的執行計劃，用 <code>sp_create_plan_guide_from_handle</code> 將其標註為建議使用的版本。</li> <li>SQL 2016+，可啟用 Query Store 長期保存執行計劃及其統計資訊，以利事後分析應用。 <img src="https://blog.darkthread.net/Posts/files/2023/Fig11_638352239216183853.png"></li></ol> <p>不得不說，Erland 的文章滿滿乾貨，但也乾到不配溫開水難以吞服，最後附上我的精簡版精華結束這回合：</p> <ol><li>遇到網站跑 SP/SQL 很慢，移到 SSMS 飛快情況，起因為 Parameter Sniffing 會產生多個版本的執行計劃，有些執行計劃遇到特定參數值效能奇差。</li> <li>SSMS 因預設 ARITHABORT ON，會另外產生執行計劃，避開不良的執行計劃，感覺解決了問題。 ARITHABORT 本身對查詢並無作用響，將應用程式也設成 ARITHABORT ON 並不能解決問題。</li> <li><code>sp_recompile &lt;sp_name&gt;</code> 會強制清空執行計劃快取，有機會解決問題</li> <li>使用 SSMS、<code>dm_exec_text_query_plan</code> 等方式取得好、壞兩種執行計劃對照分析，透過一些技巧避免產生效率低落的執行計劃</li> <li>可用技巧：OPTION (RECOMPILE)、增刪索引、加上索引提示、加上 OPTIMIZE 提示、將外界傳入參數轉成區域變數再使用可避免 Parameter Sniffing</li></ol> <p data-slug="inside-param-sniffing-n-exec-plan" data-title-en="Inside SQL parameter sniffing and execution plan" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2019/09/22/19/05/cats-4496744_1280.jpg#1280#720">This article summarizes some tips of trouble-shooting SQL parameter sniffing related issues.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>【前言】關於 SQL 執行計劃如何影響查詢效能議題很久之前研究過了(<a href="/blog/index-scan-vs-seek">SQL筆記：Index Scan vs Index Seek</a>、<a href="/blog/literal-var-param-and-exec-plan/">SQL筆記：Literal, Variable與Parameter</a>)，昨天研究 <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>找到 SQL Server MVP Erland Sommarskog 解釋 Parameter Sniffing 議題的經典文章 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，在 Parameter Sniffing 議題上有深入剖析，並附有完整範例、檢測工具，不讀完對不起自己，完食整理筆記如下。<br> (以下假設大家已知道執行計劃、Parameter Sniffing、執行計劃快取、清除快取重新建立時機... 等概念，提到這些名詞時不再多做解釋)</p> <p>開始前先釐清一個觀念， <a href="/blog/sql-slow-in-ap-fast-in-ssms/">網站執行 Stored Procedure 爆慢，在 SSMS 秒回問題</a>一文提到 SSMS 因 SET ARITHABORT ON 查詢比網站快，不是因為 ARITHABORT 改變查詢行為，純粹是因參數 ARITHABORT ON/OFF 差異導致 SSMS 使用了跟網站不同的執行計劃，而這個執行計劃比網站在用的執行計劃更適合當時的傳入參數，所以查詢效能良好。 換言之，問題的核心在於 SQL 會進行 Parameter Sniffing 依據參數決定最適合執行計劃，當不同參數產生的執行計劃差異很大時，就可能發生用參數用 A 值推算的執行計劃，查詢 A 只要一秒，用來查詢 B 卻要一分鐘。因此，問題出執行計劃對不同參數的執行效能差異過大，ARITHABORT 差異有影響源自改用另一個較沒問題的查詢計劃避開，與 ARITHABORT 本身沒有關係 (引述 Erland <a href="https://learn.microsoft.com/en-us/answers/questions/128817/what-is-arithabort-setting?WT.mc_id=DOP-MVP-37580">說法</a>：Under normal circumstances there is absolutely no difference between ARITHABORT ON and OFF)，真正要解決的是「依參數值 A 產生執行計劃，用來查 B 效能會悲劇」這個問題。而要解決這類問題，必須了解 Parameter Sniffing 與執行計劃的關係，而 Erland 的文章是絕佳敲門磚。</p> <ol><li>當遇到 SP 因為快取的執行計劃不利於特定參數查詢而異常緩慢，最簡單粗暴的解法：<code>EXEC sp_recompile problem_sp</code>，如此可強制清掉快取，於下次查詢時重新產生執行計劃，若問題不再發生，結案。</li> <li>如果不幸問題持續發生，代表你需要找出有問題的執行計劃，分析什麼參數值造成執行計劃不佳並設法改善。</li> <li>偵查 Parameter Sniffing 問題前應先掌握以下情資： <ul><li>是哪個指令慢？</li> <li>快的跟慢的執行計劃分別長什麼樣子？</li> <li>SQL 依據哪些參數決定執行計劃？</li> <li>資料表與索引結構如何？</li> <li>資料分佈統計如何？是否有更新？</li></ul> </li> <li>若狀況為 SSMS 快網站慢，同時取得快、慢執行計劃的方法是分別設 ARITHABORT ON 及 OFF 各跑一次： <pre><code>SET ARITHABORT ON
go
EXEC that_very_sp 4711, 123, 1
go
SET ARITHABORT OFF
go
EXEC that_very_sp 4711, 123, 1
</code></pre> </li> <li>執行計劃最右邊的 SELECT/INSERT... 按右鍵選 Property 找 ParameterList / Parameter Compiled Value 即嗅探值(Sniffed Value)，多半就是 Parameter Sniffing 問題的來源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638352239195576657.png"></li> <li>執行計劃圖還有幾個地方可以留意，Cost [1] 數字愈高愈是瓶頸所在(但要注意它是預測值，有可能失真)，相對的箭頭會愈粗[2]，另外，實際筆數與預測筆數落差[3]也很具參考價值。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638352239197801887.png"></li> <li>在無法使用 SSMS 的場合，以下這段 SQL 可取得執行計劃：(註：需 GRANT VIEW SERVER PERFORMANCE STATE) <pre><code>DECLARE @dbname    nvarchar(256),
@procname  nvarchar(256)
SELECT @dbname = 'Northwind', -- DB 名稱
@procname = 'dbo.List_orders_2' -- SP 名稱
; WITH basedata AS (
SELECT qs.statement_start_offset/2 AS stmt_start,
qs.statement_end_offset/2 AS stmt_end,
est.encrypted AS isencrypted, est.text AS sqltext,
epa.value AS set_options, qp.query_plan,
charindex('&lt;ParameterList&gt;', qp.query_plan) + len('&lt;ParameterList&gt;')
AS paramstart,
charindex('&lt;/ParameterList&gt;', qp.query_plan) AS paramend
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY sys.dm_exec_text_query_plan(qs.plan_handle,
qs.statement_start_offset,
qs.statement_end_offset) qp
CROSS  APPLY sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  est.objectid  = object_id (@procname)
AND  est.dbid      = db_id(@dbname)
AND  epa.attribute = 'set_options'
), next_level AS (
SELECT stmt_start, set_options, query_plan,
CASE WHEN isencrypted = 1 THEN '-- ENCRYPTED'
WHEN stmt_start &gt;= 0
THEN substring(sqltext, stmt_start + 1,
CASE stmt_end
WHEN 0 THEN datalength(sqltext)
ELSE stmt_end - stmt_start + 1
END)
END AS Statement,
CASE WHEN paramend &gt; paramstart
THEN CAST (substring(query_plan, paramstart,
paramend - paramstart) AS xml)
END AS params
FROM   basedata
)
SELECT set_options AS [SET], n.stmt_start AS Pos, n.Statement,
CR.c.value('@Column', 'nvarchar(128)') AS Parameter,
CR.c.value('@ParameterCompiledValue', 'nvarchar(128)') AS [Sniffed Value],
CAST (query_plan AS xml) AS [Query plan]
FROM   next_level n
CROSS  APPLY   n.params.nodes('ColumnReference') AS CR(c)
ORDER  BY n.set_options, n.stmt_start, Parameter
</code></pre> 上述查詢可取得該 SP 的執行計劃(Query Plan 的 XML 用 SSMS 開啟即為執行計劃流程圖)及其對映嗅探值，非常方便：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638352239199824875.png"><br> (SET - ARITHABORT 等選項值組合、POS - 查詢指令在 SP 的起始位置(第幾個字元))</li> <li>以下指令可用來清查資料表及索引： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, i.index_id, i.name, i.type_desc,
substring(ikey.cols, 3, len(ikey.cols)) AS key_cols,
substring(inc.cols, 3, len(inc.cols)) AS included_cols,
stats_date(o.object_id, i.index_id) AS stats_date,
i.filter_definition
FROM   sys.objects o
JOIN   sys.indexes i ON i.object_id = o.object_id
OUTER  APPLY (SELECT ', ' + c.name +
CASE ic.is_descending_key
WHEN 1 THEN ' DESC'
ELSE ''
END
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 0
ORDER  BY ic.key_ordinal
FOR XML PATH('')) AS ikey(cols)
OUTER  APPLY (SELECT ', ' + c.name
FROM   sys.index_columns ic
JOIN   sys.columns c ON ic.object_id = c.object_id
AND ic.column_id = c.column_id
WHERE  ic.object_id = i.object_id
AND  ic.index_id  = i.index_id
AND  ic.is_included_column = 1
ORDER  BY ic.index_column_id
FOR XML PATH('')) AS inc(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, i.index_id
</code></pre> 資料表的欄位、索引資訊一目了然：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638352239201922600.png"></li> <li>最後是統計資料狀態： <pre><code>DECLARE @tbl nvarchar(265)
SELECT @tbl = 'Orders'
SELECT o.name, s.stats_id, s.name, s.auto_created, s.user_created,
substring(scols.cols, 3, len(scols.cols)) AS stat_cols,
stats_date(o.object_id, s.stats_id) AS stats_date,
s.filter_definition
FROM   sys.objects o
JOIN   sys.stats s ON s.object_id = o.object_id
CROSS  APPLY (SELECT ', ' + c.name
FROM   sys.stats_columns sc
JOIN   sys.columns c ON sc.object_id = c.object_id
AND sc.column_id = c.column_id
WHERE  sc.object_id = s.object_id
AND  sc.stats_id  = s.stats_id
ORDER  BY sc.stats_column_id
FOR XML PATH('')) AS scols(cols)
WHERE  o.name = @tbl
ORDER  BY o.name, s.stats_id
</code></pre> stats_date 為最後更新時間，若年代久遠，則有可能是造成 Parameter Sniffing 問題的根源：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig5_638352239203911205.png"><br> 若確認統計資料過期，可用 <code>UPDATE STATISTICS tblName</code> 更新，它會以取樣方式更新，一般都能精準反映現況。若取樣更新結果不理想，再嘗試 <code>UPDATE STATISTICS tblName WITH FULL SCAN, INDEX</code> (加 INDEX 只更新索引統計，可縮執行時間)，若只更新特定索引，則可 <code>UPDATE STATISTICS tblName indexName WITH FULLSCAN</code>。更新統計會觸發重新編譯及 Parameter Sniffing，一般可馬上看到成效。</li> <li>有一些情況會導致 Parameter Sniffing 相關效能問題反覆發生，例如： <ol><li>查詢寫法不適合使用 Parameter Sniffing，依據 A 參數值產生的執行計劃用在 B 參數很慘，反之亦然</li> <li>有少數參數對映的執行計劃與其他大部分參數完全不同，若先用它跑 Parameter Sniffing，則後面的查詢只剩一個慘字</li> <li>查詢條件沒有完美的索引可用，但有好幾個可用索引供挑選，挑到不同索引執行計劃效能差異很大 實際狀況很多種，沒有一體適用的解法，需依個案分析解決。</li></ol> </li> <li>因此，在程式加上 <code>SET ARITHABORT ON</code> 並不是真的解決問題，它類似 <code>sp_recompile</code> 會強迫用現在的參數重新產生執行計劃，若 Parameter Sniffing 導致不良執行計劃的狀況仍在，遲早會再發生。</li> <li>案例：查指定客戶特定日期區間的訂單，客戶有很多訂單用 OrderDate Index Seek，若只有一筆用 CustomerId Index Seek：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig6_638352239205934277.png"></li> <li>大絕：在 SP 加上 <code>OPTION (RECOMPILE)</code> 強迫每次重新編譯，犧牲一點效能避開快取執行計劃不適用特定參數值的嚴重問題。但若呼叫頻率很高或 SP 複雜編譯耗時，要小心副作用反撲。</li> <li>常見問題：動態搜尋條件通常不利於 Parameter Sniffing，如 <code>WHERE (CustomerID = @custid OR @custid IS NULL) AND (OrderDate = @orderdate OR @orderdate IS NULL)...</code>，可考慮 <code>OPTION (RECOMPILE)</code>。</li> <li>查索引值分佈可用 <a href="https://learn.microsoft.com/en-us/sql/t-sql/database-console-commands/dbcc-show-statistics-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">DBCC SHOW_STATISTICS</a><br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig7_638352239207900029.png"><br> 以 1996-07-19 為例，RANGE_ROWS = 8 表有 8 筆資料介於 07/08 與 07/19 間(不含)，EQ_ROWS = 2 表有兩筆等於 7/19，DISTINCT_RANGE_ROWS = 8 表是 RANGE_ROWS 有 8 筆不同值，AVG_RANGE_ROWS = RANGE_ROWS / DISTINCT_RANGE_ROWS。</li> <li>案例：索引放在 Col3，Col3 資料分佈不均勻，有些類別只有 17 筆，有些有 421,121 筆 (可用)，理論上不適合用於 Index Seek，但因為第一筆查詢參數是個不存在的分類，其預測值取 1，SQL 認定其是適合的索引，造成悲劇。<br> 幾種解法： <ol><li>OPTION (RECOMPILE)</li> <li>新增完全吻合該查詢條件的專用索引(注意佔用空間及增加資料更新負擔)</li> <li>調整或刪除 Col3 索引(用 <code>sys.dm_db_index_usage_stats</code> 佐證它很廢)</li> <li>加上索引提示，如 <code>JOIN Table_A a WITH (INDEX = Combo_ix) ON a.Col4 = b.Col1</code></li> <li>加上 OPTIMIZE FOR <code>OPTION (OPTIMIZE FOR (@p3 = 'KIWI'))</code></li> <li>將 Col3 比對參數轉成區域變數(避開 Sniffing)</li> <li>調整程式行為(例如：避免先用不存在的分類查詢)</li></ol> </li> <li>SSMS 觀察執行計劃一般要等到執行完，若查詢很耗時，可使用 Live Query Plan 搶先看。(版本需求：SQL2014+/SSMS2016+)<br> 選項位於 Query/Include Live Query Statistics<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig8_638352239209836862.png"><br> 另外一個做法是從 Active Monitor / Active Expensive Queries 找到正在執行中查詢，由右鍵選單開啟：<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig9_638352239211846663.png"><br> 除此外 SQL 2016+ 新增了一個 View <a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-statistics-xml-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_statistics_xml</code></a> 也可用來取得執行中查詢的執行計劃</li> <li>SQL2019 新加 <code>ALTER DATABASE SCOPED CONFIGURATION SET LAST_QUERY_PLAN_STATS = ON</code> 方式可保存最近的執行計劃。參考：<a href="https://learn.microsoft.com/zh-tw/sql/relational-databases/system-dynamic-management-views/sys-dm-exec-query-plan-stats-transact-sql?view=azuresqldb-current&amp;WT.mc_id=DOP-MVP-37580"><code>sys.dm_exec_query_plan_stats</code></a></li> <li>用 Profiler 追蹤也可以取得執行計劃，但對效能影響較大，很少會這麼做</li> <li>.NET <code>SqlCommand.Commtext = "SELECT * FROM dbo.Orders WHERE CustomerID = @c"</code> 寫法也會產生執行計劃快取，快取 Key 由查詢語法文字雜湊產生。所以 <code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code>、<code>SELECT * FROM dbo.Orders WHERE CustomerID = @c</code> (結尾多一個空白)、<code>SELECT * FROM Orders WHERE CustomerID = @c</code> (拿掉dbo.)、<code>select * from dbo.Orders where CustomerID = @c</code> (大小寫不同)會產生四份執行計劃快取。</li> <li>注意：.NET SqlParameter 未指定長度可能導致多份執行計劃 <pre><code>cmd.CommandText = "SELECT * FROM dbo.Orders WHERE CustomerID = @c";
cmd.Parameters.Add("@c", SqlDbType.NVarChar).Value = TextBox.Value;
--會轉成如下指令，由 TextBox.Value 決定 nvarchar(n)
/*
exec sp_executesql N'SELECT * FROM Orders WHERE CustomerID = @c',
N'@c nvarchar(5)',@c=N'ALFKI'
*/
--字串長度不同，產生的 SQL 語法 nvarchar(n) 的 n 不同，會產生多份執行計劃
--若要避免請指定長度
cmd.Parameters.Add("@c", SqlDbType.NVarChar, 5).Value = TextBox.Value;
</code></pre> </li> <li>不同 DEFAULT_SCHEMA 的使用者不共用執行計劃快取，下圖為 DEFAULT_SCHEMA 為 dbo 或 Schema2 使用者查詢 <code>SELECT * FROM Orders WHERE ...</code> 留下的快取，user_id = 1 for dbo、user_id = 5 for Schema2。若指定 Schema <code>SELECT * FROM dbo.Orders WHERE ...</code>，則 user_id = -2 多使用者可共用執行計劃快取，這是表格、View、UDF 名稱一律加上 Schema 的好處(例：dbo.TableName，但 SP 例外，理由是 SP 會以自己所屬 Schema 為準，不隨使用者改變)，如果你的 DB 有區分 Schema，而非共用 dbo 的話。<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig10_638352239214049580.png"></li> <li>Auto-Parameterisation - SQL 將常數自動轉成參數的行為，有兩種模式，Simple、Forced，由資料庫設定決定，預設為 Simple，並只有在 SQL 語句複雜度很低時才啟用(只會產生一種執行計劃)，故不會因 Parameter Sniffing 出問題。Forced 模式則會將所有常數參數化，例如： <pre><code>ALTER DATABASE Northwind SET PARAMETERIZATION FORCED
DBCC FREEPROCCACHE
go
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
SELECT * FROM Orders WHERE OrderDate &gt; '19970101'
go
</code></pre> 在查詢計劃 SQL 語法會轉成 <code>select * from Orders where OrderDate &gt; @0</code>，兩次共用同一個查詢計劃；若設為 Simple 模式，SQL 發現有兩種執行計劃(前者適合 Index Seek + Key Lookup，後者適合 Clusterd Index Scan)，則會停用自動參數化。</li> <li>在 SSMS 模擬應用程式執行 SQL 動作，SET ARITHABORT OFF 之外的小技巧： <ol><li><code>EXECUTE AS USER = 'appuser'</code> 以應用程式使用的帳號執行，結束時 <code>REVERT</code> 復原</li> <li>用以下指令查應用程式送出的 SQL 指令</li></ol> <pre><code>SELECT '&lt;' + est.text + '&gt;'
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
WHERE  est.text LIKE '%some significant SQL text here%'
</code></pre> <ol start="3"><li>Retain CR/LF on copy or save &lt;-- 存檔保留原始換行符號</li> <li>若沒有 Server 管理權限，<code>ALTER TRACE</code> 可授權追蹤、<code>VIEW SERVER (PERFORMANCE) STATE</code> 可查詢 <code>sys.dm_exec_query_stats</code> 及 <code>sys.dm_exec_sql_text</code></li></ol> </li> <li>進階技巧，導引產生想到的執行計劃(Plan Guide)，以下例子指定對 OrderDate Index Seek： <pre><code>DBCC FREEPROCCACHE
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19960101'
go
EXEC sp_create_plan_guide
@name = N'MyGuide',
@stmt = N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
@type = N'SQL',
@module_or_batch = NULL,
@params = N'@orderdate datetime',
@hints =  N'OPTION (TABLE HINT (dbo.Orders , INDEX (OrderDate)))'
go
EXEC sp_executesql N'SELECT * FROM dbo.Orders WHERE OrderDate &gt; @orderdate',
N'@orderdate datetime', @orderdate = '19980101'
go
EXEC sp_control_plan_guide N'DROP', N'MyGuide'
</code></pre> </li> <li>進階技巧，若 Parameter Sniffing 會產生好的或壞的執行計劃，先用特定參數執行產生好的執行計劃，用 <code>sp_create_plan_guide_from_handle</code> 將其標註為建議使用的版本。</li> <li>SQL 2016+，可啟用 Query Store 長期保存執行計劃及其統計資訊，以利事後分析應用。 <img src="https://blog.darkthread.net/Posts/files/2023/Fig11_638352239216183853.png"></li></ol> <p>不得不說，Erland 的文章滿滿乾貨，但也乾到不配溫開水難以吞服，最後附上我的精簡版精華結束這回合：</p> <ol><li>遇到網站跑 SP/SQL 很慢，移到 SSMS 飛快情況，起因為 Parameter Sniffing 會產生多個版本的執行計劃，有些執行計劃遇到特定參數值效能奇差。</li> <li>SSMS 因預設 ARITHABORT ON，會另外產生執行計劃，避開不良的執行計劃，感覺解決了問題。 ARITHABORT 本身對查詢並無作用響，將應用程式也設成 ARITHABORT ON 並不能解決問題。</li> <li><code>sp_recompile &lt;sp_name&gt;</code> 會強制清空執行計劃快取，有機會解決問題</li> <li>使用 SSMS、<code>dm_exec_text_query_plan</code> 等方式取得好、壞兩種執行計劃對照分析，透過一些技巧避免產生效率低落的執行計劃</li> <li>可用技巧：OPTION (RECOMPILE)、增刪索引、加上索引提示、加上 OPTIMIZE 提示、將外界傳入參數轉成區域變數再使用可避免 Parameter Sniffing</li></ol> <p data-slug="inside-param-sniffing-n-exec-plan" data-title-en="Inside SQL parameter sniffing and execution plan" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2019/09/22/19/05/cats-4496744_1280.jpg#1280#720">This article summarizes some tips of trouble-shooting SQL parameter sniffing related issues.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638352239195576657.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638352239195576657.png" medium="image"/>
    <pubDate>Fri, 10 Nov 2023 14:37:44 GMT</pubDate>
  </item><item>
    <title><![CDATA[【茶包射手日記】ASP.NET WebForm 出現 System.Web.AspNetHostingPermission 安全錯誤]]></title>
    <link>https://blog.darkthread.net/blog/aspnethostingpermission-securityexception/</link>
    <itunes:title><![CDATA[【茶包射手日記】ASP.NET WebForm 出現 System.Web.AspNetHostingPermission 安全錯誤]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>接獲報案，ASP.NET WebForm 站台更新 DLL 元件後出現安全錯誤，訊息如下：</p> <pre><code>Security Exception
Description: The application attempted to perform an operation not allowed by the security policy. To grant this application the required permission please contact your system administrator or change the application's trust level in the configuration file.
Exception Details: System.Security.SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.
Stacktrace:
[SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.]
System.Reflection.Assembly._GetType(String name, Boolean throwOnError, Boolean ignoreCase) +0
System.Web.UI.Util.GetTypeFromAssemblies(ICollection assemblies, String typeName, Boolean ignoreCase) +201
System.Web.Compilation.BuildManager.GetType(String typeName, Boolean throwOnError, Boolean ignoreCase) +302
System.Web.Configuration.ConfigUtil.GetType(String typeName, String propertyName, ConfigurationElement configElement, XmlNode node, Boolean checkAptcaBit, Boolean ignoreCase) +64
</code></pre> <p>由訊息判斷與存取權限有關，爬文查到當見問題及解法有：</p> <ul><li>AppPool 進階設定的 Load User Profile/載入使用者設定檔 未設成 True<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638351429656982609.png"></li> <li>網頁檔案實體放在遠端主機，以 <code>\\my-server\my-project</code> UNC 方式存取，AppPoolIdentity 設定身分不正確 <a href="https://stackoverflow.com/a/21252240/288936">參考</a>，或未使用 caspol 設成 FullTrust <a href="https://blog.miniasp.com/post/2010/02/28/Setup-aspnet-website-on-remote-UNC-Path">參考</a></li></ul> <p>而我遇到的問題是另一種，DLL 來自網路磁碟機或網站下載，即使包 ZIP 解開後仍會被標記檔案來自遠端電腦，存取使用上有額外限制(術語為 <a href="https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-fscc/6e3f7352-d11c-4d76-8c39-2516a9df36e8?WT.mc_id=DOP-MVP-37580">Zone Identifier</a>，詳情可參考這篇：<a href="/blog/zone-identifier/">網路下載程式持續被防毒軟體攔檢</a>)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638351429658904734.png"></p> <p>除了從檔案總管屬性介面解除封鎖，使用 PowerShell <code>Get-ChildItem * -Recurse | Unblock-File</code> 可批次解封整個資料夾。另外，我發現用 7-Zip 解壓縮好像不會將封鎖傳染下去，似乎也是種解法。</p> <p data-slug="aspnethostingpermission-securityexception" data-title-en="System.Web.AspNetHostingPermission SecurityException issue" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2014/04/12/15/02/padlock-322494_960_720.jpg#960#720">System.Web.AspNetHostingPermission SecurityException caused by dll downloaded from remote computer.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>接獲報案，ASP.NET WebForm 站台更新 DLL 元件後出現安全錯誤，訊息如下：</p> <pre><code>Security Exception
Description: The application attempted to perform an operation not allowed by the security policy. To grant this application the required permission please contact your system administrator or change the application's trust level in the configuration file.
Exception Details: System.Security.SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.
Stacktrace:
[SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.]
System.Reflection.Assembly._GetType(String name, Boolean throwOnError, Boolean ignoreCase) +0
System.Web.UI.Util.GetTypeFromAssemblies(ICollection assemblies, String typeName, Boolean ignoreCase) +201
System.Web.Compilation.BuildManager.GetType(String typeName, Boolean throwOnError, Boolean ignoreCase) +302
System.Web.Configuration.ConfigUtil.GetType(String typeName, String propertyName, ConfigurationElement configElement, XmlNode node, Boolean checkAptcaBit, Boolean ignoreCase) +64
</code></pre> <p>由訊息判斷與存取權限有關，爬文查到當見問題及解法有：</p> <ul><li>AppPool 進階設定的 Load User Profile/載入使用者設定檔 未設成 True<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638351429656982609.png"></li> <li>網頁檔案實體放在遠端主機，以 <code>\\my-server\my-project</code> UNC 方式存取，AppPoolIdentity 設定身分不正確 <a href="https://stackoverflow.com/a/21252240/288936">參考</a>，或未使用 caspol 設成 FullTrust <a href="https://blog.miniasp.com/post/2010/02/28/Setup-aspnet-website-on-remote-UNC-Path">參考</a></li></ul> <p>而我遇到的問題是另一種，DLL 來自網路磁碟機或網站下載，即使包 ZIP 解開後仍會被標記檔案來自遠端電腦，存取使用上有額外限制(術語為 <a href="https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-fscc/6e3f7352-d11c-4d76-8c39-2516a9df36e8?WT.mc_id=DOP-MVP-37580">Zone Identifier</a>，詳情可參考這篇：<a href="/blog/zone-identifier/">網路下載程式持續被防毒軟體攔檢</a>)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638351429658904734.png"></p> <p>除了從檔案總管屬性介面解除封鎖，使用 PowerShell <code>Get-ChildItem * -Recurse | Unblock-File</code> 可批次解封整個資料夾。另外，我發現用 7-Zip 解壓縮好像不會將封鎖傳染下去，似乎也是種解法。</p> <p data-slug="aspnethostingpermission-securityexception" data-title-en="System.Web.AspNetHostingPermission SecurityException issue" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2014/04/12/15/02/padlock-322494_960_720.jpg#960#720">System.Web.AspNetHostingPermission SecurityException caused by dll downloaded from remote computer.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>接獲報案，ASP.NET WebForm 站台更新 DLL 元件後出現安全錯誤，訊息如下：</p> <pre><code>Security Exception
Description: The application attempted to perform an operation not allowed by the security policy. To grant this application the required permission please contact your system administrator or change the application's trust level in the configuration file.
Exception Details: System.Security.SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.
Stacktrace:
[SecurityException: Request for the permission of type 'System.Web.AspNetHostingPermission, System, Version=2.0.0.0, Culture=neutral, PublicKeyToken=b77a5c561934e089' failed.]
System.Reflection.Assembly._GetType(String name, Boolean throwOnError, Boolean ignoreCase) +0
System.Web.UI.Util.GetTypeFromAssemblies(ICollection assemblies, String typeName, Boolean ignoreCase) +201
System.Web.Compilation.BuildManager.GetType(String typeName, Boolean throwOnError, Boolean ignoreCase) +302
System.Web.Configuration.ConfigUtil.GetType(String typeName, String propertyName, ConfigurationElement configElement, XmlNode node, Boolean checkAptcaBit, Boolean ignoreCase) +64
</code></pre> <p>由訊息判斷與存取權限有關，爬文查到當見問題及解法有：</p> <ul><li>AppPool 進階設定的 Load User Profile/載入使用者設定檔 未設成 True<br> <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638351429656982609.png"></li> <li>網頁檔案實體放在遠端主機，以 <code>\\my-server\my-project</code> UNC 方式存取，AppPoolIdentity 設定身分不正確 <a href="https://stackoverflow.com/a/21252240/288936">參考</a>，或未使用 caspol 設成 FullTrust <a href="https://blog.miniasp.com/post/2010/02/28/Setup-aspnet-website-on-remote-UNC-Path">參考</a></li></ul> <p>而我遇到的問題是另一種，DLL 來自網路磁碟機或網站下載，即使包 ZIP 解開後仍會被標記檔案來自遠端電腦，存取使用上有額外限制(術語為 <a href="https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-fscc/6e3f7352-d11c-4d76-8c39-2516a9df36e8?WT.mc_id=DOP-MVP-37580">Zone Identifier</a>，詳情可參考這篇：<a href="/blog/zone-identifier/">網路下載程式持續被防毒軟體攔檢</a>)。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638351429658904734.png"></p> <p>除了從檔案總管屬性介面解除封鎖，使用 PowerShell <code>Get-ChildItem * -Recurse | Unblock-File</code> 可批次解封整個資料夾。另外，我發現用 7-Zip 解壓縮好像不會將封鎖傳染下去，似乎也是種解法。</p> <p data-slug="aspnethostingpermission-securityexception" data-title-en="System.Web.AspNetHostingPermission SecurityException issue" data-catg="Web Development" data-fbimg="https://cdn.pixabay.com/photo/2014/04/12/15/02/padlock-322494_960_720.jpg#960#720">System.Web.AspNetHostingPermission SecurityException caused by dll downloaded from remote computer.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638351429656982609.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638351429656982609.png" medium="image"/>
    <pubDate>Thu, 09 Nov 2023 15:59:17 GMT</pubDate>
  </item><item>
    <title><![CDATA[「網站執行 Stored Procedure 爆慢，移到 SSMS 跑卻飛快」問題之徹底研究]]></title>
    <link>https://blog.darkthread.net/blog/sql-slow-in-ap-fast-in-ssms/</link>
    <itunes:title><![CDATA[「網站執行 Stored Procedure 爆慢，移到 SSMS 跑卻飛快」問題之徹底研究]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>一個常見問題：SQL 上某個 Stored Procedure (以下簡稱 SP)，從網站應用程式執行超慢，甚至等到逾時報錯；將同樣的 SP 及呼叫參數拿到 SSMS (SQL Server Management Studio) 執行卻瞬間完成；或是 SP 慢到無法忍受，將其中語法邏輯 Copy 出來單獨執行卻沒半點問題。</p> <p>我最近遇到的案例是 SP 在查詢某類別資料時超過 30 秒，所有其他類別都是一秒完成；將 SP 查詢移到 SSMS 測試，不管任何類別都是一秒完成。問題關鍵很神奇也很簡單，差在一個旗標設定 <a href="https://learn.microsoft.com/en-us/sql/t-sql/statements/set-arithabort-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">SET ARITHABORT ON</a>，原因是旗標不同導致 ASP.NET 與 SSMS 使用不同的執行計劃，而 ASP.NET 採用的執行計劃遇上問題類別明顯有問題，清除並重新產生執行計劃通常就能解決。<br> 印象中看過 Rico 這篇 <a href="https://medium.com/ricos-note/ssms%E5%9F%B7%E8%A1%8Csp%E5%B0%B1%E6%98%AF%E6%AF%94-net-%E5%BF%AB%E5%BE%88%E5%A4%9A-b6de95068af6">SSMS執行SP就是比.NET 快很多? - RiCosNote</a>，停用 SSMS <code>SET ARITHABORT ON</code> 成功重現查詢超過 30 秒才快速破案，特此感謝。</p> <p>問題雖解，但對我來說具體發生原因仍然是謎，如何防範心中也沒譜，便再花了點時深入研究，略有心得，在此分享。</p> <p>推薦這篇 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，SQL Server MVP Erland Sommarskog 一篇 2011 年的老文章(網頁風格充滿年代感)，但作者到今年都還有更新，範圍甚至涵蓋 SQL 2022，故不用擔心資訊過時。</p> <p>文章詳細解釋 SP 如何編譯、何謂 Parameter Sniffing、SQL 的執行計劃快取行為，讀完可完全理解「SP 在 SSMS 跑得比較快」這件事是怎麼發生的。</p> <p>文章宇宙無敵長，內容即深且廣，不寫筆記很難吸收，以下是我摘要的重點。</p> <ol><li>查詢 Plan Cache 需要 <a href="https://learn.microsoft.com/zh-tw/sql/t-sql/statements/grant-server-permissions-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#remarks">VIEW SERVER STATE</a> 權限 (SQL 2022 則是 VIEW SERVER PERFORMANCE STATE)</li> <li>SP、Scalar UDF (SQL 2019+)、Multi-Step Table Value Function、Trigger 擁有自己專屬的執行計劃</li> <li>在 SP 呼叫另一個 SP，二者的執行計劃是獨立的，在 SP 動態組裝 SQL 用 sp_executesql 執行，則不會包含在 SP 的執行計劃裡</li> <li>SP 直到第一次執才會建立執行計劃，每次執行 SQL 依資料分佈統計預測最佳執行方式(這個程序稱為 Optimisation)</li> <li>List_orders_3 的執行計劃會跟前兩者不同，不會查索引，而是使用 Clustered Index Scan (相當於 Table Scan)，原因其預測 Estimated Number of Rows Per Execution 是 249 (前二個是 1)，筆數多 Table Scan 比 Index Seek + Key Lookup 有效率。(北風資料庫 Orders 資料表只有 830 筆)</li></ol> <pre><code>CREATE PROCEDURE List_orders_1 AS
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
CREATE PROCEDURE List_orders_2 @fromdate datetime AS
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
go
CREATE PROCEDURE List_orders_3 @fromdate datetime AS
DECLARE @fromdate_copy datetime
SELECT @fromdate_copy = @fromdate
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate_copy
go
</code></pre> <ol start="6"><li>List_orders_1 日期寫成常數，SQL 分析資料分佈都介於 1968-1998，理論上沒有半筆，但基於參考來源是統計數據非實際資料，不確定一定沒資料，Estimated Number of Rows Per Execution 取 1</li> <li>List_orders_2 日期為參數，但 Optimisation 過程會用 20000101 分析，預測筆數也是 1。這個過程稱為 Parameter Sniffing (參數嗅探)</li> <li>List_orders_3 執行時，SQL 不會分析流程邏輯，不知區域變數 @fromdate_copy 來自 @fromdate，假設日期未知，猜 30%，830 * 0.3 = 249</li> <li>List_orders_4 與跟 List_orders_1、List_orders_2 執行計劃相同，理由是編譯時 @formdate 是用 NULL 預估無符合值，預測筆數取 1，但實際會傳回所有資料</li></ol> <pre><code>CREATE PROCEDURE List_orders_4 @fromdate datetime = NULL AS
IF @fromdate IS NULL
SELECT @fromdate = '19900101'
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
</code></pre> <ol start="10"><li>新版 SSMS 執行計劃流程會直接標示預測及實際筆數，但 Popup 視窗仍有 Number of Executions 跟 Estimated Number of Execution 等重要資訊可參考</li> <li>每次重建執行計劃很耗效能，對動轍一分鐘起跳的 OLAP 複雜查詢無所謂且有個別最佳化效果；但在 OLTP 會把 SP 執行計劃存入 Cache 重複以提升效率，直到以下狀況才清除： <ol><li>Buffer Cache 空間滿了(執行計劃與資料表資料共用)，SQL 會清掉很久沒使用的</li> <li>執行 ALTER PROCEDURE</li> <li>執行 sp_recompile</li> <li>執行 <code>DBCC FREEPROCCACHE</code> 清空所有執行計劃快取</li> <li>SQL 重啟</li> <li><code>sp_configure</code> 修改某些 SQL 伺服器參數</li></ol> </li> <li>清除快取後，下回執行 SP 會重做 Parameter Sniffing 產生執行計劃 (執行計劃可能與之前不同)</li> <li>有些事件會導致 SP 的部分指令重新編譯 <ol><li>相關資料表結構異動</li> <li>索引異動(ALTER INDEX/DBCC DBREINDEX)</li> <li>更新統計(SQL自動或CREATE STATISTICS/&nbsp;UPDATE STATISTICS) (不一定觸發重新編譯)</li> <li>對相關資料表執行 sp_recompile</li></ol> </li> <li>建立何種執行計劃由第一次執行的 Parameter Sniffing 決定，故很有可能該執行計劃不適合後續的其他查詢，這是實務上出問題的常見原因。</li> <li>這點很重要：SQL 會為不同 Attribute 或選項組合建立一份專屬執行計劃，以下例子中，不同 DATEFORMAT 各有一份執行計劃：</li></ol> <pre><code>CREATE PROCEDURE List_orders_6 AS
SELECT *
FROM   Orders
WHERE  OrderDate &gt; '12/01/1998'
go
SET DATEFORMAT dmy
go
EXEC List_orders_6
go
SET DATEFORMAT mdy
go
EXEC List_orders_6
go
-- 用以下指令可觀察到兩個不同版本執行計劃
SELECT qs.plan_handle, a.attrlist
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY (SELECT epa.attribute + '=' + convert(nvarchar(127), epa.value) + '   '
FROM   sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  epa.is_cache_key = 1
ORDER  BY epa.attribute
FOR    XML PATH('')) AS a(attrlist)
WHERE  est.objectid = object_id ('dbo.List_orders_6')
AND  est.dbid     = db_id('Northwind')
/*
plan_handle                     attrlist
------------------------------- -------------------------------------------------
0x0500070064EFCA5DB8A0A90500... compat_level=150   date_first=7   date_format=1
set_options=4347   user_id=1
0x0500070064EFCA5DB8A0A80500... compat_level=150   date_first=7   date_format=2
set_options=4347   user_id=1
*/
</code></pre> <ol start="16"><li>以下選項組合可想成 Cache Key：ANSI_PADDING, Parallel Plan,&nbsp;CONCAT_NUL_YIELDS_NULL,&nbsp;ANSI_WARNINGS,&nbsp;ANSI_NULLS,&nbsp;QUOTED_IDENTIFIER,&nbsp;ANSI_NULL_DFLT_ON 及 ARITHABORT，任一個值不同，Cache Key 不同，對映一份專屬執行計劃</li> <li>.NET 跟 SSMS 的預設選項只差在 ARITHABORT，這就是為什麼二者會因使用不同執行計劃，並出現截然不同的查詢速度，而在 SSMS 關閉 ARITHABORT 或加上 <code>SET ARITHABORT OFF</code> 可讓 SSMS 重現 .NET 程式的查詢速度： <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638350569095724823.png"></li> <li>在 .NET 跑查詢時也補上 <code>SET ARITHABORT ON</code> 可以避開應用程式跟 SSMS 跑相同查詢效能天差地別的狀況，但並無法解決因執行計劃不佳造成查詢爆慢問題。</li></ol> <p>至於要怎麼解決執行計劃不適合造成效能問題，要從了解 Parameter Sniffing 下手，這又是另一個值得深入的大議題，留待下一篇討論。</p> <p data-slug="sql-slow-in-ap-fast-in-ssms" data-title-en="SQL performance slow in applciation but fast in SSMS" data-catg=".NET" data-fbimg="https://i.imgur.com/aUm0htZ.jpg#1280#720">Introduction to the execution plan cache issue on perfermance difference between application and SSMS.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>一個常見問題：SQL 上某個 Stored Procedure (以下簡稱 SP)，從網站應用程式執行超慢，甚至等到逾時報錯；將同樣的 SP 及呼叫參數拿到 SSMS (SQL Server Management Studio) 執行卻瞬間完成；或是 SP 慢到無法忍受，將其中語法邏輯 Copy 出來單獨執行卻沒半點問題。</p> <p>我最近遇到的案例是 SP 在查詢某類別資料時超過 30 秒，所有其他類別都是一秒完成；將 SP 查詢移到 SSMS 測試，不管任何類別都是一秒完成。問題關鍵很神奇也很簡單，差在一個旗標設定 <a href="https://learn.microsoft.com/en-us/sql/t-sql/statements/set-arithabort-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">SET ARITHABORT ON</a>，原因是旗標不同導致 ASP.NET 與 SSMS 使用不同的執行計劃，而 ASP.NET 採用的執行計劃遇上問題類別明顯有問題，清除並重新產生執行計劃通常就能解決。<br> 印象中看過 Rico 這篇 <a href="https://medium.com/ricos-note/ssms%E5%9F%B7%E8%A1%8Csp%E5%B0%B1%E6%98%AF%E6%AF%94-net-%E5%BF%AB%E5%BE%88%E5%A4%9A-b6de95068af6">SSMS執行SP就是比.NET 快很多? - RiCosNote</a>，停用 SSMS <code>SET ARITHABORT ON</code> 成功重現查詢超過 30 秒才快速破案，特此感謝。</p> <p>問題雖解，但對我來說具體發生原因仍然是謎，如何防範心中也沒譜，便再花了點時深入研究，略有心得，在此分享。</p> <p>推薦這篇 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，SQL Server MVP Erland Sommarskog 一篇 2011 年的老文章(網頁風格充滿年代感)，但作者到今年都還有更新，範圍甚至涵蓋 SQL 2022，故不用擔心資訊過時。</p> <p>文章詳細解釋 SP 如何編譯、何謂 Parameter Sniffing、SQL 的執行計劃快取行為，讀完可完全理解「SP 在 SSMS 跑得比較快」這件事是怎麼發生的。</p> <p>文章宇宙無敵長，內容即深且廣，不寫筆記很難吸收，以下是我摘要的重點。</p> <ol><li>查詢 Plan Cache 需要 <a href="https://learn.microsoft.com/zh-tw/sql/t-sql/statements/grant-server-permissions-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#remarks">VIEW SERVER STATE</a> 權限 (SQL 2022 則是 VIEW SERVER PERFORMANCE STATE)</li> <li>SP、Scalar UDF (SQL 2019+)、Multi-Step Table Value Function、Trigger 擁有自己專屬的執行計劃</li> <li>在 SP 呼叫另一個 SP，二者的執行計劃是獨立的，在 SP 動態組裝 SQL 用 sp_executesql 執行，則不會包含在 SP 的執行計劃裡</li> <li>SP 直到第一次執才會建立執行計劃，每次執行 SQL 依資料分佈統計預測最佳執行方式(這個程序稱為 Optimisation)</li> <li>List_orders_3 的執行計劃會跟前兩者不同，不會查索引，而是使用 Clustered Index Scan (相當於 Table Scan)，原因其預測 Estimated Number of Rows Per Execution 是 249 (前二個是 1)，筆數多 Table Scan 比 Index Seek + Key Lookup 有效率。(北風資料庫 Orders 資料表只有 830 筆)</li></ol> <pre><code>CREATE PROCEDURE List_orders_1 AS
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
CREATE PROCEDURE List_orders_2 @fromdate datetime AS
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
go
CREATE PROCEDURE List_orders_3 @fromdate datetime AS
DECLARE @fromdate_copy datetime
SELECT @fromdate_copy = @fromdate
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate_copy
go
</code></pre> <ol start="6"><li>List_orders_1 日期寫成常數，SQL 分析資料分佈都介於 1968-1998，理論上沒有半筆，但基於參考來源是統計數據非實際資料，不確定一定沒資料，Estimated Number of Rows Per Execution 取 1</li> <li>List_orders_2 日期為參數，但 Optimisation 過程會用 20000101 分析，預測筆數也是 1。這個過程稱為 Parameter Sniffing (參數嗅探)</li> <li>List_orders_3 執行時，SQL 不會分析流程邏輯，不知區域變數 @fromdate_copy 來自 @fromdate，假設日期未知，猜 30%，830 * 0.3 = 249</li> <li>List_orders_4 與跟 List_orders_1、List_orders_2 執行計劃相同，理由是編譯時 @formdate 是用 NULL 預估無符合值，預測筆數取 1，但實際會傳回所有資料</li></ol> <pre><code>CREATE PROCEDURE List_orders_4 @fromdate datetime = NULL AS
IF @fromdate IS NULL
SELECT @fromdate = '19900101'
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
</code></pre> <ol start="10"><li>新版 SSMS 執行計劃流程會直接標示預測及實際筆數，但 Popup 視窗仍有 Number of Executions 跟 Estimated Number of Execution 等重要資訊可參考</li> <li>每次重建執行計劃很耗效能，對動轍一分鐘起跳的 OLAP 複雜查詢無所謂且有個別最佳化效果；但在 OLTP 會把 SP 執行計劃存入 Cache 重複以提升效率，直到以下狀況才清除： <ol><li>Buffer Cache 空間滿了(執行計劃與資料表資料共用)，SQL 會清掉很久沒使用的</li> <li>執行 ALTER PROCEDURE</li> <li>執行 sp_recompile</li> <li>執行 <code>DBCC FREEPROCCACHE</code> 清空所有執行計劃快取</li> <li>SQL 重啟</li> <li><code>sp_configure</code> 修改某些 SQL 伺服器參數</li></ol> </li> <li>清除快取後，下回執行 SP 會重做 Parameter Sniffing 產生執行計劃 (執行計劃可能與之前不同)</li> <li>有些事件會導致 SP 的部分指令重新編譯 <ol><li>相關資料表結構異動</li> <li>索引異動(ALTER INDEX/DBCC DBREINDEX)</li> <li>更新統計(SQL自動或CREATE STATISTICS/&nbsp;UPDATE STATISTICS) (不一定觸發重新編譯)</li> <li>對相關資料表執行 sp_recompile</li></ol> </li> <li>建立何種執行計劃由第一次執行的 Parameter Sniffing 決定，故很有可能該執行計劃不適合後續的其他查詢，這是實務上出問題的常見原因。</li> <li>這點很重要：SQL 會為不同 Attribute 或選項組合建立一份專屬執行計劃，以下例子中，不同 DATEFORMAT 各有一份執行計劃：</li></ol> <pre><code>CREATE PROCEDURE List_orders_6 AS
SELECT *
FROM   Orders
WHERE  OrderDate &gt; '12/01/1998'
go
SET DATEFORMAT dmy
go
EXEC List_orders_6
go
SET DATEFORMAT mdy
go
EXEC List_orders_6
go
-- 用以下指令可觀察到兩個不同版本執行計劃
SELECT qs.plan_handle, a.attrlist
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY (SELECT epa.attribute + '=' + convert(nvarchar(127), epa.value) + '   '
FROM   sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  epa.is_cache_key = 1
ORDER  BY epa.attribute
FOR    XML PATH('')) AS a(attrlist)
WHERE  est.objectid = object_id ('dbo.List_orders_6')
AND  est.dbid     = db_id('Northwind')
/*
plan_handle                     attrlist
------------------------------- -------------------------------------------------
0x0500070064EFCA5DB8A0A90500... compat_level=150   date_first=7   date_format=1
set_options=4347   user_id=1
0x0500070064EFCA5DB8A0A80500... compat_level=150   date_first=7   date_format=2
set_options=4347   user_id=1
*/
</code></pre> <ol start="16"><li>以下選項組合可想成 Cache Key：ANSI_PADDING, Parallel Plan,&nbsp;CONCAT_NUL_YIELDS_NULL,&nbsp;ANSI_WARNINGS,&nbsp;ANSI_NULLS,&nbsp;QUOTED_IDENTIFIER,&nbsp;ANSI_NULL_DFLT_ON 及 ARITHABORT，任一個值不同，Cache Key 不同，對映一份專屬執行計劃</li> <li>.NET 跟 SSMS 的預設選項只差在 ARITHABORT，這就是為什麼二者會因使用不同執行計劃，並出現截然不同的查詢速度，而在 SSMS 關閉 ARITHABORT 或加上 <code>SET ARITHABORT OFF</code> 可讓 SSMS 重現 .NET 程式的查詢速度： <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638350569095724823.png"></li> <li>在 .NET 跑查詢時也補上 <code>SET ARITHABORT ON</code> 可以避開應用程式跟 SSMS 跑相同查詢效能天差地別的狀況，但並無法解決因執行計劃不佳造成查詢爆慢問題。</li></ol> <p>至於要怎麼解決執行計劃不適合造成效能問題，要從了解 Parameter Sniffing 下手，這又是另一個值得深入的大議題，留待下一篇討論。</p> <p data-slug="sql-slow-in-ap-fast-in-ssms" data-title-en="SQL performance slow in applciation but fast in SSMS" data-catg=".NET" data-fbimg="https://i.imgur.com/aUm0htZ.jpg#1280#720">Introduction to the execution plan cache issue on perfermance difference between application and SSMS.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>一個常見問題：SQL 上某個 Stored Procedure (以下簡稱 SP)，從網站應用程式執行超慢，甚至等到逾時報錯；將同樣的 SP 及呼叫參數拿到 SSMS (SQL Server Management Studio) 執行卻瞬間完成；或是 SP 慢到無法忍受，將其中語法邏輯 Copy 出來單獨執行卻沒半點問題。</p> <p>我最近遇到的案例是 SP 在查詢某類別資料時超過 30 秒，所有其他類別都是一秒完成；將 SP 查詢移到 SSMS 測試，不管任何類別都是一秒完成。問題關鍵很神奇也很簡單，差在一個旗標設定 <a href="https://learn.microsoft.com/en-us/sql/t-sql/statements/set-arithabort-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580">SET ARITHABORT ON</a>，原因是旗標不同導致 ASP.NET 與 SSMS 使用不同的執行計劃，而 ASP.NET 採用的執行計劃遇上問題類別明顯有問題，清除並重新產生執行計劃通常就能解決。<br> 印象中看過 Rico 這篇 <a href="https://medium.com/ricos-note/ssms%E5%9F%B7%E8%A1%8Csp%E5%B0%B1%E6%98%AF%E6%AF%94-net-%E5%BF%AB%E5%BE%88%E5%A4%9A-b6de95068af6">SSMS執行SP就是比.NET 快很多? - RiCosNote</a>，停用 SSMS <code>SET ARITHABORT ON</code> 成功重現查詢超過 30 秒才快速破案，特此感謝。</p> <p>問題雖解，但對我來說具體發生原因仍然是謎，如何防範心中也沒譜，便再花了點時深入研究，略有心得，在此分享。</p> <p>推薦這篇 - <a href="https://www.sommarskog.se/query-plan-mysteries.html">Slow in the Application, Fast in SSMS? (sommarskog.se)</a>，SQL Server MVP Erland Sommarskog 一篇 2011 年的老文章(網頁風格充滿年代感)，但作者到今年都還有更新，範圍甚至涵蓋 SQL 2022，故不用擔心資訊過時。</p> <p>文章詳細解釋 SP 如何編譯、何謂 Parameter Sniffing、SQL 的執行計劃快取行為，讀完可完全理解「SP 在 SSMS 跑得比較快」這件事是怎麼發生的。</p> <p>文章宇宙無敵長，內容即深且廣，不寫筆記很難吸收，以下是我摘要的重點。</p> <ol><li>查詢 Plan Cache 需要 <a href="https://learn.microsoft.com/zh-tw/sql/t-sql/statements/grant-server-permissions-transact-sql?view=sql-server-ver16&amp;WT.mc_id=DOP-MVP-37580#remarks">VIEW SERVER STATE</a> 權限 (SQL 2022 則是 VIEW SERVER PERFORMANCE STATE)</li> <li>SP、Scalar UDF (SQL 2019+)、Multi-Step Table Value Function、Trigger 擁有自己專屬的執行計劃</li> <li>在 SP 呼叫另一個 SP，二者的執行計劃是獨立的，在 SP 動態組裝 SQL 用 sp_executesql 執行，則不會包含在 SP 的執行計劃裡</li> <li>SP 直到第一次執才會建立執行計劃，每次執行 SQL 依資料分佈統計預測最佳執行方式(這個程序稱為 Optimisation)</li> <li>List_orders_3 的執行計劃會跟前兩者不同，不會查索引，而是使用 Clustered Index Scan (相當於 Table Scan)，原因其預測 Estimated Number of Rows Per Execution 是 249 (前二個是 1)，筆數多 Table Scan 比 Index Seek + Key Lookup 有效率。(北風資料庫 Orders 資料表只有 830 筆)</li></ol> <pre><code>CREATE PROCEDURE List_orders_1 AS
SELECT * FROM Orders WHERE OrderDate &gt; '20000101'
go
CREATE PROCEDURE List_orders_2 @fromdate datetime AS
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
go
CREATE PROCEDURE List_orders_3 @fromdate datetime AS
DECLARE @fromdate_copy datetime
SELECT @fromdate_copy = @fromdate
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate_copy
go
</code></pre> <ol start="6"><li>List_orders_1 日期寫成常數，SQL 分析資料分佈都介於 1968-1998，理論上沒有半筆，但基於參考來源是統計數據非實際資料，不確定一定沒資料，Estimated Number of Rows Per Execution 取 1</li> <li>List_orders_2 日期為參數，但 Optimisation 過程會用 20000101 分析，預測筆數也是 1。這個過程稱為 Parameter Sniffing (參數嗅探)</li> <li>List_orders_3 執行時，SQL 不會分析流程邏輯，不知區域變數 @fromdate_copy 來自 @fromdate，假設日期未知，猜 30%，830 * 0.3 = 249</li> <li>List_orders_4 與跟 List_orders_1、List_orders_2 執行計劃相同，理由是編譯時 @formdate 是用 NULL 預估無符合值，預測筆數取 1，但實際會傳回所有資料</li></ol> <pre><code>CREATE PROCEDURE List_orders_4 @fromdate datetime = NULL AS
IF @fromdate IS NULL
SELECT @fromdate = '19900101'
SELECT * FROM Orders WHERE OrderDate &gt; @fromdate
</code></pre> <ol start="10"><li>新版 SSMS 執行計劃流程會直接標示預測及實際筆數，但 Popup 視窗仍有 Number of Executions 跟 Estimated Number of Execution 等重要資訊可參考</li> <li>每次重建執行計劃很耗效能，對動轍一分鐘起跳的 OLAP 複雜查詢無所謂且有個別最佳化效果；但在 OLTP 會把 SP 執行計劃存入 Cache 重複以提升效率，直到以下狀況才清除： <ol><li>Buffer Cache 空間滿了(執行計劃與資料表資料共用)，SQL 會清掉很久沒使用的</li> <li>執行 ALTER PROCEDURE</li> <li>執行 sp_recompile</li> <li>執行 <code>DBCC FREEPROCCACHE</code> 清空所有執行計劃快取</li> <li>SQL 重啟</li> <li><code>sp_configure</code> 修改某些 SQL 伺服器參數</li></ol> </li> <li>清除快取後，下回執行 SP 會重做 Parameter Sniffing 產生執行計劃 (執行計劃可能與之前不同)</li> <li>有些事件會導致 SP 的部分指令重新編譯 <ol><li>相關資料表結構異動</li> <li>索引異動(ALTER INDEX/DBCC DBREINDEX)</li> <li>更新統計(SQL自動或CREATE STATISTICS/&nbsp;UPDATE STATISTICS) (不一定觸發重新編譯)</li> <li>對相關資料表執行 sp_recompile</li></ol> </li> <li>建立何種執行計劃由第一次執行的 Parameter Sniffing 決定，故很有可能該執行計劃不適合後續的其他查詢，這是實務上出問題的常見原因。</li> <li>這點很重要：SQL 會為不同 Attribute 或選項組合建立一份專屬執行計劃，以下例子中，不同 DATEFORMAT 各有一份執行計劃：</li></ol> <pre><code>CREATE PROCEDURE List_orders_6 AS
SELECT *
FROM   Orders
WHERE  OrderDate &gt; '12/01/1998'
go
SET DATEFORMAT dmy
go
EXEC List_orders_6
go
SET DATEFORMAT mdy
go
EXEC List_orders_6
go
-- 用以下指令可觀察到兩個不同版本執行計劃
SELECT qs.plan_handle, a.attrlist
FROM   sys.dm_exec_query_stats qs
CROSS  APPLY sys.dm_exec_sql_text(qs.sql_handle) est
CROSS  APPLY (SELECT epa.attribute + '=' + convert(nvarchar(127), epa.value) + '   '
FROM   sys.dm_exec_plan_attributes(qs.plan_handle) epa
WHERE  epa.is_cache_key = 1
ORDER  BY epa.attribute
FOR    XML PATH('')) AS a(attrlist)
WHERE  est.objectid = object_id ('dbo.List_orders_6')
AND  est.dbid     = db_id('Northwind')
/*
plan_handle                     attrlist
------------------------------- -------------------------------------------------
0x0500070064EFCA5DB8A0A90500... compat_level=150   date_first=7   date_format=1
set_options=4347   user_id=1
0x0500070064EFCA5DB8A0A80500... compat_level=150   date_first=7   date_format=2
set_options=4347   user_id=1
*/
</code></pre> <ol start="16"><li>以下選項組合可想成 Cache Key：ANSI_PADDING, Parallel Plan,&nbsp;CONCAT_NUL_YIELDS_NULL,&nbsp;ANSI_WARNINGS,&nbsp;ANSI_NULLS,&nbsp;QUOTED_IDENTIFIER,&nbsp;ANSI_NULL_DFLT_ON 及 ARITHABORT，任一個值不同，Cache Key 不同，對映一份專屬執行計劃</li> <li>.NET 跟 SSMS 的預設選項只差在 ARITHABORT，這就是為什麼二者會因使用不同執行計劃，並出現截然不同的查詢速度，而在 SSMS 關閉 ARITHABORT 或加上 <code>SET ARITHABORT OFF</code> 可讓 SSMS 重現 .NET 程式的查詢速度： <img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638350569095724823.png"></li> <li>在 .NET 跑查詢時也補上 <code>SET ARITHABORT ON</code> 可以避開應用程式跟 SSMS 跑相同查詢效能天差地別的狀況，但並無法解決因執行計劃不佳造成查詢爆慢問題。</li></ol> <p>至於要怎麼解決執行計劃不適合造成效能問題，要從了解 Parameter Sniffing 下手，這又是另一個值得深入的大議題，留待下一篇討論。</p> <p data-slug="sql-slow-in-ap-fast-in-ssms" data-title-en="SQL performance slow in applciation but fast in SSMS" data-catg=".NET" data-fbimg="https://i.imgur.com/aUm0htZ.jpg#1280#720">Introduction to the execution plan cache issue on perfermance difference between application and SSMS.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638350569095724823.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638350569095724823.png" medium="image"/>
    <pubDate>Wed, 08 Nov 2023 15:53:56 GMT</pubDate>
  </item><item>
    <title><![CDATA[大砲打小鳥之 ChatGPT 翻譯鳳梨酥 LINE Bot]]></title>
    <link>https://blog.darkthread.net/blog/line-gpt-trans-bot/</link>
    <itunes:title><![CDATA[大砲打小鳥之 ChatGPT 翻譯鳳梨酥 LINE Bot]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>身為程序員，一直把「增進人類全體之生活」當成使命，沒能力為人類產生貢獻，能「增進家人之生活」也好。</p> <p>長輩請了外籍看護，國語跟英文只到勉強溝通，最後 LINE 加翻譯軟體竟是目前效果最好的溝通方式。結果我做了件「用大砲打小鳥」的事 - 用 ASP.NET Core 寫 LINE Bot 接 ChatGPT 在 LINE 群組幫忙翻譯，專案代號就命名為「翻譯鳳梨酥 」。</p> <p><a href="https://www.facebook.com/darkthread.lds/posts/828434687819280">群組回話 LINE Bot</a>、<a href="/blog/chatgpt-console-chat/">串 ChatGPT API </a>之前玩過了，這回只是把兩個功能摻在一起，主程式不用一小時寫完，小有快刀斬亂麻的暢快。<br> (所以說，平日老為賦新辭強說愁，四處找雞毛蒜皮的無聊題目練習，看似沒意義，但說不準能在某個關鍵時刻派上用場，「你練過的功不會背叛你」再添一例。)</p> <p>使用方式是將「翻譯鳳梨酥」加入群組偷聽對話，看到 # 語系標記它就會跳出來翻譯，貼心設計是翻譯內容一併附上英文供對照，方便核結果正確性。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347745633587367.png"></p> <p>初始版本有點小問題，像是 "早上七點幫阿公量血壓" 被翻成 "I measured my grandpa's blood pressure at seven in the morning"，英文對照發揮作用有看得出問題。</p> <p>加上 "請" 字強調指示是個 Workaround，但時時需要注意措辭是小看了 ChatGPT 的能耐，也讓開發者面子掛不住，這類問題一般可以透過調整 ChatGPT 提示改善。我的原本的初始化提示是：</p> <p><code>你是一名多國語言翻譯，請將以下文字內容分別翻譯成英文以及{指定言語}，用語需口語化及生活化。</code></p> <p>修改成：</p> <p><code>你是一名語言翻譯，負責協助僱主與外籍看護溝通，主要工作為將僱主的中文指令翻成外語，將外語翻成中文，用語需口語化及生活化。現在請將以下文字內容分別翻譯成英文以及{指定言語}。</code></p> <p>Prompt 調整後，效果明顯改善，目前沒再看到明顯錯誤。</p> <p>簡單說下程式寫法。ChatGPT 部分我是直接拿 <a href="/blog/chatgpt-console-chat/">ChatGPT 聊天程式練習</a> 的 ChatGptService 來用，每次當成全新對談，不必帶入先前內容，省 Token (ChatGPT API 以 Token 計費)也加快處理速度。LINE Bot API 需一次送出完整內容，故也用不到 Streaming 技巧。</p> <p>餘下的全部程式邏輯我寫在 Minimal API Program.cs，整個專案程式就一個 Program.cs 加一個 ChatGptService.cs。Program.cs 如下，LINE API 串接細節由 David 老師的 <a href="/blog/line-bot-sdk-notes/">LineBot SDK</a> 包辦，我的實作邏輯不到一百行，其中三分之一還是我資安強迫症發作，加了 LINE 群組 ID 白名單機制管控權限，防止機器人被加到其他群組盜用。</p> <pre><code>using System.Security.Cryptography;
using System.Text;
using isRock.LineBot;
NLog.Logger logger = NLog.LogManager.GetCurrentClassLogger();
var builder = WebApplication.CreateBuilder(args);
Func&lt;string, string&gt; readConfig = (key) =&gt; builder.Configuration.GetValue&lt;string&gt;(key) ?? throw new ArgumentNullException(key);
var pwdChk = readConfig("GrantPwd");
var chatSvc = new GptTranslatorService(readConfig("OpenAiUrl"), readConfig("OpenAiKey"), readConfig("OpenAiDepName"));
var lineBotKey = readConfig("LineBotToken");
var lineBot = new Bot(lineBotKey);
var app = builder.Build();
app.MapGet("/", () =&gt; "Line GPT Translator Bot");
List&lt;string&gt; groupIds = new List&lt;string&gt;();
var grpIdPath = Path.Combine(app.Environment.ContentRootPath, "data", "groupIds.txt");
LoadGroupIds();
app.MapPost("/Grant", async (context) =&gt;
{
var grpId = context.Request.Form["grpId"].ToString();
var pwd = context.Request.Form["pwd"].ToString();
if (pwd == pwdChk)
{
if (!groupIds.Contains(grpId))
{
logger.Info($"Grant {grpId}");
groupIds.Add(grpId);
SaveGroupIds();
}
await context.Response.WriteAsync("OK");
}
else
{
context.Response.StatusCode = 401;
await context.Response.WriteAsync("Unauthorized");
}
});
app.MapPost("/MsgCallback", async (context) =&gt;
{
var body = context.Request.Body;
using var reader = new StreamReader(body);
var json = await reader.ReadToEndAsync();
var msg = Utility.Parsing(json);
foreach (var evt in msg.events)
{
if (evt.type == "message")
{
if (evt.source.type == "group" &amp;&amp; evt.message.type == "text")
{
var grpId = evt.source.groupId;
var text = evt.message.text;
logger.Trace("訊息[{0}]： {1}", grpId, text);
if (text.StartsWith("#REG"))
{
lineBot.PushMessage(grpId, "註冊序號 - " + grpId);
break;
}
if (!groupIds.Contains(grpId))
{
lineBot.PushMessage(grpId, "群組未註冊");
break;
}
var src = text.Substring(3);
if (text.StartsWith("#") &amp;&amp; Enum.TryParse&lt;Langs&gt;(text.Substring(1, 2).ToUpper(), out var lang))
{
var transText = await chatSvc.Translate(lang, src);
logger.Trace("翻譯：
" + transText);
lineBot.PushMessage(grpId, transText);
}
}
}
}
await context.Response.WriteAsync("OK");
});
app.UsePathBase("/line-gpt-trans-bot");
app.Run();
void LoadGroupIds()
{
if (File.Exists(grpIdPath))
{
groupIds = File.ReadAllLines(grpIdPath).Where(o =&gt; !string.IsNullOrEmpty(o)).ToList();
}
}
void SaveGroupIds()
{
File.WriteAllLines(grpIdPath!, groupIds.Distinct().ToArray());
}
</code></pre> <p>會寫程式真好!</p> <p data-slug="line-gpt-trans-bot" data-title-en="Group language translator Line BOT with ChatGPT" data-catg=".NET" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960">Example of LINE Bot which using Azure OpenAI ChatGPT API to translate between languages in group conversation.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>身為程序員，一直把「增進人類全體之生活」當成使命，沒能力為人類產生貢獻，能「增進家人之生活」也好。</p> <p>長輩請了外籍看護，國語跟英文只到勉強溝通，最後 LINE 加翻譯軟體竟是目前效果最好的溝通方式。結果我做了件「用大砲打小鳥」的事 - 用 ASP.NET Core 寫 LINE Bot 接 ChatGPT 在 LINE 群組幫忙翻譯，專案代號就命名為「翻譯鳳梨酥 」。</p> <p><a href="https://www.facebook.com/darkthread.lds/posts/828434687819280">群組回話 LINE Bot</a>、<a href="/blog/chatgpt-console-chat/">串 ChatGPT API </a>之前玩過了，這回只是把兩個功能摻在一起，主程式不用一小時寫完，小有快刀斬亂麻的暢快。<br> (所以說，平日老為賦新辭強說愁，四處找雞毛蒜皮的無聊題目練習，看似沒意義，但說不準能在某個關鍵時刻派上用場，「你練過的功不會背叛你」再添一例。)</p> <p>使用方式是將「翻譯鳳梨酥」加入群組偷聽對話，看到 # 語系標記它就會跳出來翻譯，貼心設計是翻譯內容一併附上英文供對照，方便核結果正確性。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347745633587367.png"></p> <p>初始版本有點小問題，像是 "早上七點幫阿公量血壓" 被翻成 "I measured my grandpa's blood pressure at seven in the morning"，英文對照發揮作用有看得出問題。</p> <p>加上 "請" 字強調指示是個 Workaround，但時時需要注意措辭是小看了 ChatGPT 的能耐，也讓開發者面子掛不住，這類問題一般可以透過調整 ChatGPT 提示改善。我的原本的初始化提示是：</p> <p><code>你是一名多國語言翻譯，請將以下文字內容分別翻譯成英文以及{指定言語}，用語需口語化及生活化。</code></p> <p>修改成：</p> <p><code>你是一名語言翻譯，負責協助僱主與外籍看護溝通，主要工作為將僱主的中文指令翻成外語，將外語翻成中文，用語需口語化及生活化。現在請將以下文字內容分別翻譯成英文以及{指定言語}。</code></p> <p>Prompt 調整後，效果明顯改善，目前沒再看到明顯錯誤。</p> <p>簡單說下程式寫法。ChatGPT 部分我是直接拿 <a href="/blog/chatgpt-console-chat/">ChatGPT 聊天程式練習</a> 的 ChatGptService 來用，每次當成全新對談，不必帶入先前內容，省 Token (ChatGPT API 以 Token 計費)也加快處理速度。LINE Bot API 需一次送出完整內容，故也用不到 Streaming 技巧。</p> <p>餘下的全部程式邏輯我寫在 Minimal API Program.cs，整個專案程式就一個 Program.cs 加一個 ChatGptService.cs。Program.cs 如下，LINE API 串接細節由 David 老師的 <a href="/blog/line-bot-sdk-notes/">LineBot SDK</a> 包辦，我的實作邏輯不到一百行，其中三分之一還是我資安強迫症發作，加了 LINE 群組 ID 白名單機制管控權限，防止機器人被加到其他群組盜用。</p> <pre><code>using System.Security.Cryptography;
using System.Text;
using isRock.LineBot;
NLog.Logger logger = NLog.LogManager.GetCurrentClassLogger();
var builder = WebApplication.CreateBuilder(args);
Func&lt;string, string&gt; readConfig = (key) =&gt; builder.Configuration.GetValue&lt;string&gt;(key) ?? throw new ArgumentNullException(key);
var pwdChk = readConfig("GrantPwd");
var chatSvc = new GptTranslatorService(readConfig("OpenAiUrl"), readConfig("OpenAiKey"), readConfig("OpenAiDepName"));
var lineBotKey = readConfig("LineBotToken");
var lineBot = new Bot(lineBotKey);
var app = builder.Build();
app.MapGet("/", () =&gt; "Line GPT Translator Bot");
List&lt;string&gt; groupIds = new List&lt;string&gt;();
var grpIdPath = Path.Combine(app.Environment.ContentRootPath, "data", "groupIds.txt");
LoadGroupIds();
app.MapPost("/Grant", async (context) =&gt;
{
var grpId = context.Request.Form["grpId"].ToString();
var pwd = context.Request.Form["pwd"].ToString();
if (pwd == pwdChk)
{
if (!groupIds.Contains(grpId))
{
logger.Info($"Grant {grpId}");
groupIds.Add(grpId);
SaveGroupIds();
}
await context.Response.WriteAsync("OK");
}
else
{
context.Response.StatusCode = 401;
await context.Response.WriteAsync("Unauthorized");
}
});
app.MapPost("/MsgCallback", async (context) =&gt;
{
var body = context.Request.Body;
using var reader = new StreamReader(body);
var json = await reader.ReadToEndAsync();
var msg = Utility.Parsing(json);
foreach (var evt in msg.events)
{
if (evt.type == "message")
{
if (evt.source.type == "group" &amp;&amp; evt.message.type == "text")
{
var grpId = evt.source.groupId;
var text = evt.message.text;
logger.Trace("訊息[{0}]： {1}", grpId, text);
if (text.StartsWith("#REG"))
{
lineBot.PushMessage(grpId, "註冊序號 - " + grpId);
break;
}
if (!groupIds.Contains(grpId))
{
lineBot.PushMessage(grpId, "群組未註冊");
break;
}
var src = text.Substring(3);
if (text.StartsWith("#") &amp;&amp; Enum.TryParse&lt;Langs&gt;(text.Substring(1, 2).ToUpper(), out var lang))
{
var transText = await chatSvc.Translate(lang, src);
logger.Trace("翻譯：
" + transText);
lineBot.PushMessage(grpId, transText);
}
}
}
}
await context.Response.WriteAsync("OK");
});
app.UsePathBase("/line-gpt-trans-bot");
app.Run();
void LoadGroupIds()
{
if (File.Exists(grpIdPath))
{
groupIds = File.ReadAllLines(grpIdPath).Where(o =&gt; !string.IsNullOrEmpty(o)).ToList();
}
}
void SaveGroupIds()
{
File.WriteAllLines(grpIdPath!, groupIds.Distinct().ToArray());
}
</code></pre> <p>會寫程式真好!</p> <p data-slug="line-gpt-trans-bot" data-title-en="Group language translator Line BOT with ChatGPT" data-catg=".NET" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960">Example of LINE Bot which using Azure OpenAI ChatGPT API to translate between languages in group conversation.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>身為程序員，一直把「增進人類全體之生活」當成使命，沒能力為人類產生貢獻，能「增進家人之生活」也好。</p> <p>長輩請了外籍看護，國語跟英文只到勉強溝通，最後 LINE 加翻譯軟體竟是目前效果最好的溝通方式。結果我做了件「用大砲打小鳥」的事 - 用 ASP.NET Core 寫 LINE Bot 接 ChatGPT 在 LINE 群組幫忙翻譯，專案代號就命名為「翻譯鳳梨酥 」。</p> <p><a href="https://www.facebook.com/darkthread.lds/posts/828434687819280">群組回話 LINE Bot</a>、<a href="/blog/chatgpt-console-chat/">串 ChatGPT API </a>之前玩過了，這回只是把兩個功能摻在一起，主程式不用一小時寫完，小有快刀斬亂麻的暢快。<br> (所以說，平日老為賦新辭強說愁，四處找雞毛蒜皮的無聊題目練習，看似沒意義，但說不準能在某個關鍵時刻派上用場，「你練過的功不會背叛你」再添一例。)</p> <p>使用方式是將「翻譯鳳梨酥」加入群組偷聽對話，看到 # 語系標記它就會跳出來翻譯，貼心設計是翻譯內容一併附上英文供對照，方便核結果正確性。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347745633587367.png"></p> <p>初始版本有點小問題，像是 "早上七點幫阿公量血壓" 被翻成 "I measured my grandpa's blood pressure at seven in the morning"，英文對照發揮作用有看得出問題。</p> <p>加上 "請" 字強調指示是個 Workaround，但時時需要注意措辭是小看了 ChatGPT 的能耐，也讓開發者面子掛不住，這類問題一般可以透過調整 ChatGPT 提示改善。我的原本的初始化提示是：</p> <p><code>你是一名多國語言翻譯，請將以下文字內容分別翻譯成英文以及{指定言語}，用語需口語化及生活化。</code></p> <p>修改成：</p> <p><code>你是一名語言翻譯，負責協助僱主與外籍看護溝通，主要工作為將僱主的中文指令翻成外語，將外語翻成中文，用語需口語化及生活化。現在請將以下文字內容分別翻譯成英文以及{指定言語}。</code></p> <p>Prompt 調整後，效果明顯改善，目前沒再看到明顯錯誤。</p> <p>簡單說下程式寫法。ChatGPT 部分我是直接拿 <a href="/blog/chatgpt-console-chat/">ChatGPT 聊天程式練習</a> 的 ChatGptService 來用，每次當成全新對談，不必帶入先前內容，省 Token (ChatGPT API 以 Token 計費)也加快處理速度。LINE Bot API 需一次送出完整內容，故也用不到 Streaming 技巧。</p> <p>餘下的全部程式邏輯我寫在 Minimal API Program.cs，整個專案程式就一個 Program.cs 加一個 ChatGptService.cs。Program.cs 如下，LINE API 串接細節由 David 老師的 <a href="/blog/line-bot-sdk-notes/">LineBot SDK</a> 包辦，我的實作邏輯不到一百行，其中三分之一還是我資安強迫症發作，加了 LINE 群組 ID 白名單機制管控權限，防止機器人被加到其他群組盜用。</p> <pre><code>using System.Security.Cryptography;
using System.Text;
using isRock.LineBot;
NLog.Logger logger = NLog.LogManager.GetCurrentClassLogger();
var builder = WebApplication.CreateBuilder(args);
Func&lt;string, string&gt; readConfig = (key) =&gt; builder.Configuration.GetValue&lt;string&gt;(key) ?? throw new ArgumentNullException(key);
var pwdChk = readConfig("GrantPwd");
var chatSvc = new GptTranslatorService(readConfig("OpenAiUrl"), readConfig("OpenAiKey"), readConfig("OpenAiDepName"));
var lineBotKey = readConfig("LineBotToken");
var lineBot = new Bot(lineBotKey);
var app = builder.Build();
app.MapGet("/", () =&gt; "Line GPT Translator Bot");
List&lt;string&gt; groupIds = new List&lt;string&gt;();
var grpIdPath = Path.Combine(app.Environment.ContentRootPath, "data", "groupIds.txt");
LoadGroupIds();
app.MapPost("/Grant", async (context) =&gt;
{
var grpId = context.Request.Form["grpId"].ToString();
var pwd = context.Request.Form["pwd"].ToString();
if (pwd == pwdChk)
{
if (!groupIds.Contains(grpId))
{
logger.Info($"Grant {grpId}");
groupIds.Add(grpId);
SaveGroupIds();
}
await context.Response.WriteAsync("OK");
}
else
{
context.Response.StatusCode = 401;
await context.Response.WriteAsync("Unauthorized");
}
});
app.MapPost("/MsgCallback", async (context) =&gt;
{
var body = context.Request.Body;
using var reader = new StreamReader(body);
var json = await reader.ReadToEndAsync();
var msg = Utility.Parsing(json);
foreach (var evt in msg.events)
{
if (evt.type == "message")
{
if (evt.source.type == "group" &amp;&amp; evt.message.type == "text")
{
var grpId = evt.source.groupId;
var text = evt.message.text;
logger.Trace("訊息[{0}]： {1}", grpId, text);
if (text.StartsWith("#REG"))
{
lineBot.PushMessage(grpId, "註冊序號 - " + grpId);
break;
}
if (!groupIds.Contains(grpId))
{
lineBot.PushMessage(grpId, "群組未註冊");
break;
}
var src = text.Substring(3);
if (text.StartsWith("#") &amp;&amp; Enum.TryParse&lt;Langs&gt;(text.Substring(1, 2).ToUpper(), out var lang))
{
var transText = await chatSvc.Translate(lang, src);
logger.Trace("翻譯：
" + transText);
lineBot.PushMessage(grpId, transText);
}
}
}
}
await context.Response.WriteAsync("OK");
});
app.UsePathBase("/line-gpt-trans-bot");
app.Run();
void LoadGroupIds()
{
if (File.Exists(grpIdPath))
{
groupIds = File.ReadAllLines(grpIdPath).Where(o =&gt; !string.IsNullOrEmpty(o)).ToList();
}
}
void SaveGroupIds()
{
File.WriteAllLines(grpIdPath!, groupIds.Distinct().ToArray());
}
</code></pre> <p>會寫程式真好!</p> <p data-slug="line-gpt-trans-bot" data-title-en="Group language translator Line BOT with ChatGPT" data-catg=".NET" data-fbimg="https://i.imgur.com/TMfgSka.jpg#1706#960">Example of LINE Bot which using Azure OpenAI ChatGPT API to translate between languages in group conversation.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638347745633587367.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638347745633587367.png" medium="image"/>
    <pubDate>Sun, 05 Nov 2023 09:45:08 GMT</pubDate>
  </item><item>
    <title><![CDATA[CLI 小技巧 - 文字檔搜尋關鍵字並顯示前後文(grep / PowerShell Select-String)]]></title>
    <link>https://blog.darkthread.net/blog/cli-show-matched-line-with-context/</link>
    <itunes:title><![CDATA[CLI 小技巧 - 文字檔搜尋關鍵字並顯示前後文(grep / PowerShell Select-String)]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>最近有個小需求，想從 cmdkey /list 記憶密碼清單找出用某帳號登入的伺服器對象，例如想查出 jeffrey 被用來登入 TERMSRV/RemoteSvr、RemoteSvr(Domain)、TERMSRV/172.28.1.1。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347065063323322.png"></p> <p>上圖項目不多，直接用眼睛看即可。我的實際案例包含 git、sourcetree、MongoDB Compass... 等五花八門紀錄數十條，我想下指令篩選，試了 findstr，找到是找到了，但只看到帳號不知連到哪裡，查了個寂寞。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638347065065231368.png"></p> <p>findstr 的能耐只到這裡，想多顯示帳號上方兩行內容，需要換工具。</p> <p><a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 內附 UNIX 界的強大文字搜尋工具 - <a href="https://zh.wikipedia.org/zh-tw/Grep">grep</a>，grep 除了支援正則表示式查詢(Regular Expression 也是讓程式設計師人生變彩色的技能之一，推薦花點時間學會)，<a href="https://www.gnu.org/software/grep/manual/grep.html#Context-Line-Control-1">顯示參數</a>包含 -B <em>num</em>、-A <em>num</em>，可指定多顯示前後幾行內容。<code>cmdkey /list | grep -B 3 jeffrey</code> 會顯示 jeffrey 所在行及其上方三行內容，得到我要的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638347065067069025.png"></p> <p>在 Windows 要使用 grep 需另外安裝，有沒有原生的 CLI 工具？</p> <p>有，Windows PowerShell 對映 grep 的 CLI 工具是 Select-String，一樣支援正則表示式並有 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.utility/select-string?view=powershell-5.1&amp;WT.mc_id=DOP-MVP-37580#-context">-Context 參數</a>指定前後文要包含的行數，-Context 3,1 代表包含上三行下一行，-Context 3 表前後多顯示三行。故本例可寫成 <code>cmdkey /list | Select-String jeffrey -Context 3,0</code>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638347065068942299.png"></p> <p>CLI 技能 + 0.5。</p> <p data-slug="cli-show-matched-line-with-context" data-fbimg="https://cdn.pixabay.com/photo/2017/05/07/22/40/uncorking-2293858_960_720.jpg#960#720"> </p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>最近有個小需求，想從 cmdkey /list 記憶密碼清單找出用某帳號登入的伺服器對象，例如想查出 jeffrey 被用來登入 TERMSRV/RemoteSvr、RemoteSvr(Domain)、TERMSRV/172.28.1.1。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347065063323322.png"></p> <p>上圖項目不多，直接用眼睛看即可。我的實際案例包含 git、sourcetree、MongoDB Compass... 等五花八門紀錄數十條，我想下指令篩選，試了 findstr，找到是找到了，但只看到帳號不知連到哪裡，查了個寂寞。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638347065065231368.png"></p> <p>findstr 的能耐只到這裡，想多顯示帳號上方兩行內容，需要換工具。</p> <p><a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 內附 UNIX 界的強大文字搜尋工具 - <a href="https://zh.wikipedia.org/zh-tw/Grep">grep</a>，grep 除了支援正則表示式查詢(Regular Expression 也是讓程式設計師人生變彩色的技能之一，推薦花點時間學會)，<a href="https://www.gnu.org/software/grep/manual/grep.html#Context-Line-Control-1">顯示參數</a>包含 -B <em>num</em>、-A <em>num</em>，可指定多顯示前後幾行內容。<code>cmdkey /list | grep -B 3 jeffrey</code> 會顯示 jeffrey 所在行及其上方三行內容，得到我要的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638347065067069025.png"></p> <p>在 Windows 要使用 grep 需另外安裝，有沒有原生的 CLI 工具？</p> <p>有，Windows PowerShell 對映 grep 的 CLI 工具是 Select-String，一樣支援正則表示式並有 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.utility/select-string?view=powershell-5.1&amp;WT.mc_id=DOP-MVP-37580#-context">-Context 參數</a>指定前後文要包含的行數，-Context 3,1 代表包含上三行下一行，-Context 3 表前後多顯示三行。故本例可寫成 <code>cmdkey /list | Select-String jeffrey -Context 3,0</code>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638347065068942299.png"></p> <p>CLI 技能 + 0.5。</p> <p data-slug="cli-show-matched-line-with-context" data-fbimg="https://cdn.pixabay.com/photo/2017/05/07/22/40/uncorking-2293858_960_720.jpg#960#720"> </p>]]>
    </description>
    <content:encoded><![CDATA[<p>最近有個小需求，想從 cmdkey /list 記憶密碼清單找出用某帳號登入的伺服器對象，例如想查出 jeffrey 被用來登入 TERMSRV/RemoteSvr、RemoteSvr(Domain)、TERMSRV/172.28.1.1。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638347065063323322.png"></p> <p>上圖項目不多，直接用眼睛看即可。我的實際案例包含 git、sourcetree、MongoDB Compass... 等五花八門紀錄數十條，我想下指令篩選，試了 findstr，找到是找到了，但只看到帳號不知連到哪裡，查了個寂寞。</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638347065065231368.png"></p> <p>findstr 的能耐只到這裡，想多顯示帳號上方兩行內容，需要換工具。</p> <p><a href="https://blog.miniasp.com/post/2015/09/27/Useful-tool-Cmder">Cmder</a> 內附 UNIX 界的強大文字搜尋工具 - <a href="https://zh.wikipedia.org/zh-tw/Grep">grep</a>，grep 除了支援正則表示式查詢(Regular Expression 也是讓程式設計師人生變彩色的技能之一，推薦花點時間學會)，<a href="https://www.gnu.org/software/grep/manual/grep.html#Context-Line-Control-1">顯示參數</a>包含 -B <em>num</em>、-A <em>num</em>，可指定多顯示前後幾行內容。<code>cmdkey /list | grep -B 3 jeffrey</code> 會顯示 jeffrey 所在行及其上方三行內容，得到我要的結果：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig3_638347065067069025.png"></p> <p>在 Windows 要使用 grep 需另外安裝，有沒有原生的 CLI 工具？</p> <p>有，Windows PowerShell 對映 grep 的 CLI 工具是 Select-String，一樣支援正則表示式並有 <a href="https://learn.microsoft.com/zh-tw/powershell/module/microsoft.powershell.utility/select-string?view=powershell-5.1&amp;WT.mc_id=DOP-MVP-37580#-context">-Context 參數</a>指定前後文要包含的行數，-Context 3,1 代表包含上三行下一行，-Context 3 表前後多顯示三行。故本例可寫成 <code>cmdkey /list | Select-String jeffrey -Context 3,0</code>：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig4_638347065068942299.png"></p> <p>CLI 技能 + 0.5。</p> <p data-slug="cli-show-matched-line-with-context" data-fbimg="https://cdn.pixabay.com/photo/2017/05/07/22/40/uncorking-2293858_960_720.jpg#960#720"> </p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638347065063323322.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638347065063323322.png" medium="image"/>
    <pubDate>Sat, 04 Nov 2023 14:53:09 GMT</pubDate>
  </item><item>
    <title><![CDATA[Coding4Fun - 將 PDF 逐頁轉成圖檔(使用 PDFiumCore)]]></title>
    <link>https://blog.darkthread.net/blog/pdfium-core/</link>
    <itunes:title><![CDATA[Coding4Fun - 將 PDF 逐頁轉成圖檔(使用 PDFiumCore)]]></itunes:title>
    <itunes:author><![CDATA[service@darkthread.net (Jeffrey Lee)]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>突發奇想閃過念頭，想把 PDF 投影片逐頁轉成圖檔，索性當成 .NET 練習題伸展暖身，說做就做。</p> <p>.NET PDF 元件選擇不少，但好用的多是商用軟體，開源專案如 <a href="https://blog.darkthread.net/blog/regex-positive-lookahead/">iTextSharp</a>、<a href="https://fullstackladder.dev/blog/2023/02/05/introduction-quest-pdf/">QuestPDF</a> 新版也改成商業授權，而轉存圖檔不同於編譯修改文件，訴求重點不同，順便趁機認識不同的程式庫。</p> <p>這回我選擇的是 Google 的開源 PDF 引擎 - <a href="https://pdfium.googlesource.com/pdfium/">PDFium</a> (Google 與 Foxit 共同開源，採 <a href="https://zh.wikipedia.org/wiki/BSD%E8%AE%B8%E5%8F%AF%E8%AF%81">BSD 3-Clause 授權</a>，<a href="https://zhuanlan.zhihu.com/p/440886739">參考</a>)，其最大賣點為 Chrome 就是靠它顯示 PDF 文件! 沒人敢懷疑 Chrome/Edge 顯示 PDF 的效能或正確性(PDF 文件沒法用 Chrome 看，大家會覺得 PDF 文件有問題 XD)，用 PDFium 完全不用擔心跑不動大檔或排版錯亂，若不幸遇上，請儘速通知 PDF 文件作者自我檢討 😛</p> <p>PDFium 是原生程式庫，直接用 .NET 呼叫太累，一般會借助第三方 .NET Wrapper 程式庫，生活比較輕鬆愉快。我找到兩個較多人推的 PDFium Wrapper：</p> <ul><li><a href="https://github.com/ArgusMagnus/PDFiumSharp">PDFiumSharp</a><br> 版本偏舊，適合 .NET Framework/Windows 平台，已六年未更新</li> <li><a href="https://github.com/Dtronix/PDFiumCore">PDFiumCore</a><br> 仍持續更新，可支援 .NET 6 及跨 Linux 平台</li></ul> <p>想用在 .NET 6+，選 PDFiumCore 就對了。</p> <p>PDFium 是個渲染引擎，核心功能是將 PDF 文件繪製成畫面，轉存圖檔正是它的拿手好戲，PDFiumCore 專案也有現成的<a href="https://github.com/Dtronix/PDFiumCore/blob/master/src/PDFiumCoreDemo/Program.cs">頁面轉存圖檔範例</a>，轉檔過程有點小複雜，需動用到矩陣運算：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638341833431896825.png"></p> <p>不過，我只是單純 PDF 頁面轉圖檔，程式不用怎麼改，拿香跟著拜就對了。程式範例如下：</p> <pre><code>using System.Drawing;
using System.Drawing.Imaging;
using PDFiumCore;
var scale = 1;
fpdfview.FPDF_InitLibrary();
var path = "blogger-story.pdf";
ExportImages(path, scale);
static void ExportImages(string pdfPath, float scale)
{
var imgFolder = Path.Combine("images", Path.GetFileNameWithoutExtension(pdfPath));
Directory.CreateDirectory(imgFolder);
// 載入PDF
var document = fpdfview.FPDF_LoadDocument(pdfPath, null);
// 取得總頁數
var pageCount = fpdfview.FPDF_GetPageCount(document);
for (var pageIndex = 0; pageIndex &lt; pageCount; pageIndex++)
{
// 逐頁載入
var page = fpdfview.FPDF_LoadPage(document, pageIndex);
// 取得尺寸，依比例縮放
var size = new FS_SIZEF_();
fpdfview.FPDF_GetPageSizeByIndexF(document, 0, size);
var width = (int)Math.Round(size.Width * scale);
var height = (int)Math.Round(size.Height * scale);
var bitmap = fpdfview.FPDFBitmapCreateEx(
width,
height,
(int)FPDFBitmapFormat.BGRA,
IntPtr.Zero,
0);
// 填入白色背景
fpdfview.FPDFBitmapFillRect(bitmap, 0, 0, width, height, (uint)Color.White.ToArgb());
// |          | a b 0 |
// | matrix = | c d 0 |
// |          | e f 1 |
using var matrix = new FS_MATRIX_();
using var clipping = new FS_RECTF_();
matrix.A = scale;
matrix.B = 0;
matrix.C = 0;
matrix.D = scale;
matrix.E = 0;
matrix.F = 0;
clipping.Left = 0;
clipping.Right = width;
clipping.Bottom = 0;
clipping.Top = height;
fpdfview.FPDF_RenderPageBitmapWithMatrix(bitmap, page, matrix, clipping, (int)RenderFlags.RenderAnnotations);
var bitmapImage = new Bitmap(
width,
height,
fpdfview.FPDFBitmapGetStride(bitmap),
PixelFormat.Format32bppArgb,
fpdfview.FPDFBitmapGetBuffer(bitmap));
bitmapImage.Save(
Path.Combine(imgFolder, $"{pageIndex:000}.png"),
ImageFormat.Png
);
}
}
</code></pre> <p>簡單拼裝一下。插電，開機，輕鬆秒殺~</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638341833439259530.png"></p> <p data-slug="pdfium-core" data-title-en="Export PDF to images with PDFiumCore" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/07/31/11/37/cold-2557515_960_720.jpg#960#720">Example of how to export PDF to images with PDFiumCore library.</p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>突發奇想閃過念頭，想把 PDF 投影片逐頁轉成圖檔，索性當成 .NET 練習題伸展暖身，說做就做。</p> <p>.NET PDF 元件選擇不少，但好用的多是商用軟體，開源專案如 <a href="https://blog.darkthread.net/blog/regex-positive-lookahead/">iTextSharp</a>、<a href="https://fullstackladder.dev/blog/2023/02/05/introduction-quest-pdf/">QuestPDF</a> 新版也改成商業授權，而轉存圖檔不同於編譯修改文件，訴求重點不同，順便趁機認識不同的程式庫。</p> <p>這回我選擇的是 Google 的開源 PDF 引擎 - <a href="https://pdfium.googlesource.com/pdfium/">PDFium</a> (Google 與 Foxit 共同開源，採 <a href="https://zh.wikipedia.org/wiki/BSD%E8%AE%B8%E5%8F%AF%E8%AF%81">BSD 3-Clause 授權</a>，<a href="https://zhuanlan.zhihu.com/p/440886739">參考</a>)，其最大賣點為 Chrome 就是靠它顯示 PDF 文件! 沒人敢懷疑 Chrome/Edge 顯示 PDF 的效能或正確性(PDF 文件沒法用 Chrome 看，大家會覺得 PDF 文件有問題 XD)，用 PDFium 完全不用擔心跑不動大檔或排版錯亂，若不幸遇上，請儘速通知 PDF 文件作者自我檢討 😛</p> <p>PDFium 是原生程式庫，直接用 .NET 呼叫太累，一般會借助第三方 .NET Wrapper 程式庫，生活比較輕鬆愉快。我找到兩個較多人推的 PDFium Wrapper：</p> <ul><li><a href="https://github.com/ArgusMagnus/PDFiumSharp">PDFiumSharp</a><br> 版本偏舊，適合 .NET Framework/Windows 平台，已六年未更新</li> <li><a href="https://github.com/Dtronix/PDFiumCore">PDFiumCore</a><br> 仍持續更新，可支援 .NET 6 及跨 Linux 平台</li></ul> <p>想用在 .NET 6+，選 PDFiumCore 就對了。</p> <p>PDFium 是個渲染引擎，核心功能是將 PDF 文件繪製成畫面，轉存圖檔正是它的拿手好戲，PDFiumCore 專案也有現成的<a href="https://github.com/Dtronix/PDFiumCore/blob/master/src/PDFiumCoreDemo/Program.cs">頁面轉存圖檔範例</a>，轉檔過程有點小複雜，需動用到矩陣運算：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638341833431896825.png"></p> <p>不過，我只是單純 PDF 頁面轉圖檔，程式不用怎麼改，拿香跟著拜就對了。程式範例如下：</p> <pre><code>using System.Drawing;
using System.Drawing.Imaging;
using PDFiumCore;
var scale = 1;
fpdfview.FPDF_InitLibrary();
var path = "blogger-story.pdf";
ExportImages(path, scale);
static void ExportImages(string pdfPath, float scale)
{
var imgFolder = Path.Combine("images", Path.GetFileNameWithoutExtension(pdfPath));
Directory.CreateDirectory(imgFolder);
// 載入PDF
var document = fpdfview.FPDF_LoadDocument(pdfPath, null);
// 取得總頁數
var pageCount = fpdfview.FPDF_GetPageCount(document);
for (var pageIndex = 0; pageIndex &lt; pageCount; pageIndex++)
{
// 逐頁載入
var page = fpdfview.FPDF_LoadPage(document, pageIndex);
// 取得尺寸，依比例縮放
var size = new FS_SIZEF_();
fpdfview.FPDF_GetPageSizeByIndexF(document, 0, size);
var width = (int)Math.Round(size.Width * scale);
var height = (int)Math.Round(size.Height * scale);
var bitmap = fpdfview.FPDFBitmapCreateEx(
width,
height,
(int)FPDFBitmapFormat.BGRA,
IntPtr.Zero,
0);
// 填入白色背景
fpdfview.FPDFBitmapFillRect(bitmap, 0, 0, width, height, (uint)Color.White.ToArgb());
// |          | a b 0 |
// | matrix = | c d 0 |
// |          | e f 1 |
using var matrix = new FS_MATRIX_();
using var clipping = new FS_RECTF_();
matrix.A = scale;
matrix.B = 0;
matrix.C = 0;
matrix.D = scale;
matrix.E = 0;
matrix.F = 0;
clipping.Left = 0;
clipping.Right = width;
clipping.Bottom = 0;
clipping.Top = height;
fpdfview.FPDF_RenderPageBitmapWithMatrix(bitmap, page, matrix, clipping, (int)RenderFlags.RenderAnnotations);
var bitmapImage = new Bitmap(
width,
height,
fpdfview.FPDFBitmapGetStride(bitmap),
PixelFormat.Format32bppArgb,
fpdfview.FPDFBitmapGetBuffer(bitmap));
bitmapImage.Save(
Path.Combine(imgFolder, $"{pageIndex:000}.png"),
ImageFormat.Png
);
}
}
</code></pre> <p>簡單拼裝一下。插電，開機，輕鬆秒殺~</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638341833439259530.png"></p> <p data-slug="pdfium-core" data-title-en="Export PDF to images with PDFiumCore" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/07/31/11/37/cold-2557515_960_720.jpg#960#720">Example of how to export PDF to images with PDFiumCore library.</p>]]>
    </description>
    <content:encoded><![CDATA[<p>突發奇想閃過念頭，想把 PDF 投影片逐頁轉成圖檔，索性當成 .NET 練習題伸展暖身，說做就做。</p> <p>.NET PDF 元件選擇不少，但好用的多是商用軟體，開源專案如 <a href="https://blog.darkthread.net/blog/regex-positive-lookahead/">iTextSharp</a>、<a href="https://fullstackladder.dev/blog/2023/02/05/introduction-quest-pdf/">QuestPDF</a> 新版也改成商業授權，而轉存圖檔不同於編譯修改文件，訴求重點不同，順便趁機認識不同的程式庫。</p> <p>這回我選擇的是 Google 的開源 PDF 引擎 - <a href="https://pdfium.googlesource.com/pdfium/">PDFium</a> (Google 與 Foxit 共同開源，採 <a href="https://zh.wikipedia.org/wiki/BSD%E8%AE%B8%E5%8F%AF%E8%AF%81">BSD 3-Clause 授權</a>，<a href="https://zhuanlan.zhihu.com/p/440886739">參考</a>)，其最大賣點為 Chrome 就是靠它顯示 PDF 文件! 沒人敢懷疑 Chrome/Edge 顯示 PDF 的效能或正確性(PDF 文件沒法用 Chrome 看，大家會覺得 PDF 文件有問題 XD)，用 PDFium 完全不用擔心跑不動大檔或排版錯亂，若不幸遇上，請儘速通知 PDF 文件作者自我檢討 😛</p> <p>PDFium 是原生程式庫，直接用 .NET 呼叫太累，一般會借助第三方 .NET Wrapper 程式庫，生活比較輕鬆愉快。我找到兩個較多人推的 PDFium Wrapper：</p> <ul><li><a href="https://github.com/ArgusMagnus/PDFiumSharp">PDFiumSharp</a><br> 版本偏舊，適合 .NET Framework/Windows 平台，已六年未更新</li> <li><a href="https://github.com/Dtronix/PDFiumCore">PDFiumCore</a><br> 仍持續更新，可支援 .NET 6 及跨 Linux 平台</li></ul> <p>想用在 .NET 6+，選 PDFiumCore 就對了。</p> <p>PDFium 是個渲染引擎，核心功能是將 PDF 文件繪製成畫面，轉存圖檔正是它的拿手好戲，PDFiumCore 專案也有現成的<a href="https://github.com/Dtronix/PDFiumCore/blob/master/src/PDFiumCoreDemo/Program.cs">頁面轉存圖檔範例</a>，轉檔過程有點小複雜，需動用到矩陣運算：</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig1_638341833431896825.png"></p> <p>不過，我只是單純 PDF 頁面轉圖檔，程式不用怎麼改，拿香跟著拜就對了。程式範例如下：</p> <pre><code>using System.Drawing;
using System.Drawing.Imaging;
using PDFiumCore;
var scale = 1;
fpdfview.FPDF_InitLibrary();
var path = "blogger-story.pdf";
ExportImages(path, scale);
static void ExportImages(string pdfPath, float scale)
{
var imgFolder = Path.Combine("images", Path.GetFileNameWithoutExtension(pdfPath));
Directory.CreateDirectory(imgFolder);
// 載入PDF
var document = fpdfview.FPDF_LoadDocument(pdfPath, null);
// 取得總頁數
var pageCount = fpdfview.FPDF_GetPageCount(document);
for (var pageIndex = 0; pageIndex &lt; pageCount; pageIndex++)
{
// 逐頁載入
var page = fpdfview.FPDF_LoadPage(document, pageIndex);
// 取得尺寸，依比例縮放
var size = new FS_SIZEF_();
fpdfview.FPDF_GetPageSizeByIndexF(document, 0, size);
var width = (int)Math.Round(size.Width * scale);
var height = (int)Math.Round(size.Height * scale);
var bitmap = fpdfview.FPDFBitmapCreateEx(
width,
height,
(int)FPDFBitmapFormat.BGRA,
IntPtr.Zero,
0);
// 填入白色背景
fpdfview.FPDFBitmapFillRect(bitmap, 0, 0, width, height, (uint)Color.White.ToArgb());
// |          | a b 0 |
// | matrix = | c d 0 |
// |          | e f 1 |
using var matrix = new FS_MATRIX_();
using var clipping = new FS_RECTF_();
matrix.A = scale;
matrix.B = 0;
matrix.C = 0;
matrix.D = scale;
matrix.E = 0;
matrix.F = 0;
clipping.Left = 0;
clipping.Right = width;
clipping.Bottom = 0;
clipping.Top = height;
fpdfview.FPDF_RenderPageBitmapWithMatrix(bitmap, page, matrix, clipping, (int)RenderFlags.RenderAnnotations);
var bitmapImage = new Bitmap(
width,
height,
fpdfview.FPDFBitmapGetStride(bitmap),
PixelFormat.Format32bppArgb,
fpdfview.FPDFBitmapGetBuffer(bitmap));
bitmapImage.Save(
Path.Combine(imgFolder, $"{pageIndex:000}.png"),
ImageFormat.Png
);
}
}
</code></pre> <p>簡單拼裝一下。插電，開機，輕鬆秒殺~</p> <p><img src="https://blog.darkthread.net/Posts/files/2023/Fig2_638341833439259530.png"></p> <p data-slug="pdfium-core" data-title-en="Export PDF to images with PDFiumCore" data-catg=".NET" data-fbimg="https://cdn.pixabay.com/photo/2017/07/31/11/37/cold-2557515_960_720.jpg#960#720">Example of how to export PDF to images with PDFiumCore library.</p>]]></content:encoded>
    <itunes:image href="https://blog.darkthread.net/Posts/files/2023/Fig1_638341833431896825.png"/>
          <media:content url="https://blog.darkthread.net/Posts/files/2023/Fig1_638341833431896825.png" medium="image"/>
    <pubDate>Sun, 29 Oct 2023 13:33:20 GMT</pubDate>
  </item></channel>
</rss>