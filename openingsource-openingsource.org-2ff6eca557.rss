<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[开源工场[RSS+]]]></title>
    <link>https://openingsource.org/feed/</link>
    <image>
      <url>https://openingsource.org/wp-content/uploads/2018/01/cropped-portrait_logo-32x32.png</url>
      <title>开源工场[RSS+]</title>
      <link>https://openingsource.org/feed/</link>
    </image>
    <language>zh-CN</language>
    <atom:link href="https://openingsource.org/feed/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[开源工场[RSS+]]]></copyright>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      人生苦短，我爱开源<br />
<br />
<a href="https://openingsource.org/feed/" target="_blank">https://openingsource.org/feed/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      人生苦短，我爱开源<br />
<br />
<a href="https://openingsource.org/feed/" target="_blank">https://openingsource.org/feed/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[开源工场[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2018/01/cropped-portrait_logo-32x32.png"/>
<item>
    <title><![CDATA[开源日报第1065期：神经网络可视化工具《netron》]]></title>
    <link>https://openingsource.org/11473/</link>
    <itunes:title><![CDATA[开源日报第1065期：神经网络可视化工具《netron》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月5日，开源日报第1065期：<br>
今日推荐开源项目：《netron》<br>
今日推荐英文原文：《React Query is the best thing that happened to React》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240105.jpg.pagespeed.ic.pzYGDgSpmC.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《netron》传送门：<a href="https://github.com/lutzroeder/netron" target="_blank">项目链接</a> </p>
<p>推荐理由：神经网络、深度学习和机器学习模型的可视化工具， PyTorch, TorchScript, TensorFlow, OpenVINO, RKNN, MediaPipe, ML.NET 和 scikit-learn</p>
<p>网站直达：netron.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@kvs.sankar23/react-query-is-the-best-thing-that-happened-to-react-abd92553e953" target="_blank">React Query is the best thing that happened to React</a></p>
<p>推荐理由：React Query是React中的一项强大数据获取库，被描述为在React开发中的一次重大变革。它简化了数据管理，优化了网络请求，并增强了缓存策略。文章认为从Redux转向React Query是值得的，特别是在处理API层数据方面，React Query已经能够很好地胜任</p>
<hr>
<h1>React Query is the best thing that happened to React</h1>
<h1>Background</h1>
<p>React Query is a powerful data-fetching library, has emerged as a game-changer in React development. It simplifies data management, optimizing network requests, and enhancing caching strategies.</p>
<p>Link to the package — <a href="https://www.npmjs.com/package/@tanstack/react-query" target="_blank">https://www.npmjs.com/package/@tanstack/react-query</a></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*lI9eJXsQJj80khkPOJTMXA.png" alt="img"></p>
<blockquote>
<p>Is it worth shifting from Redux to react-query??? Lets find out!!!</p>
</blockquote>
<h2>How does it make a difference?</h2>
<ol>
<li><strong>Effortless Data Fetching:</strong></li>
</ol>
<ul>
<li>Without React Query: Manually handling async calls with <code>fetch</code> or <code>axios</code>.</li>
<li>With React Query: Use <code>useQuery</code> for auto-fetching, reducing code complexity.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').then(response =&gt; response.json()).then(data =&gt; ...);
// With React Query
const { data } = useQuery('data', () =&gt; fetch('/api/data')
.then(response =&gt; response.json())
);</code></pre>
<p><strong>2. Automatic Caching:</strong></p>
<ul>
<li>Without React Query: Implement custom caching mechanisms.</li>
<li>With React Query: Caching is built-in and updates automatically.</li>
</ul>
<pre><code>// Without React Query
const cachedData = localStorage.getItem('cachedData');
// With React Query
const { data } = useQuery(['items'], fetchData, { enabled: isConnected });</code></pre>
<p><strong>3. Real-time Updates:</strong></p>
<ul>
<li>Without React Query: Complex setup using Web Sockets and manual state management.</li>
<li>With React Query: Seamless integration of real-time features.</li>
</ul>
<pre><code>// Without React Query
socket.on('dataUpdate', updatedData =&gt; ...);
// With React Query
const { data } = useQuery('data', fetchData);</code></pre>
<p><strong>4. Global State Management:</strong></p>
<ul>
<li>Without React Query: Combine Context or other state management libraries.</li>
<li>With React Query: Use hooks for data and global state.</li>
</ul>
<pre><code>// Without React Query
const { data, dispatch } = useContext(AppContext);
// With React Query
const { data, mutate } = useQuery(['items'], fetchData);</code></pre>
<p><strong>5. Network Request Optimization:</strong></p>
<ul>
<li>Without React Query: Manually manage network requests and debounce.</li>
<li>With React Query: Automatic query optimization</li>
</ul>
<pre><code>// Without React Query
const debouncedFetch = debounce(fetchData, 300);
// With React Query
const { data } = useQuery(['items'], fetchData, {
refetchOnWindowFocus: false
}
);</code></pre>
<p><strong>6. Server Pagination &amp; Infinite Loading:</strong></p>
<ul>
<li>Without React Query: Handle pagination manually with complex logic.</li>
<li>With React Query: Streamlined infinite loading and pagination.</li>
</ul>
<pre><code>// Without React Query
const loadMore = () =&gt; fetchMoreData(pageNumber);
// With React Query
const { data, fetchNextPage } = useInfiniteQuery(['items'], fetchPageData);</code></pre>
<p><strong>7. Error Handling &amp; Retrying:</strong></p>
<ul>
<li>Without React Query: Implement error handling and retry logic manually.</li>
<li>With React Query: Built-in error handling and retrying.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').catch(error =&gt; retry(fetchData, 3, error));
// With React Query
const { data } = useQuery(['items'], fetchData, { retry: 3 });</code></pre>
<p><strong>8. Developer-Friendly DevTools:</strong></p>
<ul>
<li>Without React Query: Rely on browser developer tools for insights.</li>
<li>With React Query: Debug with dedicated React Query DevTools.</li>
</ul>
<p>To see in action, here is an example of implementing displaying of data with pagination using react query.</p>
<h2>Example:</h2>
<p>Let's assume you have a set of data with pagination and filters. On clicking tabs you change filter, you have a dropdown to select nunber of items to display, you have previous and next buttons.</p>
<p><strong>Implementing this using react-query:</strong></p>
<pre><code>const Component = () =&gt; {
const [filterA, setFilterA] = React.useState&lt;boolean&gt;(false);
const [page, setPage] = React.useState&lt;number&gt;(1);
const [pageSize, setPageSize] = React.useState&lt;number&gt;(10);
const GetData= async (filters) =&gt; {
try {
const filterString = filters
? "?" + filters.map((filter) =&gt; filter.key + "=" + filter.value).join("&amp;")
: "";
const url = "/api/data/" + filterString;
const res = await axios.get(url);
return res.data;
} catch (err) {
throw err;
}
};
const {
data,
status,
error
} = useQuery(["data", filterA, page, pageSize], () =&gt;
GetData([
{
key: "filter_a",
value: filterA,
},
{
key: "page",
value: page,
},
{
key: "page_size",
value: pageSize,
},
])
);
return (
&lt;Tabs&gt;
&lt;TabList&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(false);
setPage(1);
}}
&gt;
Tab 1
&lt;/Tab&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(true);
setPage(1);
}}
&gt;
Tab 2
&lt;/Tab&gt;
&lt;Flex&gt;
&lt;Button
onClick={() =&gt; setPage(page - 1)}
disabled={page === 1}
&gt;
Previous
&lt;/Button&gt;
&lt;Text&gt;
{page}
&lt;/Text&gt;
&lt;Button
onClick={() =&gt; setPage(page + 1)}
disabled={data?.next === null}
&gt;
Next
&lt;/Button&gt;
&lt;Select
value={pageSize}
onChange={(e) =&gt; setPageSize(Number(e.target.value))}
&gt;
&lt;option value={10}&gt;10&lt;/option&gt;
&lt;option value={20}&gt;20&lt;/option&gt;
&lt;option value={50}&gt;50&lt;/option&gt;
&lt;/Select&gt;
&lt;/Flex&gt;
&lt;/TabList&gt;
&lt;TabPanels&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;/TabPanels&gt;
&lt;/Tabs&gt;
);
};
export default Component;</code></pre>
<h2>Note:</h2>
<blockquote>
<p>Like useEffect has dependencies array, u can treat the array in useQuery similarly, whenever any variable changes in the array, the API gets triggered again. The same info will be used to cache the data and remember the corresponding API for it.</p>
</blockquote>
<pre><code>useQuery(["data", a, b, c], () =&gt; GetData(a,b,c))</code></pre>
<p>So here whenever the a, b, c variable changes it gets triggered again.</p>
<h1>Conclusion:</h1>
<p>Yes, it’s definitely worth it to shift from Redux to react-query. You would still need Redux if u want to communicate complex local data across the components.</p>
<h2>Such use case when you would need Redux:</h2>
<p>When u have multiple components, and you are toggling display of components on different button clicks in different components which doesn’t have same parents.</p>
<p>But you no longer need to store the API layer data using Redux, since everything gets handled by react-query.</p>
<p>A round of applause, dear readers, for reaching the end — if you enjoyed this article as much as a developer loves a well-commented code, give it a clap to brighten my day! 👏</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月5日，开源日报第1065期：<br>
今日推荐开源项目：《netron》<br>
今日推荐英文原文：《React Query is the best thing that happened to React》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240105.jpg.pagespeed.ic.pzYGDgSpmC.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《netron》传送门：<a href="https://github.com/lutzroeder/netron" target="_blank">项目链接</a> </p>
<p>推荐理由：神经网络、深度学习和机器学习模型的可视化工具， PyTorch, TorchScript, TensorFlow, OpenVINO, RKNN, MediaPipe, ML.NET 和 scikit-learn</p>
<p>网站直达：netron.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@kvs.sankar23/react-query-is-the-best-thing-that-happened-to-react-abd92553e953" target="_blank">React Query is the best thing that happened to React</a></p>
<p>推荐理由：React Query是React中的一项强大数据获取库，被描述为在React开发中的一次重大变革。它简化了数据管理，优化了网络请求，并增强了缓存策略。文章认为从Redux转向React Query是值得的，特别是在处理API层数据方面，React Query已经能够很好地胜任</p>
<hr>
<h1>React Query is the best thing that happened to React</h1>
<h1>Background</h1>
<p>React Query is a powerful data-fetching library, has emerged as a game-changer in React development. It simplifies data management, optimizing network requests, and enhancing caching strategies.</p>
<p>Link to the package — <a href="https://www.npmjs.com/package/@tanstack/react-query" target="_blank">https://www.npmjs.com/package/@tanstack/react-query</a></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*lI9eJXsQJj80khkPOJTMXA.png" alt="img"></p>
<blockquote>
<p>Is it worth shifting from Redux to react-query??? Lets find out!!!</p>
</blockquote>
<h2>How does it make a difference?</h2>
<ol>
<li><strong>Effortless Data Fetching:</strong></li>
</ol>
<ul>
<li>Without React Query: Manually handling async calls with <code>fetch</code> or <code>axios</code>.</li>
<li>With React Query: Use <code>useQuery</code> for auto-fetching, reducing code complexity.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').then(response =&gt; response.json()).then(data =&gt; ...);
// With React Query
const { data } = useQuery('data', () =&gt; fetch('/api/data')
.then(response =&gt; response.json())
);</code></pre>
<p><strong>2. Automatic Caching:</strong></p>
<ul>
<li>Without React Query: Implement custom caching mechanisms.</li>
<li>With React Query: Caching is built-in and updates automatically.</li>
</ul>
<pre><code>// Without React Query
const cachedData = localStorage.getItem('cachedData');
// With React Query
const { data } = useQuery(['items'], fetchData, { enabled: isConnected });</code></pre>
<p><strong>3. Real-time Updates:</strong></p>
<ul>
<li>Without React Query: Complex setup using Web Sockets and manual state management.</li>
<li>With React Query: Seamless integration of real-time features.</li>
</ul>
<pre><code>// Without React Query
socket.on('dataUpdate', updatedData =&gt; ...);
// With React Query
const { data } = useQuery('data', fetchData);</code></pre>
<p><strong>4. Global State Management:</strong></p>
<ul>
<li>Without React Query: Combine Context or other state management libraries.</li>
<li>With React Query: Use hooks for data and global state.</li>
</ul>
<pre><code>// Without React Query
const { data, dispatch } = useContext(AppContext);
// With React Query
const { data, mutate } = useQuery(['items'], fetchData);</code></pre>
<p><strong>5. Network Request Optimization:</strong></p>
<ul>
<li>Without React Query: Manually manage network requests and debounce.</li>
<li>With React Query: Automatic query optimization</li>
</ul>
<pre><code>// Without React Query
const debouncedFetch = debounce(fetchData, 300);
// With React Query
const { data } = useQuery(['items'], fetchData, {
refetchOnWindowFocus: false
}
);</code></pre>
<p><strong>6. Server Pagination &amp; Infinite Loading:</strong></p>
<ul>
<li>Without React Query: Handle pagination manually with complex logic.</li>
<li>With React Query: Streamlined infinite loading and pagination.</li>
</ul>
<pre><code>// Without React Query
const loadMore = () =&gt; fetchMoreData(pageNumber);
// With React Query
const { data, fetchNextPage } = useInfiniteQuery(['items'], fetchPageData);</code></pre>
<p><strong>7. Error Handling &amp; Retrying:</strong></p>
<ul>
<li>Without React Query: Implement error handling and retry logic manually.</li>
<li>With React Query: Built-in error handling and retrying.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').catch(error =&gt; retry(fetchData, 3, error));
// With React Query
const { data } = useQuery(['items'], fetchData, { retry: 3 });</code></pre>
<p><strong>8. Developer-Friendly DevTools:</strong></p>
<ul>
<li>Without React Query: Rely on browser developer tools for insights.</li>
<li>With React Query: Debug with dedicated React Query DevTools.</li>
</ul>
<p>To see in action, here is an example of implementing displaying of data with pagination using react query.</p>
<h2>Example:</h2>
<p>Let's assume you have a set of data with pagination and filters. On clicking tabs you change filter, you have a dropdown to select nunber of items to display, you have previous and next buttons.</p>
<p><strong>Implementing this using react-query:</strong></p>
<pre><code>const Component = () =&gt; {
const [filterA, setFilterA] = React.useState&lt;boolean&gt;(false);
const [page, setPage] = React.useState&lt;number&gt;(1);
const [pageSize, setPageSize] = React.useState&lt;number&gt;(10);
const GetData= async (filters) =&gt; {
try {
const filterString = filters
? "?" + filters.map((filter) =&gt; filter.key + "=" + filter.value).join("&amp;")
: "";
const url = "/api/data/" + filterString;
const res = await axios.get(url);
return res.data;
} catch (err) {
throw err;
}
};
const {
data,
status,
error
} = useQuery(["data", filterA, page, pageSize], () =&gt;
GetData([
{
key: "filter_a",
value: filterA,
},
{
key: "page",
value: page,
},
{
key: "page_size",
value: pageSize,
},
])
);
return (
&lt;Tabs&gt;
&lt;TabList&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(false);
setPage(1);
}}
&gt;
Tab 1
&lt;/Tab&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(true);
setPage(1);
}}
&gt;
Tab 2
&lt;/Tab&gt;
&lt;Flex&gt;
&lt;Button
onClick={() =&gt; setPage(page - 1)}
disabled={page === 1}
&gt;
Previous
&lt;/Button&gt;
&lt;Text&gt;
{page}
&lt;/Text&gt;
&lt;Button
onClick={() =&gt; setPage(page + 1)}
disabled={data?.next === null}
&gt;
Next
&lt;/Button&gt;
&lt;Select
value={pageSize}
onChange={(e) =&gt; setPageSize(Number(e.target.value))}
&gt;
&lt;option value={10}&gt;10&lt;/option&gt;
&lt;option value={20}&gt;20&lt;/option&gt;
&lt;option value={50}&gt;50&lt;/option&gt;
&lt;/Select&gt;
&lt;/Flex&gt;
&lt;/TabList&gt;
&lt;TabPanels&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;/TabPanels&gt;
&lt;/Tabs&gt;
);
};
export default Component;</code></pre>
<h2>Note:</h2>
<blockquote>
<p>Like useEffect has dependencies array, u can treat the array in useQuery similarly, whenever any variable changes in the array, the API gets triggered again. The same info will be used to cache the data and remember the corresponding API for it.</p>
</blockquote>
<pre><code>useQuery(["data", a, b, c], () =&gt; GetData(a,b,c))</code></pre>
<p>So here whenever the a, b, c variable changes it gets triggered again.</p>
<h1>Conclusion:</h1>
<p>Yes, it’s definitely worth it to shift from Redux to react-query. You would still need Redux if u want to communicate complex local data across the components.</p>
<h2>Such use case when you would need Redux:</h2>
<p>When u have multiple components, and you are toggling display of components on different button clicks in different components which doesn’t have same parents.</p>
<p>But you no longer need to store the API layer data using Redux, since everything gets handled by react-query.</p>
<p>A round of applause, dear readers, for reaching the end — if you enjoyed this article as much as a developer loves a well-commented code, give it a clap to brighten my day! 👏</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月5日，开源日报第1065期：<br>
今日推荐开源项目：《netron》<br>
今日推荐英文原文：《React Query is the best thing that happened to React》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240105.jpg.pagespeed.ic.pzYGDgSpmC.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《netron》传送门：<a href="https://github.com/lutzroeder/netron" target="_blank">项目链接</a> </p>
<p>推荐理由：神经网络、深度学习和机器学习模型的可视化工具， PyTorch, TorchScript, TensorFlow, OpenVINO, RKNN, MediaPipe, ML.NET 和 scikit-learn</p>
<p>网站直达：netron.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@kvs.sankar23/react-query-is-the-best-thing-that-happened-to-react-abd92553e953" target="_blank">React Query is the best thing that happened to React</a></p>
<p>推荐理由：React Query是React中的一项强大数据获取库，被描述为在React开发中的一次重大变革。它简化了数据管理，优化了网络请求，并增强了缓存策略。文章认为从Redux转向React Query是值得的，特别是在处理API层数据方面，React Query已经能够很好地胜任</p>
<hr>
<h1>React Query is the best thing that happened to React</h1>
<h1>Background</h1>
<p>React Query is a powerful data-fetching library, has emerged as a game-changer in React development. It simplifies data management, optimizing network requests, and enhancing caching strategies.</p>
<p>Link to the package — <a href="https://www.npmjs.com/package/@tanstack/react-query" target="_blank">https://www.npmjs.com/package/@tanstack/react-query</a></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*lI9eJXsQJj80khkPOJTMXA.png" alt="img"></p>
<blockquote>
<p>Is it worth shifting from Redux to react-query??? Lets find out!!!</p>
</blockquote>
<h2>How does it make a difference?</h2>
<ol>
<li><strong>Effortless Data Fetching:</strong></li>
</ol>
<ul>
<li>Without React Query: Manually handling async calls with <code>fetch</code> or <code>axios</code>.</li>
<li>With React Query: Use <code>useQuery</code> for auto-fetching, reducing code complexity.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').then(response =&gt; response.json()).then(data =&gt; ...);
// With React Query
const { data } = useQuery('data', () =&gt; fetch('/api/data')
.then(response =&gt; response.json())
);</code></pre>
<p><strong>2. Automatic Caching:</strong></p>
<ul>
<li>Without React Query: Implement custom caching mechanisms.</li>
<li>With React Query: Caching is built-in and updates automatically.</li>
</ul>
<pre><code>// Without React Query
const cachedData = localStorage.getItem('cachedData');
// With React Query
const { data } = useQuery(['items'], fetchData, { enabled: isConnected });</code></pre>
<p><strong>3. Real-time Updates:</strong></p>
<ul>
<li>Without React Query: Complex setup using Web Sockets and manual state management.</li>
<li>With React Query: Seamless integration of real-time features.</li>
</ul>
<pre><code>// Without React Query
socket.on('dataUpdate', updatedData =&gt; ...);
// With React Query
const { data } = useQuery('data', fetchData);</code></pre>
<p><strong>4. Global State Management:</strong></p>
<ul>
<li>Without React Query: Combine Context or other state management libraries.</li>
<li>With React Query: Use hooks for data and global state.</li>
</ul>
<pre><code>// Without React Query
const { data, dispatch } = useContext(AppContext);
// With React Query
const { data, mutate } = useQuery(['items'], fetchData);</code></pre>
<p><strong>5. Network Request Optimization:</strong></p>
<ul>
<li>Without React Query: Manually manage network requests and debounce.</li>
<li>With React Query: Automatic query optimization</li>
</ul>
<pre><code>// Without React Query
const debouncedFetch = debounce(fetchData, 300);
// With React Query
const { data } = useQuery(['items'], fetchData, {
refetchOnWindowFocus: false
}
);</code></pre>
<p><strong>6. Server Pagination &amp; Infinite Loading:</strong></p>
<ul>
<li>Without React Query: Handle pagination manually with complex logic.</li>
<li>With React Query: Streamlined infinite loading and pagination.</li>
</ul>
<pre><code>// Without React Query
const loadMore = () =&gt; fetchMoreData(pageNumber);
// With React Query
const { data, fetchNextPage } = useInfiniteQuery(['items'], fetchPageData);</code></pre>
<p><strong>7. Error Handling &amp; Retrying:</strong></p>
<ul>
<li>Without React Query: Implement error handling and retry logic manually.</li>
<li>With React Query: Built-in error handling and retrying.</li>
</ul>
<pre><code>// Without React Query
fetch('/api/data').catch(error =&gt; retry(fetchData, 3, error));
// With React Query
const { data } = useQuery(['items'], fetchData, { retry: 3 });</code></pre>
<p><strong>8. Developer-Friendly DevTools:</strong></p>
<ul>
<li>Without React Query: Rely on browser developer tools for insights.</li>
<li>With React Query: Debug with dedicated React Query DevTools.</li>
</ul>
<p>To see in action, here is an example of implementing displaying of data with pagination using react query.</p>
<h2>Example:</h2>
<p>Let's assume you have a set of data with pagination and filters. On clicking tabs you change filter, you have a dropdown to select nunber of items to display, you have previous and next buttons.</p>
<p><strong>Implementing this using react-query:</strong></p>
<pre><code>const Component = () =&gt; {
const [filterA, setFilterA] = React.useState&lt;boolean&gt;(false);
const [page, setPage] = React.useState&lt;number&gt;(1);
const [pageSize, setPageSize] = React.useState&lt;number&gt;(10);
const GetData= async (filters) =&gt; {
try {
const filterString = filters
? "?" + filters.map((filter) =&gt; filter.key + "=" + filter.value).join("&amp;")
: "";
const url = "/api/data/" + filterString;
const res = await axios.get(url);
return res.data;
} catch (err) {
throw err;
}
};
const {
data,
status,
error
} = useQuery(["data", filterA, page, pageSize], () =&gt;
GetData([
{
key: "filter_a",
value: filterA,
},
{
key: "page",
value: page,
},
{
key: "page_size",
value: pageSize,
},
])
);
return (
&lt;Tabs&gt;
&lt;TabList&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(false);
setPage(1);
}}
&gt;
Tab 1
&lt;/Tab&gt;
&lt;Tab
onClick={() =&gt; {
setFilterA(true);
setPage(1);
}}
&gt;
Tab 2
&lt;/Tab&gt;
&lt;Flex&gt;
&lt;Button
onClick={() =&gt; setPage(page - 1)}
disabled={page === 1}
&gt;
Previous
&lt;/Button&gt;
&lt;Text&gt;
{page}
&lt;/Text&gt;
&lt;Button
onClick={() =&gt; setPage(page + 1)}
disabled={data?.next === null}
&gt;
Next
&lt;/Button&gt;
&lt;Select
value={pageSize}
onChange={(e) =&gt; setPageSize(Number(e.target.value))}
&gt;
&lt;option value={10}&gt;10&lt;/option&gt;
&lt;option value={20}&gt;20&lt;/option&gt;
&lt;option value={50}&gt;50&lt;/option&gt;
&lt;/Select&gt;
&lt;/Flex&gt;
&lt;/TabList&gt;
&lt;TabPanels&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;TabPanel&gt;
{status === "loading" ? (
&lt;ComponentLoader /&gt;
) : (
&lt;DataTable data={data.results} /&gt;
)}
&lt;/TabPanel&gt;
&lt;/TabPanels&gt;
&lt;/Tabs&gt;
);
};
export default Component;</code></pre>
<h2>Note:</h2>
<blockquote>
<p>Like useEffect has dependencies array, u can treat the array in useQuery similarly, whenever any variable changes in the array, the API gets triggered again. The same info will be used to cache the data and remember the corresponding API for it.</p>
</blockquote>
<pre><code>useQuery(["data", a, b, c], () =&gt; GetData(a,b,c))</code></pre>
<p>So here whenever the a, b, c variable changes it gets triggered again.</p>
<h1>Conclusion:</h1>
<p>Yes, it’s definitely worth it to shift from Redux to react-query. You would still need Redux if u want to communicate complex local data across the components.</p>
<h2>Such use case when you would need Redux:</h2>
<p>When u have multiple components, and you are toggling display of components on different button clicks in different components which doesn’t have same parents.</p>
<p>But you no longer need to store the API layer data using Redux, since everything gets handled by react-query.</p>
<p>A round of applause, dear readers, for reaching the end — if you enjoyed this article as much as a developer loves a well-commented code, give it a clap to brighten my day! 👏</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240105.jpg.pagespeed.ic.pzYGDgSpmC.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240105.jpg.pagespeed.ic.pzYGDgSpmC.jpg" medium="image"/>
    <pubDate>Fri, 05 Jan 2024 13:25:36 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1064期：Java性能极限之一百亿行挑战《1brc》]]></title>
    <link>https://openingsource.org/11470/</link>
    <itunes:title><![CDATA[开源日报第1064期：Java性能极限之一百亿行挑战《1brc》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月4日，开源日报第1064期：<br>
今日推荐开源项目：《1brc》<br>
今日推荐英文原文：《Navigating chaos: Designing for a three-body problem》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240104.jpg.pagespeed.ic.cHJD-_RSyV.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《1brc》传送门：<a href="https://github.com/gunnarmorling/1brc" target="_blank">项目链接</a> </p>
<p>推荐理由：一百亿行挑战（1BRC）是对现代Java性能极限的有趣探索，就是从文本文件中聚合一百亿行数据。利用所有（虚拟）线程和SIMD技术，优化垃圾回收，或者使用其他技巧，创建解决这一任务的最快实现</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/user-experience-design-1/navigating-chaos-designing-for-a-three-body-problem-eedb5abec60e" target="_blank">Navigating chaos: Designing for a three-body problem</a></p>
<p>推荐理由：在AIGC时代UX设计师必须在保证用户友好性的同时应对复杂性，本文以神秘的三体问题类比，探讨了UX设计师在生成响应混沌中的引导作用</p>
<hr>
<p>Navigating chaos: Designing for a three-body problem</p>
<h2>User Experiences Amidst Unpredictable Interactions</h2>
<p>TL;DR: In the era of generative AI, UX designers must navigate complexity while ensuring user-friendliness. This article explores their role as guides through the chaos of generative responses, drawing inspiration from the enigmatic three-body problem analogy, where interactions can be as complex and unpredictable as celestial bodies.</p>
<p>The digital interface landscape is undergoing a significant transformation, with generative AI interfaces leading the charge. These interfaces, driven by the capabilities of artificial intelligence and machine learning, introduce a complexity that goes <a href="https://medium.com/@danlittlewood/exploring-alternative-ux-patterns-for-genai-interfaces-a5c0d3ad4e01" target="_blank">beyond traditional UX paradigms</a>. In the world of generative AI, the user experience becomes a dynamic and ever-evolving entity, inviting us to explore uncharted territory where user interactions can be as unpredictable as they are innovative.</p>
<h1>The Traditional UX Paradigm: Object, Trigger, Response</h1>
<p>In the realm of User Experience (UX) design, digital interactions often follow a well-known path, often referred to as “<a href="https://designsprintkit.withgoogle.com/methodology/phase2-define/golden-path" target="_blank">the golden path</a>.” This journey symbolizes the ideal user flow, a predictable sequence where users encounter ‘objects,’ initiate ‘triggers,’ and receive ‘responses’ that align with their expectations. Think of it as a choreographed dance where user intent seamlessly meets system behavior. However, in the world of generative AI interfaces, while this path remains reliable, it’s no longer the sole destination as the landscape expands into unpredictable realms.</p>
<p>Now, let’s take a deeper dive into the [core elements](<a href="https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The" target="_blank">https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The</a> Design of Everyday Things by Don Norman.pdf)* of UX design — ‘Object,’ ‘Trigger,’ and ‘Response’ — to understand how they shape the traditional user experience and adapt in the context of generative AI interfaces.</p>
<p><strong>Object</strong>: The Heart of Interaction</p>
<p>The ‘object’ stands as a fundamental element with which users interact. These elements can take various forms — buttons, text fields, and more — serving as the foundational building blocks of the interface. Users rely on visual cues and affordances to understand how to engage with them. In the physical world, think of a door handle as a real-world ‘object.’ It’s a tangible, everyday element that people interact with regularly.</p>
<p><strong>Trigger</strong>: Initiating Action</p>
<p>The ‘trigger’ serves as the catalyst, initiating user actions that prompt specific responses from the interface. In traditional user interfaces, this concept is straightforward, with users triggering predefined actions by interacting with static elements. Imagine the ‘trigger’ as the act of gripping and turning a door handle. This user action sets off a specific response, either opening or keeping the door closed, depending on its state.</p>
<p><strong>Response</strong>: Meeting Expectations</p>
<p>The ‘response’ holds the essence of user interaction. It’s where actions culminate in expected outcomes, creating a sense of familiarity and predictability. This predictability ensures a smooth and intuitive user journey along well-trodden paths. For example, the response to turning a door handle relies on the door’s state, whether it’s locked or unlocked. If the door is unlocked, turning the handle typically opens it, granting access to the other side.</p>
<h1>A Dynamic Shift: Chaos in Generative AI Interfaces</h1>
<p>However, as we delve into the world of generative AI interfaces, a more nuanced understanding of these elements comes into focus. The ‘object,’ often embodied as a dynamic text field or an evolving prompt chip, retains its position as a comprehensible entity, even in this evolving landscape. It adapts to user input and AI-generated content, providing an interactive canvas for users.</p>
<p>Similarly, the ‘trigger’ remains the initiator of user requests, propelling interactions forward and serving as a bridge between intent and execution. Users continue to initiate actions through triggers, preserving the foundational connection between human intention and system functionality.</p>
<blockquote>
<p><strong>Where generative AI interfaces distinguish themselves lies in the ‘response.’ Here, the landscape undergoes a dramatic shift.</strong></p>
</blockquote>
<p>The ‘response’ transcends predictable patterns, venturing into uncharted territory where it delights, surprises, and, at times, frustrates. It becomes a realm of creative exploration, where AI-generated content can be as unpredictable and <a href="https://www.newyorker.com/tech/annals-of-technology/chatgpt-is-a-blurry-jpeg-of-the-web" target="_blank">unreliable</a> as it is innovative, leading to experiences that ignite curiosity and wonder in users.</p>
<p>This shift challenges us to design interfaces that artfully balance the known and the unknown, delivering delightful user experiences amid the uncertain potential of the generated ‘response.’</p>
<h1>The Three-Body Problem</h1>
<p><a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">The three-body problem</a>, a renowned issue in physics and celestial mechanics, provides a fitting analogy. It involves the study of the motion of three celestial bodies under the influence of mutual gravitational attraction. Unlike the two-body problem, where motion can be predicted, the three-body problem is infamously intricate. The interactions among all three bodies result in chaotic and unpredictable trajectories, pushing the boundaries of our ability to precisely calculate their positions and movements over time.</p>
<p>The Three-body Problem <a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">Wikipedia</a></p>
<p>This analogy resonates with the world of generative AI, where even minor variations in user input or AI algorithms can lead to significantly different responses, akin to the unpredictable celestial orbits.</p>
<p>For instance, picture a newcomer to generative AI interfaces trying out a writing assistant. They type in what seems like a simple request: ‘Help me write a playful email.’ In conventional systems, one might expect a structured response — perhaps an email template with guidance on striking the right balance of whimsy in the message, akin to measuring the ‘dad-ness’ factor to perfection.</p>
<p>However, in this dynamic generative AI interface, the ‘response’ could transcend predictable patterns, generating creative surprises. In this scenario, the user receives a response that might include a Shakespearean sonnet or a recipe for apple pie, far from the expected playful email template. Frustrated and baffled by the unpredictability, the novice user may become disenchanted with the technology, dismissing it as unreliable and impractical for their needs.</p>
<p>This example underscores the delicate balance we must strike when crafting generative AI interfaces. While unpredictability can spark creativity, it must be harnessed judiciously to avoid overwhelming users and causing them to abandon the technology due to frustration.</p>
<h1>Designers as Navigators of Chaos</h1>
<p>In this enigmatic realm, UX designers emerge as navigators of chaos. They grapple with the unpredictability that arises from the convergence of artificial intelligence and human experience. While we can discern the first two elements — the ‘object’ and the ‘trigger’ — with relative certainty, the third element — the ‘response’ — remains as elusive as celestial trajectories in the night sky.</p>
<p>Designers bridge the gap between human intention and AI’s creative potential, crafting interfaces that spark curiosity, inspire exploration, and delight users amid uncertainty. They embrace both the known and the unknown, guiding users through a digital cosmos of limitless possibilities.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月4日，开源日报第1064期：<br>
今日推荐开源项目：《1brc》<br>
今日推荐英文原文：《Navigating chaos: Designing for a three-body problem》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240104.jpg.pagespeed.ic.cHJD-_RSyV.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《1brc》传送门：<a href="https://github.com/gunnarmorling/1brc" target="_blank">项目链接</a> </p>
<p>推荐理由：一百亿行挑战（1BRC）是对现代Java性能极限的有趣探索，就是从文本文件中聚合一百亿行数据。利用所有（虚拟）线程和SIMD技术，优化垃圾回收，或者使用其他技巧，创建解决这一任务的最快实现</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/user-experience-design-1/navigating-chaos-designing-for-a-three-body-problem-eedb5abec60e" target="_blank">Navigating chaos: Designing for a three-body problem</a></p>
<p>推荐理由：在AIGC时代UX设计师必须在保证用户友好性的同时应对复杂性，本文以神秘的三体问题类比，探讨了UX设计师在生成响应混沌中的引导作用</p>
<hr>
<p>Navigating chaos: Designing for a three-body problem</p>
<h2>User Experiences Amidst Unpredictable Interactions</h2>
<p>TL;DR: In the era of generative AI, UX designers must navigate complexity while ensuring user-friendliness. This article explores their role as guides through the chaos of generative responses, drawing inspiration from the enigmatic three-body problem analogy, where interactions can be as complex and unpredictable as celestial bodies.</p>
<p>The digital interface landscape is undergoing a significant transformation, with generative AI interfaces leading the charge. These interfaces, driven by the capabilities of artificial intelligence and machine learning, introduce a complexity that goes <a href="https://medium.com/@danlittlewood/exploring-alternative-ux-patterns-for-genai-interfaces-a5c0d3ad4e01" target="_blank">beyond traditional UX paradigms</a>. In the world of generative AI, the user experience becomes a dynamic and ever-evolving entity, inviting us to explore uncharted territory where user interactions can be as unpredictable as they are innovative.</p>
<h1>The Traditional UX Paradigm: Object, Trigger, Response</h1>
<p>In the realm of User Experience (UX) design, digital interactions often follow a well-known path, often referred to as “<a href="https://designsprintkit.withgoogle.com/methodology/phase2-define/golden-path" target="_blank">the golden path</a>.” This journey symbolizes the ideal user flow, a predictable sequence where users encounter ‘objects,’ initiate ‘triggers,’ and receive ‘responses’ that align with their expectations. Think of it as a choreographed dance where user intent seamlessly meets system behavior. However, in the world of generative AI interfaces, while this path remains reliable, it’s no longer the sole destination as the landscape expands into unpredictable realms.</p>
<p>Now, let’s take a deeper dive into the [core elements](<a href="https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The" target="_blank">https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The</a> Design of Everyday Things by Don Norman.pdf)* of UX design — ‘Object,’ ‘Trigger,’ and ‘Response’ — to understand how they shape the traditional user experience and adapt in the context of generative AI interfaces.</p>
<p><strong>Object</strong>: The Heart of Interaction</p>
<p>The ‘object’ stands as a fundamental element with which users interact. These elements can take various forms — buttons, text fields, and more — serving as the foundational building blocks of the interface. Users rely on visual cues and affordances to understand how to engage with them. In the physical world, think of a door handle as a real-world ‘object.’ It’s a tangible, everyday element that people interact with regularly.</p>
<p><strong>Trigger</strong>: Initiating Action</p>
<p>The ‘trigger’ serves as the catalyst, initiating user actions that prompt specific responses from the interface. In traditional user interfaces, this concept is straightforward, with users triggering predefined actions by interacting with static elements. Imagine the ‘trigger’ as the act of gripping and turning a door handle. This user action sets off a specific response, either opening or keeping the door closed, depending on its state.</p>
<p><strong>Response</strong>: Meeting Expectations</p>
<p>The ‘response’ holds the essence of user interaction. It’s where actions culminate in expected outcomes, creating a sense of familiarity and predictability. This predictability ensures a smooth and intuitive user journey along well-trodden paths. For example, the response to turning a door handle relies on the door’s state, whether it’s locked or unlocked. If the door is unlocked, turning the handle typically opens it, granting access to the other side.</p>
<h1>A Dynamic Shift: Chaos in Generative AI Interfaces</h1>
<p>However, as we delve into the world of generative AI interfaces, a more nuanced understanding of these elements comes into focus. The ‘object,’ often embodied as a dynamic text field or an evolving prompt chip, retains its position as a comprehensible entity, even in this evolving landscape. It adapts to user input and AI-generated content, providing an interactive canvas for users.</p>
<p>Similarly, the ‘trigger’ remains the initiator of user requests, propelling interactions forward and serving as a bridge between intent and execution. Users continue to initiate actions through triggers, preserving the foundational connection between human intention and system functionality.</p>
<blockquote>
<p><strong>Where generative AI interfaces distinguish themselves lies in the ‘response.’ Here, the landscape undergoes a dramatic shift.</strong></p>
</blockquote>
<p>The ‘response’ transcends predictable patterns, venturing into uncharted territory where it delights, surprises, and, at times, frustrates. It becomes a realm of creative exploration, where AI-generated content can be as unpredictable and <a href="https://www.newyorker.com/tech/annals-of-technology/chatgpt-is-a-blurry-jpeg-of-the-web" target="_blank">unreliable</a> as it is innovative, leading to experiences that ignite curiosity and wonder in users.</p>
<p>This shift challenges us to design interfaces that artfully balance the known and the unknown, delivering delightful user experiences amid the uncertain potential of the generated ‘response.’</p>
<h1>The Three-Body Problem</h1>
<p><a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">The three-body problem</a>, a renowned issue in physics and celestial mechanics, provides a fitting analogy. It involves the study of the motion of three celestial bodies under the influence of mutual gravitational attraction. Unlike the two-body problem, where motion can be predicted, the three-body problem is infamously intricate. The interactions among all three bodies result in chaotic and unpredictable trajectories, pushing the boundaries of our ability to precisely calculate their positions and movements over time.</p>
<p>The Three-body Problem <a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">Wikipedia</a></p>
<p>This analogy resonates with the world of generative AI, where even minor variations in user input or AI algorithms can lead to significantly different responses, akin to the unpredictable celestial orbits.</p>
<p>For instance, picture a newcomer to generative AI interfaces trying out a writing assistant. They type in what seems like a simple request: ‘Help me write a playful email.’ In conventional systems, one might expect a structured response — perhaps an email template with guidance on striking the right balance of whimsy in the message, akin to measuring the ‘dad-ness’ factor to perfection.</p>
<p>However, in this dynamic generative AI interface, the ‘response’ could transcend predictable patterns, generating creative surprises. In this scenario, the user receives a response that might include a Shakespearean sonnet or a recipe for apple pie, far from the expected playful email template. Frustrated and baffled by the unpredictability, the novice user may become disenchanted with the technology, dismissing it as unreliable and impractical for their needs.</p>
<p>This example underscores the delicate balance we must strike when crafting generative AI interfaces. While unpredictability can spark creativity, it must be harnessed judiciously to avoid overwhelming users and causing them to abandon the technology due to frustration.</p>
<h1>Designers as Navigators of Chaos</h1>
<p>In this enigmatic realm, UX designers emerge as navigators of chaos. They grapple with the unpredictability that arises from the convergence of artificial intelligence and human experience. While we can discern the first two elements — the ‘object’ and the ‘trigger’ — with relative certainty, the third element — the ‘response’ — remains as elusive as celestial trajectories in the night sky.</p>
<p>Designers bridge the gap between human intention and AI’s creative potential, crafting interfaces that spark curiosity, inspire exploration, and delight users amid uncertainty. They embrace both the known and the unknown, guiding users through a digital cosmos of limitless possibilities.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月4日，开源日报第1064期：<br>
今日推荐开源项目：《1brc》<br>
今日推荐英文原文：《Navigating chaos: Designing for a three-body problem》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240104.jpg.pagespeed.ic.cHJD-_RSyV.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《1brc》传送门：<a href="https://github.com/gunnarmorling/1brc" target="_blank">项目链接</a> </p>
<p>推荐理由：一百亿行挑战（1BRC）是对现代Java性能极限的有趣探索，就是从文本文件中聚合一百亿行数据。利用所有（虚拟）线程和SIMD技术，优化垃圾回收，或者使用其他技巧，创建解决这一任务的最快实现</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/user-experience-design-1/navigating-chaos-designing-for-a-three-body-problem-eedb5abec60e" target="_blank">Navigating chaos: Designing for a three-body problem</a></p>
<p>推荐理由：在AIGC时代UX设计师必须在保证用户友好性的同时应对复杂性，本文以神秘的三体问题类比，探讨了UX设计师在生成响应混沌中的引导作用</p>
<hr>
<p>Navigating chaos: Designing for a three-body problem</p>
<h2>User Experiences Amidst Unpredictable Interactions</h2>
<p>TL;DR: In the era of generative AI, UX designers must navigate complexity while ensuring user-friendliness. This article explores their role as guides through the chaos of generative responses, drawing inspiration from the enigmatic three-body problem analogy, where interactions can be as complex and unpredictable as celestial bodies.</p>
<p>The digital interface landscape is undergoing a significant transformation, with generative AI interfaces leading the charge. These interfaces, driven by the capabilities of artificial intelligence and machine learning, introduce a complexity that goes <a href="https://medium.com/@danlittlewood/exploring-alternative-ux-patterns-for-genai-interfaces-a5c0d3ad4e01" target="_blank">beyond traditional UX paradigms</a>. In the world of generative AI, the user experience becomes a dynamic and ever-evolving entity, inviting us to explore uncharted territory where user interactions can be as unpredictable as they are innovative.</p>
<h1>The Traditional UX Paradigm: Object, Trigger, Response</h1>
<p>In the realm of User Experience (UX) design, digital interactions often follow a well-known path, often referred to as “<a href="https://designsprintkit.withgoogle.com/methodology/phase2-define/golden-path" target="_blank">the golden path</a>.” This journey symbolizes the ideal user flow, a predictable sequence where users encounter ‘objects,’ initiate ‘triggers,’ and receive ‘responses’ that align with their expectations. Think of it as a choreographed dance where user intent seamlessly meets system behavior. However, in the world of generative AI interfaces, while this path remains reliable, it’s no longer the sole destination as the landscape expands into unpredictable realms.</p>
<p>Now, let’s take a deeper dive into the [core elements](<a href="https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The" target="_blank">https://ia902800.us.archive.org/3/items/thedesignofeverydaythingsbydonnorman/The</a> Design of Everyday Things by Don Norman.pdf)* of UX design — ‘Object,’ ‘Trigger,’ and ‘Response’ — to understand how they shape the traditional user experience and adapt in the context of generative AI interfaces.</p>
<p><strong>Object</strong>: The Heart of Interaction</p>
<p>The ‘object’ stands as a fundamental element with which users interact. These elements can take various forms — buttons, text fields, and more — serving as the foundational building blocks of the interface. Users rely on visual cues and affordances to understand how to engage with them. In the physical world, think of a door handle as a real-world ‘object.’ It’s a tangible, everyday element that people interact with regularly.</p>
<p><strong>Trigger</strong>: Initiating Action</p>
<p>The ‘trigger’ serves as the catalyst, initiating user actions that prompt specific responses from the interface. In traditional user interfaces, this concept is straightforward, with users triggering predefined actions by interacting with static elements. Imagine the ‘trigger’ as the act of gripping and turning a door handle. This user action sets off a specific response, either opening or keeping the door closed, depending on its state.</p>
<p><strong>Response</strong>: Meeting Expectations</p>
<p>The ‘response’ holds the essence of user interaction. It’s where actions culminate in expected outcomes, creating a sense of familiarity and predictability. This predictability ensures a smooth and intuitive user journey along well-trodden paths. For example, the response to turning a door handle relies on the door’s state, whether it’s locked or unlocked. If the door is unlocked, turning the handle typically opens it, granting access to the other side.</p>
<h1>A Dynamic Shift: Chaos in Generative AI Interfaces</h1>
<p>However, as we delve into the world of generative AI interfaces, a more nuanced understanding of these elements comes into focus. The ‘object,’ often embodied as a dynamic text field or an evolving prompt chip, retains its position as a comprehensible entity, even in this evolving landscape. It adapts to user input and AI-generated content, providing an interactive canvas for users.</p>
<p>Similarly, the ‘trigger’ remains the initiator of user requests, propelling interactions forward and serving as a bridge between intent and execution. Users continue to initiate actions through triggers, preserving the foundational connection between human intention and system functionality.</p>
<blockquote>
<p><strong>Where generative AI interfaces distinguish themselves lies in the ‘response.’ Here, the landscape undergoes a dramatic shift.</strong></p>
</blockquote>
<p>The ‘response’ transcends predictable patterns, venturing into uncharted territory where it delights, surprises, and, at times, frustrates. It becomes a realm of creative exploration, where AI-generated content can be as unpredictable and <a href="https://www.newyorker.com/tech/annals-of-technology/chatgpt-is-a-blurry-jpeg-of-the-web" target="_blank">unreliable</a> as it is innovative, leading to experiences that ignite curiosity and wonder in users.</p>
<p>This shift challenges us to design interfaces that artfully balance the known and the unknown, delivering delightful user experiences amid the uncertain potential of the generated ‘response.’</p>
<h1>The Three-Body Problem</h1>
<p><a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">The three-body problem</a>, a renowned issue in physics and celestial mechanics, provides a fitting analogy. It involves the study of the motion of three celestial bodies under the influence of mutual gravitational attraction. Unlike the two-body problem, where motion can be predicted, the three-body problem is infamously intricate. The interactions among all three bodies result in chaotic and unpredictable trajectories, pushing the boundaries of our ability to precisely calculate their positions and movements over time.</p>
<p>The Three-body Problem <a href="https://en.wikipedia.org/wiki/Three-body_problem" target="_blank">Wikipedia</a></p>
<p>This analogy resonates with the world of generative AI, where even minor variations in user input or AI algorithms can lead to significantly different responses, akin to the unpredictable celestial orbits.</p>
<p>For instance, picture a newcomer to generative AI interfaces trying out a writing assistant. They type in what seems like a simple request: ‘Help me write a playful email.’ In conventional systems, one might expect a structured response — perhaps an email template with guidance on striking the right balance of whimsy in the message, akin to measuring the ‘dad-ness’ factor to perfection.</p>
<p>However, in this dynamic generative AI interface, the ‘response’ could transcend predictable patterns, generating creative surprises. In this scenario, the user receives a response that might include a Shakespearean sonnet or a recipe for apple pie, far from the expected playful email template. Frustrated and baffled by the unpredictability, the novice user may become disenchanted with the technology, dismissing it as unreliable and impractical for their needs.</p>
<p>This example underscores the delicate balance we must strike when crafting generative AI interfaces. While unpredictability can spark creativity, it must be harnessed judiciously to avoid overwhelming users and causing them to abandon the technology due to frustration.</p>
<h1>Designers as Navigators of Chaos</h1>
<p>In this enigmatic realm, UX designers emerge as navigators of chaos. They grapple with the unpredictability that arises from the convergence of artificial intelligence and human experience. While we can discern the first two elements — the ‘object’ and the ‘trigger’ — with relative certainty, the third element — the ‘response’ — remains as elusive as celestial trajectories in the night sky.</p>
<p>Designers bridge the gap between human intention and AI’s creative potential, crafting interfaces that spark curiosity, inspire exploration, and delight users amid uncertainty. They embrace both the known and the unknown, guiding users through a digital cosmos of limitless possibilities.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240104.jpg.pagespeed.ic.cHJD-_RSyV.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240104.jpg.pagespeed.ic.cHJD-_RSyV.jpg" medium="image"/>
    <pubDate>Thu, 04 Jan 2024 13:17:55 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1063期：非官方实现AnimateAnyone：《Open-AnimateAnyone》]]></title>
    <link>https://openingsource.org/11466/</link>
    <itunes:title><![CDATA[开源日报第1063期：非官方实现AnimateAnyone：《Open-AnimateAnyone》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月3日，开源日报第1063期：<br>
今日推荐开源项目：《Open-AnimateAnyone》<br>
今日推荐英文原文：《Say Goodbye to Debouncing: Use “useDeferredValue” Hook》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240103.jpg.pagespeed.ic.Vr3IbMyOUi.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Open-AnimateAnyone》传送门：<a href="https://github.com/guoqincode/Open-AnimateAnyone" target="_blank">项目链接</a> </p>
<p>推荐理由：动画制作的非官方实现，这个仓库是对Animate Anyone的非官方实现，基于magic-animate和AnimateDiff构建，第一阶段训练基本测试通过，目前正在训练和测试第二阶段，效果图如下</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xiShot_2024-01-05_20.00.40.jpg.pagespeed.ic.fSSQItkU1f.jpg"></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/say-goodbye-to-debouncing-use-usedeferredvalue-hook-56e1c28a3e49" target="_blank">Say Goodbye to Debouncing: Use “useDeferredValue” Hook</a></p>
<p>推荐理由：本文介绍了React 18引入的新工具——useDeferredValue钩子，用于优化应用程序性能.该钩子在处理异步数据获取（如网络请求或API数据加载）时很有用，其主要目的是推迟对应用程序中不太关键部分的更新，同时立即渲染最重要的部分</p>
<hr>
<h1>Say Goodbye to Debouncing: Use “useDeferredValue” Hook</h1>
<p>React is a popular JavaScript library for building user interfaces, and it continually evolves to provide developers with tools to optimize performance.</p>
<p>One such tool introduced in React 18 is the <code>useDeferredValue</code> hook, which is designed to improve the performance of your applications by prioritizing rendering updates.</p>
<h1>What is the useDeferredValue Hook?</h1>
<p>The <code>useDeferredValue</code> hook is a relatively new addition to React's arsenal of performance optimization tools.</p>
<p>It's particularly useful when dealing with asynchronous data fetching, such as network requests or data loading from APIs.</p>
<p>The primary purpose of <code>useDeferredValue</code> is to defer updates to less critical parts of your application while immediately rendering the most important parts.</p>
<p>This can greatly improve the perceived performance of your application by avoiding delays in rendering user interface components.</p>
<h1>Basic Usage of useDeferredValue</h1>
<p>The basic usage of the <code>useDeferredValue</code> hook involves wrapping a state value and creating a deferred version of it. Here's a simple example:</p>
<pre><code>import { useState, useDeferredValue } from 'react';
function MyComponent() {
const [data, setData] = useState([]);
const deferredData = useDeferredValue(data);
// ...
}</code></pre>
<p>In this example, we have a state variable <code>data</code>, which might be populated with data from an asynchronous operation.</p>
<p>By using <code>useDeferredValue</code>, we create <code>deferredData</code>, a version of <code>data</code> that React will prioritize rendering separately.</p>
<p>This separation ensures that the critical parts of your UI update promptly, while non-critical updates, like rendering lists, can be deferred to reduce the impact on performance.</p>
<h1>Usage</h1>
<p>When you first render the component, the deferred value will be the same as the value you gave it.</p>
<p>When you update the component, the deferred value will lag behind the latest value. This means that React will first re-render the component with the old deferred value, and then try to re-render it with the new deferred value in the background.</p>
<p>Here is an example of when this is useful:</p>
<p>Imagine you have a search bar that fetches search results as you type. You start typing “a”, and React renders the search bar with the loading fallback. The search results for “a” eventually come back, and React re-renders the search bar with the results.</p>
<pre><code>// app.js
import { Suspense, useState } from 'react';
import SearchResults from './SearchResults.js';
export default function App() {
const [query, setQuery] = useState('');
return (
&lt;&gt;
&lt;label&gt;
Search songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={query} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}
// searchResult.js
import { fetchData } from './data.js';
export default function SearchResults({ query }) {
if (query === '') {
return null;
}
const songs = use(fetchData(`/search?q=${query}`));
if (songs.length === 0) {
return &lt;p&gt;No matches for &lt;i&gt;"{query}"&lt;/i&gt;&lt;/p&gt;;
}
return (
&lt;ul&gt;
{songs.map(song =&gt; (
&lt;li key={song.id}&gt;
{song.title} ({song.year})
&lt;/li&gt;
))}
&lt;/ul&gt;
);
}
function use(promise) {
if (promise.status === 'fulfilled') {
return promise.value;
} else if (promise.status === 'rejected') {
throw promise.reason;
} else if (promise.status === 'pending') {
throw promise;
} else {
promise.status = 'pending';
promise.then(
result =&gt; {
promise.status = 'fulfilled';
promise.value = result;
},
reason =&gt; {
promise.status = 'rejected';
promise.reason = reason;
},
);
throw promise;
}
}
// data.js
let cache = new Map();
export function fetchData(url) {
if (!cache.has(url)) {
cache.set(url, getData(url));
}
return cache.get(url);
}
async function getData(url) {
if (url.startsWith('/search?q=')) {
return await getSearchResults(url.slice('/search?q='.length));
} else {
throw Error('Not implemented');
}
}
async function getSearchResults(query) {
// Add a fake delay to make waiting noticeable.
await new Promise(resolve =&gt; {
setTimeout(resolve, 500);
});
const allSongs = [{
id: 1,
title: "Bohemian Rhapsody",
year: 1975
},
{
id: 2,
title: "Imagine",
year: 1971
},
{
id: 3,
title: "Hotel California",
year: 1976
},
{
id: 4,
title: "Stairway to Heaven",
year: 1971
},
{
id: 5,
title: "Let It Be",
year: 1970
},
{
id: 6,
title: "Abbey",
year: 1976
},
{
id: 7,
title: "A Hard Day's Night",
year: 2012
}];
const lowerQuery = query.trim().toLowerCase();
return allSongs.filter(album =&gt; {
const lowerTitle = album.title.toLowerCase();
return (
lowerTitle.startsWith(lowerQuery) ||
lowerTitle.indexOf(' ' + lowerQuery) !== -1
)
});
}</code></pre>
<p>An alternative UI pattern frequently used involves postponing the update of the results list and persisting the display of the previous results until the new results become available.</p>
<p>To implement this approach, you can utilize the <code>useDeferredValue</code> hook to provide a deferred version of the query as it gets passed down the component hierarchy.</p>
<pre><code>// app.js
import { Suspense, useState, useDeferredValue } from 'react';
import SearchResults from './searchResult.js';
export default function App() {
const [query, setQuery] = useState('');
const deferredQuery = useDeferredValue(query);
return (
&lt;&gt;
&lt;label&gt;
Search Songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={deferredQuery} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}</code></pre>
<p><img src="https://miro.medium.com/v2/resize:fit:1064/1*hjshq-KagMn3ZBfsIJgUQQ.png" alt="img"></p>
<p>Now, you edit your search query to “ab”. React will first re-render the search bar with the old deferred value, which is the search results for “a”. Then, it will try to re-render the search bar with the new deferred value, which is the search results for “ab”.</p>
<p>This means that the user will never see the loading fallback again, even if the search results for “ab” take a long time to come back.</p>
<p>In other words, deferred values allow you to render your UI immediately, even if you’re waiting for asynchronous data. This can help to improve the user experience by avoiding unnecessary loading fallbacks.</p>
<h1>Benefits of useDeferredValue</h1>
<ol>
<li><strong>Improved Performance:</strong> Deferred loading and rendering of components or data can lead to faster initial page loads, reduced resource usage, and optimized performance, resulting in a better user experience.</li>
<li><strong>Efficient Resource Utilization:</strong> By loading only what’s needed when it’s needed, you can reduce unnecessary API calls, minimize memory and CPU usage, and optimize your application’s resource utilization.</li>
<li><strong>Progressive Loading and Error Isolation:</strong> Deferred value strategies enable progressive loading for a more responsive UI and provide better error isolation, ensuring that errors in one part of your application don’t disrupt the entire user experience.<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ol>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月3日，开源日报第1063期：<br>
今日推荐开源项目：《Open-AnimateAnyone》<br>
今日推荐英文原文：《Say Goodbye to Debouncing: Use “useDeferredValue” Hook》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240103.jpg.pagespeed.ic.Vr3IbMyOUi.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Open-AnimateAnyone》传送门：<a href="https://github.com/guoqincode/Open-AnimateAnyone" target="_blank">项目链接</a> </p>
<p>推荐理由：动画制作的非官方实现，这个仓库是对Animate Anyone的非官方实现，基于magic-animate和AnimateDiff构建，第一阶段训练基本测试通过，目前正在训练和测试第二阶段，效果图如下</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xiShot_2024-01-05_20.00.40.jpg.pagespeed.ic.fSSQItkU1f.jpg"></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/say-goodbye-to-debouncing-use-usedeferredvalue-hook-56e1c28a3e49" target="_blank">Say Goodbye to Debouncing: Use “useDeferredValue” Hook</a></p>
<p>推荐理由：本文介绍了React 18引入的新工具——useDeferredValue钩子，用于优化应用程序性能.该钩子在处理异步数据获取（如网络请求或API数据加载）时很有用，其主要目的是推迟对应用程序中不太关键部分的更新，同时立即渲染最重要的部分</p>
<hr>
<h1>Say Goodbye to Debouncing: Use “useDeferredValue” Hook</h1>
<p>React is a popular JavaScript library for building user interfaces, and it continually evolves to provide developers with tools to optimize performance.</p>
<p>One such tool introduced in React 18 is the <code>useDeferredValue</code> hook, which is designed to improve the performance of your applications by prioritizing rendering updates.</p>
<h1>What is the useDeferredValue Hook?</h1>
<p>The <code>useDeferredValue</code> hook is a relatively new addition to React's arsenal of performance optimization tools.</p>
<p>It's particularly useful when dealing with asynchronous data fetching, such as network requests or data loading from APIs.</p>
<p>The primary purpose of <code>useDeferredValue</code> is to defer updates to less critical parts of your application while immediately rendering the most important parts.</p>
<p>This can greatly improve the perceived performance of your application by avoiding delays in rendering user interface components.</p>
<h1>Basic Usage of useDeferredValue</h1>
<p>The basic usage of the <code>useDeferredValue</code> hook involves wrapping a state value and creating a deferred version of it. Here's a simple example:</p>
<pre><code>import { useState, useDeferredValue } from 'react';
function MyComponent() {
const [data, setData] = useState([]);
const deferredData = useDeferredValue(data);
// ...
}</code></pre>
<p>In this example, we have a state variable <code>data</code>, which might be populated with data from an asynchronous operation.</p>
<p>By using <code>useDeferredValue</code>, we create <code>deferredData</code>, a version of <code>data</code> that React will prioritize rendering separately.</p>
<p>This separation ensures that the critical parts of your UI update promptly, while non-critical updates, like rendering lists, can be deferred to reduce the impact on performance.</p>
<h1>Usage</h1>
<p>When you first render the component, the deferred value will be the same as the value you gave it.</p>
<p>When you update the component, the deferred value will lag behind the latest value. This means that React will first re-render the component with the old deferred value, and then try to re-render it with the new deferred value in the background.</p>
<p>Here is an example of when this is useful:</p>
<p>Imagine you have a search bar that fetches search results as you type. You start typing “a”, and React renders the search bar with the loading fallback. The search results for “a” eventually come back, and React re-renders the search bar with the results.</p>
<pre><code>// app.js
import { Suspense, useState } from 'react';
import SearchResults from './SearchResults.js';
export default function App() {
const [query, setQuery] = useState('');
return (
&lt;&gt;
&lt;label&gt;
Search songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={query} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}
// searchResult.js
import { fetchData } from './data.js';
export default function SearchResults({ query }) {
if (query === '') {
return null;
}
const songs = use(fetchData(`/search?q=${query}`));
if (songs.length === 0) {
return &lt;p&gt;No matches for &lt;i&gt;"{query}"&lt;/i&gt;&lt;/p&gt;;
}
return (
&lt;ul&gt;
{songs.map(song =&gt; (
&lt;li key={song.id}&gt;
{song.title} ({song.year})
&lt;/li&gt;
))}
&lt;/ul&gt;
);
}
function use(promise) {
if (promise.status === 'fulfilled') {
return promise.value;
} else if (promise.status === 'rejected') {
throw promise.reason;
} else if (promise.status === 'pending') {
throw promise;
} else {
promise.status = 'pending';
promise.then(
result =&gt; {
promise.status = 'fulfilled';
promise.value = result;
},
reason =&gt; {
promise.status = 'rejected';
promise.reason = reason;
},
);
throw promise;
}
}
// data.js
let cache = new Map();
export function fetchData(url) {
if (!cache.has(url)) {
cache.set(url, getData(url));
}
return cache.get(url);
}
async function getData(url) {
if (url.startsWith('/search?q=')) {
return await getSearchResults(url.slice('/search?q='.length));
} else {
throw Error('Not implemented');
}
}
async function getSearchResults(query) {
// Add a fake delay to make waiting noticeable.
await new Promise(resolve =&gt; {
setTimeout(resolve, 500);
});
const allSongs = [{
id: 1,
title: "Bohemian Rhapsody",
year: 1975
},
{
id: 2,
title: "Imagine",
year: 1971
},
{
id: 3,
title: "Hotel California",
year: 1976
},
{
id: 4,
title: "Stairway to Heaven",
year: 1971
},
{
id: 5,
title: "Let It Be",
year: 1970
},
{
id: 6,
title: "Abbey",
year: 1976
},
{
id: 7,
title: "A Hard Day's Night",
year: 2012
}];
const lowerQuery = query.trim().toLowerCase();
return allSongs.filter(album =&gt; {
const lowerTitle = album.title.toLowerCase();
return (
lowerTitle.startsWith(lowerQuery) ||
lowerTitle.indexOf(' ' + lowerQuery) !== -1
)
});
}</code></pre>
<p>An alternative UI pattern frequently used involves postponing the update of the results list and persisting the display of the previous results until the new results become available.</p>
<p>To implement this approach, you can utilize the <code>useDeferredValue</code> hook to provide a deferred version of the query as it gets passed down the component hierarchy.</p>
<pre><code>// app.js
import { Suspense, useState, useDeferredValue } from 'react';
import SearchResults from './searchResult.js';
export default function App() {
const [query, setQuery] = useState('');
const deferredQuery = useDeferredValue(query);
return (
&lt;&gt;
&lt;label&gt;
Search Songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={deferredQuery} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}</code></pre>
<p><img src="https://miro.medium.com/v2/resize:fit:1064/1*hjshq-KagMn3ZBfsIJgUQQ.png" alt="img"></p>
<p>Now, you edit your search query to “ab”. React will first re-render the search bar with the old deferred value, which is the search results for “a”. Then, it will try to re-render the search bar with the new deferred value, which is the search results for “ab”.</p>
<p>This means that the user will never see the loading fallback again, even if the search results for “ab” take a long time to come back.</p>
<p>In other words, deferred values allow you to render your UI immediately, even if you’re waiting for asynchronous data. This can help to improve the user experience by avoiding unnecessary loading fallbacks.</p>
<h1>Benefits of useDeferredValue</h1>
<ol>
<li><strong>Improved Performance:</strong> Deferred loading and rendering of components or data can lead to faster initial page loads, reduced resource usage, and optimized performance, resulting in a better user experience.</li>
<li><strong>Efficient Resource Utilization:</strong> By loading only what’s needed when it’s needed, you can reduce unnecessary API calls, minimize memory and CPU usage, and optimize your application’s resource utilization.</li>
<li><strong>Progressive Loading and Error Isolation:</strong> Deferred value strategies enable progressive loading for a more responsive UI and provide better error isolation, ensuring that errors in one part of your application don’t disrupt the entire user experience.<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ol>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月3日，开源日报第1063期：<br>
今日推荐开源项目：《Open-AnimateAnyone》<br>
今日推荐英文原文：《Say Goodbye to Debouncing: Use “useDeferredValue” Hook》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240103.jpg.pagespeed.ic.Vr3IbMyOUi.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Open-AnimateAnyone》传送门：<a href="https://github.com/guoqincode/Open-AnimateAnyone" target="_blank">项目链接</a> </p>
<p>推荐理由：动画制作的非官方实现，这个仓库是对Animate Anyone的非官方实现，基于magic-animate和AnimateDiff构建，第一阶段训练基本测试通过，目前正在训练和测试第二阶段，效果图如下</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xiShot_2024-01-05_20.00.40.jpg.pagespeed.ic.fSSQItkU1f.jpg"></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/say-goodbye-to-debouncing-use-usedeferredvalue-hook-56e1c28a3e49" target="_blank">Say Goodbye to Debouncing: Use “useDeferredValue” Hook</a></p>
<p>推荐理由：本文介绍了React 18引入的新工具——useDeferredValue钩子，用于优化应用程序性能.该钩子在处理异步数据获取（如网络请求或API数据加载）时很有用，其主要目的是推迟对应用程序中不太关键部分的更新，同时立即渲染最重要的部分</p>
<hr>
<h1>Say Goodbye to Debouncing: Use “useDeferredValue” Hook</h1>
<p>React is a popular JavaScript library for building user interfaces, and it continually evolves to provide developers with tools to optimize performance.</p>
<p>One such tool introduced in React 18 is the <code>useDeferredValue</code> hook, which is designed to improve the performance of your applications by prioritizing rendering updates.</p>
<h1>What is the useDeferredValue Hook?</h1>
<p>The <code>useDeferredValue</code> hook is a relatively new addition to React's arsenal of performance optimization tools.</p>
<p>It's particularly useful when dealing with asynchronous data fetching, such as network requests or data loading from APIs.</p>
<p>The primary purpose of <code>useDeferredValue</code> is to defer updates to less critical parts of your application while immediately rendering the most important parts.</p>
<p>This can greatly improve the perceived performance of your application by avoiding delays in rendering user interface components.</p>
<h1>Basic Usage of useDeferredValue</h1>
<p>The basic usage of the <code>useDeferredValue</code> hook involves wrapping a state value and creating a deferred version of it. Here's a simple example:</p>
<pre><code>import { useState, useDeferredValue } from 'react';
function MyComponent() {
const [data, setData] = useState([]);
const deferredData = useDeferredValue(data);
// ...
}</code></pre>
<p>In this example, we have a state variable <code>data</code>, which might be populated with data from an asynchronous operation.</p>
<p>By using <code>useDeferredValue</code>, we create <code>deferredData</code>, a version of <code>data</code> that React will prioritize rendering separately.</p>
<p>This separation ensures that the critical parts of your UI update promptly, while non-critical updates, like rendering lists, can be deferred to reduce the impact on performance.</p>
<h1>Usage</h1>
<p>When you first render the component, the deferred value will be the same as the value you gave it.</p>
<p>When you update the component, the deferred value will lag behind the latest value. This means that React will first re-render the component with the old deferred value, and then try to re-render it with the new deferred value in the background.</p>
<p>Here is an example of when this is useful:</p>
<p>Imagine you have a search bar that fetches search results as you type. You start typing “a”, and React renders the search bar with the loading fallback. The search results for “a” eventually come back, and React re-renders the search bar with the results.</p>
<pre><code>// app.js
import { Suspense, useState } from 'react';
import SearchResults from './SearchResults.js';
export default function App() {
const [query, setQuery] = useState('');
return (
&lt;&gt;
&lt;label&gt;
Search songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={query} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}
// searchResult.js
import { fetchData } from './data.js';
export default function SearchResults({ query }) {
if (query === '') {
return null;
}
const songs = use(fetchData(`/search?q=${query}`));
if (songs.length === 0) {
return &lt;p&gt;No matches for &lt;i&gt;"{query}"&lt;/i&gt;&lt;/p&gt;;
}
return (
&lt;ul&gt;
{songs.map(song =&gt; (
&lt;li key={song.id}&gt;
{song.title} ({song.year})
&lt;/li&gt;
))}
&lt;/ul&gt;
);
}
function use(promise) {
if (promise.status === 'fulfilled') {
return promise.value;
} else if (promise.status === 'rejected') {
throw promise.reason;
} else if (promise.status === 'pending') {
throw promise;
} else {
promise.status = 'pending';
promise.then(
result =&gt; {
promise.status = 'fulfilled';
promise.value = result;
},
reason =&gt; {
promise.status = 'rejected';
promise.reason = reason;
},
);
throw promise;
}
}
// data.js
let cache = new Map();
export function fetchData(url) {
if (!cache.has(url)) {
cache.set(url, getData(url));
}
return cache.get(url);
}
async function getData(url) {
if (url.startsWith('/search?q=')) {
return await getSearchResults(url.slice('/search?q='.length));
} else {
throw Error('Not implemented');
}
}
async function getSearchResults(query) {
// Add a fake delay to make waiting noticeable.
await new Promise(resolve =&gt; {
setTimeout(resolve, 500);
});
const allSongs = [{
id: 1,
title: "Bohemian Rhapsody",
year: 1975
},
{
id: 2,
title: "Imagine",
year: 1971
},
{
id: 3,
title: "Hotel California",
year: 1976
},
{
id: 4,
title: "Stairway to Heaven",
year: 1971
},
{
id: 5,
title: "Let It Be",
year: 1970
},
{
id: 6,
title: "Abbey",
year: 1976
},
{
id: 7,
title: "A Hard Day's Night",
year: 2012
}];
const lowerQuery = query.trim().toLowerCase();
return allSongs.filter(album =&gt; {
const lowerTitle = album.title.toLowerCase();
return (
lowerTitle.startsWith(lowerQuery) ||
lowerTitle.indexOf(' ' + lowerQuery) !== -1
)
});
}</code></pre>
<p>An alternative UI pattern frequently used involves postponing the update of the results list and persisting the display of the previous results until the new results become available.</p>
<p>To implement this approach, you can utilize the <code>useDeferredValue</code> hook to provide a deferred version of the query as it gets passed down the component hierarchy.</p>
<pre><code>// app.js
import { Suspense, useState, useDeferredValue } from 'react';
import SearchResults from './searchResult.js';
export default function App() {
const [query, setQuery] = useState('');
const deferredQuery = useDeferredValue(query);
return (
&lt;&gt;
&lt;label&gt;
Search Songs:
&lt;input value={query} onChange={e =&gt; setQuery(e.target.value)} /&gt;
&lt;/label&gt;
&lt;Suspense fallback={&lt;h2&gt;Loading...&lt;/h2&gt;}&gt;
&lt;SearchResults query={deferredQuery} /&gt;
&lt;/Suspense&gt;
&lt;/&gt;
);
}</code></pre>
<p><img src="https://miro.medium.com/v2/resize:fit:1064/1*hjshq-KagMn3ZBfsIJgUQQ.png" alt="img"></p>
<p>Now, you edit your search query to “ab”. React will first re-render the search bar with the old deferred value, which is the search results for “a”. Then, it will try to re-render the search bar with the new deferred value, which is the search results for “ab”.</p>
<p>This means that the user will never see the loading fallback again, even if the search results for “ab” take a long time to come back.</p>
<p>In other words, deferred values allow you to render your UI immediately, even if you’re waiting for asynchronous data. This can help to improve the user experience by avoiding unnecessary loading fallbacks.</p>
<h1>Benefits of useDeferredValue</h1>
<ol>
<li><strong>Improved Performance:</strong> Deferred loading and rendering of components or data can lead to faster initial page loads, reduced resource usage, and optimized performance, resulting in a better user experience.</li>
<li><strong>Efficient Resource Utilization:</strong> By loading only what’s needed when it’s needed, you can reduce unnecessary API calls, minimize memory and CPU usage, and optimize your application’s resource utilization.</li>
<li><strong>Progressive Loading and Error Isolation:</strong> Deferred value strategies enable progressive loading for a more responsive UI and provide better error isolation, ensuring that errors in one part of your application don’t disrupt the entire user experience.<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ol>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240103.jpg.pagespeed.ic.Vr3IbMyOUi.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240103.jpg.pagespeed.ic.Vr3IbMyOUi.jpg" medium="image"/>
    <pubDate>Wed, 03 Jan 2024 12:03:10 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1062期：大语言模型（LLM）课程：《llm-course》]]></title>
    <link>https://openingsource.org/11463/</link>
    <itunes:title><![CDATA[开源日报第1062期：大语言模型（LLM）课程：《llm-course》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月2日，开源日报第1062期：<br>
今日推荐开源项目：《llm-course》<br>
今日推荐英文原文：《Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240102.jpg.pagespeed.ic.FgIoKmYqkM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《llm-course》传送门：<a href="https://github.com/mlabonne/llm-course" target="_blank">项目链接</a> </p>
<p>推荐理由：这个路线图和Colab notebooks可以帮助你学习大型语言模型(LLM)相关课程，LLM（大型语言模型）课程分为三个部分：</p>
<p>🧩 <strong>LLM基础知识</strong> 包含关于数学、Python 和神经网络的基本知识</p>
<p>🧑‍🔬 <strong>LLM科学家</strong> 专注于学习如何利用最新技术构建最佳的LLM</p>
<p>👷 <strong>LLM工程师</strong> 专注于如何创建基于LLM的解决方案并将其部署</p>
<p>Blog 网站直达 <a href="https://mlabonne.github.io/blog/" target="_blank">https://mlabonne.github.io/blog/</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/mastering-react-js-best-practices-and-design-patterns-for-high-quality-applications-0e0ef381df89" target="_blank">Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
</a></p>
<p>推荐理由：React.js 已成为构建动态用户界面的首选库，其基于组件的架构和声明式语法使其成为创建可扩展且易于维护的应用程序的强大工具，本文概述了最佳实践和设计模式，如合理项目结构提高可扩展性； 明智使用状态管理，避免局部状态传递；采用函数组件和Hooks简化生命周期等等</p>
<hr>
<h1>Mastering React.js: Best Practices and Design Patterns for High-Quality Applications</h1>
<p>React.js has become the go-to library for building dynamic user interfaces. Its component-based architecture and declarative syntax make it a powerful tool for creating scalable and maintainable applications. However, as your project grows, maintaining code quality and scalability becomes paramount. In this article, we’ll explore the best practices and design patterns in React.js that will help you build high-quality applications.</p>
<h1>1. Project Structure: Organize for Scalability</h1>
<p>A well-organized project structure is the foundation for a scalable React application. Consider structuring your project based on features or modules rather than file types. This modular approach makes it easier to locate and update code related to specific functionality.</p>
<p>Example:</p>
<pre><code>/src
/components
Header.js
Footer.js
/pages
Home.js
About.js
/services
api.js
/styles
main.css
App.js
index.js</code></pre>
<h1>2. State Management: Use State Wisely</h1>
<p>State is at the core of React applications. Effective state management is crucial for building scalable applications. Lift state up to the nearest common ancestor for shared state between components. Utilize state management libraries like Redux for complex state scenarios.</p>
<p>Example:</p>
<pre><code>// Bad: Local state causing prop drilling
import React, { useState } from 'react';
// ChildComponent needs access to count and setCount, so it gets them as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// IntermediateComponent receives count and setCount as props but doesn't use them
function IntermediateComponent({ count, setCount }) {
return (
&lt;div&gt;
&lt;p&gt;Intermediate Component&lt;/p&gt;
&lt;ChildComponent count={count} setCount={setCount} /&gt;
&lt;/div&gt;
);
}
// ParentComponent manages the count state and passes it down to IntermediateComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;IntermediateComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;
// Good: State lifted up
import React, { useState } from 'react';
// ChildComponent receives count and setCount directly as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// ParentComponent manages the count state and passes it down to ChildComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;ChildComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;</code></pre>
<h1>3. Component Lifecycle: Embrace Functional Components and Hooks</h1>
<p>With the introduction of Hooks, functional components have become more powerful than ever. Embrace functional components and use Hooks for state management, side effects, and context. This simplifies code and avoids the complexities of class component lifecycles.</p>
<p>Example:</p>
<pre><code>// Class component with lifecycle methods
class ExampleComponent extends React.Component {
componentDidMount() {
console.log('Component did mount');
}
componentWillUnmount() {
console.log('Component will unmount');
}
render() {
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}
}
// Functional component with useEffect Hook
function ExampleComponent() {
useEffect(() =&gt; {
console.log('Component did mount');
return () =&gt; console.log('Component will unmount');
}, []);
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}</code></pre>
<h1>4. Props: Destructuring and PropTypes</h1>
<p>Destructuring props in functional components enhances readability. Additionally, PropTypes offer a way to document and validate component props, preventing common runtime errors.</p>
<p>Example:</p>
<pre><code>// Destructuring props for improved readability
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
// PropTypes for documenting and validating props
import PropTypes from 'prop-types';
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
Greeting.propTypes = {
name: PropTypes.string.isRequired,
age: PropTypes.number.isRequired,
};</code></pre>
<h1>5. Reusable Components: Create a Component Library</h1>
<p>Encapsulate common UI patterns into reusable components. Building a component library not only accelerates development but also ensures a consistent look and feel throughout your application.</p>
<p>Example:</p>
<pre><code>// Reusable Button component
function Button({ onClick, children }) {
return &lt;button onClick={onClick}&gt;{children}&lt;/button&gt;;
}</code></pre>
<h1>6. Error Boundaries: Graceful Handling of Errors</h1>
<p>Wrap components with error boundaries to gracefully handle errors and prevent entire UI crashes. This provides a better user experience and facilitates easier debugging.</p>
<p>Example:</p>
<pre><code>class ErrorBoundary extends React.Component {
constructor(props) {
super(props);
this.state = { hasError: false };
}
componentDidCatch(error, errorInfo) {
this.setState({ hasError: true });
// Log error information
console.error(error, errorInfo);
}
render() {
if (this.state.hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return this.props.children;
}
}
import React, { useState } from 'react';
function ErrorBoundary({ children }) {
const [hasError, setHasError] = useState(false);
const componentDidCatch = (error, errorInfo) =&gt; {
setHasError(true);
// Log error information
console.error(error, errorInfo);
};
if (hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return children;
}
export default ErrorBoundary;</code></pre>
<h1>7. Performance Optimization: Memoization and PureComponent</h1>
<p>Improve performance by memoizing components using <code>React.memo</code> and utilizing <code>PureComponent</code> for class components. This prevents unnecessary renders and optimizes your application.</p>
<p><strong>Memoized Functional Component:</strong></p>
<pre><code>import React from 'react';
const MemoizedComponent = React.memo(function MyComponent(props) {
/* render using props */
console.log('Rendering MemoizedComponent');
return (
&lt;div&gt;
&lt;p&gt;{props.text}&lt;/p&gt;
&lt;/div&gt;
);
});
export default MemoizedComponent;</code></pre>
<h1>PureComponent Class Component:</h1>
<pre><code>import React from 'react';
class MyPureComponent extends React.PureComponent {
render() {
/* render using this.props */
console.log('Rendering MyPureComponent');
return (
&lt;div&gt;
&lt;p&gt;{this.props.text}&lt;/p&gt;
&lt;/div&gt;
);
}
}
export default MyPureComponent;</code></pre>
<p><strong>Usage example:</strong></p>
<pre><code>import React, { useState } from 'react';
import MemoizedComponent from './MemoizedComponent';
import MyPureComponent from './MyPureComponent';
function App() {
const [text, setText] = useState('Initial Text');
const handleButtonClick = () =&gt; {
setText('Updated Text');
};
return (
&lt;div&gt;
&lt;button onClick={handleButtonClick}&gt;Update Text&lt;/button&gt;
&lt;MemoizedComponent text={text} /&gt;
&lt;MyPureComponent text={text} /&gt;
&lt;/div&gt;
);
}
export default App;</code></pre>
<h1>8. Code Splitting: Optimize Load Time</h1>
<p>Break down your application into smaller chunks and load them on demand. Code splitting helps reduce initial load times, making your application more performant.</p>
<p>Example:</p>
<pre><code>// Using React.lazy for code splitting
const MyComponent = React.lazy(() =&gt; import('./MyComponent'));
// Suspense for fallback UI while the component is loading
const App = () =&gt; (
&lt;React.Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;MyComponent /&gt;
&lt;/React.Suspense&gt;
);</code></pre>
<h1>9. Testing: Adopt a Robust Testing Strategy</h1>
<p>Implement a comprehensive testing strategy using tools like Jest and React Testing Library. Write unit tests, integration tests, and end-to-end tests to ensure the reliability of your application.</p>
<p>Example:</p>
<pre><code>// Jest and React Testing Library test
import { render, screen } from '@testing-library/react';
import MyComponent from './MyComponent';
test('renders component with correct text', () =&gt; {
render(&lt;MyComponent /&gt;);
expect(screen.getByText('Hello, World!')).toBeInTheDocument();
});</code></pre>
<h1>10. Accessibility: Prioritize Inclusive Design</h1>
<p>Make your application accessible to a wide range of users by following web accessibility standards. Use semantic HTML, provide proper labels, and test your application with screen readers.</p>
<p>Example:</p>
<pre><code>// Using semantic HTML for better accessibility
&lt;button aria-label="Close" onClick={handleClose}&gt;
&lt;span aria-hidden="true"&gt;×&lt;/span&gt;
&lt;/button&gt;</code></pre>
<h1>Conclusion</h1>
<p>Building high-quality React applications involves a combination of best practices and design patterns. By adhering to these guidelines, you’ll not only enhance the maintainability and scalability of your project but also contribute to a positive development experience for your team. Keep evolving with the React ecosystem, stay updated on new features, and continuously refine your approach to ensure your applications remain at the forefront of web development.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月2日，开源日报第1062期：<br>
今日推荐开源项目：《llm-course》<br>
今日推荐英文原文：《Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240102.jpg.pagespeed.ic.FgIoKmYqkM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《llm-course》传送门：<a href="https://github.com/mlabonne/llm-course" target="_blank">项目链接</a> </p>
<p>推荐理由：这个路线图和Colab notebooks可以帮助你学习大型语言模型(LLM)相关课程，LLM（大型语言模型）课程分为三个部分：</p>
<p>🧩 <strong>LLM基础知识</strong> 包含关于数学、Python 和神经网络的基本知识</p>
<p>🧑‍🔬 <strong>LLM科学家</strong> 专注于学习如何利用最新技术构建最佳的LLM</p>
<p>👷 <strong>LLM工程师</strong> 专注于如何创建基于LLM的解决方案并将其部署</p>
<p>Blog 网站直达 <a href="https://mlabonne.github.io/blog/" target="_blank">https://mlabonne.github.io/blog/</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/mastering-react-js-best-practices-and-design-patterns-for-high-quality-applications-0e0ef381df89" target="_blank">Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
</a></p>
<p>推荐理由：React.js 已成为构建动态用户界面的首选库，其基于组件的架构和声明式语法使其成为创建可扩展且易于维护的应用程序的强大工具，本文概述了最佳实践和设计模式，如合理项目结构提高可扩展性； 明智使用状态管理，避免局部状态传递；采用函数组件和Hooks简化生命周期等等</p>
<hr>
<h1>Mastering React.js: Best Practices and Design Patterns for High-Quality Applications</h1>
<p>React.js has become the go-to library for building dynamic user interfaces. Its component-based architecture and declarative syntax make it a powerful tool for creating scalable and maintainable applications. However, as your project grows, maintaining code quality and scalability becomes paramount. In this article, we’ll explore the best practices and design patterns in React.js that will help you build high-quality applications.</p>
<h1>1. Project Structure: Organize for Scalability</h1>
<p>A well-organized project structure is the foundation for a scalable React application. Consider structuring your project based on features or modules rather than file types. This modular approach makes it easier to locate and update code related to specific functionality.</p>
<p>Example:</p>
<pre><code>/src
/components
Header.js
Footer.js
/pages
Home.js
About.js
/services
api.js
/styles
main.css
App.js
index.js</code></pre>
<h1>2. State Management: Use State Wisely</h1>
<p>State is at the core of React applications. Effective state management is crucial for building scalable applications. Lift state up to the nearest common ancestor for shared state between components. Utilize state management libraries like Redux for complex state scenarios.</p>
<p>Example:</p>
<pre><code>// Bad: Local state causing prop drilling
import React, { useState } from 'react';
// ChildComponent needs access to count and setCount, so it gets them as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// IntermediateComponent receives count and setCount as props but doesn't use them
function IntermediateComponent({ count, setCount }) {
return (
&lt;div&gt;
&lt;p&gt;Intermediate Component&lt;/p&gt;
&lt;ChildComponent count={count} setCount={setCount} /&gt;
&lt;/div&gt;
);
}
// ParentComponent manages the count state and passes it down to IntermediateComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;IntermediateComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;
// Good: State lifted up
import React, { useState } from 'react';
// ChildComponent receives count and setCount directly as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// ParentComponent manages the count state and passes it down to ChildComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;ChildComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;</code></pre>
<h1>3. Component Lifecycle: Embrace Functional Components and Hooks</h1>
<p>With the introduction of Hooks, functional components have become more powerful than ever. Embrace functional components and use Hooks for state management, side effects, and context. This simplifies code and avoids the complexities of class component lifecycles.</p>
<p>Example:</p>
<pre><code>// Class component with lifecycle methods
class ExampleComponent extends React.Component {
componentDidMount() {
console.log('Component did mount');
}
componentWillUnmount() {
console.log('Component will unmount');
}
render() {
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}
}
// Functional component with useEffect Hook
function ExampleComponent() {
useEffect(() =&gt; {
console.log('Component did mount');
return () =&gt; console.log('Component will unmount');
}, []);
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}</code></pre>
<h1>4. Props: Destructuring and PropTypes</h1>
<p>Destructuring props in functional components enhances readability. Additionally, PropTypes offer a way to document and validate component props, preventing common runtime errors.</p>
<p>Example:</p>
<pre><code>// Destructuring props for improved readability
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
// PropTypes for documenting and validating props
import PropTypes from 'prop-types';
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
Greeting.propTypes = {
name: PropTypes.string.isRequired,
age: PropTypes.number.isRequired,
};</code></pre>
<h1>5. Reusable Components: Create a Component Library</h1>
<p>Encapsulate common UI patterns into reusable components. Building a component library not only accelerates development but also ensures a consistent look and feel throughout your application.</p>
<p>Example:</p>
<pre><code>// Reusable Button component
function Button({ onClick, children }) {
return &lt;button onClick={onClick}&gt;{children}&lt;/button&gt;;
}</code></pre>
<h1>6. Error Boundaries: Graceful Handling of Errors</h1>
<p>Wrap components with error boundaries to gracefully handle errors and prevent entire UI crashes. This provides a better user experience and facilitates easier debugging.</p>
<p>Example:</p>
<pre><code>class ErrorBoundary extends React.Component {
constructor(props) {
super(props);
this.state = { hasError: false };
}
componentDidCatch(error, errorInfo) {
this.setState({ hasError: true });
// Log error information
console.error(error, errorInfo);
}
render() {
if (this.state.hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return this.props.children;
}
}
import React, { useState } from 'react';
function ErrorBoundary({ children }) {
const [hasError, setHasError] = useState(false);
const componentDidCatch = (error, errorInfo) =&gt; {
setHasError(true);
// Log error information
console.error(error, errorInfo);
};
if (hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return children;
}
export default ErrorBoundary;</code></pre>
<h1>7. Performance Optimization: Memoization and PureComponent</h1>
<p>Improve performance by memoizing components using <code>React.memo</code> and utilizing <code>PureComponent</code> for class components. This prevents unnecessary renders and optimizes your application.</p>
<p><strong>Memoized Functional Component:</strong></p>
<pre><code>import React from 'react';
const MemoizedComponent = React.memo(function MyComponent(props) {
/* render using props */
console.log('Rendering MemoizedComponent');
return (
&lt;div&gt;
&lt;p&gt;{props.text}&lt;/p&gt;
&lt;/div&gt;
);
});
export default MemoizedComponent;</code></pre>
<h1>PureComponent Class Component:</h1>
<pre><code>import React from 'react';
class MyPureComponent extends React.PureComponent {
render() {
/* render using this.props */
console.log('Rendering MyPureComponent');
return (
&lt;div&gt;
&lt;p&gt;{this.props.text}&lt;/p&gt;
&lt;/div&gt;
);
}
}
export default MyPureComponent;</code></pre>
<p><strong>Usage example:</strong></p>
<pre><code>import React, { useState } from 'react';
import MemoizedComponent from './MemoizedComponent';
import MyPureComponent from './MyPureComponent';
function App() {
const [text, setText] = useState('Initial Text');
const handleButtonClick = () =&gt; {
setText('Updated Text');
};
return (
&lt;div&gt;
&lt;button onClick={handleButtonClick}&gt;Update Text&lt;/button&gt;
&lt;MemoizedComponent text={text} /&gt;
&lt;MyPureComponent text={text} /&gt;
&lt;/div&gt;
);
}
export default App;</code></pre>
<h1>8. Code Splitting: Optimize Load Time</h1>
<p>Break down your application into smaller chunks and load them on demand. Code splitting helps reduce initial load times, making your application more performant.</p>
<p>Example:</p>
<pre><code>// Using React.lazy for code splitting
const MyComponent = React.lazy(() =&gt; import('./MyComponent'));
// Suspense for fallback UI while the component is loading
const App = () =&gt; (
&lt;React.Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;MyComponent /&gt;
&lt;/React.Suspense&gt;
);</code></pre>
<h1>9. Testing: Adopt a Robust Testing Strategy</h1>
<p>Implement a comprehensive testing strategy using tools like Jest and React Testing Library. Write unit tests, integration tests, and end-to-end tests to ensure the reliability of your application.</p>
<p>Example:</p>
<pre><code>// Jest and React Testing Library test
import { render, screen } from '@testing-library/react';
import MyComponent from './MyComponent';
test('renders component with correct text', () =&gt; {
render(&lt;MyComponent /&gt;);
expect(screen.getByText('Hello, World!')).toBeInTheDocument();
});</code></pre>
<h1>10. Accessibility: Prioritize Inclusive Design</h1>
<p>Make your application accessible to a wide range of users by following web accessibility standards. Use semantic HTML, provide proper labels, and test your application with screen readers.</p>
<p>Example:</p>
<pre><code>// Using semantic HTML for better accessibility
&lt;button aria-label="Close" onClick={handleClose}&gt;
&lt;span aria-hidden="true"&gt;×&lt;/span&gt;
&lt;/button&gt;</code></pre>
<h1>Conclusion</h1>
<p>Building high-quality React applications involves a combination of best practices and design patterns. By adhering to these guidelines, you’ll not only enhance the maintainability and scalability of your project but also contribute to a positive development experience for your team. Keep evolving with the React ecosystem, stay updated on new features, and continuously refine your approach to ensure your applications remain at the forefront of web development.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2024年1月2日，开源日报第1062期：<br>
今日推荐开源项目：《llm-course》<br>
今日推荐英文原文：《Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240102.jpg.pagespeed.ic.FgIoKmYqkM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《llm-course》传送门：<a href="https://github.com/mlabonne/llm-course" target="_blank">项目链接</a> </p>
<p>推荐理由：这个路线图和Colab notebooks可以帮助你学习大型语言模型(LLM)相关课程，LLM（大型语言模型）课程分为三个部分：</p>
<p>🧩 <strong>LLM基础知识</strong> 包含关于数学、Python 和神经网络的基本知识</p>
<p>🧑‍🔬 <strong>LLM科学家</strong> 专注于学习如何利用最新技术构建最佳的LLM</p>
<p>👷 <strong>LLM工程师</strong> 专注于如何创建基于LLM的解决方案并将其部署</p>
<p>Blog 网站直达 <a href="https://mlabonne.github.io/blog/" target="_blank">https://mlabonne.github.io/blog/</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/mastering-react-js-best-practices-and-design-patterns-for-high-quality-applications-0e0ef381df89" target="_blank">Mastering React.js: Best Practices and Design Patterns for High-Quality Applications<br>
</a></p>
<p>推荐理由：React.js 已成为构建动态用户界面的首选库，其基于组件的架构和声明式语法使其成为创建可扩展且易于维护的应用程序的强大工具，本文概述了最佳实践和设计模式，如合理项目结构提高可扩展性； 明智使用状态管理，避免局部状态传递；采用函数组件和Hooks简化生命周期等等</p>
<hr>
<h1>Mastering React.js: Best Practices and Design Patterns for High-Quality Applications</h1>
<p>React.js has become the go-to library for building dynamic user interfaces. Its component-based architecture and declarative syntax make it a powerful tool for creating scalable and maintainable applications. However, as your project grows, maintaining code quality and scalability becomes paramount. In this article, we’ll explore the best practices and design patterns in React.js that will help you build high-quality applications.</p>
<h1>1. Project Structure: Organize for Scalability</h1>
<p>A well-organized project structure is the foundation for a scalable React application. Consider structuring your project based on features or modules rather than file types. This modular approach makes it easier to locate and update code related to specific functionality.</p>
<p>Example:</p>
<pre><code>/src
/components
Header.js
Footer.js
/pages
Home.js
About.js
/services
api.js
/styles
main.css
App.js
index.js</code></pre>
<h1>2. State Management: Use State Wisely</h1>
<p>State is at the core of React applications. Effective state management is crucial for building scalable applications. Lift state up to the nearest common ancestor for shared state between components. Utilize state management libraries like Redux for complex state scenarios.</p>
<p>Example:</p>
<pre><code>// Bad: Local state causing prop drilling
import React, { useState } from 'react';
// ChildComponent needs access to count and setCount, so it gets them as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// IntermediateComponent receives count and setCount as props but doesn't use them
function IntermediateComponent({ count, setCount }) {
return (
&lt;div&gt;
&lt;p&gt;Intermediate Component&lt;/p&gt;
&lt;ChildComponent count={count} setCount={setCount} /&gt;
&lt;/div&gt;
);
}
// ParentComponent manages the count state and passes it down to IntermediateComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;IntermediateComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;
// Good: State lifted up
import React, { useState } from 'react';
// ChildComponent receives count and setCount directly as props
function ChildComponent({ count, setCount }) {
return &lt;button onClick={() =&gt; setCount(count + 1)}&gt;Increment&lt;/button&gt;;
}
// ParentComponent manages the count state and passes it down to ChildComponent
function ParentComponent() {
const [count, setCount] = useState(0);
return &lt;ChildComponent count={count} setCount={setCount} /&gt;;
}
// App component renders the ParentComponent
function App() {
return &lt;ParentComponent /&gt;;
}
export default App;</code></pre>
<h1>3. Component Lifecycle: Embrace Functional Components and Hooks</h1>
<p>With the introduction of Hooks, functional components have become more powerful than ever. Embrace functional components and use Hooks for state management, side effects, and context. This simplifies code and avoids the complexities of class component lifecycles.</p>
<p>Example:</p>
<pre><code>// Class component with lifecycle methods
class ExampleComponent extends React.Component {
componentDidMount() {
console.log('Component did mount');
}
componentWillUnmount() {
console.log('Component will unmount');
}
render() {
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}
}
// Functional component with useEffect Hook
function ExampleComponent() {
useEffect(() =&gt; {
console.log('Component did mount');
return () =&gt; console.log('Component will unmount');
}, []);
return &lt;div&gt;Hello, World!&lt;/div&gt;;
}</code></pre>
<h1>4. Props: Destructuring and PropTypes</h1>
<p>Destructuring props in functional components enhances readability. Additionally, PropTypes offer a way to document and validate component props, preventing common runtime errors.</p>
<p>Example:</p>
<pre><code>// Destructuring props for improved readability
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
// PropTypes for documenting and validating props
import PropTypes from 'prop-types';
function Greeting({ name, age }) {
return &lt;div&gt;{`Hello, ${name}! You are ${age} years old.`}&lt;/div&gt;;
}
Greeting.propTypes = {
name: PropTypes.string.isRequired,
age: PropTypes.number.isRequired,
};</code></pre>
<h1>5. Reusable Components: Create a Component Library</h1>
<p>Encapsulate common UI patterns into reusable components. Building a component library not only accelerates development but also ensures a consistent look and feel throughout your application.</p>
<p>Example:</p>
<pre><code>// Reusable Button component
function Button({ onClick, children }) {
return &lt;button onClick={onClick}&gt;{children}&lt;/button&gt;;
}</code></pre>
<h1>6. Error Boundaries: Graceful Handling of Errors</h1>
<p>Wrap components with error boundaries to gracefully handle errors and prevent entire UI crashes. This provides a better user experience and facilitates easier debugging.</p>
<p>Example:</p>
<pre><code>class ErrorBoundary extends React.Component {
constructor(props) {
super(props);
this.state = { hasError: false };
}
componentDidCatch(error, errorInfo) {
this.setState({ hasError: true });
// Log error information
console.error(error, errorInfo);
}
render() {
if (this.state.hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return this.props.children;
}
}
import React, { useState } from 'react';
function ErrorBoundary({ children }) {
const [hasError, setHasError] = useState(false);
const componentDidCatch = (error, errorInfo) =&gt; {
setHasError(true);
// Log error information
console.error(error, errorInfo);
};
if (hasError) {
return &lt;div&gt;Something went wrong.&lt;/div&gt;;
}
return children;
}
export default ErrorBoundary;</code></pre>
<h1>7. Performance Optimization: Memoization and PureComponent</h1>
<p>Improve performance by memoizing components using <code>React.memo</code> and utilizing <code>PureComponent</code> for class components. This prevents unnecessary renders and optimizes your application.</p>
<p><strong>Memoized Functional Component:</strong></p>
<pre><code>import React from 'react';
const MemoizedComponent = React.memo(function MyComponent(props) {
/* render using props */
console.log('Rendering MemoizedComponent');
return (
&lt;div&gt;
&lt;p&gt;{props.text}&lt;/p&gt;
&lt;/div&gt;
);
});
export default MemoizedComponent;</code></pre>
<h1>PureComponent Class Component:</h1>
<pre><code>import React from 'react';
class MyPureComponent extends React.PureComponent {
render() {
/* render using this.props */
console.log('Rendering MyPureComponent');
return (
&lt;div&gt;
&lt;p&gt;{this.props.text}&lt;/p&gt;
&lt;/div&gt;
);
}
}
export default MyPureComponent;</code></pre>
<p><strong>Usage example:</strong></p>
<pre><code>import React, { useState } from 'react';
import MemoizedComponent from './MemoizedComponent';
import MyPureComponent from './MyPureComponent';
function App() {
const [text, setText] = useState('Initial Text');
const handleButtonClick = () =&gt; {
setText('Updated Text');
};
return (
&lt;div&gt;
&lt;button onClick={handleButtonClick}&gt;Update Text&lt;/button&gt;
&lt;MemoizedComponent text={text} /&gt;
&lt;MyPureComponent text={text} /&gt;
&lt;/div&gt;
);
}
export default App;</code></pre>
<h1>8. Code Splitting: Optimize Load Time</h1>
<p>Break down your application into smaller chunks and load them on demand. Code splitting helps reduce initial load times, making your application more performant.</p>
<p>Example:</p>
<pre><code>// Using React.lazy for code splitting
const MyComponent = React.lazy(() =&gt; import('./MyComponent'));
// Suspense for fallback UI while the component is loading
const App = () =&gt; (
&lt;React.Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;MyComponent /&gt;
&lt;/React.Suspense&gt;
);</code></pre>
<h1>9. Testing: Adopt a Robust Testing Strategy</h1>
<p>Implement a comprehensive testing strategy using tools like Jest and React Testing Library. Write unit tests, integration tests, and end-to-end tests to ensure the reliability of your application.</p>
<p>Example:</p>
<pre><code>// Jest and React Testing Library test
import { render, screen } from '@testing-library/react';
import MyComponent from './MyComponent';
test('renders component with correct text', () =&gt; {
render(&lt;MyComponent /&gt;);
expect(screen.getByText('Hello, World!')).toBeInTheDocument();
});</code></pre>
<h1>10. Accessibility: Prioritize Inclusive Design</h1>
<p>Make your application accessible to a wide range of users by following web accessibility standards. Use semantic HTML, provide proper labels, and test your application with screen readers.</p>
<p>Example:</p>
<pre><code>// Using semantic HTML for better accessibility
&lt;button aria-label="Close" onClick={handleClose}&gt;
&lt;span aria-hidden="true"&gt;×&lt;/span&gt;
&lt;/button&gt;</code></pre>
<h1>Conclusion</h1>
<p>Building high-quality React applications involves a combination of best practices and design patterns. By adhering to these guidelines, you’ll not only enhance the maintainability and scalability of your project but also contribute to a positive development experience for your team. Keep evolving with the React ecosystem, stay updated on new features, and continuously refine your approach to ensure your applications remain at the forefront of web development.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240102.jpg.pagespeed.ic.FgIoKmYqkM.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240102.jpg.pagespeed.ic.FgIoKmYqkM.jpg" medium="image"/>
    <pubDate>Tue, 02 Jan 2024 11:41:35 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1061期：开发者路线图：《developer-roadmap》]]></title>
    <link>https://openingsource.org/11460/</link>
    <itunes:title><![CDATA[开源日报第1061期：开发者路线图：《developer-roadmap》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月1日，开源日报第1061期：<br>
今日推荐开源项目：《developer-roadmap》<br>
今日推荐英文原文：《Building a Simple React Hook to Detect Screen Size》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240101.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《developer-roadmap》传送门：<a href="https://github.com/kamranahmedse/developer-roadmap" target="_blank">项目链接</a> </p>
<p>推荐理由：非常全面的 developer 路线图、实战指南和深度技术解析，帮助开发者全面成长。直达链接 roadmap.sh</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@josephat94/building-a-simple-react-hook-to-detect-screen-size-404a867fa2d2" target="_blank">Building a Simple React Hook to Detect Screen Size</a></p>
<p>推荐理由：本文介绍如何使用React创建一个简单的屏幕尺寸检测的自定义Hook, 通过监听窗口大小变化，实时更新屏幕尺寸，适合练手</p>
<hr>
<h1>Building a Simple React Hook to Detect Screen Size</h1>
<p>Hey folks! 🌟 In this super cool article, we’ll dive into the world of responsive web design using React. We’ll learn how to create a simple React hook that detects screen size changes. <em>Exciting, right? Let’s get started and rock that responsive game! 🚀</em></p>
<h1>Prerequisites</h1>
<p>Before we begin, make sure you’re familiar with React and React hooks. Some basic JavaScript and JSX knowledge would be helpful too. If you’re already on board, let’s rock ’n’ roll! 🎸</p>
<h1>Setting Up the Project</h1>
<p>To start things off, we need a React project. No worries, just create a new React project using Create React App or any other fancy method:</p>
<pre><code>npx create-react-app screen-size-hook-demo
cd screen-size-hook-demo</code></pre>
<h1>Creating the Custom Hook ⚓️</h1>
<p>Alright, we’re all set! Now, let’s dive into creating our own custom hook for detecting screen size changes. In the <code>src</code> folder, let's create a new file called <code>useScreenSize.js</code>. This is where the magic happens! ✨</p>
<pre><code>// useScreenSize.js
import { useState, useEffect } from 'react';
const useScreenSize = () =&gt; {
const [screenSize, setScreenSize] = useState({
width: window.innerWidth,
height: window.innerHeight,
});
useEffect(() =&gt; {
const handleResize = () =&gt; {
setScreenSize({
width: window.innerWidth,
height: window.innerHeight,
});
};
window.addEventListener('resize', handleResize);
// Clean up the event listener when the component unmounts
return () =&gt; {
window.removeEventListener('resize', handleResize);
};
}, []);
return screenSize;
};
export default useScreenSize;</code></pre>
<h1>Custom Hook Magic ✨</h1>
<p>Hey, look at this fancy code! 😎 Let’s break it down real quick:</p>
<ol>
<li>We import the <code>useState</code> and <code>useEffect</code> hooks from React. You know, React's got some sweet hooks! 🎣</li>
<li>We create our custom hook called <code>useScreenSize</code>, which sets up the initial state for <code>screenSize</code> with the current screen dimensions.</li>
<li>Inside the <code>useEffect</code> hook, we add an event listener to the window object to detect screen resize events.</li>
<li>Whenever the window size changes, the <code>handleResize</code> function kicks in, updating the <code>screenSize</code> state with the new dimensions.</li>
<li>We’re responsible developers, so we remove the event listener in the cleanup function to avoid any unwanted mess. 🧹</li>
</ol>
<h1>Using the Custom Hook</h1>
<p>Alright, we’ve built the hook, but how do we use it? Don’t worry; it’s a breeze! Just take a look at this <code>App.js</code> file:</p>
<pre><code>// App.js
import React from 'react';
import useScreenSize from './useScreenSize';
const App = () =&gt; {
const screenSize = useScreenSize();
return (
&lt;div&gt;
&lt;h1&gt;Screen Size Detection with React Hook&lt;/h1&gt;
&lt;p&gt;Width: {screenSize.width}&lt;/p&gt;
&lt;p&gt;Height: {screenSize.height}&lt;/p&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<h1>Usage Made Easy-Peasy! 🌈</h1>
<p>Super simple, right? All we did was import the <code>useScreenSize</code> hook we created and called it within the <code>App</code> component. The <code>screenSize</code> object contains the current <code>width</code> and <code>height</code> of the screen, and we proudly display them on the page! 🎉</p>
<h1>Conclusion</h1>
<p>There you have it, amigos!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月1日，开源日报第1061期：<br>
今日推荐开源项目：《developer-roadmap》<br>
今日推荐英文原文：《Building a Simple React Hook to Detect Screen Size》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240101.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《developer-roadmap》传送门：<a href="https://github.com/kamranahmedse/developer-roadmap" target="_blank">项目链接</a> </p>
<p>推荐理由：非常全面的 developer 路线图、实战指南和深度技术解析，帮助开发者全面成长。直达链接 roadmap.sh</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@josephat94/building-a-simple-react-hook-to-detect-screen-size-404a867fa2d2" target="_blank">Building a Simple React Hook to Detect Screen Size</a></p>
<p>推荐理由：本文介绍如何使用React创建一个简单的屏幕尺寸检测的自定义Hook, 通过监听窗口大小变化，实时更新屏幕尺寸，适合练手</p>
<hr>
<h1>Building a Simple React Hook to Detect Screen Size</h1>
<p>Hey folks! 🌟 In this super cool article, we’ll dive into the world of responsive web design using React. We’ll learn how to create a simple React hook that detects screen size changes. <em>Exciting, right? Let’s get started and rock that responsive game! 🚀</em></p>
<h1>Prerequisites</h1>
<p>Before we begin, make sure you’re familiar with React and React hooks. Some basic JavaScript and JSX knowledge would be helpful too. If you’re already on board, let’s rock ’n’ roll! 🎸</p>
<h1>Setting Up the Project</h1>
<p>To start things off, we need a React project. No worries, just create a new React project using Create React App or any other fancy method:</p>
<pre><code>npx create-react-app screen-size-hook-demo
cd screen-size-hook-demo</code></pre>
<h1>Creating the Custom Hook ⚓️</h1>
<p>Alright, we’re all set! Now, let’s dive into creating our own custom hook for detecting screen size changes. In the <code>src</code> folder, let's create a new file called <code>useScreenSize.js</code>. This is where the magic happens! ✨</p>
<pre><code>// useScreenSize.js
import { useState, useEffect } from 'react';
const useScreenSize = () =&gt; {
const [screenSize, setScreenSize] = useState({
width: window.innerWidth,
height: window.innerHeight,
});
useEffect(() =&gt; {
const handleResize = () =&gt; {
setScreenSize({
width: window.innerWidth,
height: window.innerHeight,
});
};
window.addEventListener('resize', handleResize);
// Clean up the event listener when the component unmounts
return () =&gt; {
window.removeEventListener('resize', handleResize);
};
}, []);
return screenSize;
};
export default useScreenSize;</code></pre>
<h1>Custom Hook Magic ✨</h1>
<p>Hey, look at this fancy code! 😎 Let’s break it down real quick:</p>
<ol>
<li>We import the <code>useState</code> and <code>useEffect</code> hooks from React. You know, React's got some sweet hooks! 🎣</li>
<li>We create our custom hook called <code>useScreenSize</code>, which sets up the initial state for <code>screenSize</code> with the current screen dimensions.</li>
<li>Inside the <code>useEffect</code> hook, we add an event listener to the window object to detect screen resize events.</li>
<li>Whenever the window size changes, the <code>handleResize</code> function kicks in, updating the <code>screenSize</code> state with the new dimensions.</li>
<li>We’re responsible developers, so we remove the event listener in the cleanup function to avoid any unwanted mess. 🧹</li>
</ol>
<h1>Using the Custom Hook</h1>
<p>Alright, we’ve built the hook, but how do we use it? Don’t worry; it’s a breeze! Just take a look at this <code>App.js</code> file:</p>
<pre><code>// App.js
import React from 'react';
import useScreenSize from './useScreenSize';
const App = () =&gt; {
const screenSize = useScreenSize();
return (
&lt;div&gt;
&lt;h1&gt;Screen Size Detection with React Hook&lt;/h1&gt;
&lt;p&gt;Width: {screenSize.width}&lt;/p&gt;
&lt;p&gt;Height: {screenSize.height}&lt;/p&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<h1>Usage Made Easy-Peasy! 🌈</h1>
<p>Super simple, right? All we did was import the <code>useScreenSize</code> hook we created and called it within the <code>App</code> component. The <code>screenSize</code> object contains the current <code>width</code> and <code>height</code> of the screen, and we proudly display them on the page! 🎉</p>
<h1>Conclusion</h1>
<p>There you have it, amigos!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月1日，开源日报第1061期：<br>
今日推荐开源项目：《developer-roadmap》<br>
今日推荐英文原文：《Building a Simple React Hook to Detect Screen Size》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240101.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《developer-roadmap》传送门：<a href="https://github.com/kamranahmedse/developer-roadmap" target="_blank">项目链接</a> </p>
<p>推荐理由：非常全面的 developer 路线图、实战指南和深度技术解析，帮助开发者全面成长。直达链接 roadmap.sh</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@josephat94/building-a-simple-react-hook-to-detect-screen-size-404a867fa2d2" target="_blank">Building a Simple React Hook to Detect Screen Size</a></p>
<p>推荐理由：本文介绍如何使用React创建一个简单的屏幕尺寸检测的自定义Hook, 通过监听窗口大小变化，实时更新屏幕尺寸，适合练手</p>
<hr>
<h1>Building a Simple React Hook to Detect Screen Size</h1>
<p>Hey folks! 🌟 In this super cool article, we’ll dive into the world of responsive web design using React. We’ll learn how to create a simple React hook that detects screen size changes. <em>Exciting, right? Let’s get started and rock that responsive game! 🚀</em></p>
<h1>Prerequisites</h1>
<p>Before we begin, make sure you’re familiar with React and React hooks. Some basic JavaScript and JSX knowledge would be helpful too. If you’re already on board, let’s rock ’n’ roll! 🎸</p>
<h1>Setting Up the Project</h1>
<p>To start things off, we need a React project. No worries, just create a new React project using Create React App or any other fancy method:</p>
<pre><code>npx create-react-app screen-size-hook-demo
cd screen-size-hook-demo</code></pre>
<h1>Creating the Custom Hook ⚓️</h1>
<p>Alright, we’re all set! Now, let’s dive into creating our own custom hook for detecting screen size changes. In the <code>src</code> folder, let's create a new file called <code>useScreenSize.js</code>. This is where the magic happens! ✨</p>
<pre><code>// useScreenSize.js
import { useState, useEffect } from 'react';
const useScreenSize = () =&gt; {
const [screenSize, setScreenSize] = useState({
width: window.innerWidth,
height: window.innerHeight,
});
useEffect(() =&gt; {
const handleResize = () =&gt; {
setScreenSize({
width: window.innerWidth,
height: window.innerHeight,
});
};
window.addEventListener('resize', handleResize);
// Clean up the event listener when the component unmounts
return () =&gt; {
window.removeEventListener('resize', handleResize);
};
}, []);
return screenSize;
};
export default useScreenSize;</code></pre>
<h1>Custom Hook Magic ✨</h1>
<p>Hey, look at this fancy code! 😎 Let’s break it down real quick:</p>
<ol>
<li>We import the <code>useState</code> and <code>useEffect</code> hooks from React. You know, React's got some sweet hooks! 🎣</li>
<li>We create our custom hook called <code>useScreenSize</code>, which sets up the initial state for <code>screenSize</code> with the current screen dimensions.</li>
<li>Inside the <code>useEffect</code> hook, we add an event listener to the window object to detect screen resize events.</li>
<li>Whenever the window size changes, the <code>handleResize</code> function kicks in, updating the <code>screenSize</code> state with the new dimensions.</li>
<li>We’re responsible developers, so we remove the event listener in the cleanup function to avoid any unwanted mess. 🧹</li>
</ol>
<h1>Using the Custom Hook</h1>
<p>Alright, we’ve built the hook, but how do we use it? Don’t worry; it’s a breeze! Just take a look at this <code>App.js</code> file:</p>
<pre><code>// App.js
import React from 'react';
import useScreenSize from './useScreenSize';
const App = () =&gt; {
const screenSize = useScreenSize();
return (
&lt;div&gt;
&lt;h1&gt;Screen Size Detection with React Hook&lt;/h1&gt;
&lt;p&gt;Width: {screenSize.width}&lt;/p&gt;
&lt;p&gt;Height: {screenSize.height}&lt;/p&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<h1>Usage Made Easy-Peasy! 🌈</h1>
<p>Super simple, right? All we did was import the <code>useScreenSize</code> hook we created and called it within the <code>App</code> component. The <code>screenSize</code> object contains the current <code>width</code> and <code>height</code> of the screen, and we proudly display them on the page! 🎉</p>
<h1>Conclusion</h1>
<p>There you have it, amigos!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240101.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240101.jpg" medium="image"/>
    <pubDate>Mon, 01 Jan 2024 15:53:39 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1060期：优秀论文合集：《papers-we-love》]]></title>
    <link>https://openingsource.org/11454/</link>
    <itunes:title><![CDATA[开源日报第1060期：优秀论文合集：《papers-we-love》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月31日，开源日报第1060期：<br>
今日推荐开源项目：《papers-we-love》<br>
今日推荐英文原文：《React Component Architecture for Job Interviews》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20231231.jpg.pagespeed.ic.1pMe97jlpg.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《papers-we-love》传送门：<a href="https://github.com/papers-we-love/papers-we-love?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： "Papers We Love"（PWL）是一个聚焦学术计算机科学论文的社区，通过阅读、讨论和学习推广优秀论文, 也可以在Discord讨论PWL相关内容</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@yuribett/react-component-architecture-for-job-interviews-c6ad2e3ed5fc" target="_blank">React Component Architecture for Job Interviews</a></p>
<p>推荐理由：本文介绍了在React面试中关于组件架构的重要概念和技巧，涵盖组件架构的基本理念，包括文件结构、Atomic Design、容器组件与展示组件的区分等等，还介绍了在构建大型React应用时如何通过高阶组件（HOC）和高阶函数（HOF）来实现代码重用和可扩展性，最后有自定义Hooks来实现可重用逻辑的方法的例子，比较实用</p>
<hr>
<h1>React Component Architecture for Job Interviews</h1>
<p>Are you preparing for a React job interview and want to get right the questions on component architecture? In this post, we’ll cover essential concepts and tips related to component architecture in React, along with some examples. By the end, you’ll be well-equipped to tackle component-related questions with confidence. Let’s dive in!</p>
<h1>Understanding Component Architecture</h1>
<p>Component architecture is a fundamental concept in React development. It involves breaking down your React application into smaller, self-contained building blocks called components. Each component handles a specific functionality or represents a UI element. This approach improves code organization, making the project more scalable and maintainable.</p>
<h2>Tips on Organizing Components</h2>
<p><strong>File Structure</strong>: A clean file structure is essential for managing large React projects. Group related components into folders and use TypeScript <code>.tsx</code> files and <code>.scss</code> for styles. For instance:</p>
<pre><code>src/
components/
Header/
Header.tsx
Header.scss
Sidebar/
Sidebar.tsx
Sidebar.scss
pages/
Home/
Home.tsx
Home.scss
About/
About.tsx
About.scss
App.tsx
index.tsx</code></pre>
<p>This is just one of many possibilities we can have for file structure. Frameworks like Next.js already provide their own proposal of the file structure, especially for the pages. it's worth taking a look.</p>
<p><strong>Atomic Design</strong>: Familiarize yourself with Atomic Design principles to organize components based on complexity and reusability. Start with atoms (e.g., buttons, inputs), then build molecules (e.g., forms), organisms (e.g., complete headers), and finally templates and pages.</p>
<p><strong>Container vs. Presentational Components</strong>: Understand the distinction between container and presentational components. Container components handle logic and data manipulation, while presentational components focus solely on visual representation. This promotes code reuse and easier testing.</p>
<h1>Scaling Up with Higher-Order Components (HOC) and Higher-Order Functions (HOF)</h1>
<p>When building larger React applications, you’ll often encounter the need for code reuse and scalability. Here’s how Higher-Order Components (HOCs) and Higher-Order Functions (HOFs) come to the rescue.</p>
<h2>Higher-Order Components (HOC)</h2>
<p>HOCs are functions that take a component as input and return an enhanced version of that component. They are widely used for cross-cutting concerns such as authentication or authorization. Let’s see an example:</p>
<pre><code>import React from 'react';
interface WithAuthProps {
isAuthenticated: boolean;
}
const withAuth = &lt;P extends object&gt;(
WrappedComponent: React.ComponentType&lt;P &amp; WithAuthProps&gt;
) =&gt; {
return class extends React.Component&lt;P &amp; WithAuthProps&gt; {
render() {
if (this.props.isAuthenticated) {
return &lt;WrappedComponent {...this.props as P} /&gt;;
} else {
return &lt;p&gt;Please login to access this content.&lt;/p&gt;;
}
}
};
};
interface MyComponentProps {
// Add your component's props here
}
const MyComponent: React.FC&lt;MyComponentProps&gt; = (props) =&gt; {
// Your component logic here
};
const AuthenticatedComponent = withAuth(MyComponent);</code></pre>
<p>Now, <code>AuthenticatedComponent</code> will render <code>MyComponent</code> only if the user is authenticated; otherwise, it displays a message asking the user to log in.</p>
<h2>Higher-Order Functions (HOF)</h2>
<p>HOFs, on the other hand, are functions that take a function as input and return a new function with extended functionality. They shine when you need to apply similar transformations or filters to different datasets.</p>
<pre><code>const withFilter = &lt;T extends unknown&gt;(
filterFunction: (data: T) =&gt; boolean
) =&gt; (data: T[]) =&gt; {
return data.filter(filterFunction);
};
const numbers = [1, 2, 3, 4, 5];
const evenNumbersFilter = (num: number) =&gt; num % 2 === 0;
const getEvenNumbers = withFilter&lt;number&gt;(evenNumbersFilter);
const evenNumbers = getEvenNumbers(numbers);
// Output: [2, 4]</code></pre>
<p>Both HOCs and HOFs are essential concepts to grasp for writing scalable and maintainable React applications.</p>
<h1>Reusable Logic with Custom Hooks</h1>
<p>Custom Hooks offer an elegant solution for organizing and reusing component logic. They enable you to extract logic into reusable functions and improve code readability.</p>
<pre><code>import React, { useState } from 'react';
const useFormInput = (initialValue: string) =&gt; {
const [value, setValue] = useState(initialValue);
const handleChange = (e: React.ChangeEvent&lt;HTMLInputElement&gt;) =&gt; {
setValue(e.target.value);
};
return {
value,
onChange: handleChange,
};
};
const MyFormComponent: React.FC = () =&gt; {
const firstName = useFormInput('');
const lastName = useFormInput('');
return (
&lt;form&gt;
&lt;input type="text" {...firstName} /&gt;
&lt;input type="text" {...lastName} /&gt;
&lt;/form&gt;
);
};</code></pre>
<p>By using this custom Hook, you can easily add form handling to any component without duplicating the logic.</p>
<h1>Conclusion</h1>
<p>Congratulations! You’ve now learned the ins and outs of React component architecture and how to apply them to create scalable and maintainable code. Armed with the knowledge of Higher-Order Components (HOCs), Higher-Order Functions (HOFs), and Custom Hooks, you’re well-prepared for React job interviews.</p>
<p>Remember to practice implementing these concepts in real projects, as hands-on experience is the best way to solidify your understanding. Best of luck in your React job interviews, and happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月31日，开源日报第1060期：<br>
今日推荐开源项目：《papers-we-love》<br>
今日推荐英文原文：《React Component Architecture for Job Interviews》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20231231.jpg.pagespeed.ic.1pMe97jlpg.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《papers-we-love》传送门：<a href="https://github.com/papers-we-love/papers-we-love?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： "Papers We Love"（PWL）是一个聚焦学术计算机科学论文的社区，通过阅读、讨论和学习推广优秀论文, 也可以在Discord讨论PWL相关内容</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@yuribett/react-component-architecture-for-job-interviews-c6ad2e3ed5fc" target="_blank">React Component Architecture for Job Interviews</a></p>
<p>推荐理由：本文介绍了在React面试中关于组件架构的重要概念和技巧，涵盖组件架构的基本理念，包括文件结构、Atomic Design、容器组件与展示组件的区分等等，还介绍了在构建大型React应用时如何通过高阶组件（HOC）和高阶函数（HOF）来实现代码重用和可扩展性，最后有自定义Hooks来实现可重用逻辑的方法的例子，比较实用</p>
<hr>
<h1>React Component Architecture for Job Interviews</h1>
<p>Are you preparing for a React job interview and want to get right the questions on component architecture? In this post, we’ll cover essential concepts and tips related to component architecture in React, along with some examples. By the end, you’ll be well-equipped to tackle component-related questions with confidence. Let’s dive in!</p>
<h1>Understanding Component Architecture</h1>
<p>Component architecture is a fundamental concept in React development. It involves breaking down your React application into smaller, self-contained building blocks called components. Each component handles a specific functionality or represents a UI element. This approach improves code organization, making the project more scalable and maintainable.</p>
<h2>Tips on Organizing Components</h2>
<p><strong>File Structure</strong>: A clean file structure is essential for managing large React projects. Group related components into folders and use TypeScript <code>.tsx</code> files and <code>.scss</code> for styles. For instance:</p>
<pre><code>src/
components/
Header/
Header.tsx
Header.scss
Sidebar/
Sidebar.tsx
Sidebar.scss
pages/
Home/
Home.tsx
Home.scss
About/
About.tsx
About.scss
App.tsx
index.tsx</code></pre>
<p>This is just one of many possibilities we can have for file structure. Frameworks like Next.js already provide their own proposal of the file structure, especially for the pages. it's worth taking a look.</p>
<p><strong>Atomic Design</strong>: Familiarize yourself with Atomic Design principles to organize components based on complexity and reusability. Start with atoms (e.g., buttons, inputs), then build molecules (e.g., forms), organisms (e.g., complete headers), and finally templates and pages.</p>
<p><strong>Container vs. Presentational Components</strong>: Understand the distinction between container and presentational components. Container components handle logic and data manipulation, while presentational components focus solely on visual representation. This promotes code reuse and easier testing.</p>
<h1>Scaling Up with Higher-Order Components (HOC) and Higher-Order Functions (HOF)</h1>
<p>When building larger React applications, you’ll often encounter the need for code reuse and scalability. Here’s how Higher-Order Components (HOCs) and Higher-Order Functions (HOFs) come to the rescue.</p>
<h2>Higher-Order Components (HOC)</h2>
<p>HOCs are functions that take a component as input and return an enhanced version of that component. They are widely used for cross-cutting concerns such as authentication or authorization. Let’s see an example:</p>
<pre><code>import React from 'react';
interface WithAuthProps {
isAuthenticated: boolean;
}
const withAuth = &lt;P extends object&gt;(
WrappedComponent: React.ComponentType&lt;P &amp; WithAuthProps&gt;
) =&gt; {
return class extends React.Component&lt;P &amp; WithAuthProps&gt; {
render() {
if (this.props.isAuthenticated) {
return &lt;WrappedComponent {...this.props as P} /&gt;;
} else {
return &lt;p&gt;Please login to access this content.&lt;/p&gt;;
}
}
};
};
interface MyComponentProps {
// Add your component's props here
}
const MyComponent: React.FC&lt;MyComponentProps&gt; = (props) =&gt; {
// Your component logic here
};
const AuthenticatedComponent = withAuth(MyComponent);</code></pre>
<p>Now, <code>AuthenticatedComponent</code> will render <code>MyComponent</code> only if the user is authenticated; otherwise, it displays a message asking the user to log in.</p>
<h2>Higher-Order Functions (HOF)</h2>
<p>HOFs, on the other hand, are functions that take a function as input and return a new function with extended functionality. They shine when you need to apply similar transformations or filters to different datasets.</p>
<pre><code>const withFilter = &lt;T extends unknown&gt;(
filterFunction: (data: T) =&gt; boolean
) =&gt; (data: T[]) =&gt; {
return data.filter(filterFunction);
};
const numbers = [1, 2, 3, 4, 5];
const evenNumbersFilter = (num: number) =&gt; num % 2 === 0;
const getEvenNumbers = withFilter&lt;number&gt;(evenNumbersFilter);
const evenNumbers = getEvenNumbers(numbers);
// Output: [2, 4]</code></pre>
<p>Both HOCs and HOFs are essential concepts to grasp for writing scalable and maintainable React applications.</p>
<h1>Reusable Logic with Custom Hooks</h1>
<p>Custom Hooks offer an elegant solution for organizing and reusing component logic. They enable you to extract logic into reusable functions and improve code readability.</p>
<pre><code>import React, { useState } from 'react';
const useFormInput = (initialValue: string) =&gt; {
const [value, setValue] = useState(initialValue);
const handleChange = (e: React.ChangeEvent&lt;HTMLInputElement&gt;) =&gt; {
setValue(e.target.value);
};
return {
value,
onChange: handleChange,
};
};
const MyFormComponent: React.FC = () =&gt; {
const firstName = useFormInput('');
const lastName = useFormInput('');
return (
&lt;form&gt;
&lt;input type="text" {...firstName} /&gt;
&lt;input type="text" {...lastName} /&gt;
&lt;/form&gt;
);
};</code></pre>
<p>By using this custom Hook, you can easily add form handling to any component without duplicating the logic.</p>
<h1>Conclusion</h1>
<p>Congratulations! You’ve now learned the ins and outs of React component architecture and how to apply them to create scalable and maintainable code. Armed with the knowledge of Higher-Order Components (HOCs), Higher-Order Functions (HOFs), and Custom Hooks, you’re well-prepared for React job interviews.</p>
<p>Remember to practice implementing these concepts in real projects, as hands-on experience is the best way to solidify your understanding. Best of luck in your React job interviews, and happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月31日，开源日报第1060期：<br>
今日推荐开源项目：《papers-we-love》<br>
今日推荐英文原文：《React Component Architecture for Job Interviews》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20231231.jpg.pagespeed.ic.1pMe97jlpg.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《papers-we-love》传送门：<a href="https://github.com/papers-we-love/papers-we-love?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： "Papers We Love"（PWL）是一个聚焦学术计算机科学论文的社区，通过阅读、讨论和学习推广优秀论文, 也可以在Discord讨论PWL相关内容</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@yuribett/react-component-architecture-for-job-interviews-c6ad2e3ed5fc" target="_blank">React Component Architecture for Job Interviews</a></p>
<p>推荐理由：本文介绍了在React面试中关于组件架构的重要概念和技巧，涵盖组件架构的基本理念，包括文件结构、Atomic Design、容器组件与展示组件的区分等等，还介绍了在构建大型React应用时如何通过高阶组件（HOC）和高阶函数（HOF）来实现代码重用和可扩展性，最后有自定义Hooks来实现可重用逻辑的方法的例子，比较实用</p>
<hr>
<h1>React Component Architecture for Job Interviews</h1>
<p>Are you preparing for a React job interview and want to get right the questions on component architecture? In this post, we’ll cover essential concepts and tips related to component architecture in React, along with some examples. By the end, you’ll be well-equipped to tackle component-related questions with confidence. Let’s dive in!</p>
<h1>Understanding Component Architecture</h1>
<p>Component architecture is a fundamental concept in React development. It involves breaking down your React application into smaller, self-contained building blocks called components. Each component handles a specific functionality or represents a UI element. This approach improves code organization, making the project more scalable and maintainable.</p>
<h2>Tips on Organizing Components</h2>
<p><strong>File Structure</strong>: A clean file structure is essential for managing large React projects. Group related components into folders and use TypeScript <code>.tsx</code> files and <code>.scss</code> for styles. For instance:</p>
<pre><code>src/
components/
Header/
Header.tsx
Header.scss
Sidebar/
Sidebar.tsx
Sidebar.scss
pages/
Home/
Home.tsx
Home.scss
About/
About.tsx
About.scss
App.tsx
index.tsx</code></pre>
<p>This is just one of many possibilities we can have for file structure. Frameworks like Next.js already provide their own proposal of the file structure, especially for the pages. it's worth taking a look.</p>
<p><strong>Atomic Design</strong>: Familiarize yourself with Atomic Design principles to organize components based on complexity and reusability. Start with atoms (e.g., buttons, inputs), then build molecules (e.g., forms), organisms (e.g., complete headers), and finally templates and pages.</p>
<p><strong>Container vs. Presentational Components</strong>: Understand the distinction between container and presentational components. Container components handle logic and data manipulation, while presentational components focus solely on visual representation. This promotes code reuse and easier testing.</p>
<h1>Scaling Up with Higher-Order Components (HOC) and Higher-Order Functions (HOF)</h1>
<p>When building larger React applications, you’ll often encounter the need for code reuse and scalability. Here’s how Higher-Order Components (HOCs) and Higher-Order Functions (HOFs) come to the rescue.</p>
<h2>Higher-Order Components (HOC)</h2>
<p>HOCs are functions that take a component as input and return an enhanced version of that component. They are widely used for cross-cutting concerns such as authentication or authorization. Let’s see an example:</p>
<pre><code>import React from 'react';
interface WithAuthProps {
isAuthenticated: boolean;
}
const withAuth = &lt;P extends object&gt;(
WrappedComponent: React.ComponentType&lt;P &amp; WithAuthProps&gt;
) =&gt; {
return class extends React.Component&lt;P &amp; WithAuthProps&gt; {
render() {
if (this.props.isAuthenticated) {
return &lt;WrappedComponent {...this.props as P} /&gt;;
} else {
return &lt;p&gt;Please login to access this content.&lt;/p&gt;;
}
}
};
};
interface MyComponentProps {
// Add your component's props here
}
const MyComponent: React.FC&lt;MyComponentProps&gt; = (props) =&gt; {
// Your component logic here
};
const AuthenticatedComponent = withAuth(MyComponent);</code></pre>
<p>Now, <code>AuthenticatedComponent</code> will render <code>MyComponent</code> only if the user is authenticated; otherwise, it displays a message asking the user to log in.</p>
<h2>Higher-Order Functions (HOF)</h2>
<p>HOFs, on the other hand, are functions that take a function as input and return a new function with extended functionality. They shine when you need to apply similar transformations or filters to different datasets.</p>
<pre><code>const withFilter = &lt;T extends unknown&gt;(
filterFunction: (data: T) =&gt; boolean
) =&gt; (data: T[]) =&gt; {
return data.filter(filterFunction);
};
const numbers = [1, 2, 3, 4, 5];
const evenNumbersFilter = (num: number) =&gt; num % 2 === 0;
const getEvenNumbers = withFilter&lt;number&gt;(evenNumbersFilter);
const evenNumbers = getEvenNumbers(numbers);
// Output: [2, 4]</code></pre>
<p>Both HOCs and HOFs are essential concepts to grasp for writing scalable and maintainable React applications.</p>
<h1>Reusable Logic with Custom Hooks</h1>
<p>Custom Hooks offer an elegant solution for organizing and reusing component logic. They enable you to extract logic into reusable functions and improve code readability.</p>
<pre><code>import React, { useState } from 'react';
const useFormInput = (initialValue: string) =&gt; {
const [value, setValue] = useState(initialValue);
const handleChange = (e: React.ChangeEvent&lt;HTMLInputElement&gt;) =&gt; {
setValue(e.target.value);
};
return {
value,
onChange: handleChange,
};
};
const MyFormComponent: React.FC = () =&gt; {
const firstName = useFormInput('');
const lastName = useFormInput('');
return (
&lt;form&gt;
&lt;input type="text" {...firstName} /&gt;
&lt;input type="text" {...lastName} /&gt;
&lt;/form&gt;
);
};</code></pre>
<p>By using this custom Hook, you can easily add form handling to any component without duplicating the logic.</p>
<h1>Conclusion</h1>
<p>Congratulations! You’ve now learned the ins and outs of React component architecture and how to apply them to create scalable and maintainable code. Armed with the knowledge of Higher-Order Components (HOCs), Higher-Order Functions (HOFs), and Custom Hooks, you’re well-prepared for React job interviews.</p>
<p>Remember to practice implementing these concepts in real projects, as hands-on experience is the best way to solidify your understanding. Best of luck in your React job interviews, and happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20231231.jpg.pagespeed.ic.1pMe97jlpg.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20231231.jpg.pagespeed.ic.1pMe97jlpg.jpg" medium="image"/>
    <pubDate>Sun, 31 Dec 2023 13:45:48 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1059期：部署自己的 Spotify :《Spotube》]]></title>
    <link>https://openingsource.org/11450/</link>
    <itunes:title><![CDATA[开源日报第1059期：部署自己的 Spotify :《Spotube》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月30日，开源日报第1059期：<br>
今日推荐开源项目：《Spotube》<br>
今日推荐英文原文：《Can CppRef<t> be ergonomic?》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/osd-20231230.jpg"></t></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Spotube》传送门：<a href="https://github.com/KRTirtho/spotube?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： 该项目是一个开源的Spotify客户端，旨在跨多个平台兼容，而无需依赖Spotify Premium，它利用Spotify的数据API和其他音频来源，如YouTube、Piped.video或JioSaavn</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@adetaylor/can-cppref-t-be-ergonomic-c9cb1365bda1" target="_blank">Can CppRef<t> be ergonomic?</t></a></p>
<p>推荐理由：该文章主要在讲CppRef<t>的Rust类型，用于处理C++类型的引用, 引入了CppPin<t>确保Rust中对对象的所有权，同时允许在C++中引用。作者也提到字段访问需通过函数或宏，方法调用涉及泛型self类型的支持</t></t></p>
<hr>
<h1>Can CppRef<t> be ergonomic?</t></h1>
<p><a href="https://medium.com/@adetaylor/are-we-reference-yet-c-references-in-rust-72c1c6c7015a" target="_blank">In a previous post</a>, I said that we simply can’t use Rust references to point to C++ types. This might work at small scale, but for any sizable C++ project, humans can’t promise that there are no other C++ references to the same data — so you run into aliasing violations, unexpected mutations, and the dreaded Undefined Behavior.</p>
<p>So, instead of using <code>&amp;T</code> we’ll create a <code>CppRef&lt;T&gt;</code> to create a C++ reference (or pointer). There’s some early work <a href="https://docs.rs/autocxx/latest/autocxx/macro.safety.html" target="_blank">here</a> — see <code>unsafe_references_wrapped</code> and the linked type, <a href="https://github.com/google/autocxx/blob/main/examples/reference-wrappers/src/main.rs" target="_blank">plus this example</a>). This ideally relies on a Rust feature called “arbitrary self types” which I’m working on <a href="https://github.com/rust-lang/rfcs/pull/3519/files" target="_blank">here in an RFC</a> along with some very fine other people (thanks!)</p>
<p>So far so good. But, one of the open questions has been — can <code>CppRef&lt;T&gt;</code> be ergonomic? And one specific question has come up during the RFC — is it desirable to support generic receivers? For example, is this monstrosity a good or bad idea?</p>
<pre><code>impl SomeType {
fn some_method(self: impl SomeTrait &lt; Target = Self &gt;) { ... }
}</code></pre>
<p>It turns out that these questions are related.</p>
<p>First, let’s talk about <code>CppPin&lt;T&gt;</code> . If you have some data which may have C++ pointers or references to it, it’s simply <em>not OK</em> to have a Rust reference.</p>
<p>There are some circumstances where the object will be stored over in C++ and all you would ever have in Rust is <code>CppRef</code> s to it:</p>
<ul>
<li>It’s stored in something like a <code>cxx::UniquePtr</code></li>
<li>A C++ method has returned you a reference to something stored over in C++ land entirely.</li>
</ul>
<p>But, you might sometimes want to own objects in Rust and yet make them available to C++. These might be C++ types or they might be Rust types. In such a case, you need a way to ensure there are only C++ references but no Rust references. That’s what <code>CppPin&lt;T&gt;</code> is for.</p>
<p><code>CppPin::new(something)</code> consumes the <code>something</code> , thus proving there are no existing Rust references. It can create new C++ references — <code>CppPin::as_cpp_ref() -&gt; CppRef&lt;T&gt;</code> — but there’s no way to get a <code>&amp;T</code> or <code>&amp;mut T</code> . You can safely do weird things to this type in C++, including storing references or pointers to it which you later manipulate, and there are guaranteed to be no Rust references which you discombobulate.</p>
<p><code>CppPin</code> might seem a strange name in that it’s not exactly about preventing things moving — but it shares lots of the same properties as the regular <code>Pin</code> including an inability to vend references, complexity about “pin projections”, and a general level of annoyingness. <code>CppJail</code> or <code>CppBubble</code> might be better names — opinions welcomed.</p>
<p>Overall, though, I think <code>CppPin&lt;T&gt;</code> is necessary and fairly straightforward.</p>
<p>What about field access? We can’t have <code>&amp;T</code> so we can’t have <code>some_reference.some_field</code> . So, all field access needs to be either via function calls over into C++, or via macros based around <code>addr_of</code> and <code>read</code> (which would be in a function call itself).</p>
<p>So,</p>
<pre><code>// entirely auto-generated code from bindings generator
struct SomeCppType {
// my_field: usize, // not actually represented
}
impl SomeCppType {
fn get_my_field(self: CppRef&lt;Self&gt;) -&gt; usize { ... }
fn set_my_field(self: CppRef&lt;Self&gt;, val: usize) { ... }
fn get_my_field_ref(self: CppRef&lt;Self&gt;) -&gt; CppRef&lt;usize&gt; { ... }
}</code></pre>
<p>I wanted to find out if this could be made slightly more ergonomic using a macro like <code>field!(some_value, field_name)</code> . This may depend upon stabilization of <code>concat_idents!</code> . I couldn’t get it to work, but ultimately I don’t think it’s a huge deal to need to call methods to get and set field values.</p>
<p>What about method calls?</p>
<p>The awesome thing about <code>CppRef&lt;T&gt;</code> is that it’s pretty much an opaque token. You’ll most commonly get a <code>CppRef&lt;T&gt;</code> from C++, and pass it back to C++, without any need to manipulate or touch the <code>CppRef&lt;T&gt;</code> at all. Most commonly, you’ll pass it back to C++ using as the <code>this</code> pointer in a method call:</p>
<pre><code>fn main() {
let vulture: CppRef&lt;Vulture&gt; = get_cpp_reference_to_vulture_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is what the “arbitrary self types” feature allows.</p>
<p>However, it would also be nice to call <code>squawk()</code> on a <code>CppPin&lt;Vulture&gt;:</code></p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is where the question of generic <code>self</code> types first comes in.</p>
<p>Which of these is better for our (auto-generated) <code>squawk</code> method signature?</p>
<pre><code>impl Vulture {
// This code would be auto generated
fn squawk(self: CppRef&lt;Self&gt;) {} // 1
fn squawk(self: impl AsCppRef&lt;Target=Self&gt;) {} // 2
}</code></pre>
<p>The second option seems appealing because we could implement <code>AsCppRef</code> even on <code>CppPin</code> . This <a href="https://github.com/adetaylor/cpppin-ergonomics-experiments/blob/main/src/main.rs" target="_blank">works</a>, but it turns out not to be especially ergonomic, because it consumes the <code>CppPin</code> each time. That is, you couldn’t do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk();
vulture.squawk();
}</code></pre>
<p>You would instead have to do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.as_cpp_ref().squawk();
vulture.squawk();
}</code></pre>
<p>which is very similar to the annoying <code>Pin::as_mut</code> method.</p>
<p>Overall, it seems better to pick option 1, and force people to call <code>as_cpp_ref()</code> each time they want to call a method on the contents of the <code>CppPin</code> . This doesn’t yet seem like a sufficiently good motivation for generic self types.</p>
<p>Finally — what about code that wants to be generic over the <em>type of reference?</em> That is, code which can handle a <code>&amp;Vulture</code> or a <code>CppRef&lt;Vulture&gt;</code> ? Is that even achievable?</p>
<p>Yes!</p>
<pre><code>impl Vulture {
/// This method can accept either &amp;Self or CppRef&lt;Self&gt;
/// because both of them impl a Ref trait
fn squawk(self: impl Ref&lt;Target = Self&gt;) -&gt; u32 {
// What to do here?
}
fn squawk_only_in_rust(&amp;self) {}
fn squawk_only_in_cpp(self: CppRef&lt;Self&gt;) {}
}</code></pre>
<p>One oddity here is that Rust method calls’ autoref functionality doesn’t work here, so if we want to call this method with a <code>&amp;Vulture</code> we need to say <code>(&amp;my_vulture_by_value).squawk()</code> . Here’s how we’d call this:</p>
<pre><code>let rust_accessible_vulture = Vulture(1);
let cpp_accessible_vulture = CppPin::new(Vulture(2));
(&amp;rust_accessible_vulture).squawk();
cpp_accessible_vulture.as_cpp_ref().squawk();</code></pre>
<p>But more importantly, what could <code>squawk</code> actually do here? The <code>impl Ref</code> is pretty useless — it’s no longer even a useful opaque token to pass back into C++. However, since both references can emit raw pointers, we can do field access. Even though <code>CppRef&lt;T&gt;</code> promises nothing about aliasing or mutability, it can still uphold C++ reference-like promises around alignment and not being null. So the <code>squawk</code> function here could, with suitable use of macros and autogenerated code, access fields within the <code>Vulture</code> and do useful work.</p>
<p>This <em>is</em> a good use for generic receivers.</p>
<p>So. Conclusions are:</p>
<ul>
<li><code>CppPin&lt;T&gt;</code> is necessary and I don’t think it sucks, though it is annoyingly like <code>Pin</code> in some ways.</li>
<li>Field access to <code>CppRef&lt;T&gt;</code> and/or <code>impl Ref&lt;T&gt;</code> are ugly and will probably need methods calls or macros, but this is OK since usually a <code>CppRef&lt;T&gt;</code> is just an opaque token which will be passed back to C++, and field access will be rare.</li>
<li>We probably <em>do</em> want to support generic self types, since sometimes people will want to write code that’s generic over <code>&amp;T</code> or <code>CppRef&lt;T&gt;</code> .<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ul>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月30日，开源日报第1059期：<br>
今日推荐开源项目：《Spotube》<br>
今日推荐英文原文：《Can CppRef<t> be ergonomic?》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/osd-20231230.jpg"></t></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Spotube》传送门：<a href="https://github.com/KRTirtho/spotube?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： 该项目是一个开源的Spotify客户端，旨在跨多个平台兼容，而无需依赖Spotify Premium，它利用Spotify的数据API和其他音频来源，如YouTube、Piped.video或JioSaavn</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@adetaylor/can-cppref-t-be-ergonomic-c9cb1365bda1" target="_blank">Can CppRef<t> be ergonomic?</t></a></p>
<p>推荐理由：该文章主要在讲CppRef<t>的Rust类型，用于处理C++类型的引用, 引入了CppPin<t>确保Rust中对对象的所有权，同时允许在C++中引用。作者也提到字段访问需通过函数或宏，方法调用涉及泛型self类型的支持</t></t></p>
<hr>
<h1>Can CppRef<t> be ergonomic?</t></h1>
<p><a href="https://medium.com/@adetaylor/are-we-reference-yet-c-references-in-rust-72c1c6c7015a" target="_blank">In a previous post</a>, I said that we simply can’t use Rust references to point to C++ types. This might work at small scale, but for any sizable C++ project, humans can’t promise that there are no other C++ references to the same data — so you run into aliasing violations, unexpected mutations, and the dreaded Undefined Behavior.</p>
<p>So, instead of using <code>&amp;T</code> we’ll create a <code>CppRef&lt;T&gt;</code> to create a C++ reference (or pointer). There’s some early work <a href="https://docs.rs/autocxx/latest/autocxx/macro.safety.html" target="_blank">here</a> — see <code>unsafe_references_wrapped</code> and the linked type, <a href="https://github.com/google/autocxx/blob/main/examples/reference-wrappers/src/main.rs" target="_blank">plus this example</a>). This ideally relies on a Rust feature called “arbitrary self types” which I’m working on <a href="https://github.com/rust-lang/rfcs/pull/3519/files" target="_blank">here in an RFC</a> along with some very fine other people (thanks!)</p>
<p>So far so good. But, one of the open questions has been — can <code>CppRef&lt;T&gt;</code> be ergonomic? And one specific question has come up during the RFC — is it desirable to support generic receivers? For example, is this monstrosity a good or bad idea?</p>
<pre><code>impl SomeType {
fn some_method(self: impl SomeTrait &lt; Target = Self &gt;) { ... }
}</code></pre>
<p>It turns out that these questions are related.</p>
<p>First, let’s talk about <code>CppPin&lt;T&gt;</code> . If you have some data which may have C++ pointers or references to it, it’s simply <em>not OK</em> to have a Rust reference.</p>
<p>There are some circumstances where the object will be stored over in C++ and all you would ever have in Rust is <code>CppRef</code> s to it:</p>
<ul>
<li>It’s stored in something like a <code>cxx::UniquePtr</code></li>
<li>A C++ method has returned you a reference to something stored over in C++ land entirely.</li>
</ul>
<p>But, you might sometimes want to own objects in Rust and yet make them available to C++. These might be C++ types or they might be Rust types. In such a case, you need a way to ensure there are only C++ references but no Rust references. That’s what <code>CppPin&lt;T&gt;</code> is for.</p>
<p><code>CppPin::new(something)</code> consumes the <code>something</code> , thus proving there are no existing Rust references. It can create new C++ references — <code>CppPin::as_cpp_ref() -&gt; CppRef&lt;T&gt;</code> — but there’s no way to get a <code>&amp;T</code> or <code>&amp;mut T</code> . You can safely do weird things to this type in C++, including storing references or pointers to it which you later manipulate, and there are guaranteed to be no Rust references which you discombobulate.</p>
<p><code>CppPin</code> might seem a strange name in that it’s not exactly about preventing things moving — but it shares lots of the same properties as the regular <code>Pin</code> including an inability to vend references, complexity about “pin projections”, and a general level of annoyingness. <code>CppJail</code> or <code>CppBubble</code> might be better names — opinions welcomed.</p>
<p>Overall, though, I think <code>CppPin&lt;T&gt;</code> is necessary and fairly straightforward.</p>
<p>What about field access? We can’t have <code>&amp;T</code> so we can’t have <code>some_reference.some_field</code> . So, all field access needs to be either via function calls over into C++, or via macros based around <code>addr_of</code> and <code>read</code> (which would be in a function call itself).</p>
<p>So,</p>
<pre><code>// entirely auto-generated code from bindings generator
struct SomeCppType {
// my_field: usize, // not actually represented
}
impl SomeCppType {
fn get_my_field(self: CppRef&lt;Self&gt;) -&gt; usize { ... }
fn set_my_field(self: CppRef&lt;Self&gt;, val: usize) { ... }
fn get_my_field_ref(self: CppRef&lt;Self&gt;) -&gt; CppRef&lt;usize&gt; { ... }
}</code></pre>
<p>I wanted to find out if this could be made slightly more ergonomic using a macro like <code>field!(some_value, field_name)</code> . This may depend upon stabilization of <code>concat_idents!</code> . I couldn’t get it to work, but ultimately I don’t think it’s a huge deal to need to call methods to get and set field values.</p>
<p>What about method calls?</p>
<p>The awesome thing about <code>CppRef&lt;T&gt;</code> is that it’s pretty much an opaque token. You’ll most commonly get a <code>CppRef&lt;T&gt;</code> from C++, and pass it back to C++, without any need to manipulate or touch the <code>CppRef&lt;T&gt;</code> at all. Most commonly, you’ll pass it back to C++ using as the <code>this</code> pointer in a method call:</p>
<pre><code>fn main() {
let vulture: CppRef&lt;Vulture&gt; = get_cpp_reference_to_vulture_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is what the “arbitrary self types” feature allows.</p>
<p>However, it would also be nice to call <code>squawk()</code> on a <code>CppPin&lt;Vulture&gt;:</code></p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is where the question of generic <code>self</code> types first comes in.</p>
<p>Which of these is better for our (auto-generated) <code>squawk</code> method signature?</p>
<pre><code>impl Vulture {
// This code would be auto generated
fn squawk(self: CppRef&lt;Self&gt;) {} // 1
fn squawk(self: impl AsCppRef&lt;Target=Self&gt;) {} // 2
}</code></pre>
<p>The second option seems appealing because we could implement <code>AsCppRef</code> even on <code>CppPin</code> . This <a href="https://github.com/adetaylor/cpppin-ergonomics-experiments/blob/main/src/main.rs" target="_blank">works</a>, but it turns out not to be especially ergonomic, because it consumes the <code>CppPin</code> each time. That is, you couldn’t do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk();
vulture.squawk();
}</code></pre>
<p>You would instead have to do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.as_cpp_ref().squawk();
vulture.squawk();
}</code></pre>
<p>which is very similar to the annoying <code>Pin::as_mut</code> method.</p>
<p>Overall, it seems better to pick option 1, and force people to call <code>as_cpp_ref()</code> each time they want to call a method on the contents of the <code>CppPin</code> . This doesn’t yet seem like a sufficiently good motivation for generic self types.</p>
<p>Finally — what about code that wants to be generic over the <em>type of reference?</em> That is, code which can handle a <code>&amp;Vulture</code> or a <code>CppRef&lt;Vulture&gt;</code> ? Is that even achievable?</p>
<p>Yes!</p>
<pre><code>impl Vulture {
/// This method can accept either &amp;Self or CppRef&lt;Self&gt;
/// because both of them impl a Ref trait
fn squawk(self: impl Ref&lt;Target = Self&gt;) -&gt; u32 {
// What to do here?
}
fn squawk_only_in_rust(&amp;self) {}
fn squawk_only_in_cpp(self: CppRef&lt;Self&gt;) {}
}</code></pre>
<p>One oddity here is that Rust method calls’ autoref functionality doesn’t work here, so if we want to call this method with a <code>&amp;Vulture</code> we need to say <code>(&amp;my_vulture_by_value).squawk()</code> . Here’s how we’d call this:</p>
<pre><code>let rust_accessible_vulture = Vulture(1);
let cpp_accessible_vulture = CppPin::new(Vulture(2));
(&amp;rust_accessible_vulture).squawk();
cpp_accessible_vulture.as_cpp_ref().squawk();</code></pre>
<p>But more importantly, what could <code>squawk</code> actually do here? The <code>impl Ref</code> is pretty useless — it’s no longer even a useful opaque token to pass back into C++. However, since both references can emit raw pointers, we can do field access. Even though <code>CppRef&lt;T&gt;</code> promises nothing about aliasing or mutability, it can still uphold C++ reference-like promises around alignment and not being null. So the <code>squawk</code> function here could, with suitable use of macros and autogenerated code, access fields within the <code>Vulture</code> and do useful work.</p>
<p>This <em>is</em> a good use for generic receivers.</p>
<p>So. Conclusions are:</p>
<ul>
<li><code>CppPin&lt;T&gt;</code> is necessary and I don’t think it sucks, though it is annoyingly like <code>Pin</code> in some ways.</li>
<li>Field access to <code>CppRef&lt;T&gt;</code> and/or <code>impl Ref&lt;T&gt;</code> are ugly and will probably need methods calls or macros, but this is OK since usually a <code>CppRef&lt;T&gt;</code> is just an opaque token which will be passed back to C++, and field access will be rare.</li>
<li>We probably <em>do</em> want to support generic self types, since sometimes people will want to write code that’s generic over <code>&amp;T</code> or <code>CppRef&lt;T&gt;</code> .<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ul>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月30日，开源日报第1059期：<br>
今日推荐开源项目：《Spotube》<br>
今日推荐英文原文：《Can CppRef<t> be ergonomic?》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/osd-20231230.jpg"></t></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《Spotube》传送门：<a href="https://github.com/KRTirtho/spotube?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由： 该项目是一个开源的Spotify客户端，旨在跨多个平台兼容，而无需依赖Spotify Premium，它利用Spotify的数据API和其他音频来源，如YouTube、Piped.video或JioSaavn</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@adetaylor/can-cppref-t-be-ergonomic-c9cb1365bda1" target="_blank">Can CppRef<t> be ergonomic?</t></a></p>
<p>推荐理由：该文章主要在讲CppRef<t>的Rust类型，用于处理C++类型的引用, 引入了CppPin<t>确保Rust中对对象的所有权，同时允许在C++中引用。作者也提到字段访问需通过函数或宏，方法调用涉及泛型self类型的支持</t></t></p>
<hr>
<h1>Can CppRef<t> be ergonomic?</t></h1>
<p><a href="https://medium.com/@adetaylor/are-we-reference-yet-c-references-in-rust-72c1c6c7015a" target="_blank">In a previous post</a>, I said that we simply can’t use Rust references to point to C++ types. This might work at small scale, but for any sizable C++ project, humans can’t promise that there are no other C++ references to the same data — so you run into aliasing violations, unexpected mutations, and the dreaded Undefined Behavior.</p>
<p>So, instead of using <code>&amp;T</code> we’ll create a <code>CppRef&lt;T&gt;</code> to create a C++ reference (or pointer). There’s some early work <a href="https://docs.rs/autocxx/latest/autocxx/macro.safety.html" target="_blank">here</a> — see <code>unsafe_references_wrapped</code> and the linked type, <a href="https://github.com/google/autocxx/blob/main/examples/reference-wrappers/src/main.rs" target="_blank">plus this example</a>). This ideally relies on a Rust feature called “arbitrary self types” which I’m working on <a href="https://github.com/rust-lang/rfcs/pull/3519/files" target="_blank">here in an RFC</a> along with some very fine other people (thanks!)</p>
<p>So far so good. But, one of the open questions has been — can <code>CppRef&lt;T&gt;</code> be ergonomic? And one specific question has come up during the RFC — is it desirable to support generic receivers? For example, is this monstrosity a good or bad idea?</p>
<pre><code>impl SomeType {
fn some_method(self: impl SomeTrait &lt; Target = Self &gt;) { ... }
}</code></pre>
<p>It turns out that these questions are related.</p>
<p>First, let’s talk about <code>CppPin&lt;T&gt;</code> . If you have some data which may have C++ pointers or references to it, it’s simply <em>not OK</em> to have a Rust reference.</p>
<p>There are some circumstances where the object will be stored over in C++ and all you would ever have in Rust is <code>CppRef</code> s to it:</p>
<ul>
<li>It’s stored in something like a <code>cxx::UniquePtr</code></li>
<li>A C++ method has returned you a reference to something stored over in C++ land entirely.</li>
</ul>
<p>But, you might sometimes want to own objects in Rust and yet make them available to C++. These might be C++ types or they might be Rust types. In such a case, you need a way to ensure there are only C++ references but no Rust references. That’s what <code>CppPin&lt;T&gt;</code> is for.</p>
<p><code>CppPin::new(something)</code> consumes the <code>something</code> , thus proving there are no existing Rust references. It can create new C++ references — <code>CppPin::as_cpp_ref() -&gt; CppRef&lt;T&gt;</code> — but there’s no way to get a <code>&amp;T</code> or <code>&amp;mut T</code> . You can safely do weird things to this type in C++, including storing references or pointers to it which you later manipulate, and there are guaranteed to be no Rust references which you discombobulate.</p>
<p><code>CppPin</code> might seem a strange name in that it’s not exactly about preventing things moving — but it shares lots of the same properties as the regular <code>Pin</code> including an inability to vend references, complexity about “pin projections”, and a general level of annoyingness. <code>CppJail</code> or <code>CppBubble</code> might be better names — opinions welcomed.</p>
<p>Overall, though, I think <code>CppPin&lt;T&gt;</code> is necessary and fairly straightforward.</p>
<p>What about field access? We can’t have <code>&amp;T</code> so we can’t have <code>some_reference.some_field</code> . So, all field access needs to be either via function calls over into C++, or via macros based around <code>addr_of</code> and <code>read</code> (which would be in a function call itself).</p>
<p>So,</p>
<pre><code>// entirely auto-generated code from bindings generator
struct SomeCppType {
// my_field: usize, // not actually represented
}
impl SomeCppType {
fn get_my_field(self: CppRef&lt;Self&gt;) -&gt; usize { ... }
fn set_my_field(self: CppRef&lt;Self&gt;, val: usize) { ... }
fn get_my_field_ref(self: CppRef&lt;Self&gt;) -&gt; CppRef&lt;usize&gt; { ... }
}</code></pre>
<p>I wanted to find out if this could be made slightly more ergonomic using a macro like <code>field!(some_value, field_name)</code> . This may depend upon stabilization of <code>concat_idents!</code> . I couldn’t get it to work, but ultimately I don’t think it’s a huge deal to need to call methods to get and set field values.</p>
<p>What about method calls?</p>
<p>The awesome thing about <code>CppRef&lt;T&gt;</code> is that it’s pretty much an opaque token. You’ll most commonly get a <code>CppRef&lt;T&gt;</code> from C++, and pass it back to C++, without any need to manipulate or touch the <code>CppRef&lt;T&gt;</code> at all. Most commonly, you’ll pass it back to C++ using as the <code>this</code> pointer in a method call:</p>
<pre><code>fn main() {
let vulture: CppRef&lt;Vulture&gt; = get_cpp_reference_to_vulture_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is what the “arbitrary self types” feature allows.</p>
<p>However, it would also be nice to call <code>squawk()</code> on a <code>CppPin&lt;Vulture&gt;:</code></p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk(); // autogenerated method
}</code></pre>
<p>This is where the question of generic <code>self</code> types first comes in.</p>
<p>Which of these is better for our (auto-generated) <code>squawk</code> method signature?</p>
<pre><code>impl Vulture {
// This code would be auto generated
fn squawk(self: CppRef&lt;Self&gt;) {} // 1
fn squawk(self: impl AsCppRef&lt;Target=Self&gt;) {} // 2
}</code></pre>
<p>The second option seems appealing because we could implement <code>AsCppRef</code> even on <code>CppPin</code> . This <a href="https://github.com/adetaylor/cpppin-ergonomics-experiments/blob/main/src/main.rs" target="_blank">works</a>, but it turns out not to be especially ergonomic, because it consumes the <code>CppPin</code> each time. That is, you couldn’t do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.squawk();
vulture.squawk();
}</code></pre>
<p>You would instead have to do:</p>
<pre><code>fn main() {
let vulture: CppPin&lt;Vulture&gt; = obtain_vulture_by_value_from_cpp();
vulture.as_cpp_ref().squawk();
vulture.squawk();
}</code></pre>
<p>which is very similar to the annoying <code>Pin::as_mut</code> method.</p>
<p>Overall, it seems better to pick option 1, and force people to call <code>as_cpp_ref()</code> each time they want to call a method on the contents of the <code>CppPin</code> . This doesn’t yet seem like a sufficiently good motivation for generic self types.</p>
<p>Finally — what about code that wants to be generic over the <em>type of reference?</em> That is, code which can handle a <code>&amp;Vulture</code> or a <code>CppRef&lt;Vulture&gt;</code> ? Is that even achievable?</p>
<p>Yes!</p>
<pre><code>impl Vulture {
/// This method can accept either &amp;Self or CppRef&lt;Self&gt;
/// because both of them impl a Ref trait
fn squawk(self: impl Ref&lt;Target = Self&gt;) -&gt; u32 {
// What to do here?
}
fn squawk_only_in_rust(&amp;self) {}
fn squawk_only_in_cpp(self: CppRef&lt;Self&gt;) {}
}</code></pre>
<p>One oddity here is that Rust method calls’ autoref functionality doesn’t work here, so if we want to call this method with a <code>&amp;Vulture</code> we need to say <code>(&amp;my_vulture_by_value).squawk()</code> . Here’s how we’d call this:</p>
<pre><code>let rust_accessible_vulture = Vulture(1);
let cpp_accessible_vulture = CppPin::new(Vulture(2));
(&amp;rust_accessible_vulture).squawk();
cpp_accessible_vulture.as_cpp_ref().squawk();</code></pre>
<p>But more importantly, what could <code>squawk</code> actually do here? The <code>impl Ref</code> is pretty useless — it’s no longer even a useful opaque token to pass back into C++. However, since both references can emit raw pointers, we can do field access. Even though <code>CppRef&lt;T&gt;</code> promises nothing about aliasing or mutability, it can still uphold C++ reference-like promises around alignment and not being null. So the <code>squawk</code> function here could, with suitable use of macros and autogenerated code, access fields within the <code>Vulture</code> and do useful work.</p>
<p>This <em>is</em> a good use for generic receivers.</p>
<p>So. Conclusions are:</p>
<ul>
<li><code>CppPin&lt;T&gt;</code> is necessary and I don’t think it sucks, though it is annoyingly like <code>Pin</code> in some ways.</li>
<li>Field access to <code>CppRef&lt;T&gt;</code> and/or <code>impl Ref&lt;T&gt;</code> are ugly and will probably need methods calls or macros, but this is OK since usually a <code>CppRef&lt;T&gt;</code> is just an opaque token which will be passed back to C++, and field access will be rare.</li>
<li>We probably <em>do</em> want to support generic self types, since sometimes people will want to write code that’s generic over <code>&amp;T</code> or <code>CppRef&lt;T&gt;</code> .<br>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>
</li>
</ul>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2023/12/osd-20231230.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2023/12/osd-20231230.jpg" medium="image"/>
    <pubDate>Sat, 30 Dec 2023 03:29:32 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1058期：推荐免费实用的 mac app：《awesome-mac》]]></title>
    <link>https://openingsource.org/11447/</link>
    <itunes:title><![CDATA[开源日报第1058期：推荐免费实用的 mac app：《awesome-mac》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月29日，开源日报第1058期：<br>
今日推荐开源项目：《awesome-mac》<br>
今日推荐英文原文：《How to Make an API Call with All CRUD Operations in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/xosd-20231229.jpg.pagespeed.ic.Oz4x-5PiH-.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《awesome-mac》传送门：<a href="https://github.com/jaywcjlove/awesome-mac" target="_blank">项目链接</a> </p>
<p>推荐理由： 推荐非常好用的 Mac 应用程序、软件以及工具, 通过 Pull Request 或者 issues 给这个仓库推荐优秀好用的Mac应用，文档格式参照 awesome，一起mac 社区变得更加繁荣</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/gitconnected/how-to-make-an-api-call-with-all-crud-operations-in-react-ed6e6b94c363" target="_blank">How to Make an API Call with All CRUD Operations in React</a></p>
<p>推荐理由：本文详细介绍了在React中使用Axios来CRUD（创建、读取、更新、删除）操作的方法。通过详细例子演示了如何使用Axios和REST API执行每个操作，同时也有对JSON Placeholder进行API调用的实际示例，并介绍了Axios的设置过程</p>
<hr>
<h1>How to Make an API Call with All CRUD Operations in React</h1>
<p>CRUD Operations in React using Axios</p>
<h2>Introduction 🌟</h2>
<p>Creating, Reading, Updating, and Deleting (CRUD) operations are essential in web development, forming the core of interaction between a user interface and a server. In React applications, especially given their component-based architecture, these operations are integral for creating dynamic user experiences. As each component potentially interacts with data, understanding CRUD operations allows you to manage and display this data effectively.</p>
<p>Throughout this guide, we will explain how to perform each CRUD operation using React and a REST API. For these examples, we’ll be using Axios, a popular and powerful library that simplifies HTTP requests and integrates seamlessly with React’s ecosystem.</p>
<h2>Understanding CRUD Operations 🔍</h2>
<p>Here’s a quick overview of each CRUD operation and its corresponding HTTP method:</p>
<table>
<thead>
<tr>
<th>Operation</th>
<th>HTTP Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>Create</td>
<td>POST</td>
<td>Adds new data</td>
</tr>
<tr>
<td>Read</td>
<td>GET</td>
<td>Retrieves data</td>
</tr>
<tr>
<td>Update</td>
<td>PUT/PATCH</td>
<td>Modifies existing data</td>
</tr>
<tr>
<td>Delete</td>
<td>DELETE</td>
<td>Removes data</td>
</tr>
</tbody>
</table>
<h2>Using JSON Placeholder for API Calls 🌐</h2>
<p>For our practical examples, we will utilize JSON Placeholder, a free online REST API that mimics real server responses with fake data. This service is ideal for testing and prototyping, as it provides typical responses you would expect from a real server without the need for setting up a backend. For those interested in exploring or using this tool for their own testing purposes, you can find more information at <a href="https://jsonplaceholder.typicode.com/" target="_blank">JSON Placeholder</a>.</p>
<h2>Setting Up Axios in Your Project ⚙️</h2>
<p>Before diving into CRUD operations, let’s set up Axios in our project. Axios is a popular library for making HTTP requests, preferred over the native fetch API for its automatic handling of JSON data conversion and more user-friendly error handling.</p>
<p>To add Axios to your project, run:</p>
<pre><code>npm install axios</code></pre>
<p>Then, import it into your React component:</p>
<pre><code>import axios from 'axios';</code></pre>
<p>Now your axios set up is ready, let’s dive into the examples</p>
<h2>1. GET Operation 📥</h2>
<p>The GET operation is used to retrieve data from a server at the specified resource. When you make a GET request, you’re asking the server to send data back to you.</p>
<p>Let’s make a GET request to retrieve a list of posts from JSON Placeholder:</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const Posts = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
})
.catch(error =&gt; {
console.error('Error fetching data:', error);
});
}, []);
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;p&gt;{post.body}&lt;/p&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
}
export default Posts;</code></pre>
<p>A successful GET request typically returns a status code of <code>200 OK</code>, along with the requested data, usually in JSON format. In case of failure (e.g., <code>network issues</code>, <code>wrong URL</code>), Axios will catch the error, which can be handled in the <code>.catch</code> block. It's crucial to handle these errors to prevent your application from crashing and to provide feedback to the user.</p>
<h2>2. POST Operation (Create) 🆕</h2>
<p>The POST method is used to send data to the server to create a new resource. It’s a fundamental part of web development that deals with submitting forms, uploading files, or creating new data entries in a database.</p>
<p>To demonstrate, we’ll create a new post using JSON Placeholder:</p>
<pre><code>import React, { useState } from 'react';
import axios from 'axios';
const CreatePost = () =&gt; {
const [title, setTitle] = useState('');
const [body, setBody] = useState('');
const handleSubmit = (e) =&gt; {
e.preventDefault();
const newPost = { title, body };
axios.post('https://jsonplaceholder.typicode.com/posts', newPost)
.then(response =&gt; {
alert(`New Post Created with ID: ${response.data.id}`);
})
.catch(error =&gt; {
console.error('Error creating a post:', error);
});
};
return (
&lt;form onSubmit={handleSubmit}&gt;
&lt;input type="text" value={title} onChange={(e) =&gt; setTitle(e.target.value)} placeholder="Title" /&gt;
&lt;textarea value={body} onChange={(e) =&gt; setBody(e.target.value)} placeholder="Body"&gt;&lt;/textarea&gt;
&lt;button type="submit"&gt;Create Post&lt;/button&gt;
&lt;/form&gt;
);
};
export default CreatePost;</code></pre>
<p>This example demonstrates a typical form in React for submitting data. The <code>useState</code> hooks track form inputs, and the <code>handleSubmit</code> function sends a POST request with the form data.</p>
<h2>Understanding the Response</h2>
<p>A successful POST request is acknowledged by the server with a <code>201 Created</code> status code. The response generally contains the data of the newly created resource, including any server-generated fields like an ID. This feedback is crucial for confirming the creation and updating the UI accordingly.</p>
<h2>3. PUT/PATCH Operation (Update) 🔄</h2>
<p>PUT and PATCH methods are employed for updating existing resources. The distinction lies in their approach: PUT is used for replacing an entire resource, while PATCH is for making partial updates.</p>
<p>To update a post, we’ll create a simple interface with a button. When clicked, it sends a PUT request with a hardcoded payload to update a specific post.</p>
<pre><code>import React from "react";
import axios from "axios";
const UpdatePost = () =&gt; {
const postId = 1; // Assuming we are updating the post with ID 1
const updatedData = { title: "New Title", body: "Updated body text" };
const handleUpdate = () =&gt; {
axios
.put(`https://jsonplaceholder.typicode.com/posts/${postId}`, updatedData)
.then((response) =&gt; {
console.log("Updated Post:", response.data);
})
.catch((error) =&gt; {
console.error("Error updating the post:", error);
});
};
return (
&lt;div&gt;
&lt;button onClick={handleUpdate}&gt;Update Post&lt;/button&gt;
&lt;/div&gt;
);
};
export default UpdatePost;</code></pre>
<p>In this example, the <code>handleUpdate</code> function triggers a PUT request with predefined <code>updatedData</code>. This setup simulates updating a post, and upon success, logs the updated post data to the console.</p>
<h2>Understanding the Response</h2>
<p>When you send a PUT request to update a resource, the server’s response can vary based on the API’s design. Typically, a <code>200 OK</code> status code is returned, along with the updated resource data. This response confirms that the update was successful and allows you to see the changes made. In some cases, if the update doesn't return any content, you might receive a <code>204 No Content</code> status. It's important to handle these responses in your application to confirm the update to the user and to update the UI accordingly.</p>
<p>Errors in PUT requests could occur due to various reasons like invalid data sent in the request or trying to update a non-existent resource. These errors are usually indicated by 4xx status codes like <code>400 Bad Request</code> or <code>404 Not Found</code>. Proper error handling is crucial for informing users about what went wrong.</p>
<h2>4. DELETE Operation 🗑️</h2>
<p>For the DELETE operation, we’ll display a list of posts, each with a ‘Delete’ button. Clicking the button will send a DELETE request to remove that specific post.</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const DeletePost = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
});
}, []);
const handleDelete = (postId) =&gt; {
axios.delete(`https://jsonplaceholder.typicode.com/posts/${postId}`)
.then(() =&gt; {
setPosts(posts.filter(post =&gt; post.id !== postId));
console.log(`Post with ID ${postId} deleted successfully`);
})
.catch(error =&gt; {
console.error('Error deleting the post:', error);
});
};
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;button onClick={() =&gt; handleDelete(post.id)}&gt;Delete Post&lt;/button&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
};
export default DeletePost;</code></pre>
<p>In this DELETE example, each post has a button attached to it. Clicking the button triggers <code>handleDelete</code>, which sends a DELETE request for that specific post. The post list is then updated to reflect the deletion.</p>
<h2>Understanding the Response</h2>
<p>For DELETE requests, the typical successful response is a <code>200 OK</code> or <code>204 No Content</code> status code, often with no return data. This indicates that the resource was successfully deleted. Some APIs might return the deleted resource's data as part of the response, which can be useful for confirming what was deleted or for record-keeping purposes in the application.</p>
<p>In case of failure, such as attempting to delete a resource that doesn’t exist or one that can’t be deleted due to server rules, the server responds with appropriate error status codes like <code>404 Not Found</code> or <code>403 Forbidden</code>. Handling these errors is essential to provide clear feedback to the user, ensuring they understand why the operation didn't succeed.</p>
<h2>Conclusion 🚀</h2>
<p>In this guide, we’ve covered the essential CRUD operations — Create, Read, Update, and Delete — using Axios in React. By walking through each operation with practical examples, we aimed to provide a clear and accessible approach to interacting with APIs in your React applications. Remember, understanding how to make these requests and handle server responses is crucial for building effective and user-friendly web applications.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月29日，开源日报第1058期：<br>
今日推荐开源项目：《awesome-mac》<br>
今日推荐英文原文：《How to Make an API Call with All CRUD Operations in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/xosd-20231229.jpg.pagespeed.ic.Oz4x-5PiH-.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《awesome-mac》传送门：<a href="https://github.com/jaywcjlove/awesome-mac" target="_blank">项目链接</a> </p>
<p>推荐理由： 推荐非常好用的 Mac 应用程序、软件以及工具, 通过 Pull Request 或者 issues 给这个仓库推荐优秀好用的Mac应用，文档格式参照 awesome，一起mac 社区变得更加繁荣</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/gitconnected/how-to-make-an-api-call-with-all-crud-operations-in-react-ed6e6b94c363" target="_blank">How to Make an API Call with All CRUD Operations in React</a></p>
<p>推荐理由：本文详细介绍了在React中使用Axios来CRUD（创建、读取、更新、删除）操作的方法。通过详细例子演示了如何使用Axios和REST API执行每个操作，同时也有对JSON Placeholder进行API调用的实际示例，并介绍了Axios的设置过程</p>
<hr>
<h1>How to Make an API Call with All CRUD Operations in React</h1>
<p>CRUD Operations in React using Axios</p>
<h2>Introduction 🌟</h2>
<p>Creating, Reading, Updating, and Deleting (CRUD) operations are essential in web development, forming the core of interaction between a user interface and a server. In React applications, especially given their component-based architecture, these operations are integral for creating dynamic user experiences. As each component potentially interacts with data, understanding CRUD operations allows you to manage and display this data effectively.</p>
<p>Throughout this guide, we will explain how to perform each CRUD operation using React and a REST API. For these examples, we’ll be using Axios, a popular and powerful library that simplifies HTTP requests and integrates seamlessly with React’s ecosystem.</p>
<h2>Understanding CRUD Operations 🔍</h2>
<p>Here’s a quick overview of each CRUD operation and its corresponding HTTP method:</p>
<table>
<thead>
<tr>
<th>Operation</th>
<th>HTTP Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>Create</td>
<td>POST</td>
<td>Adds new data</td>
</tr>
<tr>
<td>Read</td>
<td>GET</td>
<td>Retrieves data</td>
</tr>
<tr>
<td>Update</td>
<td>PUT/PATCH</td>
<td>Modifies existing data</td>
</tr>
<tr>
<td>Delete</td>
<td>DELETE</td>
<td>Removes data</td>
</tr>
</tbody>
</table>
<h2>Using JSON Placeholder for API Calls 🌐</h2>
<p>For our practical examples, we will utilize JSON Placeholder, a free online REST API that mimics real server responses with fake data. This service is ideal for testing and prototyping, as it provides typical responses you would expect from a real server without the need for setting up a backend. For those interested in exploring or using this tool for their own testing purposes, you can find more information at <a href="https://jsonplaceholder.typicode.com/" target="_blank">JSON Placeholder</a>.</p>
<h2>Setting Up Axios in Your Project ⚙️</h2>
<p>Before diving into CRUD operations, let’s set up Axios in our project. Axios is a popular library for making HTTP requests, preferred over the native fetch API for its automatic handling of JSON data conversion and more user-friendly error handling.</p>
<p>To add Axios to your project, run:</p>
<pre><code>npm install axios</code></pre>
<p>Then, import it into your React component:</p>
<pre><code>import axios from 'axios';</code></pre>
<p>Now your axios set up is ready, let’s dive into the examples</p>
<h2>1. GET Operation 📥</h2>
<p>The GET operation is used to retrieve data from a server at the specified resource. When you make a GET request, you’re asking the server to send data back to you.</p>
<p>Let’s make a GET request to retrieve a list of posts from JSON Placeholder:</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const Posts = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
})
.catch(error =&gt; {
console.error('Error fetching data:', error);
});
}, []);
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;p&gt;{post.body}&lt;/p&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
}
export default Posts;</code></pre>
<p>A successful GET request typically returns a status code of <code>200 OK</code>, along with the requested data, usually in JSON format. In case of failure (e.g., <code>network issues</code>, <code>wrong URL</code>), Axios will catch the error, which can be handled in the <code>.catch</code> block. It's crucial to handle these errors to prevent your application from crashing and to provide feedback to the user.</p>
<h2>2. POST Operation (Create) 🆕</h2>
<p>The POST method is used to send data to the server to create a new resource. It’s a fundamental part of web development that deals with submitting forms, uploading files, or creating new data entries in a database.</p>
<p>To demonstrate, we’ll create a new post using JSON Placeholder:</p>
<pre><code>import React, { useState } from 'react';
import axios from 'axios';
const CreatePost = () =&gt; {
const [title, setTitle] = useState('');
const [body, setBody] = useState('');
const handleSubmit = (e) =&gt; {
e.preventDefault();
const newPost = { title, body };
axios.post('https://jsonplaceholder.typicode.com/posts', newPost)
.then(response =&gt; {
alert(`New Post Created with ID: ${response.data.id}`);
})
.catch(error =&gt; {
console.error('Error creating a post:', error);
});
};
return (
&lt;form onSubmit={handleSubmit}&gt;
&lt;input type="text" value={title} onChange={(e) =&gt; setTitle(e.target.value)} placeholder="Title" /&gt;
&lt;textarea value={body} onChange={(e) =&gt; setBody(e.target.value)} placeholder="Body"&gt;&lt;/textarea&gt;
&lt;button type="submit"&gt;Create Post&lt;/button&gt;
&lt;/form&gt;
);
};
export default CreatePost;</code></pre>
<p>This example demonstrates a typical form in React for submitting data. The <code>useState</code> hooks track form inputs, and the <code>handleSubmit</code> function sends a POST request with the form data.</p>
<h2>Understanding the Response</h2>
<p>A successful POST request is acknowledged by the server with a <code>201 Created</code> status code. The response generally contains the data of the newly created resource, including any server-generated fields like an ID. This feedback is crucial for confirming the creation and updating the UI accordingly.</p>
<h2>3. PUT/PATCH Operation (Update) 🔄</h2>
<p>PUT and PATCH methods are employed for updating existing resources. The distinction lies in their approach: PUT is used for replacing an entire resource, while PATCH is for making partial updates.</p>
<p>To update a post, we’ll create a simple interface with a button. When clicked, it sends a PUT request with a hardcoded payload to update a specific post.</p>
<pre><code>import React from "react";
import axios from "axios";
const UpdatePost = () =&gt; {
const postId = 1; // Assuming we are updating the post with ID 1
const updatedData = { title: "New Title", body: "Updated body text" };
const handleUpdate = () =&gt; {
axios
.put(`https://jsonplaceholder.typicode.com/posts/${postId}`, updatedData)
.then((response) =&gt; {
console.log("Updated Post:", response.data);
})
.catch((error) =&gt; {
console.error("Error updating the post:", error);
});
};
return (
&lt;div&gt;
&lt;button onClick={handleUpdate}&gt;Update Post&lt;/button&gt;
&lt;/div&gt;
);
};
export default UpdatePost;</code></pre>
<p>In this example, the <code>handleUpdate</code> function triggers a PUT request with predefined <code>updatedData</code>. This setup simulates updating a post, and upon success, logs the updated post data to the console.</p>
<h2>Understanding the Response</h2>
<p>When you send a PUT request to update a resource, the server’s response can vary based on the API’s design. Typically, a <code>200 OK</code> status code is returned, along with the updated resource data. This response confirms that the update was successful and allows you to see the changes made. In some cases, if the update doesn't return any content, you might receive a <code>204 No Content</code> status. It's important to handle these responses in your application to confirm the update to the user and to update the UI accordingly.</p>
<p>Errors in PUT requests could occur due to various reasons like invalid data sent in the request or trying to update a non-existent resource. These errors are usually indicated by 4xx status codes like <code>400 Bad Request</code> or <code>404 Not Found</code>. Proper error handling is crucial for informing users about what went wrong.</p>
<h2>4. DELETE Operation 🗑️</h2>
<p>For the DELETE operation, we’ll display a list of posts, each with a ‘Delete’ button. Clicking the button will send a DELETE request to remove that specific post.</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const DeletePost = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
});
}, []);
const handleDelete = (postId) =&gt; {
axios.delete(`https://jsonplaceholder.typicode.com/posts/${postId}`)
.then(() =&gt; {
setPosts(posts.filter(post =&gt; post.id !== postId));
console.log(`Post with ID ${postId} deleted successfully`);
})
.catch(error =&gt; {
console.error('Error deleting the post:', error);
});
};
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;button onClick={() =&gt; handleDelete(post.id)}&gt;Delete Post&lt;/button&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
};
export default DeletePost;</code></pre>
<p>In this DELETE example, each post has a button attached to it. Clicking the button triggers <code>handleDelete</code>, which sends a DELETE request for that specific post. The post list is then updated to reflect the deletion.</p>
<h2>Understanding the Response</h2>
<p>For DELETE requests, the typical successful response is a <code>200 OK</code> or <code>204 No Content</code> status code, often with no return data. This indicates that the resource was successfully deleted. Some APIs might return the deleted resource's data as part of the response, which can be useful for confirming what was deleted or for record-keeping purposes in the application.</p>
<p>In case of failure, such as attempting to delete a resource that doesn’t exist or one that can’t be deleted due to server rules, the server responds with appropriate error status codes like <code>404 Not Found</code> or <code>403 Forbidden</code>. Handling these errors is essential to provide clear feedback to the user, ensuring they understand why the operation didn't succeed.</p>
<h2>Conclusion 🚀</h2>
<p>In this guide, we’ve covered the essential CRUD operations — Create, Read, Update, and Delete — using Axios in React. By walking through each operation with practical examples, we aimed to provide a clear and accessible approach to interacting with APIs in your React applications. Remember, understanding how to make these requests and handle server responses is crucial for building effective and user-friendly web applications.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
2023年12月29日，开源日报第1058期：<br>
今日推荐开源项目：《awesome-mac》<br>
今日推荐英文原文：《How to Make an API Call with All CRUD Operations in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2023/12/xosd-20231229.jpg.pagespeed.ic.Oz4x-5PiH-.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《awesome-mac》传送门：<a href="https://github.com/jaywcjlove/awesome-mac" target="_blank">项目链接</a> </p>
<p>推荐理由： 推荐非常好用的 Mac 应用程序、软件以及工具, 通过 Pull Request 或者 issues 给这个仓库推荐优秀好用的Mac应用，文档格式参照 awesome，一起mac 社区变得更加繁荣</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/gitconnected/how-to-make-an-api-call-with-all-crud-operations-in-react-ed6e6b94c363" target="_blank">How to Make an API Call with All CRUD Operations in React</a></p>
<p>推荐理由：本文详细介绍了在React中使用Axios来CRUD（创建、读取、更新、删除）操作的方法。通过详细例子演示了如何使用Axios和REST API执行每个操作，同时也有对JSON Placeholder进行API调用的实际示例，并介绍了Axios的设置过程</p>
<hr>
<h1>How to Make an API Call with All CRUD Operations in React</h1>
<p>CRUD Operations in React using Axios</p>
<h2>Introduction 🌟</h2>
<p>Creating, Reading, Updating, and Deleting (CRUD) operations are essential in web development, forming the core of interaction between a user interface and a server. In React applications, especially given their component-based architecture, these operations are integral for creating dynamic user experiences. As each component potentially interacts with data, understanding CRUD operations allows you to manage and display this data effectively.</p>
<p>Throughout this guide, we will explain how to perform each CRUD operation using React and a REST API. For these examples, we’ll be using Axios, a popular and powerful library that simplifies HTTP requests and integrates seamlessly with React’s ecosystem.</p>
<h2>Understanding CRUD Operations 🔍</h2>
<p>Here’s a quick overview of each CRUD operation and its corresponding HTTP method:</p>
<table>
<thead>
<tr>
<th>Operation</th>
<th>HTTP Method</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>Create</td>
<td>POST</td>
<td>Adds new data</td>
</tr>
<tr>
<td>Read</td>
<td>GET</td>
<td>Retrieves data</td>
</tr>
<tr>
<td>Update</td>
<td>PUT/PATCH</td>
<td>Modifies existing data</td>
</tr>
<tr>
<td>Delete</td>
<td>DELETE</td>
<td>Removes data</td>
</tr>
</tbody>
</table>
<h2>Using JSON Placeholder for API Calls 🌐</h2>
<p>For our practical examples, we will utilize JSON Placeholder, a free online REST API that mimics real server responses with fake data. This service is ideal for testing and prototyping, as it provides typical responses you would expect from a real server without the need for setting up a backend. For those interested in exploring or using this tool for their own testing purposes, you can find more information at <a href="https://jsonplaceholder.typicode.com/" target="_blank">JSON Placeholder</a>.</p>
<h2>Setting Up Axios in Your Project ⚙️</h2>
<p>Before diving into CRUD operations, let’s set up Axios in our project. Axios is a popular library for making HTTP requests, preferred over the native fetch API for its automatic handling of JSON data conversion and more user-friendly error handling.</p>
<p>To add Axios to your project, run:</p>
<pre><code>npm install axios</code></pre>
<p>Then, import it into your React component:</p>
<pre><code>import axios from 'axios';</code></pre>
<p>Now your axios set up is ready, let’s dive into the examples</p>
<h2>1. GET Operation 📥</h2>
<p>The GET operation is used to retrieve data from a server at the specified resource. When you make a GET request, you’re asking the server to send data back to you.</p>
<p>Let’s make a GET request to retrieve a list of posts from JSON Placeholder:</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const Posts = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
})
.catch(error =&gt; {
console.error('Error fetching data:', error);
});
}, []);
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;p&gt;{post.body}&lt;/p&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
}
export default Posts;</code></pre>
<p>A successful GET request typically returns a status code of <code>200 OK</code>, along with the requested data, usually in JSON format. In case of failure (e.g., <code>network issues</code>, <code>wrong URL</code>), Axios will catch the error, which can be handled in the <code>.catch</code> block. It's crucial to handle these errors to prevent your application from crashing and to provide feedback to the user.</p>
<h2>2. POST Operation (Create) 🆕</h2>
<p>The POST method is used to send data to the server to create a new resource. It’s a fundamental part of web development that deals with submitting forms, uploading files, or creating new data entries in a database.</p>
<p>To demonstrate, we’ll create a new post using JSON Placeholder:</p>
<pre><code>import React, { useState } from 'react';
import axios from 'axios';
const CreatePost = () =&gt; {
const [title, setTitle] = useState('');
const [body, setBody] = useState('');
const handleSubmit = (e) =&gt; {
e.preventDefault();
const newPost = { title, body };
axios.post('https://jsonplaceholder.typicode.com/posts', newPost)
.then(response =&gt; {
alert(`New Post Created with ID: ${response.data.id}`);
})
.catch(error =&gt; {
console.error('Error creating a post:', error);
});
};
return (
&lt;form onSubmit={handleSubmit}&gt;
&lt;input type="text" value={title} onChange={(e) =&gt; setTitle(e.target.value)} placeholder="Title" /&gt;
&lt;textarea value={body} onChange={(e) =&gt; setBody(e.target.value)} placeholder="Body"&gt;&lt;/textarea&gt;
&lt;button type="submit"&gt;Create Post&lt;/button&gt;
&lt;/form&gt;
);
};
export default CreatePost;</code></pre>
<p>This example demonstrates a typical form in React for submitting data. The <code>useState</code> hooks track form inputs, and the <code>handleSubmit</code> function sends a POST request with the form data.</p>
<h2>Understanding the Response</h2>
<p>A successful POST request is acknowledged by the server with a <code>201 Created</code> status code. The response generally contains the data of the newly created resource, including any server-generated fields like an ID. This feedback is crucial for confirming the creation and updating the UI accordingly.</p>
<h2>3. PUT/PATCH Operation (Update) 🔄</h2>
<p>PUT and PATCH methods are employed for updating existing resources. The distinction lies in their approach: PUT is used for replacing an entire resource, while PATCH is for making partial updates.</p>
<p>To update a post, we’ll create a simple interface with a button. When clicked, it sends a PUT request with a hardcoded payload to update a specific post.</p>
<pre><code>import React from "react";
import axios from "axios";
const UpdatePost = () =&gt; {
const postId = 1; // Assuming we are updating the post with ID 1
const updatedData = { title: "New Title", body: "Updated body text" };
const handleUpdate = () =&gt; {
axios
.put(`https://jsonplaceholder.typicode.com/posts/${postId}`, updatedData)
.then((response) =&gt; {
console.log("Updated Post:", response.data);
})
.catch((error) =&gt; {
console.error("Error updating the post:", error);
});
};
return (
&lt;div&gt;
&lt;button onClick={handleUpdate}&gt;Update Post&lt;/button&gt;
&lt;/div&gt;
);
};
export default UpdatePost;</code></pre>
<p>In this example, the <code>handleUpdate</code> function triggers a PUT request with predefined <code>updatedData</code>. This setup simulates updating a post, and upon success, logs the updated post data to the console.</p>
<h2>Understanding the Response</h2>
<p>When you send a PUT request to update a resource, the server’s response can vary based on the API’s design. Typically, a <code>200 OK</code> status code is returned, along with the updated resource data. This response confirms that the update was successful and allows you to see the changes made. In some cases, if the update doesn't return any content, you might receive a <code>204 No Content</code> status. It's important to handle these responses in your application to confirm the update to the user and to update the UI accordingly.</p>
<p>Errors in PUT requests could occur due to various reasons like invalid data sent in the request or trying to update a non-existent resource. These errors are usually indicated by 4xx status codes like <code>400 Bad Request</code> or <code>404 Not Found</code>. Proper error handling is crucial for informing users about what went wrong.</p>
<h2>4. DELETE Operation 🗑️</h2>
<p>For the DELETE operation, we’ll display a list of posts, each with a ‘Delete’ button. Clicking the button will send a DELETE request to remove that specific post.</p>
<pre><code>import React, { useState, useEffect } from 'react';
import axios from 'axios';
const DeletePost = () =&gt; {
const [posts, setPosts] = useState([]);
useEffect(() =&gt; {
axios.get('https://jsonplaceholder.typicode.com/posts')
.then(response =&gt; {
setPosts(response.data);
});
}, []);
const handleDelete = (postId) =&gt; {
axios.delete(`https://jsonplaceholder.typicode.com/posts/${postId}`)
.then(() =&gt; {
setPosts(posts.filter(post =&gt; post.id !== postId));
console.log(`Post with ID ${postId} deleted successfully`);
})
.catch(error =&gt; {
console.error('Error deleting the post:', error);
});
};
return (
&lt;div&gt;
&lt;h1&gt;Posts&lt;/h1&gt;
{posts.map(post =&gt; (
&lt;div key={post.id}&gt;
&lt;h2&gt;{post.title}&lt;/h2&gt;
&lt;button onClick={() =&gt; handleDelete(post.id)}&gt;Delete Post&lt;/button&gt;
&lt;/div&gt;
))}
&lt;/div&gt;
);
};
export default DeletePost;</code></pre>
<p>In this DELETE example, each post has a button attached to it. Clicking the button triggers <code>handleDelete</code>, which sends a DELETE request for that specific post. The post list is then updated to reflect the deletion.</p>
<h2>Understanding the Response</h2>
<p>For DELETE requests, the typical successful response is a <code>200 OK</code> or <code>204 No Content</code> status code, often with no return data. This indicates that the resource was successfully deleted. Some APIs might return the deleted resource's data as part of the response, which can be useful for confirming what was deleted or for record-keeping purposes in the application.</p>
<p>In case of failure, such as attempting to delete a resource that doesn’t exist or one that can’t be deleted due to server rules, the server responds with appropriate error status codes like <code>404 Not Found</code> or <code>403 Forbidden</code>. Handling these errors is essential to provide clear feedback to the user, ensuring they understand why the operation didn't succeed.</p>
<h2>Conclusion 🚀</h2>
<p>In this guide, we’ve covered the essential CRUD operations — Create, Read, Update, and Delete — using Axios in React. By walking through each operation with practical examples, we aimed to provide a clear and accessible approach to interacting with APIs in your React applications. Remember, understanding how to make these requests and handle server responses is crucial for building effective and user-friendly web applications.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2023/12/xosd-20231229.jpg.pagespeed.ic.Oz4x-5PiH-.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2023/12/xosd-20231229.jpg.pagespeed.ic.Oz4x-5PiH-.jpg" medium="image"/>
    <pubDate>Fri, 29 Dec 2023 04:24:56 +0000</pubDate>
  </item></channel>
</rss>