<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:itunes="http://www.itunes.com/dtds/podcast-1.0.dtd" xmlns:googleplay="http://www.google.com/schemas/play-podcasts/1.0" xmlns:media="http://www.rssboard.org/media-rss" version="2.0">
  <channel>
    <title><![CDATA[开源工场[RSS+]]]></title>
    <link>https://openingsource.org/feed/</link>
    <image>
      <url>https://openingsource.org/wp-content/uploads/2018/01/cropped-portrait_logo-32x32.png</url>
      <title>开源工场[RSS+]</title>
      <link>https://openingsource.org/feed/</link>
    </image>
    <language>zh-CN</language>
    <atom:link href="https://openingsource.org/feed/" rel="self" type="application/rss+xml"/>
    <copyright><![CDATA[开源工场[RSS+]]]></copyright>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[
      人生苦短，我爱开源<br />
<br />
<a href="https://openingsource.org/feed/" target="_blank">https://openingsource.org/feed/</a>
      ]]>
    </itunes:summary>
    <description>
      <![CDATA[
      人生苦短，我爱开源<br />
<br />
<a href="https://openingsource.org/feed/" target="_blank">https://openingsource.org/feed/</a>
      ]]>
    </description>
    <itunes:owner>
      <itunes:name><![CDATA[开源工场[RSS+]]]></itunes:name>
    </itunes:owner>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2018/01/cropped-portrait_logo-32x32.png"/>
<item>
    <title><![CDATA[开源日报第1076期：OpenAI扩展模型：《danswer》]]></title>
    <link>https://openingsource.org/11521/</link>
    <itunes:title><![CDATA[开源日报第1076期：OpenAI扩展模型：《danswer》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月16日，开源日报第1076期：<br>
今日推荐开源项目：《danswer》<br>
今日推荐英文原文：《React Custom Hook: Hovering Made Easy with useHover》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240116.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《danswer》传送门：<a href="https://github.com/hiteshchoudhary/js-hindi-youtube?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： danswer可以自然语言提问并获得由私人来源支持的答案，并通过引用和参考来源材料获得可靠答案，连接到诸如Slack、GitHub、Confluence等工具<br>
官网直达：<a href="https://docs.danswer.dev" target="_blank">https://docs.danswer.dev</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/react-custom-hook-hovering-made-easy-with-usehover-ed5b097a78a" target="_blank">React Custom Hook: Hovering Made Easy with useHover!</a></p>
<p>推荐理由：这篇文章介绍了如何创建和使用自定义 React Hook，名为 useHover，用于检测元素悬停状态，通过 useState 管理悬停状态，useEffect 添加悬停事件监听器，使其可在不同组件中重用，比较实用的 hook</p>
<hr>
<h1>React Custom Hook: Hovering Made Easy with useHover!</h1>
<p>Hey there, fellow React enthusiasts! Today, we’re diving into the world of custom hooks and learning about one of the most useful ones: <code>useHover</code>.</p>
<p>If you're new to React or just starting your journey, don't worry!</p>
<p>We'll take it step by step and make sure you understand everything with plenty of examples.</p>
<h1>What’s a Custom Hook?</h1>
<p>First things first, what’s a custom hook? In React, a custom hook is simply a JavaScript function that starts with the word “use” and lets you hook into React state and lifecycle features from functional components.</p>
<p>Think of them as a way to reuse stateful logic across different components.</p>
<p>Custom hooks can be a real lifesaver when you find yourself repeating the same logic in multiple places.</p>
<p>Today, we’re going to create a custom hook called <code>useHover</code> that will help us detect when an element is being hovered over.</p>
<h1>The <code>useHover</code> Hook</h1>
<p>Here’s a simple use case for our <code>useHover</code> hook: Let's say you have a button, and you want to change its color when a user hovers over it.</p>
<p>We could write this behavior in a custom hook to make it reusable. Here's how you can create the <code>useHover</code> hook:</p>
<pre><code>import { useState, useEffect } from 'react';
function useHover() {
const [isHovered, setIsHovered] = useState(false);
const handleMouseEnter = () =&gt; {
setIsHovered(true);
};
const handleMouseLeave = () =&gt; {
setIsHovered(false);
};
useEffect(() =&gt; {
const element = document.querySelector('.hover-element'); // Replace with your element selector
element.addEventListener('mouseenter', handleMouseEnter);
element.addEventListener('mouseleave', handleMouseLeave);
return () =&gt; {
element.removeEventListener('mouseenter', handleMouseEnter);
element.removeEventListener('mouseleave', handleMouseLeave);
};
}, []);
return isHovered;
}</code></pre>
<p>Here’s a breakdown of what’s happening:</p>
<ol>
<li>We import the necessary React hooks, <code>useState</code> and <code>useEffect</code>.</li>
<li>Inside the <code>useHover</code> function, we initialize a state variable, <code>isHovered</code>, and two event handlers, <code>handleMouseEnter</code> and <code>handleMouseLeave</code>.</li>
<li>We use the <code>useEffect</code> hook to add event listeners for <code>mouseenter</code> and <code>mouseleave</code> events to the specified element (you'll need to replace <code>.hover-element</code> with your own selector). These event listeners will call our event handlers when the mouse enters and leaves the element.</li>
<li>We return the <code>isHovered</code> state variable. This tells us whether the element is currently being hovered over or not.</li>
</ol>
<h1>Using the <code>useHover</code> Hook</h1>
<p>Now that we have our <code>useHover</code> hook ready, let's see how we can use it in a functional component:</p>
<pre><code>import React from 'react';
import useHover from './useHover'; // Import our custom hook
function HoverExample() {
const isHovered = useHover();
const buttonColor = isHovered ? 'blue' : 'red';
return (
&lt;button
className="hover-element"
style={{ backgroundColor: buttonColor }}
&gt;
Hover me!
&lt;/button&gt;
);
}
export default HoverExample;</code></pre>
<p>Here’s what’s happening in the <code>HoverExample</code> component:</p>
<ol>
<li>We import our custom hook <code>useHover</code> at the top of the file.</li>
<li>Inside the <code>HoverExample</code> component, we call <code>useHover()</code>, which gives us the <code>isHovered</code> value.</li>
<li>We use the <code>isHovered</code> value to determine the button's background color. If <code>isHovered</code> is <code>true</code>, we set the color to blue; otherwise, it's red.</li>
<li>Finally, we render a button element with the class name <code>hover-element</code> and apply the dynamic background color based on the hover state.</li>
</ol>
<p><strong>Result:</strong></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1054/1*JI-qFV4FPM5Yh_tdKuFnLw.png" alt="img"></p>
<h1>Wrapping Up</h1>
<p>And that’s it! You’ve just created and used your own custom hook, <code>useHover</code>, to add hover functionality to a button.</p>
<p>The beauty of custom hooks is that you can reuse them across different components, making your code more readable and maintainable.</p>
<p>Custom hooks are a powerful tool in your React toolbox. They allow you to abstract away complex logic and provide a clean API for your components.</p>
<p>So, go ahead and create your custom hooks for various use cases, and level up your React development skills.</p>
<p>Happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月16日，开源日报第1076期：<br>
今日推荐开源项目：《danswer》<br>
今日推荐英文原文：《React Custom Hook: Hovering Made Easy with useHover》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240116.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《danswer》传送门：<a href="https://github.com/hiteshchoudhary/js-hindi-youtube?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： danswer可以自然语言提问并获得由私人来源支持的答案，并通过引用和参考来源材料获得可靠答案，连接到诸如Slack、GitHub、Confluence等工具<br>
官网直达：<a href="https://docs.danswer.dev" target="_blank">https://docs.danswer.dev</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/react-custom-hook-hovering-made-easy-with-usehover-ed5b097a78a" target="_blank">React Custom Hook: Hovering Made Easy with useHover!</a></p>
<p>推荐理由：这篇文章介绍了如何创建和使用自定义 React Hook，名为 useHover，用于检测元素悬停状态，通过 useState 管理悬停状态，useEffect 添加悬停事件监听器，使其可在不同组件中重用，比较实用的 hook</p>
<hr>
<h1>React Custom Hook: Hovering Made Easy with useHover!</h1>
<p>Hey there, fellow React enthusiasts! Today, we’re diving into the world of custom hooks and learning about one of the most useful ones: <code>useHover</code>.</p>
<p>If you're new to React or just starting your journey, don't worry!</p>
<p>We'll take it step by step and make sure you understand everything with plenty of examples.</p>
<h1>What’s a Custom Hook?</h1>
<p>First things first, what’s a custom hook? In React, a custom hook is simply a JavaScript function that starts with the word “use” and lets you hook into React state and lifecycle features from functional components.</p>
<p>Think of them as a way to reuse stateful logic across different components.</p>
<p>Custom hooks can be a real lifesaver when you find yourself repeating the same logic in multiple places.</p>
<p>Today, we’re going to create a custom hook called <code>useHover</code> that will help us detect when an element is being hovered over.</p>
<h1>The <code>useHover</code> Hook</h1>
<p>Here’s a simple use case for our <code>useHover</code> hook: Let's say you have a button, and you want to change its color when a user hovers over it.</p>
<p>We could write this behavior in a custom hook to make it reusable. Here's how you can create the <code>useHover</code> hook:</p>
<pre><code>import { useState, useEffect } from 'react';
function useHover() {
const [isHovered, setIsHovered] = useState(false);
const handleMouseEnter = () =&gt; {
setIsHovered(true);
};
const handleMouseLeave = () =&gt; {
setIsHovered(false);
};
useEffect(() =&gt; {
const element = document.querySelector('.hover-element'); // Replace with your element selector
element.addEventListener('mouseenter', handleMouseEnter);
element.addEventListener('mouseleave', handleMouseLeave);
return () =&gt; {
element.removeEventListener('mouseenter', handleMouseEnter);
element.removeEventListener('mouseleave', handleMouseLeave);
};
}, []);
return isHovered;
}</code></pre>
<p>Here’s a breakdown of what’s happening:</p>
<ol>
<li>We import the necessary React hooks, <code>useState</code> and <code>useEffect</code>.</li>
<li>Inside the <code>useHover</code> function, we initialize a state variable, <code>isHovered</code>, and two event handlers, <code>handleMouseEnter</code> and <code>handleMouseLeave</code>.</li>
<li>We use the <code>useEffect</code> hook to add event listeners for <code>mouseenter</code> and <code>mouseleave</code> events to the specified element (you'll need to replace <code>.hover-element</code> with your own selector). These event listeners will call our event handlers when the mouse enters and leaves the element.</li>
<li>We return the <code>isHovered</code> state variable. This tells us whether the element is currently being hovered over or not.</li>
</ol>
<h1>Using the <code>useHover</code> Hook</h1>
<p>Now that we have our <code>useHover</code> hook ready, let's see how we can use it in a functional component:</p>
<pre><code>import React from 'react';
import useHover from './useHover'; // Import our custom hook
function HoverExample() {
const isHovered = useHover();
const buttonColor = isHovered ? 'blue' : 'red';
return (
&lt;button
className="hover-element"
style={{ backgroundColor: buttonColor }}
&gt;
Hover me!
&lt;/button&gt;
);
}
export default HoverExample;</code></pre>
<p>Here’s what’s happening in the <code>HoverExample</code> component:</p>
<ol>
<li>We import our custom hook <code>useHover</code> at the top of the file.</li>
<li>Inside the <code>HoverExample</code> component, we call <code>useHover()</code>, which gives us the <code>isHovered</code> value.</li>
<li>We use the <code>isHovered</code> value to determine the button's background color. If <code>isHovered</code> is <code>true</code>, we set the color to blue; otherwise, it's red.</li>
<li>Finally, we render a button element with the class name <code>hover-element</code> and apply the dynamic background color based on the hover state.</li>
</ol>
<p><strong>Result:</strong></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1054/1*JI-qFV4FPM5Yh_tdKuFnLw.png" alt="img"></p>
<h1>Wrapping Up</h1>
<p>And that’s it! You’ve just created and used your own custom hook, <code>useHover</code>, to add hover functionality to a button.</p>
<p>The beauty of custom hooks is that you can reuse them across different components, making your code more readable and maintainable.</p>
<p>Custom hooks are a powerful tool in your React toolbox. They allow you to abstract away complex logic and provide a clean API for your components.</p>
<p>So, go ahead and create your custom hooks for various use cases, and level up your React development skills.</p>
<p>Happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月16日，开源日报第1076期：<br>
今日推荐开源项目：《danswer》<br>
今日推荐英文原文：《React Custom Hook: Hovering Made Easy with useHover》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240116.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《danswer》传送门：<a href="https://github.com/hiteshchoudhary/js-hindi-youtube?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： danswer可以自然语言提问并获得由私人来源支持的答案，并通过引用和参考来源材料获得可靠答案，连接到诸如Slack、GitHub、Confluence等工具<br>
官网直达：<a href="https://docs.danswer.dev" target="_blank">https://docs.danswer.dev</a></p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@Evelyn.Taylor/react-custom-hook-hovering-made-easy-with-usehover-ed5b097a78a" target="_blank">React Custom Hook: Hovering Made Easy with useHover!</a></p>
<p>推荐理由：这篇文章介绍了如何创建和使用自定义 React Hook，名为 useHover，用于检测元素悬停状态，通过 useState 管理悬停状态，useEffect 添加悬停事件监听器，使其可在不同组件中重用，比较实用的 hook</p>
<hr>
<h1>React Custom Hook: Hovering Made Easy with useHover!</h1>
<p>Hey there, fellow React enthusiasts! Today, we’re diving into the world of custom hooks and learning about one of the most useful ones: <code>useHover</code>.</p>
<p>If you're new to React or just starting your journey, don't worry!</p>
<p>We'll take it step by step and make sure you understand everything with plenty of examples.</p>
<h1>What’s a Custom Hook?</h1>
<p>First things first, what’s a custom hook? In React, a custom hook is simply a JavaScript function that starts with the word “use” and lets you hook into React state and lifecycle features from functional components.</p>
<p>Think of them as a way to reuse stateful logic across different components.</p>
<p>Custom hooks can be a real lifesaver when you find yourself repeating the same logic in multiple places.</p>
<p>Today, we’re going to create a custom hook called <code>useHover</code> that will help us detect when an element is being hovered over.</p>
<h1>The <code>useHover</code> Hook</h1>
<p>Here’s a simple use case for our <code>useHover</code> hook: Let's say you have a button, and you want to change its color when a user hovers over it.</p>
<p>We could write this behavior in a custom hook to make it reusable. Here's how you can create the <code>useHover</code> hook:</p>
<pre><code>import { useState, useEffect } from 'react';
function useHover() {
const [isHovered, setIsHovered] = useState(false);
const handleMouseEnter = () =&gt; {
setIsHovered(true);
};
const handleMouseLeave = () =&gt; {
setIsHovered(false);
};
useEffect(() =&gt; {
const element = document.querySelector('.hover-element'); // Replace with your element selector
element.addEventListener('mouseenter', handleMouseEnter);
element.addEventListener('mouseleave', handleMouseLeave);
return () =&gt; {
element.removeEventListener('mouseenter', handleMouseEnter);
element.removeEventListener('mouseleave', handleMouseLeave);
};
}, []);
return isHovered;
}</code></pre>
<p>Here’s a breakdown of what’s happening:</p>
<ol>
<li>We import the necessary React hooks, <code>useState</code> and <code>useEffect</code>.</li>
<li>Inside the <code>useHover</code> function, we initialize a state variable, <code>isHovered</code>, and two event handlers, <code>handleMouseEnter</code> and <code>handleMouseLeave</code>.</li>
<li>We use the <code>useEffect</code> hook to add event listeners for <code>mouseenter</code> and <code>mouseleave</code> events to the specified element (you'll need to replace <code>.hover-element</code> with your own selector). These event listeners will call our event handlers when the mouse enters and leaves the element.</li>
<li>We return the <code>isHovered</code> state variable. This tells us whether the element is currently being hovered over or not.</li>
</ol>
<h1>Using the <code>useHover</code> Hook</h1>
<p>Now that we have our <code>useHover</code> hook ready, let's see how we can use it in a functional component:</p>
<pre><code>import React from 'react';
import useHover from './useHover'; // Import our custom hook
function HoverExample() {
const isHovered = useHover();
const buttonColor = isHovered ? 'blue' : 'red';
return (
&lt;button
className="hover-element"
style={{ backgroundColor: buttonColor }}
&gt;
Hover me!
&lt;/button&gt;
);
}
export default HoverExample;</code></pre>
<p>Here’s what’s happening in the <code>HoverExample</code> component:</p>
<ol>
<li>We import our custom hook <code>useHover</code> at the top of the file.</li>
<li>Inside the <code>HoverExample</code> component, we call <code>useHover()</code>, which gives us the <code>isHovered</code> value.</li>
<li>We use the <code>isHovered</code> value to determine the button's background color. If <code>isHovered</code> is <code>true</code>, we set the color to blue; otherwise, it's red.</li>
<li>Finally, we render a button element with the class name <code>hover-element</code> and apply the dynamic background color based on the hover state.</li>
</ol>
<p><strong>Result:</strong></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1054/1*JI-qFV4FPM5Yh_tdKuFnLw.png" alt="img"></p>
<h1>Wrapping Up</h1>
<p>And that’s it! You’ve just created and used your own custom hook, <code>useHover</code>, to add hover functionality to a button.</p>
<p>The beauty of custom hooks is that you can reuse them across different components, making your code more readable and maintainable.</p>
<p>Custom hooks are a powerful tool in your React toolbox. They allow you to abstract away complex logic and provide a clean API for your components.</p>
<p>So, go ahead and create your custom hooks for various use cases, and level up your React development skills.</p>
<p>Happy coding! 😄🚀</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240116.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240116.jpg" medium="image"/>
    <pubDate>Tue, 16 Jan 2024 12:24:16 +0000</pubDate>
  </item><item>
    <title><![CDATA[GUADEC 2024 开放演讲报名，2月18日截止]]></title>
    <link>https://openingsource.org/6618/</link>
    <itunes:title><![CDATA[GUADEC 2024 开放演讲报名，2月18日截止]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p>GUADEC 2024 峰会将于 7 月 19 日至 24 日在充满活力的科罗拉多州丹佛市举行，这是GNOME 基金会在北美举行的第二次会议，也是在美国的首次活动。GUADEC 2024 将继续作为混合活动举办，与会者可以通过虚拟方式或亲自在位于丹佛的场地参加。</p>
<p><img src="https://openingsource.org/wp-content/uploads/2019/05/x2024GUADEC-denverimg.webp.pagespeed.ic.lNDWbppK82.webp"></p>
<p>丹佛坐落在落基山脉的壮丽背景下，因其海拔而被称为“英里高城”。丹佛拥有蓬勃发展的科技社区、多元化的社区和丰富的文化景观，为 GUADEC 2024 提供了完美的背景。探索这座城市著名的美食、户外探险和独特的景点。 会议场地 Auraria Campus 位于丹佛市中心附近。</p>
<p>GUADEC 2024 的演讲正式于1月10日开放，2月18日截至，话题包括但不限于下列：</p>
<ul>
<li>应用开发</li>
<li>隐私和安全</li>
<li>社区和团队建设</li>
<li>用户和开发者体验的设计</li>
<li>在桌面之外使用 GNOME 技术</li>
<li>新人倡议</li>
<li>项目规划和治理</li>
</ul>
<h3>活动日期</h3>
<ul>
<li>7 月 19 日至 21 日（周五至周日）：峰会及演讲</li>
<li>7 月 22 日至 23 日（周一至周二）：BoF 和研讨会</li>
<li>7 月 24 日（星期三）：社交日</li>
</ul>
<h3>报名参加</h3>
<ul>
<li>报名演讲：<a href="https://events.gnome.org/event/209/abstracts/" target="_blank">https://events.gnome.org/event/209/abstracts/</a></li>
<li>报名参加：<a href="https://events.gnome.org/event/209/" target="_blank">https://events.gnome.org/event/209/</a></li>
</ul>
<div><span><i></i> <a href="https://openingsource.org/tag/gnome/">GNOME</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec/">GUADEC</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec-2024/">GUADEC 2024</a></span><span><i></i> <a href="https://openingsource.org/tag/%e4%b8%b9%e4%bd%9b/">丹佛</a></span><span><i></i> <a href="https://openingsource.org/tag/%e7%be%8e%e5%9b%bd/">美国</a></span></div>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p>GUADEC 2024 峰会将于 7 月 19 日至 24 日在充满活力的科罗拉多州丹佛市举行，这是GNOME 基金会在北美举行的第二次会议，也是在美国的首次活动。GUADEC 2024 将继续作为混合活动举办，与会者可以通过虚拟方式或亲自在位于丹佛的场地参加。</p>
<p><img src="https://openingsource.org/wp-content/uploads/2019/05/x2024GUADEC-denverimg.webp.pagespeed.ic.lNDWbppK82.webp"></p>
<p>丹佛坐落在落基山脉的壮丽背景下，因其海拔而被称为“英里高城”。丹佛拥有蓬勃发展的科技社区、多元化的社区和丰富的文化景观，为 GUADEC 2024 提供了完美的背景。探索这座城市著名的美食、户外探险和独特的景点。 会议场地 Auraria Campus 位于丹佛市中心附近。</p>
<p>GUADEC 2024 的演讲正式于1月10日开放，2月18日截至，话题包括但不限于下列：</p>
<ul>
<li>应用开发</li>
<li>隐私和安全</li>
<li>社区和团队建设</li>
<li>用户和开发者体验的设计</li>
<li>在桌面之外使用 GNOME 技术</li>
<li>新人倡议</li>
<li>项目规划和治理</li>
</ul>
<h3>活动日期</h3>
<ul>
<li>7 月 19 日至 21 日（周五至周日）：峰会及演讲</li>
<li>7 月 22 日至 23 日（周一至周二）：BoF 和研讨会</li>
<li>7 月 24 日（星期三）：社交日</li>
</ul>
<h3>报名参加</h3>
<ul>
<li>报名演讲：<a href="https://events.gnome.org/event/209/abstracts/" target="_blank">https://events.gnome.org/event/209/abstracts/</a></li>
<li>报名参加：<a href="https://events.gnome.org/event/209/" target="_blank">https://events.gnome.org/event/209/</a></li>
</ul>
<div><span><i></i> <a href="https://openingsource.org/tag/gnome/">GNOME</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec/">GUADEC</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec-2024/">GUADEC 2024</a></span><span><i></i> <a href="https://openingsource.org/tag/%e4%b8%b9%e4%bd%9b/">丹佛</a></span><span><i></i> <a href="https://openingsource.org/tag/%e7%be%8e%e5%9b%bd/">美国</a></span></div>]]>
    </description>
    <content:encoded><![CDATA[<p>GUADEC 2024 峰会将于 7 月 19 日至 24 日在充满活力的科罗拉多州丹佛市举行，这是GNOME 基金会在北美举行的第二次会议，也是在美国的首次活动。GUADEC 2024 将继续作为混合活动举办，与会者可以通过虚拟方式或亲自在位于丹佛的场地参加。</p>
<p><img src="https://openingsource.org/wp-content/uploads/2019/05/x2024GUADEC-denverimg.webp.pagespeed.ic.lNDWbppK82.webp"></p>
<p>丹佛坐落在落基山脉的壮丽背景下，因其海拔而被称为“英里高城”。丹佛拥有蓬勃发展的科技社区、多元化的社区和丰富的文化景观，为 GUADEC 2024 提供了完美的背景。探索这座城市著名的美食、户外探险和独特的景点。 会议场地 Auraria Campus 位于丹佛市中心附近。</p>
<p>GUADEC 2024 的演讲正式于1月10日开放，2月18日截至，话题包括但不限于下列：</p>
<ul>
<li>应用开发</li>
<li>隐私和安全</li>
<li>社区和团队建设</li>
<li>用户和开发者体验的设计</li>
<li>在桌面之外使用 GNOME 技术</li>
<li>新人倡议</li>
<li>项目规划和治理</li>
</ul>
<h3>活动日期</h3>
<ul>
<li>7 月 19 日至 21 日（周五至周日）：峰会及演讲</li>
<li>7 月 22 日至 23 日（周一至周二）：BoF 和研讨会</li>
<li>7 月 24 日（星期三）：社交日</li>
</ul>
<h3>报名参加</h3>
<ul>
<li>报名演讲：<a href="https://events.gnome.org/event/209/abstracts/" target="_blank">https://events.gnome.org/event/209/abstracts/</a></li>
<li>报名参加：<a href="https://events.gnome.org/event/209/" target="_blank">https://events.gnome.org/event/209/</a></li>
</ul>
<div><span><i></i> <a href="https://openingsource.org/tag/gnome/">GNOME</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec/">GUADEC</a></span><span><i></i> <a href="https://openingsource.org/tag/guadec-2024/">GUADEC 2024</a></span><span><i></i> <a href="https://openingsource.org/tag/%e4%b8%b9%e4%bd%9b/">丹佛</a></span><span><i></i> <a href="https://openingsource.org/tag/%e7%be%8e%e5%9b%bd/">美国</a></span></div>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2019/05/x2024GUADEC-denverimg.webp.pagespeed.ic.lNDWbppK82.webp"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2019/05/x2024GUADEC-denverimg.webp.pagespeed.ic.lNDWbppK82.webp" medium="image"/>
    <pubDate>Mon, 15 Jan 2024 15:06:31 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1075期：单一源API 管理中心：《apihub》]]></title>
    <link>https://openingsource.org/11515/</link>
    <itunes:title><![CDATA[开源日报第1075期：单一源API 管理中心：《apihub》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月15日，开源日报第1075期：<br>
今日推荐开源项目：《apihub》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240115.jpg.pagespeed.ic.K5TJ9RANOO.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《apihub》传送门：<a href="https://github.com/hiteshchoudhary/apihub?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： freeapi是一个单一源API 管理中心，用于学习任何编程语言中的api处理， 也用freeapi在Web和移动应用程序中构建自己前端作品集</p>
<p>官网直达：freeapi.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：React Native的新架构引入了重大变化 主要组件包括：</p>
<ul>
<li>
<p>JavaScript Interface (JSI): 一个轻量级层，使得JavaScript能够直接调用本地方法。这有助于在线程之间实现互操作性，消除了JSON序列化的需求，提高了性能</p>
</li>
<li>
<p>Fabric: 一种新的渲染系统，取代了UI Manager，实现了JS和UI线程之间的同步。它提升了用户交互（如滚动和手势）的性能，将同步执行重点放在面向用户的任务上</p>
</li>
<li>
<p>Turbo Modules: 对旧的Native Modules的改进，Turbo Modules使JavaScript能够在需要时加载模块，减少了React Native应用的启动时间</p>
</li>
<li>
<p>CodeGen: 一个静态类型检查器，确保JavaScript（动态类型）和C++（静态类型）之间的顺畅通信。它在构建时定义了Turbo Modules和Fabric使用的接口元素，而不是在运行时生成更多的本地代码</p>
</li>
</ul>
<hr>
<h1>ReactNative New vs. Old Architecture</h1>
<p>ReactNative Multi platform application Development.</p>
<p>This is a good time to understand what changes are taking place under the hood and how they might affect your React Native App.</p>
<blockquote>
<p>This article aims to cover the most important changes.</p>
</blockquote>
<ul>
<li><strong>JavaScript Interface(JSI)</strong></li>
<li><strong>Fabric</strong></li>
<li><strong>Turbo Modules</strong></li>
<li><strong>CodeGen</strong></li>
</ul>
<h1>Old Architecture</h1>
<p>Before we get to the new architecture, let’s recap how the old one works.</p>
<p>R<a href="https://reactnative.dev/" target="_blank">eact Native</a> is a JavaScript library that allows us to create mobile apps that will run on both Android and iOS. As they sell it, “Learn once, write anywhere.”</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1356/1*4p7zu_yWeTioHAhejneoCA.png" alt="img"></p>
<h1>How the UI of a React Native app is rendered.</h1>
<p>React Native allows you to build mobile apps using JavaScript and React, but the actual rendering of the user interface happens using native components of the respective platforms (iOS or Android).</p>
<ul>
<li><strong>Rendering with Native Views</strong>: React Native doesn’t directly translate your JavaScript code into native code (Java for Android or Objective C/Swift for iOS). Instead, it uses native views to render the UI. This is more efficient and provides a better user experience.</li>
<li><strong>Communication between Components</strong>: In React Native, you build your UI using components. Components communicate with each other through properties (props) and callbacks. This follows a one-way flow, making it easier to manage data flow in your app.</li>
<li><strong>Passing Data</strong>: If a parent component needs something from its child, it passes down a callback. Similarly, if a child component needs something from its parent, it receives a property. This unidirectional flow simplifies the design and maintenance of your app.</li>
<li><strong>Native Code Integration</strong>: While React Native encourages building the UI using its components, if you need to use a native component (e.g., MapView), you can integrate it seamlessly. React Native provides a bridge for communication between the JavaScript code and native components.</li>
<li><strong>How OS Understands JavaScript</strong>: The OS doesn’t directly understand JavaScript code. Instead, the React Native framework acts as an intermediary. It interprets and executes your JavaScript code, and through its bridge, it communicates with the native components to render the UI on the device.</li>
</ul>
<p>So far, so good. But how does the OS understand the JavaScript code?</p>
<h1>The Bundling Process</h1>
<p>A native mobile app will be developed using the programming language that is specific to that platform. When developing with React Native, you can pretty much get by without writing any Objective C/Java code — unless you are required to do something that is not covered by the library, such as integrating a payment provider that only offers SDKs for Android and iOS.</p>
<p>However, any React Native project contains an <code>ios</code> directory and an <code>android</code> one. These directories act as the entry points for each platform — they basically bootstrap React Native. They contain code that is specific to each platform, and here’s where your JS code is bridged for each platform.</p>
<p>In order to start your app, you’ll generally run <code>yarn android</code> or <code>yarn ios</code> and then you’ll wait until the app magically opens on your desired device. But what happens while you wait?</p>
<p>As soon as you type in one of those commands (which are <code>react-native run-android</code> and <code>react native run-ios</code>, respectively) you start up the packager. One such packager is <a href="https://facebook.github.io/metro/" target="_blank">Metro</a>. The packager takes all your JS code and puts it into a single file: <code>main.bundle.js</code>. When your app finally opens on your phone, the phone will look in the place that’s familiar to it: either the <code>android</code> or the <code>ios</code> directory. That’s the native entry point that I mentioned above. This native entry point will start the JavaScript virtual machine in a thread. The bundled code that is contained in <code>main.bundle.js</code> will then be run on this thread.</p>
<p>The code that’s now running within this JavaScript VM thread will communicate with the native thread using the React Native bridge.</p>
<p>Before jumping to the RN bridge and then analyzing the performance of RN apps, let’s understand how your phone is able to run JavaScript code.</p>
<h1>JavaScriptCore</h1>
<p><a href="https://github.com/WebKit/webkit/tree/main/Source/JavaScriptCore" target="_blank">JavaScriptCore</a> is a framework that allows JavaScript code to be run on mobile devices, for instance. On iOS devices, this framework is directly provided by the OS. Android devices don’t have the framework, so React Native bundles it along with the app itself. This increases the app size just a little bit, but it’ll barely matter in the end.</p>
<p>There’s one thing I want to point out that might save you some debugging time. JavaScriptCore is used for running JS code when the app is run on a device. However, if you choose to debug your app, the JS code is going to run inside Chrome. Chrome uses the V8 engine and uses WebSockets for communicating with the native code, so you’ll be able to see important info such as properly formatted logs and what network requests are being made. Just remember that there are differences between the V8 engine and JavaScriptCore — they are different environments and you might run into bugs that only happen when the debugger is attached but not when your app is run normally on your device!</p>
<h1>The React Native Bridge</h1>
<p>The RN bridge is written in Java/C++ and it allows the communication between the main thread of your app and the JavaScript thread. It uses a custom message-passing protocol to allow this communication to happen.</p>
<p>The JavaScript thread will decide what must be rendered on the screen. It will say to the main thread, “Hey, I need you to render a button and a text. Thanks.” It will use the bridge to say this. The message will be sent as a serialized JSON. But apart from what needs to be rendered on the screen, the message must also state where it will be rendered. Here’s where the shadow thread comes into play. The shadow thread is launched along with the JavaScript thread, and it helps to compute the positions of the views. The results are passed along in the aforementioned message, sent by means of the bridge to the main thread.</p>
<p>Any action that the user does on the UI will happen on the main thread. Tapping on a button, toggling a switch — any action must be serialized and sent by means of the bridge to the JavaScript thread. There’s where all the logic of your app happens.</p>
<h1>How Performance Is Affected</h1>
<p>Let’s review what we have covered so far. The user taps on a button. This action is understood by the main thread and passed as a message to the JavaScript thread. Here, some logic is handled, then the UI must change accordingly. The shadow thread decides where these changes happen, then the updates are sent as a message back to the native thread. Since the user won’t tap too rapidly on the screen, we won’t generally have any performance issues in normal usage scenarios — the bridge handles the communication pretty quickly.</p>
<p>What is cool about React Native (in comparison to other platforms such as Cordova) is that it doesn’t run its code inside of a <code>WebView</code>. It uses native views.</p>
<p>This advantage means that we’ll be able to develop smooth and fast apps that can run at 60 FPS. If you modify the state of a component that is very high in the tree (and you didn’t dedicate too much time to prevent useless re-renders), then the whole component tree will be re-rendered. This won’t be visible to the user in most cases. However, if those descendants are computationally expensive, then you’ll notice your app stuttering for a little bit.</p>
<h1>In a nutshell:</h1>
<p>When you run a RN app, all you JavaScript code is bundled together into a package called the JS Bundle. The Native Code is kept separately.</p>
<p>The Execution of React Native apps happens over three threads:</p>
<ul>
<li>The JavaScript thread: used by the JS Engine, to run the JS Bundle</li>
<li>The Native/UI thread: used to run the Native Modules and to handle operations like UI Rendering, user gesture events etc.</li>
<li>Additionally there is a 3rd thread called the shadow thread, which is used to calculate the Layout of Elements before rendering them on the host screen</li>
</ul>
<p>The Communication between the JS and Native Threads is carried over an entity called the bridge. When sending data through the bridge it has to be batched(optimized) and serialized as JSON. This bridge can only handle asynchronous communication.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Sw5UOASq_phL4Z7W4tfnIQ.png" alt="img"></p>
<p>Old Architecture in React native</p>
<p>JavaScriptCore: <em>It is the name of a JavaScript Engine, which is used by React Native to execute JS code.</em></p>
<p>Yoga: <em>It is the name of a Layout engine, which is used to calculate positions of UI elements for the user’s screen.</em></p>
<h1>JavaScript Interface (JSI)</h1>
<p>In the old architecture, React Native uses the Bridge Module to make communication possible between the JS and Native threads. Every time data is sent across the bridge, it has to be serialized as JSON. When the data is received on the other side it must be decoded as well.</p>
<p>This means that the JavaScript and Native worlds are unaware of each other <em>(ie. the JS thread cannot directly call a method on the Native thread)</em></p>
<p>Another important point to note is; messages send over the bridge are asynchronous in nature, which is a good thing for most use cases, but there are certain instances when JS code and native code needs to be in sync.</p>
<h1>Let’s take an example to better understand the bridge:</h1>
<p>If the JavaScript thread needs access to some native modules (eg. Bluetooth), it will need to send a message to the native thread. The JS thread will send a serialized JSON message to the bridge. The bridge will optimize this message and send it over to the native thread. The message will be decoded on the native thread, and eventually the required native code will be executed.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*g1NSRP7XNFK4H60hfTpiIQ.png" alt="img"></p>
<ul>
<li>JS thread prepares message for the Native Thread</li>
<li>It is serizlized as JSON before sending across the bridge</li>
<li>It is decoded when recieved on the other end of the bridge</li>
<li>Then the native thread executes the required native code</li>
</ul>
<h1>New Architecture</h1>
<p>However, in the New Architecture, the bridge is going to be replaced with a module called <strong><em>JavaScript Interface\</em></strong>, which is a lightweight, general-purpose layer, written in C++ that can be used by the JavaScript engine to directly invoke/call methods in the native realm.</p>
<h1>What does general-purpose mean?</h1>
<p>The current architecture uses the JavaScriptCore Engine. The bridge is only compatible with this particular engine. However, this is not the case for JSI. The JavaScript Interface will be decoupled from the Engine, which means that the new architecture enables the use of other JavaScript Engines like Chakra, v8, Hermes etc. Hence the term “<em>general-purpose”</em>.</p>
<h1>How can JSI enable JavaScript to directly call native methods?</h1>
<p>Through the JSI, Native methods will be exposed to JavaScript via C++ Host Objects. JavaScript can hold a reference to these objects. And can invoke the methods directly using that reference. This is similar to the web, where JavaScript code can hold a reference to any DOM element, and call methods on it. For Example: when you write:</p>
<blockquote>
<p><em>const container = document.createElement(‘div’);</em></p>
</blockquote>
<p>Here, the container is a JavaScript variable, but it holds a reference to a DOM element which was probably initialized in C++. If we call any method on the “container” variable, it will in turn call the method on the DOM element. The JSI will work similarly.</p>
<p>Unlike the bridge, the JSI will allow JavaScript code to hold a reference to Native Modules. And through the JSI, JavaScript can call methods on this reference directly.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*2yQoZjBjJyXAB_TTK_wtWg.png" alt="img"></p>
<ol>
<li>JavaScript has a direct reference to a native module</li>
<li>It calls a method on this native module, via the JavaScript Interface.</li>
</ol>
<p>To Sum it up, JSI will enable the use of other JavaScript Engines &amp; it will allow for complete interoperability between the threads, the JavaScript code could communicate with the native side directly from the JS thread. This will eliminate the need to serialize JSON messages and will fix the congestion and asynchronous issues on the bridge.</p>
<p>Another big advantage of the JSI is that it is written in C++. With the power of C++ React Native can target large number of Systems like Smart TVs, Watches etc.</p>
<h1>Fabric</h1>
<p>Fabric is the rendering system, which will replace the current UI Manager.</p>
<p>To understand the advantages of Fabric, first let’s look at how UI is currently rendered in React Native:</p>
<p>When your app is run, React executes your code and creates a ReactElementTree in JavaScript. Based on this tree, the Renderer creates a ReactShadowTree in C++.</p>
<p>This shadow Tree is used by the Layout Engine to calculate the positions of UI elements for the host screen. Once the results of the Layout calculation are available, the shadow tree is transformed into HostViewTree, which comprises of Native Elements. <em>(For example The ReactNative <view> element will be translated into ViewGroup in Android &amp; UIView in iOS respectively).</view></em></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Pv-4VyvyhonoO7Jv7r7VJA.png" alt="img"></p>
<p>ReactElementTree (JavaScript) -&gt; ReactShadowTree(C++) -&gt; HostViewTree(Native)</p>
<h1>Problems with this approach:</h1>
<p>As we know, all the communication between threads happens over the bridge. Which means slow transfer rates, and unnecessary copying of data.</p>
<p>For Example: If a ReactElementTree Node happens to be an <img>, then the consequent node of the ReactShadowTree will also be an image. However, this data will have to be duplicated and stored separately in both the nodes.</p>
<p>That’s not all. Since the JS and UI threads are not in sync, there are certain use cases when your app can seem laggy as it drops frames. (Example: Scrolling through a FlatList with a huge list of data)</p>
<h1>What is Fabric?</h1>
<p>According to the official <a href="https://reactnative.dev/docs/fabric-renderer" target="_blank">ReactNative documentation</a>,</p>
<blockquote>
<p><em>“Fabric is React Native’s new rendering system, a conceptual evolution of the legacy render system”</em></p>
</blockquote>
<p>As we have seen in the JSI section of this article, the JavaScript Interface will directly expose native methods to JavaScript, which also includes UI methods. As a result of this, the JS and UI threads can be in sync. This will improve performance for lists, navigation, gesture handling etc.</p>
<h1>What are the benefits of Fabric?</h1>
<p>With the new rendering system, user interactions such as scrolling, gestures, etc can be prioritized to be executed synchronously in the main thread or native thread. While other tasks such as API requests will be executed asynchronously.</p>
<p>That’s not all. The new Shadow Tree will be immutable, and it will be shared between the JS and UI threads, to allow straight interaction from both ends.</p>
<p>As we have seen, in the old architecture React Native has to maintain two hierarchies/DOM nodes. But since the shadow tree will now be shared among realms, it will help with reducing memory consumption as well.</p>
<h1>3. Turbo Modules</h1>
<p>In the old architecture, all the Native Modules used by JavaScript (e.g. Bluetooth, Geo Location, File Storage, etc) have to be initialized before the app is opened. This means, that even if the user doesn’t require a particular module, it still has to be initialized at start-up.</p>
<p>Turbo Modules are an enhancement over these old Native modules. As we have seen in the previous part of this article, now JavaScript will be able to hold a reference to these modules, which will allow JS Code to load each module only when it is required. This will significantly improve start-up time for React Native apps.</p>
<h1>4. CodeGen</h1>
<p>All this talk of Turbo Modules and Fabric sounds promising, but JavaScript is a Dynamically typed language, and JSI is written in C++, which is a Statically Typed Language. Consequently, there is a need to ensure smooth communication between the two.</p>
<p>That’s why the new architecture will also include a static type checker called CodeGen.</p>
<p>By using the typed JavaScript as the source of truth CodeGen will define interface elements used by Turbo Modules and Fabric. It will also generate more native code at build time, instead of run time.</p>
<h1>Summary</h1>
<p>If we combine all the changes, the new architecture will look like this:</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*94FA1Tqj13GnwQXhV0WlPw.png" alt="img"></p>
<p>Here the key highlights are:</p>
<p>• Bridge will be replaced by JSI</p>
<p>• Ability to swap the JavaScriptCore with other Engines</p>
<p>• Complete Interoperability between all threads</p>
<ul>
<li>Web-like Rendering system</li>
<li>Time-sensitive tasks can be executed synchronously</li>
<li>Lazy Loading of Turbo Modules</li>
<li>Static Type Checking for compatibility between JS and Native Side.</li>
</ul>
<p>this new structure will give React Native some powerful improvements.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月15日，开源日报第1075期：<br>
今日推荐开源项目：《apihub》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240115.jpg.pagespeed.ic.K5TJ9RANOO.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《apihub》传送门：<a href="https://github.com/hiteshchoudhary/apihub?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： freeapi是一个单一源API 管理中心，用于学习任何编程语言中的api处理， 也用freeapi在Web和移动应用程序中构建自己前端作品集</p>
<p>官网直达：freeapi.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：React Native的新架构引入了重大变化 主要组件包括：</p>
<ul>
<li>
<p>JavaScript Interface (JSI): 一个轻量级层，使得JavaScript能够直接调用本地方法。这有助于在线程之间实现互操作性，消除了JSON序列化的需求，提高了性能</p>
</li>
<li>
<p>Fabric: 一种新的渲染系统，取代了UI Manager，实现了JS和UI线程之间的同步。它提升了用户交互（如滚动和手势）的性能，将同步执行重点放在面向用户的任务上</p>
</li>
<li>
<p>Turbo Modules: 对旧的Native Modules的改进，Turbo Modules使JavaScript能够在需要时加载模块，减少了React Native应用的启动时间</p>
</li>
<li>
<p>CodeGen: 一个静态类型检查器，确保JavaScript（动态类型）和C++（静态类型）之间的顺畅通信。它在构建时定义了Turbo Modules和Fabric使用的接口元素，而不是在运行时生成更多的本地代码</p>
</li>
</ul>
<hr>
<h1>ReactNative New vs. Old Architecture</h1>
<p>ReactNative Multi platform application Development.</p>
<p>This is a good time to understand what changes are taking place under the hood and how they might affect your React Native App.</p>
<blockquote>
<p>This article aims to cover the most important changes.</p>
</blockquote>
<ul>
<li><strong>JavaScript Interface(JSI)</strong></li>
<li><strong>Fabric</strong></li>
<li><strong>Turbo Modules</strong></li>
<li><strong>CodeGen</strong></li>
</ul>
<h1>Old Architecture</h1>
<p>Before we get to the new architecture, let’s recap how the old one works.</p>
<p>R<a href="https://reactnative.dev/" target="_blank">eact Native</a> is a JavaScript library that allows us to create mobile apps that will run on both Android and iOS. As they sell it, “Learn once, write anywhere.”</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1356/1*4p7zu_yWeTioHAhejneoCA.png" alt="img"></p>
<h1>How the UI of a React Native app is rendered.</h1>
<p>React Native allows you to build mobile apps using JavaScript and React, but the actual rendering of the user interface happens using native components of the respective platforms (iOS or Android).</p>
<ul>
<li><strong>Rendering with Native Views</strong>: React Native doesn’t directly translate your JavaScript code into native code (Java for Android or Objective C/Swift for iOS). Instead, it uses native views to render the UI. This is more efficient and provides a better user experience.</li>
<li><strong>Communication between Components</strong>: In React Native, you build your UI using components. Components communicate with each other through properties (props) and callbacks. This follows a one-way flow, making it easier to manage data flow in your app.</li>
<li><strong>Passing Data</strong>: If a parent component needs something from its child, it passes down a callback. Similarly, if a child component needs something from its parent, it receives a property. This unidirectional flow simplifies the design and maintenance of your app.</li>
<li><strong>Native Code Integration</strong>: While React Native encourages building the UI using its components, if you need to use a native component (e.g., MapView), you can integrate it seamlessly. React Native provides a bridge for communication between the JavaScript code and native components.</li>
<li><strong>How OS Understands JavaScript</strong>: The OS doesn’t directly understand JavaScript code. Instead, the React Native framework acts as an intermediary. It interprets and executes your JavaScript code, and through its bridge, it communicates with the native components to render the UI on the device.</li>
</ul>
<p>So far, so good. But how does the OS understand the JavaScript code?</p>
<h1>The Bundling Process</h1>
<p>A native mobile app will be developed using the programming language that is specific to that platform. When developing with React Native, you can pretty much get by without writing any Objective C/Java code — unless you are required to do something that is not covered by the library, such as integrating a payment provider that only offers SDKs for Android and iOS.</p>
<p>However, any React Native project contains an <code>ios</code> directory and an <code>android</code> one. These directories act as the entry points for each platform — they basically bootstrap React Native. They contain code that is specific to each platform, and here’s where your JS code is bridged for each platform.</p>
<p>In order to start your app, you’ll generally run <code>yarn android</code> or <code>yarn ios</code> and then you’ll wait until the app magically opens on your desired device. But what happens while you wait?</p>
<p>As soon as you type in one of those commands (which are <code>react-native run-android</code> and <code>react native run-ios</code>, respectively) you start up the packager. One such packager is <a href="https://facebook.github.io/metro/" target="_blank">Metro</a>. The packager takes all your JS code and puts it into a single file: <code>main.bundle.js</code>. When your app finally opens on your phone, the phone will look in the place that’s familiar to it: either the <code>android</code> or the <code>ios</code> directory. That’s the native entry point that I mentioned above. This native entry point will start the JavaScript virtual machine in a thread. The bundled code that is contained in <code>main.bundle.js</code> will then be run on this thread.</p>
<p>The code that’s now running within this JavaScript VM thread will communicate with the native thread using the React Native bridge.</p>
<p>Before jumping to the RN bridge and then analyzing the performance of RN apps, let’s understand how your phone is able to run JavaScript code.</p>
<h1>JavaScriptCore</h1>
<p><a href="https://github.com/WebKit/webkit/tree/main/Source/JavaScriptCore" target="_blank">JavaScriptCore</a> is a framework that allows JavaScript code to be run on mobile devices, for instance. On iOS devices, this framework is directly provided by the OS. Android devices don’t have the framework, so React Native bundles it along with the app itself. This increases the app size just a little bit, but it’ll barely matter in the end.</p>
<p>There’s one thing I want to point out that might save you some debugging time. JavaScriptCore is used for running JS code when the app is run on a device. However, if you choose to debug your app, the JS code is going to run inside Chrome. Chrome uses the V8 engine and uses WebSockets for communicating with the native code, so you’ll be able to see important info such as properly formatted logs and what network requests are being made. Just remember that there are differences between the V8 engine and JavaScriptCore — they are different environments and you might run into bugs that only happen when the debugger is attached but not when your app is run normally on your device!</p>
<h1>The React Native Bridge</h1>
<p>The RN bridge is written in Java/C++ and it allows the communication between the main thread of your app and the JavaScript thread. It uses a custom message-passing protocol to allow this communication to happen.</p>
<p>The JavaScript thread will decide what must be rendered on the screen. It will say to the main thread, “Hey, I need you to render a button and a text. Thanks.” It will use the bridge to say this. The message will be sent as a serialized JSON. But apart from what needs to be rendered on the screen, the message must also state where it will be rendered. Here’s where the shadow thread comes into play. The shadow thread is launched along with the JavaScript thread, and it helps to compute the positions of the views. The results are passed along in the aforementioned message, sent by means of the bridge to the main thread.</p>
<p>Any action that the user does on the UI will happen on the main thread. Tapping on a button, toggling a switch — any action must be serialized and sent by means of the bridge to the JavaScript thread. There’s where all the logic of your app happens.</p>
<h1>How Performance Is Affected</h1>
<p>Let’s review what we have covered so far. The user taps on a button. This action is understood by the main thread and passed as a message to the JavaScript thread. Here, some logic is handled, then the UI must change accordingly. The shadow thread decides where these changes happen, then the updates are sent as a message back to the native thread. Since the user won’t tap too rapidly on the screen, we won’t generally have any performance issues in normal usage scenarios — the bridge handles the communication pretty quickly.</p>
<p>What is cool about React Native (in comparison to other platforms such as Cordova) is that it doesn’t run its code inside of a <code>WebView</code>. It uses native views.</p>
<p>This advantage means that we’ll be able to develop smooth and fast apps that can run at 60 FPS. If you modify the state of a component that is very high in the tree (and you didn’t dedicate too much time to prevent useless re-renders), then the whole component tree will be re-rendered. This won’t be visible to the user in most cases. However, if those descendants are computationally expensive, then you’ll notice your app stuttering for a little bit.</p>
<h1>In a nutshell:</h1>
<p>When you run a RN app, all you JavaScript code is bundled together into a package called the JS Bundle. The Native Code is kept separately.</p>
<p>The Execution of React Native apps happens over three threads:</p>
<ul>
<li>The JavaScript thread: used by the JS Engine, to run the JS Bundle</li>
<li>The Native/UI thread: used to run the Native Modules and to handle operations like UI Rendering, user gesture events etc.</li>
<li>Additionally there is a 3rd thread called the shadow thread, which is used to calculate the Layout of Elements before rendering them on the host screen</li>
</ul>
<p>The Communication between the JS and Native Threads is carried over an entity called the bridge. When sending data through the bridge it has to be batched(optimized) and serialized as JSON. This bridge can only handle asynchronous communication.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Sw5UOASq_phL4Z7W4tfnIQ.png" alt="img"></p>
<p>Old Architecture in React native</p>
<p>JavaScriptCore: <em>It is the name of a JavaScript Engine, which is used by React Native to execute JS code.</em></p>
<p>Yoga: <em>It is the name of a Layout engine, which is used to calculate positions of UI elements for the user’s screen.</em></p>
<h1>JavaScript Interface (JSI)</h1>
<p>In the old architecture, React Native uses the Bridge Module to make communication possible between the JS and Native threads. Every time data is sent across the bridge, it has to be serialized as JSON. When the data is received on the other side it must be decoded as well.</p>
<p>This means that the JavaScript and Native worlds are unaware of each other <em>(ie. the JS thread cannot directly call a method on the Native thread)</em></p>
<p>Another important point to note is; messages send over the bridge are asynchronous in nature, which is a good thing for most use cases, but there are certain instances when JS code and native code needs to be in sync.</p>
<h1>Let’s take an example to better understand the bridge:</h1>
<p>If the JavaScript thread needs access to some native modules (eg. Bluetooth), it will need to send a message to the native thread. The JS thread will send a serialized JSON message to the bridge. The bridge will optimize this message and send it over to the native thread. The message will be decoded on the native thread, and eventually the required native code will be executed.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*g1NSRP7XNFK4H60hfTpiIQ.png" alt="img"></p>
<ul>
<li>JS thread prepares message for the Native Thread</li>
<li>It is serizlized as JSON before sending across the bridge</li>
<li>It is decoded when recieved on the other end of the bridge</li>
<li>Then the native thread executes the required native code</li>
</ul>
<h1>New Architecture</h1>
<p>However, in the New Architecture, the bridge is going to be replaced with a module called <strong><em>JavaScript Interface\</em></strong>, which is a lightweight, general-purpose layer, written in C++ that can be used by the JavaScript engine to directly invoke/call methods in the native realm.</p>
<h1>What does general-purpose mean?</h1>
<p>The current architecture uses the JavaScriptCore Engine. The bridge is only compatible with this particular engine. However, this is not the case for JSI. The JavaScript Interface will be decoupled from the Engine, which means that the new architecture enables the use of other JavaScript Engines like Chakra, v8, Hermes etc. Hence the term “<em>general-purpose”</em>.</p>
<h1>How can JSI enable JavaScript to directly call native methods?</h1>
<p>Through the JSI, Native methods will be exposed to JavaScript via C++ Host Objects. JavaScript can hold a reference to these objects. And can invoke the methods directly using that reference. This is similar to the web, where JavaScript code can hold a reference to any DOM element, and call methods on it. For Example: when you write:</p>
<blockquote>
<p><em>const container = document.createElement(‘div’);</em></p>
</blockquote>
<p>Here, the container is a JavaScript variable, but it holds a reference to a DOM element which was probably initialized in C++. If we call any method on the “container” variable, it will in turn call the method on the DOM element. The JSI will work similarly.</p>
<p>Unlike the bridge, the JSI will allow JavaScript code to hold a reference to Native Modules. And through the JSI, JavaScript can call methods on this reference directly.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*2yQoZjBjJyXAB_TTK_wtWg.png" alt="img"></p>
<ol>
<li>JavaScript has a direct reference to a native module</li>
<li>It calls a method on this native module, via the JavaScript Interface.</li>
</ol>
<p>To Sum it up, JSI will enable the use of other JavaScript Engines &amp; it will allow for complete interoperability between the threads, the JavaScript code could communicate with the native side directly from the JS thread. This will eliminate the need to serialize JSON messages and will fix the congestion and asynchronous issues on the bridge.</p>
<p>Another big advantage of the JSI is that it is written in C++. With the power of C++ React Native can target large number of Systems like Smart TVs, Watches etc.</p>
<h1>Fabric</h1>
<p>Fabric is the rendering system, which will replace the current UI Manager.</p>
<p>To understand the advantages of Fabric, first let’s look at how UI is currently rendered in React Native:</p>
<p>When your app is run, React executes your code and creates a ReactElementTree in JavaScript. Based on this tree, the Renderer creates a ReactShadowTree in C++.</p>
<p>This shadow Tree is used by the Layout Engine to calculate the positions of UI elements for the host screen. Once the results of the Layout calculation are available, the shadow tree is transformed into HostViewTree, which comprises of Native Elements. <em>(For example The ReactNative <view> element will be translated into ViewGroup in Android &amp; UIView in iOS respectively).</view></em></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Pv-4VyvyhonoO7Jv7r7VJA.png" alt="img"></p>
<p>ReactElementTree (JavaScript) -&gt; ReactShadowTree(C++) -&gt; HostViewTree(Native)</p>
<h1>Problems with this approach:</h1>
<p>As we know, all the communication between threads happens over the bridge. Which means slow transfer rates, and unnecessary copying of data.</p>
<p>For Example: If a ReactElementTree Node happens to be an <img>, then the consequent node of the ReactShadowTree will also be an image. However, this data will have to be duplicated and stored separately in both the nodes.</p>
<p>That’s not all. Since the JS and UI threads are not in sync, there are certain use cases when your app can seem laggy as it drops frames. (Example: Scrolling through a FlatList with a huge list of data)</p>
<h1>What is Fabric?</h1>
<p>According to the official <a href="https://reactnative.dev/docs/fabric-renderer" target="_blank">ReactNative documentation</a>,</p>
<blockquote>
<p><em>“Fabric is React Native’s new rendering system, a conceptual evolution of the legacy render system”</em></p>
</blockquote>
<p>As we have seen in the JSI section of this article, the JavaScript Interface will directly expose native methods to JavaScript, which also includes UI methods. As a result of this, the JS and UI threads can be in sync. This will improve performance for lists, navigation, gesture handling etc.</p>
<h1>What are the benefits of Fabric?</h1>
<p>With the new rendering system, user interactions such as scrolling, gestures, etc can be prioritized to be executed synchronously in the main thread or native thread. While other tasks such as API requests will be executed asynchronously.</p>
<p>That’s not all. The new Shadow Tree will be immutable, and it will be shared between the JS and UI threads, to allow straight interaction from both ends.</p>
<p>As we have seen, in the old architecture React Native has to maintain two hierarchies/DOM nodes. But since the shadow tree will now be shared among realms, it will help with reducing memory consumption as well.</p>
<h1>3. Turbo Modules</h1>
<p>In the old architecture, all the Native Modules used by JavaScript (e.g. Bluetooth, Geo Location, File Storage, etc) have to be initialized before the app is opened. This means, that even if the user doesn’t require a particular module, it still has to be initialized at start-up.</p>
<p>Turbo Modules are an enhancement over these old Native modules. As we have seen in the previous part of this article, now JavaScript will be able to hold a reference to these modules, which will allow JS Code to load each module only when it is required. This will significantly improve start-up time for React Native apps.</p>
<h1>4. CodeGen</h1>
<p>All this talk of Turbo Modules and Fabric sounds promising, but JavaScript is a Dynamically typed language, and JSI is written in C++, which is a Statically Typed Language. Consequently, there is a need to ensure smooth communication between the two.</p>
<p>That’s why the new architecture will also include a static type checker called CodeGen.</p>
<p>By using the typed JavaScript as the source of truth CodeGen will define interface elements used by Turbo Modules and Fabric. It will also generate more native code at build time, instead of run time.</p>
<h1>Summary</h1>
<p>If we combine all the changes, the new architecture will look like this:</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*94FA1Tqj13GnwQXhV0WlPw.png" alt="img"></p>
<p>Here the key highlights are:</p>
<p>• Bridge will be replaced by JSI</p>
<p>• Ability to swap the JavaScriptCore with other Engines</p>
<p>• Complete Interoperability between all threads</p>
<ul>
<li>Web-like Rendering system</li>
<li>Time-sensitive tasks can be executed synchronously</li>
<li>Lazy Loading of Turbo Modules</li>
<li>Static Type Checking for compatibility between JS and Native Side.</li>
</ul>
<p>this new structure will give React Native some powerful improvements.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月15日，开源日报第1075期：<br>
今日推荐开源项目：《apihub》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240115.jpg.pagespeed.ic.K5TJ9RANOO.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《apihub》传送门：<a href="https://github.com/hiteshchoudhary/apihub?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由： freeapi是一个单一源API 管理中心，用于学习任何编程语言中的api处理， 也用freeapi在Web和移动应用程序中构建自己前端作品集</p>
<p>官网直达：freeapi.app</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：React Native的新架构引入了重大变化 主要组件包括：</p>
<ul>
<li>
<p>JavaScript Interface (JSI): 一个轻量级层，使得JavaScript能够直接调用本地方法。这有助于在线程之间实现互操作性，消除了JSON序列化的需求，提高了性能</p>
</li>
<li>
<p>Fabric: 一种新的渲染系统，取代了UI Manager，实现了JS和UI线程之间的同步。它提升了用户交互（如滚动和手势）的性能，将同步执行重点放在面向用户的任务上</p>
</li>
<li>
<p>Turbo Modules: 对旧的Native Modules的改进，Turbo Modules使JavaScript能够在需要时加载模块，减少了React Native应用的启动时间</p>
</li>
<li>
<p>CodeGen: 一个静态类型检查器，确保JavaScript（动态类型）和C++（静态类型）之间的顺畅通信。它在构建时定义了Turbo Modules和Fabric使用的接口元素，而不是在运行时生成更多的本地代码</p>
</li>
</ul>
<hr>
<h1>ReactNative New vs. Old Architecture</h1>
<p>ReactNative Multi platform application Development.</p>
<p>This is a good time to understand what changes are taking place under the hood and how they might affect your React Native App.</p>
<blockquote>
<p>This article aims to cover the most important changes.</p>
</blockquote>
<ul>
<li><strong>JavaScript Interface(JSI)</strong></li>
<li><strong>Fabric</strong></li>
<li><strong>Turbo Modules</strong></li>
<li><strong>CodeGen</strong></li>
</ul>
<h1>Old Architecture</h1>
<p>Before we get to the new architecture, let’s recap how the old one works.</p>
<p>R<a href="https://reactnative.dev/" target="_blank">eact Native</a> is a JavaScript library that allows us to create mobile apps that will run on both Android and iOS. As they sell it, “Learn once, write anywhere.”</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1356/1*4p7zu_yWeTioHAhejneoCA.png" alt="img"></p>
<h1>How the UI of a React Native app is rendered.</h1>
<p>React Native allows you to build mobile apps using JavaScript and React, but the actual rendering of the user interface happens using native components of the respective platforms (iOS or Android).</p>
<ul>
<li><strong>Rendering with Native Views</strong>: React Native doesn’t directly translate your JavaScript code into native code (Java for Android or Objective C/Swift for iOS). Instead, it uses native views to render the UI. This is more efficient and provides a better user experience.</li>
<li><strong>Communication between Components</strong>: In React Native, you build your UI using components. Components communicate with each other through properties (props) and callbacks. This follows a one-way flow, making it easier to manage data flow in your app.</li>
<li><strong>Passing Data</strong>: If a parent component needs something from its child, it passes down a callback. Similarly, if a child component needs something from its parent, it receives a property. This unidirectional flow simplifies the design and maintenance of your app.</li>
<li><strong>Native Code Integration</strong>: While React Native encourages building the UI using its components, if you need to use a native component (e.g., MapView), you can integrate it seamlessly. React Native provides a bridge for communication between the JavaScript code and native components.</li>
<li><strong>How OS Understands JavaScript</strong>: The OS doesn’t directly understand JavaScript code. Instead, the React Native framework acts as an intermediary. It interprets and executes your JavaScript code, and through its bridge, it communicates with the native components to render the UI on the device.</li>
</ul>
<p>So far, so good. But how does the OS understand the JavaScript code?</p>
<h1>The Bundling Process</h1>
<p>A native mobile app will be developed using the programming language that is specific to that platform. When developing with React Native, you can pretty much get by without writing any Objective C/Java code — unless you are required to do something that is not covered by the library, such as integrating a payment provider that only offers SDKs for Android and iOS.</p>
<p>However, any React Native project contains an <code>ios</code> directory and an <code>android</code> one. These directories act as the entry points for each platform — they basically bootstrap React Native. They contain code that is specific to each platform, and here’s where your JS code is bridged for each platform.</p>
<p>In order to start your app, you’ll generally run <code>yarn android</code> or <code>yarn ios</code> and then you’ll wait until the app magically opens on your desired device. But what happens while you wait?</p>
<p>As soon as you type in one of those commands (which are <code>react-native run-android</code> and <code>react native run-ios</code>, respectively) you start up the packager. One such packager is <a href="https://facebook.github.io/metro/" target="_blank">Metro</a>. The packager takes all your JS code and puts it into a single file: <code>main.bundle.js</code>. When your app finally opens on your phone, the phone will look in the place that’s familiar to it: either the <code>android</code> or the <code>ios</code> directory. That’s the native entry point that I mentioned above. This native entry point will start the JavaScript virtual machine in a thread. The bundled code that is contained in <code>main.bundle.js</code> will then be run on this thread.</p>
<p>The code that’s now running within this JavaScript VM thread will communicate with the native thread using the React Native bridge.</p>
<p>Before jumping to the RN bridge and then analyzing the performance of RN apps, let’s understand how your phone is able to run JavaScript code.</p>
<h1>JavaScriptCore</h1>
<p><a href="https://github.com/WebKit/webkit/tree/main/Source/JavaScriptCore" target="_blank">JavaScriptCore</a> is a framework that allows JavaScript code to be run on mobile devices, for instance. On iOS devices, this framework is directly provided by the OS. Android devices don’t have the framework, so React Native bundles it along with the app itself. This increases the app size just a little bit, but it’ll barely matter in the end.</p>
<p>There’s one thing I want to point out that might save you some debugging time. JavaScriptCore is used for running JS code when the app is run on a device. However, if you choose to debug your app, the JS code is going to run inside Chrome. Chrome uses the V8 engine and uses WebSockets for communicating with the native code, so you’ll be able to see important info such as properly formatted logs and what network requests are being made. Just remember that there are differences between the V8 engine and JavaScriptCore — they are different environments and you might run into bugs that only happen when the debugger is attached but not when your app is run normally on your device!</p>
<h1>The React Native Bridge</h1>
<p>The RN bridge is written in Java/C++ and it allows the communication between the main thread of your app and the JavaScript thread. It uses a custom message-passing protocol to allow this communication to happen.</p>
<p>The JavaScript thread will decide what must be rendered on the screen. It will say to the main thread, “Hey, I need you to render a button and a text. Thanks.” It will use the bridge to say this. The message will be sent as a serialized JSON. But apart from what needs to be rendered on the screen, the message must also state where it will be rendered. Here’s where the shadow thread comes into play. The shadow thread is launched along with the JavaScript thread, and it helps to compute the positions of the views. The results are passed along in the aforementioned message, sent by means of the bridge to the main thread.</p>
<p>Any action that the user does on the UI will happen on the main thread. Tapping on a button, toggling a switch — any action must be serialized and sent by means of the bridge to the JavaScript thread. There’s where all the logic of your app happens.</p>
<h1>How Performance Is Affected</h1>
<p>Let’s review what we have covered so far. The user taps on a button. This action is understood by the main thread and passed as a message to the JavaScript thread. Here, some logic is handled, then the UI must change accordingly. The shadow thread decides where these changes happen, then the updates are sent as a message back to the native thread. Since the user won’t tap too rapidly on the screen, we won’t generally have any performance issues in normal usage scenarios — the bridge handles the communication pretty quickly.</p>
<p>What is cool about React Native (in comparison to other platforms such as Cordova) is that it doesn’t run its code inside of a <code>WebView</code>. It uses native views.</p>
<p>This advantage means that we’ll be able to develop smooth and fast apps that can run at 60 FPS. If you modify the state of a component that is very high in the tree (and you didn’t dedicate too much time to prevent useless re-renders), then the whole component tree will be re-rendered. This won’t be visible to the user in most cases. However, if those descendants are computationally expensive, then you’ll notice your app stuttering for a little bit.</p>
<h1>In a nutshell:</h1>
<p>When you run a RN app, all you JavaScript code is bundled together into a package called the JS Bundle. The Native Code is kept separately.</p>
<p>The Execution of React Native apps happens over three threads:</p>
<ul>
<li>The JavaScript thread: used by the JS Engine, to run the JS Bundle</li>
<li>The Native/UI thread: used to run the Native Modules and to handle operations like UI Rendering, user gesture events etc.</li>
<li>Additionally there is a 3rd thread called the shadow thread, which is used to calculate the Layout of Elements before rendering them on the host screen</li>
</ul>
<p>The Communication between the JS and Native Threads is carried over an entity called the bridge. When sending data through the bridge it has to be batched(optimized) and serialized as JSON. This bridge can only handle asynchronous communication.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Sw5UOASq_phL4Z7W4tfnIQ.png" alt="img"></p>
<p>Old Architecture in React native</p>
<p>JavaScriptCore: <em>It is the name of a JavaScript Engine, which is used by React Native to execute JS code.</em></p>
<p>Yoga: <em>It is the name of a Layout engine, which is used to calculate positions of UI elements for the user’s screen.</em></p>
<h1>JavaScript Interface (JSI)</h1>
<p>In the old architecture, React Native uses the Bridge Module to make communication possible between the JS and Native threads. Every time data is sent across the bridge, it has to be serialized as JSON. When the data is received on the other side it must be decoded as well.</p>
<p>This means that the JavaScript and Native worlds are unaware of each other <em>(ie. the JS thread cannot directly call a method on the Native thread)</em></p>
<p>Another important point to note is; messages send over the bridge are asynchronous in nature, which is a good thing for most use cases, but there are certain instances when JS code and native code needs to be in sync.</p>
<h1>Let’s take an example to better understand the bridge:</h1>
<p>If the JavaScript thread needs access to some native modules (eg. Bluetooth), it will need to send a message to the native thread. The JS thread will send a serialized JSON message to the bridge. The bridge will optimize this message and send it over to the native thread. The message will be decoded on the native thread, and eventually the required native code will be executed.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*g1NSRP7XNFK4H60hfTpiIQ.png" alt="img"></p>
<ul>
<li>JS thread prepares message for the Native Thread</li>
<li>It is serizlized as JSON before sending across the bridge</li>
<li>It is decoded when recieved on the other end of the bridge</li>
<li>Then the native thread executes the required native code</li>
</ul>
<h1>New Architecture</h1>
<p>However, in the New Architecture, the bridge is going to be replaced with a module called <strong><em>JavaScript Interface\</em></strong>, which is a lightweight, general-purpose layer, written in C++ that can be used by the JavaScript engine to directly invoke/call methods in the native realm.</p>
<h1>What does general-purpose mean?</h1>
<p>The current architecture uses the JavaScriptCore Engine. The bridge is only compatible with this particular engine. However, this is not the case for JSI. The JavaScript Interface will be decoupled from the Engine, which means that the new architecture enables the use of other JavaScript Engines like Chakra, v8, Hermes etc. Hence the term “<em>general-purpose”</em>.</p>
<h1>How can JSI enable JavaScript to directly call native methods?</h1>
<p>Through the JSI, Native methods will be exposed to JavaScript via C++ Host Objects. JavaScript can hold a reference to these objects. And can invoke the methods directly using that reference. This is similar to the web, where JavaScript code can hold a reference to any DOM element, and call methods on it. For Example: when you write:</p>
<blockquote>
<p><em>const container = document.createElement(‘div’);</em></p>
</blockquote>
<p>Here, the container is a JavaScript variable, but it holds a reference to a DOM element which was probably initialized in C++. If we call any method on the “container” variable, it will in turn call the method on the DOM element. The JSI will work similarly.</p>
<p>Unlike the bridge, the JSI will allow JavaScript code to hold a reference to Native Modules. And through the JSI, JavaScript can call methods on this reference directly.</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*2yQoZjBjJyXAB_TTK_wtWg.png" alt="img"></p>
<ol>
<li>JavaScript has a direct reference to a native module</li>
<li>It calls a method on this native module, via the JavaScript Interface.</li>
</ol>
<p>To Sum it up, JSI will enable the use of other JavaScript Engines &amp; it will allow for complete interoperability between the threads, the JavaScript code could communicate with the native side directly from the JS thread. This will eliminate the need to serialize JSON messages and will fix the congestion and asynchronous issues on the bridge.</p>
<p>Another big advantage of the JSI is that it is written in C++. With the power of C++ React Native can target large number of Systems like Smart TVs, Watches etc.</p>
<h1>Fabric</h1>
<p>Fabric is the rendering system, which will replace the current UI Manager.</p>
<p>To understand the advantages of Fabric, first let’s look at how UI is currently rendered in React Native:</p>
<p>When your app is run, React executes your code and creates a ReactElementTree in JavaScript. Based on this tree, the Renderer creates a ReactShadowTree in C++.</p>
<p>This shadow Tree is used by the Layout Engine to calculate the positions of UI elements for the host screen. Once the results of the Layout calculation are available, the shadow tree is transformed into HostViewTree, which comprises of Native Elements. <em>(For example The ReactNative <view> element will be translated into ViewGroup in Android &amp; UIView in iOS respectively).</view></em></p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*Pv-4VyvyhonoO7Jv7r7VJA.png" alt="img"></p>
<p>ReactElementTree (JavaScript) -&gt; ReactShadowTree(C++) -&gt; HostViewTree(Native)</p>
<h1>Problems with this approach:</h1>
<p>As we know, all the communication between threads happens over the bridge. Which means slow transfer rates, and unnecessary copying of data.</p>
<p>For Example: If a ReactElementTree Node happens to be an <img>, then the consequent node of the ReactShadowTree will also be an image. However, this data will have to be duplicated and stored separately in both the nodes.</p>
<p>That’s not all. Since the JS and UI threads are not in sync, there are certain use cases when your app can seem laggy as it drops frames. (Example: Scrolling through a FlatList with a huge list of data)</p>
<h1>What is Fabric?</h1>
<p>According to the official <a href="https://reactnative.dev/docs/fabric-renderer" target="_blank">ReactNative documentation</a>,</p>
<blockquote>
<p><em>“Fabric is React Native’s new rendering system, a conceptual evolution of the legacy render system”</em></p>
</blockquote>
<p>As we have seen in the JSI section of this article, the JavaScript Interface will directly expose native methods to JavaScript, which also includes UI methods. As a result of this, the JS and UI threads can be in sync. This will improve performance for lists, navigation, gesture handling etc.</p>
<h1>What are the benefits of Fabric?</h1>
<p>With the new rendering system, user interactions such as scrolling, gestures, etc can be prioritized to be executed synchronously in the main thread or native thread. While other tasks such as API requests will be executed asynchronously.</p>
<p>That’s not all. The new Shadow Tree will be immutable, and it will be shared between the JS and UI threads, to allow straight interaction from both ends.</p>
<p>As we have seen, in the old architecture React Native has to maintain two hierarchies/DOM nodes. But since the shadow tree will now be shared among realms, it will help with reducing memory consumption as well.</p>
<h1>3. Turbo Modules</h1>
<p>In the old architecture, all the Native Modules used by JavaScript (e.g. Bluetooth, Geo Location, File Storage, etc) have to be initialized before the app is opened. This means, that even if the user doesn’t require a particular module, it still has to be initialized at start-up.</p>
<p>Turbo Modules are an enhancement over these old Native modules. As we have seen in the previous part of this article, now JavaScript will be able to hold a reference to these modules, which will allow JS Code to load each module only when it is required. This will significantly improve start-up time for React Native apps.</p>
<h1>4. CodeGen</h1>
<p>All this talk of Turbo Modules and Fabric sounds promising, but JavaScript is a Dynamically typed language, and JSI is written in C++, which is a Statically Typed Language. Consequently, there is a need to ensure smooth communication between the two.</p>
<p>That’s why the new architecture will also include a static type checker called CodeGen.</p>
<p>By using the typed JavaScript as the source of truth CodeGen will define interface elements used by Turbo Modules and Fabric. It will also generate more native code at build time, instead of run time.</p>
<h1>Summary</h1>
<p>If we combine all the changes, the new architecture will look like this:</p>
<p><img src="https://miro.medium.com/v2/resize:fit:1400/1*94FA1Tqj13GnwQXhV0WlPw.png" alt="img"></p>
<p>Here the key highlights are:</p>
<p>• Bridge will be replaced by JSI</p>
<p>• Ability to swap the JavaScriptCore with other Engines</p>
<p>• Complete Interoperability between all threads</p>
<ul>
<li>Web-like Rendering system</li>
<li>Time-sensitive tasks can be executed synchronously</li>
<li>Lazy Loading of Turbo Modules</li>
<li>Static Type Checking for compatibility between JS and Native Side.</li>
</ul>
<p>this new structure will give React Native some powerful improvements.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240115.jpg.pagespeed.ic.K5TJ9RANOO.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240115.jpg.pagespeed.ic.K5TJ9RANOO.jpg" medium="image"/>
    <pubDate>Mon, 15 Jan 2024 13:39:42 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1074期：前端框架：《htmx》]]></title>
    <link>https://openingsource.org/11511/</link>
    <itunes:title><![CDATA[开源日报第1074期：前端框架：《htmx》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月14日，开源日报第1074期：<br>
今日推荐开源项目：《htmx》<br>
今日推荐英文原文：《Dynamic Imports in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240114.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《htmx》传送门：<a href="https://github.com/bigskysoftware/htmx?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由：htmx在2023年JavaScript Rising Stars“前端框架”类别中获得第二名，仅次于React；<br>
htmx使您可以直接在HTML中使用属性访问AJAX、CSS过渡、WebSockets和服务器发送事件；<br>
htmx非常小巧（~14k min.gz'd），无需依赖，可扩展，兼容IE11，并且与React相比减少了67%的代码基大小</p>
<p>官方直达 htmx.org</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shubham3480/dynamic-imports-in-react-3e3e7ad1d210" target="_blank">Dynamic Imports in React</a></p>
<p>推荐理由：该文章讨论了在React中的动态导入，允许在运行时加载JavaScript模块，从而提高应用程序的性能和加载速度</p>
<p>import()函数返回一个Promise，允许异步加载模块，使用在例如代码拆分和懒加载，确保只在需要时加载必要的代码<br>
文章还介绍了React.lazy()，用于将动态导入呈现为常规组件，以及React.Suspense()，用于在加载完成之前有条件地暂停组件的呈现，通过示例和用例说明了实际实现的方法</p>
<hr>
<h1>Dynamic Imports in React</h1>
<p>Dynamic imports in React allow you to dynamically load JavaScript modules at runtime, which can significantly improve your application’s performance and load times. This technique is particularly useful for code splitting and lazy loading, ensuring that only the necessary code is loaded when needed.</p>
<p><strong><em>The\</em></strong> <code>***import()\***</code> <strong><em>function returns a Promise that resolves to the module you want to use dynamic import.\</em></strong></p>
<p><strong><em>A normal import in JavaScript (using the\</em></strong> <code>***import\***</code> <strong><em>statement) does not return a Promise. It's a synchronous operation and returns the exported values from the imported module.\</em></strong></p>
<pre><code>import React from 'react';
const AnotherComponent = () =&gt; {
return &lt;div&gt;Another component loaded dynamically!&lt;/div&gt;;
};
export default AnotherComponent;
const MyComponent = () =&gt; {
const [importedComponent, setImportedComponent] = useState(null);
useEffect(() =&gt; {
const importComponent = async () =&gt; {
const module = await import('./AnotherComponent');
const AnotherComponent = module.default;
setImportedComponent(&lt;AnotherComponent /&gt;);
};
importComponent();
}, []);
return (
&lt;div&gt;
{importedComponent}
&lt;div&gt;This is my functional component!&lt;/div&gt;
&lt;/div&gt;
);
};
///Another Way is ...///
const MyComponent = async () =&gt; {
const module = import('./AnotherComponent');
const {AnotherComponent} = await module;
return &lt;AnotherComponent /&gt;;
};
export default MyComponent;
// when u console the export module { default: ""} is contains the all the
// key value pair of the functions name as the key and the value as the
// definition, with a default key if something is imported as default it
// will be it's value else it will be undefined</code></pre>
<h2>When To Use Dynamic Imports?</h2>
<p>Although dynamic imports are a great way to improve the performance of your React applications, there are better use cases for using dynamic imports in React apps.</p>
<ul>
<li>Code Modulation: Dynamic imports can be used when there’s a need for code modulation and fetching data from a server. An example can be found in server-side rendered applications.</li>
<li>Dynamic imports can be used when components are not needed when an application is still loading.</li>
<li>Conditional imports are an excellent use case for dynamic imports; here, a module or component is only imported on pages where they’re needed and when needed in an application.</li>
</ul>
<h2>React.lazy()</h2>
<p>The React.lazy() function allows you to render a dynamic import as a regular component. Basically, React.lazy() makes a call to a dynamic import and returns a promise.</p>
<pre><code>import React, { lazy } from "react";
const Blog = React.lazy(() =&gt;
import('./Pages/Blog'));</code></pre>
<h1>React.Suspense()</h1>
<p>React.Suspense allows React developers to conditionally suspend the rendering of a React component until it is loaded. React.Suspense provides a fallback prop that accepts a React element which either is a JSX snippet or a React component.</p>
<p>When users visit a page using React dynamic imports, oftentimes they experience a blank page screen, this is done while the application loads the module, this can also cause errors as a result of slow internet connectivity for some users. React.lazy() and React.Suspense when combined solves this issue for users.</p>
<p>To do this, use React.Suspense to suspend the rendering of a component until all dependencies are lazy-loaded, React.Suspense will also display your fallback UI for the user.</p>
<pre><code>import React, { Suspense } from 'react';
const LazyComponent = React.lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月14日，开源日报第1074期：<br>
今日推荐开源项目：《htmx》<br>
今日推荐英文原文：《Dynamic Imports in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240114.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《htmx》传送门：<a href="https://github.com/bigskysoftware/htmx?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由：htmx在2023年JavaScript Rising Stars“前端框架”类别中获得第二名，仅次于React；<br>
htmx使您可以直接在HTML中使用属性访问AJAX、CSS过渡、WebSockets和服务器发送事件；<br>
htmx非常小巧（~14k min.gz'd），无需依赖，可扩展，兼容IE11，并且与React相比减少了67%的代码基大小</p>
<p>官方直达 htmx.org</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shubham3480/dynamic-imports-in-react-3e3e7ad1d210" target="_blank">Dynamic Imports in React</a></p>
<p>推荐理由：该文章讨论了在React中的动态导入，允许在运行时加载JavaScript模块，从而提高应用程序的性能和加载速度</p>
<p>import()函数返回一个Promise，允许异步加载模块，使用在例如代码拆分和懒加载，确保只在需要时加载必要的代码<br>
文章还介绍了React.lazy()，用于将动态导入呈现为常规组件，以及React.Suspense()，用于在加载完成之前有条件地暂停组件的呈现，通过示例和用例说明了实际实现的方法</p>
<hr>
<h1>Dynamic Imports in React</h1>
<p>Dynamic imports in React allow you to dynamically load JavaScript modules at runtime, which can significantly improve your application’s performance and load times. This technique is particularly useful for code splitting and lazy loading, ensuring that only the necessary code is loaded when needed.</p>
<p><strong><em>The\</em></strong> <code>***import()\***</code> <strong><em>function returns a Promise that resolves to the module you want to use dynamic import.\</em></strong></p>
<p><strong><em>A normal import in JavaScript (using the\</em></strong> <code>***import\***</code> <strong><em>statement) does not return a Promise. It's a synchronous operation and returns the exported values from the imported module.\</em></strong></p>
<pre><code>import React from 'react';
const AnotherComponent = () =&gt; {
return &lt;div&gt;Another component loaded dynamically!&lt;/div&gt;;
};
export default AnotherComponent;
const MyComponent = () =&gt; {
const [importedComponent, setImportedComponent] = useState(null);
useEffect(() =&gt; {
const importComponent = async () =&gt; {
const module = await import('./AnotherComponent');
const AnotherComponent = module.default;
setImportedComponent(&lt;AnotherComponent /&gt;);
};
importComponent();
}, []);
return (
&lt;div&gt;
{importedComponent}
&lt;div&gt;This is my functional component!&lt;/div&gt;
&lt;/div&gt;
);
};
///Another Way is ...///
const MyComponent = async () =&gt; {
const module = import('./AnotherComponent');
const {AnotherComponent} = await module;
return &lt;AnotherComponent /&gt;;
};
export default MyComponent;
// when u console the export module { default: ""} is contains the all the
// key value pair of the functions name as the key and the value as the
// definition, with a default key if something is imported as default it
// will be it's value else it will be undefined</code></pre>
<h2>When To Use Dynamic Imports?</h2>
<p>Although dynamic imports are a great way to improve the performance of your React applications, there are better use cases for using dynamic imports in React apps.</p>
<ul>
<li>Code Modulation: Dynamic imports can be used when there’s a need for code modulation and fetching data from a server. An example can be found in server-side rendered applications.</li>
<li>Dynamic imports can be used when components are not needed when an application is still loading.</li>
<li>Conditional imports are an excellent use case for dynamic imports; here, a module or component is only imported on pages where they’re needed and when needed in an application.</li>
</ul>
<h2>React.lazy()</h2>
<p>The React.lazy() function allows you to render a dynamic import as a regular component. Basically, React.lazy() makes a call to a dynamic import and returns a promise.</p>
<pre><code>import React, { lazy } from "react";
const Blog = React.lazy(() =&gt;
import('./Pages/Blog'));</code></pre>
<h1>React.Suspense()</h1>
<p>React.Suspense allows React developers to conditionally suspend the rendering of a React component until it is loaded. React.Suspense provides a fallback prop that accepts a React element which either is a JSX snippet or a React component.</p>
<p>When users visit a page using React dynamic imports, oftentimes they experience a blank page screen, this is done while the application loads the module, this can also cause errors as a result of slow internet connectivity for some users. React.lazy() and React.Suspense when combined solves this issue for users.</p>
<p>To do this, use React.Suspense to suspend the rendering of a component until all dependencies are lazy-loaded, React.Suspense will also display your fallback UI for the user.</p>
<pre><code>import React, { Suspense } from 'react';
const LazyComponent = React.lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月14日，开源日报第1074期：<br>
今日推荐开源项目：《htmx》<br>
今日推荐英文原文：《Dynamic Imports in React》<br>
<img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240114.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《htmx》传送门：<a href="https://github.com/bigskysoftware/htmx?utm_source=gold_browser_extension" target="_blank">项目链接</a> </p>
<p>推荐理由：htmx在2023年JavaScript Rising Stars“前端框架”类别中获得第二名，仅次于React；<br>
htmx使您可以直接在HTML中使用属性访问AJAX、CSS过渡、WebSockets和服务器发送事件；<br>
htmx非常小巧（~14k min.gz'd），无需依赖，可扩展，兼容IE11，并且与React相比减少了67%的代码基大小</p>
<p>官方直达 htmx.org</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shubham3480/dynamic-imports-in-react-3e3e7ad1d210" target="_blank">Dynamic Imports in React</a></p>
<p>推荐理由：该文章讨论了在React中的动态导入，允许在运行时加载JavaScript模块，从而提高应用程序的性能和加载速度</p>
<p>import()函数返回一个Promise，允许异步加载模块，使用在例如代码拆分和懒加载，确保只在需要时加载必要的代码<br>
文章还介绍了React.lazy()，用于将动态导入呈现为常规组件，以及React.Suspense()，用于在加载完成之前有条件地暂停组件的呈现，通过示例和用例说明了实际实现的方法</p>
<hr>
<h1>Dynamic Imports in React</h1>
<p>Dynamic imports in React allow you to dynamically load JavaScript modules at runtime, which can significantly improve your application’s performance and load times. This technique is particularly useful for code splitting and lazy loading, ensuring that only the necessary code is loaded when needed.</p>
<p><strong><em>The\</em></strong> <code>***import()\***</code> <strong><em>function returns a Promise that resolves to the module you want to use dynamic import.\</em></strong></p>
<p><strong><em>A normal import in JavaScript (using the\</em></strong> <code>***import\***</code> <strong><em>statement) does not return a Promise. It's a synchronous operation and returns the exported values from the imported module.\</em></strong></p>
<pre><code>import React from 'react';
const AnotherComponent = () =&gt; {
return &lt;div&gt;Another component loaded dynamically!&lt;/div&gt;;
};
export default AnotherComponent;
const MyComponent = () =&gt; {
const [importedComponent, setImportedComponent] = useState(null);
useEffect(() =&gt; {
const importComponent = async () =&gt; {
const module = await import('./AnotherComponent');
const AnotherComponent = module.default;
setImportedComponent(&lt;AnotherComponent /&gt;);
};
importComponent();
}, []);
return (
&lt;div&gt;
{importedComponent}
&lt;div&gt;This is my functional component!&lt;/div&gt;
&lt;/div&gt;
);
};
///Another Way is ...///
const MyComponent = async () =&gt; {
const module = import('./AnotherComponent');
const {AnotherComponent} = await module;
return &lt;AnotherComponent /&gt;;
};
export default MyComponent;
// when u console the export module { default: ""} is contains the all the
// key value pair of the functions name as the key and the value as the
// definition, with a default key if something is imported as default it
// will be it's value else it will be undefined</code></pre>
<h2>When To Use Dynamic Imports?</h2>
<p>Although dynamic imports are a great way to improve the performance of your React applications, there are better use cases for using dynamic imports in React apps.</p>
<ul>
<li>Code Modulation: Dynamic imports can be used when there’s a need for code modulation and fetching data from a server. An example can be found in server-side rendered applications.</li>
<li>Dynamic imports can be used when components are not needed when an application is still loading.</li>
<li>Conditional imports are an excellent use case for dynamic imports; here, a module or component is only imported on pages where they’re needed and when needed in an application.</li>
</ul>
<h2>React.lazy()</h2>
<p>The React.lazy() function allows you to render a dynamic import as a regular component. Basically, React.lazy() makes a call to a dynamic import and returns a promise.</p>
<pre><code>import React, { lazy } from "react";
const Blog = React.lazy(() =&gt;
import('./Pages/Blog'));</code></pre>
<h1>React.Suspense()</h1>
<p>React.Suspense allows React developers to conditionally suspend the rendering of a React component until it is loaded. React.Suspense provides a fallback prop that accepts a React element which either is a JSX snippet or a React component.</p>
<p>When users visit a page using React dynamic imports, oftentimes they experience a blank page screen, this is done while the application loads the module, this can also cause errors as a result of slow internet connectivity for some users. React.lazy() and React.Suspense when combined solves this issue for users.</p>
<p>To do this, use React.Suspense to suspend the rendering of a component until all dependencies are lazy-loaded, React.Suspense will also display your fallback UI for the user.</p>
<pre><code>import React, { Suspense } from 'react';
const LazyComponent = React.lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240114.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240114.jpg" medium="image"/>
    <pubDate>Sun, 14 Jan 2024 12:01:36 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1073期：构建Web、移动和桌面应用程序：《meteor》]]></title>
    <link>https://openingsource.org/11508/</link>
    <itunes:title><![CDATA[开源日报第1073期：构建Web、移动和桌面应用程序：《meteor》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
开源日报第1073期：构建Web、移动和桌面应用程序：《meteor》<br>
2024年1月13日，开源日报第1073期：<br>
今日推荐开源项目：《meteor》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240113.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meteor》传送门：<a href="https://github.com/meteor/meteor?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由：Meteor.js是一个用于构建Web、移动和桌面应用程序的开源平台，api文档非常详细</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：该文章强调了在React中使用useEffect()钩子时常见的陷阱，useEffect()钩子经常被错误使用，导致代码效率低下且难以阅读，举了一些例子详细解释</p>
<hr>
<h1>React: You are Using useEffect() Wrong, Do This Instead</h1>
<p>Once one decides to move forward with learning <strong>React</strong>, hooks are among the first things to learn (and to be frustrated with). Hooks are essential parts of <strong>React</strong>, as they were created to solve several problems that appeared in the first couple of versions of <strong>React</strong>, when every rendering was done inside the component’s lifecycle functions, such as <strong>componentDidMount()</strong>, <strong>componentWillMout(),</strong> <strong>componentDidUpdate()</strong>.</p>
<p>That said, the first hooks everyone starts touching are <strong>useState()</strong> and <strong>useEffect()</strong>. The first is used for state management and control when the component should be rendered again, while the second behaves somewhat similarly to the lifecycle functions stated above.</p>
<p>The <strong>useEffect()</strong> hook can receive two outputs: the first is a callback function, while the second is optional and defines when this hook should be called.</p>
<pre><code>  useEffect((prevProps) =&gt; { //prevProps are optional and has some specific uses. Compare with what happens with the lifecycle functions.
//Custom function content….
custom function content…
return () =&gt; {
// Code to run when the component is unmounted or when dependencies change
// It helps in avoiding memory leaks and unexpected behavior
};
}, [dependencies in array form]);</code></pre>
<p>One caveat that gets a lot of beginners is how the second parameter works. Here is a resume:</p>
<blockquote>
<p>Case A: If nothing is added, then <strong>useEffect</strong> will run at every change of state inside the current component.</p>
<p>Case B: If an empty array is added ([]), then the <strong>useEffect</strong> will run only once when the component is mounted.</p>
<p>Case C: If some array is provided ([state]), then <strong>useEffect</strong> will run every time the state changes</p>
<p>Case C*: If some array is provided ([state1, state2, ….], <strong>useEffect</strong> will run every time <strong>ANY</strong> of these states changes.</p>
</blockquote>
<p>Now that we recalled how <strong>useEffect</strong> works, it’s time to state some of the drawbacks of using it as most people do.</p>
<p>The main idea of the <strong>useEffect</strong> hook is to synchronize data transfer with external <strong>APIs</strong> or another system, like when you are accessing a database, or waiting for an <strong>HTTP</strong> request to complete. The trouble is that we tend to use this hook in every situation possible inside our code, especially <strong>Case A</strong> and C* listed above, and the code can become incredibly unreadable with just a couple of lines of code, including triggering a loop if you change one of the states in the dependency array during the process.</p>
<p>This can make your code inefficient too, as useEffect works as if you were stepping aside to run some code and then coming back to the main thread. This is inefficient.</p>
<p>Great, now you know why to avoid it. But how?</p>
<p>Let’s talk about each one of the Cases in detail:</p>
<blockquote>
<p>Case A — No dependency array: This one should be abolished from your code, as it will certainly trigger unnecessary calculations every time a state changes. In this case, you should specify which states really should trigger this function using a dependency array.</p>
<p>Case B — Empty dependency array: This is one of the good ones, the only recommendation that I can provide is to keep just one of these for each component and wrap its content into a function.</p>
<p>Case C — One dependency state only. It’s ok to use if you are processing external data. Otherwise, you should change it to the solution I will provide below.</p>
<p>Case C* — Multiple dependency states in the same <strong>useEffect</strong>. This is the one I consider the most troublesome. I recommend you try to untangle the states into different useEffect hooks before anything, as it makes your code very unreadable.</p>
</blockquote>
<p>Now for the solution that I promised. Let’s consider these two Components (Parent and Child):</p>
<pre><code>// ParentComponent.js
import React, { useState, useEffect } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
useEffect(() =&gt; {
setMessage(`Button clicked ${count} times!`);
},[count]}
return (
&lt;ChildComponent count={count} message={message} setCount={setCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, setCount }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={() =&gt; {setCount(count+1)}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>Now let’s explain what is happening here:</p>
<blockquote>
<p>1 — Once the user clicks the button on the <strong>ChildComponent</strong>, we change the state “count” by incrementing 1. It will take one render loop to happen and change the state.</p>
<p>2 — Once the state “count” changes, the child component will be rendered again and it will also trigger the <strong>useEffect</strong> hook on both components, which will trigger the change in the “message” state. Again, it will only happen in the next render.</p>
<p>3 — When the “message” state changes, then another render happens in the components changing the message.</p>
<p>In this case, we ended up having two renders. It may now seem much, but it can grow at a large scale once you have more states at play.</p>
<p>Now look what happens when we make the following changes in the components:</p>
</blockquote>
<pre><code>// ParentComponent.js
import React, { useState } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
const incrementCount = () =&gt; {
setCount(count + 1);
setMessage(`Button clicked ${count + 1} times!`);
}
return (
&lt;ChildComponent count={count} message={message}
callbackFunction={incrementCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, callbackFunction }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={callbackFunction}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>We changed the code to pass a <strong>Callback Function</strong> to the Child Component. You may notice that:</p>
<blockquote>
<p>· We don’t have a <strong>useEffect</strong> anymore defined on the Parent Component. This makes the code easier to read, as we can understand our code as, let’s say, more linear and single-threaded than the original.</p>
<p>· We don’t have to wait for two render cycles to display our final message, or worse, render both components two times.</p>
<p>· We can separate concerns between components, making them more reusable and easier to read or adapt, as we can put whatever we want in the callback function.</p>
<p>· Both the state changes at the same time, avoiding the chaining of <strong>useEffect</strong> statements.</p>
</blockquote>
<p>And that’s it for today. Thank you for reading!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
开源日报第1073期：构建Web、移动和桌面应用程序：《meteor》<br>
2024年1月13日，开源日报第1073期：<br>
今日推荐开源项目：《meteor》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240113.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meteor》传送门：<a href="https://github.com/meteor/meteor?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由：Meteor.js是一个用于构建Web、移动和桌面应用程序的开源平台，api文档非常详细</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：该文章强调了在React中使用useEffect()钩子时常见的陷阱，useEffect()钩子经常被错误使用，导致代码效率低下且难以阅读，举了一些例子详细解释</p>
<hr>
<h1>React: You are Using useEffect() Wrong, Do This Instead</h1>
<p>Once one decides to move forward with learning <strong>React</strong>, hooks are among the first things to learn (and to be frustrated with). Hooks are essential parts of <strong>React</strong>, as they were created to solve several problems that appeared in the first couple of versions of <strong>React</strong>, when every rendering was done inside the component’s lifecycle functions, such as <strong>componentDidMount()</strong>, <strong>componentWillMout(),</strong> <strong>componentDidUpdate()</strong>.</p>
<p>That said, the first hooks everyone starts touching are <strong>useState()</strong> and <strong>useEffect()</strong>. The first is used for state management and control when the component should be rendered again, while the second behaves somewhat similarly to the lifecycle functions stated above.</p>
<p>The <strong>useEffect()</strong> hook can receive two outputs: the first is a callback function, while the second is optional and defines when this hook should be called.</p>
<pre><code>  useEffect((prevProps) =&gt; { //prevProps are optional and has some specific uses. Compare with what happens with the lifecycle functions.
//Custom function content….
custom function content…
return () =&gt; {
// Code to run when the component is unmounted or when dependencies change
// It helps in avoiding memory leaks and unexpected behavior
};
}, [dependencies in array form]);</code></pre>
<p>One caveat that gets a lot of beginners is how the second parameter works. Here is a resume:</p>
<blockquote>
<p>Case A: If nothing is added, then <strong>useEffect</strong> will run at every change of state inside the current component.</p>
<p>Case B: If an empty array is added ([]), then the <strong>useEffect</strong> will run only once when the component is mounted.</p>
<p>Case C: If some array is provided ([state]), then <strong>useEffect</strong> will run every time the state changes</p>
<p>Case C*: If some array is provided ([state1, state2, ….], <strong>useEffect</strong> will run every time <strong>ANY</strong> of these states changes.</p>
</blockquote>
<p>Now that we recalled how <strong>useEffect</strong> works, it’s time to state some of the drawbacks of using it as most people do.</p>
<p>The main idea of the <strong>useEffect</strong> hook is to synchronize data transfer with external <strong>APIs</strong> or another system, like when you are accessing a database, or waiting for an <strong>HTTP</strong> request to complete. The trouble is that we tend to use this hook in every situation possible inside our code, especially <strong>Case A</strong> and C* listed above, and the code can become incredibly unreadable with just a couple of lines of code, including triggering a loop if you change one of the states in the dependency array during the process.</p>
<p>This can make your code inefficient too, as useEffect works as if you were stepping aside to run some code and then coming back to the main thread. This is inefficient.</p>
<p>Great, now you know why to avoid it. But how?</p>
<p>Let’s talk about each one of the Cases in detail:</p>
<blockquote>
<p>Case A — No dependency array: This one should be abolished from your code, as it will certainly trigger unnecessary calculations every time a state changes. In this case, you should specify which states really should trigger this function using a dependency array.</p>
<p>Case B — Empty dependency array: This is one of the good ones, the only recommendation that I can provide is to keep just one of these for each component and wrap its content into a function.</p>
<p>Case C — One dependency state only. It’s ok to use if you are processing external data. Otherwise, you should change it to the solution I will provide below.</p>
<p>Case C* — Multiple dependency states in the same <strong>useEffect</strong>. This is the one I consider the most troublesome. I recommend you try to untangle the states into different useEffect hooks before anything, as it makes your code very unreadable.</p>
</blockquote>
<p>Now for the solution that I promised. Let’s consider these two Components (Parent and Child):</p>
<pre><code>// ParentComponent.js
import React, { useState, useEffect } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
useEffect(() =&gt; {
setMessage(`Button clicked ${count} times!`);
},[count]}
return (
&lt;ChildComponent count={count} message={message} setCount={setCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, setCount }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={() =&gt; {setCount(count+1)}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>Now let’s explain what is happening here:</p>
<blockquote>
<p>1 — Once the user clicks the button on the <strong>ChildComponent</strong>, we change the state “count” by incrementing 1. It will take one render loop to happen and change the state.</p>
<p>2 — Once the state “count” changes, the child component will be rendered again and it will also trigger the <strong>useEffect</strong> hook on both components, which will trigger the change in the “message” state. Again, it will only happen in the next render.</p>
<p>3 — When the “message” state changes, then another render happens in the components changing the message.</p>
<p>In this case, we ended up having two renders. It may now seem much, but it can grow at a large scale once you have more states at play.</p>
<p>Now look what happens when we make the following changes in the components:</p>
</blockquote>
<pre><code>// ParentComponent.js
import React, { useState } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
const incrementCount = () =&gt; {
setCount(count + 1);
setMessage(`Button clicked ${count + 1} times!`);
}
return (
&lt;ChildComponent count={count} message={message}
callbackFunction={incrementCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, callbackFunction }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={callbackFunction}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>We changed the code to pass a <strong>Callback Function</strong> to the Child Component. You may notice that:</p>
<blockquote>
<p>· We don’t have a <strong>useEffect</strong> anymore defined on the Parent Component. This makes the code easier to read, as we can understand our code as, let’s say, more linear and single-threaded than the original.</p>
<p>· We don’t have to wait for two render cycles to display our final message, or worse, render both components two times.</p>
<p>· We can separate concerns between components, making them more reusable and easier to read or adapt, as we can put whatever we want in the callback function.</p>
<p>· Both the state changes at the same time, avoiding the chaining of <strong>useEffect</strong> statements.</p>
</blockquote>
<p>And that’s it for today. Thank you for reading!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。<br>
开源日报第1073期：构建Web、移动和桌面应用程序：《meteor》<br>
2024年1月13日，开源日报第1073期：<br>
今日推荐开源项目：《meteor》<br>
今日推荐英文原文：《React: You are Using useEffect() Wrong, Do This Instead》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240113.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meteor》传送门：<a href="https://github.com/meteor/meteor?tab=readme-ov-file" target="_blank">项目链接</a> </p>
<p>推荐理由：Meteor.js是一个用于构建Web、移动和桌面应用程序的开源平台，api文档非常详细</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/stackademic/why-you-should-avoid-using-useeffect-hook-in-react-and-what-to-do-instead-740660e33420" target="_blank">React: You are Using useEffect() Wrong, Do This Instead</a></p>
<p>推荐理由：该文章强调了在React中使用useEffect()钩子时常见的陷阱，useEffect()钩子经常被错误使用，导致代码效率低下且难以阅读，举了一些例子详细解释</p>
<hr>
<h1>React: You are Using useEffect() Wrong, Do This Instead</h1>
<p>Once one decides to move forward with learning <strong>React</strong>, hooks are among the first things to learn (and to be frustrated with). Hooks are essential parts of <strong>React</strong>, as they were created to solve several problems that appeared in the first couple of versions of <strong>React</strong>, when every rendering was done inside the component’s lifecycle functions, such as <strong>componentDidMount()</strong>, <strong>componentWillMout(),</strong> <strong>componentDidUpdate()</strong>.</p>
<p>That said, the first hooks everyone starts touching are <strong>useState()</strong> and <strong>useEffect()</strong>. The first is used for state management and control when the component should be rendered again, while the second behaves somewhat similarly to the lifecycle functions stated above.</p>
<p>The <strong>useEffect()</strong> hook can receive two outputs: the first is a callback function, while the second is optional and defines when this hook should be called.</p>
<pre><code>  useEffect((prevProps) =&gt; { //prevProps are optional and has some specific uses. Compare with what happens with the lifecycle functions.
//Custom function content….
custom function content…
return () =&gt; {
// Code to run when the component is unmounted or when dependencies change
// It helps in avoiding memory leaks and unexpected behavior
};
}, [dependencies in array form]);</code></pre>
<p>One caveat that gets a lot of beginners is how the second parameter works. Here is a resume:</p>
<blockquote>
<p>Case A: If nothing is added, then <strong>useEffect</strong> will run at every change of state inside the current component.</p>
<p>Case B: If an empty array is added ([]), then the <strong>useEffect</strong> will run only once when the component is mounted.</p>
<p>Case C: If some array is provided ([state]), then <strong>useEffect</strong> will run every time the state changes</p>
<p>Case C*: If some array is provided ([state1, state2, ….], <strong>useEffect</strong> will run every time <strong>ANY</strong> of these states changes.</p>
</blockquote>
<p>Now that we recalled how <strong>useEffect</strong> works, it’s time to state some of the drawbacks of using it as most people do.</p>
<p>The main idea of the <strong>useEffect</strong> hook is to synchronize data transfer with external <strong>APIs</strong> or another system, like when you are accessing a database, or waiting for an <strong>HTTP</strong> request to complete. The trouble is that we tend to use this hook in every situation possible inside our code, especially <strong>Case A</strong> and C* listed above, and the code can become incredibly unreadable with just a couple of lines of code, including triggering a loop if you change one of the states in the dependency array during the process.</p>
<p>This can make your code inefficient too, as useEffect works as if you were stepping aside to run some code and then coming back to the main thread. This is inefficient.</p>
<p>Great, now you know why to avoid it. But how?</p>
<p>Let’s talk about each one of the Cases in detail:</p>
<blockquote>
<p>Case A — No dependency array: This one should be abolished from your code, as it will certainly trigger unnecessary calculations every time a state changes. In this case, you should specify which states really should trigger this function using a dependency array.</p>
<p>Case B — Empty dependency array: This is one of the good ones, the only recommendation that I can provide is to keep just one of these for each component and wrap its content into a function.</p>
<p>Case C — One dependency state only. It’s ok to use if you are processing external data. Otherwise, you should change it to the solution I will provide below.</p>
<p>Case C* — Multiple dependency states in the same <strong>useEffect</strong>. This is the one I consider the most troublesome. I recommend you try to untangle the states into different useEffect hooks before anything, as it makes your code very unreadable.</p>
</blockquote>
<p>Now for the solution that I promised. Let’s consider these two Components (Parent and Child):</p>
<pre><code>// ParentComponent.js
import React, { useState, useEffect } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
useEffect(() =&gt; {
setMessage(`Button clicked ${count} times!`);
},[count]}
return (
&lt;ChildComponent count={count} message={message} setCount={setCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, setCount }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={() =&gt; {setCount(count+1)}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>Now let’s explain what is happening here:</p>
<blockquote>
<p>1 — Once the user clicks the button on the <strong>ChildComponent</strong>, we change the state “count” by incrementing 1. It will take one render loop to happen and change the state.</p>
<p>2 — Once the state “count” changes, the child component will be rendered again and it will also trigger the <strong>useEffect</strong> hook on both components, which will trigger the change in the “message” state. Again, it will only happen in the next render.</p>
<p>3 — When the “message” state changes, then another render happens in the components changing the message.</p>
<p>In this case, we ended up having two renders. It may now seem much, but it can grow at a large scale once you have more states at play.</p>
<p>Now look what happens when we make the following changes in the components:</p>
</blockquote>
<pre><code>// ParentComponent.js
import React, { useState } from 'react';
import ChildComponent from './ChildComponent';
function ParentComponent() {
const [count, setCount] = useState(0);
const [message, setMessage] = useState('Hello from Parent!');
const incrementCount = () =&gt; {
setCount(count + 1);
setMessage(`Button clicked ${count + 1} times!`);
}
return (
&lt;ChildComponent count={count} message={message}
callbackFunction={incrementCount} /&gt;
);
}
export default ParentComponent;
// ChildComponent.js
import React from 'react';
function ChildComponent({ count, message, callbackFunction }) {
return (
&lt;div&gt;
&lt;h3&gt;Child Component&lt;/h3&gt;
&lt;p&gt;Received Count from Parent: {count}&lt;/p&gt;
&lt;p&gt;Received Message from Parent: {message}&lt;/p&gt;
&lt;button onClick={callbackFunction}&gt;Click Me&lt;/button&gt;
&lt;/div&gt;
);
}
export default ChildComponent;</code></pre>
<p>We changed the code to pass a <strong>Callback Function</strong> to the Child Component. You may notice that:</p>
<blockquote>
<p>· We don’t have a <strong>useEffect</strong> anymore defined on the Parent Component. This makes the code easier to read, as we can understand our code as, let’s say, more linear and single-threaded than the original.</p>
<p>· We don’t have to wait for two render cycles to display our final message, or worse, render both components two times.</p>
<p>· We can separate concerns between components, making them more reusable and easier to read or adapt, as we can put whatever we want in the callback function.</p>
<p>· Both the state changes at the same time, avoiding the chaining of <strong>useEffect</strong> statements.</p>
</blockquote>
<p>And that’s it for today. Thank you for reading!</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240113.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240113.jpg" medium="image"/>
    <pubDate>Sat, 13 Jan 2024 13:23:16 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1072期：自助工程平台：《meshery》]]></title>
    <link>https://openingsource.org/11504/</link>
    <itunes:title><![CDATA[开源日报第1072期：自助工程平台：《meshery》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月12日，开源日报第1072期：<br>
今日推荐开源项目：《meshery》<br>
今日推荐英文原文：《How I Have Mastered Closures in React 🚀》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240112.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meshery》传送门：<a href="https://github.com/meshery/meshery" target="_blank">项目链接</a> </p>
<p>推荐理由：Meshery是一个自助工程平台，是开源的、云原生的管理器，可以实现基于K8s的所有基础设施和应用程序的设计和管理。除了其他功能外，作为一个可扩展的平台，Meshery提供了可视化和协作式GitOps，在管理K8s多集群部署时摆脱了YAML文件的束缚</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/how-i-have-mastered-closures-in-react-a6b121095a92" target="_blank">How I Have Mastered Closures in React 🚀</a></p>
<p>推荐理由：文章解释了JavaScript中闭包的概念，通过示例说明展示了一种更清晰、更模块化处理状态逻辑的方法，也强调的好处包括封装、私有作用域、更清晰的代码以及减少全局作用域污染</p>
<hr>
<h1>How I Have Mastered Closures in React 🚀</h1>
<p>React, a powerful and widely-used JavaScript library for building user interfaces, offers developers a flexible and efficient way to create interactive web applications. One of the key concepts in React that can greatly enhance your programming skills is the use of closures. In this article, we’ll explore how closures work and how I have leveraged them to write cleaner and more maintainable React code.</p>
<h1>Understanding Closures in JavaScript</h1>
<p>Before diving into React-specific examples, let’s ensure we have a solid understanding of closures in JavaScript. A closure is formed when a function is defined inside another function, allowing the inner function to access the outer function’s variables and parameters. This encapsulation of variables creates a private scope for the inner function, even after the outer function has finished executing.</p>
<pre><code>function outerFunction() {
let outerVariable = 'I am from the outer function';
function innerFunction() {
console.log(outerVariable);
}
return innerFunction;
}
const closureExample = outerFunction();
closureExample(); // Output: "I am from the outer function"</code></pre>
<p>In the example above, <code>innerFunction</code> forms a closure by referencing <code>outerVariable</code>, even though the outer function <code>outerFunction</code> has completed execution. This ability to "remember" variables from the outer scope is crucial for various programming scenarios.</p>
<h1>Applying Closures in React Components</h1>
<p>Now, let’s see how closures can be applied in the context of React components. Consider a common scenario where you want to create a reusable component that handles state logic internally.</p>
<pre><code>import React, { useState } from 'react';
const Counter = () =&gt; {
// State is declared inside the component
const [count, setCount] = useState(0);
const increment = () =&gt; {
setCount(count + 1);
};
const decrement = () =&gt; {
setCount(count - 1);
};
return (
&lt;div&gt;
&lt;p&gt;Count: {count}&lt;/p&gt;
&lt;button onClick={increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the state (<code>count</code>) and the state updater function (<code>setCount</code>) are declared using the <code>useState</code> hook. The <code>increment</code> and <code>decrement</code> functions use these state-related variables. However, what if you want to encapsulate the state logic and provide an external function to handle the state updates?</p>
<p>This is where closures come into play:</p>
<pre><code>import React from 'react';
const Counter = () =&gt; {
// State is declared inside the closure
const createCounter = () =&gt; {
let count = 0;
const increment = () =&gt; {
count += 1;
console.log('Incremented! Current count:', count);
};
const decrement = () =&gt; {
count -= 1;
console.log('Decremented! Current count:', count);
};
const getCount = () =&gt; count;
return { increment, decrement, getCount };
};
const counterInstance = createCounter();
return (
&lt;div&gt;
&lt;p&gt;Count: {counterInstance.getCount()}&lt;/p&gt;
&lt;button onClick={counterInstance.increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={counterInstance.decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the <code>createCounter</code> function serves as a closure, encapsulating the state (<code>count</code>) and state manipulation functions (<code>increment</code> and <code>decrement</code>). The returned object from <code>createCounter</code> provides access to the internal state and functions, creating a more modular and reusable component.</p>
<h1>Benefits of Using Closures in React</h1>
<ol>
<li><strong>Encapsulation and Modularity:</strong> Closures allow you to encapsulate state and behavior within a component, promoting a modular and reusable code structure.</li>
<li><strong>Private Scoping</strong>: The variables inside the closure are not accessible from outside, providing a level of privacy and preventing unintended manipulation.</li>
<li><strong>Cleaner Code:</strong> By encapsulating logic within closures, your component code becomes cleaner and more focused, making it easier to understand and maintain.</li>
<li><strong>Reduced Global Scope Pollution:</strong> Closures help in minimizing the use of global variables, reducing the risk of naming conflicts and unintended side effects.</li>
</ol>
<p>In conclusion, mastering closures in React can significantly enhance your ability to write efficient and maintainable code. By understanding how closures work and applying them judiciously, you can create more module.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月12日，开源日报第1072期：<br>
今日推荐开源项目：《meshery》<br>
今日推荐英文原文：《How I Have Mastered Closures in React 🚀》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240112.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meshery》传送门：<a href="https://github.com/meshery/meshery" target="_blank">项目链接</a> </p>
<p>推荐理由：Meshery是一个自助工程平台，是开源的、云原生的管理器，可以实现基于K8s的所有基础设施和应用程序的设计和管理。除了其他功能外，作为一个可扩展的平台，Meshery提供了可视化和协作式GitOps，在管理K8s多集群部署时摆脱了YAML文件的束缚</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/how-i-have-mastered-closures-in-react-a6b121095a92" target="_blank">How I Have Mastered Closures in React 🚀</a></p>
<p>推荐理由：文章解释了JavaScript中闭包的概念，通过示例说明展示了一种更清晰、更模块化处理状态逻辑的方法，也强调的好处包括封装、私有作用域、更清晰的代码以及减少全局作用域污染</p>
<hr>
<h1>How I Have Mastered Closures in React 🚀</h1>
<p>React, a powerful and widely-used JavaScript library for building user interfaces, offers developers a flexible and efficient way to create interactive web applications. One of the key concepts in React that can greatly enhance your programming skills is the use of closures. In this article, we’ll explore how closures work and how I have leveraged them to write cleaner and more maintainable React code.</p>
<h1>Understanding Closures in JavaScript</h1>
<p>Before diving into React-specific examples, let’s ensure we have a solid understanding of closures in JavaScript. A closure is formed when a function is defined inside another function, allowing the inner function to access the outer function’s variables and parameters. This encapsulation of variables creates a private scope for the inner function, even after the outer function has finished executing.</p>
<pre><code>function outerFunction() {
let outerVariable = 'I am from the outer function';
function innerFunction() {
console.log(outerVariable);
}
return innerFunction;
}
const closureExample = outerFunction();
closureExample(); // Output: "I am from the outer function"</code></pre>
<p>In the example above, <code>innerFunction</code> forms a closure by referencing <code>outerVariable</code>, even though the outer function <code>outerFunction</code> has completed execution. This ability to "remember" variables from the outer scope is crucial for various programming scenarios.</p>
<h1>Applying Closures in React Components</h1>
<p>Now, let’s see how closures can be applied in the context of React components. Consider a common scenario where you want to create a reusable component that handles state logic internally.</p>
<pre><code>import React, { useState } from 'react';
const Counter = () =&gt; {
// State is declared inside the component
const [count, setCount] = useState(0);
const increment = () =&gt; {
setCount(count + 1);
};
const decrement = () =&gt; {
setCount(count - 1);
};
return (
&lt;div&gt;
&lt;p&gt;Count: {count}&lt;/p&gt;
&lt;button onClick={increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the state (<code>count</code>) and the state updater function (<code>setCount</code>) are declared using the <code>useState</code> hook. The <code>increment</code> and <code>decrement</code> functions use these state-related variables. However, what if you want to encapsulate the state logic and provide an external function to handle the state updates?</p>
<p>This is where closures come into play:</p>
<pre><code>import React from 'react';
const Counter = () =&gt; {
// State is declared inside the closure
const createCounter = () =&gt; {
let count = 0;
const increment = () =&gt; {
count += 1;
console.log('Incremented! Current count:', count);
};
const decrement = () =&gt; {
count -= 1;
console.log('Decremented! Current count:', count);
};
const getCount = () =&gt; count;
return { increment, decrement, getCount };
};
const counterInstance = createCounter();
return (
&lt;div&gt;
&lt;p&gt;Count: {counterInstance.getCount()}&lt;/p&gt;
&lt;button onClick={counterInstance.increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={counterInstance.decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the <code>createCounter</code> function serves as a closure, encapsulating the state (<code>count</code>) and state manipulation functions (<code>increment</code> and <code>decrement</code>). The returned object from <code>createCounter</code> provides access to the internal state and functions, creating a more modular and reusable component.</p>
<h1>Benefits of Using Closures in React</h1>
<ol>
<li><strong>Encapsulation and Modularity:</strong> Closures allow you to encapsulate state and behavior within a component, promoting a modular and reusable code structure.</li>
<li><strong>Private Scoping</strong>: The variables inside the closure are not accessible from outside, providing a level of privacy and preventing unintended manipulation.</li>
<li><strong>Cleaner Code:</strong> By encapsulating logic within closures, your component code becomes cleaner and more focused, making it easier to understand and maintain.</li>
<li><strong>Reduced Global Scope Pollution:</strong> Closures help in minimizing the use of global variables, reducing the risk of naming conflicts and unintended side effects.</li>
</ol>
<p>In conclusion, mastering closures in React can significantly enhance your ability to write efficient and maintainable code. By understanding how closures work and applying them judiciously, you can create more module.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月12日，开源日报第1072期：<br>
今日推荐开源项目：《meshery》<br>
今日推荐英文原文：《How I Have Mastered Closures in React 🚀》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240112.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《meshery》传送门：<a href="https://github.com/meshery/meshery" target="_blank">项目链接</a> </p>
<p>推荐理由：Meshery是一个自助工程平台，是开源的、云原生的管理器，可以实现基于K8s的所有基础设施和应用程序的设计和管理。除了其他功能外，作为一个可扩展的平台，Meshery提供了可视化和协作式GitOps，在管理K8s多集群部署时摆脱了YAML文件的束缚</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@techsuneel99/how-i-have-mastered-closures-in-react-a6b121095a92" target="_blank">How I Have Mastered Closures in React 🚀</a></p>
<p>推荐理由：文章解释了JavaScript中闭包的概念，通过示例说明展示了一种更清晰、更模块化处理状态逻辑的方法，也强调的好处包括封装、私有作用域、更清晰的代码以及减少全局作用域污染</p>
<hr>
<h1>How I Have Mastered Closures in React 🚀</h1>
<p>React, a powerful and widely-used JavaScript library for building user interfaces, offers developers a flexible and efficient way to create interactive web applications. One of the key concepts in React that can greatly enhance your programming skills is the use of closures. In this article, we’ll explore how closures work and how I have leveraged them to write cleaner and more maintainable React code.</p>
<h1>Understanding Closures in JavaScript</h1>
<p>Before diving into React-specific examples, let’s ensure we have a solid understanding of closures in JavaScript. A closure is formed when a function is defined inside another function, allowing the inner function to access the outer function’s variables and parameters. This encapsulation of variables creates a private scope for the inner function, even after the outer function has finished executing.</p>
<pre><code>function outerFunction() {
let outerVariable = 'I am from the outer function';
function innerFunction() {
console.log(outerVariable);
}
return innerFunction;
}
const closureExample = outerFunction();
closureExample(); // Output: "I am from the outer function"</code></pre>
<p>In the example above, <code>innerFunction</code> forms a closure by referencing <code>outerVariable</code>, even though the outer function <code>outerFunction</code> has completed execution. This ability to "remember" variables from the outer scope is crucial for various programming scenarios.</p>
<h1>Applying Closures in React Components</h1>
<p>Now, let’s see how closures can be applied in the context of React components. Consider a common scenario where you want to create a reusable component that handles state logic internally.</p>
<pre><code>import React, { useState } from 'react';
const Counter = () =&gt; {
// State is declared inside the component
const [count, setCount] = useState(0);
const increment = () =&gt; {
setCount(count + 1);
};
const decrement = () =&gt; {
setCount(count - 1);
};
return (
&lt;div&gt;
&lt;p&gt;Count: {count}&lt;/p&gt;
&lt;button onClick={increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the state (<code>count</code>) and the state updater function (<code>setCount</code>) are declared using the <code>useState</code> hook. The <code>increment</code> and <code>decrement</code> functions use these state-related variables. However, what if you want to encapsulate the state logic and provide an external function to handle the state updates?</p>
<p>This is where closures come into play:</p>
<pre><code>import React from 'react';
const Counter = () =&gt; {
// State is declared inside the closure
const createCounter = () =&gt; {
let count = 0;
const increment = () =&gt; {
count += 1;
console.log('Incremented! Current count:', count);
};
const decrement = () =&gt; {
count -= 1;
console.log('Decremented! Current count:', count);
};
const getCount = () =&gt; count;
return { increment, decrement, getCount };
};
const counterInstance = createCounter();
return (
&lt;div&gt;
&lt;p&gt;Count: {counterInstance.getCount()}&lt;/p&gt;
&lt;button onClick={counterInstance.increment}&gt;Increment&lt;/button&gt;
&lt;button onClick={counterInstance.decrement}&gt;Decrement&lt;/button&gt;
&lt;/div&gt;
);
};</code></pre>
<p>In this example, the <code>createCounter</code> function serves as a closure, encapsulating the state (<code>count</code>) and state manipulation functions (<code>increment</code> and <code>decrement</code>). The returned object from <code>createCounter</code> provides access to the internal state and functions, creating a more modular and reusable component.</p>
<h1>Benefits of Using Closures in React</h1>
<ol>
<li><strong>Encapsulation and Modularity:</strong> Closures allow you to encapsulate state and behavior within a component, promoting a modular and reusable code structure.</li>
<li><strong>Private Scoping</strong>: The variables inside the closure are not accessible from outside, providing a level of privacy and preventing unintended manipulation.</li>
<li><strong>Cleaner Code:</strong> By encapsulating logic within closures, your component code becomes cleaner and more focused, making it easier to understand and maintain.</li>
<li><strong>Reduced Global Scope Pollution:</strong> Closures help in minimizing the use of global variables, reducing the risk of naming conflicts and unintended side effects.</li>
</ol>
<p>In conclusion, mastering closures in React can significantly enhance your ability to write efficient and maintainable code. By understanding how closures work and applying them judiciously, you can create more module.</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240112.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240112.jpg" medium="image"/>
    <pubDate>Fri, 12 Jan 2024 12:22:02 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1071期：无服务器框架：《serverless》]]></title>
    <link>https://openingsource.org/11500/</link>
    <itunes:title><![CDATA[开源日报第1071期：无服务器框架：《serverless》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月11日，开源日报第1071期：<br>
今日推荐开源项目：《serverless》<br>
今日推荐英文原文：《Code Splitting in React: Optimize Performance by Splitting your Code》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240111.jpg.pagespeed.ic.nLka81c2fM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《serverless》传送门：<a href="https://github.com/serverless/serverless" target="_blank">项目链接</a> </p>
<p>推荐理由：⚡severless框架-使用AWS Lambda、Azure Functions、Google CloudFunctions等构建具有无服务器架构的Web、移动和物联网应用程序！</p>
<p>官网直达：serverless.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shriharim006/code-splitting-in-react-optimize-performance-by-splitting-your-code-e3e70d0c3d91" target="_blank">Code Splitting in React: Optimize Performance by Splitting your Code</a></p>
<p>推荐理由：Code Splitting是现代Web开发中优化React项目是比较关键的，使组件捆绑包拆分为较小的块，只在特定上下文中加载所需的代码，从而减少初始加载时间<br>
本文主要介绍了两种常用的实现方法：React.lazy()和Suspense，以及利用Webpack的动态导入功能</p>
<hr>
<h1>Code Splitting in React: Optimize Performance by Splitting your Code</h1>
<p>Code splitting is an essential technique in modern web development to optimize the performance of React applications. It allows you to split your JavaScript bundle into smaller chunks. With code splitting, you can only load the code that is needed in a particular context, reducing the initial load time and improving the overall user experience. In this blog, we will explore code splitting in React and learn how to implement it in our applications.</p>
<h2><strong>Why is Code Splitting important?</strong></h2>
<p>By default, when you build a React application, all the JavaScript code is bundled into a single file. While this approach simplifies the development process, it can result in larger bundle sizes. A large bundle takes longer to load, especially on slower networks or mobile devices, leading to increased load times.</p>
<p>On the other hand, code splitting allows you to split your code into smaller chunks, which are loaded on-demand. This enables users to only download the necessary code when they navigate to a specific route or interact with a particular feature. As a result, the initial load time is significantly reduced, boosting the performance and user experience of the application.</p>
<h2><strong>Implementing Code Splitting in React</strong></h2>
<p>There are several ways to implement code splitting in a React application. Let’s explore two commonly used methods:</p>
<h2><strong>Method 1: React.lazy() with Suspense</strong></h2>
<p>The React.lazy() function allows you to load a component lazily, which means it is loaded only when needed. Combined with the Suspense component, you can provide a smooth loading experience while waiting for the lazily loaded component to be fetched and rendered.</p>
<p>Here’s an example:</p>
<pre><code>import React, { lazy, Suspense } from 'react';
const LazyComponent = lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<p>In this example, the <strong>LazyComponent</strong> is loaded lazily using <strong>React.lazy()</strong>. The <strong>Suspense</strong> component provides a fallback UI (e.g., a loading spinner or a message) while the component is being loaded.</p>
<p><strong>Method 2: Dynamic Import with Webpack</strong></p>
<p>If you are using Webpack as your build tool, you can leverage its dynamic import feature to achieve code splitting.</p>
<pre><code>import React, { Component } from 'react';
class App extends Component {
handleClick = async () =&gt; {
const module = await import('./DynamicComponent');
// Do something with the dynamically imported module
};
render() {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;button onClick={this.handleClick}&gt;Load Dynamic Component&lt;/button&gt;
&lt;/div&gt;
);
}
}
export default App;</code></pre>
<p>In this example, the <strong>DynamicComponent</strong> is imported dynamically using the <strong>import()</strong> syntax. When the button is clicked, the component is loaded dynamically, giving you control over when and where to load it.</p>
<h1><strong>Conclusion</strong></h1>
<p>Code splitting is a powerful technique to optimize the performance of React applications. By splitting your code into smaller chunks and loading them on-demand, you can significantly reduce load times and provide a better user experience. React provides built-in tools like React.lazy() and Suspense, along with the support of tools like Webpack, to make code splitting easy to implement. So, start applying code splitting to your React applications and boost their performance now!</p>
<p>Enjoyed reading this ? Please share it with others.</p>
<p>Please hit 👏 button below as many times as possible to show your support!<br>
Thanks for the read. <strong>Cheers!!!.</strong> You are <strong>Awesome</strong> !</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月11日，开源日报第1071期：<br>
今日推荐开源项目：《serverless》<br>
今日推荐英文原文：《Code Splitting in React: Optimize Performance by Splitting your Code》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240111.jpg.pagespeed.ic.nLka81c2fM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《serverless》传送门：<a href="https://github.com/serverless/serverless" target="_blank">项目链接</a> </p>
<p>推荐理由：⚡severless框架-使用AWS Lambda、Azure Functions、Google CloudFunctions等构建具有无服务器架构的Web、移动和物联网应用程序！</p>
<p>官网直达：serverless.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shriharim006/code-splitting-in-react-optimize-performance-by-splitting-your-code-e3e70d0c3d91" target="_blank">Code Splitting in React: Optimize Performance by Splitting your Code</a></p>
<p>推荐理由：Code Splitting是现代Web开发中优化React项目是比较关键的，使组件捆绑包拆分为较小的块，只在特定上下文中加载所需的代码，从而减少初始加载时间<br>
本文主要介绍了两种常用的实现方法：React.lazy()和Suspense，以及利用Webpack的动态导入功能</p>
<hr>
<h1>Code Splitting in React: Optimize Performance by Splitting your Code</h1>
<p>Code splitting is an essential technique in modern web development to optimize the performance of React applications. It allows you to split your JavaScript bundle into smaller chunks. With code splitting, you can only load the code that is needed in a particular context, reducing the initial load time and improving the overall user experience. In this blog, we will explore code splitting in React and learn how to implement it in our applications.</p>
<h2><strong>Why is Code Splitting important?</strong></h2>
<p>By default, when you build a React application, all the JavaScript code is bundled into a single file. While this approach simplifies the development process, it can result in larger bundle sizes. A large bundle takes longer to load, especially on slower networks or mobile devices, leading to increased load times.</p>
<p>On the other hand, code splitting allows you to split your code into smaller chunks, which are loaded on-demand. This enables users to only download the necessary code when they navigate to a specific route or interact with a particular feature. As a result, the initial load time is significantly reduced, boosting the performance and user experience of the application.</p>
<h2><strong>Implementing Code Splitting in React</strong></h2>
<p>There are several ways to implement code splitting in a React application. Let’s explore two commonly used methods:</p>
<h2><strong>Method 1: React.lazy() with Suspense</strong></h2>
<p>The React.lazy() function allows you to load a component lazily, which means it is loaded only when needed. Combined with the Suspense component, you can provide a smooth loading experience while waiting for the lazily loaded component to be fetched and rendered.</p>
<p>Here’s an example:</p>
<pre><code>import React, { lazy, Suspense } from 'react';
const LazyComponent = lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<p>In this example, the <strong>LazyComponent</strong> is loaded lazily using <strong>React.lazy()</strong>. The <strong>Suspense</strong> component provides a fallback UI (e.g., a loading spinner or a message) while the component is being loaded.</p>
<p><strong>Method 2: Dynamic Import with Webpack</strong></p>
<p>If you are using Webpack as your build tool, you can leverage its dynamic import feature to achieve code splitting.</p>
<pre><code>import React, { Component } from 'react';
class App extends Component {
handleClick = async () =&gt; {
const module = await import('./DynamicComponent');
// Do something with the dynamically imported module
};
render() {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;button onClick={this.handleClick}&gt;Load Dynamic Component&lt;/button&gt;
&lt;/div&gt;
);
}
}
export default App;</code></pre>
<p>In this example, the <strong>DynamicComponent</strong> is imported dynamically using the <strong>import()</strong> syntax. When the button is clicked, the component is loaded dynamically, giving you control over when and where to load it.</p>
<h1><strong>Conclusion</strong></h1>
<p>Code splitting is a powerful technique to optimize the performance of React applications. By splitting your code into smaller chunks and loading them on-demand, you can significantly reduce load times and provide a better user experience. React provides built-in tools like React.lazy() and Suspense, along with the support of tools like Webpack, to make code splitting easy to implement. So, start applying code splitting to your React applications and boost their performance now!</p>
<p>Enjoyed reading this ? Please share it with others.</p>
<p>Please hit 👏 button below as many times as possible to show your support!<br>
Thanks for the read. <strong>Cheers!!!.</strong> You are <strong>Awesome</strong> !</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月11日，开源日报第1071期：<br>
今日推荐开源项目：《serverless》<br>
今日推荐英文原文：《Code Splitting in React: Optimize Performance by Splitting your Code》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240111.jpg.pagespeed.ic.nLka81c2fM.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《serverless》传送门：<a href="https://github.com/serverless/serverless" target="_blank">项目链接</a> </p>
<p>推荐理由：⚡severless框架-使用AWS Lambda、Azure Functions、Google CloudFunctions等构建具有无服务器架构的Web、移动和物联网应用程序！</p>
<p>官网直达：serverless.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://medium.com/@shriharim006/code-splitting-in-react-optimize-performance-by-splitting-your-code-e3e70d0c3d91" target="_blank">Code Splitting in React: Optimize Performance by Splitting your Code</a></p>
<p>推荐理由：Code Splitting是现代Web开发中优化React项目是比较关键的，使组件捆绑包拆分为较小的块，只在特定上下文中加载所需的代码，从而减少初始加载时间<br>
本文主要介绍了两种常用的实现方法：React.lazy()和Suspense，以及利用Webpack的动态导入功能</p>
<hr>
<h1>Code Splitting in React: Optimize Performance by Splitting your Code</h1>
<p>Code splitting is an essential technique in modern web development to optimize the performance of React applications. It allows you to split your JavaScript bundle into smaller chunks. With code splitting, you can only load the code that is needed in a particular context, reducing the initial load time and improving the overall user experience. In this blog, we will explore code splitting in React and learn how to implement it in our applications.</p>
<h2><strong>Why is Code Splitting important?</strong></h2>
<p>By default, when you build a React application, all the JavaScript code is bundled into a single file. While this approach simplifies the development process, it can result in larger bundle sizes. A large bundle takes longer to load, especially on slower networks or mobile devices, leading to increased load times.</p>
<p>On the other hand, code splitting allows you to split your code into smaller chunks, which are loaded on-demand. This enables users to only download the necessary code when they navigate to a specific route or interact with a particular feature. As a result, the initial load time is significantly reduced, boosting the performance and user experience of the application.</p>
<h2><strong>Implementing Code Splitting in React</strong></h2>
<p>There are several ways to implement code splitting in a React application. Let’s explore two commonly used methods:</p>
<h2><strong>Method 1: React.lazy() with Suspense</strong></h2>
<p>The React.lazy() function allows you to load a component lazily, which means it is loaded only when needed. Combined with the Suspense component, you can provide a smooth loading experience while waiting for the lazily loaded component to be fetched and rendered.</p>
<p>Here’s an example:</p>
<pre><code>import React, { lazy, Suspense } from 'react';
const LazyComponent = lazy(() =&gt; import('./LazyComponent'));
const App = () =&gt; {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;Suspense fallback={&lt;div&gt;Loading...&lt;/div&gt;}&gt;
&lt;LazyComponent /&gt;
&lt;/Suspense&gt;
&lt;/div&gt;
);
};
export default App;</code></pre>
<p>In this example, the <strong>LazyComponent</strong> is loaded lazily using <strong>React.lazy()</strong>. The <strong>Suspense</strong> component provides a fallback UI (e.g., a loading spinner or a message) while the component is being loaded.</p>
<p><strong>Method 2: Dynamic Import with Webpack</strong></p>
<p>If you are using Webpack as your build tool, you can leverage its dynamic import feature to achieve code splitting.</p>
<pre><code>import React, { Component } from 'react';
class App extends Component {
handleClick = async () =&gt; {
const module = await import('./DynamicComponent');
// Do something with the dynamically imported module
};
render() {
return (
&lt;div&gt;
&lt;h1&gt;My React App&lt;/h1&gt;
&lt;button onClick={this.handleClick}&gt;Load Dynamic Component&lt;/button&gt;
&lt;/div&gt;
);
}
}
export default App;</code></pre>
<p>In this example, the <strong>DynamicComponent</strong> is imported dynamically using the <strong>import()</strong> syntax. When the button is clicked, the component is loaded dynamically, giving you control over when and where to load it.</p>
<h1><strong>Conclusion</strong></h1>
<p>Code splitting is a powerful technique to optimize the performance of React applications. By splitting your code into smaller chunks and loading them on-demand, you can significantly reduce load times and provide a better user experience. React provides built-in tools like React.lazy() and Suspense, along with the support of tools like Webpack, to make code splitting easy to implement. So, start applying code splitting to your React applications and boost their performance now!</p>
<p>Enjoyed reading this ? Please share it with others.</p>
<p>Please hit 👏 button below as many times as possible to show your support!<br>
Thanks for the read. <strong>Cheers!!!.</strong> You are <strong>Awesome</strong> !</p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240111.jpg.pagespeed.ic.nLka81c2fM.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/xosd-20240111.jpg.pagespeed.ic.nLka81c2fM.jpg" medium="image"/>
    <pubDate>Thu, 11 Jan 2024 14:14:01 +0000</pubDate>
  </item><item>
    <title><![CDATA[开源日报第1070期：绘制虚拟面板神器：《excalidraw》]]></title>
    <link>https://openingsource.org/11497/</link>
    <itunes:title><![CDATA[开源日报第1070期：绘制虚拟面板神器：《excalidraw》]]></itunes:title>
    <itunes:author><![CDATA[开源工场[RSS+]]]></itunes:author>
    <itunes:summary>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月10日，开源日报第1070期：<br>
今日推荐开源项目：《excalidraw》<br>
今日推荐英文原文：《The Top Accessibility Errors Found in 2023》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240110.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《excalidraw》传送门：<a href="https://github.com/excalidraw/excalidraw" target="_blank">项目链接</a> </p>
<p>推荐理由：用于绘制手绘类图表的虚拟白板：excalidraw可以使用excalidraw构建创建手绘图、线框或任何你想绘制的东西，excalidraw具有以下功能：</p>
<ul>
<li>📡 PWA支持（离线工作）</li>
<li>🤼 实时协作</li>
<li>🔒 端到端加密</li>
<li>💾 本地优先支持（自动保存到浏览器）</li>
<li>🔗 可共享链接（导出为只读链接，您可以与其他人共享）</li>
</ul>
<p>直达链接 excalidraw.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://www.tpgi.com/the-top-accessibility-errors-found-in-2023/" target="_blank">The Top Accessibility Errors Found in 2023</a></p>
<p>推荐理由： 2023年发现的最常见无障碍错误总结：无按钮元素标签：按钮缺少可访问标签，使辅助技术用户无法理解按钮用途，无效aria-labelledby：aria-labelledby未正确引用文本元素的ID，使辅助技术无法连接控件和标签等等</p>
<hr>
<h1>The Top Accessibility Errors Found in 2023</h1>
<p>TPGi’s accessibility experts have been busy this year, identifying thousands of accessibility errors through manual and automated testing. From all those results, the following 10 issues were the most commonly found by our ARC automation tools. We hope this list helps guide your team to avoid some of this year’s most common barriers. Cheers to another year of making things accessible!</p>
<h2>1. No link text</h2>
<p>The <code>&lt;a&gt;</code> element is HTML’s way of specifying links to other web pages, or locations within the same webpage, providing the foundation for the web’s purpose as a connected set of pages. But developers sometimes use the <code>&lt;a&gt;</code> element to provide other interactive functionality, and often this is done in a way that omits any link text. The result is that assistive technologies such as screen readers have no way to communicate the purpose of the link, leaving users unsure what will happen when they follow the link. That’s why every link must communicate its purpose in an accessible way.</p>
<p><a href="https://wcag.com/blog/writing-meaningful-link-text/" target="_blank">Learn more about writing meaningful link text.</a></p>
<h2>2. Non-active element in tab order</h2>
<p>This error occurs when a non-interactive element has been placed in the tab order using <code>tabindex="0"</code>, which means it becomes focusable using the keyboard when it doesn’t need to be, reducing efficiency and increasing effort for keyboard users. Generally, only elements that are interactive, such as links, buttons and form input fields, should be included in focus order. Screen reader users have access to a wide range of additional keyboard commands to allow quick navigation through content, so there’s no need to include static text and images in the focus order. <strong>Edit:</strong> In fact, all users who rely on keyboard navigation are negatively affected by this common error.</p>
<p><a href="https://webaim.org/techniques/keyboard/" target="_blank">Learn more about keyboard interaction.</a></p>
<h2>3. Missing link alt attribute</h2>
<p>Closely related to item #1 in our list, often links are labeled with an icon or other graphic that visually indicates the purpose of the link. A common error is to provide this graphic without any text alternative. When an image link has no alternative text, assistive technology users will be unaware of the link’s purpose. For that reason, it’s essential to provide alternative text for image links, that describes the purpose of the link.</p>
<p><a href="https://webaim.org/techniques/alttext/" target="_blank">Learn more about all things ‘alt’.</a></p>
<h2>4. No alt text</h2>
<p>Related to item #3, an image’s <a href="https://webaim.org/techniques/alttext/" target="_blank">alternative text</a>, provides a text description of the image content. When an <code>&lt;img&gt;</code> element lacks a text alternative, screen reader users have no way of knowing what the image is for. Providing a text alternative that communicates the same information as the image is one of the most fundamental accessibility requirements; but exactly what the alternative text should be would depend on the image’s purpose.</p>
<p><a href="https://abilitynet.org.uk/news-blogs/five-golden-rules-compliant-alt-text" target="_blank">Learn about the five golden rules of compliant alt text.</a></p>
<h2>5. List not nested correctly</h2>
<p>Using markup to identify lists helps communicate the structure of page content in an accessible way. When a list item has a nested list, it’s important to use markup in the correct way to make the nesting relationships clear. This helps avoid confusion when the list structure is described by screen readers.</p>
<p><a href="https://www.w3.org/WAI/tutorials/page-structure/content/" target="_blank">Learn more about HTML lists as a way to structure content.</a></p>
<h2>6. Duplicate labels used</h2>
<p>Another accessibility issue commonly detected by automated tools is form fields that have multiple identical labels. Having more than one label with exactly the same text can make it harder for some users to understand the form’s structure, and decide what content to enter in a particular field, increasing the chances of input errors. If your form has multiple labels with the same text, that’s a sign you might want to reconsider the design of the form or change the labeling of each input field.</p>
<p><a href="https://www.w3.org/WAI/tutorials/forms/labels/" target="_blank">Learn more about accessibility and labeling.</a></p>
<h2>7. Positive tabindex value</h2>
<p>The <code>tabindex</code> attribute is a way to control which elements on a web page receive keyboard focus. In the past, developers would use different values of <code>tabindex</code> to try to control the focus order through a page. However, this approach is very brittle and difficult to maintain, and today, the only values of the <code>tabindex</code> attribute that should be used are either <code>0</code> (see item #2 in this list for more on that topic) or, in specific situations where focus is managed using scripting, <code>-1</code>. To optimize keyboard usability, don’t try to control focus order using positive <code>tabindex</code> values.</p>
<p><a href="https://webaim.org/techniques/keyboard/tabindex" target="_blank">Learn more about using the tabindex attribute.</a></p>
<h2>8. Invalid aria-describedby</h2>
<p>Sometimes, a control may need a description as well as a label, when users might need to know more details about the state or purpose of the control. The <code>aria-describedby</code> attribute can be used to provide a connection between a control and some descriptive text elsewhere on the page. But the ID value defined by <code>aria-describedby</code> must match an element on the page with the same ID, otherwise the connection between those elements will not be conveyed by assistive technologies.</p>
<p><a href="https://benmyers.dev/blog/aria-labels-and-descriptions/" target="_blank">Learn more about using ARIA attributes for labels and descriptions.</a></p>
<h2>9. No label for button element</h2>
<p>Similar to links without link text (item #1), a button without an accessible label does not convey its purpose to assistive technology users. Unfortunately, many buttons are implemented in a way that relies on visual icons, which means the user has to be able to see that icon in order to understand the button’s purpose. To avoid this problem, provide each control with an accessible label that clearly communicates its purpose to assistive technologies.</p>
<p><a href="https://www.tpgi.com/labeling-the-point-scenarios-of-label-misuse-in-wcag/" target="_blank">Learn more about label use in WCAG.</a></p>
<h2>10. Invalid aria-labelledby</h2>
<p>Very similar to item #8 on this list, in specific situations a control may be provided with an accessible label by connecting the control to some text elsewhere on a page. The <code>aria-labelledby</code> attribute can provide this connection, but it needs to correctly reference the text element’s ID, otherwise the connection between the control and its label is lost to assistive technology users.</p>
<p><a href="https://www.w3.org/TR/html-aria/" target="_blank">Learn more about using ARIA in HTML correctly.</a></p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </itunes:summary>
    <description>
      <![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月10日，开源日报第1070期：<br>
今日推荐开源项目：《excalidraw》<br>
今日推荐英文原文：《The Top Accessibility Errors Found in 2023》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240110.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《excalidraw》传送门：<a href="https://github.com/excalidraw/excalidraw" target="_blank">项目链接</a> </p>
<p>推荐理由：用于绘制手绘类图表的虚拟白板：excalidraw可以使用excalidraw构建创建手绘图、线框或任何你想绘制的东西，excalidraw具有以下功能：</p>
<ul>
<li>📡 PWA支持（离线工作）</li>
<li>🤼 实时协作</li>
<li>🔒 端到端加密</li>
<li>💾 本地优先支持（自动保存到浏览器）</li>
<li>🔗 可共享链接（导出为只读链接，您可以与其他人共享）</li>
</ul>
<p>直达链接 excalidraw.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://www.tpgi.com/the-top-accessibility-errors-found-in-2023/" target="_blank">The Top Accessibility Errors Found in 2023</a></p>
<p>推荐理由： 2023年发现的最常见无障碍错误总结：无按钮元素标签：按钮缺少可访问标签，使辅助技术用户无法理解按钮用途，无效aria-labelledby：aria-labelledby未正确引用文本元素的ID，使辅助技术无法连接控件和标签等等</p>
<hr>
<h1>The Top Accessibility Errors Found in 2023</h1>
<p>TPGi’s accessibility experts have been busy this year, identifying thousands of accessibility errors through manual and automated testing. From all those results, the following 10 issues were the most commonly found by our ARC automation tools. We hope this list helps guide your team to avoid some of this year’s most common barriers. Cheers to another year of making things accessible!</p>
<h2>1. No link text</h2>
<p>The <code>&lt;a&gt;</code> element is HTML’s way of specifying links to other web pages, or locations within the same webpage, providing the foundation for the web’s purpose as a connected set of pages. But developers sometimes use the <code>&lt;a&gt;</code> element to provide other interactive functionality, and often this is done in a way that omits any link text. The result is that assistive technologies such as screen readers have no way to communicate the purpose of the link, leaving users unsure what will happen when they follow the link. That’s why every link must communicate its purpose in an accessible way.</p>
<p><a href="https://wcag.com/blog/writing-meaningful-link-text/" target="_blank">Learn more about writing meaningful link text.</a></p>
<h2>2. Non-active element in tab order</h2>
<p>This error occurs when a non-interactive element has been placed in the tab order using <code>tabindex="0"</code>, which means it becomes focusable using the keyboard when it doesn’t need to be, reducing efficiency and increasing effort for keyboard users. Generally, only elements that are interactive, such as links, buttons and form input fields, should be included in focus order. Screen reader users have access to a wide range of additional keyboard commands to allow quick navigation through content, so there’s no need to include static text and images in the focus order. <strong>Edit:</strong> In fact, all users who rely on keyboard navigation are negatively affected by this common error.</p>
<p><a href="https://webaim.org/techniques/keyboard/" target="_blank">Learn more about keyboard interaction.</a></p>
<h2>3. Missing link alt attribute</h2>
<p>Closely related to item #1 in our list, often links are labeled with an icon or other graphic that visually indicates the purpose of the link. A common error is to provide this graphic without any text alternative. When an image link has no alternative text, assistive technology users will be unaware of the link’s purpose. For that reason, it’s essential to provide alternative text for image links, that describes the purpose of the link.</p>
<p><a href="https://webaim.org/techniques/alttext/" target="_blank">Learn more about all things ‘alt’.</a></p>
<h2>4. No alt text</h2>
<p>Related to item #3, an image’s <a href="https://webaim.org/techniques/alttext/" target="_blank">alternative text</a>, provides a text description of the image content. When an <code>&lt;img&gt;</code> element lacks a text alternative, screen reader users have no way of knowing what the image is for. Providing a text alternative that communicates the same information as the image is one of the most fundamental accessibility requirements; but exactly what the alternative text should be would depend on the image’s purpose.</p>
<p><a href="https://abilitynet.org.uk/news-blogs/five-golden-rules-compliant-alt-text" target="_blank">Learn about the five golden rules of compliant alt text.</a></p>
<h2>5. List not nested correctly</h2>
<p>Using markup to identify lists helps communicate the structure of page content in an accessible way. When a list item has a nested list, it’s important to use markup in the correct way to make the nesting relationships clear. This helps avoid confusion when the list structure is described by screen readers.</p>
<p><a href="https://www.w3.org/WAI/tutorials/page-structure/content/" target="_blank">Learn more about HTML lists as a way to structure content.</a></p>
<h2>6. Duplicate labels used</h2>
<p>Another accessibility issue commonly detected by automated tools is form fields that have multiple identical labels. Having more than one label with exactly the same text can make it harder for some users to understand the form’s structure, and decide what content to enter in a particular field, increasing the chances of input errors. If your form has multiple labels with the same text, that’s a sign you might want to reconsider the design of the form or change the labeling of each input field.</p>
<p><a href="https://www.w3.org/WAI/tutorials/forms/labels/" target="_blank">Learn more about accessibility and labeling.</a></p>
<h2>7. Positive tabindex value</h2>
<p>The <code>tabindex</code> attribute is a way to control which elements on a web page receive keyboard focus. In the past, developers would use different values of <code>tabindex</code> to try to control the focus order through a page. However, this approach is very brittle and difficult to maintain, and today, the only values of the <code>tabindex</code> attribute that should be used are either <code>0</code> (see item #2 in this list for more on that topic) or, in specific situations where focus is managed using scripting, <code>-1</code>. To optimize keyboard usability, don’t try to control focus order using positive <code>tabindex</code> values.</p>
<p><a href="https://webaim.org/techniques/keyboard/tabindex" target="_blank">Learn more about using the tabindex attribute.</a></p>
<h2>8. Invalid aria-describedby</h2>
<p>Sometimes, a control may need a description as well as a label, when users might need to know more details about the state or purpose of the control. The <code>aria-describedby</code> attribute can be used to provide a connection between a control and some descriptive text elsewhere on the page. But the ID value defined by <code>aria-describedby</code> must match an element on the page with the same ID, otherwise the connection between those elements will not be conveyed by assistive technologies.</p>
<p><a href="https://benmyers.dev/blog/aria-labels-and-descriptions/" target="_blank">Learn more about using ARIA attributes for labels and descriptions.</a></p>
<h2>9. No label for button element</h2>
<p>Similar to links without link text (item #1), a button without an accessible label does not convey its purpose to assistive technology users. Unfortunately, many buttons are implemented in a way that relies on visual icons, which means the user has to be able to see that icon in order to understand the button’s purpose. To avoid this problem, provide each control with an accessible label that clearly communicates its purpose to assistive technologies.</p>
<p><a href="https://www.tpgi.com/labeling-the-point-scenarios-of-label-misuse-in-wcag/" target="_blank">Learn more about label use in WCAG.</a></p>
<h2>10. Invalid aria-labelledby</h2>
<p>Very similar to item #8 on this list, in specific situations a control may be provided with an accessible label by connecting the control to some text elsewhere on a page. The <code>aria-labelledby</code> attribute can provide this connection, but it needs to correctly reference the text element’s ID, otherwise the connection between the control and its label is lost to assistive technology users.</p>
<p><a href="https://www.w3.org/TR/html-aria/" target="_blank">Learn more about using ARIA in HTML correctly.</a></p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]>
    </description>
    <content:encoded><![CDATA[<p><a href="https://openingsource.org/daily/">开源日报</a>每天推荐一个 GitHub 优质开源项目和一篇精选英文科技或编程文章原文，坚持阅读《开源日报》，保持每日学习的好习惯。</p>
<p>2024年1月10日，开源日报第1070期：<br>
今日推荐开源项目：《excalidraw》<br>
今日推荐英文原文：《The Top Accessibility Errors Found in 2023》</p>
<p><img src="https://openingsource.org/wp-content/uploads/2024/01/osd-20240110.jpg"></p>
<hr>
<h3>开源项目</h3>
<p>今日推荐开源项目：《excalidraw》传送门：<a href="https://github.com/excalidraw/excalidraw" target="_blank">项目链接</a> </p>
<p>推荐理由：用于绘制手绘类图表的虚拟白板：excalidraw可以使用excalidraw构建创建手绘图、线框或任何你想绘制的东西，excalidraw具有以下功能：</p>
<ul>
<li>📡 PWA支持（离线工作）</li>
<li>🤼 实时协作</li>
<li>🔒 端到端加密</li>
<li>💾 本地优先支持（自动保存到浏览器）</li>
<li>🔗 可共享链接（导出为只读链接，您可以与其他人共享）</li>
</ul>
<p>直达链接 excalidraw.com</p>
<hr>
<h3>英文原文</h3>
<p>今日推荐英文原文：<a href="https://www.tpgi.com/the-top-accessibility-errors-found-in-2023/" target="_blank">The Top Accessibility Errors Found in 2023</a></p>
<p>推荐理由： 2023年发现的最常见无障碍错误总结：无按钮元素标签：按钮缺少可访问标签，使辅助技术用户无法理解按钮用途，无效aria-labelledby：aria-labelledby未正确引用文本元素的ID，使辅助技术无法连接控件和标签等等</p>
<hr>
<h1>The Top Accessibility Errors Found in 2023</h1>
<p>TPGi’s accessibility experts have been busy this year, identifying thousands of accessibility errors through manual and automated testing. From all those results, the following 10 issues were the most commonly found by our ARC automation tools. We hope this list helps guide your team to avoid some of this year’s most common barriers. Cheers to another year of making things accessible!</p>
<h2>1. No link text</h2>
<p>The <code>&lt;a&gt;</code> element is HTML’s way of specifying links to other web pages, or locations within the same webpage, providing the foundation for the web’s purpose as a connected set of pages. But developers sometimes use the <code>&lt;a&gt;</code> element to provide other interactive functionality, and often this is done in a way that omits any link text. The result is that assistive technologies such as screen readers have no way to communicate the purpose of the link, leaving users unsure what will happen when they follow the link. That’s why every link must communicate its purpose in an accessible way.</p>
<p><a href="https://wcag.com/blog/writing-meaningful-link-text/" target="_blank">Learn more about writing meaningful link text.</a></p>
<h2>2. Non-active element in tab order</h2>
<p>This error occurs when a non-interactive element has been placed in the tab order using <code>tabindex="0"</code>, which means it becomes focusable using the keyboard when it doesn’t need to be, reducing efficiency and increasing effort for keyboard users. Generally, only elements that are interactive, such as links, buttons and form input fields, should be included in focus order. Screen reader users have access to a wide range of additional keyboard commands to allow quick navigation through content, so there’s no need to include static text and images in the focus order. <strong>Edit:</strong> In fact, all users who rely on keyboard navigation are negatively affected by this common error.</p>
<p><a href="https://webaim.org/techniques/keyboard/" target="_blank">Learn more about keyboard interaction.</a></p>
<h2>3. Missing link alt attribute</h2>
<p>Closely related to item #1 in our list, often links are labeled with an icon or other graphic that visually indicates the purpose of the link. A common error is to provide this graphic without any text alternative. When an image link has no alternative text, assistive technology users will be unaware of the link’s purpose. For that reason, it’s essential to provide alternative text for image links, that describes the purpose of the link.</p>
<p><a href="https://webaim.org/techniques/alttext/" target="_blank">Learn more about all things ‘alt’.</a></p>
<h2>4. No alt text</h2>
<p>Related to item #3, an image’s <a href="https://webaim.org/techniques/alttext/" target="_blank">alternative text</a>, provides a text description of the image content. When an <code>&lt;img&gt;</code> element lacks a text alternative, screen reader users have no way of knowing what the image is for. Providing a text alternative that communicates the same information as the image is one of the most fundamental accessibility requirements; but exactly what the alternative text should be would depend on the image’s purpose.</p>
<p><a href="https://abilitynet.org.uk/news-blogs/five-golden-rules-compliant-alt-text" target="_blank">Learn about the five golden rules of compliant alt text.</a></p>
<h2>5. List not nested correctly</h2>
<p>Using markup to identify lists helps communicate the structure of page content in an accessible way. When a list item has a nested list, it’s important to use markup in the correct way to make the nesting relationships clear. This helps avoid confusion when the list structure is described by screen readers.</p>
<p><a href="https://www.w3.org/WAI/tutorials/page-structure/content/" target="_blank">Learn more about HTML lists as a way to structure content.</a></p>
<h2>6. Duplicate labels used</h2>
<p>Another accessibility issue commonly detected by automated tools is form fields that have multiple identical labels. Having more than one label with exactly the same text can make it harder for some users to understand the form’s structure, and decide what content to enter in a particular field, increasing the chances of input errors. If your form has multiple labels with the same text, that’s a sign you might want to reconsider the design of the form or change the labeling of each input field.</p>
<p><a href="https://www.w3.org/WAI/tutorials/forms/labels/" target="_blank">Learn more about accessibility and labeling.</a></p>
<h2>7. Positive tabindex value</h2>
<p>The <code>tabindex</code> attribute is a way to control which elements on a web page receive keyboard focus. In the past, developers would use different values of <code>tabindex</code> to try to control the focus order through a page. However, this approach is very brittle and difficult to maintain, and today, the only values of the <code>tabindex</code> attribute that should be used are either <code>0</code> (see item #2 in this list for more on that topic) or, in specific situations where focus is managed using scripting, <code>-1</code>. To optimize keyboard usability, don’t try to control focus order using positive <code>tabindex</code> values.</p>
<p><a href="https://webaim.org/techniques/keyboard/tabindex" target="_blank">Learn more about using the tabindex attribute.</a></p>
<h2>8. Invalid aria-describedby</h2>
<p>Sometimes, a control may need a description as well as a label, when users might need to know more details about the state or purpose of the control. The <code>aria-describedby</code> attribute can be used to provide a connection between a control and some descriptive text elsewhere on the page. But the ID value defined by <code>aria-describedby</code> must match an element on the page with the same ID, otherwise the connection between those elements will not be conveyed by assistive technologies.</p>
<p><a href="https://benmyers.dev/blog/aria-labels-and-descriptions/" target="_blank">Learn more about using ARIA attributes for labels and descriptions.</a></p>
<h2>9. No label for button element</h2>
<p>Similar to links without link text (item #1), a button without an accessible label does not convey its purpose to assistive technology users. Unfortunately, many buttons are implemented in a way that relies on visual icons, which means the user has to be able to see that icon in order to understand the button’s purpose. To avoid this problem, provide each control with an accessible label that clearly communicates its purpose to assistive technologies.</p>
<p><a href="https://www.tpgi.com/labeling-the-point-scenarios-of-label-misuse-in-wcag/" target="_blank">Learn more about label use in WCAG.</a></p>
<h2>10. Invalid aria-labelledby</h2>
<p>Very similar to item #8 on this list, in specific situations a control may be provided with an accessible label by connecting the control to some text elsewhere on a page. The <code>aria-labelledby</code> attribute can provide this connection, but it needs to correctly reference the text element’s ID, otherwise the connection between the control and its label is lost to assistive technology users.</p>
<p><a href="https://www.w3.org/TR/html-aria/" target="_blank">Learn more about using ARIA in HTML correctly.</a></p>
<hr>
<p>下载开源日报APP：<a href="https://openingsource.org/2579/"></a><a href="https://openingsource.org/2579/">https://openingsource.org/2579/</a><br>
加入我们：<a href="https://openingsource.org/about/join/"></a><a href="https://openingsource.org/about/join/">https://openingsource.org/about/join/</a><br>
关注我们：<a href="https://openingsource.org/about/love/"></a><a href="https://openingsource.org/about/love/">https://openingsource.org/about/love/</a></p>]]></content:encoded>
    <itunes:image href="https://openingsource.org/wp-content/uploads/2024/01/osd-20240110.jpg"/>
          <media:content url="https://openingsource.org/wp-content/uploads/2024/01/osd-20240110.jpg" medium="image"/>
    <pubDate>Wed, 10 Jan 2024 03:42:56 +0000</pubDate>
  </item></channel>
</rss>